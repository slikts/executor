import {
  CauseTypeId,
  CloseableScopeTypeId,
  CooperativeYielding,
  DeferredTypeId,
  DifferTypeId,
  EffectTypeId as EffectTypeId2,
  ExceededCapacityException,
  ExceededCapacityExceptionTypeId,
  IllegalArgumentException,
  IllegalArgumentExceptionTypeId,
  InterruptedException,
  InterruptedExceptionTypeId,
  Interruption,
  InvalidPubSubCapacityExceptionTypeId,
  NoSuchElementException,
  NoSuchElementExceptionTypeId,
  OpSupervision,
  RevertFlags,
  RuntimeException,
  RuntimeExceptionTypeId,
  RuntimeMetrics,
  ScopeTypeId,
  TimeoutException,
  TimeoutExceptionTypeId,
  UnknownException,
  UnknownExceptionTypeId,
  WindDown,
  YieldableError,
  allLogLevels,
  andThen,
  as,
  as2,
  asVoid,
  asyncInterrupt,
  async_,
  blocked,
  capture,
  catchAll,
  catchAllCause,
  catchIf,
  causeSquash,
  causeSquashWith,
  chunk,
  combine as combine2,
  compareAndSet,
  cons,
  contains as contains2,
  context,
  contextWithEffect,
  cooperativeYielding,
  currentConcurrency,
  currentContext,
  currentForkScopeOverride,
  currentInterruptedCause,
  currentLogAnnotations,
  currentLogLevel,
  currentLogSpan,
  currentMaxOpsBeforeYield,
  currentMetricLabels,
  currentRequestBatching,
  currentSchedulingPriority,
  currentSpanFromFiber,
  currentTracerEnabled,
  currentTracerSpanAnnotations,
  currentTracerSpanLinks,
  currentTracerTimingEnabled,
  currentUnhandledErrorLogLevel,
  currentVersionMismatchErrorLogLevel,
  decode,
  defects,
  deferredAwait,
  deferredComplete,
  deferredCompleteWith,
  deferredDie,
  deferredDieSync,
  deferredDone,
  deferredFail,
  deferredFailCause,
  deferredFailCauseSync,
  deferredFailSync,
  deferredInterrupt,
  deferredInterruptWith,
  deferredIsDone,
  deferredMake,
  deferredMakeAs,
  deferredPoll,
  deferredSucceed,
  deferredSync,
  deferredUnsafeDone,
  deferredUnsafeMake,
  die,
  die2,
  dieMessage,
  dieOption,
  dieSync,
  diff,
  difference,
  disable2 as disable,
  either,
  empty as empty4,
  empty2 as empty5,
  empty3 as empty6,
  empty4 as empty7,
  empty5 as empty8,
  empty6 as empty9,
  enable2 as enable,
  environment,
  exclude,
  exit,
  exitAs,
  exitAsVoid,
  exitCauseOption,
  exitCollectAll,
  exitDie,
  exitExists,
  exitFail,
  exitFailCause,
  exitFlatMap,
  exitFlatMapEffect,
  exitFlatten,
  exitForEachEffect,
  exitFromEither,
  exitFromOption,
  exitGetOrElse,
  exitInterrupt,
  exitIsExit,
  exitIsFailure,
  exitIsInterrupted,
  exitIsSuccess,
  exitMap,
  exitMapBoth,
  exitMapError,
  exitMapErrorCause,
  exitMatch,
  exitMatchEffect,
  exitSucceed,
  exitVoid,
  exitZip,
  exitZipLeft,
  exitZipPar,
  exitZipParLeft,
  exitZipParRight,
  exitZipRight,
  exitZipWith,
  fail,
  fail2,
  failCause,
  failCauseSync,
  failSync,
  failureOption,
  failureOrCause,
  failures,
  fiberId,
  fiberIdWith,
  fiberRefDelete,
  fiberRefGet,
  fiberRefGetWith,
  fiberRefLocally,
  fiberRefLocallyWith,
  fiberRefSet,
  fiberRefUnsafeMake,
  fiberRefUnsafeMakeContext,
  fiberRefUnsafeMakeHashSet,
  fiberRefUnsafeMakePatch,
  fiberRefUnsafeMakeRuntimeFlags,
  fiberRefUpdate,
  filter,
  find,
  flatMap as flatMap3,
  flatMap2 as flatMap4,
  flatten as flatten2,
  flatten2 as flatten3,
  flatten3 as flatten4,
  flip,
  flipCauseOption,
  fnUntraced,
  forEach2 as forEach,
  forEachSequential,
  forEachSequentialDiscard,
  fromIterable as fromIterable3,
  fromIterable2 as fromIterable4,
  gen,
  get as get2,
  getOrElse as getOrElse2,
  greaterThanOrEqualTo as greaterThanOrEqualTo2,
  hashMap,
  hashSet,
  ids,
  if_,
  interrupt,
  interrupt2,
  interruptAsFiber,
  interruptFiber,
  interruptOption,
  interruptible,
  interruptible2,
  interruptors,
  intoDeferred,
  isCause,
  isCons,
  isDie,
  isDieType,
  isEffect,
  isEmpty2,
  isEmptyType,
  isExceededCapacityException,
  isFailType,
  isFailure,
  isIllegalArgumentException,
  isInterruptType,
  isInterrupted,
  isInterruptedException,
  isInterruptedOnly,
  isNoSuchElementException,
  isParallelType,
  isRuntimeException,
  isSequentialType,
  isTimeoutException,
  isUnknownException,
  keepDefects,
  lessThanOrEqualTo as lessThanOrEqualTo2,
  linearize,
  logLevelAll,
  logLevelDebug,
  logLevelError,
  logLevelFatal,
  logLevelInfo,
  logLevelNone,
  logLevelTrace,
  logLevelWarning,
  make as make3,
  make2 as make4,
  make3 as make5,
  make6,
  make7,
  makeEntry,
  map as map3,
  map2 as map4,
  mapBoth,
  mapError,
  mapInputContext,
  match as match3,
  matchCause,
  matchCauseEffect,
  matchEffect,
  millis,
  mutate,
  nanos,
  never,
  none as none3,
  none2 as none4,
  none3 as none5,
  noopSpan,
  of as of3,
  onError,
  onExit,
  onInterrupt,
  orElse,
  orElseEither,
  originalInstance,
  par,
  parallel,
  partitionMap,
  patch,
  prepend as prepend3,
  pretty,
  prettyErrors,
  provideContext,
  provideSomeContext,
  readonlyArray,
  reduce as reduce3,
  reduce3 as reduce4,
  reduceWithContext,
  runRequestBlock,
  runtimeFlags,
  runtimeMetrics,
  scopeAddFinalizer,
  scopeAddFinalizerExit,
  scopeClose,
  scopeFork,
  seconds,
  sequential,
  sequentialCollectionToChunk,
  set,
  set2,
  single,
  size,
  size2,
  size3,
  spanToTrace,
  step,
  stripFailures,
  stripSomeDefects,
  struct,
  succeed,
  suspend,
  sync,
  tap,
  threadName,
  threadName2,
  timeoutExceptionFromDuration,
  toMillis,
  transform,
  transplant,
  uninterruptible,
  uninterruptibleMask,
  union as union2,
  unsafeAsync,
  unsafeMake,
  update2 as update,
  updateRuntimeFlags,
  updateWith,
  void_,
  whenEffect,
  whileLoop,
  withFiberRuntime,
  withRuntimeFlags,
  yieldNow,
  zero,
  zip as zip2,
  zip2 as zip3,
  zipLeft,
  zipRight,
  zipWith as zipWith2
} from "./chunk-7GS5HKXM.js";
import {
  allocate,
  append,
  append2,
  appendAll,
  dedupe,
  empty3 as empty,
  empty4 as empty2,
  ensure,
  filterMap,
  flatMap as flatMap2,
  flatten,
  fromIterable,
  fromIterable2,
  getEquivalence3 as getEquivalence,
  getSomes,
  head2 as head,
  head3 as head2,
  headNonEmpty,
  headNonEmpty2,
  isArray,
  isEmpty,
  isEmptyReadonlyArray,
  isLeft,
  isNonEmpty,
  isNonEmptyArray2 as isNonEmptyArray,
  isNonEmptyReadonlyArray,
  isRight,
  join,
  last,
  lastNonEmpty,
  left,
  make2 as make,
  makeBy,
  map4 as map2,
  match2,
  merge,
  of,
  of2,
  prepend,
  prepend2,
  reduce,
  reduce2,
  reduceRight,
  reverse2 as reverse,
  right,
  sort,
  span,
  tailNonEmpty,
  tailNonEmpty2,
  toReadonlyArray,
  unfold,
  union,
  unsafeFromArray,
  unsafeGet,
  zip
} from "./chunk-JDLTRRA5.js";
import {
  GenericTag,
  Reference,
  add,
  empty as empty3,
  get,
  getOption,
  isContext,
  isTag,
  make as make2,
  merge as merge2,
  mergeAll,
  unsafeGet as unsafeGet2,
  unsafeGetReference
} from "./chunk-RG46YT3R.js";
import {
  all,
  between,
  bind,
  bindTo,
  boolean,
  boolean2,
  clamp,
  contains,
  flatMap,
  fromNullable,
  getOrElse,
  getOrUndefined,
  greaterThan,
  greaterThanOrEqualTo,
  isNone,
  isSome,
  lessThan,
  lessThanOrEqualTo,
  let_,
  map,
  mapInput2 as mapInput,
  match,
  max,
  min,
  none as none2,
  number,
  number2,
  some as some2,
  tuple2 as tuple,
  zipWith
} from "./chunk-5XJTUU64.js";
import {
  Base,
  ChannelTypeId,
  CommitPrototype,
  EffectPrototype,
  EffectTypeId,
  NodeInspectSymbol,
  OP_ASYNC,
  OP_COMMIT,
  OP_FAILURE,
  OP_ITERATOR,
  OP_ON_FAILURE,
  OP_ON_SUCCESS,
  OP_ON_SUCCESS_AND_FAILURE,
  OP_REVERT_FLAGS,
  OP_SUCCESS,
  OP_SYNC,
  OP_TAG,
  OP_UPDATE_RUNTIME_FLAGS,
  OP_WHILE,
  OP_WITH_RUNTIME,
  OP_YIELD,
  PCGRandom,
  SingleShotGen,
  SinkTypeId,
  StreamTypeId,
  StructuralBase,
  StructuralCommitPrototype,
  StructuralPrototype,
  YieldWrap,
  array,
  cached,
  combine,
  equals,
  format,
  getBugErrorMessage,
  getCurrentVersion,
  globalValue,
  hasProperty,
  hash,
  internalCall,
  isBoolean,
  isEqual,
  isFunction,
  isIterable,
  isNotNullable,
  isNumber,
  isRegExp,
  isString,
  isTagged,
  none,
  pipeArguments,
  redact,
  some,
  string,
  stringifyCircular,
  symbol,
  symbol2,
  toJSON,
  toStringUnknown,
  withRedactableContext,
  yieldWrapGet
} from "./chunk-SPGJQP32.js";
import {
  constFalse,
  constTrue,
  constVoid,
  dual,
  identity,
  pipe
} from "./chunk-LX4DLJUA.js";
import {
  __export
} from "./chunk-G3PMV62Z.js";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/clock.js
var ClockSymbolKey = "effect/Clock";
var ClockTypeId = Symbol.for(ClockSymbolKey);
var clockTag = GenericTag("effect/Clock");
var MAX_TIMER_MILLIS = 2 ** 31 - 1;
var globalClockScheduler = {
  unsafeSchedule(task, duration) {
    const millis2 = toMillis(duration);
    if (millis2 > MAX_TIMER_MILLIS) {
      return constFalse;
    }
    let completed = false;
    const handle = setTimeout(() => {
      completed = true;
      task();
    }, millis2);
    return () => {
      clearTimeout(handle);
      return !completed;
    };
  }
};
var performanceNowNanos = (function() {
  const bigint1e6 = BigInt(1e6);
  if (typeof performance === "undefined" || typeof performance.now !== "function") {
    return () => BigInt(Date.now()) * bigint1e6;
  }
  let origin;
  return () => {
    if (origin === void 0) {
      origin = BigInt(Date.now()) * bigint1e6 - BigInt(Math.round(performance.now() * 1e6));
    }
    return origin + BigInt(Math.round(performance.now() * 1e6));
  };
})();
var processOrPerformanceNow = (function() {
  const processHrtime = typeof process === "object" && "hrtime" in process && typeof process.hrtime.bigint === "function" ? process.hrtime : void 0;
  if (!processHrtime) {
    return performanceNowNanos;
  }
  const origin = performanceNowNanos() - processHrtime.bigint();
  return () => origin + processHrtime.bigint();
})();
var ClockImpl = class {
  [ClockTypeId] = ClockTypeId;
  unsafeCurrentTimeMillis() {
    return Date.now();
  }
  unsafeCurrentTimeNanos() {
    return processOrPerformanceNow();
  }
  currentTimeMillis = sync(() => this.unsafeCurrentTimeMillis());
  currentTimeNanos = sync(() => this.unsafeCurrentTimeNanos());
  scheduler() {
    return succeed(globalClockScheduler);
  }
  sleep(duration) {
    return async_((resume2) => {
      const canceler = globalClockScheduler.unsafeSchedule(() => resume2(void_), duration);
      return asVoid(sync(canceler));
    });
  }
};
var make8 = () => new ClockImpl();

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Number.js
var Number_exports = {};
__export(Number_exports, {
  Equivalence: () => Equivalence,
  Order: () => Order,
  between: () => between2,
  clamp: () => clamp2,
  decrement: () => decrement,
  divide: () => divide,
  greaterThan: () => greaterThan2,
  greaterThanOrEqualTo: () => greaterThanOrEqualTo3,
  increment: () => increment,
  isNumber: () => isNumber2,
  lessThan: () => lessThan2,
  lessThanOrEqualTo: () => lessThanOrEqualTo3,
  max: () => max2,
  min: () => min2,
  multiply: () => multiply,
  multiplyAll: () => multiplyAll,
  negate: () => negate,
  nextPow2: () => nextPow2,
  parse: () => parse,
  remainder: () => remainder,
  round: () => round,
  sign: () => sign,
  subtract: () => subtract,
  sum: () => sum,
  sumAll: () => sumAll,
  unsafeDivide: () => unsafeDivide
});
var isNumber2 = isNumber;
var negate = (n) => multiply(n, -1);
var sum = dual(2, (self, that) => self + that);
var sumAll = (collection) => reduce(collection, 0, sum);
var subtract = dual(2, (minuend, subtrahend) => minuend - subtrahend);
var multiply = dual(2, (multiplier, multiplicand) => multiplier * multiplicand);
var multiplyAll = (collection) => {
  let out = 1;
  for (const n of collection) {
    if (n === 0) {
      return 0;
    }
    out *= n;
  }
  return out;
};
var divide = dual(2, (dividend, divisor) => divisor === 0 ? none : some(dividend / divisor));
var unsafeDivide = dual(2, (dividend, divisor) => dividend / divisor);
var increment = (n) => sum(n, 1);
var decrement = (n) => subtract(n, 1);
var Equivalence = number;
var Order = number2;
var lessThan2 = lessThan(Order);
var lessThanOrEqualTo3 = lessThanOrEqualTo(Order);
var greaterThan2 = greaterThan(Order);
var greaterThanOrEqualTo3 = greaterThanOrEqualTo(Order);
var between2 = between(Order);
var clamp2 = clamp(Order);
var min2 = min(Order);
var max2 = max(Order);
var sign = (n) => Order(n, 0);
var remainder = dual(2, (dividend, divisor) => {
  const selfDecCount = (dividend.toString().split(".")[1] || "").length;
  const divisorDecCount = (divisor.toString().split(".")[1] || "").length;
  const decCount = selfDecCount > divisorDecCount ? selfDecCount : divisorDecCount;
  const selfInt = parseInt(dividend.toFixed(decCount).replace(".", ""));
  const divisorInt = parseInt(divisor.toFixed(decCount).replace(".", ""));
  return selfInt % divisorInt / Math.pow(10, decCount);
});
var nextPow2 = (n) => {
  const nextPow = Math.ceil(Math.log(n) / Math.log(2));
  return Math.max(Math.pow(2, nextPow), 2);
};
var parse = (s) => {
  if (s === "NaN") {
    return some(NaN);
  }
  if (s === "Infinity") {
    return some(Infinity);
  }
  if (s === "-Infinity") {
    return some(-Infinity);
  }
  if (s.trim() === "") {
    return none;
  }
  const n = Number(s);
  return Number.isNaN(n) ? none : some(n);
};
var round = dual(2, (self, precision) => {
  const factor = Math.pow(10, precision);
  return Math.round(self * factor) / factor;
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RegExp.js
var RegExp_exports = {};
__export(RegExp_exports, {
  escape: () => escape,
  isRegExp: () => isRegExp2
});
var isRegExp2 = isRegExp;
var escape = (string2) => string2.replace(/[/\\^$*+?.()|[\]{}]/g, "\\$&");

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/random.js
var RandomSymbolKey = "effect/Random";
var RandomTypeId = Symbol.for(RandomSymbolKey);
var randomTag = GenericTag("effect/Random");
var RandomImpl = class {
  seed;
  [RandomTypeId] = RandomTypeId;
  PRNG;
  constructor(seed) {
    this.seed = seed;
    this.PRNG = new PCGRandom(seed);
  }
  get next() {
    return sync(() => this.PRNG.number());
  }
  get nextBoolean() {
    return map4(this.next, (n) => n > 0.5);
  }
  get nextInt() {
    return sync(() => this.PRNG.integer(Number.MAX_SAFE_INTEGER));
  }
  nextRange(min5, max7) {
    return map4(this.next, (n) => (max7 - min5) * n + min5);
  }
  nextIntBetween(min5, max7) {
    return sync(() => this.PRNG.integer(max7 - min5) + min5);
  }
  shuffle(elements) {
    return shuffleWith(elements, (n) => this.nextIntBetween(0, n));
  }
};
var shuffleWith = (elements, nextIntBounded) => {
  return suspend(() => pipe(sync(() => Array.from(elements)), flatMap4((buffer) => {
    const numbers = [];
    for (let i = buffer.length; i >= 2; i = i - 1) {
      numbers.push(i);
    }
    return pipe(numbers, forEachSequentialDiscard((n) => pipe(nextIntBounded(n), map4((k) => swap(buffer, n - 1, k)))), as2(fromIterable2(buffer)));
  })));
};
var swap = (buffer, index1, index2) => {
  const tmp = buffer[index1];
  buffer[index1] = buffer[index2];
  buffer[index2] = tmp;
  return buffer;
};
var make9 = (seed) => new RandomImpl(hash(seed));
var FixedRandomImpl = class {
  values;
  [RandomTypeId] = RandomTypeId;
  index = 0;
  constructor(values5) {
    this.values = values5;
    if (values5.length === 0) {
      throw new Error("Requires at least one value");
    }
  }
  getNextValue() {
    const value2 = this.values[this.index];
    this.index = (this.index + 1) % this.values.length;
    return value2;
  }
  get next() {
    return sync(() => {
      const value2 = this.getNextValue();
      if (typeof value2 === "number") {
        return Math.max(0, Math.min(1, value2));
      }
      return hash(value2) / 2147483647;
    });
  }
  get nextBoolean() {
    return sync(() => {
      const value2 = this.getNextValue();
      if (typeof value2 === "boolean") {
        return value2;
      }
      return hash(value2) % 2 === 0;
    });
  }
  get nextInt() {
    return sync(() => {
      const value2 = this.getNextValue();
      if (typeof value2 === "number" && Number.isFinite(value2)) {
        return Math.round(value2);
      }
      return Math.abs(hash(value2));
    });
  }
  nextRange(min5, max7) {
    return map4(this.next, (n) => (max7 - min5) * n + min5);
  }
  nextIntBetween(min5, max7) {
    return sync(() => {
      const value2 = this.getNextValue();
      if (typeof value2 === "number" && Number.isFinite(value2)) {
        return Math.max(min5, Math.min(max7 - 1, Math.round(value2)));
      }
      const hash2 = Math.abs(hash(value2));
      return min5 + hash2 % (max7 - min5);
    });
  }
  shuffle(elements) {
    return shuffleWith(elements, (n) => this.nextIntBetween(0, n));
  }
};
var fixed = (values5) => new FixedRandomImpl(values5);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/tracer.js
var TracerTypeId = Symbol.for("effect/Tracer");
var make10 = (options) => ({
  [TracerTypeId]: TracerTypeId,
  ...options
});
var tracerTag = GenericTag("effect/Tracer");
var spanTag = GenericTag("effect/ParentSpan");
var randomHexString = (function() {
  const characters = "abcdef0123456789";
  const charactersLength = characters.length;
  return function(length3) {
    let result = "";
    for (let i = 0; i < length3; i++) {
      result += characters.charAt(Math.floor(Math.random() * charactersLength));
    }
    return result;
  };
})();
var NativeSpan = class {
  name;
  parent;
  context;
  startTime;
  kind;
  _tag = "Span";
  spanId;
  traceId = "native";
  sampled = true;
  status;
  attributes;
  events = [];
  links;
  constructor(name, parent, context4, links, startTime, kind) {
    this.name = name;
    this.parent = parent;
    this.context = context4;
    this.startTime = startTime;
    this.kind = kind;
    this.status = {
      _tag: "Started",
      startTime
    };
    this.attributes = /* @__PURE__ */ new Map();
    this.traceId = parent._tag === "Some" ? parent.value.traceId : randomHexString(32);
    this.spanId = randomHexString(16);
    this.links = Array.from(links);
  }
  end(endTime, exit3) {
    this.status = {
      _tag: "Ended",
      endTime,
      exit: exit3,
      startTime: this.status.startTime
    };
  }
  attribute(key, value2) {
    this.attributes.set(key, value2);
  }
  event(name, startTime, attributes) {
    this.events.push([name, startTime, attributes ?? {}]);
  }
  addLinks(links) {
    this.links.push(...links);
  }
};
var nativeTracer = make10({
  span: (name, parent, context4, links, startTime, kind) => new NativeSpan(name, parent, context4, links, startTime, kind),
  context: (f) => f()
});
var externalSpan = (options) => ({
  _tag: "ExternalSpan",
  spanId: options.spanId,
  traceId: options.traceId,
  sampled: options.sampled ?? true,
  context: options.context ?? empty3()
});
var addSpanStackTrace = (options) => {
  if (options?.captureStackTrace === false) {
    return options;
  } else if (options?.captureStackTrace !== void 0 && typeof options.captureStackTrace !== "boolean") {
    return options;
  }
  const limit = Error.stackTraceLimit;
  Error.stackTraceLimit = 3;
  const traceError = new Error();
  Error.stackTraceLimit = limit;
  let cache = false;
  return {
    ...options,
    captureStackTrace: () => {
      if (cache !== false) {
        return cache;
      }
      if (traceError.stack !== void 0) {
        const stack = traceError.stack.split("\n");
        if (stack[3] !== void 0) {
          cache = stack[3].trim();
          return cache;
        }
      }
    }
  };
};
var DisablePropagation = Reference()("effect/Tracer/DisablePropagation", {
  defaultValue: constFalse
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/configError.js
var OP_AND = "And";
var OP_OR = "Or";
var OP_INVALID_DATA = "InvalidData";
var OP_MISSING_DATA = "MissingData";
var OP_SOURCE_UNAVAILABLE = "SourceUnavailable";
var OP_UNSUPPORTED = "Unsupported";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/configError.js
var ConfigErrorSymbolKey = "effect/ConfigError";
var ConfigErrorTypeId = Symbol.for(ConfigErrorSymbolKey);
var proto = {
  _tag: "ConfigError",
  [ConfigErrorTypeId]: ConfigErrorTypeId
};
var And = (self, that) => {
  const error = Object.create(proto);
  error._op = OP_AND;
  error.left = self;
  error.right = that;
  Object.defineProperty(error, "toString", {
    enumerable: false,
    value() {
      return `${this.left} and ${this.right}`;
    }
  });
  Object.defineProperty(error, "message", {
    enumerable: false,
    get() {
      return this.toString();
    }
  });
  return error;
};
var Or = (self, that) => {
  const error = Object.create(proto);
  error._op = OP_OR;
  error.left = self;
  error.right = that;
  Object.defineProperty(error, "toString", {
    enumerable: false,
    value() {
      return `${this.left} or ${this.right}`;
    }
  });
  Object.defineProperty(error, "message", {
    enumerable: false,
    get() {
      return this.toString();
    }
  });
  return error;
};
var InvalidData = (path, message, options = {
  pathDelim: "."
}) => {
  const error = Object.create(proto);
  error._op = OP_INVALID_DATA;
  error.path = path;
  error.message = message;
  Object.defineProperty(error, "toString", {
    enumerable: false,
    value() {
      const path2 = pipe(this.path, join(options.pathDelim));
      return `(Invalid data at ${path2}: "${this.message}")`;
    }
  });
  return error;
};
var MissingData = (path, message, options = {
  pathDelim: "."
}) => {
  const error = Object.create(proto);
  error._op = OP_MISSING_DATA;
  error.path = path;
  error.message = message;
  Object.defineProperty(error, "toString", {
    enumerable: false,
    value() {
      const path2 = pipe(this.path, join(options.pathDelim));
      return `(Missing data at ${path2}: "${this.message}")`;
    }
  });
  return error;
};
var SourceUnavailable = (path, message, cause2, options = {
  pathDelim: "."
}) => {
  const error = Object.create(proto);
  error._op = OP_SOURCE_UNAVAILABLE;
  error.path = path;
  error.message = message;
  error.cause = cause2;
  Object.defineProperty(error, "toString", {
    enumerable: false,
    value() {
      const path2 = pipe(this.path, join(options.pathDelim));
      return `(Source unavailable at ${path2}: "${this.message}")`;
    }
  });
  return error;
};
var Unsupported = (path, message, options = {
  pathDelim: "."
}) => {
  const error = Object.create(proto);
  error._op = OP_UNSUPPORTED;
  error.path = path;
  error.message = message;
  Object.defineProperty(error, "toString", {
    enumerable: false,
    value() {
      const path2 = pipe(this.path, join(options.pathDelim));
      return `(Unsupported operation at ${path2}: "${this.message}")`;
    }
  });
  return error;
};
var isConfigError = (u) => hasProperty(u, ConfigErrorTypeId);
var isAnd = (self) => self._op === OP_AND;
var isOr = (self) => self._op === OP_OR;
var isInvalidData = (self) => self._op === OP_INVALID_DATA;
var isMissingData = (self) => self._op === OP_MISSING_DATA;
var isSourceUnavailable = (self) => self._op === OP_SOURCE_UNAVAILABLE;
var isUnsupported = (self) => self._op === OP_UNSUPPORTED;
var prefixed = dual(2, (self, prefix) => {
  switch (self._op) {
    case OP_AND: {
      return And(prefixed(self.left, prefix), prefixed(self.right, prefix));
    }
    case OP_OR: {
      return Or(prefixed(self.left, prefix), prefixed(self.right, prefix));
    }
    case OP_INVALID_DATA: {
      return InvalidData([...prefix, ...self.path], self.message);
    }
    case OP_MISSING_DATA: {
      return MissingData([...prefix, ...self.path], self.message);
    }
    case OP_SOURCE_UNAVAILABLE: {
      return SourceUnavailable([...prefix, ...self.path], self.message, self.cause);
    }
    case OP_UNSUPPORTED: {
      return Unsupported([...prefix, ...self.path], self.message);
    }
  }
});
var IsMissingDataOnlyReducer = {
  andCase: (_, left2, right2) => left2 && right2,
  orCase: (_, left2, right2) => left2 && right2,
  invalidDataCase: constFalse,
  missingDataCase: constTrue,
  sourceUnavailableCase: constFalse,
  unsupportedCase: constFalse
};
var reduceWithContext2 = dual(3, (self, context4, reducer) => {
  const input = [self];
  const output = [];
  while (input.length > 0) {
    const error = input.pop();
    switch (error._op) {
      case OP_AND: {
        input.push(error.right);
        input.push(error.left);
        output.push(left({
          _op: "AndCase"
        }));
        break;
      }
      case OP_OR: {
        input.push(error.right);
        input.push(error.left);
        output.push(left({
          _op: "OrCase"
        }));
        break;
      }
      case OP_INVALID_DATA: {
        output.push(right(reducer.invalidDataCase(context4, error.path, error.message)));
        break;
      }
      case OP_MISSING_DATA: {
        output.push(right(reducer.missingDataCase(context4, error.path, error.message)));
        break;
      }
      case OP_SOURCE_UNAVAILABLE: {
        output.push(right(reducer.sourceUnavailableCase(context4, error.path, error.message, error.cause)));
        break;
      }
      case OP_UNSUPPORTED: {
        output.push(right(reducer.unsupportedCase(context4, error.path, error.message)));
        break;
      }
    }
  }
  const accumulator = [];
  while (output.length > 0) {
    const either3 = output.pop();
    switch (either3._op) {
      case "Left": {
        switch (either3.left._op) {
          case "AndCase": {
            const left2 = accumulator.pop();
            const right2 = accumulator.pop();
            const value2 = reducer.andCase(context4, left2, right2);
            accumulator.push(value2);
            break;
          }
          case "OrCase": {
            const left2 = accumulator.pop();
            const right2 = accumulator.pop();
            const value2 = reducer.orCase(context4, left2, right2);
            accumulator.push(value2);
            break;
          }
        }
        break;
      }
      case "Right": {
        accumulator.push(either3.right);
        break;
      }
    }
  }
  if (accumulator.length === 0) {
    throw new Error("BUG: ConfigError.reduceWithContext - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  return accumulator.pop();
});
var isMissingDataOnly = (self) => reduceWithContext2(self, void 0, IsMissingDataOnlyReducer);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/configProvider/pathPatch.js
var empty10 = {
  _tag: "Empty"
};
var andThen2 = dual(2, (self, that) => ({
  _tag: "AndThen",
  first: self,
  second: that
}));
var mapName = dual(2, (self, f) => andThen2(self, {
  _tag: "MapName",
  f
}));
var nested = dual(2, (self, name) => andThen2(self, {
  _tag: "Nested",
  name
}));
var unnested = dual(2, (self, name) => andThen2(self, {
  _tag: "Unnested",
  name
}));
var patch2 = dual(2, (path, patch7) => {
  let input = of3(patch7);
  let output = path;
  while (isCons(input)) {
    const patch8 = input.head;
    switch (patch8._tag) {
      case "Empty": {
        input = input.tail;
        break;
      }
      case "AndThen": {
        input = cons(patch8.first, cons(patch8.second, input.tail));
        break;
      }
      case "MapName": {
        output = map2(output, patch8.f);
        input = input.tail;
        break;
      }
      case "Nested": {
        output = prepend(output, patch8.name);
        input = input.tail;
        break;
      }
      case "Unnested": {
        const containsName = pipe(head(output), contains(patch8.name));
        if (containsName) {
          output = tailNonEmpty(output);
          input = input.tail;
        } else {
          return left(MissingData(output, `Expected ${patch8.name} to be in path in ConfigProvider#unnested`));
        }
        break;
      }
    }
  }
  return right(output);
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/config.js
var OP_CONSTANT = "Constant";
var OP_FAIL = "Fail";
var OP_FALLBACK = "Fallback";
var OP_DESCRIBED = "Described";
var OP_LAZY = "Lazy";
var OP_MAP_OR_FAIL = "MapOrFail";
var OP_NESTED = "Nested";
var OP_PRIMITIVE = "Primitive";
var OP_SEQUENCE = "Sequence";
var OP_HASHMAP = "HashMap";
var OP_ZIP_WITH = "ZipWith";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/string-utils.js
var lowerCase = (str) => str.toLowerCase();
var upperCase = (str) => str.toUpperCase();
var replace = (input, re, value2) => re instanceof RegExp ? input.replace(re, value2) : re.reduce((input2, re2) => input2.replace(re2, value2), input);
var DEFAULT_SPLIT_REGEXP = [/([a-z0-9])([A-Z])/g, /([A-Z])([A-Z][a-z])/g];
var DEFAULT_STRIP_REGEXP = /[^A-Z0-9]+/gi;
var noCase = (input, options = {}) => {
  const {
    delimiter = " ",
    splitRegexp = DEFAULT_SPLIT_REGEXP,
    stripRegexp = DEFAULT_STRIP_REGEXP,
    transform: transform3 = lowerCase
  } = options;
  const result = replace(replace(input, splitRegexp, "$1\0$2"), stripRegexp, "\0");
  let start3 = 0;
  let end3 = result.length;
  while (result.charAt(start3) === "\0") {
    start3++;
  }
  while (result.charAt(end3 - 1) === "\0") {
    end3--;
  }
  return result.slice(start3, end3).split("\0").map(transform3).join(delimiter);
};
var constantCase = (input, options) => noCase(input, {
  delimiter: "_",
  transform: upperCase,
  ...options
});
var kebabCase = (input, options) => noCase(input, {
  delimiter: "-",
  ...options
});
var snakeCase = (input, options) => noCase(input, {
  delimiter: "_",
  ...options
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/configProvider.js
var concat = (l, r) => [...l, ...r];
var ConfigProviderSymbolKey = "effect/ConfigProvider";
var ConfigProviderTypeId = Symbol.for(ConfigProviderSymbolKey);
var configProviderTag = GenericTag("effect/ConfigProvider");
var FlatConfigProviderSymbolKey = "effect/ConfigProviderFlat";
var FlatConfigProviderTypeId = Symbol.for(FlatConfigProviderSymbolKey);
var make11 = (options) => ({
  [ConfigProviderTypeId]: ConfigProviderTypeId,
  pipe() {
    return pipeArguments(this, arguments);
  },
  ...options
});
var makeFlat = (options) => ({
  [FlatConfigProviderTypeId]: FlatConfigProviderTypeId,
  patch: options.patch,
  load: (path, config2, split = true) => options.load(path, config2, split),
  enumerateChildren: options.enumerateChildren
});
var fromFlat = (flat) => make11({
  load: (config2) => flatMap4(fromFlatLoop(flat, empty(), config2, false), (chunk3) => match(head(chunk3), {
    onNone: () => fail2(MissingData(empty(), `Expected a single value having structure: ${config2}`)),
    onSome: succeed
  })),
  flattened: flat
});
var fromEnv = (options) => {
  const {
    pathDelim,
    seqDelim
  } = Object.assign({}, {
    pathDelim: "_",
    seqDelim: ","
  }, options);
  const makePathString = (path) => pipe(path, join(pathDelim));
  const unmakePathString = (pathString) => pathString.split(pathDelim);
  const getEnv = () => typeof process !== "undefined" && "env" in process && typeof process.env === "object" ? process.env : {};
  const load = (path, primitive, split = true) => {
    const pathString = makePathString(path);
    const current = getEnv();
    const valueOpt = pathString in current ? some2(current[pathString]) : none2();
    return pipe(valueOpt, mapError(() => MissingData(path, `Expected ${pathString} to exist in the process context`)), flatMap4((value2) => parsePrimitive(value2, path, primitive, seqDelim, split)));
  };
  const enumerateChildren = (path) => sync(() => {
    const current = getEnv();
    const keys4 = Object.keys(current);
    const keyPaths = keys4.map((value2) => unmakePathString(value2.toUpperCase()));
    const filteredKeyPaths = keyPaths.filter((keyPath) => {
      for (let i = 0; i < path.length; i++) {
        const pathComponent = pipe(path, unsafeGet(i));
        const currentElement = keyPath[i];
        if (currentElement === void 0 || pathComponent !== currentElement) {
          return false;
        }
      }
      return true;
    }).flatMap((keyPath) => keyPath.slice(path.length, path.length + 1));
    return fromIterable3(filteredKeyPaths);
  });
  return fromFlat(makeFlat({
    load,
    enumerateChildren,
    patch: empty10
  }));
};
var fromMap = (map15, config2) => {
  const {
    pathDelim,
    seqDelim
  } = Object.assign({
    seqDelim: ",",
    pathDelim: "."
  }, config2);
  const makePathString = (path) => pipe(path, join(pathDelim));
  const unmakePathString = (pathString) => pathString.split(pathDelim);
  const mapWithIndexSplit = splitIndexInKeys(map15, (str) => unmakePathString(str), makePathString);
  const load = (path, primitive, split = true) => {
    const pathString = makePathString(path);
    const valueOpt = mapWithIndexSplit.has(pathString) ? some2(mapWithIndexSplit.get(pathString)) : none2();
    return pipe(valueOpt, mapError(() => MissingData(path, `Expected ${pathString} to exist in the provided map`)), flatMap4((value2) => parsePrimitive(value2, path, primitive, seqDelim, split)));
  };
  const enumerateChildren = (path) => sync(() => {
    const keyPaths = fromIterable(mapWithIndexSplit.keys()).map(unmakePathString);
    const filteredKeyPaths = keyPaths.filter((keyPath) => {
      for (let i = 0; i < path.length; i++) {
        const pathComponent = pipe(path, unsafeGet(i));
        const currentElement = keyPath[i];
        if (currentElement === void 0 || pathComponent !== currentElement) {
          return false;
        }
      }
      return true;
    }).flatMap((keyPath) => keyPath.slice(path.length, path.length + 1));
    return fromIterable3(filteredKeyPaths);
  });
  return fromFlat(makeFlat({
    load,
    enumerateChildren,
    patch: empty10
  }));
};
var extend = (leftDef, rightDef, left2, right2) => {
  const leftPad = unfold(left2.length, (index) => index >= right2.length ? none2() : some2([leftDef(index), index + 1]));
  const rightPad = unfold(right2.length, (index) => index >= left2.length ? none2() : some2([rightDef(index), index + 1]));
  const leftExtension = concat(left2, leftPad);
  const rightExtension = concat(right2, rightPad);
  return [leftExtension, rightExtension];
};
var appendConfigPath = (path, config2) => {
  let op = config2;
  if (op._tag === "Nested") {
    const out = path.slice();
    while (op._tag === "Nested") {
      out.push(op.name);
      op = op.config;
    }
    return out;
  }
  return path;
};
var fromFlatLoop = (flat, prefix, config2, split) => {
  const op = config2;
  switch (op._tag) {
    case OP_CONSTANT: {
      return succeed(of(op.value));
    }
    case OP_DESCRIBED: {
      return suspend(() => fromFlatLoop(flat, prefix, op.config, split));
    }
    case OP_FAIL: {
      return fail2(MissingData(prefix, op.message));
    }
    case OP_FALLBACK: {
      return pipe(suspend(() => fromFlatLoop(flat, prefix, op.first, split)), catchAll((error1) => {
        if (op.condition(error1)) {
          return pipe(fromFlatLoop(flat, prefix, op.second, split), catchAll((error2) => fail2(Or(error1, error2))));
        }
        return fail2(error1);
      }));
    }
    case OP_LAZY: {
      return suspend(() => fromFlatLoop(flat, prefix, op.config(), split));
    }
    case OP_MAP_OR_FAIL: {
      return suspend(() => pipe(fromFlatLoop(flat, prefix, op.original, split), flatMap4(forEachSequential((a) => pipe(op.mapOrFail(a), mapError(prefixed(appendConfigPath(prefix, op.original))))))));
    }
    case OP_NESTED: {
      return suspend(() => fromFlatLoop(flat, concat(prefix, of(op.name)), op.config, split));
    }
    case OP_PRIMITIVE: {
      return pipe(patch2(prefix, flat.patch), flatMap4((prefix2) => pipe(flat.load(prefix2, op, split), flatMap4((values5) => {
        if (values5.length === 0) {
          const name = pipe(last(prefix2), getOrElse(() => "<n/a>"));
          return fail2(MissingData([], `Expected ${op.description} with name ${name}`));
        }
        return succeed(values5);
      }))));
    }
    case OP_SEQUENCE: {
      return pipe(patch2(prefix, flat.patch), flatMap4((patchedPrefix) => pipe(flat.enumerateChildren(patchedPrefix), flatMap4(indicesFrom), flatMap4((indices) => {
        if (indices.length === 0) {
          return suspend(() => map4(fromFlatLoop(flat, prefix, op.config, true), of));
        }
        return pipe(forEachSequential(indices, (index) => fromFlatLoop(flat, append(prefix, `[${index}]`), op.config, true)), map4((chunkChunk) => {
          const flattened = flatten(chunkChunk);
          if (flattened.length === 0) {
            return of(empty());
          }
          return of(flattened);
        }));
      }))));
    }
    case OP_HASHMAP: {
      return suspend(() => pipe(patch2(prefix, flat.patch), flatMap4((prefix2) => pipe(flat.enumerateChildren(prefix2), flatMap4((keys4) => {
        return pipe(keys4, forEachSequential((key) => fromFlatLoop(flat, concat(prefix2, of(key)), op.valueConfig, split)), map4((matrix) => {
          if (matrix.length === 0) {
            return of(empty5());
          }
          return pipe(transpose(matrix), map2((values5) => fromIterable4(zip(fromIterable(keys4), values5))));
        }));
      })))));
    }
    case OP_ZIP_WITH: {
      return suspend(() => pipe(fromFlatLoop(flat, prefix, op.left, split), either, flatMap4((left2) => pipe(fromFlatLoop(flat, prefix, op.right, split), either, flatMap4((right2) => {
        if (isLeft(left2) && isLeft(right2)) {
          return fail2(And(left2.left, right2.left));
        }
        if (isLeft(left2) && isRight(right2)) {
          return fail2(left2.left);
        }
        if (isRight(left2) && isLeft(right2)) {
          return fail2(right2.left);
        }
        if (isRight(left2) && isRight(right2)) {
          const path = pipe(prefix, join("."));
          const fail11 = fromFlatLoopFail(prefix, path);
          const [lefts, rights] = extend(fail11, fail11, pipe(left2.right, map2(right)), pipe(right2.right, map2(right)));
          return pipe(lefts, zip(rights), forEachSequential(([left3, right3]) => pipe(zip3(left3, right3), map4(([left4, right4]) => op.zip(left4, right4)))));
        }
        throw new Error("BUG: ConfigProvider.fromFlatLoop - please report an issue at https://github.com/Effect-TS/effect/issues");
      })))));
    }
  }
};
var fromFlatLoopFail = (prefix, path) => (index) => left(MissingData(prefix, `The element at index ${index} in a sequence at path "${path}" was missing`));
var mapInputPath = dual(2, (self, f) => fromFlat(mapInputPathFlat(self.flattened, f)));
var mapInputPathFlat = (self, f) => makeFlat({
  load: (path, config2, split = true) => self.load(path, config2, split),
  enumerateChildren: (path) => self.enumerateChildren(path),
  patch: mapName(self.patch, f)
});
var nested2 = dual(2, (self, name) => fromFlat(makeFlat({
  load: (path, config2) => self.flattened.load(path, config2, true),
  enumerateChildren: (path) => self.flattened.enumerateChildren(path),
  patch: nested(self.flattened.patch, name)
})));
var unnested2 = dual(2, (self, name) => fromFlat(makeFlat({
  load: (path, config2) => self.flattened.load(path, config2, true),
  enumerateChildren: (path) => self.flattened.enumerateChildren(path),
  patch: unnested(self.flattened.patch, name)
})));
var orElse2 = dual(2, (self, that) => fromFlat(orElseFlat(self.flattened, () => that().flattened)));
var orElseFlat = (self, that) => makeFlat({
  load: (path, config2, split) => pipe(patch2(path, self.patch), flatMap4((patch7) => self.load(patch7, config2, split)), catchAll((error1) => pipe(sync(that), flatMap4((that2) => pipe(patch2(path, that2.patch), flatMap4((patch7) => that2.load(patch7, config2, split)), catchAll((error2) => fail2(Or(error1, error2)))))))),
  enumerateChildren: (path) => pipe(patch2(path, self.patch), flatMap4((patch7) => self.enumerateChildren(patch7)), either, flatMap4((left2) => pipe(sync(that), flatMap4((that2) => pipe(patch2(path, that2.patch), flatMap4((patch7) => that2.enumerateChildren(patch7)), either, flatMap4((right2) => {
    if (isLeft(left2) && isLeft(right2)) {
      return fail2(And(left2.left, right2.left));
    }
    if (isLeft(left2) && isRight(right2)) {
      return succeed(right2.right);
    }
    if (isRight(left2) && isLeft(right2)) {
      return succeed(left2.right);
    }
    if (isRight(left2) && isRight(right2)) {
      return succeed(pipe(left2.right, union2(right2.right)));
    }
    throw new Error("BUG: ConfigProvider.orElseFlat - please report an issue at https://github.com/Effect-TS/effect/issues");
  })))))),
  patch: empty10
});
var constantCase2 = (self) => mapInputPath(self, constantCase);
var kebabCase2 = (self) => mapInputPath(self, kebabCase);
var lowerCase2 = (self) => mapInputPath(self, lowerCase);
var snakeCase2 = (self) => mapInputPath(self, snakeCase);
var upperCase2 = (self) => mapInputPath(self, upperCase);
var within = dual(3, (self, path, f) => {
  const unnest = reduce2(path, self, (provider, name) => unnested2(provider, name));
  const nest = reduceRight(path, f(unnest), (provider, name) => nested2(provider, name));
  return orElse2(nest, () => self);
});
var splitPathString = (text, delim) => {
  const split = text.split(new RegExp(`\\s*${escape(delim)}\\s*`));
  return split;
};
var parsePrimitive = (text, path, primitive, delimiter, split) => {
  if (!split) {
    return pipe(primitive.parse(text), mapBoth({
      onFailure: prefixed(path),
      onSuccess: of
    }));
  }
  return pipe(splitPathString(text, delimiter), forEachSequential((char) => primitive.parse(char.trim())), mapError(prefixed(path)));
};
var transpose = (array2) => {
  return Object.keys(array2[0]).map((column) => array2.map((row) => row[column]));
};
var indicesFrom = (quotedIndices) => pipe(forEachSequential(quotedIndices, parseQuotedIndex), mapBoth({
  onFailure: () => empty(),
  onSuccess: sort(Order)
}), either, map4(merge));
var STR_INDEX_REGEX = /(^.+)(\[(\d+)\])$/;
var QUOTED_INDEX_REGEX = /^(\[(\d+)\])$/;
var parseQuotedIndex = (str) => {
  const match15 = str.match(QUOTED_INDEX_REGEX);
  if (match15 !== null) {
    const matchedIndex = match15[2];
    return pipe(matchedIndex !== void 0 && matchedIndex.length > 0 ? some2(matchedIndex) : none2(), flatMap(parseInteger));
  }
  return none2();
};
var splitIndexInKeys = (map15, unmakePathString, makePathString) => {
  const newMap = /* @__PURE__ */ new Map();
  for (const [pathString, value2] of map15) {
    const keyWithIndex = pipe(unmakePathString(pathString), flatMap2((key) => match(splitIndexFrom(key), {
      onNone: () => of(key),
      onSome: ([key2, index]) => make(key2, `[${index}]`)
    })));
    newMap.set(makePathString(keyWithIndex), value2);
  }
  return newMap;
};
var splitIndexFrom = (key) => {
  const match15 = key.match(STR_INDEX_REGEX);
  if (match15 !== null) {
    const matchedString = match15[1];
    const matchedIndex = match15[3];
    const optionalString = matchedString !== void 0 && matchedString.length > 0 ? some2(matchedString) : none2();
    const optionalIndex = pipe(matchedIndex !== void 0 && matchedIndex.length > 0 ? some2(matchedIndex) : none2(), flatMap(parseInteger));
    return all([optionalString, optionalIndex]);
  }
  return none2();
};
var parseInteger = (str) => {
  const parsedIndex = Number.parseInt(str);
  return Number.isNaN(parsedIndex) ? none2() : some2(parsedIndex);
};
var keyName = (name) => ({
  _tag: "KeyName",
  name
});
var keyIndex = (index) => ({
  _tag: "KeyIndex",
  index
});
var fromJson = (json) => {
  const hiddenDelimiter = "\uFEFF";
  const indexedEntries = map2(getIndexedEntries(json), ([key, value2]) => [configPathToString(key).join(hiddenDelimiter), value2]);
  return fromMap(new Map(indexedEntries), {
    pathDelim: hiddenDelimiter,
    seqDelim: hiddenDelimiter
  });
};
var configPathToString = (path) => {
  const output = [];
  let i = 0;
  while (i < path.length) {
    const component = path[i];
    if (component._tag === "KeyName") {
      if (i + 1 < path.length) {
        const nextComponent = path[i + 1];
        if (nextComponent._tag === "KeyIndex") {
          output.push(`${component.name}[${nextComponent.index}]`);
          i += 2;
        } else {
          output.push(component.name);
          i += 1;
        }
      } else {
        output.push(component.name);
        i += 1;
      }
    }
  }
  return output;
};
var getIndexedEntries = (config2) => {
  const loopAny = (path, value2) => {
    if (typeof value2 === "string") {
      return make([path, value2]);
    }
    if (typeof value2 === "number" || typeof value2 === "boolean") {
      return make([path, String(value2)]);
    }
    if (isArray(value2)) {
      return loopArray(path, value2);
    }
    if (typeof value2 === "object" && value2 !== null) {
      return loopObject(path, value2);
    }
    return empty();
  };
  const loopArray = (path, values5) => match2(values5, {
    onEmpty: () => make([path, "<nil>"]),
    onNonEmpty: flatMap2((value2, index) => loopAny(append(path, keyIndex(index)), value2))
  });
  const loopObject = (path, value2) => Object.entries(value2).filter(([, value3]) => isNotNullable(value3)).flatMap(([key, value3]) => {
    const newPath = append(path, keyName(key));
    const result = loopAny(newPath, value3);
    if (isEmptyReadonlyArray(result)) {
      return make([newPath, ""]);
    }
    return result;
  });
  return loopObject(empty(), config2);
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/defaultServices/console.js
var TypeId = Symbol.for("effect/Console");
var consoleTag = GenericTag("effect/Console");
var defaultConsole = {
  [TypeId]: TypeId,
  assert(condition, ...args2) {
    return sync(() => {
      console.assert(condition, ...args2);
    });
  },
  clear: sync(() => {
    console.clear();
  }),
  count(label) {
    return sync(() => {
      console.count(label);
    });
  },
  countReset(label) {
    return sync(() => {
      console.countReset(label);
    });
  },
  debug(...args2) {
    return sync(() => {
      console.debug(...args2);
    });
  },
  dir(item, options) {
    return sync(() => {
      console.dir(item, options);
    });
  },
  dirxml(...args2) {
    return sync(() => {
      console.dirxml(...args2);
    });
  },
  error(...args2) {
    return sync(() => {
      console.error(...args2);
    });
  },
  group(options) {
    return options?.collapsed ? sync(() => console.groupCollapsed(options?.label)) : sync(() => console.group(options?.label));
  },
  groupEnd: sync(() => {
    console.groupEnd();
  }),
  info(...args2) {
    return sync(() => {
      console.info(...args2);
    });
  },
  log(...args2) {
    return sync(() => {
      console.log(...args2);
    });
  },
  table(tabularData, properties) {
    return sync(() => {
      console.table(tabularData, properties);
    });
  },
  time(label) {
    return sync(() => console.time(label));
  },
  timeEnd(label) {
    return sync(() => console.timeEnd(label));
  },
  timeLog(label, ...args2) {
    return sync(() => {
      console.timeLog(label, ...args2);
    });
  },
  trace(...args2) {
    return sync(() => {
      console.trace(...args2);
    });
  },
  warn(...args2) {
    return sync(() => {
      console.warn(...args2);
    });
  },
  unsafe: console
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/defaultServices.js
var liveServices = pipe(empty3(), add(clockTag, make8()), add(consoleTag, defaultConsole), add(randomTag, make9(Math.random())), add(configProviderTag, fromEnv()), add(tracerTag, nativeTracer));
var currentServices = globalValue(Symbol.for("effect/DefaultServices/currentServices"), () => fiberRefUnsafeMakeContext(liveServices));
var sleep = (duration) => {
  const decodedDuration = decode(duration);
  return clockWith((clock2) => clock2.sleep(decodedDuration));
};
var defaultServicesWith = (f) => withFiberRuntime((fiber) => f(fiber.currentDefaultServices));
var clockWith = (f) => defaultServicesWith((services) => f(services.unsafeMap.get(clockTag.key)));
var currentTimeMillis = clockWith((clock2) => clock2.currentTimeMillis);
var currentTimeNanos = clockWith((clock2) => clock2.currentTimeNanos);
var withClock = dual(2, (effect, c) => fiberRefLocallyWith(currentServices, add(clockTag, c))(effect));
var withConfigProvider = dual(2, (self, provider) => fiberRefLocallyWith(currentServices, add(configProviderTag, provider))(self));
var configProviderWith = (f) => defaultServicesWith((services) => f(services.unsafeMap.get(configProviderTag.key)));
var config = (config2) => configProviderWith((_) => _.load(config2));
var randomWith = (f) => defaultServicesWith((services) => f(services.unsafeMap.get(randomTag.key)));
var withRandom = dual(2, (effect, value2) => fiberRefLocallyWith(currentServices, add(randomTag, value2))(effect));
var next = randomWith((random2) => random2.next);
var nextInt = randomWith((random2) => random2.nextInt);
var nextBoolean = randomWith((random2) => random2.nextBoolean);
var nextRange = (min5, max7) => randomWith((random2) => random2.nextRange(min5, max7));
var nextIntBetween = (min5, max7) => randomWith((random2) => random2.nextIntBetween(min5, max7));
var shuffle = (elements) => randomWith((random2) => random2.shuffle(elements));
var choice = (elements) => {
  const array2 = fromIterable(elements);
  return map4(array2.length === 0 ? fail2(new NoSuchElementException("Cannot select a random element from an empty array")) : randomWith((random2) => random2.nextIntBetween(0, array2.length)), (i) => array2[i]);
};
var tracerWith = (f) => defaultServicesWith((services) => f(services.unsafeMap.get(tracerTag.key)));
var withTracer = dual(2, (effect, value2) => fiberRefLocallyWith(currentServices, add(tracerTag, value2))(effect));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Boolean.js
var Boolean_exports = {};
__export(Boolean_exports, {
  Equivalence: () => Equivalence2,
  Order: () => Order2,
  and: () => and,
  eqv: () => eqv,
  every: () => every,
  implies: () => implies,
  isBoolean: () => isBoolean2,
  match: () => match4,
  nand: () => nand,
  nor: () => nor,
  not: () => not,
  or: () => or,
  some: () => some3,
  xor: () => xor
});
var isBoolean2 = isBoolean;
var match4 = dual(2, (value2, options) => value2 ? options.onTrue() : options.onFalse());
var Equivalence2 = boolean;
var Order2 = boolean2;
var not = (self) => !self;
var and = dual(2, (self, that) => self && that);
var nand = dual(2, (self, that) => !(self && that));
var or = dual(2, (self, that) => self || that);
var nor = dual(2, (self, that) => !(self || that));
var xor = dual(2, (self, that) => !self && that || self && !that);
var eqv = dual(2, (self, that) => !xor(self, that));
var implies = dual(2, (self, that) => self ? that : true);
var every = (collection) => {
  for (const b of collection) {
    if (!b) {
      return false;
    }
  }
  return true;
};
var some3 = (collection) => {
  for (const b of collection) {
    if (b) {
      return true;
    }
  }
  return false;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Effectable.js
var Effectable_exports = {};
__export(Effectable_exports, {
  ChannelTypeId: () => ChannelTypeId2,
  Class: () => Class,
  CommitPrototype: () => CommitPrototype2,
  EffectPrototype: () => EffectPrototype2,
  EffectTypeId: () => EffectTypeId3,
  SinkTypeId: () => SinkTypeId2,
  StreamTypeId: () => StreamTypeId2,
  StructuralClass: () => StructuralClass,
  StructuralCommitPrototype: () => StructuralCommitPrototype2
});
var EffectTypeId3 = EffectTypeId;
var StreamTypeId2 = StreamTypeId;
var SinkTypeId2 = SinkTypeId;
var ChannelTypeId2 = ChannelTypeId;
var EffectPrototype2 = EffectPrototype;
var CommitPrototype2 = CommitPrototype;
var StructuralCommitPrototype2 = StructuralCommitPrototype;
var Base2 = Base;
var StructuralBase2 = StructuralBase;
var Class = class extends Base2 {
};
var StructuralClass = class extends StructuralBase2 {
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ExecutionStrategy.js
var ExecutionStrategy_exports = {};
__export(ExecutionStrategy_exports, {
  isParallel: () => isParallel2,
  isParallelN: () => isParallelN2,
  isSequential: () => isSequential2,
  match: () => match6,
  parallel: () => parallel3,
  parallelN: () => parallelN2,
  sequential: () => sequential3
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/executionStrategy.js
var OP_SEQUENTIAL = "Sequential";
var OP_PARALLEL = "Parallel";
var OP_PARALLEL_N = "ParallelN";
var sequential2 = {
  _tag: OP_SEQUENTIAL
};
var parallel2 = {
  _tag: OP_PARALLEL
};
var parallelN = (parallelism) => ({
  _tag: OP_PARALLEL_N,
  parallelism
});
var isSequential = (self) => self._tag === OP_SEQUENTIAL;
var isParallel = (self) => self._tag === OP_PARALLEL;
var isParallelN = (self) => self._tag === OP_PARALLEL_N;
var match5 = dual(2, (self, options) => {
  switch (self._tag) {
    case OP_SEQUENTIAL: {
      return options.onSequential();
    }
    case OP_PARALLEL: {
      return options.onParallel();
    }
    case OP_PARALLEL_N: {
      return options.onParallelN(self.parallelism);
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ExecutionStrategy.js
var sequential3 = sequential2;
var parallel3 = parallel2;
var parallelN2 = parallelN;
var isSequential2 = isSequential;
var isParallel2 = isParallel;
var isParallelN2 = isParallelN;
var match6 = match5;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberRefs.js
var FiberRefs_exports = {};
__export(FiberRefs_exports, {
  FiberRefsSym: () => FiberRefsSym2,
  delete: () => delete_2,
  empty: () => empty12,
  fiberRefs: () => fiberRefs2,
  forkAs: () => forkAs2,
  get: () => get4,
  getOrDefault: () => getOrDefault2,
  joinAs: () => joinAs2,
  setAll: () => setAll2,
  unsafeMake: () => unsafeMake3,
  updateAs: () => updateAs2,
  updateManyAs: () => updateManyAs2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiberRefs.js
function unsafeMake2(fiberRefLocals) {
  return new FiberRefsImpl(fiberRefLocals);
}
function empty11() {
  return unsafeMake2(/* @__PURE__ */ new Map());
}
var FiberRefsSym = Symbol.for("effect/FiberRefs");
var FiberRefsImpl = class {
  locals;
  [FiberRefsSym] = FiberRefsSym;
  constructor(locals) {
    this.locals = locals;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var findAncestor = (_ref, _parentStack, _childStack, _childModified = false) => {
  const ref = _ref;
  let parentStack = _parentStack;
  let childStack = _childStack;
  let childModified = _childModified;
  let ret = void 0;
  while (ret === void 0) {
    if (isNonEmptyReadonlyArray(parentStack) && isNonEmptyReadonlyArray(childStack)) {
      const parentFiberId = headNonEmpty(parentStack)[0];
      const parentAncestors = tailNonEmpty(parentStack);
      const childFiberId = headNonEmpty(childStack)[0];
      const childRefValue = headNonEmpty(childStack)[1];
      const childAncestors = tailNonEmpty(childStack);
      if (parentFiberId.startTimeMillis < childFiberId.startTimeMillis) {
        childStack = childAncestors;
        childModified = true;
      } else if (parentFiberId.startTimeMillis > childFiberId.startTimeMillis) {
        parentStack = parentAncestors;
      } else {
        if (parentFiberId.id < childFiberId.id) {
          childStack = childAncestors;
          childModified = true;
        } else if (parentFiberId.id > childFiberId.id) {
          parentStack = parentAncestors;
        } else {
          ret = [childRefValue, childModified];
        }
      }
    } else {
      ret = [ref.initial, true];
    }
  }
  return ret;
};
var joinAs = dual(3, (self, fiberId2, that) => {
  const parentFiberRefs = new Map(self.locals);
  that.locals.forEach((childStack, fiberRef) => {
    const childValue = childStack[0][1];
    if (!childStack[0][0][symbol2](fiberId2)) {
      if (!parentFiberRefs.has(fiberRef)) {
        if (equals(childValue, fiberRef.initial)) {
          return;
        }
        parentFiberRefs.set(fiberRef, [[fiberId2, fiberRef.join(fiberRef.initial, childValue)]]);
        return;
      }
      const parentStack = parentFiberRefs.get(fiberRef);
      const [ancestor, wasModified] = findAncestor(fiberRef, parentStack, childStack);
      if (wasModified) {
        const patch7 = fiberRef.diff(ancestor, childValue);
        const oldValue = parentStack[0][1];
        const newValue = fiberRef.join(oldValue, fiberRef.patch(patch7)(oldValue));
        if (!equals(oldValue, newValue)) {
          let newStack;
          const parentFiberId = parentStack[0][0];
          if (parentFiberId[symbol2](fiberId2)) {
            newStack = [[parentFiberId, newValue], ...parentStack.slice(1)];
          } else {
            newStack = [[fiberId2, newValue], ...parentStack];
          }
          parentFiberRefs.set(fiberRef, newStack);
        }
      }
    }
  });
  return new FiberRefsImpl(parentFiberRefs);
});
var forkAs = dual(2, (self, childId) => {
  const map15 = /* @__PURE__ */ new Map();
  unsafeForkAs(self, map15, childId);
  return new FiberRefsImpl(map15);
});
var unsafeForkAs = (self, map15, fiberId2) => {
  self.locals.forEach((stack, fiberRef) => {
    const oldValue = stack[0][1];
    const newValue = fiberRef.patch(fiberRef.fork)(oldValue);
    if (equals(oldValue, newValue)) {
      map15.set(fiberRef, stack);
    } else {
      map15.set(fiberRef, [[fiberId2, newValue], ...stack]);
    }
  });
};
var fiberRefs = (self) => fromIterable3(self.locals.keys());
var setAll = (self) => forEachSequentialDiscard(fiberRefs(self), (fiberRef) => fiberRefSet(fiberRef, getOrDefault(self, fiberRef)));
var delete_ = dual(2, (self, fiberRef) => {
  const locals = new Map(self.locals);
  locals.delete(fiberRef);
  return new FiberRefsImpl(locals);
});
var get3 = dual(2, (self, fiberRef) => {
  if (!self.locals.has(fiberRef)) {
    return none2();
  }
  return some2(headNonEmpty(self.locals.get(fiberRef))[1]);
});
var getOrDefault = dual(2, (self, fiberRef) => pipe(get3(self, fiberRef), getOrElse(() => fiberRef.initial)));
var updateAs = dual(2, (self, {
  fiberId: fiberId2,
  fiberRef,
  value: value2
}) => {
  if (self.locals.size === 0) {
    return new FiberRefsImpl(/* @__PURE__ */ new Map([[fiberRef, [[fiberId2, value2]]]]));
  }
  const locals = new Map(self.locals);
  unsafeUpdateAs(locals, fiberId2, fiberRef, value2);
  return new FiberRefsImpl(locals);
});
var unsafeUpdateAs = (locals, fiberId2, fiberRef, value2) => {
  const oldStack = locals.get(fiberRef) ?? [];
  let newStack;
  if (isNonEmptyReadonlyArray(oldStack)) {
    const [currentId, currentValue] = headNonEmpty(oldStack);
    if (currentId[symbol2](fiberId2)) {
      if (equals(currentValue, value2)) {
        return;
      } else {
        newStack = [[fiberId2, value2], ...oldStack.slice(1)];
      }
    } else {
      newStack = [[fiberId2, value2], ...oldStack];
    }
  } else {
    newStack = [[fiberId2, value2]];
  }
  locals.set(fiberRef, newStack);
};
var updateManyAs = dual(2, (self, {
  entries,
  forkAs: forkAs3
}) => {
  if (self.locals.size === 0) {
    return new FiberRefsImpl(new Map(entries));
  }
  const locals = new Map(self.locals);
  if (forkAs3 !== void 0) {
    unsafeForkAs(self, locals, forkAs3);
  }
  entries.forEach(([fiberRef, values5]) => {
    if (values5.length === 1) {
      unsafeUpdateAs(locals, values5[0][0], fiberRef, values5[0][1]);
    } else {
      values5.forEach(([fiberId2, value2]) => {
        unsafeUpdateAs(locals, fiberId2, fiberRef, value2);
      });
    }
  });
  return new FiberRefsImpl(locals);
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberRefs.js
var FiberRefsSym2 = FiberRefsSym;
var delete_2 = delete_;
var fiberRefs2 = fiberRefs;
var forkAs2 = forkAs;
var get4 = get3;
var getOrDefault2 = getOrDefault;
var joinAs2 = joinAs;
var setAll2 = setAll;
var updateAs2 = updateAs;
var updateManyAs2 = updateManyAs;
var unsafeMake3 = unsafeMake2;
var empty12 = empty11;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberRefsPatch.js
var FiberRefsPatch_exports = {};
__export(FiberRefsPatch_exports, {
  combine: () => combine4,
  diff: () => diff3,
  empty: () => empty14,
  patch: () => patch4
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiberRefs/patch.js
var OP_EMPTY = "Empty";
var OP_ADD = "Add";
var OP_REMOVE = "Remove";
var OP_UPDATE = "Update";
var OP_AND_THEN = "AndThen";
var empty13 = {
  _tag: OP_EMPTY
};
var diff2 = (oldValue, newValue) => {
  const missingLocals = new Map(oldValue.locals);
  let patch7 = empty13;
  for (const [fiberRef, pairs] of newValue.locals.entries()) {
    const newValue2 = headNonEmpty(pairs)[1];
    const old = missingLocals.get(fiberRef);
    if (old !== void 0) {
      const oldValue2 = headNonEmpty(old)[1];
      if (!equals(oldValue2, newValue2)) {
        patch7 = combine3({
          _tag: OP_UPDATE,
          fiberRef,
          patch: fiberRef.diff(oldValue2, newValue2)
        })(patch7);
      }
    } else {
      patch7 = combine3({
        _tag: OP_ADD,
        fiberRef,
        value: newValue2
      })(patch7);
    }
    missingLocals.delete(fiberRef);
  }
  for (const [fiberRef] of missingLocals.entries()) {
    patch7 = combine3({
      _tag: OP_REMOVE,
      fiberRef
    })(patch7);
  }
  return patch7;
};
var combine3 = dual(2, (self, that) => ({
  _tag: OP_AND_THEN,
  first: self,
  second: that
}));
var patch3 = dual(3, (self, fiberId2, oldValue) => {
  let fiberRefs4 = oldValue;
  let patches = of(self);
  while (isNonEmptyReadonlyArray(patches)) {
    const head5 = headNonEmpty(patches);
    const tail2 = tailNonEmpty(patches);
    switch (head5._tag) {
      case OP_EMPTY: {
        patches = tail2;
        break;
      }
      case OP_ADD: {
        fiberRefs4 = updateAs(fiberRefs4, {
          fiberId: fiberId2,
          fiberRef: head5.fiberRef,
          value: head5.value
        });
        patches = tail2;
        break;
      }
      case OP_REMOVE: {
        fiberRefs4 = delete_(fiberRefs4, head5.fiberRef);
        patches = tail2;
        break;
      }
      case OP_UPDATE: {
        const value2 = getOrDefault(fiberRefs4, head5.fiberRef);
        fiberRefs4 = updateAs(fiberRefs4, {
          fiberId: fiberId2,
          fiberRef: head5.fiberRef,
          value: head5.fiberRef.patch(head5.patch)(value2)
        });
        patches = tail2;
        break;
      }
      case OP_AND_THEN: {
        patches = prepend(head5.first)(prepend(head5.second)(tail2));
        break;
      }
    }
  }
  return fiberRefs4;
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberRefsPatch.js
var empty14 = empty13;
var diff3 = diff2;
var combine4 = combine3;
var patch4 = patch3;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberStatus.js
var FiberStatus_exports = {};
__export(FiberStatus_exports, {
  FiberStatusTypeId: () => FiberStatusTypeId2,
  done: () => done2,
  isDone: () => isDone2,
  isFiberStatus: () => isFiberStatus2,
  isRunning: () => isRunning2,
  isSuspended: () => isSuspended2,
  running: () => running2,
  suspended: () => suspended2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiberStatus.js
var FiberStatusSymbolKey = "effect/FiberStatus";
var FiberStatusTypeId = Symbol.for(FiberStatusSymbolKey);
var OP_DONE = "Done";
var OP_RUNNING = "Running";
var OP_SUSPENDED = "Suspended";
var DoneHash = string(`${FiberStatusSymbolKey}-${OP_DONE}`);
var Done = class {
  [FiberStatusTypeId] = FiberStatusTypeId;
  _tag = OP_DONE;
  [symbol]() {
    return DoneHash;
  }
  [symbol2](that) {
    return isFiberStatus(that) && that._tag === OP_DONE;
  }
};
var Running = class {
  runtimeFlags;
  [FiberStatusTypeId] = FiberStatusTypeId;
  _tag = OP_RUNNING;
  constructor(runtimeFlags2) {
    this.runtimeFlags = runtimeFlags2;
  }
  [symbol]() {
    return pipe(hash(FiberStatusSymbolKey), combine(hash(this._tag)), combine(hash(this.runtimeFlags)), cached(this));
  }
  [symbol2](that) {
    return isFiberStatus(that) && that._tag === OP_RUNNING && this.runtimeFlags === that.runtimeFlags;
  }
};
var Suspended = class {
  runtimeFlags;
  blockingOn;
  [FiberStatusTypeId] = FiberStatusTypeId;
  _tag = OP_SUSPENDED;
  constructor(runtimeFlags2, blockingOn) {
    this.runtimeFlags = runtimeFlags2;
    this.blockingOn = blockingOn;
  }
  [symbol]() {
    return pipe(hash(FiberStatusSymbolKey), combine(hash(this._tag)), combine(hash(this.runtimeFlags)), combine(hash(this.blockingOn)), cached(this));
  }
  [symbol2](that) {
    return isFiberStatus(that) && that._tag === OP_SUSPENDED && this.runtimeFlags === that.runtimeFlags && equals(this.blockingOn, that.blockingOn);
  }
};
var done = new Done();
var running = (runtimeFlags2) => new Running(runtimeFlags2);
var suspended = (runtimeFlags2, blockingOn) => new Suspended(runtimeFlags2, blockingOn);
var isFiberStatus = (u) => hasProperty(u, FiberStatusTypeId);
var isDone = (self) => self._tag === OP_DONE;
var isRunning = (self) => self._tag === OP_RUNNING;
var isSuspended = (self) => self._tag === OP_SUSPENDED;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberStatus.js
var FiberStatusTypeId2 = FiberStatusTypeId;
var done2 = done;
var running2 = running;
var suspended2 = suspended;
var isFiberStatus2 = isFiberStatus;
var isDone2 = isDone;
var isRunning2 = isRunning;
var isSuspended2 = isSuspended;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/LogLevel.js
var LogLevel_exports = {};
__export(LogLevel_exports, {
  All: () => All,
  Debug: () => Debug,
  Error: () => Error2,
  Fatal: () => Fatal,
  Info: () => Info,
  None: () => None,
  Order: () => Order3,
  Trace: () => Trace,
  Warning: () => Warning,
  allLevels: () => allLevels,
  fromLiteral: () => fromLiteral,
  greaterThan: () => greaterThan3,
  greaterThanEqual: () => greaterThanEqual,
  lessThan: () => lessThan3,
  lessThanEqual: () => lessThanEqual,
  locally: () => locally
});
var All = logLevelAll;
var Fatal = logLevelFatal;
var Error2 = logLevelError;
var Warning = logLevelWarning;
var Info = logLevelInfo;
var Debug = logLevelDebug;
var Trace = logLevelTrace;
var None = logLevelNone;
var allLevels = allLogLevels;
var locally = dual(2, (use2, self) => fiberRefLocally(use2, currentLogLevel, self));
var Order3 = pipe(Order, mapInput((level) => level.ordinal));
var lessThan3 = lessThan(Order3);
var lessThanEqual = lessThanOrEqualTo(Order3);
var greaterThan3 = greaterThan(Order3);
var greaterThanEqual = greaterThanOrEqualTo(Order3);
var fromLiteral = (literal) => {
  switch (literal) {
    case "All":
      return All;
    case "Debug":
      return Debug;
    case "Error":
      return Error2;
    case "Fatal":
      return Fatal;
    case "Info":
      return Info;
    case "Trace":
      return Trace;
    case "None":
      return None;
    case "Warning":
      return Warning;
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Micro.js
var Micro_exports = {};
__export(Micro_exports, {
  CurrentConcurrency: () => CurrentConcurrency,
  CurrentScheduler: () => CurrentScheduler,
  Do: () => Do,
  Error: () => Error3,
  MaxOpsBeforeYield: () => MaxOpsBeforeYield,
  MicroCauseTypeId: () => MicroCauseTypeId,
  MicroExitTypeId: () => MicroExitTypeId,
  MicroFiberTypeId: () => MicroFiberTypeId,
  MicroSchedulerDefault: () => MicroSchedulerDefault,
  MicroScope: () => MicroScope,
  MicroScopeTypeId: () => MicroScopeTypeId,
  NoSuchElementException: () => NoSuchElementException2,
  TaggedError: () => TaggedError,
  TimeoutException: () => TimeoutException2,
  TypeId: () => TypeId2,
  acquireRelease: () => acquireRelease,
  acquireUseRelease: () => acquireUseRelease,
  addFinalizer: () => addFinalizer,
  all: () => all2,
  andThen: () => andThen3,
  as: () => as3,
  asSome: () => asSome,
  asVoid: () => asVoid2,
  async: () => async,
  bind: () => bind2,
  bindTo: () => bindTo2,
  catchAll: () => catchAll2,
  catchAllCause: () => catchAllCause2,
  catchAllDefect: () => catchAllDefect,
  catchCauseIf: () => catchCauseIf,
  catchIf: () => catchIf2,
  catchTag: () => catchTag,
  causeDie: () => causeDie,
  causeFail: () => causeFail,
  causeInterrupt: () => causeInterrupt,
  causeIsDie: () => causeIsDie,
  causeIsFail: () => causeIsFail,
  causeIsInterrupt: () => causeIsInterrupt,
  causeSquash: () => causeSquash2,
  causeWithTrace: () => causeWithTrace,
  context: () => context2,
  delay: () => delay,
  die: () => die3,
  either: () => either2,
  ensuring: () => ensuring,
  exit: () => exit2,
  exitDie: () => exitDie2,
  exitFail: () => exitFail2,
  exitFailCause: () => exitFailCause2,
  exitInterrupt: () => exitInterrupt2,
  exitIsDie: () => exitIsDie,
  exitIsFail: () => exitIsFail,
  exitIsFailure: () => exitIsFailure2,
  exitIsInterrupt: () => exitIsInterrupt,
  exitIsSuccess: () => exitIsSuccess2,
  exitSucceed: () => exitSucceed2,
  exitVoid: () => exitVoid2,
  exitVoidAll: () => exitVoidAll,
  fail: () => fail3,
  failCause: () => failCause2,
  failCauseSync: () => failCauseSync2,
  failSync: () => failSync2,
  fiberAwait: () => fiberAwait,
  fiberInterrupt: () => fiberInterrupt,
  fiberInterruptAll: () => fiberInterruptAll,
  fiberJoin: () => fiberJoin,
  filter: () => filter2,
  filterMap: () => filterMap2,
  filterOrFail: () => filterOrFail,
  filterOrFailCause: () => filterOrFailCause,
  flatMap: () => flatMap5,
  flatten: () => flatten5,
  flip: () => flip2,
  forEach: () => forEach2,
  forever: () => forever,
  fork: () => fork,
  forkDaemon: () => forkDaemon,
  forkIn: () => forkIn,
  forkScoped: () => forkScoped,
  fromEither: () => fromEither,
  fromOption: () => fromOption,
  gen: () => gen2,
  ignore: () => ignore,
  ignoreLogged: () => ignoreLogged,
  interrupt: () => interrupt3,
  interruptible: () => interruptible3,
  isMicro: () => isMicro,
  isMicroCause: () => isMicroCause,
  isMicroExit: () => isMicroExit,
  let: () => let_2,
  map: () => map5,
  mapError: () => mapError2,
  mapErrorCause: () => mapErrorCause,
  match: () => match7,
  matchCause: () => matchCause2,
  matchCauseEffect: () => matchCauseEffect2,
  matchEffect: () => matchEffect2,
  never: () => never2,
  onError: () => onError2,
  onExit: () => onExit2,
  onExitIf: () => onExitIf,
  onInterrupt: () => onInterrupt2,
  option: () => option,
  orDie: () => orDie2,
  orElseSucceed: () => orElseSucceed,
  promise: () => promise,
  provideContext: () => provideContext2,
  provideScope: () => provideScope,
  provideService: () => provideService,
  provideServiceEffect: () => provideServiceEffect,
  race: () => race,
  raceAll: () => raceAll,
  raceAllFirst: () => raceAllFirst,
  raceFirst: () => raceFirst,
  repeat: () => repeat,
  repeatExit: () => repeatExit,
  replicate: () => replicate,
  replicateEffect: () => replicateEffect,
  retry: () => retry,
  runFork: () => runFork,
  runPromise: () => runPromise,
  runPromiseExit: () => runPromiseExit,
  runSync: () => runSync,
  runSyncExit: () => runSyncExit,
  sandbox: () => sandbox,
  scheduleAddDelay: () => scheduleAddDelay,
  scheduleExponential: () => scheduleExponential,
  scheduleIntersect: () => scheduleIntersect,
  scheduleRecurs: () => scheduleRecurs,
  scheduleSpaced: () => scheduleSpaced,
  scheduleUnion: () => scheduleUnion,
  scheduleWithMaxDelay: () => scheduleWithMaxDelay,
  scheduleWithMaxElapsed: () => scheduleWithMaxElapsed,
  scope: () => scope,
  scopeMake: () => scopeMake,
  scopeUnsafeMake: () => scopeUnsafeMake,
  scoped: () => scoped,
  service: () => service,
  serviceOption: () => serviceOption,
  sleep: () => sleep2,
  succeed: () => succeed2,
  succeedNone: () => succeedNone,
  succeedSome: () => succeedSome,
  suspend: () => suspend2,
  sync: () => sync2,
  tap: () => tap2,
  tapDefect: () => tapDefect,
  tapError: () => tapError,
  tapErrorCause: () => tapErrorCause,
  tapErrorCauseIf: () => tapErrorCauseIf,
  timeout: () => timeout,
  timeoutOption: () => timeoutOption,
  timeoutOrElse: () => timeoutOrElse,
  try: () => try_,
  tryPromise: () => tryPromise,
  uninterruptible: () => uninterruptible2,
  uninterruptibleMask: () => uninterruptibleMask2,
  updateContext: () => updateContext,
  updateService: () => updateService,
  void: () => void_2,
  when: () => when,
  whileLoop: () => whileLoop2,
  withConcurrency: () => withConcurrency,
  withMicroFiber: () => withMicroFiber,
  withTrace: () => withTrace,
  yieldFlush: () => yieldFlush,
  yieldNow: () => yieldNow2,
  yieldNowWith: () => yieldNowWith,
  zip: () => zip4,
  zipWith: () => zipWith3
});
var TypeId2 = Symbol.for("effect/Micro");
var MicroExitTypeId = Symbol.for("effect/Micro/MicroExit");
var isMicro = (u) => typeof u === "object" && u !== null && TypeId2 in u;
var MicroCauseTypeId = Symbol.for("effect/Micro/MicroCause");
var isMicroCause = (self) => hasProperty(self, MicroCauseTypeId);
var microCauseVariance = {
  _E: identity
};
var MicroCauseImpl = class extends globalThis.Error {
  _tag;
  traces;
  [MicroCauseTypeId];
  constructor(_tag, originalError2, traces) {
    const causeName = `MicroCause.${_tag}`;
    let name;
    let message;
    let stack;
    if (originalError2 instanceof globalThis.Error) {
      name = `(${causeName}) ${originalError2.name}`;
      message = originalError2.message;
      const messageLines = message.split("\n").length;
      stack = originalError2.stack ? `(${causeName}) ${originalError2.stack.split("\n").slice(0, messageLines + 3).join("\n")}` : `${name}: ${message}`;
    } else {
      name = causeName;
      message = toStringUnknown(originalError2, 0);
      stack = `${name}: ${message}`;
    }
    if (traces.length > 0) {
      stack += `
    ${traces.join("\n    ")}`;
    }
    super(message);
    this._tag = _tag;
    this.traces = traces;
    this[MicroCauseTypeId] = microCauseVariance;
    this.name = name;
    this.stack = stack;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  toString() {
    return this.stack;
  }
  [NodeInspectSymbol]() {
    return this.stack;
  }
};
var Fail = class extends MicroCauseImpl {
  error;
  constructor(error, traces = []) {
    super("Fail", error, traces);
    this.error = error;
  }
};
var causeFail = (error, traces = []) => new Fail(error, traces);
var Die = class extends MicroCauseImpl {
  defect;
  constructor(defect, traces = []) {
    super("Die", defect, traces);
    this.defect = defect;
  }
};
var causeDie = (defect, traces = []) => new Die(defect, traces);
var Interrupt = class extends MicroCauseImpl {
  constructor(traces = []) {
    super("Interrupt", "interrupted", traces);
  }
};
var causeInterrupt = (traces = []) => new Interrupt(traces);
var causeIsFail = (self) => self._tag === "Fail";
var causeIsDie = (self) => self._tag === "Die";
var causeIsInterrupt = (self) => self._tag === "Interrupt";
var causeSquash2 = (self) => self._tag === "Fail" ? self.error : self._tag === "Die" ? self.defect : self;
var causeWithTrace = dual(2, (self, trace) => {
  const traces = [...self.traces, trace];
  switch (self._tag) {
    case "Die":
      return causeDie(self.defect, traces);
    case "Interrupt":
      return causeInterrupt(traces);
    case "Fail":
      return causeFail(self.error, traces);
  }
});
var MicroFiberTypeId = Symbol.for("effect/Micro/MicroFiber");
var fiberVariance = {
  _A: identity,
  _E: identity
};
var MicroFiberImpl = class {
  context;
  interruptible;
  [MicroFiberTypeId];
  _stack = [];
  _observers = [];
  _exit;
  _children;
  currentOpCount = 0;
  constructor(context4, interruptible4 = true) {
    this.context = context4;
    this.interruptible = interruptible4;
    this[MicroFiberTypeId] = fiberVariance;
  }
  getRef(ref) {
    return unsafeGetReference(this.context, ref);
  }
  addObserver(cb) {
    if (this._exit) {
      cb(this._exit);
      return constVoid;
    }
    this._observers.push(cb);
    return () => {
      const index = this._observers.indexOf(cb);
      if (index >= 0) {
        this._observers.splice(index, 1);
      }
    };
  }
  _interrupted = false;
  unsafeInterrupt() {
    if (this._exit) {
      return;
    }
    this._interrupted = true;
    if (this.interruptible) {
      this.evaluate(exitInterrupt2);
    }
  }
  unsafePoll() {
    return this._exit;
  }
  evaluate(effect) {
    if (this._exit) {
      return;
    } else if (this._yielded !== void 0) {
      const yielded = this._yielded;
      this._yielded = void 0;
      yielded();
    }
    const exit3 = this.runLoop(effect);
    if (exit3 === Yield) {
      return;
    }
    const interruptChildren = fiberMiddleware.interruptChildren && fiberMiddleware.interruptChildren(this);
    if (interruptChildren !== void 0) {
      return this.evaluate(flatMap5(interruptChildren, () => exit3));
    }
    this._exit = exit3;
    for (let i = 0; i < this._observers.length; i++) {
      this._observers[i](exit3);
    }
    this._observers.length = 0;
  }
  runLoop(effect) {
    let yielding = false;
    let current = effect;
    this.currentOpCount = 0;
    try {
      while (true) {
        this.currentOpCount++;
        if (!yielding && this.getRef(CurrentScheduler).shouldYield(this)) {
          yielding = true;
          const prev = current;
          current = flatMap5(yieldNow2, () => prev);
        }
        current = current[evaluate](this);
        if (current === Yield) {
          const yielded = this._yielded;
          if (MicroExitTypeId in yielded) {
            this._yielded = void 0;
            return yielded;
          }
          return Yield;
        }
      }
    } catch (error) {
      if (!hasProperty(current, evaluate)) {
        return exitDie2(`MicroFiber.runLoop: Not a valid effect: ${String(current)}`);
      }
      return exitDie2(error);
    }
  }
  getCont(symbol3) {
    while (true) {
      const op = this._stack.pop();
      if (!op) return void 0;
      const cont = op[ensureCont] && op[ensureCont](this);
      if (cont) return {
        [symbol3]: cont
      };
      if (op[symbol3]) return op;
    }
  }
  // cancel the yielded operation, or for the yielded exit value
  _yielded = void 0;
  yieldWith(value2) {
    this._yielded = value2;
    return Yield;
  }
  children() {
    return this._children ??= /* @__PURE__ */ new Set();
  }
};
var fiberMiddleware = globalValue("effect/Micro/fiberMiddleware", () => ({
  interruptChildren: void 0
}));
var fiberInterruptChildren = (fiber) => {
  if (fiber._children === void 0 || fiber._children.size === 0) {
    return void 0;
  }
  return fiberInterruptAll(fiber._children);
};
var fiberAwait = (self) => async((resume2) => sync2(self.addObserver((exit3) => resume2(succeed2(exit3)))));
var fiberJoin = (self) => flatten5(fiberAwait(self));
var fiberInterrupt = (self) => suspend2(() => {
  self.unsafeInterrupt();
  return asVoid2(fiberAwait(self));
});
var fiberInterruptAll = (fibers) => suspend2(() => {
  for (const fiber of fibers) fiber.unsafeInterrupt();
  const iter = fibers[Symbol.iterator]();
  const wait = suspend2(() => {
    let result = iter.next();
    while (!result.done) {
      if (result.value.unsafePoll()) {
        result = iter.next();
        continue;
      }
      const fiber = result.value;
      return async((resume2) => {
        fiber.addObserver((_) => {
          resume2(wait);
        });
      });
    }
    return exitVoid2;
  });
  return wait;
});
var identifier = Symbol.for("effect/Micro/identifier");
var args = Symbol.for("effect/Micro/args");
var evaluate = Symbol.for("effect/Micro/evaluate");
var successCont = Symbol.for("effect/Micro/successCont");
var failureCont = Symbol.for("effect/Micro/failureCont");
var ensureCont = Symbol.for("effect/Micro/ensureCont");
var Yield = Symbol.for("effect/Micro/Yield");
var microVariance = {
  _A: identity,
  _E: identity,
  _R: identity
};
var MicroProto = {
  ...EffectPrototype2,
  _op: "Micro",
  [TypeId2]: microVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  [Symbol.iterator]() {
    return new SingleShotGen(new YieldWrap(this));
  },
  toJSON() {
    return {
      _id: "Micro",
      op: this[identifier],
      ...args in this ? {
        args: this[args]
      } : void 0
    };
  },
  toString() {
    return format(this);
  },
  [NodeInspectSymbol]() {
    return format(this);
  }
};
function defaultEvaluate(_fiber) {
  return exitDie2(`Micro.evaluate: Not implemented`);
}
var makePrimitiveProto = (options) => ({
  ...MicroProto,
  [identifier]: options.op,
  [evaluate]: options.eval ?? defaultEvaluate,
  [successCont]: options.contA,
  [failureCont]: options.contE,
  [ensureCont]: options.ensure
});
var makePrimitive = (options) => {
  const Proto2 = makePrimitiveProto(options);
  return function() {
    const self = Object.create(Proto2);
    self[args] = options.single === false ? arguments : arguments[0];
    return self;
  };
};
var makeExit = (options) => {
  const Proto2 = {
    ...makePrimitiveProto(options),
    [MicroExitTypeId]: MicroExitTypeId,
    _tag: options.op,
    get [options.prop]() {
      return this[args];
    },
    toJSON() {
      return {
        _id: "MicroExit",
        _tag: options.op,
        [options.prop]: this[args]
      };
    },
    [symbol2](that) {
      return isMicroExit(that) && that._tag === options.op && equals(this[args], that[args]);
    },
    [symbol]() {
      return cached(this, combine(string(options.op))(hash(this[args])));
    }
  };
  return function(value2) {
    const self = Object.create(Proto2);
    self[args] = value2;
    self[successCont] = void 0;
    self[failureCont] = void 0;
    self[ensureCont] = void 0;
    return self;
  };
};
var succeed2 = makeExit({
  op: "Success",
  prop: "value",
  eval(fiber) {
    const cont = fiber.getCont(successCont);
    return cont ? cont[successCont](this[args], fiber) : fiber.yieldWith(this);
  }
});
var failCause2 = makeExit({
  op: "Failure",
  prop: "cause",
  eval(fiber) {
    let cont = fiber.getCont(failureCont);
    while (causeIsInterrupt(this[args]) && cont && fiber.interruptible) {
      cont = fiber.getCont(failureCont);
    }
    return cont ? cont[failureCont](this[args], fiber) : fiber.yieldWith(this);
  }
});
var fail3 = (error) => failCause2(causeFail(error));
var sync2 = makePrimitive({
  op: "Sync",
  eval(fiber) {
    const value2 = this[args]();
    const cont = fiber.getCont(successCont);
    return cont ? cont[successCont](value2, fiber) : fiber.yieldWith(exitSucceed2(value2));
  }
});
var suspend2 = makePrimitive({
  op: "Suspend",
  eval(_fiber) {
    return this[args]();
  }
});
var yieldNowWith = makePrimitive({
  op: "Yield",
  eval(fiber) {
    let resumed = false;
    fiber.getRef(CurrentScheduler).scheduleTask(() => {
      if (resumed) return;
      fiber.evaluate(exitVoid2);
    }, this[args] ?? 0);
    return fiber.yieldWith(() => {
      resumed = true;
    });
  }
});
var yieldNow2 = yieldNowWith(0);
var succeedSome = (a) => succeed2(some2(a));
var succeedNone = succeed2(none2());
var failCauseSync2 = (evaluate2) => suspend2(() => failCause2(evaluate2()));
var die3 = (defect) => exitDie2(defect);
var failSync2 = (error) => suspend2(() => fail3(error()));
var fromOption = (option3) => option3._tag === "Some" ? succeed2(option3.value) : fail3(new NoSuchElementException2({}));
var fromEither = (either3) => either3._tag === "Right" ? succeed2(either3.right) : fail3(either3.left);
var void_2 = succeed2(void 0);
var try_ = (options) => suspend2(() => {
  try {
    return succeed2(options.try());
  } catch (err) {
    return fail3(options.catch(err));
  }
});
var promise = (evaluate2) => asyncOptions(function(resume2, signal) {
  evaluate2(signal).then((a) => resume2(succeed2(a)), (e) => resume2(die3(e)));
}, evaluate2.length !== 0);
var tryPromise = (options) => asyncOptions(function(resume2, signal) {
  try {
    options.try(signal).then((a) => resume2(succeed2(a)), (e) => resume2(fail3(options.catch(e))));
  } catch (err) {
    resume2(fail3(options.catch(err)));
  }
}, options.try.length !== 0);
var withMicroFiber = makePrimitive({
  op: "WithMicroFiber",
  eval(fiber) {
    return this[args](fiber);
  }
});
var yieldFlush = withMicroFiber((fiber) => {
  fiber.getRef(CurrentScheduler).flush();
  return exitVoid2;
});
var asyncOptions = makePrimitive({
  op: "Async",
  single: false,
  eval(fiber) {
    const register = this[args][0];
    let resumed = false;
    let yielded = false;
    const controller = this[args][1] ? new AbortController() : void 0;
    const onCancel = register((effect) => {
      if (resumed) return;
      resumed = true;
      if (yielded) {
        fiber.evaluate(effect);
      } else {
        yielded = effect;
      }
    }, controller?.signal);
    if (yielded !== false) return yielded;
    yielded = true;
    fiber._yielded = () => {
      resumed = true;
    };
    if (controller === void 0 && onCancel === void 0) {
      return Yield;
    }
    fiber._stack.push(asyncFinalizer(() => {
      resumed = true;
      controller?.abort();
      return onCancel ?? exitVoid2;
    }));
    return Yield;
  }
});
var asyncFinalizer = makePrimitive({
  op: "AsyncFinalizer",
  ensure(fiber) {
    if (fiber.interruptible) {
      fiber.interruptible = false;
      fiber._stack.push(setInterruptible(true));
    }
  },
  contE(cause2, _fiber) {
    return causeIsInterrupt(cause2) ? flatMap5(this[args](), () => failCause2(cause2)) : failCause2(cause2);
  }
});
var async = (register) => asyncOptions(register, register.length >= 2);
var never2 = async(function() {
  const interval = setInterval(constVoid, 2147483646);
  return sync2(() => clearInterval(interval));
});
var gen2 = (...args2) => suspend2(() => fromIterator(args2.length === 1 ? args2[0]() : args2[1].call(args2[0])));
var fromIterator = makePrimitive({
  op: "Iterator",
  contA(value2, fiber) {
    const state = this[args].next(value2);
    if (state.done) return succeed2(state.value);
    fiber._stack.push(this);
    return yieldWrapGet(state.value);
  },
  eval(fiber) {
    return this[successCont](void 0, fiber);
  }
});
var as3 = dual(2, (self, value2) => map5(self, (_) => value2));
var asSome = (self) => map5(self, some2);
var flip2 = (self) => matchEffect2(self, {
  onFailure: succeed2,
  onSuccess: fail3
});
var andThen3 = dual(2, (self, f) => flatMap5(self, (a) => {
  const value2 = isMicro(f) ? f : typeof f === "function" ? f(a) : f;
  return isMicro(value2) ? value2 : succeed2(value2);
}));
var tap2 = dual(2, (self, f) => flatMap5(self, (a) => {
  const value2 = isMicro(f) ? f : typeof f === "function" ? f(a) : f;
  return isMicro(value2) ? as3(value2, a) : succeed2(a);
}));
var asVoid2 = (self) => flatMap5(self, (_) => exitVoid2);
var exit2 = (self) => matchCause2(self, {
  onFailure: exitFailCause2,
  onSuccess: exitSucceed2
});
var sandbox = (self) => catchAllCause2(self, fail3);
var raceAll = (all6) => withMicroFiber((parent) => async((resume2) => {
  const effects = fromIterable(all6);
  const len = effects.length;
  let doneCount = 0;
  let done8 = false;
  const fibers = /* @__PURE__ */ new Set();
  const causes = [];
  const onExit3 = (exit3) => {
    doneCount++;
    if (exit3._tag === "Failure") {
      causes.push(exit3.cause);
      if (doneCount >= len) {
        resume2(failCause2(causes[0]));
      }
      return;
    }
    done8 = true;
    resume2(fibers.size === 0 ? exit3 : flatMap5(uninterruptible2(fiberInterruptAll(fibers)), () => exit3));
  };
  for (let i = 0; i < len; i++) {
    if (done8) break;
    const fiber = unsafeFork(parent, interruptible3(effects[i]), true, true);
    fibers.add(fiber);
    fiber.addObserver((exit3) => {
      fibers.delete(fiber);
      onExit3(exit3);
    });
  }
  return fiberInterruptAll(fibers);
}));
var raceAllFirst = (all6) => withMicroFiber((parent) => async((resume2) => {
  let done8 = false;
  const fibers = /* @__PURE__ */ new Set();
  const onExit3 = (exit3) => {
    done8 = true;
    resume2(fibers.size === 0 ? exit3 : flatMap5(fiberInterruptAll(fibers), () => exit3));
  };
  for (const effect of all6) {
    if (done8) break;
    const fiber = unsafeFork(parent, interruptible3(effect), true, true);
    fibers.add(fiber);
    fiber.addObserver((exit3) => {
      fibers.delete(fiber);
      onExit3(exit3);
    });
  }
  return fiberInterruptAll(fibers);
}));
var race = dual(2, (self, that) => raceAll([self, that]));
var raceFirst = dual(2, (self, that) => raceAllFirst([self, that]));
var flatMap5 = dual(2, (self, f) => {
  const onSuccess = Object.create(OnSuccessProto);
  onSuccess[args] = self;
  onSuccess[successCont] = f;
  return onSuccess;
});
var OnSuccessProto = makePrimitiveProto({
  op: "OnSuccess",
  eval(fiber) {
    fiber._stack.push(this);
    return this[args];
  }
});
var flatten5 = (self) => flatMap5(self, identity);
var map5 = dual(2, (self, f) => flatMap5(self, (a) => succeed2(f(a))));
var isMicroExit = (u) => hasProperty(u, MicroExitTypeId);
var exitSucceed2 = succeed2;
var exitFailCause2 = failCause2;
var exitInterrupt2 = exitFailCause2(causeInterrupt());
var exitFail2 = (e) => exitFailCause2(causeFail(e));
var exitDie2 = (defect) => exitFailCause2(causeDie(defect));
var exitIsSuccess2 = (self) => self._tag === "Success";
var exitIsFailure2 = (self) => self._tag === "Failure";
var exitIsInterrupt = (self) => exitIsFailure2(self) && self.cause._tag === "Interrupt";
var exitIsFail = (self) => exitIsFailure2(self) && self.cause._tag === "Fail";
var exitIsDie = (self) => exitIsFailure2(self) && self.cause._tag === "Die";
var exitVoid2 = exitSucceed2(void 0);
var exitVoidAll = (exits) => {
  for (const exit3 of exits) {
    if (exit3._tag === "Failure") {
      return exit3;
    }
  }
  return exitVoid2;
};
var setImmediate = "setImmediate" in globalThis ? globalThis.setImmediate : (f) => setTimeout(f, 0);
var MicroSchedulerDefault = class {
  tasks = [];
  running = false;
  /**
   * @since 3.5.9
   */
  scheduleTask(task, _priority) {
    this.tasks.push(task);
    if (!this.running) {
      this.running = true;
      setImmediate(this.afterScheduled);
    }
  }
  /**
   * @since 3.5.9
   */
  afterScheduled = () => {
    this.running = false;
    this.runTasks();
  };
  /**
   * @since 3.5.9
   */
  runTasks() {
    const tasks = this.tasks;
    this.tasks = [];
    for (let i = 0, len = tasks.length; i < len; i++) {
      tasks[i]();
    }
  }
  /**
   * @since 3.5.9
   */
  shouldYield(fiber) {
    return fiber.currentOpCount >= fiber.getRef(MaxOpsBeforeYield);
  }
  /**
   * @since 3.5.9
   */
  flush() {
    while (this.tasks.length > 0) {
      this.runTasks();
    }
  }
};
var service = (tag) => withMicroFiber((fiber) => succeed2(unsafeGet2(fiber.context, tag)));
var serviceOption = (tag) => withMicroFiber((fiber) => succeed2(getOption(fiber.context, tag)));
var updateContext = dual(2, (self, f) => withMicroFiber((fiber) => {
  const prev = fiber.context;
  fiber.context = f(prev);
  return onExit2(self, () => {
    fiber.context = prev;
    return void_2;
  });
}));
var updateService = dual(3, (self, tag, f) => withMicroFiber((fiber) => {
  const prev = unsafeGet2(fiber.context, tag);
  fiber.context = add(fiber.context, tag, f(prev));
  return onExit2(self, () => {
    fiber.context = add(fiber.context, tag, prev);
    return void_2;
  });
}));
var context2 = () => getContext;
var getContext = withMicroFiber((fiber) => succeed2(fiber.context));
var provideContext2 = dual(2, (self, provided) => updateContext(self, merge2(provided)));
var provideService = dual(3, (self, tag, service3) => updateContext(self, add(tag, service3)));
var provideServiceEffect = dual(3, (self, tag, acquire) => flatMap5(acquire, (service3) => provideService(self, tag, service3)));
var MaxOpsBeforeYield = class extends Reference()("effect/Micro/currentMaxOpsBeforeYield", {
  defaultValue: () => 2048
}) {
};
var CurrentConcurrency = class extends Reference()("effect/Micro/currentConcurrency", {
  defaultValue: () => "unbounded"
}) {
};
var CurrentScheduler = class extends Reference()("effect/Micro/currentScheduler", {
  defaultValue: () => new MicroSchedulerDefault()
}) {
};
var withConcurrency = dual(2, (self, concurrency) => provideService(self, CurrentConcurrency, concurrency));
var zip4 = dual((args2) => isMicro(args2[1]), (self, that, options) => zipWith3(self, that, (a, a2) => [a, a2], options));
var zipWith3 = dual((args2) => isMicro(args2[1]), (self, that, f, options) => options?.concurrent ? map5(all2([self, that], {
  concurrency: 2
}), ([a, a2]) => f(a, a2)) : flatMap5(self, (a) => map5(that, (a2) => f(a, a2))));
var filterOrFailCause = dual((args2) => isMicro(args2[0]), (self, refinement, orFailWith) => flatMap5(self, (a) => refinement(a) ? succeed2(a) : failCause2(orFailWith(a))));
var filterOrFail = dual((args2) => isMicro(args2[0]), (self, refinement, orFailWith) => flatMap5(self, (a) => refinement(a) ? succeed2(a) : fail3(orFailWith(a))));
var when = dual(2, (self, condition) => flatMap5(isMicro(condition) ? condition : sync2(condition), (pass) => pass ? asSome(self) : succeedNone));
var repeatExit = dual(2, (self, options) => suspend2(() => {
  const startedAt = options.schedule ? Date.now() : 0;
  let attempt = 0;
  const loop2 = flatMap5(exit2(self), (exit3) => {
    if (options.while !== void 0 && !options.while(exit3)) {
      return exit3;
    } else if (options.times !== void 0 && attempt >= options.times) {
      return exit3;
    }
    attempt++;
    let delayEffect = yieldNow2;
    if (options.schedule !== void 0) {
      const elapsed = Date.now() - startedAt;
      const duration = options.schedule(attempt, elapsed);
      if (isNone(duration)) {
        return exit3;
      }
      delayEffect = sleep2(duration.value);
    }
    return flatMap5(delayEffect, () => loop2);
  });
  return loop2;
}));
var repeat = dual((args2) => isMicro(args2[0]), (self, options) => repeatExit(self, {
  ...options,
  while: (exit3) => exit3._tag === "Success" && (options?.while === void 0 || options.while(exit3.value))
}));
var replicate = dual(2, (self, n) => Array.from({
  length: n
}, () => self));
var replicateEffect = dual((args2) => isMicro(args2[0]), (self, n, options) => all2(replicate(self, n), options));
var forever = (self) => repeat(self);
var scheduleRecurs = (n) => (attempt) => attempt <= n ? some2(0) : none2();
var scheduleSpaced = (millis2) => () => some2(millis2);
var scheduleExponential = (baseMillis, factor = 2) => (attempt) => some2(Math.pow(factor, attempt) * baseMillis);
var scheduleAddDelay = dual(2, (self, f) => (attempt, elapsed) => map(self(attempt, elapsed), (duration) => duration + f()));
var scheduleWithMaxDelay = dual(2, (self, max7) => (attempt, elapsed) => map(self(attempt, elapsed), (duration) => Math.min(duration, max7)));
var scheduleWithMaxElapsed = dual(2, (self, max7) => (attempt, elapsed) => elapsed < max7 ? self(attempt, elapsed) : none2());
var scheduleUnion = dual(2, (self, that) => (attempt, elapsed) => zipWith(self(attempt, elapsed), that(attempt, elapsed), (d1, d2) => Math.min(d1, d2)));
var scheduleIntersect = dual(2, (self, that) => (attempt, elapsed) => zipWith(self(attempt, elapsed), that(attempt, elapsed), (d1, d2) => Math.max(d1, d2)));
var catchAllCause2 = dual(2, (self, f) => {
  const onFailure = Object.create(OnFailureProto);
  onFailure[args] = self;
  onFailure[failureCont] = f;
  return onFailure;
});
var OnFailureProto = makePrimitiveProto({
  op: "OnFailure",
  eval(fiber) {
    fiber._stack.push(this);
    return this[args];
  }
});
var catchCauseIf = dual(3, (self, predicate, f) => catchAllCause2(self, (cause2) => predicate(cause2) ? f(cause2) : failCause2(cause2)));
var catchAll2 = dual(2, (self, f) => catchCauseIf(self, causeIsFail, (cause2) => f(cause2.error)));
var catchAllDefect = dual(2, (self, f) => catchCauseIf(self, causeIsDie, (die8) => f(die8.defect)));
var tapErrorCause = dual(2, (self, f) => tapErrorCauseIf(self, constTrue, f));
var tapErrorCauseIf = dual(3, (self, refinement, f) => catchCauseIf(self, refinement, (cause2) => andThen3(f(cause2), failCause2(cause2))));
var tapError = dual(2, (self, f) => tapErrorCauseIf(self, causeIsFail, (fail11) => f(fail11.error)));
var tapDefect = dual(2, (self, f) => tapErrorCauseIf(self, causeIsDie, (die8) => f(die8.defect)));
var catchIf2 = dual(3, (self, predicate, f) => catchCauseIf(self, (f2) => causeIsFail(f2) && predicate(f2.error), (fail11) => f(fail11.error)));
var catchTag = dual(3, (self, k, f) => catchIf2(self, isTagged(k), f));
var mapErrorCause = dual(2, (self, f) => catchAllCause2(self, (cause2) => failCause2(f(cause2))));
var mapError2 = dual(2, (self, f) => catchAll2(self, (error) => fail3(f(error))));
var orDie2 = (self) => catchAll2(self, die3);
var orElseSucceed = dual(2, (self, f) => catchAll2(self, (_) => sync2(f)));
var ignore = (self) => matchEffect2(self, {
  onFailure: (_) => void_2,
  onSuccess: (_) => void_2
});
var ignoreLogged = (self) => matchEffect2(self, {
  // eslint-disable-next-line no-console
  onFailure: (error) => sync2(() => console.error(error)),
  onSuccess: (_) => void_2
});
var option = (self) => match7(self, {
  onFailure: none2,
  onSuccess: some2
});
var either2 = (self) => match7(self, {
  onFailure: left,
  onSuccess: right
});
var retry = dual((args2) => isMicro(args2[0]), (self, options) => repeatExit(self, {
  ...options,
  while: (exit3) => exit3._tag === "Failure" && exit3.cause._tag === "Fail" && (options?.while === void 0 || options.while(exit3.cause.error))
}));
var withTrace = function() {
  const prevLimit = globalThis.Error.stackTraceLimit;
  globalThis.Error.stackTraceLimit = 2;
  const error = new globalThis.Error();
  globalThis.Error.stackTraceLimit = prevLimit;
  function generate(name, cause2) {
    const stack = error.stack;
    if (!stack) {
      return cause2;
    }
    const line = stack.split("\n")[2]?.trim().replace(/^at /, "");
    if (!line) {
      return cause2;
    }
    const lineMatch = line.match(/\((.*)\)$/);
    return causeWithTrace(cause2, `at ${name} (${lineMatch ? lineMatch[1] : line})`);
  }
  const f = (name) => (self) => onError2(self, (cause2) => failCause2(generate(name, cause2)));
  if (arguments.length === 2) {
    return f(arguments[1])(arguments[0]);
  }
  return f(arguments[0]);
};
var matchCauseEffect2 = dual(2, (self, options) => {
  const primitive = Object.create(OnSuccessAndFailureProto);
  primitive[args] = self;
  primitive[successCont] = options.onSuccess;
  primitive[failureCont] = options.onFailure;
  return primitive;
});
var OnSuccessAndFailureProto = makePrimitiveProto({
  op: "OnSuccessAndFailure",
  eval(fiber) {
    fiber._stack.push(this);
    return this[args];
  }
});
var matchCause2 = dual(2, (self, options) => matchCauseEffect2(self, {
  onFailure: (cause2) => sync2(() => options.onFailure(cause2)),
  onSuccess: (value2) => sync2(() => options.onSuccess(value2))
}));
var matchEffect2 = dual(2, (self, options) => matchCauseEffect2(self, {
  onFailure: (cause2) => cause2._tag === "Fail" ? options.onFailure(cause2.error) : failCause2(cause2),
  onSuccess: options.onSuccess
}));
var match7 = dual(2, (self, options) => matchEffect2(self, {
  onFailure: (error) => sync2(() => options.onFailure(error)),
  onSuccess: (value2) => sync2(() => options.onSuccess(value2))
}));
var sleep2 = (millis2) => async((resume2) => {
  const timeout3 = setTimeout(() => {
    resume2(void_2);
  }, millis2);
  return sync2(() => {
    clearTimeout(timeout3);
  });
});
var delay = dual(2, (self, millis2) => andThen3(sleep2(millis2), self));
var timeoutOrElse = dual(2, (self, options) => raceFirst(self, andThen3(interruptible3(sleep2(options.duration)), options.onTimeout)));
var timeout = dual(2, (self, millis2) => timeoutOrElse(self, {
  duration: millis2,
  onTimeout: () => fail3(new TimeoutException2())
}));
var timeoutOption = dual(2, (self, millis2) => raceFirst(asSome(self), as3(interruptible3(sleep2(millis2)), none2())));
var MicroScopeTypeId = Symbol.for("effect/Micro/MicroScope");
var MicroScope = GenericTag("effect/Micro/MicroScope");
var MicroScopeImpl = class _MicroScopeImpl {
  [MicroScopeTypeId];
  state = {
    _tag: "Open",
    finalizers: /* @__PURE__ */ new Set()
  };
  constructor() {
    this[MicroScopeTypeId] = MicroScopeTypeId;
  }
  unsafeAddFinalizer(finalizer) {
    if (this.state._tag === "Open") {
      this.state.finalizers.add(finalizer);
    }
  }
  addFinalizer(finalizer) {
    return suspend2(() => {
      if (this.state._tag === "Open") {
        this.state.finalizers.add(finalizer);
        return void_2;
      }
      return finalizer(this.state.exit);
    });
  }
  unsafeRemoveFinalizer(finalizer) {
    if (this.state._tag === "Open") {
      this.state.finalizers.delete(finalizer);
    }
  }
  close(microExit) {
    return suspend2(() => {
      if (this.state._tag === "Open") {
        const finalizers = Array.from(this.state.finalizers).reverse();
        this.state = {
          _tag: "Closed",
          exit: microExit
        };
        return flatMap5(forEach2(finalizers, (finalizer) => exit2(finalizer(microExit))), exitVoidAll);
      }
      return void_2;
    });
  }
  get fork() {
    return sync2(() => {
      const newScope = new _MicroScopeImpl();
      if (this.state._tag === "Closed") {
        newScope.state = this.state;
        return newScope;
      }
      function fin(exit3) {
        return newScope.close(exit3);
      }
      this.state.finalizers.add(fin);
      newScope.unsafeAddFinalizer((_) => sync2(() => this.unsafeRemoveFinalizer(fin)));
      return newScope;
    });
  }
};
var scopeMake = sync2(() => new MicroScopeImpl());
var scopeUnsafeMake = () => new MicroScopeImpl();
var scope = service(MicroScope);
var provideScope = dual(2, (self, scope4) => provideService(self, MicroScope, scope4));
var scoped = (self) => suspend2(() => {
  const scope4 = new MicroScopeImpl();
  return onExit2(provideService(self, MicroScope, scope4), (exit3) => scope4.close(exit3));
});
var acquireRelease = (acquire, release) => uninterruptible2(flatMap5(scope, (scope4) => tap2(acquire, (a) => scope4.addFinalizer((exit3) => release(a, exit3)))));
var addFinalizer = (finalizer) => flatMap5(scope, (scope4) => scope4.addFinalizer(finalizer));
var onExit2 = dual(2, (self, f) => uninterruptibleMask2((restore) => matchCauseEffect2(restore(self), {
  onFailure: (cause2) => flatMap5(f(exitFailCause2(cause2)), () => failCause2(cause2)),
  onSuccess: (a) => flatMap5(f(exitSucceed2(a)), () => succeed2(a))
})));
var ensuring = dual(2, (self, finalizer) => onExit2(self, (_) => finalizer));
var onExitIf = dual(3, (self, refinement, f) => onExit2(self, (exit3) => refinement(exit3) ? f(exit3) : exitVoid2));
var onError2 = dual(2, (self, f) => onExitIf(self, exitIsFailure2, (exit3) => f(exit3.cause)));
var onInterrupt2 = dual(2, (self, finalizer) => onExitIf(self, exitIsInterrupt, (_) => finalizer));
var acquireUseRelease = (acquire, use2, release) => uninterruptibleMask2((restore) => flatMap5(acquire, (a) => flatMap5(exit2(restore(use2(a))), (exit3) => andThen3(release(a, exit3), exit3))));
var interrupt3 = failCause2(causeInterrupt());
var uninterruptible2 = (self) => withMicroFiber((fiber) => {
  if (!fiber.interruptible) return self;
  fiber.interruptible = false;
  fiber._stack.push(setInterruptible(true));
  return self;
});
var setInterruptible = makePrimitive({
  op: "SetInterruptible",
  ensure(fiber) {
    fiber.interruptible = this[args];
    if (fiber._interrupted && fiber.interruptible) {
      return () => exitInterrupt2;
    }
  }
});
var interruptible3 = (self) => withMicroFiber((fiber) => {
  if (fiber.interruptible) return self;
  fiber.interruptible = true;
  fiber._stack.push(setInterruptible(false));
  if (fiber._interrupted) return exitInterrupt2;
  return self;
});
var uninterruptibleMask2 = (f) => withMicroFiber((fiber) => {
  if (!fiber.interruptible) return f(identity);
  fiber.interruptible = false;
  fiber._stack.push(setInterruptible(true));
  return f(interruptible3);
});
var all2 = (arg, options) => {
  if (Array.isArray(arg) || isIterable(arg)) {
    return forEach2(arg, identity, options);
  } else if (options?.discard) {
    return forEach2(Object.values(arg), identity, options);
  }
  return suspend2(() => {
    const out = {};
    return as3(forEach2(Object.entries(arg), ([key, effect]) => map5(effect, (value2) => {
      out[key] = value2;
    }), {
      discard: true,
      concurrency: options?.concurrency
    }), out);
  });
};
var whileLoop2 = makePrimitive({
  op: "While",
  contA(value2, fiber) {
    this[args].step(value2);
    if (this[args].while()) {
      fiber._stack.push(this);
      return this[args].body();
    }
    return exitVoid2;
  },
  eval(fiber) {
    if (this[args].while()) {
      fiber._stack.push(this);
      return this[args].body();
    }
    return exitVoid2;
  }
});
var forEach2 = (iterable, f, options) => withMicroFiber((parent) => {
  const concurrencyOption = options?.concurrency === "inherit" ? parent.getRef(CurrentConcurrency) : options?.concurrency ?? 1;
  const concurrency = concurrencyOption === "unbounded" ? Number.POSITIVE_INFINITY : Math.max(1, concurrencyOption);
  const items = fromIterable(iterable);
  let length3 = items.length;
  if (length3 === 0) {
    return options?.discard ? void_2 : succeed2([]);
  }
  const out = options?.discard ? void 0 : new Array(length3);
  let index = 0;
  if (concurrency === 1) {
    return as3(whileLoop2({
      while: () => index < items.length,
      body: () => f(items[index], index),
      step: out ? (b) => out[index++] = b : (_) => index++
    }), out);
  }
  return async((resume2) => {
    const fibers = /* @__PURE__ */ new Set();
    let result = void 0;
    let inProgress = 0;
    let doneCount = 0;
    let pumping = false;
    let interrupted3 = false;
    function pump() {
      pumping = true;
      while (inProgress < concurrency && index < length3) {
        const currentIndex = index;
        const item = items[currentIndex];
        index++;
        inProgress++;
        try {
          const child = unsafeFork(parent, f(item, currentIndex), true, true);
          fibers.add(child);
          child.addObserver((exit3) => {
            fibers.delete(child);
            if (interrupted3) {
              return;
            } else if (exit3._tag === "Failure") {
              if (result === void 0) {
                result = exit3;
                length3 = index;
                fibers.forEach((fiber) => fiber.unsafeInterrupt());
              }
            } else if (out !== void 0) {
              out[currentIndex] = exit3.value;
            }
            doneCount++;
            inProgress--;
            if (doneCount === length3) {
              resume2(result ?? succeed2(out));
            } else if (!pumping && inProgress < concurrency) {
              pump();
            }
          });
        } catch (err) {
          result = exitDie2(err);
          length3 = index;
          fibers.forEach((fiber) => fiber.unsafeInterrupt());
        }
      }
      pumping = false;
    }
    pump();
    return suspend2(() => {
      interrupted3 = true;
      index = length3;
      return fiberInterruptAll(fibers);
    });
  });
});
var filter2 = (iterable, f, options) => filterMap2(iterable, (a) => map5(f(a), (pass) => {
  pass = options?.negate ? !pass : pass;
  return pass ? some2(a) : none2();
}), options);
var filterMap2 = (iterable, f, options) => suspend2(() => {
  const out = [];
  return as3(forEach2(iterable, (a) => map5(f(a), (o) => {
    if (o._tag === "Some") {
      out.push(o.value);
    }
  }), {
    discard: true,
    concurrency: options?.concurrency
  }), out);
});
var Do = succeed2({});
var bindTo2 = bindTo(map5);
var bind2 = bind(map5, flatMap5);
var let_2 = let_(map5);
var fork = (self) => withMicroFiber((fiber) => {
  fiberMiddleware.interruptChildren ??= fiberInterruptChildren;
  return succeed2(unsafeFork(fiber, self));
});
var unsafeFork = (parent, effect, immediate = false, daemon = false) => {
  const child = new MicroFiberImpl(parent.context, parent.interruptible);
  if (!daemon) {
    parent.children().add(child);
    child.addObserver(() => parent.children().delete(child));
  }
  if (immediate) {
    child.evaluate(effect);
  } else {
    parent.getRef(CurrentScheduler).scheduleTask(() => child.evaluate(effect), 0);
  }
  return child;
};
var forkDaemon = (self) => withMicroFiber((fiber) => succeed2(unsafeFork(fiber, self, false, true)));
var forkIn = dual(2, (self, scope4) => uninterruptibleMask2((restore) => flatMap5(scope4.fork, (scope5) => tap2(restore(forkDaemon(onExit2(self, (exit3) => scope5.close(exit3)))), (fiber) => scope5.addFinalizer((_) => fiberInterrupt(fiber))))));
var forkScoped = (self) => flatMap5(scope, (scope4) => forkIn(self, scope4));
var runFork = (effect, options) => {
  const fiber = new MicroFiberImpl(CurrentScheduler.context(options?.scheduler ?? new MicroSchedulerDefault()));
  fiber.evaluate(effect);
  if (options?.signal) {
    if (options.signal.aborted) {
      fiber.unsafeInterrupt();
    } else {
      const abort = () => fiber.unsafeInterrupt();
      options.signal.addEventListener("abort", abort, {
        once: true
      });
      fiber.addObserver(() => options.signal.removeEventListener("abort", abort));
    }
  }
  return fiber;
};
var runPromiseExit = (effect, options) => new Promise((resolve, _reject) => {
  const handle = runFork(effect, options);
  handle.addObserver(resolve);
});
var runPromise = (effect, options) => runPromiseExit(effect, options).then((exit3) => {
  if (exit3._tag === "Failure") {
    throw exit3.cause;
  }
  return exit3.value;
});
var runSyncExit = (effect) => {
  const scheduler = new MicroSchedulerDefault();
  const fiber = runFork(effect, {
    scheduler
  });
  scheduler.flush();
  return fiber._exit ?? exitDie2(fiber);
};
var runSync = (effect) => {
  const exit3 = runSyncExit(effect);
  if (exit3._tag === "Failure") throw exit3.cause;
  return exit3.value;
};
var YieldableError2 = (function() {
  class YieldableError4 extends globalThis.Error {
  }
  Object.assign(YieldableError4.prototype, MicroProto, StructuralPrototype, {
    [identifier]: "Failure",
    [evaluate]() {
      return fail3(this);
    },
    toString() {
      return this.message ? `${this.name}: ${this.message}` : this.name;
    },
    toJSON() {
      return {
        ...this
      };
    },
    [NodeInspectSymbol]() {
      const stack = this.stack;
      if (stack) {
        return `${this.toString()}
${stack.split("\n").slice(1).join("\n")}`;
      }
      return this.toString();
    }
  });
  return YieldableError4;
})();
var Error3 = /* @__PURE__ */ (function() {
  return class extends YieldableError2 {
    constructor(args2) {
      super();
      if (args2) {
        Object.assign(this, args2);
      }
    }
  };
})();
var TaggedError = (tag) => {
  class Base3 extends Error3 {
    _tag = tag;
  }
  ;
  Base3.prototype.name = tag;
  return Base3;
};
var NoSuchElementException2 = class extends TaggedError("NoSuchElementException") {
};
var TimeoutException2 = class extends TaggedError("TimeoutException") {
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Readable.js
var Readable_exports = {};
__export(Readable_exports, {
  TypeId: () => TypeId3,
  isReadable: () => isReadable,
  make: () => make12,
  map: () => map6,
  mapEffect: () => mapEffect,
  unwrap: () => unwrap
});
var TypeId3 = Symbol.for("effect/Readable");
var isReadable = (u) => hasProperty(u, TypeId3);
var Proto = {
  [TypeId3]: TypeId3,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make12 = (get8) => {
  const self = Object.create(Proto);
  self.get = get8;
  return self;
};
var map6 = dual(2, (self, f) => make12(map4(self.get, f)));
var mapEffect = dual(2, (self, f) => make12(flatMap4(self.get, f)));
var unwrap = (effect) => make12(flatMap4(effect, (s) => s.get));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Ref.js
var Ref_exports = {};
__export(Ref_exports, {
  RefTypeId: () => RefTypeId2,
  get: () => get6,
  getAndSet: () => getAndSet2,
  getAndUpdate: () => getAndUpdate2,
  getAndUpdateSome: () => getAndUpdateSome2,
  make: () => make14,
  modify: () => modify2,
  modifySome: () => modifySome2,
  set: () => set4,
  setAndGet: () => setAndGet2,
  unsafeMake: () => unsafeMake5,
  update: () => update3,
  updateAndGet: () => updateAndGet2,
  updateSome: () => updateSome2,
  updateSomeAndGet: () => updateSomeAndGet2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/ref.js
var RefTypeId = Symbol.for("effect/Ref");
var refVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var RefImpl = class extends Class {
  ref;
  commit() {
    return this.get;
  }
  [RefTypeId] = refVariance;
  [TypeId3] = TypeId3;
  constructor(ref) {
    super();
    this.ref = ref;
    this.get = sync(() => get2(this.ref));
  }
  get;
  modify(f) {
    return sync(() => {
      const current = get2(this.ref);
      const [b, a] = f(current);
      if (current !== a) {
        set(a)(this.ref);
      }
      return b;
    });
  }
};
var unsafeMake4 = (value2) => new RefImpl(make5(value2));
var make13 = (value2) => sync(() => unsafeMake4(value2));
var get5 = (self) => self.get;
var set3 = dual(2, (self, value2) => self.modify(() => [void 0, value2]));
var getAndSet = dual(2, (self, value2) => self.modify((a) => [a, value2]));
var getAndUpdate = dual(2, (self, f) => self.modify((a) => [a, f(a)]));
var getAndUpdateSome = dual(2, (self, pf) => self.modify((value2) => {
  const option3 = pf(value2);
  switch (option3._tag) {
    case "None": {
      return [value2, value2];
    }
    case "Some": {
      return [value2, option3.value];
    }
  }
}));
var setAndGet = dual(2, (self, value2) => self.modify(() => [value2, value2]));
var modify = dual(2, (self, f) => self.modify(f));
var modifySome = dual(3, (self, fallback, pf) => self.modify((value2) => {
  const option3 = pf(value2);
  switch (option3._tag) {
    case "None": {
      return [fallback, value2];
    }
    case "Some": {
      return option3.value;
    }
  }
}));
var update2 = dual(2, (self, f) => self.modify((a) => [void 0, f(a)]));
var updateAndGet = dual(2, (self, f) => self.modify((a) => {
  const result = f(a);
  return [result, result];
}));
var updateSome = dual(2, (self, f) => self.modify((a) => [void 0, match(f(a), {
  onNone: () => a,
  onSome: (b) => b
})]));
var updateSomeAndGet = dual(2, (self, pf) => self.modify((value2) => {
  const option3 = pf(value2);
  switch (option3._tag) {
    case "None": {
      return [value2, value2];
    }
    case "Some": {
      return [option3.value, option3.value];
    }
  }
}));
var unsafeGet3 = (self) => get2(self.ref);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Ref.js
var RefTypeId2 = RefTypeId;
var make14 = make13;
var get6 = get5;
var getAndSet2 = getAndSet;
var getAndUpdate2 = getAndUpdate;
var getAndUpdateSome2 = getAndUpdateSome;
var modify2 = modify;
var modifySome2 = modifySome;
var set4 = set3;
var setAndGet2 = setAndGet;
var update3 = update2;
var updateAndGet2 = updateAndGet;
var updateSome2 = updateSome;
var updateSomeAndGet2 = updateSomeAndGet;
var unsafeMake5 = unsafeMake4;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Scheduler.js
var Scheduler_exports = {};
__export(Scheduler_exports, {
  ControlledScheduler: () => ControlledScheduler,
  MixedScheduler: () => MixedScheduler,
  PriorityBuckets: () => PriorityBuckets,
  SyncScheduler: () => SyncScheduler,
  currentScheduler: () => currentScheduler,
  defaultScheduler: () => defaultScheduler,
  defaultShouldYield: () => defaultShouldYield,
  make: () => make15,
  makeBatched: () => makeBatched,
  makeMatrix: () => makeMatrix,
  timer: () => timer,
  timerBatched: () => timerBatched,
  withScheduler: () => withScheduler
});
var PriorityBuckets = class {
  /**
   * @since 2.0.0
   */
  buckets = [];
  /**
   * @since 2.0.0
   */
  scheduleTask(task, priority) {
    const length3 = this.buckets.length;
    let bucket = void 0;
    let index = 0;
    for (; index < length3; index++) {
      if (this.buckets[index][0] <= priority) {
        bucket = this.buckets[index];
      } else {
        break;
      }
    }
    if (bucket && bucket[0] === priority) {
      bucket[1].push(task);
    } else if (index === length3) {
      this.buckets.push([priority, [task]]);
    } else {
      this.buckets.splice(index, 0, [priority, [task]]);
    }
  }
};
var MixedScheduler = class {
  maxNextTickBeforeTimer;
  /**
   * @since 2.0.0
   */
  running = false;
  /**
   * @since 2.0.0
   */
  tasks = new PriorityBuckets();
  constructor(maxNextTickBeforeTimer) {
    this.maxNextTickBeforeTimer = maxNextTickBeforeTimer;
  }
  /**
   * @since 2.0.0
   */
  starveInternal(depth) {
    const tasks = this.tasks.buckets;
    this.tasks.buckets = [];
    for (const [_, toRun] of tasks) {
      for (let i = 0; i < toRun.length; i++) {
        toRun[i]();
      }
    }
    if (this.tasks.buckets.length === 0) {
      this.running = false;
    } else {
      this.starve(depth);
    }
  }
  /**
   * @since 2.0.0
   */
  starve(depth = 0) {
    if (depth >= this.maxNextTickBeforeTimer) {
      setTimeout(() => this.starveInternal(0), 0);
    } else {
      Promise.resolve(void 0).then(() => this.starveInternal(depth + 1));
    }
  }
  /**
   * @since 2.0.0
   */
  shouldYield(fiber) {
    return fiber.currentOpCount > fiber.getFiberRef(currentMaxOpsBeforeYield) ? fiber.getFiberRef(currentSchedulingPriority) : false;
  }
  /**
   * @since 2.0.0
   */
  scheduleTask(task, priority) {
    this.tasks.scheduleTask(task, priority);
    if (!this.running) {
      this.running = true;
      this.starve();
    }
  }
};
var defaultScheduler = globalValue(Symbol.for("effect/Scheduler/defaultScheduler"), () => new MixedScheduler(2048));
var SyncScheduler = class {
  /**
   * @since 2.0.0
   */
  tasks = new PriorityBuckets();
  /**
   * @since 2.0.0
   */
  deferred = false;
  /**
   * @since 2.0.0
   */
  scheduleTask(task, priority) {
    if (this.deferred) {
      defaultScheduler.scheduleTask(task, priority);
    } else {
      this.tasks.scheduleTask(task, priority);
    }
  }
  /**
   * @since 2.0.0
   */
  shouldYield(fiber) {
    return fiber.currentOpCount > fiber.getFiberRef(currentMaxOpsBeforeYield) ? fiber.getFiberRef(currentSchedulingPriority) : false;
  }
  /**
   * @since 2.0.0
   */
  flush() {
    while (this.tasks.buckets.length > 0) {
      const tasks = this.tasks.buckets;
      this.tasks.buckets = [];
      for (const [_, toRun] of tasks) {
        for (let i = 0; i < toRun.length; i++) {
          toRun[i]();
        }
      }
    }
    this.deferred = true;
  }
};
var ControlledScheduler = class {
  /**
   * @since 2.0.0
   */
  tasks = new PriorityBuckets();
  /**
   * @since 2.0.0
   */
  deferred = false;
  /**
   * @since 2.0.0
   */
  scheduleTask(task, priority) {
    if (this.deferred) {
      defaultScheduler.scheduleTask(task, priority);
    } else {
      this.tasks.scheduleTask(task, priority);
    }
  }
  /**
   * @since 2.0.0
   */
  shouldYield(fiber) {
    return fiber.currentOpCount > fiber.getFiberRef(currentMaxOpsBeforeYield) ? fiber.getFiberRef(currentSchedulingPriority) : false;
  }
  /**
   * @since 2.0.0
   */
  step() {
    const tasks = this.tasks.buckets;
    this.tasks.buckets = [];
    for (const [_, toRun] of tasks) {
      for (let i = 0; i < toRun.length; i++) {
        toRun[i]();
      }
    }
  }
};
var makeMatrix = (...record) => {
  const index = record.sort(([p0], [p1]) => p0 < p1 ? -1 : p0 > p1 ? 1 : 0);
  return {
    shouldYield(fiber) {
      for (const scheduler of record) {
        const priority = scheduler[1].shouldYield(fiber);
        if (priority !== false) {
          return priority;
        }
      }
      return false;
    },
    scheduleTask(task, priority) {
      let scheduler = void 0;
      for (const i of index) {
        if (priority >= i[0]) {
          scheduler = i[1];
        } else {
          return (scheduler ?? defaultScheduler).scheduleTask(task, priority);
        }
      }
      return (scheduler ?? defaultScheduler).scheduleTask(task, priority);
    }
  };
};
var defaultShouldYield = (fiber) => {
  return fiber.currentOpCount > fiber.getFiberRef(currentMaxOpsBeforeYield) ? fiber.getFiberRef(currentSchedulingPriority) : false;
};
var make15 = (scheduleTask, shouldYield = defaultShouldYield) => ({
  scheduleTask,
  shouldYield
});
var makeBatched = (callback, shouldYield = defaultShouldYield) => {
  let running3 = false;
  const tasks = new PriorityBuckets();
  const starveInternal = () => {
    const tasksToRun = tasks.buckets;
    tasks.buckets = [];
    for (const [_, toRun] of tasksToRun) {
      for (let i = 0; i < toRun.length; i++) {
        toRun[i]();
      }
    }
    if (tasks.buckets.length === 0) {
      running3 = false;
    } else {
      starve();
    }
  };
  const starve = () => callback(starveInternal);
  return make15((task, priority) => {
    tasks.scheduleTask(task, priority);
    if (!running3) {
      running3 = true;
      starve();
    }
  }, shouldYield);
};
var timer = (ms, shouldYield = defaultShouldYield) => make15((task) => setTimeout(task, ms), shouldYield);
var timerBatched = (ms, shouldYield = defaultShouldYield) => makeBatched((task) => setTimeout(task, ms), shouldYield);
var currentScheduler = globalValue(Symbol.for("effect/FiberRef/currentScheduler"), () => fiberRefUnsafeMake(defaultScheduler));
var withScheduler = dual(2, (self, scheduler) => fiberRefLocally(self, currentScheduler, scheduler));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Clock.js
var Clock_exports = {};
__export(Clock_exports, {
  Clock: () => Clock,
  ClockTypeId: () => ClockTypeId2,
  clockWith: () => clockWith2,
  currentTimeMillis: () => currentTimeMillis2,
  currentTimeNanos: () => currentTimeNanos2,
  make: () => make16,
  sleep: () => sleep3
});
var ClockTypeId2 = ClockTypeId;
var make16 = make8;
var sleep3 = sleep;
var currentTimeMillis2 = currentTimeMillis;
var currentTimeNanos2 = currentTimeNanos;
var clockWith2 = clockWith;
var Clock = clockTag;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/LogSpan.js
var LogSpan_exports = {};
__export(LogSpan_exports, {
  make: () => make18,
  render: () => render2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/logSpan.js
var make17 = (label, startTime) => ({
  label,
  startTime
});
var formatLabel = (key) => key.replace(/[\s="]/g, "_");
var render = (now) => (self) => {
  const label = formatLabel(self.label);
  return `${label}=${now - self.startTime}ms`;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/LogSpan.js
var make18 = make17;
var render2 = render;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Tracer.js
var Tracer_exports = {};
__export(Tracer_exports, {
  DisablePropagation: () => DisablePropagation2,
  ParentSpan: () => ParentSpan,
  Tracer: () => Tracer,
  TracerTypeId: () => TracerTypeId2,
  externalSpan: () => externalSpan2,
  make: () => make19,
  tracerWith: () => tracerWith2
});
var TracerTypeId2 = TracerTypeId;
var ParentSpan = spanTag;
var Tracer = tracerTag;
var make19 = make10;
var externalSpan2 = externalSpan;
var tracerWith2 = tracerWith;
var DisablePropagation2 = DisablePropagation;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/label.js
var MetricLabelSymbolKey = "effect/MetricLabel";
var MetricLabelTypeId = Symbol.for(MetricLabelSymbolKey);
var MetricLabelImpl = class {
  key;
  value;
  [MetricLabelTypeId] = MetricLabelTypeId;
  _hash;
  constructor(key, value2) {
    this.key = key;
    this.value = value2;
    this._hash = string(MetricLabelSymbolKey + this.key + this.value);
  }
  [symbol]() {
    return this._hash;
  }
  [symbol2](that) {
    return isMetricLabel(that) && this.key === that.key && this.value === that.value;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make20 = (key, value2) => {
  return new MetricLabelImpl(key, value2);
};
var isMetricLabel = (u) => hasProperty(u, MetricLabelTypeId);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/core-effect.js
var annotateLogs = dual((args2) => isEffect(args2[0]), function() {
  const args2 = arguments;
  return fiberRefLocallyWith(args2[0], currentLogAnnotations, typeof args2[1] === "string" ? set2(args2[1], args2[2]) : (annotations) => Object.entries(args2[1]).reduce((acc, [key, value2]) => set2(acc, key, value2), annotations));
});
var asSome2 = (self) => map4(self, some2);
var asSomeError = (self) => mapError(self, some2);
var try_2 = (arg) => {
  let evaluate2;
  let onFailure = void 0;
  if (typeof arg === "function") {
    evaluate2 = arg;
  } else {
    evaluate2 = arg.try;
    onFailure = arg.catch;
  }
  return suspend(() => {
    try {
      return succeed(internalCall(evaluate2));
    } catch (error) {
      return fail2(onFailure ? internalCall(() => onFailure(error)) : new UnknownException(error, "An unknown error occurred in Effect.try"));
    }
  });
};
var _catch = dual(3, (self, tag, options) => catchAll(self, (e) => {
  if (hasProperty(e, tag) && e[tag] === options.failure) {
    return options.onFailure(e);
  }
  return fail2(e);
}));
var catchAllDefect2 = dual(2, (self, f) => catchAllCause(self, (cause2) => {
  const option3 = find(cause2, (_) => isDieType(_) ? some2(_) : none2());
  switch (option3._tag) {
    case "None": {
      return failCause(cause2);
    }
    case "Some": {
      return f(option3.value.defect);
    }
  }
}));
var catchSomeCause = dual(2, (self, f) => matchCauseEffect(self, {
  onFailure: (cause2) => {
    const option3 = f(cause2);
    switch (option3._tag) {
      case "None": {
        return failCause(cause2);
      }
      case "Some": {
        return option3.value;
      }
    }
  },
  onSuccess: succeed
}));
var catchSomeDefect = dual(2, (self, pf) => catchAllCause(self, (cause2) => {
  const option3 = find(cause2, (_) => isDieType(_) ? some2(_) : none2());
  switch (option3._tag) {
    case "None": {
      return failCause(cause2);
    }
    case "Some": {
      const optionEffect = pf(option3.value.defect);
      return optionEffect._tag === "Some" ? optionEffect.value : failCause(cause2);
    }
  }
}));
var catchTag2 = dual((args2) => isEffect(args2[0]), (self, ...args2) => {
  const f = args2[args2.length - 1];
  let predicate;
  if (args2.length === 2) {
    predicate = isTagged(args2[0]);
  } else {
    predicate = (e) => {
      const tag = hasProperty(e, "_tag") ? e["_tag"] : void 0;
      if (!tag) return false;
      for (let i = 0; i < args2.length - 1; i++) {
        if (args2[i] === tag) return true;
      }
      return false;
    };
  }
  return catchIf(self, predicate, f);
});
var catchTags = dual(2, (self, cases) => {
  let keys4;
  return catchIf(self, (e) => {
    keys4 ??= Object.keys(cases);
    return hasProperty(e, "_tag") && isString(e["_tag"]) && keys4.includes(e["_tag"]);
  }, (e) => cases[e["_tag"]](e));
});
var cause = (self) => matchCause(self, {
  onFailure: identity,
  onSuccess: () => empty9
});
var clockWith3 = clockWith2;
var clock = clockWith3(succeed);
var delay2 = dual(2, (self, duration) => zipRight(sleep3(duration), self));
var descriptorWith = (f) => withFiberRuntime((state, status3) => f({
  id: state.id(),
  status: status3,
  interruptors: interruptors(state.getFiberRef(currentInterruptedCause))
}));
var allowInterrupt = descriptorWith((descriptor2) => size(descriptor2.interruptors) > 0 ? interrupt2 : void_);
var descriptor = descriptorWith(succeed);
var diffFiberRefs = (self) => summarized(self, fiberRefs3, diff2);
var diffFiberRefsAndRuntimeFlags = (self) => summarized(self, zip3(fiberRefs3, runtimeFlags), ([refs, flags], [refsNew, flagsNew]) => [diff2(refs, refsNew), diff(flags, flagsNew)]);
var Do2 = succeed({});
var bind3 = bind(map4, flatMap4);
var bindTo3 = bindTo(map4);
var let_3 = let_(map4);
var dropUntil = dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let next2;
  let dropping = succeed(false);
  let i = 0;
  while ((next2 = iterator.next()) && !next2.done) {
    const a = next2.value;
    const index = i++;
    dropping = flatMap4(dropping, (bool) => {
      if (bool) {
        builder.push(a);
        return succeed(true);
      }
      return predicate(a, index);
    });
  }
  return map4(dropping, () => builder);
}));
var dropWhile = dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let next2;
  let dropping = succeed(true);
  let i = 0;
  while ((next2 = iterator.next()) && !next2.done) {
    const a = next2.value;
    const index = i++;
    dropping = flatMap4(dropping, (d) => map4(d ? predicate(a, index) : succeed(false), (b) => {
      if (!b) {
        builder.push(a);
      }
      return b;
    }));
  }
  return map4(dropping, () => builder);
}));
var contextWith = (f) => map4(context(), f);
var eventually = (self) => orElse(self, () => flatMap4(yieldNow(), () => eventually(self)));
var filterMap3 = dual(2, (elements, pf) => map4(forEachSequential(elements, identity), filterMap(pf)));
var filterOrDie = dual(3, (self, predicate, orDieWith) => filterOrElse(self, predicate, (a) => dieSync(() => orDieWith(a))));
var filterOrDieMessage = dual(3, (self, predicate, message) => filterOrElse(self, predicate, () => dieMessage(message)));
var filterOrElse = dual(3, (self, predicate, orElse6) => flatMap4(self, (a) => predicate(a) ? succeed(a) : orElse6(a)));
var liftPredicate = dual(3, (self, predicate, orFailWith) => suspend(() => predicate(self) ? succeed(self) : fail2(orFailWith(self))));
var filterOrFail2 = dual((args2) => isEffect(args2[0]), (self, predicate, orFailWith) => filterOrElse(self, predicate, (a) => orFailWith === void 0 ? fail2(new NoSuchElementException()) : failSync(() => orFailWith(a))));
var findFirst = dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const next2 = iterator.next();
  if (!next2.done) {
    return findLoop(iterator, 0, predicate, next2.value);
  }
  return succeed(none2());
}));
var findLoop = (iterator, index, f, value2) => flatMap4(f(value2, index), (result) => {
  if (result) {
    return succeed(some2(value2));
  }
  const next2 = iterator.next();
  if (!next2.done) {
    return findLoop(iterator, index + 1, f, next2.value);
  }
  return succeed(none2());
});
var firstSuccessOf = (effects) => suspend(() => {
  const list = fromIterable2(effects);
  if (!isNonEmpty(list)) {
    return dieSync(() => new IllegalArgumentException(`Received an empty collection of effects`));
  }
  return pipe(tailNonEmpty2(list), reduce2(headNonEmpty2(list), (left2, right2) => orElse(left2, () => right2)));
});
var flipWith = dual(2, (self, f) => flip(f(flip(self))));
var match8 = dual(2, (self, options) => matchEffect(self, {
  onFailure: (e) => succeed(options.onFailure(e)),
  onSuccess: (a) => succeed(options.onSuccess(a))
}));
var every2 = dual(2, (elements, predicate) => suspend(() => forAllLoop(elements[Symbol.iterator](), 0, predicate)));
var forAllLoop = (iterator, index, f) => {
  const next2 = iterator.next();
  return next2.done ? succeed(true) : flatMap4(f(next2.value, index), (b) => b ? forAllLoop(iterator, index + 1, f) : succeed(b));
};
var forever2 = (self) => {
  const loop2 = flatMap4(flatMap4(self, () => yieldNow()), () => loop2);
  return loop2;
};
var fiberRefs3 = withFiberRuntime((state) => succeed(state.getFiberRefs()));
var head3 = (self) => flatMap4(self, (as6) => {
  const iterator = as6[Symbol.iterator]();
  const next2 = iterator.next();
  if (next2.done) {
    return fail2(new NoSuchElementException());
  }
  return succeed(next2.value);
});
var ignore2 = (self) => match8(self, {
  onFailure: constVoid,
  onSuccess: constVoid
});
var ignoreLogged2 = (self) => matchCauseEffect(self, {
  onFailure: (cause2) => logDebug(cause2, "An error was silently ignored because it is not anticipated to be useful"),
  onSuccess: () => void_
});
var inheritFiberRefs = (childFiberRefs) => updateFiberRefs((parentFiberId, parentFiberRefs) => joinAs2(parentFiberRefs, parentFiberId, childFiberRefs));
var isFailure2 = (self) => match8(self, {
  onFailure: constTrue,
  onSuccess: constFalse
});
var isSuccess = (self) => match8(self, {
  onFailure: constFalse,
  onSuccess: constTrue
});
var iterate = (initial, options) => suspend(() => {
  if (options.while(initial)) {
    return flatMap4(options.body(initial), (z2) => iterate(z2, options));
  }
  return succeed(initial);
});
var logWithLevel = (level) => (...message) => {
  const levelOption = fromNullable(level);
  let cause2 = void 0;
  for (let i = 0, len = message.length; i < len; i++) {
    const msg = message[i];
    if (isCause(msg)) {
      if (cause2 !== void 0) {
        cause2 = sequential(cause2, msg);
      } else {
        cause2 = msg;
      }
      message = [...message.slice(0, i), ...message.slice(i + 1)];
      i--;
    }
  }
  if (cause2 === void 0) {
    cause2 = empty9;
  }
  return withFiberRuntime((fiberState) => {
    fiberState.log(message, cause2, levelOption);
    return void_;
  });
};
var log = logWithLevel();
var logTrace = logWithLevel(Trace);
var logDebug = logWithLevel(Debug);
var logInfo = logWithLevel(Info);
var logWarning = logWithLevel(Warning);
var logError = logWithLevel(Error2);
var logFatal = logWithLevel(Fatal);
var withLogSpan = dual(2, (effect, label) => flatMap4(currentTimeMillis2, (now) => fiberRefLocallyWith(effect, currentLogSpan, prepend3(make18(label, now)))));
var logAnnotations = fiberRefGet(currentLogAnnotations);
var loop = (initial, options) => options.discard ? loopDiscard(initial, options.while, options.step, options.body) : map4(loopInternal(initial, options.while, options.step, options.body), fromIterable);
var loopInternal = (initial, cont, inc, body) => suspend(() => cont(initial) ? flatMap4(body(initial), (a) => map4(loopInternal(inc(initial), cont, inc, body), prepend3(a))) : sync(() => empty6()));
var loopDiscard = (initial, cont, inc, body) => suspend(() => cont(initial) ? flatMap4(body(initial), () => loopDiscard(inc(initial), cont, inc, body)) : void_);
var mapAccum = dual(3, (elements, initial, f) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let result = succeed(initial);
  let next2;
  let i = 0;
  while (!(next2 = iterator.next()).done) {
    const index = i++;
    const value2 = next2.value;
    result = flatMap4(result, (state) => map4(f(state, value2, index), ([z, b]) => {
      builder.push(b);
      return z;
    }));
  }
  return map4(result, (z) => [z, builder]);
}));
var mapErrorCause2 = dual(2, (self, f) => matchCauseEffect(self, {
  onFailure: (c) => failCauseSync(() => f(c)),
  onSuccess: succeed
}));
var memoize = (self) => pipe(deferredMake(), flatMap4((deferred) => pipe(diffFiberRefsAndRuntimeFlags(self), intoDeferred(deferred), once, map4((complete4) => zipRight(complete4, pipe(deferredAwait(deferred), flatMap4(([patch7, a]) => as2(zip3(patchFiberRefs(patch7[0]), updateRuntimeFlags(patch7[1])), a))))))));
var merge3 = (self) => matchEffect(self, {
  onFailure: (e) => succeed(e),
  onSuccess: succeed
});
var negate2 = (self) => map4(self, (b) => !b);
var none6 = (self) => flatMap4(self, (option3) => {
  switch (option3._tag) {
    case "None":
      return void_;
    case "Some":
      return fail2(new NoSuchElementException());
  }
});
var once = (self) => map4(make14(true), (ref) => asVoid(whenEffect(self, getAndSet2(ref, false))));
var option2 = (self) => matchEffect(self, {
  onFailure: () => succeed(none2()),
  onSuccess: (a) => succeed(some2(a))
});
var orElseFail = dual(2, (self, evaluate2) => orElse(self, () => failSync(evaluate2)));
var orElseSucceed2 = dual(2, (self, evaluate2) => orElse(self, () => sync(evaluate2)));
var parallelErrors = (self) => matchCauseEffect(self, {
  onFailure: (cause2) => {
    const errors = fromIterable(failures(cause2));
    return errors.length === 0 ? failCause(cause2) : fail2(errors);
  },
  onSuccess: succeed
});
var patchFiberRefs = (patch7) => updateFiberRefs((fiberId2, fiberRefs4) => pipe(patch7, patch3(fiberId2, fiberRefs4)));
var promise2 = (evaluate2) => evaluate2.length >= 1 ? async_((resolve, signal) => {
  try {
    evaluate2(signal).then((a) => resolve(succeed(a)), (e) => resolve(die2(e)));
  } catch (e) {
    resolve(die2(e));
  }
}) : async_((resolve) => {
  try {
    ;
    evaluate2().then((a) => resolve(succeed(a)), (e) => resolve(die2(e)));
  } catch (e) {
    resolve(die2(e));
  }
});
var provideService2 = dual(3, (self, tag, service3) => contextWithEffect((env) => provideContext(self, add(env, tag, service3))));
var provideServiceEffect2 = dual(3, (self, tag, effect) => contextWithEffect((env) => flatMap4(effect, (service3) => provideContext(self, pipe(env, add(tag, service3))))));
var random = randomWith(succeed);
var reduce5 = dual(3, (elements, zero2, f) => fromIterable(elements).reduce((acc, el, i) => flatMap4(acc, (a) => f(a, el, i)), succeed(zero2)));
var reduceRight2 = dual(3, (elements, zero2, f) => fromIterable(elements).reduceRight((acc, el, i) => flatMap4(acc, (a) => f(el, a, i)), succeed(zero2)));
var reduceWhile = dual(3, (elements, zero2, options) => flatMap4(sync(() => elements[Symbol.iterator]()), (iterator) => reduceWhileLoop(iterator, 0, zero2, options.while, options.body)));
var reduceWhileLoop = (iterator, index, state, predicate, f) => {
  const next2 = iterator.next();
  if (!next2.done && predicate(state)) {
    return flatMap4(f(state, next2.value, index), (nextState) => reduceWhileLoop(iterator, index + 1, nextState, predicate, f));
  }
  return succeed(state);
};
var repeatN = dual(2, (self, n) => suspend(() => repeatNLoop(self, n)));
var repeatNLoop = (self, n) => flatMap4(self, (a) => n <= 0 ? succeed(a) : zipRight(yieldNow(), repeatNLoop(self, n - 1)));
var sandbox2 = (self) => matchCauseEffect(self, {
  onFailure: fail2,
  onSuccess: succeed
});
var setFiberRefs = (fiberRefs4) => suspend(() => setAll2(fiberRefs4));
var sleep4 = sleep3;
var succeedNone2 = succeed(none2());
var succeedSome2 = (value2) => succeed(some2(value2));
var summarized = dual(3, (self, summary6, f) => flatMap4(summary6, (start3) => flatMap4(self, (value2) => map4(summary6, (end3) => [f(start3, end3), value2]))));
var tagMetrics = dual((args2) => isEffect(args2[0]), function() {
  return labelMetrics(arguments[0], typeof arguments[1] === "string" ? [make20(arguments[1], arguments[2])] : Object.entries(arguments[1]).map(([k, v]) => make20(k, v)));
});
var labelMetrics = dual(2, (self, labels) => fiberRefLocallyWith(self, currentMetricLabels, (old) => union(old, labels)));
var takeUntil = dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let next2;
  let effect = succeed(false);
  let i = 0;
  while ((next2 = iterator.next()) && !next2.done) {
    const a = next2.value;
    const index = i++;
    effect = flatMap4(effect, (bool) => {
      if (bool) {
        return succeed(true);
      }
      builder.push(a);
      return predicate(a, index);
    });
  }
  return map4(effect, () => builder);
}));
var takeWhile = dual(2, (elements, predicate) => suspend(() => {
  const iterator = elements[Symbol.iterator]();
  const builder = [];
  let next2;
  let taking = succeed(true);
  let i = 0;
  while ((next2 = iterator.next()) && !next2.done) {
    const a = next2.value;
    const index = i++;
    taking = flatMap4(taking, (taking2) => pipe(taking2 ? predicate(a, index) : succeed(false), map4((bool) => {
      if (bool) {
        builder.push(a);
      }
      return bool;
    })));
  }
  return map4(taking, () => builder);
}));
var tapBoth = dual(2, (self, {
  onFailure,
  onSuccess
}) => matchCauseEffect(self, {
  onFailure: (cause2) => {
    const either3 = failureOrCause(cause2);
    switch (either3._tag) {
      case "Left": {
        return zipRight(onFailure(either3.left), failCause(cause2));
      }
      case "Right": {
        return failCause(cause2);
      }
    }
  },
  onSuccess: (a) => as2(onSuccess(a), a)
}));
var tapDefect2 = dual(2, (self, f) => catchAllCause(self, (cause2) => match(keepDefects(cause2), {
  onNone: () => failCause(cause2),
  onSome: (a) => zipRight(f(a), failCause(cause2))
})));
var tapError2 = dual(2, (self, f) => matchCauseEffect(self, {
  onFailure: (cause2) => {
    const either3 = failureOrCause(cause2);
    switch (either3._tag) {
      case "Left":
        return zipRight(f(either3.left), failCause(cause2));
      case "Right":
        return failCause(cause2);
    }
  },
  onSuccess: succeed
}));
var tapErrorTag = dual(3, (self, k, f) => tapError2(self, (e) => {
  if (isTagged(e, k)) {
    return f(e);
  }
  return void_;
}));
var tapErrorCause2 = dual(2, (self, f) => matchCauseEffect(self, {
  onFailure: (cause2) => zipRight(f(cause2), failCause(cause2)),
  onSuccess: succeed
}));
var timed = (self) => timedWith(self, currentTimeNanos2);
var timedWith = dual(2, (self, nanos2) => summarized(self, nanos2, (start3, end3) => nanos(end3 - start3)));
var tracerWith3 = tracerWith2;
var tracer = tracerWith3(succeed);
var tryPromise2 = (arg) => {
  let evaluate2;
  let catcher = void 0;
  if (typeof arg === "function") {
    evaluate2 = arg;
  } else {
    evaluate2 = arg.try;
    catcher = arg.catch;
  }
  const fail11 = (e) => catcher ? failSync(() => catcher(e)) : fail2(new UnknownException(e, "An unknown error occurred in Effect.tryPromise"));
  if (evaluate2.length >= 1) {
    return async_((resolve, signal) => {
      try {
        evaluate2(signal).then((a) => resolve(succeed(a)), (e) => resolve(fail11(e)));
      } catch (e) {
        resolve(fail11(e));
      }
    });
  }
  return async_((resolve) => {
    try {
      evaluate2().then((a) => resolve(succeed(a)), (e) => resolve(fail11(e)));
    } catch (e) {
      resolve(fail11(e));
    }
  });
};
var tryMap = dual(2, (self, options) => flatMap4(self, (a) => try_2({
  try: () => options.try(a),
  catch: options.catch
})));
var tryMapPromise = dual(2, (self, options) => flatMap4(self, (a) => tryPromise2({
  try: options.try.length >= 1 ? (signal) => options.try(a, signal) : () => options.try(a),
  catch: options.catch
})));
var unless = dual(2, (self, condition) => suspend(() => condition() ? succeedNone2 : asSome2(self)));
var unlessEffect = dual(2, (self, condition) => flatMap4(condition, (b) => b ? succeedNone2 : asSome2(self)));
var unsandbox = (self) => mapErrorCause2(self, flatten3);
var updateFiberRefs = (f) => withFiberRuntime((state) => {
  state.setFiberRefs(f(state.id(), state.getFiberRefs()));
  return void_;
});
var updateService2 = dual(3, (self, tag, f) => mapInputContext(self, (context4) => add(context4, tag, f(unsafeGet2(context4, tag)))));
var when2 = dual(2, (self, condition) => suspend(() => condition() ? map4(self, some2) : succeed(none2())));
var whenFiberRef = dual(3, (self, fiberRef, predicate) => flatMap4(fiberRefGet(fiberRef), (s) => predicate(s) ? map4(self, (a) => [s, some2(a)]) : succeed([s, none2()])));
var whenRef = dual(3, (self, ref, predicate) => flatMap4(get6(ref), (s) => predicate(s) ? map4(self, (a) => [s, some2(a)]) : succeed([s, none2()])));
var withMetric = dual(2, (self, metric) => metric(self));
var serviceFunctionEffect = (getService, f) => (...args2) => flatMap4(getService, (a) => f(a)(...args2));
var serviceFunction = (getService, f) => (...args2) => map4(getService, (a) => f(a)(...args2));
var serviceFunctions = (getService) => new Proxy({}, {
  get(_target, prop, _receiver) {
    return (...args2) => flatMap4(getService, (s) => s[prop](...args2));
  }
});
var serviceConstants = (getService) => new Proxy({}, {
  get(_target, prop, _receiver) {
    return flatMap4(getService, (s) => isEffect(s[prop]) ? s[prop] : succeed(s[prop]));
  }
});
var serviceMembers = (getService) => ({
  functions: serviceFunctions(getService),
  constants: serviceConstants(getService)
});
var serviceOption2 = (tag) => map4(context(), getOption(tag));
var serviceOptional = (tag) => flatMap4(context(), getOption(tag));
var annotateCurrentSpan = function() {
  const args2 = arguments;
  return ignore2(flatMap4(currentSpan, (span2) => sync(() => {
    if (typeof args2[0] === "string") {
      span2.attribute(args2[0], args2[1]);
    } else {
      for (const key in args2[0]) {
        span2.attribute(key, args2[0][key]);
      }
    }
  })));
};
var linkSpanCurrent = function() {
  const args2 = arguments;
  const links = Array.isArray(args2[0]) ? args2[0] : [{
    _tag: "SpanLink",
    span: args2[0],
    attributes: args2[1] ?? {}
  }];
  return ignore2(flatMap4(currentSpan, (span2) => sync(() => span2.addLinks(links))));
};
var annotateSpans = dual((args2) => isEffect(args2[0]), function() {
  const args2 = arguments;
  return fiberRefLocallyWith(args2[0], currentTracerSpanAnnotations, typeof args2[1] === "string" ? set2(args2[1], args2[2]) : (annotations) => Object.entries(args2[1]).reduce((acc, [key, value2]) => set2(acc, key, value2), annotations));
});
var currentParentSpan = serviceOptional(spanTag);
var currentSpan = flatMap4(context(), (context4) => {
  const span2 = context4.unsafeMap.get(spanTag.key);
  return span2 !== void 0 && span2._tag === "Span" ? succeed(span2) : fail2(new NoSuchElementException());
});
var linkSpans = dual((args2) => isEffect(args2[0]), (self, span2, attributes) => fiberRefLocallyWith(self, currentTracerSpanLinks, append2({
  _tag: "SpanLink",
  span: span2,
  attributes: attributes ?? {}
})));
var bigint0 = BigInt(0);
var filterDisablePropagation = flatMap((span2) => get(span2.context, DisablePropagation) ? span2._tag === "Span" ? filterDisablePropagation(span2.parent) : none2() : some2(span2));
var unsafeMakeSpan = (fiber, name, options) => {
  const disablePropagation = !fiber.getFiberRef(currentTracerEnabled) || options.context && get(options.context, DisablePropagation);
  const context4 = fiber.getFiberRef(currentContext);
  const parent = options.parent ? some2(options.parent) : options.root ? none2() : filterDisablePropagation(getOption(context4, spanTag));
  let span2;
  if (disablePropagation) {
    span2 = noopSpan({
      name,
      parent,
      context: add(options.context ?? empty3(), DisablePropagation, true)
    });
  } else {
    const services = fiber.getFiberRef(currentServices);
    const tracer2 = get(services, tracerTag);
    const clock2 = get(services, Clock);
    const timingEnabled = fiber.getFiberRef(currentTracerTimingEnabled);
    const fiberRefs4 = fiber.getFiberRefs();
    const annotationsFromEnv = get4(fiberRefs4, currentTracerSpanAnnotations);
    const linksFromEnv = get4(fiberRefs4, currentTracerSpanLinks);
    const links = linksFromEnv._tag === "Some" ? options.links !== void 0 ? [...toReadonlyArray(linksFromEnv.value), ...options.links ?? []] : toReadonlyArray(linksFromEnv.value) : options.links ?? empty();
    span2 = tracer2.span(name, parent, options.context ?? empty3(), links, timingEnabled ? clock2.unsafeCurrentTimeNanos() : bigint0, options.kind ?? "internal", options);
    if (annotationsFromEnv._tag === "Some") {
      forEach(annotationsFromEnv.value, (value2, key) => span2.attribute(key, value2));
    }
    if (options.attributes !== void 0) {
      Object.entries(options.attributes).forEach(([k, v]) => span2.attribute(k, v));
    }
  }
  if (typeof options.captureStackTrace === "function") {
    spanToTrace.set(span2, options.captureStackTrace);
  }
  return span2;
};
var makeSpan = (name, options) => {
  options = addSpanStackTrace(options);
  return withFiberRuntime((fiber) => succeed(unsafeMakeSpan(fiber, name, options)));
};
var spanAnnotations = fiberRefGet(currentTracerSpanAnnotations);
var spanLinks = fiberRefGet(currentTracerSpanLinks);
var endSpan = (span2, exit3, clock2, timingEnabled) => sync(() => {
  if (span2.status._tag === "Ended") {
    return;
  }
  if (exitIsFailure(exit3) && spanToTrace.has(span2)) {
    span2.attribute("code.stacktrace", spanToTrace.get(span2)());
  }
  span2.end(timingEnabled ? clock2.unsafeCurrentTimeNanos() : bigint0, exit3);
});
var useSpan = (name, ...args2) => {
  const options = addSpanStackTrace(args2.length === 1 ? void 0 : args2[0]);
  const evaluate2 = args2[args2.length - 1];
  return withFiberRuntime((fiber) => {
    const span2 = unsafeMakeSpan(fiber, name, options);
    const timingEnabled = fiber.getFiberRef(currentTracerTimingEnabled);
    const clock2 = get(fiber.getFiberRef(currentServices), clockTag);
    return onExit(evaluate2(span2), (exit3) => endSpan(span2, exit3, clock2, timingEnabled));
  });
};
var withParentSpan = dual(2, (self, span2) => provideService2(self, spanTag, span2));
var withSpan = function() {
  const dataFirst = typeof arguments[0] !== "string";
  const name = dataFirst ? arguments[1] : arguments[0];
  const options = addSpanStackTrace(dataFirst ? arguments[2] : arguments[1]);
  if (dataFirst) {
    const self = arguments[0];
    return useSpan(name, options, (span2) => withParentSpan(self, span2));
  }
  return (self) => useSpan(name, options, (span2) => withParentSpan(self, span2));
};
var functionWithSpan = (options) => function() {
  let captureStackTrace = options.captureStackTrace ?? false;
  if (options.captureStackTrace !== false) {
    const limit = Error.stackTraceLimit;
    Error.stackTraceLimit = 2;
    const error = new Error();
    Error.stackTraceLimit = limit;
    let cache = false;
    captureStackTrace = () => {
      if (cache !== false) {
        return cache;
      }
      if (error.stack) {
        const stack = error.stack.trim().split("\n");
        cache = stack.slice(2).join("\n").trim();
        return cache;
      }
    };
  }
  return suspend(() => {
    const opts = typeof options.options === "function" ? options.options.apply(null, arguments) : options.options;
    return withSpan(suspend(() => internalCall(() => options.body.apply(this, arguments))), opts.name, {
      ...opts,
      captureStackTrace
    });
  });
};
var fromNullable2 = (value2) => value2 == null ? fail2(new NoSuchElementException()) : succeed(value2);
var optionFromOptional = (self) => catchAll(map4(self, some2), (error) => isNoSuchElementException(error) ? succeedNone2 : fail2(error));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Exit.js
var Exit_exports = {};
__export(Exit_exports, {
  all: () => all3,
  as: () => as4,
  asVoid: () => asVoid3,
  causeOption: () => causeOption,
  die: () => die4,
  exists: () => exists,
  fail: () => fail4,
  failCause: () => failCause3,
  flatMap: () => flatMap6,
  flatMapEffect: () => flatMapEffect,
  flatten: () => flatten6,
  forEachEffect: () => forEachEffect,
  fromEither: () => fromEither2,
  fromOption: () => fromOption2,
  getOrElse: () => getOrElse3,
  interrupt: () => interrupt4,
  isExit: () => isExit,
  isFailure: () => isFailure3,
  isInterrupted: () => isInterrupted2,
  isSuccess: () => isSuccess2,
  map: () => map7,
  mapBoth: () => mapBoth2,
  mapError: () => mapError3,
  mapErrorCause: () => mapErrorCause3,
  match: () => match9,
  matchEffect: () => matchEffect3,
  succeed: () => succeed3,
  void: () => void_3,
  zip: () => zip5,
  zipLeft: () => zipLeft2,
  zipPar: () => zipPar,
  zipParLeft: () => zipParLeft,
  zipParRight: () => zipParRight,
  zipRight: () => zipRight2,
  zipWith: () => zipWith4
});
var isExit = exitIsExit;
var isFailure3 = exitIsFailure;
var isSuccess2 = exitIsSuccess;
var isInterrupted2 = exitIsInterrupted;
var as4 = exitAs;
var asVoid3 = exitAsVoid;
var causeOption = exitCauseOption;
var all3 = exitCollectAll;
var die4 = exitDie;
var exists = exitExists;
var fail4 = exitFail;
var failCause3 = exitFailCause;
var flatMap6 = exitFlatMap;
var flatMapEffect = exitFlatMapEffect;
var flatten6 = exitFlatten;
var forEachEffect = exitForEachEffect;
var fromEither2 = exitFromEither;
var fromOption2 = exitFromOption;
var getOrElse3 = exitGetOrElse;
var interrupt4 = exitInterrupt;
var map7 = exitMap;
var mapBoth2 = exitMapBoth;
var mapError3 = exitMapError;
var mapErrorCause3 = exitMapErrorCause;
var match9 = exitMatch;
var matchEffect3 = exitMatchEffect;
var succeed3 = exitSucceed;
var void_3 = exitVoid;
var zip5 = exitZip;
var zipLeft2 = exitZipLeft;
var zipRight2 = exitZipRight;
var zipPar = exitZipPar;
var zipParLeft = exitZipParLeft;
var zipParRight = exitZipParRight;
var zipWith4 = exitZipWith;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MutableHashMap.js
var MutableHashMap_exports = {};
__export(MutableHashMap_exports, {
  clear: () => clear,
  empty: () => empty15,
  forEach: () => forEach3,
  fromIterable: () => fromIterable5,
  get: () => get7,
  has: () => has,
  isEmpty: () => isEmpty3,
  keys: () => keys,
  make: () => make21,
  modify: () => modify3,
  modifyAt: () => modifyAt,
  remove: () => remove,
  set: () => set5,
  size: () => size4,
  values: () => values
});
var TypeId4 = Symbol.for("effect/MutableHashMap");
var MutableHashMapProto = {
  [TypeId4]: TypeId4,
  [Symbol.iterator]() {
    return new MutableHashMapIterator(this);
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "MutableHashMap",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var MutableHashMapIterator = class _MutableHashMapIterator {
  self;
  referentialIterator;
  bucketIterator;
  constructor(self) {
    this.self = self;
    this.referentialIterator = self.referential[Symbol.iterator]();
  }
  next() {
    if (this.bucketIterator !== void 0) {
      return this.bucketIterator.next();
    }
    const result = this.referentialIterator.next();
    if (result.done) {
      this.bucketIterator = new BucketIterator(this.self.buckets.values());
      return this.next();
    }
    return result;
  }
  [Symbol.iterator]() {
    return new _MutableHashMapIterator(this.self);
  }
};
var BucketIterator = class {
  backing;
  constructor(backing) {
    this.backing = backing;
  }
  currentBucket;
  next() {
    if (this.currentBucket === void 0) {
      const result2 = this.backing.next();
      if (result2.done) {
        return result2;
      }
      this.currentBucket = result2.value[Symbol.iterator]();
    }
    const result = this.currentBucket.next();
    if (result.done) {
      this.currentBucket = void 0;
      return this.next();
    }
    return result;
  }
};
var empty15 = () => {
  const self = Object.create(MutableHashMapProto);
  self.referential = /* @__PURE__ */ new Map();
  self.buckets = /* @__PURE__ */ new Map();
  self.bucketsSize = 0;
  return self;
};
var make21 = (...entries) => fromIterable5(entries);
var fromIterable5 = (entries) => {
  const self = empty15();
  for (const [key, value2] of entries) {
    set5(self, key, value2);
  }
  return self;
};
var get7 = dual(2, (self, key) => {
  if (isEqual(key) === false) {
    return self.referential.has(key) ? some2(self.referential.get(key)) : none2();
  }
  const hash2 = key[symbol]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    return none2();
  }
  return getFromBucket(self, bucket, key);
});
var keys = (self) => {
  const keys4 = Array.from(self.referential.keys());
  for (const bucket of self.buckets.values()) {
    for (let i = 0, len = bucket.length; i < len; i++) {
      keys4.push(bucket[i][0]);
    }
  }
  return keys4;
};
var values = (self) => {
  const values5 = Array.from(self.referential.values());
  for (const bucket of self.buckets.values()) {
    for (let i = 0, len = bucket.length; i < len; i++) {
      values5.push(bucket[i][1]);
    }
  }
  return values5;
};
var getFromBucket = (self, bucket, key, remove4 = false) => {
  for (let i = 0, len = bucket.length; i < len; i++) {
    if (key[symbol2](bucket[i][0])) {
      const value2 = bucket[i][1];
      if (remove4) {
        bucket.splice(i, 1);
        self.bucketsSize--;
      }
      return some2(value2);
    }
  }
  return none2();
};
var has = dual(2, (self, key) => isSome(get7(self, key)));
var set5 = dual(3, (self, key, value2) => {
  if (isEqual(key) === false) {
    self.referential.set(key, value2);
    return self;
  }
  const hash2 = key[symbol]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    self.buckets.set(hash2, [[key, value2]]);
    self.bucketsSize++;
    return self;
  }
  removeFromBucket(self, bucket, key);
  bucket.push([key, value2]);
  self.bucketsSize++;
  return self;
});
var removeFromBucket = (self, bucket, key) => {
  for (let i = 0, len = bucket.length; i < len; i++) {
    if (key[symbol2](bucket[i][0])) {
      bucket.splice(i, 1);
      self.bucketsSize--;
      return;
    }
  }
};
var modify3 = dual(3, (self, key, f) => {
  if (isEqual(key) === false) {
    if (self.referential.has(key)) {
      self.referential.set(key, f(self.referential.get(key)));
    }
    return self;
  }
  const hash2 = key[symbol]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    return self;
  }
  const value2 = getFromBucket(self, bucket, key, true);
  if (isNone(value2)) {
    return self;
  }
  bucket.push([key, f(value2.value)]);
  self.bucketsSize++;
  return self;
});
var modifyAt = dual(3, (self, key, f) => {
  if (isEqual(key) === false) {
    const result2 = f(get7(self, key));
    if (isSome(result2)) {
      set5(self, key, result2.value);
    } else {
      remove(self, key);
    }
    return self;
  }
  const hash2 = key[symbol]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    const result2 = f(none2());
    return isSome(result2) ? set5(self, key, result2.value) : self;
  }
  const result = f(getFromBucket(self, bucket, key, true));
  if (isNone(result)) {
    if (bucket.length === 0) {
      self.buckets.delete(hash2);
    }
    return self;
  }
  bucket.push([key, result.value]);
  self.bucketsSize++;
  return self;
});
var remove = dual(2, (self, key) => {
  if (isEqual(key) === false) {
    self.referential.delete(key);
    return self;
  }
  const hash2 = key[symbol]();
  const bucket = self.buckets.get(hash2);
  if (bucket === void 0) {
    return self;
  }
  removeFromBucket(self, bucket, key);
  if (bucket.length === 0) {
    self.buckets.delete(hash2);
  }
  return self;
});
var clear = (self) => {
  self.referential.clear();
  self.buckets.clear();
  self.bucketsSize = 0;
  return self;
};
var size4 = (self) => {
  return self.referential.size + self.bucketsSize;
};
var isEmpty3 = (self) => size4(self) === 0;
var forEach3 = dual(2, (self, f) => {
  self.referential.forEach(f);
  for (const bucket of self.buckets.values()) {
    for (const [key, value2] of bucket) {
      f(value2, key);
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RedBlackTree.js
var RedBlackTree_exports = {};
__export(RedBlackTree_exports, {
  Direction: () => Direction2,
  at: () => at2,
  atReversed: () => atReversed,
  empty: () => empty17,
  findAll: () => findAll2,
  findFirst: () => findFirst3,
  first: () => first2,
  forEach: () => forEach5,
  forEachBetween: () => forEachBetween2,
  forEachGreaterThanEqual: () => forEachGreaterThanEqual2,
  forEachLessThan: () => forEachLessThan2,
  fromIterable: () => fromIterable7,
  getAt: () => getAt2,
  getOrder: () => getOrder2,
  greaterThan: () => greaterThan5,
  greaterThanEqual: () => greaterThanEqual3,
  greaterThanEqualReversed: () => greaterThanEqualReversed,
  greaterThanReversed: () => greaterThanReversed,
  has: () => has3,
  insert: () => insert2,
  isRedBlackTree: () => isRedBlackTree2,
  keys: () => keys3,
  keysReversed: () => keysReversed,
  last: () => last3,
  lessThan: () => lessThan5,
  lessThanEqual: () => lessThanEqual3,
  lessThanEqualReversed: () => lessThanEqualReversed,
  lessThanReversed: () => lessThanReversed,
  make: () => make23,
  reduce: () => reduce7,
  removeFirst: () => removeFirst2,
  reversed: () => reversed2,
  size: () => size6,
  values: () => values3,
  valuesReversed: () => valuesReversed
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/redBlackTree/iterator.js
var Direction = {
  Forward: 0,
  Backward: 1 << 0
};
var RedBlackTreeIterator = class _RedBlackTreeIterator {
  self;
  stack;
  direction;
  count = 0;
  constructor(self, stack, direction) {
    this.self = self;
    this.stack = stack;
    this.direction = direction;
  }
  /**
   * Clones the iterator
   */
  clone() {
    return new _RedBlackTreeIterator(this.self, this.stack.slice(), this.direction);
  }
  /**
   * Reverse the traversal direction
   */
  reversed() {
    return new _RedBlackTreeIterator(this.self, this.stack.slice(), this.direction === Direction.Forward ? Direction.Backward : Direction.Forward);
  }
  /**
   * Iterator next
   */
  next() {
    const entry = this.entry;
    this.count++;
    if (this.direction === Direction.Forward) {
      this.moveNext();
    } else {
      this.movePrev();
    }
    switch (entry._tag) {
      case "None": {
        return {
          done: true,
          value: this.count
        };
      }
      case "Some": {
        return {
          done: false,
          value: entry.value
        };
      }
    }
  }
  /**
   * Returns the key
   */
  get key() {
    if (this.stack.length > 0) {
      return some2(this.stack[this.stack.length - 1].key);
    }
    return none2();
  }
  /**
   * Returns the value
   */
  get value() {
    if (this.stack.length > 0) {
      return some2(this.stack[this.stack.length - 1].value);
    }
    return none2();
  }
  /**
   * Returns the key
   */
  get entry() {
    return map(last(this.stack), (node) => [node.key, node.value]);
  }
  /**
   * Returns the position of this iterator in the sorted list
   */
  get index() {
    let idx = 0;
    const stack = this.stack;
    if (stack.length === 0) {
      const r = this.self._root;
      if (r != null) {
        return r.count;
      }
      return 0;
    } else if (stack[stack.length - 1].left != null) {
      idx = stack[stack.length - 1].left.count;
    }
    for (let s = stack.length - 2; s >= 0; --s) {
      if (stack[s + 1] === stack[s].right) {
        ++idx;
        if (stack[s].left != null) {
          idx += stack[s].left.count;
        }
      }
    }
    return idx;
  }
  /**
   * Advances iterator to next element in list
   */
  moveNext() {
    const stack = this.stack;
    if (stack.length === 0) {
      return;
    }
    let n = stack[stack.length - 1];
    if (n.right != null) {
      n = n.right;
      while (n != null) {
        stack.push(n);
        n = n.left;
      }
    } else {
      stack.pop();
      while (stack.length > 0 && stack[stack.length - 1].right === n) {
        n = stack[stack.length - 1];
        stack.pop();
      }
    }
  }
  /**
   * Checks if there is a next element
   */
  get hasNext() {
    const stack = this.stack;
    if (stack.length === 0) {
      return false;
    }
    if (stack[stack.length - 1].right != null) {
      return true;
    }
    for (let s = stack.length - 1; s > 0; --s) {
      if (stack[s - 1].left === stack[s]) {
        return true;
      }
    }
    return false;
  }
  /**
   * Advances iterator to previous element in list
   */
  movePrev() {
    const stack = this.stack;
    if (stack.length === 0) {
      return;
    }
    let n = stack[stack.length - 1];
    if (n != null && n.left != null) {
      n = n.left;
      while (n != null) {
        stack.push(n);
        n = n.right;
      }
    } else {
      stack.pop();
      while (stack.length > 0 && stack[stack.length - 1].left === n) {
        n = stack[stack.length - 1];
        stack.pop();
      }
    }
  }
  /**
   * Checks if there is a previous element
   */
  get hasPrev() {
    const stack = this.stack;
    if (stack.length === 0) {
      return false;
    }
    if (stack[stack.length - 1].left != null) {
      return true;
    }
    for (let s = stack.length - 1; s > 0; --s) {
      if (stack[s - 1].right === stack[s]) {
        return true;
      }
    }
    return false;
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/redBlackTree/node.js
var Color = {
  Red: 0,
  Black: 1 << 0
};
var clone = ({
  color,
  count,
  key,
  left: left2,
  right: right2,
  value: value2
}) => ({
  color,
  key,
  value: value2,
  left: left2,
  right: right2,
  count
});
function swap2(n, v) {
  n.key = v.key;
  n.value = v.value;
  n.left = v.left;
  n.right = v.right;
  n.color = v.color;
  n.count = v.count;
}
var repaint = ({
  count,
  key,
  left: left2,
  right: right2,
  value: value2
}, color) => ({
  color,
  key,
  value: value2,
  left: left2,
  right: right2,
  count
});
var recount = (node) => {
  node.count = 1 + (node.left?.count ?? 0) + (node.right?.count ?? 0);
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/redBlackTree.js
var RedBlackTreeSymbolKey = "effect/RedBlackTree";
var RedBlackTreeTypeId = Symbol.for(RedBlackTreeSymbolKey);
var redBlackTreeVariance = {
  /* c8 ignore next */
  _Key: (_) => _,
  /* c8 ignore next */
  _Value: (_) => _
};
var RedBlackTreeProto = {
  [RedBlackTreeTypeId]: redBlackTreeVariance,
  [symbol]() {
    let hash2 = hash(RedBlackTreeSymbolKey);
    for (const item of this) {
      hash2 ^= pipe(hash(item[0]), combine(hash(item[1])));
    }
    return cached(this, hash2);
  },
  [symbol2](that) {
    if (isRedBlackTree(that)) {
      if ((this._root?.count ?? 0) !== (that._root?.count ?? 0)) {
        return false;
      }
      const entries = Array.from(that);
      return Array.from(this).every((itemSelf, i) => {
        const itemThat = entries[i];
        return equals(itemSelf[0], itemThat[0]) && equals(itemSelf[1], itemThat[1]);
      });
    }
    return false;
  },
  [Symbol.iterator]() {
    const stack = [];
    let n = this._root;
    while (n != null) {
      stack.push(n);
      n = n.left;
    }
    return new RedBlackTreeIterator(this, stack, Direction.Forward);
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "RedBlackTree",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeImpl = (ord, root) => {
  const tree = Object.create(RedBlackTreeProto);
  tree._ord = ord;
  tree._root = root;
  return tree;
};
var isRedBlackTree = (u) => hasProperty(u, RedBlackTreeTypeId);
var empty16 = (ord) => makeImpl(ord, void 0);
var fromIterable6 = dual(2, (entries, ord) => {
  let tree = empty16(ord);
  for (const [key, value2] of entries) {
    tree = insert(tree, key, value2);
  }
  return tree;
});
var make22 = (ord) => (...entries) => {
  return fromIterable6(entries, ord);
};
var atBackwards = dual(2, (self, index) => at(self, index, Direction.Backward));
var atForwards = dual(2, (self, index) => at(self, index, Direction.Forward));
var at = (self, index, direction) => {
  return {
    [Symbol.iterator]: () => {
      if (index < 0) {
        return new RedBlackTreeIterator(self, [], direction);
      }
      let node = self._root;
      const stack = [];
      while (node !== void 0) {
        stack.push(node);
        if (node.left !== void 0) {
          if (index < node.left.count) {
            node = node.left;
            continue;
          }
          index -= node.left.count;
        }
        if (!index) {
          return new RedBlackTreeIterator(self, stack, direction);
        }
        index -= 1;
        if (node.right !== void 0) {
          if (index >= node.right.count) {
            break;
          }
          node = node.right;
        } else {
          break;
        }
      }
      return new RedBlackTreeIterator(self, [], direction);
    }
  };
};
var findAll = dual(2, (self, key) => {
  const stack = [];
  let node = self._root;
  let result = empty2();
  while (node !== void 0 || stack.length > 0) {
    if (node) {
      stack.push(node);
      node = node.left;
    } else {
      const current = stack.pop();
      if (equals(key, current.key)) {
        result = prepend2(current.value)(result);
      }
      node = current.right;
    }
  }
  return result;
});
var findFirst2 = dual(2, (self, key) => {
  const cmp = self._ord;
  let node = self._root;
  while (node !== void 0) {
    const d = cmp(key, node.key);
    if (equals(key, node.key)) {
      return some2(node.value);
    }
    if (d <= 0) {
      node = node.left;
    } else {
      node = node.right;
    }
  }
  return none2();
});
var first = (self) => {
  let node = self._root;
  let current = self._root;
  while (node !== void 0) {
    current = node;
    node = node.left;
  }
  return current ? some2([current.key, current.value]) : none2();
};
var getAt = dual(2, (self, index) => {
  if (index < 0) {
    return none2();
  }
  let root = self._root;
  let node = void 0;
  while (root !== void 0) {
    node = root;
    if (root.left) {
      if (index < root.left.count) {
        root = root.left;
        continue;
      }
      index -= root.left.count;
    }
    if (!index) {
      return some2([node.key, node.value]);
    }
    index -= 1;
    if (root.right) {
      if (index >= root.right.count) {
        break;
      }
      root = root.right;
    } else {
      break;
    }
  }
  return none2();
});
var getOrder = (tree) => tree._ord;
var has2 = dual(2, (self, key) => isSome(findFirst2(self, key)));
var insert = dual(3, (self, key, value2) => {
  const cmp = self._ord;
  let n = self._root;
  const n_stack = [];
  const d_stack = [];
  while (n != null) {
    const d = cmp(key, n.key);
    n_stack.push(n);
    d_stack.push(d);
    if (d <= 0) {
      n = n.left;
    } else {
      n = n.right;
    }
  }
  n_stack.push({
    color: Color.Red,
    key,
    value: value2,
    left: void 0,
    right: void 0,
    count: 1
  });
  for (let s = n_stack.length - 2; s >= 0; --s) {
    const n2 = n_stack[s];
    if (d_stack[s] <= 0) {
      n_stack[s] = {
        color: n2.color,
        key: n2.key,
        value: n2.value,
        left: n_stack[s + 1],
        right: n2.right,
        count: n2.count + 1
      };
    } else {
      n_stack[s] = {
        color: n2.color,
        key: n2.key,
        value: n2.value,
        left: n2.left,
        right: n_stack[s + 1],
        count: n2.count + 1
      };
    }
  }
  for (let s = n_stack.length - 1; s > 1; --s) {
    const p = n_stack[s - 1];
    const n3 = n_stack[s];
    if (p.color === Color.Black || n3.color === Color.Black) {
      break;
    }
    const pp = n_stack[s - 2];
    if (pp.left === p) {
      if (p.left === n3) {
        const y = pp.right;
        if (y && y.color === Color.Red) {
          p.color = Color.Black;
          pp.right = repaint(y, Color.Black);
          pp.color = Color.Red;
          s -= 1;
        } else {
          pp.color = Color.Red;
          pp.left = p.right;
          p.color = Color.Black;
          p.right = pp;
          n_stack[s - 2] = p;
          n_stack[s - 1] = n3;
          recount(pp);
          recount(p);
          if (s >= 3) {
            const ppp = n_stack[s - 3];
            if (ppp.left === pp) {
              ppp.left = p;
            } else {
              ppp.right = p;
            }
          }
          break;
        }
      } else {
        const y = pp.right;
        if (y && y.color === Color.Red) {
          p.color = Color.Black;
          pp.right = repaint(y, Color.Black);
          pp.color = Color.Red;
          s -= 1;
        } else {
          p.right = n3.left;
          pp.color = Color.Red;
          pp.left = n3.right;
          n3.color = Color.Black;
          n3.left = p;
          n3.right = pp;
          n_stack[s - 2] = n3;
          n_stack[s - 1] = p;
          recount(pp);
          recount(p);
          recount(n3);
          if (s >= 3) {
            const ppp = n_stack[s - 3];
            if (ppp.left === pp) {
              ppp.left = n3;
            } else {
              ppp.right = n3;
            }
          }
          break;
        }
      }
    } else {
      if (p.right === n3) {
        const y = pp.left;
        if (y && y.color === Color.Red) {
          p.color = Color.Black;
          pp.left = repaint(y, Color.Black);
          pp.color = Color.Red;
          s -= 1;
        } else {
          pp.color = Color.Red;
          pp.right = p.left;
          p.color = Color.Black;
          p.left = pp;
          n_stack[s - 2] = p;
          n_stack[s - 1] = n3;
          recount(pp);
          recount(p);
          if (s >= 3) {
            const ppp = n_stack[s - 3];
            if (ppp.right === pp) {
              ppp.right = p;
            } else {
              ppp.left = p;
            }
          }
          break;
        }
      } else {
        const y = pp.left;
        if (y && y.color === Color.Red) {
          p.color = Color.Black;
          pp.left = repaint(y, Color.Black);
          pp.color = Color.Red;
          s -= 1;
        } else {
          p.left = n3.right;
          pp.color = Color.Red;
          pp.right = n3.left;
          n3.color = Color.Black;
          n3.right = p;
          n3.left = pp;
          n_stack[s - 2] = n3;
          n_stack[s - 1] = p;
          recount(pp);
          recount(p);
          recount(n3);
          if (s >= 3) {
            const ppp = n_stack[s - 3];
            if (ppp.right === pp) {
              ppp.right = n3;
            } else {
              ppp.left = n3;
            }
          }
          break;
        }
      }
    }
  }
  n_stack[0].color = Color.Black;
  return makeImpl(self._ord, n_stack[0]);
});
var keysForward = (self) => keys2(self, Direction.Forward);
var keysBackward = (self) => keys2(self, Direction.Backward);
var keys2 = (self, direction) => {
  const begin = self[Symbol.iterator]();
  let count = 0;
  return {
    [Symbol.iterator]: () => keys2(self, direction),
    next: () => {
      count++;
      const entry = begin.key;
      if (direction === Direction.Forward) {
        begin.moveNext();
      } else {
        begin.movePrev();
      }
      switch (entry._tag) {
        case "None": {
          return {
            done: true,
            value: count
          };
        }
        case "Some": {
          return {
            done: false,
            value: entry.value
          };
        }
      }
    }
  };
};
var last2 = (self) => {
  let node = self._root;
  let current = self._root;
  while (node !== void 0) {
    current = node;
    node = node.right;
  }
  return current ? some2([current.key, current.value]) : none2();
};
var reversed = (self) => {
  return {
    [Symbol.iterator]: () => {
      const stack = [];
      let node = self._root;
      while (node !== void 0) {
        stack.push(node);
        node = node.right;
      }
      return new RedBlackTreeIterator(self, stack, Direction.Backward);
    }
  };
};
var greaterThanBackwards = dual(2, (self, key) => greaterThan4(self, key, Direction.Backward));
var greaterThanForwards = dual(2, (self, key) => greaterThan4(self, key, Direction.Forward));
var greaterThan4 = (self, key, direction) => {
  return {
    [Symbol.iterator]: () => {
      const cmp = self._ord;
      let node = self._root;
      const stack = [];
      let last_ptr = 0;
      while (node !== void 0) {
        const d = cmp(key, node.key);
        stack.push(node);
        if (d < 0) {
          last_ptr = stack.length;
        }
        if (d < 0) {
          node = node.left;
        } else {
          node = node.right;
        }
      }
      stack.length = last_ptr;
      return new RedBlackTreeIterator(self, stack, direction);
    }
  };
};
var greaterThanEqualBackwards = dual(2, (self, key) => greaterThanEqual2(self, key, Direction.Backward));
var greaterThanEqualForwards = dual(2, (self, key) => greaterThanEqual2(self, key, Direction.Forward));
var greaterThanEqual2 = (self, key, direction = Direction.Forward) => {
  return {
    [Symbol.iterator]: () => {
      const cmp = self._ord;
      let node = self._root;
      const stack = [];
      let last_ptr = 0;
      while (node !== void 0) {
        const d = cmp(key, node.key);
        stack.push(node);
        if (d <= 0) {
          last_ptr = stack.length;
        }
        if (d <= 0) {
          node = node.left;
        } else {
          node = node.right;
        }
      }
      stack.length = last_ptr;
      return new RedBlackTreeIterator(self, stack, direction);
    }
  };
};
var lessThanBackwards = dual(2, (self, key) => lessThan4(self, key, Direction.Backward));
var lessThanForwards = dual(2, (self, key) => lessThan4(self, key, Direction.Forward));
var lessThan4 = (self, key, direction) => {
  return {
    [Symbol.iterator]: () => {
      const cmp = self._ord;
      let node = self._root;
      const stack = [];
      let last_ptr = 0;
      while (node !== void 0) {
        const d = cmp(key, node.key);
        stack.push(node);
        if (d > 0) {
          last_ptr = stack.length;
        }
        if (d <= 0) {
          node = node.left;
        } else {
          node = node.right;
        }
      }
      stack.length = last_ptr;
      return new RedBlackTreeIterator(self, stack, direction);
    }
  };
};
var lessThanEqualBackwards = dual(2, (self, key) => lessThanEqual2(self, key, Direction.Backward));
var lessThanEqualForwards = dual(2, (self, key) => lessThanEqual2(self, key, Direction.Forward));
var lessThanEqual2 = (self, key, direction) => {
  return {
    [Symbol.iterator]: () => {
      const cmp = self._ord;
      let node = self._root;
      const stack = [];
      let last_ptr = 0;
      while (node !== void 0) {
        const d = cmp(key, node.key);
        stack.push(node);
        if (d >= 0) {
          last_ptr = stack.length;
        }
        if (d < 0) {
          node = node.left;
        } else {
          node = node.right;
        }
      }
      stack.length = last_ptr;
      return new RedBlackTreeIterator(self, stack, direction);
    }
  };
};
var forEach4 = dual(2, (self, f) => {
  const root = self._root;
  if (root !== void 0) {
    visitFull(root, (key, value2) => {
      f(key, value2);
      return none2();
    });
  }
});
var forEachGreaterThanEqual = dual(3, (self, min5, f) => {
  const root = self._root;
  const ord = self._ord;
  if (root !== void 0) {
    visitGreaterThanEqual(root, min5, ord, (key, value2) => {
      f(key, value2);
      return none2();
    });
  }
});
var forEachLessThan = dual(3, (self, max7, f) => {
  const root = self._root;
  const ord = self._ord;
  if (root !== void 0) {
    visitLessThan(root, max7, ord, (key, value2) => {
      f(key, value2);
      return none2();
    });
  }
});
var forEachBetween = dual(2, (self, {
  body,
  max: max7,
  min: min5
}) => {
  const root = self._root;
  const ord = self._ord;
  if (root) {
    visitBetween(root, min5, max7, ord, (key, value2) => {
      body(key, value2);
      return none2();
    });
  }
});
var reduce6 = dual(3, (self, zero2, f) => {
  let accumulator = zero2;
  for (const entry of self) {
    accumulator = f(accumulator, entry[1], entry[0]);
  }
  return accumulator;
});
var removeFirst = dual(2, (self, key) => {
  if (!has2(self, key)) {
    return self;
  }
  const ord = self._ord;
  const cmp = ord;
  let node = self._root;
  const stack = [];
  while (node !== void 0) {
    const d = cmp(key, node.key);
    stack.push(node);
    if (equals(key, node.key)) {
      node = void 0;
    } else if (d <= 0) {
      node = node.left;
    } else {
      node = node.right;
    }
  }
  if (stack.length === 0) {
    return self;
  }
  const cstack = new Array(stack.length);
  let n = stack[stack.length - 1];
  cstack[cstack.length - 1] = {
    color: n.color,
    key: n.key,
    value: n.value,
    left: n.left,
    right: n.right,
    count: n.count
  };
  for (let i = stack.length - 2; i >= 0; --i) {
    n = stack[i];
    if (n.left === stack[i + 1]) {
      cstack[i] = {
        color: n.color,
        key: n.key,
        value: n.value,
        left: cstack[i + 1],
        right: n.right,
        count: n.count
      };
    } else {
      cstack[i] = {
        color: n.color,
        key: n.key,
        value: n.value,
        left: n.left,
        right: cstack[i + 1],
        count: n.count
      };
    }
  }
  n = cstack[cstack.length - 1];
  if (n.left !== void 0 && n.right !== void 0) {
    const split = cstack.length;
    n = n.left;
    while (n.right != null) {
      cstack.push(n);
      n = n.right;
    }
    const v = cstack[split - 1];
    cstack.push({
      color: n.color,
      key: v.key,
      value: v.value,
      left: n.left,
      right: n.right,
      count: n.count
    });
    cstack[split - 1].key = n.key;
    cstack[split - 1].value = n.value;
    for (let i = cstack.length - 2; i >= split; --i) {
      n = cstack[i];
      cstack[i] = {
        color: n.color,
        key: n.key,
        value: n.value,
        left: n.left,
        right: cstack[i + 1],
        count: n.count
      };
    }
    cstack[split - 1].left = cstack[split];
  }
  n = cstack[cstack.length - 1];
  if (n.color === Color.Red) {
    const p = cstack[cstack.length - 2];
    if (p.left === n) {
      p.left = void 0;
    } else if (p.right === n) {
      p.right = void 0;
    }
    cstack.pop();
    for (let i = 0; i < cstack.length; ++i) {
      cstack[i].count--;
    }
    return makeImpl(ord, cstack[0]);
  } else {
    if (n.left !== void 0 || n.right !== void 0) {
      if (n.left !== void 0) {
        swap2(n, n.left);
      } else if (n.right !== void 0) {
        swap2(n, n.right);
      }
      n.color = Color.Black;
      for (let i = 0; i < cstack.length - 1; ++i) {
        cstack[i].count--;
      }
      return makeImpl(ord, cstack[0]);
    } else if (cstack.length === 1) {
      return makeImpl(ord, void 0);
    } else {
      for (let i = 0; i < cstack.length; ++i) {
        cstack[i].count--;
      }
      const parent = cstack[cstack.length - 2];
      fixDoubleBlack(cstack);
      if (parent.left === n) {
        parent.left = void 0;
      } else {
        parent.right = void 0;
      }
    }
  }
  return makeImpl(ord, cstack[0]);
});
var size5 = (self) => self._root?.count ?? 0;
var valuesForward = (self) => values2(self, Direction.Forward);
var valuesBackward = (self) => values2(self, Direction.Backward);
var values2 = (self, direction) => {
  const begin = self[Symbol.iterator]();
  let count = 0;
  return {
    [Symbol.iterator]: () => values2(self, direction),
    next: () => {
      count++;
      const entry = begin.value;
      if (direction === Direction.Forward) {
        begin.moveNext();
      } else {
        begin.movePrev();
      }
      switch (entry._tag) {
        case "None": {
          return {
            done: true,
            value: count
          };
        }
        case "Some": {
          return {
            done: false,
            value: entry.value
          };
        }
      }
    }
  };
};
var visitFull = (node, visit) => {
  let current = node;
  let stack = void 0;
  let done8 = false;
  while (!done8) {
    if (current != null) {
      stack = make3(current, stack);
      current = current.left;
    } else if (stack != null) {
      const value2 = visit(stack.value.key, stack.value.value);
      if (isSome(value2)) {
        return value2;
      }
      current = stack.value.right;
      stack = stack.previous;
    } else {
      done8 = true;
    }
  }
  return none2();
};
var visitGreaterThanEqual = (node, min5, ord, visit) => {
  let current = node;
  let stack = void 0;
  let done8 = false;
  while (!done8) {
    if (current !== void 0) {
      stack = make3(current, stack);
      if (ord(min5, current.key) <= 0) {
        current = current.left;
      } else {
        current = void 0;
      }
    } else if (stack !== void 0) {
      if (ord(min5, stack.value.key) <= 0) {
        const value2 = visit(stack.value.key, stack.value.value);
        if (isSome(value2)) {
          return value2;
        }
      }
      current = stack.value.right;
      stack = stack.previous;
    } else {
      done8 = true;
    }
  }
  return none2();
};
var visitLessThan = (node, max7, ord, visit) => {
  let current = node;
  let stack = void 0;
  let done8 = false;
  while (!done8) {
    if (current !== void 0) {
      stack = make3(current, stack);
      current = current.left;
    } else if (stack !== void 0 && ord(max7, stack.value.key) > 0) {
      const value2 = visit(stack.value.key, stack.value.value);
      if (isSome(value2)) {
        return value2;
      }
      current = stack.value.right;
      stack = stack.previous;
    } else {
      done8 = true;
    }
  }
  return none2();
};
var visitBetween = (node, min5, max7, ord, visit) => {
  let current = node;
  let stack = void 0;
  let done8 = false;
  while (!done8) {
    if (current !== void 0) {
      stack = make3(current, stack);
      if (ord(min5, current.key) <= 0) {
        current = current.left;
      } else {
        current = void 0;
      }
    } else if (stack !== void 0 && ord(max7, stack.value.key) > 0) {
      if (ord(min5, stack.value.key) <= 0) {
        const value2 = visit(stack.value.key, stack.value.value);
        if (isSome(value2)) {
          return value2;
        }
      }
      current = stack.value.right;
      stack = stack.previous;
    } else {
      done8 = true;
    }
  }
  return none2();
};
var fixDoubleBlack = (stack) => {
  let n, p, s, z;
  for (let i = stack.length - 1; i >= 0; --i) {
    n = stack[i];
    if (i === 0) {
      n.color = Color.Black;
      return;
    }
    p = stack[i - 1];
    if (p.left === n) {
      s = p.right;
      if (s !== void 0 && s.right !== void 0 && s.right.color === Color.Red) {
        s = p.right = clone(s);
        z = s.right = clone(s.right);
        p.right = s.left;
        s.left = p;
        s.right = z;
        s.color = p.color;
        n.color = Color.Black;
        p.color = Color.Black;
        z.color = Color.Black;
        recount(p);
        recount(s);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.left === p) {
            pp.left = s;
          } else {
            pp.right = s;
          }
        }
        stack[i - 1] = s;
        return;
      } else if (s !== void 0 && s.left !== void 0 && s.left.color === Color.Red) {
        s = p.right = clone(s);
        z = s.left = clone(s.left);
        p.right = z.left;
        s.left = z.right;
        z.left = p;
        z.right = s;
        z.color = p.color;
        p.color = Color.Black;
        s.color = Color.Black;
        n.color = Color.Black;
        recount(p);
        recount(s);
        recount(z);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.left === p) {
            pp.left = z;
          } else {
            pp.right = z;
          }
        }
        stack[i - 1] = z;
        return;
      }
      if (s !== void 0 && s.color === Color.Black) {
        if (p.color === Color.Red) {
          p.color = Color.Black;
          p.right = repaint(s, Color.Red);
          return;
        } else {
          p.right = repaint(s, Color.Red);
          continue;
        }
      } else if (s !== void 0) {
        s = clone(s);
        p.right = s.left;
        s.left = p;
        s.color = p.color;
        p.color = Color.Red;
        recount(p);
        recount(s);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.left === p) {
            pp.left = s;
          } else {
            pp.right = s;
          }
        }
        stack[i - 1] = s;
        stack[i] = p;
        if (i + 1 < stack.length) {
          stack[i + 1] = n;
        } else {
          stack.push(n);
        }
        i = i + 2;
      }
    } else {
      s = p.left;
      if (s !== void 0 && s.left !== void 0 && s.left.color === Color.Red) {
        s = p.left = clone(s);
        z = s.left = clone(s.left);
        p.left = s.right;
        s.right = p;
        s.left = z;
        s.color = p.color;
        n.color = Color.Black;
        p.color = Color.Black;
        z.color = Color.Black;
        recount(p);
        recount(s);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.right === p) {
            pp.right = s;
          } else {
            pp.left = s;
          }
        }
        stack[i - 1] = s;
        return;
      } else if (s !== void 0 && s.right !== void 0 && s.right.color === Color.Red) {
        s = p.left = clone(s);
        z = s.right = clone(s.right);
        p.left = z.right;
        s.right = z.left;
        z.right = p;
        z.left = s;
        z.color = p.color;
        p.color = Color.Black;
        s.color = Color.Black;
        n.color = Color.Black;
        recount(p);
        recount(s);
        recount(z);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.right === p) {
            pp.right = z;
          } else {
            pp.left = z;
          }
        }
        stack[i - 1] = z;
        return;
      }
      if (s !== void 0 && s.color === Color.Black) {
        if (p.color === Color.Red) {
          p.color = Color.Black;
          p.left = repaint(s, Color.Red);
          return;
        } else {
          p.left = repaint(s, Color.Red);
          continue;
        }
      } else if (s !== void 0) {
        s = clone(s);
        p.left = s.right;
        s.right = p;
        s.color = p.color;
        p.color = Color.Red;
        recount(p);
        recount(s);
        if (i > 1) {
          const pp = stack[i - 2];
          if (pp.right === p) {
            pp.right = s;
          } else {
            pp.left = s;
          }
        }
        stack[i - 1] = s;
        stack[i] = p;
        if (i + 1 < stack.length) {
          stack[i + 1] = n;
        } else {
          stack.push(n);
        }
        i = i + 2;
      }
    }
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RedBlackTree.js
var Direction2 = Direction;
var isRedBlackTree2 = isRedBlackTree;
var empty17 = empty16;
var fromIterable7 = fromIterable6;
var make23 = make22;
var at2 = atForwards;
var atReversed = atBackwards;
var findAll2 = findAll;
var findFirst3 = findFirst2;
var first2 = first;
var getAt2 = getAt;
var getOrder2 = getOrder;
var greaterThan5 = greaterThanForwards;
var greaterThanReversed = greaterThanBackwards;
var greaterThanEqual3 = greaterThanEqualForwards;
var greaterThanEqualReversed = greaterThanEqualBackwards;
var has3 = has2;
var insert2 = insert;
var keys3 = keysForward;
var keysReversed = keysBackward;
var last3 = last2;
var lessThan5 = lessThanForwards;
var lessThanReversed = lessThanBackwards;
var lessThanEqual3 = lessThanEqualForwards;
var lessThanEqualReversed = lessThanEqualBackwards;
var forEach5 = forEach4;
var forEachGreaterThanEqual2 = forEachGreaterThanEqual;
var forEachLessThan2 = forEachLessThan;
var forEachBetween2 = forEachBetween;
var reduce7 = reduce6;
var removeFirst2 = removeFirst;
var reversed2 = reversed;
var size6 = size5;
var values3 = valuesForward;
var valuesReversed = valuesBackward;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/SortedSet.js
var SortedSet_exports = {};
__export(SortedSet_exports, {
  add: () => add2,
  difference: () => difference2,
  empty: () => empty18,
  every: () => every3,
  filter: () => filter3,
  flatMap: () => flatMap7,
  forEach: () => forEach6,
  fromIterable: () => fromIterable8,
  getEquivalence: () => getEquivalence2,
  has: () => has4,
  intersection: () => intersection,
  isSortedSet: () => isSortedSet,
  isSubset: () => isSubset,
  make: () => make24,
  map: () => map8,
  partition: () => partition,
  remove: () => remove2,
  size: () => size7,
  some: () => some4,
  toggle: () => toggle,
  union: () => union3,
  values: () => values4
});
var TypeId5 = Symbol.for("effect/SortedSet");
var SortedSetProto = {
  [TypeId5]: {
    _A: (_) => _
  },
  [symbol]() {
    return pipe(hash(this.keyTree), combine(hash(TypeId5)), cached(this));
  },
  [symbol2](that) {
    return isSortedSet(that) && equals(this.keyTree, that.keyTree);
  },
  [Symbol.iterator]() {
    return keys3(this.keyTree);
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "SortedSet",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var fromTree = (keyTree) => {
  const a = Object.create(SortedSetProto);
  a.keyTree = keyTree;
  return a;
};
var isSortedSet = (u) => hasProperty(u, TypeId5);
var empty18 = (O) => fromTree(empty17(O));
var fromIterable8 = dual(2, (iterable, ord) => fromTree(fromIterable7(Array.from(iterable).map((k) => [k, true]), ord)));
var make24 = (ord) => (...entries) => fromIterable8(entries, ord);
var add2 = dual(2, (self, value2) => has3(self.keyTree, value2) ? self : fromTree(insert2(self.keyTree, value2, true)));
var difference2 = dual(2, (self, that) => {
  let out = self;
  for (const value2 of that) {
    out = remove2(out, value2);
  }
  return out;
});
var every3 = dual(2, (self, predicate) => {
  for (const value2 of self) {
    if (!predicate(value2)) {
      return false;
    }
  }
  return true;
});
var filter3 = dual(2, (self, predicate) => {
  const ord = getOrder2(self.keyTree);
  let out = empty18(ord);
  for (const value2 of self) {
    if (predicate(value2)) {
      out = add2(out, value2);
    }
  }
  return out;
});
var flatMap7 = dual(3, (self, O, f) => {
  let out = empty18(O);
  forEach6(self, (a) => {
    for (const b of f(a)) {
      out = add2(out, b);
    }
  });
  return out;
});
var forEach6 = dual(2, (self, f) => forEach5(self.keyTree, f));
var has4 = dual(2, (self, value2) => has3(self.keyTree, value2));
var intersection = dual(2, (self, that) => {
  const ord = getOrder2(self.keyTree);
  let out = empty18(ord);
  for (const value2 of that) {
    if (has4(self, value2)) {
      out = add2(out, value2);
    }
  }
  return out;
});
var isSubset = dual(2, (self, that) => every3(self, (a) => has4(that, a)));
var map8 = dual(3, (self, O, f) => {
  let out = empty18(O);
  forEach6(self, (a) => {
    const b = f(a);
    if (!has4(out, b)) {
      out = add2(out, b);
    }
  });
  return out;
});
var partition = dual(2, (self, predicate) => {
  const ord = getOrder2(self.keyTree);
  let right2 = empty18(ord);
  let left2 = empty18(ord);
  for (const value2 of self) {
    if (predicate(value2)) {
      right2 = add2(right2, value2);
    } else {
      left2 = add2(left2, value2);
    }
  }
  return [left2, right2];
});
var remove2 = dual(2, (self, value2) => fromTree(removeFirst2(self.keyTree, value2)));
var size7 = (self) => size6(self.keyTree);
var some4 = dual(2, (self, predicate) => {
  for (const value2 of self) {
    if (predicate(value2)) {
      return true;
    }
  }
  return false;
});
var toggle = dual(2, (self, value2) => has4(self, value2) ? remove2(self, value2) : add2(self, value2));
var union3 = dual(2, (self, that) => {
  const ord = getOrder2(self.keyTree);
  let out = empty18(ord);
  for (const value2 of self) {
    out = add2(value2)(out);
  }
  for (const value2 of that) {
    out = add2(value2)(out);
  }
  return out;
});
var values4 = (self) => keys3(self.keyTree);
var getEquivalence2 = () => (a, b) => isSubset(a, b) && isSubset(b, a);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Differ.js
var Differ_exports = {};
__export(Differ_exports, {
  TypeId: () => TypeId6,
  chunk: () => chunk2,
  combine: () => combine5,
  diff: () => diff4,
  empty: () => empty19,
  environment: () => environment2,
  hashMap: () => hashMap2,
  hashSet: () => hashSet2,
  make: () => make25,
  orElseEither: () => orElseEither2,
  patch: () => patch5,
  readonlyArray: () => readonlyArray2,
  transform: () => transform2,
  update: () => update4,
  updateWith: () => updateWith2,
  zip: () => zip6
});
var TypeId6 = DifferTypeId;
var empty19 = (self) => self.empty;
var diff4 = dual(3, (self, oldValue, newValue) => self.diff(oldValue, newValue));
var combine5 = dual(3, (self, first3, second) => self.combine(first3, second));
var patch5 = dual(3, (self, patch7, oldValue) => self.patch(patch7, oldValue));
var make25 = make6;
var environment2 = environment;
var chunk2 = chunk;
var hashMap2 = hashMap;
var hashSet2 = hashSet;
var orElseEither2 = orElseEither;
var readonlyArray2 = readonlyArray;
var transform2 = transform;
var update4 = update;
var updateWith2 = updateWith;
var zip6 = zip2;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/completedRequestMap.js
var currentRequestMap = globalValue(Symbol.for("effect/FiberRef/currentRequestMap"), () => fiberRefUnsafeMake(/* @__PURE__ */ new Map()));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/concurrency.js
var match10 = (concurrency, sequential5, unbounded2, bounded2) => {
  switch (concurrency) {
    case void 0:
      return sequential5();
    case "unbounded":
      return unbounded2();
    case "inherit":
      return fiberRefGetWith(currentConcurrency, (concurrency2) => concurrency2 === "unbounded" ? unbounded2() : concurrency2 > 1 ? bounded2(concurrency2) : sequential5());
    default:
      return concurrency > 1 ? bounded2(concurrency) : sequential5();
  }
};
var matchSimple = (concurrency, sequential5, concurrent) => {
  switch (concurrency) {
    case void 0:
      return sequential5();
    case "unbounded":
      return concurrent();
    case "inherit":
      return fiberRefGetWith(currentConcurrency, (concurrency2) => concurrency2 === "unbounded" || concurrency2 > 1 ? concurrent() : sequential5());
    default:
      return concurrency > 1 ? concurrent() : sequential5();
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiberMessage.js
var OP_INTERRUPT_SIGNAL = "InterruptSignal";
var OP_STATEFUL = "Stateful";
var OP_RESUME = "Resume";
var OP_YIELD_NOW = "YieldNow";
var interruptSignal = (cause2) => ({
  _tag: OP_INTERRUPT_SIGNAL,
  cause: cause2
});
var stateful = (onFiber) => ({
  _tag: OP_STATEFUL,
  onFiber
});
var resume = (effect) => ({
  _tag: OP_RESUME,
  effect
});
var yieldNow3 = () => ({
  _tag: OP_YIELD_NOW
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiberScope.js
var FiberScopeSymbolKey = "effect/FiberScope";
var FiberScopeTypeId = Symbol.for(FiberScopeSymbolKey);
var Global = class {
  [FiberScopeTypeId] = FiberScopeTypeId;
  fiberId = none4;
  roots = /* @__PURE__ */ new Set();
  add(_runtimeFlags, child) {
    this.roots.add(child);
    child.addObserver(() => {
      this.roots.delete(child);
    });
  }
};
var Local = class {
  fiberId;
  parent;
  [FiberScopeTypeId] = FiberScopeTypeId;
  constructor(fiberId2, parent) {
    this.fiberId = fiberId2;
    this.parent = parent;
  }
  add(_runtimeFlags, child) {
    this.parent.tell(stateful((parentFiber) => {
      parentFiber.addChild(child);
      child.addObserver(() => {
        parentFiber.removeChild(child);
      });
    }));
  }
};
var unsafeMake6 = (fiber) => {
  return new Local(fiber.id(), fiber);
};
var globalScope = globalValue(Symbol.for("effect/FiberScope/Global"), () => new Global());

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiber.js
var FiberSymbolKey = "effect/Fiber";
var FiberTypeId = Symbol.for(FiberSymbolKey);
var fiberVariance2 = {
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _A: (_) => _
};
var fiberProto = {
  [FiberTypeId]: fiberVariance2,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var RuntimeFiberSymbolKey = "effect/Fiber";
var RuntimeFiberTypeId = Symbol.for(RuntimeFiberSymbolKey);
var Order4 = pipe(tuple(Order, Order), mapInput((fiber) => [fiber.id().startTimeMillis, fiber.id().id]));
var isFiber = (u) => hasProperty(u, FiberTypeId);
var isRuntimeFiber = (self) => RuntimeFiberTypeId in self;
var _await = (self) => self.await;
var children = (self) => self.children;
var done3 = (exit3) => {
  const _fiber = {
    ...CommitPrototype,
    commit() {
      return join2(this);
    },
    ...fiberProto,
    id: () => none4,
    await: succeed(exit3),
    children: succeed([]),
    inheritAll: void_,
    poll: succeed(some2(exit3)),
    interruptAsFork: () => void_
  };
  return _fiber;
};
var dump = (self) => map4(self.status, (status3) => ({
  id: self.id(),
  status: status3
}));
var dumpAll = (fibers) => forEachSequential(fibers, dump);
var fail5 = (error) => done3(fail4(error));
var failCause4 = (cause2) => done3(failCause3(cause2));
var fromEffect = (effect) => map4(exit(effect), done3);
var id = (self) => self.id();
var inheritAll = (self) => self.inheritAll;
var interrupted = (fiberId2) => done3(interrupt4(fiberId2));
var interruptAll = (fibers) => flatMap4(fiberId, (fiberId2) => pipe(fibers, interruptAllAs(fiberId2)));
var interruptAllAs = dual(2, fnUntraced(function* (fibers, fiberId2) {
  for (const fiber of fibers) {
    if (isRuntimeFiber(fiber)) {
      fiber.unsafeInterruptAsFork(fiberId2);
      continue;
    }
    yield* fiber.interruptAsFork(fiberId2);
  }
  for (const fiber of fibers) {
    if (isRuntimeFiber(fiber) && fiber.unsafePoll()) {
      continue;
    }
    yield* fiber.await;
  }
}));
var interruptAsFork = dual(2, (self, fiberId2) => self.interruptAsFork(fiberId2));
var join2 = (self) => zipLeft(flatten4(self.await), self.inheritAll);
var map9 = dual(2, (self, f) => mapEffect2(self, (a) => sync(() => f(a))));
var mapEffect2 = dual(2, (self, f) => {
  const _fiber = {
    ...CommitPrototype,
    commit() {
      return join2(this);
    },
    ...fiberProto,
    id: () => self.id(),
    await: flatMap4(self.await, forEachEffect(f)),
    children: self.children,
    inheritAll: self.inheritAll,
    poll: flatMap4(self.poll, (result) => {
      switch (result._tag) {
        case "None":
          return succeed(none2());
        case "Some":
          return pipe(forEachEffect(result.value, f), map4(some2));
      }
    }),
    interruptAsFork: (id3) => self.interruptAsFork(id3)
  };
  return _fiber;
});
var mapFiber = dual(2, (self, f) => map4(self.await, match9({
  onFailure: (cause2) => failCause4(cause2),
  onSuccess: (a) => f(a)
})));
var match11 = dual(2, (self, {
  onFiber,
  onRuntimeFiber
}) => {
  if (isRuntimeFiber(self)) {
    return onRuntimeFiber(self);
  }
  return onFiber(self);
});
var _never = {
  ...CommitPrototype,
  commit() {
    return join2(this);
  },
  ...fiberProto,
  id: () => none4,
  await: never,
  children: succeed([]),
  inheritAll: never,
  poll: succeed(none2()),
  interruptAsFork: () => never
};
var never3 = _never;
var orElse3 = dual(2, (self, that) => ({
  ...CommitPrototype,
  commit() {
    return join2(this);
  },
  ...fiberProto,
  id: () => getOrElse2(self.id(), that.id()),
  await: zipWith2(self.await, that.await, (exit1, exit22) => isSuccess2(exit1) ? exit1 : exit22),
  children: self.children,
  inheritAll: zipRight(that.inheritAll, self.inheritAll),
  poll: zipWith2(self.poll, that.poll, (option1, option22) => {
    switch (option1._tag) {
      case "None": {
        return none2();
      }
      case "Some": {
        return isSuccess2(option1.value) ? option1 : option22;
      }
    }
  }),
  interruptAsFork: (id3) => pipe(interruptAsFiber(self, id3), zipRight(pipe(that, interruptAsFiber(id3))), asVoid)
}));
var orElseEither3 = dual(2, (self, that) => orElse3(map9(self, left), map9(that, right)));
var poll = (self) => self.poll;
var parseMs = (milliseconds) => {
  const roundTowardsZero = milliseconds > 0 ? Math.floor : Math.ceil;
  return {
    days: roundTowardsZero(milliseconds / 864e5),
    hours: roundTowardsZero(milliseconds / 36e5) % 24,
    minutes: roundTowardsZero(milliseconds / 6e4) % 60,
    seconds: roundTowardsZero(milliseconds / 1e3) % 60,
    milliseconds: roundTowardsZero(milliseconds) % 1e3,
    microseconds: roundTowardsZero(milliseconds * 1e3) % 1e3,
    nanoseconds: roundTowardsZero(milliseconds * 1e6) % 1e3
  };
};
var renderStatus = (status3) => {
  if (isDone2(status3)) {
    return "Done";
  }
  if (isRunning2(status3)) {
    return "Running";
  }
  const isInterruptible = interruptible(status3.runtimeFlags) ? "interruptible" : "uninterruptible";
  return `Suspended(${isInterruptible})`;
};
var pretty2 = (self) => flatMap4(currentTimeMillis2, (now) => map4(dump(self), (dump3) => {
  const time = now - dump3.id.startTimeMillis;
  const {
    days,
    hours,
    milliseconds,
    minutes,
    seconds: seconds2
  } = parseMs(time);
  const lifeMsg = (days === 0 ? "" : `${days}d`) + (days === 0 && hours === 0 ? "" : `${hours}h`) + (days === 0 && hours === 0 && minutes === 0 ? "" : `${minutes}m`) + (days === 0 && hours === 0 && minutes === 0 && seconds2 === 0 ? "" : `${seconds2}s`) + `${milliseconds}ms`;
  const waitMsg = isSuspended2(dump3.status) ? (() => {
    const ids2 = ids(dump3.status.blockingOn);
    return size(ids2) > 0 ? `waiting on ` + Array.from(ids2).map((id3) => `${id3}`).join(", ") : "";
  })() : "";
  const statusMsg = renderStatus(dump3.status);
  return `[Fiber](#${dump3.id.id}) (${lifeMsg}) ${waitMsg}
   Status: ${statusMsg}`;
}));
var unsafeRoots = () => Array.from(globalScope.roots);
var roots = sync(unsafeRoots);
var status = (self) => self.status;
var succeed4 = (value2) => done3(succeed3(value2));
var void_4 = succeed4(void 0);
var currentFiberURI = "effect/FiberCurrent";
var getCurrentFiber = () => fromNullable(globalThis[currentFiberURI]);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/logger.js
var LoggerSymbolKey = "effect/Logger";
var LoggerTypeId = Symbol.for(LoggerSymbolKey);
var loggerVariance = {
  /* c8 ignore next */
  _Message: (_) => _,
  /* c8 ignore next */
  _Output: (_) => _
};
var makeLogger = (log2) => ({
  [LoggerTypeId]: loggerVariance,
  log: log2,
  pipe() {
    return pipeArguments(this, arguments);
  }
});
var mapInput2 = dual(2, (self, f) => makeLogger((options) => self.log({
  ...options,
  message: f(options.message)
})));
var mapInputOptions = dual(2, (self, f) => makeLogger((options) => self.log(f(options))));
var filterLogLevel = dual(2, (self, f) => makeLogger((options) => f(options.logLevel) ? some2(self.log(options)) : none2()));
var map10 = dual(2, (self, f) => makeLogger((options) => f(self.log(options))));
var none7 = {
  [LoggerTypeId]: loggerVariance,
  log: constVoid,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var simple = (log2) => ({
  [LoggerTypeId]: loggerVariance,
  log: ({
    message
  }) => log2(message),
  pipe() {
    return pipeArguments(this, arguments);
  }
});
var succeed5 = (value2) => {
  return simple(() => value2);
};
var sync3 = (evaluate2) => {
  return simple(evaluate2);
};
var zip7 = dual(2, (self, that) => makeLogger((options) => [self.log(options), that.log(options)]));
var zipLeft3 = dual(2, (self, that) => map10(zip7(self, that), (tuple2) => tuple2[0]));
var zipRight3 = dual(2, (self, that) => map10(zip7(self, that), (tuple2) => tuple2[1]));
var textOnly = /^[^\s"=]*$/;
var format2 = (quoteValue, whitespace) => ({
  annotations,
  cause: cause2,
  date,
  fiberId: fiberId2,
  logLevel,
  message,
  spans
}) => {
  const formatValue = (value2) => value2.match(textOnly) ? value2 : quoteValue(value2);
  const format3 = (label, value2) => `${formatLabel(label)}=${formatValue(value2)}`;
  const append4 = (label, value2) => " " + format3(label, value2);
  let out = format3("timestamp", date.toISOString());
  out += append4("level", logLevel.label);
  out += append4("fiber", threadName(fiberId2));
  const messages = ensure(message);
  for (let i = 0; i < messages.length; i++) {
    out += append4("message", toStringUnknown(messages[i], whitespace));
  }
  if (!isEmptyType(cause2)) {
    out += append4("cause", pretty(cause2, {
      renderErrorCause: true
    }));
  }
  for (const span2 of spans) {
    out += " " + render(date.getTime())(span2);
  }
  for (const [label, value2] of annotations) {
    out += append4(label, toStringUnknown(value2, whitespace));
  }
  return out;
};
var escapeDoubleQuotes = (s) => `"${s.replace(/\\([\s\S])|(")/g, "\\$1$2")}"`;
var stringLogger = makeLogger(format2(escapeDoubleQuotes));
var logfmtLogger = makeLogger(format2(JSON.stringify, 0));
var structuredLogger = makeLogger(({
  annotations,
  cause: cause2,
  date,
  fiberId: fiberId2,
  logLevel,
  message,
  spans
}) => {
  const now = date.getTime();
  const annotationsObj = {};
  const spansObj = {};
  if (size2(annotations) > 0) {
    for (const [k, v] of annotations) {
      annotationsObj[k] = structuredMessage(v);
    }
  }
  if (isCons(spans)) {
    for (const span2 of spans) {
      spansObj[span2.label] = now - span2.startTime;
    }
  }
  const messageArr = ensure(message);
  return {
    message: messageArr.length === 1 ? structuredMessage(messageArr[0]) : messageArr.map(structuredMessage),
    logLevel: logLevel.label,
    timestamp: date.toISOString(),
    cause: isEmpty2(cause2) ? void 0 : pretty(cause2, {
      renderErrorCause: true
    }),
    annotations: annotationsObj,
    spans: spansObj,
    fiberId: threadName(fiberId2)
  };
});
var structuredMessage = (u) => {
  switch (typeof u) {
    case "bigint":
    case "function":
    case "symbol": {
      return String(u);
    }
    default: {
      return toJSON(u);
    }
  }
};
var jsonLogger = map10(structuredLogger, stringifyCircular);
var isLogger = (u) => {
  return typeof u === "object" && u != null && LoggerTypeId in u;
};
var withColor = (text, ...colors2) => {
  let out = "";
  for (let i = 0; i < colors2.length; i++) {
    out += `\x1B[${colors2[i]}m`;
  }
  return out + text + "\x1B[0m";
};
var withColorNoop = (text, ..._colors) => text;
var colors = {
  bold: "1",
  red: "31",
  green: "32",
  yellow: "33",
  blue: "34",
  cyan: "36",
  white: "37",
  gray: "90",
  black: "30",
  bgBrightRed: "101"
};
var logLevelColors = {
  None: [],
  All: [],
  Trace: [colors.gray],
  Debug: [colors.blue],
  Info: [colors.green],
  Warning: [colors.yellow],
  Error: [colors.red],
  Fatal: [colors.bgBrightRed, colors.black]
};
var logLevelStyle = {
  None: "",
  All: "",
  Trace: "color:gray",
  Debug: "color:blue",
  Info: "color:green",
  Warning: "color:orange",
  Error: "color:red",
  Fatal: "background-color:red;color:white"
};
var defaultDateFormat = (date) => `${date.getHours().toString().padStart(2, "0")}:${date.getMinutes().toString().padStart(2, "0")}:${date.getSeconds().toString().padStart(2, "0")}.${date.getMilliseconds().toString().padStart(3, "0")}`;
var hasProcessStdout = typeof process === "object" && process !== null && typeof process.stdout === "object" && process.stdout !== null;
var processStdoutIsTTY = hasProcessStdout && process.stdout.isTTY === true;
var hasProcessStdoutOrDeno = hasProcessStdout || "Deno" in globalThis;
var prettyLogger = (options) => {
  const mode_ = options?.mode ?? "auto";
  const mode = mode_ === "auto" ? hasProcessStdoutOrDeno ? "tty" : "browser" : mode_;
  const isBrowser = mode === "browser";
  const showColors = typeof options?.colors === "boolean" ? options.colors : processStdoutIsTTY || isBrowser;
  const formatDate = options?.formatDate ?? defaultDateFormat;
  return isBrowser ? prettyLoggerBrowser({
    colors: showColors,
    formatDate
  }) : prettyLoggerTty({
    colors: showColors,
    formatDate,
    stderr: options?.stderr === true
  });
};
var prettyLoggerTty = (options) => {
  const color = options.colors ? withColor : withColorNoop;
  return makeLogger(({
    annotations,
    cause: cause2,
    context: context4,
    date,
    fiberId: fiberId2,
    logLevel,
    message: message_,
    spans
  }) => {
    const services = getOrDefault2(context4, currentServices);
    const console2 = get(services, consoleTag).unsafe;
    const log2 = options.stderr === true ? console2.error : console2.log;
    const message = ensure(message_);
    let firstLine = color(`[${options.formatDate(date)}]`, colors.white) + ` ${color(logLevel.label, ...logLevelColors[logLevel._tag])} (${threadName(fiberId2)})`;
    if (isCons(spans)) {
      const now = date.getTime();
      const render3 = render(now);
      for (const span2 of spans) {
        firstLine += " " + render3(span2);
      }
    }
    firstLine += ":";
    let messageIndex = 0;
    if (message.length > 0) {
      const firstMaybeString = structuredMessage(message[0]);
      if (typeof firstMaybeString === "string") {
        firstLine += " " + color(firstMaybeString, colors.bold, colors.cyan);
        messageIndex++;
      }
    }
    log2(firstLine);
    console2.group();
    if (!isEmpty2(cause2)) {
      log2(pretty(cause2, {
        renderErrorCause: true
      }));
    }
    if (messageIndex < message.length) {
      for (; messageIndex < message.length; messageIndex++) {
        log2(redact(message[messageIndex]));
      }
    }
    if (size2(annotations) > 0) {
      for (const [key, value2] of annotations) {
        log2(color(`${key}:`, colors.bold, colors.white), redact(value2));
      }
    }
    console2.groupEnd();
  });
};
var prettyLoggerBrowser = (options) => {
  const color = options.colors ? "%c" : "";
  return makeLogger(({
    annotations,
    cause: cause2,
    context: context4,
    date,
    fiberId: fiberId2,
    logLevel,
    message: message_,
    spans
  }) => {
    const services = getOrDefault2(context4, currentServices);
    const console2 = get(services, consoleTag).unsafe;
    const message = ensure(message_);
    let firstLine = `${color}[${options.formatDate(date)}]`;
    const firstParams = [];
    if (options.colors) {
      firstParams.push("color:gray");
    }
    firstLine += ` ${color}${logLevel.label}${color} (${threadName(fiberId2)})`;
    if (options.colors) {
      firstParams.push(logLevelStyle[logLevel._tag], "");
    }
    if (isCons(spans)) {
      const now = date.getTime();
      const render3 = render(now);
      for (const span2 of spans) {
        firstLine += " " + render3(span2);
      }
    }
    firstLine += ":";
    let messageIndex = 0;
    if (message.length > 0) {
      const firstMaybeString = structuredMessage(message[0]);
      if (typeof firstMaybeString === "string") {
        firstLine += ` ${color}${firstMaybeString}`;
        if (options.colors) {
          firstParams.push("color:deepskyblue");
        }
        messageIndex++;
      }
    }
    console2.groupCollapsed(firstLine, ...firstParams);
    if (!isEmpty2(cause2)) {
      console2.error(pretty(cause2, {
        renderErrorCause: true
      }));
    }
    if (messageIndex < message.length) {
      for (; messageIndex < message.length; messageIndex++) {
        console2.log(redact(message[messageIndex]));
      }
    }
    if (size2(annotations) > 0) {
      for (const [key, value2] of annotations) {
        const redacted = redact(value2);
        if (options.colors) {
          console2.log(`%c${key}:`, "color:gray", redacted);
        } else {
          console2.log(`${key}:`, redacted);
        }
      }
    }
    console2.groupEnd();
  });
};
var prettyLoggerDefault = globalValue("effect/Logger/prettyLoggerDefault", () => prettyLogger());

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/boundaries.js
var MetricBoundariesSymbolKey = "effect/MetricBoundaries";
var MetricBoundariesTypeId = Symbol.for(MetricBoundariesSymbolKey);
var MetricBoundariesImpl = class {
  values;
  [MetricBoundariesTypeId] = MetricBoundariesTypeId;
  constructor(values5) {
    this.values = values5;
    this._hash = pipe(string(MetricBoundariesSymbolKey), combine(array(this.values)));
  }
  _hash;
  [symbol]() {
    return this._hash;
  }
  [symbol2](u) {
    return isMetricBoundaries(u) && equals(this.values, u.values);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isMetricBoundaries = (u) => hasProperty(u, MetricBoundariesTypeId);
var fromIterable9 = (iterable) => {
  const values5 = pipe(iterable, appendAll(of2(Number.POSITIVE_INFINITY)), dedupe);
  return new MetricBoundariesImpl(values5);
};
var linear = (options) => pipe(makeBy(options.count - 1, (i) => options.start + i * options.width), unsafeFromArray, fromIterable9);
var exponential = (options) => pipe(makeBy(options.count - 1, (i) => options.start * Math.pow(options.factor, i)), unsafeFromArray, fromIterable9);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/keyType.js
var MetricKeyTypeSymbolKey = "effect/MetricKeyType";
var MetricKeyTypeTypeId = Symbol.for(MetricKeyTypeSymbolKey);
var CounterKeyTypeSymbolKey = "effect/MetricKeyType/Counter";
var CounterKeyTypeTypeId = Symbol.for(CounterKeyTypeSymbolKey);
var FrequencyKeyTypeSymbolKey = "effect/MetricKeyType/Frequency";
var FrequencyKeyTypeTypeId = Symbol.for(FrequencyKeyTypeSymbolKey);
var GaugeKeyTypeSymbolKey = "effect/MetricKeyType/Gauge";
var GaugeKeyTypeTypeId = Symbol.for(GaugeKeyTypeSymbolKey);
var HistogramKeyTypeSymbolKey = "effect/MetricKeyType/Histogram";
var HistogramKeyTypeTypeId = Symbol.for(HistogramKeyTypeSymbolKey);
var SummaryKeyTypeSymbolKey = "effect/MetricKeyType/Summary";
var SummaryKeyTypeTypeId = Symbol.for(SummaryKeyTypeSymbolKey);
var metricKeyTypeVariance = {
  /* c8 ignore next */
  _In: (_) => _,
  /* c8 ignore next */
  _Out: (_) => _
};
var CounterKeyType = class {
  incremental;
  bigint;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [CounterKeyTypeTypeId] = CounterKeyTypeTypeId;
  constructor(incremental, bigint) {
    this.incremental = incremental;
    this.bigint = bigint;
    this._hash = string(CounterKeyTypeSymbolKey);
  }
  _hash;
  [symbol]() {
    return this._hash;
  }
  [symbol2](that) {
    return isCounterKey(that);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var FrequencyKeyTypeHash = string(FrequencyKeyTypeSymbolKey);
var FrequencyKeyType = class {
  preregisteredWords;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [FrequencyKeyTypeTypeId] = FrequencyKeyTypeTypeId;
  constructor(preregisteredWords) {
    this.preregisteredWords = preregisteredWords;
  }
  [symbol]() {
    return FrequencyKeyTypeHash;
  }
  [symbol2](that) {
    return isFrequencyKey(that);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var GaugeKeyTypeHash = string(GaugeKeyTypeSymbolKey);
var GaugeKeyType = class {
  bigint;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [GaugeKeyTypeTypeId] = GaugeKeyTypeTypeId;
  constructor(bigint) {
    this.bigint = bigint;
  }
  [symbol]() {
    return GaugeKeyTypeHash;
  }
  [symbol2](that) {
    return isGaugeKey(that);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var HistogramKeyType = class {
  boundaries;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [HistogramKeyTypeTypeId] = HistogramKeyTypeTypeId;
  constructor(boundaries) {
    this.boundaries = boundaries;
    this._hash = pipe(string(HistogramKeyTypeSymbolKey), combine(hash(this.boundaries)));
  }
  _hash;
  [symbol]() {
    return this._hash;
  }
  [symbol2](that) {
    return isHistogramKey(that) && equals(this.boundaries, that.boundaries);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var SummaryKeyType = class {
  maxAge;
  maxSize;
  error;
  quantiles;
  [MetricKeyTypeTypeId] = metricKeyTypeVariance;
  [SummaryKeyTypeTypeId] = SummaryKeyTypeTypeId;
  constructor(maxAge, maxSize, error, quantiles) {
    this.maxAge = maxAge;
    this.maxSize = maxSize;
    this.error = error;
    this.quantiles = quantiles;
    this._hash = pipe(string(SummaryKeyTypeSymbolKey), combine(hash(this.maxAge)), combine(hash(this.maxSize)), combine(hash(this.error)), combine(array(this.quantiles)));
  }
  _hash;
  [symbol]() {
    return this._hash;
  }
  [symbol2](that) {
    return isSummaryKey(that) && equals(this.maxAge, that.maxAge) && this.maxSize === that.maxSize && this.error === that.error && equals(this.quantiles, that.quantiles);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var counter = (options) => new CounterKeyType(options?.incremental ?? false, options?.bigint ?? false);
var frequency = (options) => new FrequencyKeyType(options?.preregisteredWords ?? []);
var gauge = (options) => new GaugeKeyType(options?.bigint ?? false);
var histogram = (boundaries) => {
  return new HistogramKeyType(boundaries);
};
var summary = (options) => {
  return new SummaryKeyType(decode(options.maxAge), options.maxSize, options.error, options.quantiles);
};
var isMetricKeyType = (u) => hasProperty(u, MetricKeyTypeTypeId);
var isCounterKey = (u) => hasProperty(u, CounterKeyTypeTypeId);
var isFrequencyKey = (u) => hasProperty(u, FrequencyKeyTypeTypeId);
var isGaugeKey = (u) => hasProperty(u, GaugeKeyTypeTypeId);
var isHistogramKey = (u) => hasProperty(u, HistogramKeyTypeTypeId);
var isSummaryKey = (u) => hasProperty(u, SummaryKeyTypeTypeId);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/key.js
var MetricKeySymbolKey = "effect/MetricKey";
var MetricKeyTypeId = Symbol.for(MetricKeySymbolKey);
var metricKeyVariance = {
  /* c8 ignore next */
  _Type: (_) => _
};
var arrayEquivilence = getEquivalence(equals);
var MetricKeyImpl = class {
  name;
  keyType;
  description;
  tags;
  [MetricKeyTypeId] = metricKeyVariance;
  constructor(name, keyType, description, tags = []) {
    this.name = name;
    this.keyType = keyType;
    this.description = description;
    this.tags = tags;
    this._hash = pipe(string(this.name + this.description), combine(hash(this.keyType)), combine(array(this.tags)));
  }
  _hash;
  [symbol]() {
    return this._hash;
  }
  [symbol2](u) {
    return isMetricKey(u) && this.name === u.name && equals(this.keyType, u.keyType) && equals(this.description, u.description) && arrayEquivilence(this.tags, u.tags);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isMetricKey = (u) => hasProperty(u, MetricKeyTypeId);
var counter2 = (name, options) => new MetricKeyImpl(name, counter(options), fromNullable(options?.description));
var frequency2 = (name, options) => new MetricKeyImpl(name, frequency(options), fromNullable(options?.description));
var gauge2 = (name, options) => new MetricKeyImpl(name, gauge(options), fromNullable(options?.description));
var histogram2 = (name, boundaries, description) => new MetricKeyImpl(name, histogram(boundaries), fromNullable(description));
var summary2 = (options) => new MetricKeyImpl(options.name, summary(options), fromNullable(options.description));
var tagged = dual(3, (self, key, value2) => taggedWithLabels(self, [make20(key, value2)]));
var taggedWithLabels = dual(2, (self, extraTags) => extraTags.length === 0 ? self : new MetricKeyImpl(self.name, self.keyType, self.description, union(self.tags, extraTags)));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/state.js
var MetricStateSymbolKey = "effect/MetricState";
var MetricStateTypeId = Symbol.for(MetricStateSymbolKey);
var CounterStateSymbolKey = "effect/MetricState/Counter";
var CounterStateTypeId = Symbol.for(CounterStateSymbolKey);
var FrequencyStateSymbolKey = "effect/MetricState/Frequency";
var FrequencyStateTypeId = Symbol.for(FrequencyStateSymbolKey);
var GaugeStateSymbolKey = "effect/MetricState/Gauge";
var GaugeStateTypeId = Symbol.for(GaugeStateSymbolKey);
var HistogramStateSymbolKey = "effect/MetricState/Histogram";
var HistogramStateTypeId = Symbol.for(HistogramStateSymbolKey);
var SummaryStateSymbolKey = "effect/MetricState/Summary";
var SummaryStateTypeId = Symbol.for(SummaryStateSymbolKey);
var metricStateVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var CounterState = class {
  count;
  [MetricStateTypeId] = metricStateVariance;
  [CounterStateTypeId] = CounterStateTypeId;
  constructor(count) {
    this.count = count;
  }
  [symbol]() {
    return pipe(hash(CounterStateSymbolKey), combine(hash(this.count)), cached(this));
  }
  [symbol2](that) {
    return isCounterState(that) && this.count === that.count;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var arrayEquals = getEquivalence(equals);
var FrequencyState = class {
  occurrences;
  [MetricStateTypeId] = metricStateVariance;
  [FrequencyStateTypeId] = FrequencyStateTypeId;
  constructor(occurrences) {
    this.occurrences = occurrences;
  }
  _hash;
  [symbol]() {
    return pipe(string(FrequencyStateSymbolKey), combine(array(fromIterable(this.occurrences.entries()))), cached(this));
  }
  [symbol2](that) {
    return isFrequencyState(that) && arrayEquals(fromIterable(this.occurrences.entries()), fromIterable(that.occurrences.entries()));
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var GaugeState = class {
  value;
  [MetricStateTypeId] = metricStateVariance;
  [GaugeStateTypeId] = GaugeStateTypeId;
  constructor(value2) {
    this.value = value2;
  }
  [symbol]() {
    return pipe(hash(GaugeStateSymbolKey), combine(hash(this.value)), cached(this));
  }
  [symbol2](u) {
    return isGaugeState(u) && this.value === u.value;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var HistogramState = class {
  buckets;
  count;
  min;
  max;
  sum;
  [MetricStateTypeId] = metricStateVariance;
  [HistogramStateTypeId] = HistogramStateTypeId;
  constructor(buckets, count, min5, max7, sum2) {
    this.buckets = buckets;
    this.count = count;
    this.min = min5;
    this.max = max7;
    this.sum = sum2;
  }
  [symbol]() {
    return pipe(hash(HistogramStateSymbolKey), combine(hash(this.buckets)), combine(hash(this.count)), combine(hash(this.min)), combine(hash(this.max)), combine(hash(this.sum)), cached(this));
  }
  [symbol2](that) {
    return isHistogramState(that) && equals(this.buckets, that.buckets) && this.count === that.count && this.min === that.min && this.max === that.max && this.sum === that.sum;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var SummaryState = class {
  error;
  quantiles;
  count;
  min;
  max;
  sum;
  [MetricStateTypeId] = metricStateVariance;
  [SummaryStateTypeId] = SummaryStateTypeId;
  constructor(error, quantiles, count, min5, max7, sum2) {
    this.error = error;
    this.quantiles = quantiles;
    this.count = count;
    this.min = min5;
    this.max = max7;
    this.sum = sum2;
  }
  [symbol]() {
    return pipe(hash(SummaryStateSymbolKey), combine(hash(this.error)), combine(hash(this.quantiles)), combine(hash(this.count)), combine(hash(this.min)), combine(hash(this.max)), combine(hash(this.sum)), cached(this));
  }
  [symbol2](that) {
    return isSummaryState(that) && this.error === that.error && equals(this.quantiles, that.quantiles) && this.count === that.count && this.min === that.min && this.max === that.max && this.sum === that.sum;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var counter3 = (count) => new CounterState(count);
var frequency3 = (occurrences) => {
  return new FrequencyState(occurrences);
};
var gauge3 = (count) => new GaugeState(count);
var histogram3 = (options) => new HistogramState(options.buckets, options.count, options.min, options.max, options.sum);
var summary3 = (options) => new SummaryState(options.error, options.quantiles, options.count, options.min, options.max, options.sum);
var isMetricState = (u) => hasProperty(u, MetricStateTypeId);
var isCounterState = (u) => hasProperty(u, CounterStateTypeId);
var isFrequencyState = (u) => hasProperty(u, FrequencyStateTypeId);
var isGaugeState = (u) => hasProperty(u, GaugeStateTypeId);
var isHistogramState = (u) => hasProperty(u, HistogramStateTypeId);
var isSummaryState = (u) => hasProperty(u, SummaryStateTypeId);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/hook.js
var MetricHookSymbolKey = "effect/MetricHook";
var MetricHookTypeId = Symbol.for(MetricHookSymbolKey);
var metricHookVariance = {
  /* c8 ignore next */
  _In: (_) => _,
  /* c8 ignore next */
  _Out: (_) => _
};
var make26 = (options) => ({
  [MetricHookTypeId]: metricHookVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  ...options
});
var onModify = dual(2, (self, f) => ({
  [MetricHookTypeId]: metricHookVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  get: self.get,
  update: self.update,
  modify: (input) => {
    self.modify(input);
    return f(input);
  }
}));
var onUpdate = dual(2, (self, f) => ({
  [MetricHookTypeId]: metricHookVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  get: self.get,
  update: (input) => {
    self.update(input);
    return f(input);
  },
  modify: self.modify
}));
var bigint02 = BigInt(0);
var counter4 = (key) => {
  let sum2 = key.keyType.bigint ? bigint02 : 0;
  const canUpdate = key.keyType.incremental ? key.keyType.bigint ? (value2) => value2 >= bigint02 : (value2) => value2 >= 0 : (_value) => true;
  const update6 = (value2) => {
    if (canUpdate(value2)) {
      sum2 = sum2 + value2;
    }
  };
  return make26({
    get: () => counter3(sum2),
    update: update6,
    modify: update6
  });
};
var frequency4 = (key) => {
  const values5 = /* @__PURE__ */ new Map();
  for (const word of key.keyType.preregisteredWords) {
    values5.set(word, 0);
  }
  const update6 = (word) => {
    const slotCount = values5.get(word) ?? 0;
    values5.set(word, slotCount + 1);
  };
  return make26({
    get: () => frequency3(values5),
    update: update6,
    modify: update6
  });
};
var gauge4 = (_key, startAt) => {
  let value2 = startAt;
  return make26({
    get: () => gauge3(value2),
    update: (v) => {
      value2 = v;
    },
    modify: (v) => {
      value2 = value2 + v;
    }
  });
};
var histogram4 = (key) => {
  const bounds = key.keyType.boundaries.values;
  const size11 = bounds.length;
  const values5 = new Uint32Array(size11 + 1);
  const boundaries = new Float64Array(size11);
  let count = 0;
  let sum2 = 0;
  let min5 = Number.MAX_VALUE;
  let max7 = Number.MIN_VALUE;
  pipe(bounds, sort(Order), map2((n, i) => {
    boundaries[i] = n;
  }));
  const update6 = (value2) => {
    let from = 0;
    let to = size11;
    while (from !== to) {
      const mid = Math.floor(from + (to - from) / 2);
      const boundary = boundaries[mid];
      if (value2 <= boundary) {
        to = mid;
      } else {
        from = mid;
      }
      if (to === from + 1) {
        if (value2 <= boundaries[from]) {
          to = from;
        } else {
          from = to;
        }
      }
    }
    values5[from] = values5[from] + 1;
    count = count + 1;
    sum2 = sum2 + value2;
    if (value2 < min5) {
      min5 = value2;
    }
    if (value2 > max7) {
      max7 = value2;
    }
  };
  const getBuckets = () => {
    const builder = allocate(size11);
    let cumulated = 0;
    for (let i = 0; i < size11; i++) {
      const boundary = boundaries[i];
      const value2 = values5[i];
      cumulated = cumulated + value2;
      builder[i] = [boundary, cumulated];
    }
    return builder;
  };
  return make26({
    get: () => histogram3({
      buckets: getBuckets(),
      count,
      min: min5,
      max: max7,
      sum: sum2
    }),
    update: update6,
    modify: update6
  });
};
var summary4 = (key) => {
  const {
    error,
    maxAge,
    maxSize,
    quantiles
  } = key.keyType;
  const sortedQuantiles = pipe(quantiles, sort(Order));
  const values5 = allocate(maxSize);
  let head5 = 0;
  let count = 0;
  let sum2 = 0;
  let min5 = 0;
  let max7 = 0;
  const snapshot2 = (now) => {
    const builder = [];
    let i = 0;
    while (i !== maxSize - 1) {
      const item = values5[i];
      if (item != null) {
        const [t, v] = item;
        const age = millis(now - t);
        if (greaterThanOrEqualTo2(age, zero) && lessThanOrEqualTo2(age, maxAge)) {
          builder.push(v);
        }
      }
      i = i + 1;
    }
    return calculateQuantiles(error, sortedQuantiles, sort(builder, Order));
  };
  const observe = (value2, timestamp) => {
    if (maxSize > 0) {
      head5 = head5 + 1;
      const target = head5 % maxSize;
      values5[target] = [timestamp, value2];
    }
    min5 = count === 0 ? value2 : Math.min(min5, value2);
    max7 = count === 0 ? value2 : Math.max(max7, value2);
    count = count + 1;
    sum2 = sum2 + value2;
  };
  return make26({
    get: () => summary3({
      error,
      quantiles: snapshot2(Date.now()),
      count,
      min: min5,
      max: max7,
      sum: sum2
    }),
    update: ([value2, timestamp]) => observe(value2, timestamp),
    modify: ([value2, timestamp]) => observe(value2, timestamp)
  });
};
var calculateQuantiles = (error, sortedQuantiles, sortedSamples) => {
  const sampleCount = sortedSamples.length;
  if (!isNonEmptyReadonlyArray(sortedQuantiles)) {
    return empty();
  }
  const head5 = sortedQuantiles[0];
  const tail2 = sortedQuantiles.slice(1);
  const resolvedHead = resolveQuantile(error, sampleCount, none2(), 0, head5, sortedSamples);
  const resolved = of(resolvedHead);
  tail2.forEach((quantile) => {
    resolved.push(resolveQuantile(error, sampleCount, resolvedHead.value, resolvedHead.consumed, quantile, resolvedHead.rest));
  });
  return map2(resolved, (rq) => [rq.quantile, rq.value]);
};
var resolveQuantile = (error, sampleCount, current, consumed, quantile, rest) => {
  let error_1 = error;
  let sampleCount_1 = sampleCount;
  let current_1 = current;
  let consumed_1 = consumed;
  let quantile_1 = quantile;
  let rest_1 = rest;
  let error_2 = error;
  let sampleCount_2 = sampleCount;
  let current_2 = current;
  let consumed_2 = consumed;
  let quantile_2 = quantile;
  let rest_2 = rest;
  while (1) {
    if (!isNonEmptyReadonlyArray(rest_1)) {
      return {
        quantile: quantile_1,
        value: none2(),
        consumed: consumed_1,
        rest: []
      };
    }
    if (quantile_1 === 1) {
      return {
        quantile: quantile_1,
        value: some2(lastNonEmpty(rest_1)),
        consumed: consumed_1 + rest_1.length,
        rest: []
      };
    }
    const headValue = headNonEmpty(rest_1);
    const sameHead = span(rest_1, (n) => n === headValue);
    const desired = quantile_1 * sampleCount_1;
    const allowedError = error_1 / 2 * desired;
    const candConsumed = consumed_1 + sameHead[0].length;
    const candError = Math.abs(candConsumed - desired);
    if (candConsumed < desired - allowedError) {
      error_2 = error_1;
      sampleCount_2 = sampleCount_1;
      current_2 = head(rest_1);
      consumed_2 = candConsumed;
      quantile_2 = quantile_1;
      rest_2 = sameHead[1];
      error_1 = error_2;
      sampleCount_1 = sampleCount_2;
      current_1 = current_2;
      consumed_1 = consumed_2;
      quantile_1 = quantile_2;
      rest_1 = rest_2;
      continue;
    }
    if (candConsumed > desired + allowedError) {
      const valueToReturn = isNone(current_1) ? some2(headValue) : current_1;
      return {
        quantile: quantile_1,
        value: valueToReturn,
        consumed: consumed_1,
        rest: rest_1
      };
    }
    switch (current_1._tag) {
      case "None": {
        error_2 = error_1;
        sampleCount_2 = sampleCount_1;
        current_2 = head(rest_1);
        consumed_2 = candConsumed;
        quantile_2 = quantile_1;
        rest_2 = sameHead[1];
        error_1 = error_2;
        sampleCount_1 = sampleCount_2;
        current_1 = current_2;
        consumed_1 = consumed_2;
        quantile_1 = quantile_2;
        rest_1 = rest_2;
        continue;
      }
      case "Some": {
        const prevError = Math.abs(desired - current_1.value);
        if (candError < prevError) {
          error_2 = error_1;
          sampleCount_2 = sampleCount_1;
          current_2 = head(rest_1);
          consumed_2 = candConsumed;
          quantile_2 = quantile_1;
          rest_2 = sameHead[1];
          error_1 = error_2;
          sampleCount_1 = sampleCount_2;
          current_1 = current_2;
          consumed_1 = consumed_2;
          quantile_1 = quantile_2;
          rest_1 = rest_2;
          continue;
        }
        return {
          quantile: quantile_1,
          value: some2(current_1.value),
          consumed: consumed_1,
          rest: rest_1
        };
      }
    }
  }
  throw new Error("BUG: MetricHook.resolveQuantiles - please report an issue at https://github.com/Effect-TS/effect/issues");
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/pair.js
var MetricPairSymbolKey = "effect/MetricPair";
var MetricPairTypeId = Symbol.for(MetricPairSymbolKey);
var metricPairVariance = {
  /* c8 ignore next */
  _Type: (_) => _
};
var make27 = (metricKey, metricState) => {
  return {
    [MetricPairTypeId]: metricPairVariance,
    metricKey,
    metricState,
    pipe() {
      return pipeArguments(this, arguments);
    }
  };
};
var unsafeMake7 = (metricKey, metricState) => {
  return {
    [MetricPairTypeId]: metricPairVariance,
    metricKey,
    metricState,
    pipe() {
      return pipeArguments(this, arguments);
    }
  };
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric/registry.js
var MetricRegistrySymbolKey = "effect/MetricRegistry";
var MetricRegistryTypeId = Symbol.for(MetricRegistrySymbolKey);
var MetricRegistryImpl = class {
  [MetricRegistryTypeId] = MetricRegistryTypeId;
  map = empty15();
  snapshot() {
    const result = [];
    for (const [key, hook] of this.map) {
      result.push(unsafeMake7(key, hook.get()));
    }
    return result;
  }
  get(key) {
    const hook = pipe(this.map, get7(key), getOrUndefined);
    if (hook == null) {
      if (isCounterKey(key.keyType)) {
        return this.getCounter(key);
      }
      if (isGaugeKey(key.keyType)) {
        return this.getGauge(key);
      }
      if (isFrequencyKey(key.keyType)) {
        return this.getFrequency(key);
      }
      if (isHistogramKey(key.keyType)) {
        return this.getHistogram(key);
      }
      if (isSummaryKey(key.keyType)) {
        return this.getSummary(key);
      }
      throw new Error("BUG: MetricRegistry.get - unknown MetricKeyType - please report an issue at https://github.com/Effect-TS/effect/issues");
    } else {
      return hook;
    }
  }
  getCounter(key) {
    let value2 = pipe(this.map, get7(key), getOrUndefined);
    if (value2 == null) {
      const counter6 = counter4(key);
      if (!pipe(this.map, has(key))) {
        pipe(this.map, set5(key, counter6));
      }
      value2 = counter6;
    }
    return value2;
  }
  getFrequency(key) {
    let value2 = pipe(this.map, get7(key), getOrUndefined);
    if (value2 == null) {
      const frequency6 = frequency4(key);
      if (!pipe(this.map, has(key))) {
        pipe(this.map, set5(key, frequency6));
      }
      value2 = frequency6;
    }
    return value2;
  }
  getGauge(key) {
    let value2 = pipe(this.map, get7(key), getOrUndefined);
    if (value2 == null) {
      const gauge6 = gauge4(key, key.keyType.bigint ? BigInt(0) : 0);
      if (!pipe(this.map, has(key))) {
        pipe(this.map, set5(key, gauge6));
      }
      value2 = gauge6;
    }
    return value2;
  }
  getHistogram(key) {
    let value2 = pipe(this.map, get7(key), getOrUndefined);
    if (value2 == null) {
      const histogram6 = histogram4(key);
      if (!pipe(this.map, has(key))) {
        pipe(this.map, set5(key, histogram6));
      }
      value2 = histogram6;
    }
    return value2;
  }
  getSummary(key) {
    let value2 = pipe(this.map, get7(key), getOrUndefined);
    if (value2 == null) {
      const summary6 = summary4(key);
      if (!pipe(this.map, has(key))) {
        pipe(this.map, set5(key, summary6));
      }
      value2 = summary6;
    }
    return value2;
  }
};
var make28 = () => {
  return new MetricRegistryImpl();
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/metric.js
var MetricSymbolKey = "effect/Metric";
var MetricTypeId = Symbol.for(MetricSymbolKey);
var metricVariance = {
  /* c8 ignore next */
  _Type: (_) => _,
  /* c8 ignore next */
  _In: (_) => _,
  /* c8 ignore next */
  _Out: (_) => _
};
var globalMetricRegistry = globalValue(Symbol.for("effect/Metric/globalMetricRegistry"), () => make28());
var make29 = function(keyType, unsafeUpdate, unsafeValue, unsafeModify) {
  const metric = Object.assign((effect) => tap(effect, (a) => update5(metric, a)), {
    [MetricTypeId]: metricVariance,
    keyType,
    unsafeUpdate,
    unsafeValue,
    unsafeModify,
    register() {
      this.unsafeValue([]);
      return this;
    },
    pipe() {
      return pipeArguments(this, arguments);
    }
  });
  return metric;
};
var mapInput3 = dual(2, (self, f) => make29(self.keyType, (input, extraTags) => self.unsafeUpdate(f(input), extraTags), self.unsafeValue, (input, extraTags) => self.unsafeModify(f(input), extraTags)));
var counter5 = (name, options) => fromMetricKey(counter2(name, options));
var frequency5 = (name, options) => fromMetricKey(frequency2(name, options));
var withConstantInput = dual(2, (self, input) => mapInput3(self, () => input));
var fromMetricKey = (key) => {
  let untaggedHook;
  const hookCache = /* @__PURE__ */ new WeakMap();
  const hook = (extraTags) => {
    if (extraTags.length === 0) {
      if (untaggedHook !== void 0) {
        return untaggedHook;
      }
      untaggedHook = globalMetricRegistry.get(key);
      return untaggedHook;
    }
    let hook2 = hookCache.get(extraTags);
    if (hook2 !== void 0) {
      return hook2;
    }
    hook2 = globalMetricRegistry.get(taggedWithLabels(key, extraTags));
    hookCache.set(extraTags, hook2);
    return hook2;
  };
  return make29(key.keyType, (input, extraTags) => hook(extraTags).update(input), (extraTags) => hook(extraTags).get(), (input, extraTags) => hook(extraTags).modify(input));
};
var gauge5 = (name, options) => fromMetricKey(gauge2(name, options));
var histogram5 = (name, boundaries, description) => fromMetricKey(histogram2(name, boundaries, description));
var increment2 = (self) => isCounterKey(self.keyType) ? update5(self, self.keyType.bigint ? BigInt(1) : 1) : modify4(self, self.keyType.bigint ? BigInt(1) : 1);
var incrementBy = dual(2, (self, amount) => isCounterKey(self.keyType) ? update5(self, amount) : modify4(self, amount));
var map11 = dual(2, (self, f) => make29(self.keyType, self.unsafeUpdate, (extraTags) => f(self.unsafeValue(extraTags)), self.unsafeModify));
var mapType = dual(2, (self, f) => make29(f(self.keyType), self.unsafeUpdate, self.unsafeValue, self.unsafeModify));
var modify4 = dual(2, (self, input) => fiberRefGetWith(currentMetricLabels, (tags) => sync(() => self.unsafeModify(input, tags))));
var set6 = dual(2, (self, value2) => update5(self, value2));
var succeed6 = (out) => make29(void 0, constVoid, () => out, constVoid);
var sync4 = (evaluate2) => make29(void 0, constVoid, evaluate2, constVoid);
var summary5 = (options) => withNow(summaryTimestamp(options));
var summaryTimestamp = (options) => fromMetricKey(summary2(options));
var tagged2 = dual(3, (self, key, value2) => taggedWithLabels2(self, [make20(key, value2)]));
var taggedWithLabelsInput = dual(2, (self, f) => map11(make29(self.keyType, (input, extraTags) => self.unsafeUpdate(input, union(f(input), extraTags)), self.unsafeValue, (input, extraTags) => self.unsafeModify(input, union(f(input), extraTags))), constVoid));
var taggedWithLabels2 = dual(2, (self, extraTags) => {
  return make29(self.keyType, (input, extraTags1) => self.unsafeUpdate(input, union(extraTags, extraTags1)), (extraTags1) => self.unsafeValue(union(extraTags, extraTags1)), (input, extraTags1) => self.unsafeModify(input, union(extraTags, extraTags1)));
});
var timer2 = (name, description) => {
  const boundaries = exponential({
    start: 0.5,
    factor: 2,
    count: 35
  });
  const base = pipe(histogram5(name, boundaries, description), tagged2("time_unit", "milliseconds"));
  return mapInput3(base, toMillis);
};
var timerWithBoundaries = (name, boundaries, description) => {
  const base = pipe(histogram5(name, fromIterable9(boundaries), description), tagged2("time_unit", "milliseconds"));
  return mapInput3(base, toMillis);
};
var trackAll = dual(2, (self, input) => (effect) => matchCauseEffect(effect, {
  onFailure: (cause2) => zipRight(update5(self, input), failCause(cause2)),
  onSuccess: (value2) => zipRight(update5(self, input), succeed(value2))
}));
var trackDefect = dual(2, (self, metric) => trackDefectWith(self, metric, identity));
var trackDefectWith = dual(3, (self, metric, f) => {
  const updater = (defect) => update5(metric, f(defect));
  return tapDefect2(self, (cause2) => forEachSequentialDiscard(defects(cause2), updater));
});
var trackDuration = dual(2, (self, metric) => trackDurationWith(self, metric, identity));
var trackDurationWith = dual(3, (self, metric, f) => clockWith2((clock2) => {
  const startTime = clock2.unsafeCurrentTimeNanos();
  return tap(self, (_) => {
    const endTime = clock2.unsafeCurrentTimeNanos();
    const duration = nanos(endTime - startTime);
    return update5(metric, f(duration));
  });
}));
var trackError = dual(2, (self, metric) => trackErrorWith(self, metric, (a) => a));
var trackErrorWith = dual(3, (self, metric, f) => {
  const updater = (error) => update5(metric, f(error));
  return tapError2(self, updater);
});
var trackSuccess = dual(2, (self, metric) => trackSuccessWith(self, metric, (a) => a));
var trackSuccessWith = dual(3, (self, metric, f) => {
  const updater = (value2) => update5(metric, f(value2));
  return tap(self, updater);
});
var update5 = dual(2, (self, input) => fiberRefGetWith(currentMetricLabels, (tags) => sync(() => self.unsafeUpdate(input, tags))));
var value = (self) => fiberRefGetWith(currentMetricLabels, (tags) => sync(() => self.unsafeValue(tags)));
var withNow = (self) => mapInput3(self, (input) => [input, Date.now()]);
var zip8 = dual(2, (self, that) => make29([self.keyType, that.keyType], (input, extraTags) => {
  const [l, r] = input;
  self.unsafeUpdate(l, extraTags);
  that.unsafeUpdate(r, extraTags);
}, (extraTags) => [self.unsafeValue(extraTags), that.unsafeValue(extraTags)], (input, extraTags) => {
  const [l, r] = input;
  self.unsafeModify(l, extraTags);
  that.unsafeModify(r, extraTags);
}));
var unsafeSnapshot = () => globalMetricRegistry.snapshot();
var snapshot = sync(unsafeSnapshot);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/request.js
var RequestSymbolKey = "effect/Request";
var RequestTypeId = Symbol.for(RequestSymbolKey);
var requestVariance = {
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _A: (_) => _
};
var RequestPrototype = {
  ...StructuralPrototype,
  [RequestTypeId]: requestVariance
};
var isRequest = (u) => hasProperty(u, RequestTypeId);
var of4 = () => (args2) => Object.assign(Object.create(RequestPrototype), args2);
var tagged3 = (tag) => (args2) => {
  const request = Object.assign(Object.create(RequestPrototype), args2);
  request._tag = tag;
  return request;
};
var Class2 = (function() {
  function Class3(args2) {
    if (args2) {
      Object.assign(this, args2);
    }
  }
  Class3.prototype = RequestPrototype;
  return Class3;
})();
var TaggedClass = (tag) => {
  return class TaggedClass extends Class2 {
    _tag = tag;
  };
};
var complete = dual(2, (self, result) => fiberRefGetWith(currentRequestMap, (map15) => sync(() => {
  if (map15.has(self)) {
    const entry = map15.get(self);
    if (!entry.state.completed) {
      entry.state.completed = true;
      deferredUnsafeDone(entry.result, result);
    }
  }
})));
var completeEffect = dual(2, (self, effect) => matchEffect(effect, {
  onFailure: (error) => complete(self, exitFail(error)),
  onSuccess: (value2) => complete(self, exitSucceed(value2))
}));
var fail6 = dual(2, (self, error) => complete(self, exitFail(error)));
var failCause5 = dual(2, (self, cause2) => complete(self, exitFailCause(cause2)));
var succeed7 = dual(2, (self, value2) => complete(self, exitSucceed(value2)));
var Listeners = class {
  count = 0;
  observers = /* @__PURE__ */ new Set();
  interrupted = false;
  addObserver(f) {
    this.observers.add(f);
  }
  removeObserver(f) {
    this.observers.delete(f);
  }
  increment() {
    this.count++;
    this.observers.forEach((f) => f(this.count));
  }
  decrement() {
    this.count--;
    this.observers.forEach((f) => f(this.count));
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/supervisor.js
var SupervisorSymbolKey = "effect/Supervisor";
var SupervisorTypeId = Symbol.for(SupervisorSymbolKey);
var supervisorVariance = {
  /* c8 ignore next */
  _T: (_) => _
};
var ProxySupervisor = class _ProxySupervisor {
  underlying;
  value0;
  [SupervisorTypeId] = supervisorVariance;
  constructor(underlying, value0) {
    this.underlying = underlying;
    this.value0 = value0;
  }
  get value() {
    return this.value0;
  }
  onStart(context4, effect, parent, fiber) {
    this.underlying.onStart(context4, effect, parent, fiber);
  }
  onEnd(value2, fiber) {
    this.underlying.onEnd(value2, fiber);
  }
  onEffect(fiber, effect) {
    this.underlying.onEffect(fiber, effect);
  }
  onSuspend(fiber) {
    this.underlying.onSuspend(fiber);
  }
  onResume(fiber) {
    this.underlying.onResume(fiber);
  }
  map(f) {
    return new _ProxySupervisor(this, pipe(this.value, map4(f)));
  }
  zip(right2) {
    return new Zip(this, right2);
  }
};
var Zip = class _Zip {
  left;
  right;
  _tag = "Zip";
  [SupervisorTypeId] = supervisorVariance;
  constructor(left2, right2) {
    this.left = left2;
    this.right = right2;
  }
  get value() {
    return zip3(this.left.value, this.right.value);
  }
  onStart(context4, effect, parent, fiber) {
    this.left.onStart(context4, effect, parent, fiber);
    this.right.onStart(context4, effect, parent, fiber);
  }
  onEnd(value2, fiber) {
    this.left.onEnd(value2, fiber);
    this.right.onEnd(value2, fiber);
  }
  onEffect(fiber, effect) {
    this.left.onEffect(fiber, effect);
    this.right.onEffect(fiber, effect);
  }
  onSuspend(fiber) {
    this.left.onSuspend(fiber);
    this.right.onSuspend(fiber);
  }
  onResume(fiber) {
    this.left.onResume(fiber);
    this.right.onResume(fiber);
  }
  map(f) {
    return new ProxySupervisor(this, pipe(this.value, map4(f)));
  }
  zip(right2) {
    return new _Zip(this, right2);
  }
};
var isZip = (self) => hasProperty(self, SupervisorTypeId) && isTagged(self, "Zip");
var Track = class {
  [SupervisorTypeId] = supervisorVariance;
  fibers = /* @__PURE__ */ new Set();
  get value() {
    return sync(() => Array.from(this.fibers));
  }
  onStart(_context, _effect, _parent, fiber) {
    this.fibers.add(fiber);
  }
  onEnd(_value, fiber) {
    this.fibers.delete(fiber);
  }
  onEffect(_fiber, _effect) {
  }
  onSuspend(_fiber) {
  }
  onResume(_fiber) {
  }
  map(f) {
    return new ProxySupervisor(this, pipe(this.value, map4(f)));
  }
  zip(right2) {
    return new Zip(this, right2);
  }
  onRun(execution, _fiber) {
    return execution();
  }
};
var Const = class {
  effect;
  [SupervisorTypeId] = supervisorVariance;
  constructor(effect) {
    this.effect = effect;
  }
  get value() {
    return this.effect;
  }
  onStart(_context, _effect, _parent, _fiber) {
  }
  onEnd(_value, _fiber) {
  }
  onEffect(_fiber, _effect) {
  }
  onSuspend(_fiber) {
  }
  onResume(_fiber) {
  }
  map(f) {
    return new ProxySupervisor(this, pipe(this.value, map4(f)));
  }
  zip(right2) {
    return new Zip(this, right2);
  }
  onRun(execution, _fiber) {
    return execution();
  }
};
var FibersIn = class {
  ref;
  [SupervisorTypeId] = supervisorVariance;
  constructor(ref) {
    this.ref = ref;
  }
  get value() {
    return sync(() => get2(this.ref));
  }
  onStart(_context, _effect, _parent, fiber) {
    pipe(this.ref, set(pipe(get2(this.ref), add2(fiber))));
  }
  onEnd(_value, fiber) {
    pipe(this.ref, set(pipe(get2(this.ref), remove2(fiber))));
  }
  onEffect(_fiber, _effect) {
  }
  onSuspend(_fiber) {
  }
  onResume(_fiber) {
  }
  map(f) {
    return new ProxySupervisor(this, pipe(this.value, map4(f)));
  }
  zip(right2) {
    return new Zip(this, right2);
  }
  onRun(execution, _fiber) {
    return execution();
  }
};
var unsafeTrack = () => {
  return new Track();
};
var track = sync(unsafeTrack);
var fromEffect2 = (effect) => {
  return new Const(effect);
};
var none8 = globalValue("effect/Supervisor/none", () => fromEffect2(void_));
var fibersIn = (ref) => sync(() => new FibersIn(ref));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/supervisor/patch.js
var OP_EMPTY2 = "Empty";
var OP_ADD_SUPERVISOR = "AddSupervisor";
var OP_REMOVE_SUPERVISOR = "RemoveSupervisor";
var OP_AND_THEN2 = "AndThen";
var empty20 = {
  _tag: OP_EMPTY2
};
var combine6 = (self, that) => {
  return {
    _tag: OP_AND_THEN2,
    first: self,
    second: that
  };
};
var patch6 = (self, supervisor) => {
  return patchLoop(supervisor, of2(self));
};
var patchLoop = (_supervisor, _patches) => {
  let supervisor = _supervisor;
  let patches = _patches;
  while (isNonEmpty(patches)) {
    const head5 = headNonEmpty2(patches);
    switch (head5._tag) {
      case OP_EMPTY2: {
        patches = tailNonEmpty2(patches);
        break;
      }
      case OP_ADD_SUPERVISOR: {
        supervisor = supervisor.zip(head5.supervisor);
        patches = tailNonEmpty2(patches);
        break;
      }
      case OP_REMOVE_SUPERVISOR: {
        supervisor = removeSupervisor(supervisor, head5.supervisor);
        patches = tailNonEmpty2(patches);
        break;
      }
      case OP_AND_THEN2: {
        patches = prepend2(head5.first)(prepend2(head5.second)(tailNonEmpty2(patches)));
        break;
      }
    }
  }
  return supervisor;
};
var removeSupervisor = (self, that) => {
  if (equals(self, that)) {
    return none8;
  } else {
    if (isZip(self)) {
      return removeSupervisor(self.left, that).zip(removeSupervisor(self.right, that));
    } else {
      return self;
    }
  }
};
var toSet = (self) => {
  if (equals(self, none8)) {
    return empty4();
  } else {
    if (isZip(self)) {
      return pipe(toSet(self.left), union2(toSet(self.right)));
    } else {
      return make4(self);
    }
  }
};
var diff5 = (oldValue, newValue) => {
  if (equals(oldValue, newValue)) {
    return empty20;
  }
  const oldSupervisors = toSet(oldValue);
  const newSupervisors = toSet(newValue);
  const added = pipe(newSupervisors, difference(oldSupervisors), reduce3(empty20, (patch7, supervisor) => combine6(patch7, {
    _tag: OP_ADD_SUPERVISOR,
    supervisor
  })));
  const removed = pipe(oldSupervisors, difference(newSupervisors), reduce3(empty20, (patch7, supervisor) => combine6(patch7, {
    _tag: OP_REMOVE_SUPERVISOR,
    supervisor
  })));
  return combine6(added, removed);
};
var differ = make25({
  empty: empty20,
  patch: patch6,
  combine: combine6,
  diff: diff5
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiberRuntime.js
var fiberStarted = counter5("effect_fiber_started", {
  incremental: true
});
var fiberActive = counter5("effect_fiber_active");
var fiberSuccesses = counter5("effect_fiber_successes", {
  incremental: true
});
var fiberFailures = counter5("effect_fiber_failures", {
  incremental: true
});
var fiberLifetimes = tagged2(histogram5("effect_fiber_lifetimes", exponential({
  start: 0.5,
  factor: 2,
  count: 35
})), "time_unit", "milliseconds");
var EvaluationSignalContinue = "Continue";
var EvaluationSignalDone = "Done";
var EvaluationSignalYieldNow = "Yield";
var runtimeFiberVariance = {
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _A: (_) => _
};
var absurd = (_) => {
  throw new Error(`BUG: FiberRuntime - ${toStringUnknown(_)} - please report an issue at https://github.com/Effect-TS/effect/issues`);
};
var YieldedOp = Symbol.for("effect/internal/fiberRuntime/YieldedOp");
var yieldedOpChannel = globalValue("effect/internal/fiberRuntime/yieldedOpChannel", () => ({
  currentOp: null
}));
var contOpSuccess = {
  [OP_ON_SUCCESS]: (_, cont, value2) => {
    return internalCall(() => cont.effect_instruction_i1(value2));
  },
  ["OnStep"]: (_, _cont, value2) => {
    return exitSucceed(exitSucceed(value2));
  },
  [OP_ON_SUCCESS_AND_FAILURE]: (_, cont, value2) => {
    return internalCall(() => cont.effect_instruction_i2(value2));
  },
  [OP_REVERT_FLAGS]: (self, cont, value2) => {
    self.patchRuntimeFlags(self.currentRuntimeFlags, cont.patch);
    if (interruptible(self.currentRuntimeFlags) && self.isInterrupted()) {
      return exitFailCause(self.getInterruptedCause());
    } else {
      return exitSucceed(value2);
    }
  },
  [OP_WHILE]: (self, cont, value2) => {
    internalCall(() => cont.effect_instruction_i2(value2));
    if (internalCall(() => cont.effect_instruction_i0())) {
      self.pushStack(cont);
      return internalCall(() => cont.effect_instruction_i1());
    } else {
      return void_;
    }
  },
  [OP_ITERATOR]: (self, cont, value2) => {
    while (true) {
      const state = internalCall(() => cont.effect_instruction_i0.next(value2));
      if (state.done) {
        return exitSucceed(state.value);
      }
      const primitive = yieldWrapGet(state.value);
      if (!exitIsExit(primitive)) {
        self.pushStack(cont);
        return primitive;
      } else if (primitive._tag === "Failure") {
        return primitive;
      }
      value2 = primitive.value;
    }
  }
};
var drainQueueWhileRunningTable = {
  [OP_INTERRUPT_SIGNAL]: (self, runtimeFlags2, cur, message) => {
    self.processNewInterruptSignal(message.cause);
    return interruptible(runtimeFlags2) ? exitFailCause(message.cause) : cur;
  },
  [OP_RESUME]: (_self, _runtimeFlags, _cur, _message) => {
    throw new Error("It is illegal to have multiple concurrent run loops in a single fiber");
  },
  [OP_STATEFUL]: (self, runtimeFlags2, cur, message) => {
    message.onFiber(self, running2(runtimeFlags2));
    return cur;
  },
  [OP_YIELD_NOW]: (_self, _runtimeFlags, cur, _message) => {
    return flatMap4(yieldNow(), () => cur);
  }
};
var runBlockedRequests = (self) => forEachSequentialDiscard(flatten2(self), (requestsByRequestResolver) => forEachConcurrentDiscard(sequentialCollectionToChunk(requestsByRequestResolver), ([dataSource, sequential5]) => {
  const map15 = /* @__PURE__ */ new Map();
  const arr = [];
  for (const block of sequential5) {
    arr.push(toReadonlyArray(block));
    for (const entry of block) {
      map15.set(entry.request, entry);
    }
  }
  const flat = arr.flat();
  return fiberRefLocally(invokeWithInterrupt(dataSource.runAll(arr), flat, () => flat.forEach((entry) => {
    entry.listeners.interrupted = true;
  })), currentRequestMap, map15);
}, false, false));
var _version = getCurrentVersion();
var FiberRuntime = class extends Class {
  [FiberTypeId] = fiberVariance2;
  [RuntimeFiberTypeId] = runtimeFiberVariance;
  _fiberRefs;
  _fiberId;
  _queue = new Array();
  _children = null;
  _observers = new Array();
  _running = false;
  _stack = [];
  _asyncInterruptor = null;
  _asyncBlockingOn = null;
  _exitValue = null;
  _steps = [];
  _isYielding = false;
  currentRuntimeFlags;
  currentOpCount = 0;
  currentSupervisor;
  currentScheduler;
  currentTracer;
  currentSpan;
  currentContext;
  currentDefaultServices;
  constructor(fiberId2, fiberRefs0, runtimeFlags0) {
    super();
    this.currentRuntimeFlags = runtimeFlags0;
    this._fiberId = fiberId2;
    this._fiberRefs = fiberRefs0;
    if (runtimeMetrics(runtimeFlags0)) {
      const tags = this.getFiberRef(currentMetricLabels);
      fiberStarted.unsafeUpdate(1, tags);
      fiberActive.unsafeUpdate(1, tags);
    }
    this.refreshRefCache();
  }
  commit() {
    return join2(this);
  }
  /**
   * The identity of the fiber.
   */
  id() {
    return this._fiberId;
  }
  /**
   * Begins execution of the effect associated with this fiber on in the
   * background. This can be called to "kick off" execution of a fiber after
   * it has been created.
   */
  resume(effect) {
    this.tell(resume(effect));
  }
  /**
   * The status of the fiber.
   */
  get status() {
    return this.ask((_, status3) => status3);
  }
  /**
   * Gets the fiber runtime flags.
   */
  get runtimeFlags() {
    return this.ask((state, status3) => {
      if (isDone2(status3)) {
        return state.currentRuntimeFlags;
      }
      return status3.runtimeFlags;
    });
  }
  /**
   * Returns the current `FiberScope` for the fiber.
   */
  scope() {
    return unsafeMake6(this);
  }
  /**
   * Retrieves the immediate children of the fiber.
   */
  get children() {
    return this.ask((fiber) => Array.from(fiber.getChildren()));
  }
  /**
   * Gets the fiber's set of children.
   */
  getChildren() {
    if (this._children === null) {
      this._children = /* @__PURE__ */ new Set();
    }
    return this._children;
  }
  /**
   * Retrieves the interrupted cause of the fiber, which will be `Cause.empty`
   * if the fiber has not been interrupted.
   *
   * **NOTE**: This method is safe to invoke on any fiber, but if not invoked
   * on this fiber, then values derived from the fiber's state (including the
   * log annotations and log level) may not be up-to-date.
   */
  getInterruptedCause() {
    return this.getFiberRef(currentInterruptedCause);
  }
  /**
   * Retrieves the whole set of fiber refs.
   */
  fiberRefs() {
    return this.ask((fiber) => fiber.getFiberRefs());
  }
  /**
   * Returns an effect that will contain information computed from the fiber
   * state and status while running on the fiber.
   *
   * This allows the outside world to interact safely with mutable fiber state
   * without locks or immutable data.
   */
  ask(f) {
    return suspend(() => {
      const deferred = deferredUnsafeMake(this._fiberId);
      this.tell(stateful((fiber, status3) => {
        deferredUnsafeDone(deferred, sync(() => f(fiber, status3)));
      }));
      return deferredAwait(deferred);
    });
  }
  /**
   * Adds a message to be processed by the fiber on the fiber.
   */
  tell(message) {
    this._queue.push(message);
    if (!this._running) {
      this._running = true;
      this.drainQueueLaterOnExecutor();
    }
  }
  get await() {
    return async_((resume2) => {
      const cb = (exit3) => resume2(succeed(exit3));
      if (this._exitValue !== null) {
        cb(this._exitValue);
        return;
      }
      this.tell(stateful((fiber, _) => {
        if (fiber._exitValue !== null) {
          cb(this._exitValue);
        } else {
          fiber.addObserver(cb);
        }
      }));
      return sync(() => this.tell(stateful((fiber, _) => {
        fiber.removeObserver(cb);
      })));
    }, this.id());
  }
  get inheritAll() {
    return withFiberRuntime((parentFiber, parentStatus) => {
      const parentFiberId = parentFiber.id();
      const parentFiberRefs = parentFiber.getFiberRefs();
      const parentRuntimeFlags = parentStatus.runtimeFlags;
      const childFiberRefs = this.getFiberRefs();
      const updatedFiberRefs = joinAs(parentFiberRefs, parentFiberId, childFiberRefs);
      parentFiber.setFiberRefs(updatedFiberRefs);
      const updatedRuntimeFlags = parentFiber.getFiberRef(currentRuntimeFlags);
      const patch7 = pipe(
        diff(parentRuntimeFlags, updatedRuntimeFlags),
        // Do not inherit WindDown or Interruption!
        exclude(Interruption),
        exclude(WindDown)
      );
      return updateRuntimeFlags(patch7);
    });
  }
  /**
   * Tentatively observes the fiber, but returns immediately if it is not
   * already done.
   */
  get poll() {
    return sync(() => fromNullable(this._exitValue));
  }
  /**
   * Unsafely observes the fiber, but returns immediately if it is not
   * already done.
   */
  unsafePoll() {
    return this._exitValue;
  }
  /**
   * In the background, interrupts the fiber as if interrupted from the specified fiber.
   */
  interruptAsFork(fiberId2) {
    return sync(() => this.tell(interruptSignal(interrupt(fiberId2))));
  }
  /**
   * In the background, interrupts the fiber as if interrupted from the specified fiber.
   */
  unsafeInterruptAsFork(fiberId2) {
    this.tell(interruptSignal(interrupt(fiberId2)));
  }
  /**
   * Adds an observer to the list of observers.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  addObserver(observer) {
    if (this._exitValue !== null) {
      observer(this._exitValue);
    } else {
      this._observers.push(observer);
    }
  }
  /**
   * Removes the specified observer from the list of observers that will be
   * notified when the fiber exits.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  removeObserver(observer) {
    this._observers = this._observers.filter((o) => o !== observer);
  }
  /**
   * Retrieves all fiber refs of the fiber.
   *
   * **NOTE**: This method is safe to invoke on any fiber, but if not invoked
   * on this fiber, then values derived from the fiber's state (including the
   * log annotations and log level) may not be up-to-date.
   */
  getFiberRefs() {
    this.setFiberRef(currentRuntimeFlags, this.currentRuntimeFlags);
    return this._fiberRefs;
  }
  /**
   * Deletes the specified fiber ref.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  unsafeDeleteFiberRef(fiberRef) {
    this._fiberRefs = delete_(this._fiberRefs, fiberRef);
  }
  /**
   * Retrieves the state of the fiber ref, or else its initial value.
   *
   * **NOTE**: This method is safe to invoke on any fiber, but if not invoked
   * on this fiber, then values derived from the fiber's state (including the
   * log annotations and log level) may not be up-to-date.
   */
  getFiberRef(fiberRef) {
    if (this._fiberRefs.locals.has(fiberRef)) {
      return this._fiberRefs.locals.get(fiberRef)[0][1];
    }
    return fiberRef.initial;
  }
  /**
   * Sets the fiber ref to the specified value.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  setFiberRef(fiberRef, value2) {
    this._fiberRefs = updateAs(this._fiberRefs, {
      fiberId: this._fiberId,
      fiberRef,
      value: value2
    });
    this.refreshRefCache();
  }
  refreshRefCache() {
    this.currentDefaultServices = this.getFiberRef(currentServices);
    this.currentTracer = this.currentDefaultServices.unsafeMap.get(tracerTag.key);
    this.currentSupervisor = this.getFiberRef(currentSupervisor);
    this.currentScheduler = this.getFiberRef(currentScheduler);
    this.currentContext = this.getFiberRef(currentContext);
    this.currentSpan = this.currentContext.unsafeMap.get(spanTag.key);
  }
  /**
   * Wholesale replaces all fiber refs of this fiber.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  setFiberRefs(fiberRefs4) {
    this._fiberRefs = fiberRefs4;
    this.refreshRefCache();
  }
  /**
   * Adds a reference to the specified fiber inside the children set.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  addChild(child) {
    this.getChildren().add(child);
  }
  /**
   * Removes a reference to the specified fiber inside the children set.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  removeChild(child) {
    this.getChildren().delete(child);
  }
  /**
   * Transfers all children of this fiber that are currently running to the
   * specified fiber scope.
   *
   * **NOTE**: This method must be invoked by the fiber itself after it has
   * evaluated the effects but prior to exiting.
   */
  transferChildren(scope4) {
    const children3 = this._children;
    this._children = null;
    if (children3 !== null && children3.size > 0) {
      for (const child of children3) {
        if (child._exitValue === null) {
          scope4.add(this.currentRuntimeFlags, child);
        }
      }
    }
  }
  /**
   * On the current thread, executes all messages in the fiber's inbox. This
   * method may return before all work is done, in the event the fiber executes
   * an asynchronous operation.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  drainQueueOnCurrentThread() {
    let recurse = true;
    while (recurse) {
      let evaluationSignal = EvaluationSignalContinue;
      const prev = globalThis[currentFiberURI];
      globalThis[currentFiberURI] = this;
      try {
        while (evaluationSignal === EvaluationSignalContinue) {
          evaluationSignal = this._queue.length === 0 ? EvaluationSignalDone : this.evaluateMessageWhileSuspended(this._queue.splice(0, 1)[0]);
        }
      } finally {
        this._running = false;
        globalThis[currentFiberURI] = prev;
      }
      if (this._queue.length > 0 && !this._running) {
        this._running = true;
        if (evaluationSignal === EvaluationSignalYieldNow) {
          this.drainQueueLaterOnExecutor();
          recurse = false;
        } else {
          recurse = true;
        }
      } else {
        recurse = false;
      }
    }
  }
  /**
   * Schedules the execution of all messages in the fiber's inbox.
   *
   * This method will return immediately after the scheduling
   * operation is completed, but potentially before such messages have been
   * executed.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  drainQueueLaterOnExecutor() {
    this.currentScheduler.scheduleTask(this.run, this.getFiberRef(currentSchedulingPriority));
  }
  /**
   * Drains the fiber's message queue while the fiber is actively running,
   * returning the next effect to execute, which may be the input effect if no
   * additional effect needs to be executed.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  drainQueueWhileRunning(runtimeFlags2, cur0) {
    let cur = cur0;
    while (this._queue.length > 0) {
      const message = this._queue.splice(0, 1)[0];
      cur = drainQueueWhileRunningTable[message._tag](this, runtimeFlags2, cur, message);
    }
    return cur;
  }
  /**
   * Determines if the fiber is interrupted.
   *
   * **NOTE**: This method is safe to invoke on any fiber, but if not invoked
   * on this fiber, then values derived from the fiber's state (including the
   * log annotations and log level) may not be up-to-date.
   */
  isInterrupted() {
    return !isEmpty2(this.getFiberRef(currentInterruptedCause));
  }
  /**
   * Adds an interruptor to the set of interruptors that are interrupting this
   * fiber.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  addInterruptedCause(cause2) {
    const oldSC = this.getFiberRef(currentInterruptedCause);
    this.setFiberRef(currentInterruptedCause, sequential(oldSC, cause2));
  }
  /**
   * Processes a new incoming interrupt signal.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  processNewInterruptSignal(cause2) {
    this.addInterruptedCause(cause2);
    this.sendInterruptSignalToAllChildren();
  }
  /**
   * Interrupts all children of the current fiber, returning an effect that will
   * await the exit of the children. This method will return null if the fiber
   * has no children.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  sendInterruptSignalToAllChildren() {
    if (this._children === null || this._children.size === 0) {
      return false;
    }
    let told = false;
    for (const child of this._children) {
      child.tell(interruptSignal(interrupt(this.id())));
      told = true;
    }
    return told;
  }
  /**
   * Interrupts all children of the current fiber, returning an effect that will
   * await the exit of the children. This method will return null if the fiber
   * has no children.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  interruptAllChildren() {
    if (this.sendInterruptSignalToAllChildren()) {
      const it = this._children.values();
      this._children = null;
      let isDone6 = false;
      const body = () => {
        const next2 = it.next();
        if (!next2.done) {
          return asVoid(next2.value.await);
        } else {
          return sync(() => {
            isDone6 = true;
          });
        }
      };
      return whileLoop({
        while: () => !isDone6,
        body,
        step: () => {
        }
      });
    }
    return null;
  }
  reportExitValue(exit3) {
    if (runtimeMetrics(this.currentRuntimeFlags)) {
      const tags = this.getFiberRef(currentMetricLabels);
      const startTimeMillis = this.id().startTimeMillis;
      const endTimeMillis = Date.now();
      fiberLifetimes.unsafeUpdate(endTimeMillis - startTimeMillis, tags);
      fiberActive.unsafeUpdate(-1, tags);
      switch (exit3._tag) {
        case OP_SUCCESS: {
          fiberSuccesses.unsafeUpdate(1, tags);
          break;
        }
        case OP_FAILURE: {
          fiberFailures.unsafeUpdate(1, tags);
          break;
        }
      }
    }
    if (exit3._tag === "Failure") {
      const level = this.getFiberRef(currentUnhandledErrorLogLevel);
      if (!isInterruptedOnly(exit3.cause) && level._tag === "Some") {
        this.log("Fiber terminated with an unhandled error", exit3.cause, level);
      }
    }
  }
  setExitValue(exit3) {
    this._exitValue = exit3;
    this.reportExitValue(exit3);
    for (let i = this._observers.length - 1; i >= 0; i--) {
      this._observers[i](exit3);
    }
    this._observers = [];
  }
  getLoggers() {
    return this.getFiberRef(currentLoggers);
  }
  log(message, cause2, overrideLogLevel) {
    const logLevel = isSome(overrideLogLevel) ? overrideLogLevel.value : this.getFiberRef(currentLogLevel);
    const minimumLogLevel = this.getFiberRef(currentMinimumLogLevel);
    if (greaterThan3(minimumLogLevel, logLevel)) {
      return;
    }
    const spans = this.getFiberRef(currentLogSpan);
    const annotations = this.getFiberRef(currentLogAnnotations);
    const loggers = this.getLoggers();
    const contextMap = this.getFiberRefs();
    if (size(loggers) > 0) {
      const clockService = get(this.getFiberRef(currentServices), clockTag);
      const date = new Date(clockService.unsafeCurrentTimeMillis());
      withRedactableContext(contextMap, () => {
        for (const logger of loggers) {
          logger.log({
            fiberId: this.id(),
            logLevel,
            message,
            cause: cause2,
            context: contextMap,
            spans,
            annotations,
            date
          });
        }
      });
    }
  }
  /**
   * Evaluates a single message on the current thread, while the fiber is
   * suspended. This method should only be called while evaluation of the
   * fiber's effect is suspended due to an asynchronous operation.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  evaluateMessageWhileSuspended(message) {
    switch (message._tag) {
      case OP_YIELD_NOW: {
        return EvaluationSignalYieldNow;
      }
      case OP_INTERRUPT_SIGNAL: {
        this.processNewInterruptSignal(message.cause);
        if (this._asyncInterruptor !== null) {
          this._asyncInterruptor(exitFailCause(message.cause));
          this._asyncInterruptor = null;
        }
        return EvaluationSignalContinue;
      }
      case OP_RESUME: {
        this._asyncInterruptor = null;
        this._asyncBlockingOn = null;
        this.evaluateEffect(message.effect);
        return EvaluationSignalContinue;
      }
      case OP_STATEFUL: {
        message.onFiber(this, this._exitValue !== null ? done2 : suspended2(this.currentRuntimeFlags, this._asyncBlockingOn));
        return EvaluationSignalContinue;
      }
      default: {
        return absurd(message);
      }
    }
  }
  /**
   * Evaluates an effect until completion, potentially asynchronously.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  evaluateEffect(effect0) {
    this.currentSupervisor.onResume(this);
    try {
      let effect = interruptible(this.currentRuntimeFlags) && this.isInterrupted() ? exitFailCause(this.getInterruptedCause()) : effect0;
      while (effect !== null) {
        const eff = effect;
        const exit3 = this.runLoop(eff);
        if (exit3 === YieldedOp) {
          const op = yieldedOpChannel.currentOp;
          yieldedOpChannel.currentOp = null;
          if (op._op === OP_YIELD) {
            if (cooperativeYielding(this.currentRuntimeFlags)) {
              this.tell(yieldNow3());
              this.tell(resume(exitVoid));
              effect = null;
            } else {
              effect = exitVoid;
            }
          } else if (op._op === OP_ASYNC) {
            effect = null;
          }
        } else {
          this.currentRuntimeFlags = pipe(this.currentRuntimeFlags, enable(WindDown));
          const interruption = this.interruptAllChildren();
          if (interruption !== null) {
            effect = flatMap4(interruption, () => exit3);
          } else {
            if (this._queue.length === 0) {
              this.setExitValue(exit3);
            } else {
              this.tell(resume(exit3));
            }
            effect = null;
          }
        }
      }
    } finally {
      this.currentSupervisor.onSuspend(this);
    }
  }
  /**
   * Begins execution of the effect associated with this fiber on the current
   * thread. This can be called to "kick off" execution of a fiber after it has
   * been created, in hopes that the effect can be executed synchronously.
   *
   * This is not the normal way of starting a fiber, but it is useful when the
   * express goal of executing the fiber is to synchronously produce its exit.
   */
  start(effect) {
    if (!this._running) {
      this._running = true;
      const prev = globalThis[currentFiberURI];
      globalThis[currentFiberURI] = this;
      try {
        this.evaluateEffect(effect);
      } finally {
        this._running = false;
        globalThis[currentFiberURI] = prev;
        if (this._queue.length > 0) {
          this.drainQueueLaterOnExecutor();
        }
      }
    } else {
      this.tell(resume(effect));
    }
  }
  /**
   * Begins execution of the effect associated with this fiber on in the
   * background, and on the correct thread pool. This can be called to "kick
   * off" execution of a fiber after it has been created, in hopes that the
   * effect can be executed synchronously.
   */
  startFork(effect) {
    this.tell(resume(effect));
  }
  /**
   * Takes the current runtime flags, patches them to return the new runtime
   * flags, and then makes any changes necessary to fiber state based on the
   * specified patch.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  patchRuntimeFlags(oldRuntimeFlags, patch7) {
    const newRuntimeFlags = patch(oldRuntimeFlags, patch7);
    globalThis[currentFiberURI] = this;
    this.currentRuntimeFlags = newRuntimeFlags;
    return newRuntimeFlags;
  }
  /**
   * Initiates an asynchronous operation, by building a callback that will
   * resume execution, and then feeding that callback to the registration
   * function, handling error cases and repeated resumptions appropriately.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  initiateAsync(runtimeFlags2, asyncRegister) {
    let alreadyCalled = false;
    const callback = (effect) => {
      if (!alreadyCalled) {
        alreadyCalled = true;
        this.tell(resume(effect));
      }
    };
    if (interruptible(runtimeFlags2)) {
      this._asyncInterruptor = callback;
    }
    try {
      asyncRegister(callback);
    } catch (e) {
      callback(failCause(die(e)));
    }
  }
  pushStack(cont) {
    this._stack.push(cont);
    if (cont._op === "OnStep") {
      this._steps.push({
        refs: this.getFiberRefs(),
        flags: this.currentRuntimeFlags
      });
    }
  }
  popStack() {
    const item = this._stack.pop();
    if (item) {
      if (item._op === "OnStep") {
        this._steps.pop();
      }
      return item;
    }
    return;
  }
  getNextSuccessCont() {
    let frame = this.popStack();
    while (frame) {
      if (frame._op !== OP_ON_FAILURE) {
        return frame;
      }
      frame = this.popStack();
    }
  }
  getNextFailCont() {
    let frame = this.popStack();
    while (frame) {
      if (frame._op !== OP_ON_SUCCESS && frame._op !== OP_WHILE && frame._op !== OP_ITERATOR) {
        return frame;
      }
      frame = this.popStack();
    }
  }
  [OP_TAG](op) {
    return sync(() => unsafeGet2(this.currentContext, op));
  }
  ["Left"](op) {
    return fail2(op.left);
  }
  ["None"](_) {
    return fail2(new NoSuchElementException());
  }
  ["Right"](op) {
    return exitSucceed(op.right);
  }
  ["Some"](op) {
    return exitSucceed(op.value);
  }
  ["Micro"](op) {
    return unsafeAsync((microResume) => {
      let resume2 = microResume;
      const fiber = runFork(provideContext2(op, this.currentContext));
      fiber.addObserver((exit3) => {
        if (exit3._tag === "Success") {
          return resume2(exitSucceed(exit3.value));
        }
        switch (exit3.cause._tag) {
          case "Interrupt": {
            return resume2(exitFailCause(interrupt(none4)));
          }
          case "Fail": {
            return resume2(fail2(exit3.cause.error));
          }
          case "Die": {
            return resume2(die2(exit3.cause.defect));
          }
        }
      });
      return unsafeAsync((abortResume) => {
        resume2 = (_) => {
          abortResume(void_);
        };
        fiber.unsafeInterrupt();
      });
    });
  }
  [OP_SYNC](op) {
    const value2 = internalCall(() => op.effect_instruction_i0());
    const cont = this.getNextSuccessCont();
    if (cont !== void 0) {
      if (!(cont._op in contOpSuccess)) {
        absurd(cont);
      }
      return contOpSuccess[cont._op](this, cont, value2);
    } else {
      yieldedOpChannel.currentOp = exitSucceed(value2);
      return YieldedOp;
    }
  }
  [OP_SUCCESS](op) {
    const oldCur = op;
    const cont = this.getNextSuccessCont();
    if (cont !== void 0) {
      if (!(cont._op in contOpSuccess)) {
        absurd(cont);
      }
      return contOpSuccess[cont._op](this, cont, oldCur.effect_instruction_i0);
    } else {
      yieldedOpChannel.currentOp = oldCur;
      return YieldedOp;
    }
  }
  [OP_FAILURE](op) {
    const cause2 = op.effect_instruction_i0;
    const cont = this.getNextFailCont();
    if (cont !== void 0) {
      switch (cont._op) {
        case OP_ON_FAILURE:
        case OP_ON_SUCCESS_AND_FAILURE: {
          if (!(interruptible(this.currentRuntimeFlags) && this.isInterrupted())) {
            return internalCall(() => cont.effect_instruction_i1(cause2));
          } else {
            return exitFailCause(stripFailures(cause2));
          }
        }
        case "OnStep": {
          if (!(interruptible(this.currentRuntimeFlags) && this.isInterrupted())) {
            return exitSucceed(exitFailCause(cause2));
          } else {
            return exitFailCause(stripFailures(cause2));
          }
        }
        case OP_REVERT_FLAGS: {
          this.patchRuntimeFlags(this.currentRuntimeFlags, cont.patch);
          if (interruptible(this.currentRuntimeFlags) && this.isInterrupted()) {
            return exitFailCause(sequential(cause2, this.getInterruptedCause()));
          } else {
            return exitFailCause(cause2);
          }
        }
        default: {
          absurd(cont);
        }
      }
    } else {
      yieldedOpChannel.currentOp = exitFailCause(cause2);
      return YieldedOp;
    }
  }
  [OP_WITH_RUNTIME](op) {
    return internalCall(() => op.effect_instruction_i0(this, running2(this.currentRuntimeFlags)));
  }
  ["Blocked"](op) {
    const refs = this.getFiberRefs();
    const flags = this.currentRuntimeFlags;
    if (this._steps.length > 0) {
      const frames = [];
      const snap = this._steps[this._steps.length - 1];
      let frame = this.popStack();
      while (frame && frame._op !== "OnStep") {
        frames.push(frame);
        frame = this.popStack();
      }
      this.setFiberRefs(snap.refs);
      this.currentRuntimeFlags = snap.flags;
      const patchRefs = diff3(snap.refs, refs);
      const patchFlags = diff(snap.flags, flags);
      return exitSucceed(blocked(op.effect_instruction_i0, withFiberRuntime((newFiber) => {
        while (frames.length > 0) {
          newFiber.pushStack(frames.pop());
        }
        newFiber.setFiberRefs(patch4(newFiber.id(), newFiber.getFiberRefs())(patchRefs));
        newFiber.currentRuntimeFlags = patch(patchFlags)(newFiber.currentRuntimeFlags);
        return op.effect_instruction_i1;
      })));
    }
    return uninterruptibleMask((restore) => flatMap4(forkDaemon2(runRequestBlock(op.effect_instruction_i0)), () => restore(op.effect_instruction_i1)));
  }
  ["RunBlocked"](op) {
    return runBlockedRequests(op.effect_instruction_i0);
  }
  [OP_UPDATE_RUNTIME_FLAGS](op) {
    const updateFlags = op.effect_instruction_i0;
    const oldRuntimeFlags = this.currentRuntimeFlags;
    const newRuntimeFlags = patch(oldRuntimeFlags, updateFlags);
    if (interruptible(newRuntimeFlags) && this.isInterrupted()) {
      return exitFailCause(this.getInterruptedCause());
    } else {
      this.patchRuntimeFlags(this.currentRuntimeFlags, updateFlags);
      if (op.effect_instruction_i1) {
        const revertFlags = diff(newRuntimeFlags, oldRuntimeFlags);
        this.pushStack(new RevertFlags(revertFlags, op));
        return internalCall(() => op.effect_instruction_i1(oldRuntimeFlags));
      } else {
        return exitVoid;
      }
    }
  }
  [OP_ON_SUCCESS](op) {
    this.pushStack(op);
    return op.effect_instruction_i0;
  }
  ["OnStep"](op) {
    this.pushStack(op);
    return op.effect_instruction_i0;
  }
  [OP_ON_FAILURE](op) {
    this.pushStack(op);
    return op.effect_instruction_i0;
  }
  [OP_ON_SUCCESS_AND_FAILURE](op) {
    this.pushStack(op);
    return op.effect_instruction_i0;
  }
  [OP_ASYNC](op) {
    this._asyncBlockingOn = op.effect_instruction_i1;
    this.initiateAsync(this.currentRuntimeFlags, op.effect_instruction_i0);
    yieldedOpChannel.currentOp = op;
    return YieldedOp;
  }
  [OP_YIELD](op) {
    this._isYielding = false;
    yieldedOpChannel.currentOp = op;
    return YieldedOp;
  }
  [OP_WHILE](op) {
    const check = op.effect_instruction_i0;
    const body = op.effect_instruction_i1;
    if (check()) {
      this.pushStack(op);
      return body();
    } else {
      return exitVoid;
    }
  }
  [OP_ITERATOR](op) {
    return contOpSuccess[OP_ITERATOR](this, op, void 0);
  }
  [OP_COMMIT](op) {
    return internalCall(() => op.commit());
  }
  /**
   * The main run-loop for evaluating effects.
   *
   * **NOTE**: This method must be invoked by the fiber itself.
   */
  runLoop(effect0) {
    let cur = effect0;
    this.currentOpCount = 0;
    while (true) {
      if ((this.currentRuntimeFlags & OpSupervision) !== 0) {
        this.currentSupervisor.onEffect(this, cur);
      }
      if (this._queue.length > 0) {
        cur = this.drainQueueWhileRunning(this.currentRuntimeFlags, cur);
      }
      if (!this._isYielding) {
        this.currentOpCount += 1;
        const shouldYield = this.currentScheduler.shouldYield(this);
        if (shouldYield !== false) {
          this._isYielding = true;
          this.currentOpCount = 0;
          const oldCur = cur;
          cur = flatMap4(yieldNow({
            priority: shouldYield
          }), () => oldCur);
        }
      }
      try {
        cur = this.currentTracer.context(() => {
          if (_version !== cur[EffectTypeId2]._V) {
            const level = this.getFiberRef(currentVersionMismatchErrorLogLevel);
            if (level._tag === "Some") {
              const effectVersion = cur[EffectTypeId2]._V;
              this.log(`Executing an Effect versioned ${effectVersion} with a Runtime of version ${getCurrentVersion()}, you may want to dedupe the effect dependencies, you can use the language service plugin to detect this at compile time: https://github.com/Effect-TS/language-service`, empty9, level);
            }
          }
          return this[cur._op](cur);
        }, this);
        if (cur === YieldedOp) {
          const op = yieldedOpChannel.currentOp;
          if (op._op === OP_YIELD || op._op === OP_ASYNC) {
            return YieldedOp;
          }
          yieldedOpChannel.currentOp = null;
          return op._op === OP_SUCCESS || op._op === OP_FAILURE ? op : exitFailCause(die(op));
        }
      } catch (e) {
        if (cur !== YieldedOp && !hasProperty(cur, "_op") || !(cur._op in this)) {
          cur = dieMessage(`Not a valid effect: ${toStringUnknown(cur)}`);
        } else if (isInterruptedException(e)) {
          cur = exitFailCause(sequential(die(e), interrupt(none4)));
        } else {
          cur = die2(e);
        }
      }
    }
  }
  run = () => {
    this.drainQueueOnCurrentThread();
  };
};
var currentMinimumLogLevel = globalValue("effect/FiberRef/currentMinimumLogLevel", () => fiberRefUnsafeMake(fromLiteral("Info")));
var loggerWithConsoleLog = (self) => makeLogger((opts) => {
  const services = getOrDefault2(opts.context, currentServices);
  get(services, consoleTag).unsafe.log(self.log(opts));
});
var loggerWithLeveledLog = (self) => makeLogger((opts) => {
  const services = getOrDefault2(opts.context, currentServices);
  const unsafeLogger = get(services, consoleTag).unsafe;
  switch (opts.logLevel._tag) {
    case "Debug":
      return unsafeLogger.debug(self.log(opts));
    case "Info":
      return unsafeLogger.info(self.log(opts));
    case "Trace":
      return unsafeLogger.trace(self.log(opts));
    case "Warning":
      return unsafeLogger.warn(self.log(opts));
    case "Error":
    case "Fatal":
      return unsafeLogger.error(self.log(opts));
    default:
      return unsafeLogger.log(self.log(opts));
  }
});
var loggerWithConsoleError = (self) => makeLogger((opts) => {
  const services = getOrDefault2(opts.context, currentServices);
  get(services, consoleTag).unsafe.error(self.log(opts));
});
var defaultLogger = globalValue(Symbol.for("effect/Logger/defaultLogger"), () => loggerWithConsoleLog(stringLogger));
var jsonLogger2 = globalValue(Symbol.for("effect/Logger/jsonLogger"), () => loggerWithConsoleLog(jsonLogger));
var logFmtLogger = globalValue(Symbol.for("effect/Logger/logFmtLogger"), () => loggerWithConsoleLog(logfmtLogger));
var prettyLogger2 = globalValue(Symbol.for("effect/Logger/prettyLogger"), () => prettyLoggerDefault);
var structuredLogger2 = globalValue(Symbol.for("effect/Logger/structuredLogger"), () => loggerWithConsoleLog(structuredLogger));
var tracerLogger = globalValue(Symbol.for("effect/Logger/tracerLogger"), () => makeLogger(({
  annotations,
  cause: cause2,
  context: context4,
  fiberId: fiberId2,
  logLevel,
  message
}) => {
  const span2 = getOption(getOrDefault(context4, currentContext), spanTag);
  if (span2._tag === "None" || span2.value._tag === "ExternalSpan") {
    return;
  }
  const clockService = unsafeGet2(getOrDefault(context4, currentServices), clockTag);
  const attributes = {};
  for (const [key, value2] of annotations) {
    attributes[key] = value2;
  }
  attributes["effect.fiberId"] = threadName2(fiberId2);
  attributes["effect.logLevel"] = logLevel.label;
  if (cause2 !== null && cause2._tag !== "Empty") {
    attributes["effect.cause"] = pretty(cause2, {
      renderErrorCause: true
    });
  }
  span2.value.event(toStringUnknown(Array.isArray(message) && message.length === 1 ? message[0] : message), clockService.unsafeCurrentTimeNanos(), attributes);
}));
var loggerWithSpanAnnotations = (self) => mapInputOptions(self, (options) => {
  const span2 = flatMap(get3(options.context, currentContext), getOption(spanTag));
  if (span2._tag === "None") {
    return options;
  }
  return {
    ...options,
    annotations: pipe(options.annotations, set2("effect.traceId", span2.value.traceId), set2("effect.spanId", span2.value.spanId), span2.value._tag === "Span" ? set2("effect.spanName", span2.value.name) : identity)
  };
});
var currentLoggers = globalValue(Symbol.for("effect/FiberRef/currentLoggers"), () => fiberRefUnsafeMakeHashSet(make4(defaultLogger, tracerLogger)));
var batchedLogger = dual(3, (self, window, f) => flatMap4(scope2, (scope4) => {
  let buffer = [];
  const flush = suspend(() => {
    if (buffer.length === 0) {
      return void_;
    }
    const arr = buffer;
    buffer = [];
    return f(arr);
  });
  return uninterruptibleMask((restore) => pipe(sleep4(window), zipRight(flush), forever2, restore, forkDaemon2, flatMap4((fiber) => scopeAddFinalizer(scope4, interruptFiber(fiber))), zipRight(addFinalizer2(() => flush)), as2(makeLogger((options) => {
    buffer.push(self.log(options));
  }))));
}));
var annotateLogsScoped = function() {
  if (typeof arguments[0] === "string") {
    return fiberRefLocallyScopedWith(currentLogAnnotations, set2(arguments[0], arguments[1]));
  }
  const entries = Object.entries(arguments[0]);
  return fiberRefLocallyScopedWith(currentLogAnnotations, mutate((annotations) => {
    for (let i = 0; i < entries.length; i++) {
      const [key, value2] = entries[i];
      set2(annotations, key, value2);
    }
    return annotations;
  }));
};
var whenLogLevel = dual(2, (effect, level) => {
  const requiredLogLevel = typeof level === "string" ? fromLiteral(level) : level;
  return withFiberRuntime((fiberState) => {
    const minimumLogLevel = fiberState.getFiberRef(currentMinimumLogLevel);
    if (greaterThan3(minimumLogLevel, requiredLogLevel)) {
      return succeed(none2());
    }
    return map4(effect, some2);
  });
});
var acquireRelease2 = dual((args2) => isEffect(args2[0]), (acquire, release) => uninterruptible(tap(acquire, (a) => addFinalizer2((exit3) => release(a, exit3)))));
var acquireReleaseInterruptible = dual((args2) => isEffect(args2[0]), (acquire, release) => ensuring2(acquire, addFinalizer2((exit3) => release(exit3))));
var addFinalizer2 = (finalizer) => withFiberRuntime((runtime2) => {
  const acquireRefs = runtime2.getFiberRefs();
  const acquireFlags = disable(runtime2.currentRuntimeFlags, Interruption);
  return flatMap4(scope2, (scope4) => scopeAddFinalizerExit(scope4, (exit3) => withFiberRuntime((runtimeFinalizer) => {
    const preRefs = runtimeFinalizer.getFiberRefs();
    const preFlags = runtimeFinalizer.currentRuntimeFlags;
    const patchRefs = diff3(preRefs, acquireRefs);
    const patchFlags = diff(preFlags, acquireFlags);
    const inverseRefs = diff3(acquireRefs, preRefs);
    runtimeFinalizer.setFiberRefs(patch4(patchRefs, runtimeFinalizer.id(), acquireRefs));
    return ensuring2(withRuntimeFlags(finalizer(exit3), patchFlags), sync(() => {
      runtimeFinalizer.setFiberRefs(patch4(inverseRefs, runtimeFinalizer.id(), runtimeFinalizer.getFiberRefs()));
    }));
  })));
});
var daemonChildren = (self) => {
  const forkScope = fiberRefLocally(currentForkScopeOverride, some2(globalScope));
  return forkScope(self);
};
var _existsParFound = Symbol.for("effect/Effect/existsPar/found");
var exists2 = dual((args2) => isIterable(args2[0]) && !isEffect(args2[0]), (elements, predicate, options) => matchSimple(options?.concurrency, () => suspend(() => existsLoop(elements[Symbol.iterator](), 0, predicate)), () => matchEffect(forEach7(elements, (a, i) => if_(predicate(a, i), {
  onTrue: () => fail2(_existsParFound),
  onFalse: () => void_
}), options), {
  onFailure: (e) => e === _existsParFound ? succeed(true) : fail2(e),
  onSuccess: () => succeed(false)
})));
var existsLoop = (iterator, index, f) => {
  const next2 = iterator.next();
  if (next2.done) {
    return succeed(false);
  }
  return flatMap4(f(next2.value, index), (b) => b ? succeed(b) : existsLoop(iterator, index + 1, f));
};
var filter4 = dual((args2) => isIterable(args2[0]) && !isEffect(args2[0]), (elements, predicate, options) => {
  const predicate_ = options?.negate ? (a, i) => map4(predicate(a, i), not) : predicate;
  return matchSimple(options?.concurrency, () => suspend(() => fromIterable(elements).reduceRight((effect, a, i) => zipWith2(effect, suspend(() => predicate_(a, i)), (list, b) => b ? [a, ...list] : list), sync(() => new Array()))), () => map4(forEach7(elements, (a, i) => map4(predicate_(a, i), (b) => b ? some2(a) : none2()), options), getSomes));
});
var allResolveInput = (input) => {
  if (Array.isArray(input) || isIterable(input)) {
    return [input, none2()];
  }
  const keys4 = Object.keys(input);
  const size11 = keys4.length;
  return [keys4.map((k) => input[k]), some2((values5) => {
    const res = {};
    for (let i = 0; i < size11; i++) {
      ;
      res[keys4[i]] = values5[i];
    }
    return res;
  })];
};
var allValidate = (effects, reconcile, options) => {
  const eitherEffects = [];
  for (const effect of effects) {
    eitherEffects.push(either(effect));
  }
  return flatMap4(forEach7(eitherEffects, identity, {
    concurrency: options?.concurrency,
    batching: options?.batching,
    concurrentFinalizers: options?.concurrentFinalizers
  }), (eithers) => {
    const none9 = none2();
    const size11 = eithers.length;
    const errors = new Array(size11);
    const successes = new Array(size11);
    let errored = false;
    for (let i = 0; i < size11; i++) {
      const either3 = eithers[i];
      if (either3._tag === "Left") {
        errors[i] = some2(either3.left);
        errored = true;
      } else {
        successes[i] = either3.right;
        errors[i] = none9;
      }
    }
    if (errored) {
      return reconcile._tag === "Some" ? fail2(reconcile.value(errors)) : fail2(errors);
    } else if (options?.discard) {
      return void_;
    }
    return reconcile._tag === "Some" ? succeed(reconcile.value(successes)) : succeed(successes);
  });
};
var allEither = (effects, reconcile, options) => {
  const eitherEffects = [];
  for (const effect of effects) {
    eitherEffects.push(either(effect));
  }
  if (options?.discard) {
    return forEach7(eitherEffects, identity, {
      concurrency: options?.concurrency,
      batching: options?.batching,
      discard: true,
      concurrentFinalizers: options?.concurrentFinalizers
    });
  }
  return map4(forEach7(eitherEffects, identity, {
    concurrency: options?.concurrency,
    batching: options?.batching,
    concurrentFinalizers: options?.concurrentFinalizers
  }), (eithers) => reconcile._tag === "Some" ? reconcile.value(eithers) : eithers);
};
var all4 = (arg, options) => {
  const [effects, reconcile] = allResolveInput(arg);
  if (options?.mode === "validate") {
    return allValidate(effects, reconcile, options);
  } else if (options?.mode === "either") {
    return allEither(effects, reconcile, options);
  }
  return options?.discard !== true && reconcile._tag === "Some" ? map4(forEach7(effects, identity, options), reconcile.value) : forEach7(effects, identity, options);
};
var allWith = (options) => (arg) => all4(arg, options);
var allSuccesses = (elements, options) => map4(all4(fromIterable(elements).map(exit), options), filterMap((exit3) => exitIsSuccess(exit3) ? some2(exit3.effect_instruction_i0) : none2()));
var replicate2 = dual(2, (self, n) => Array.from({
  length: n
}, () => self));
var replicateEffect2 = dual((args2) => isEffect(args2[0]), (self, n, options) => all4(replicate2(self, n), options));
var forEach7 = dual((args2) => isIterable(args2[0]), (self, f, options) => withFiberRuntime((r) => {
  const isRequestBatchingEnabled = options?.batching === true || options?.batching === "inherit" && r.getFiberRef(currentRequestBatching);
  if (options?.discard) {
    return match10(options.concurrency, () => finalizersMaskInternal(sequential3, options?.concurrentFinalizers)((restore) => isRequestBatchingEnabled ? forEachConcurrentDiscard(self, (a, i) => restore(f(a, i)), true, false, 1) : forEachSequentialDiscard(self, (a, i) => restore(f(a, i)))), () => finalizersMaskInternal(parallel3, options?.concurrentFinalizers)((restore) => forEachConcurrentDiscard(self, (a, i) => restore(f(a, i)), isRequestBatchingEnabled, false)), (n) => finalizersMaskInternal(parallelN2(n), options?.concurrentFinalizers)((restore) => forEachConcurrentDiscard(self, (a, i) => restore(f(a, i)), isRequestBatchingEnabled, false, n)));
  }
  return match10(options?.concurrency, () => finalizersMaskInternal(sequential3, options?.concurrentFinalizers)((restore) => isRequestBatchingEnabled ? forEachParN(self, 1, (a, i) => restore(f(a, i)), true) : forEachSequential(self, (a, i) => restore(f(a, i)))), () => finalizersMaskInternal(parallel3, options?.concurrentFinalizers)((restore) => forEachParUnbounded(self, (a, i) => restore(f(a, i)), isRequestBatchingEnabled)), (n) => finalizersMaskInternal(parallelN2(n), options?.concurrentFinalizers)((restore) => forEachParN(self, n, (a, i) => restore(f(a, i)), isRequestBatchingEnabled)));
}));
var forEachParUnbounded = (self, f, batching) => suspend(() => {
  const as6 = fromIterable(self);
  const array2 = new Array(as6.length);
  const fn = (a, i) => flatMap4(f(a, i), (b) => sync(() => array2[i] = b));
  return zipRight(forEachConcurrentDiscard(as6, fn, batching, false), succeed(array2));
});
var forEachConcurrentDiscard = (self, f, batching, processAll, n) => uninterruptibleMask((restore) => transplant((graft) => withFiberRuntime((parent) => {
  let todos = Array.from(self).reverse();
  let target = todos.length;
  if (target === 0) {
    return void_;
  }
  let counter6 = 0;
  let interrupted3 = false;
  const fibersCount = n ? Math.min(todos.length, n) : todos.length;
  const fibers = /* @__PURE__ */ new Set();
  const results = new Array();
  const interruptAll3 = () => fibers.forEach((fiber) => {
    fiber.currentScheduler.scheduleTask(() => {
      fiber.unsafeInterruptAsFork(parent.id());
    }, 0);
  });
  const startOrder = new Array();
  const joinOrder = new Array();
  const residual = new Array();
  const collectExits = () => {
    const exits = results.filter(({
      exit: exit3
    }) => exit3._tag === "Failure").sort((a, b) => a.index < b.index ? -1 : a.index === b.index ? 0 : 1).map(({
      exit: exit3
    }) => exit3);
    if (exits.length === 0) {
      exits.push(exitVoid);
    }
    return exits;
  };
  const runFiber = (eff, interruptImmediately = false) => {
    const runnable = uninterruptible(graft(eff));
    const fiber = unsafeForkUnstarted(runnable, parent, parent.currentRuntimeFlags, globalScope);
    parent.currentScheduler.scheduleTask(() => {
      if (interruptImmediately) {
        fiber.unsafeInterruptAsFork(parent.id());
      }
      fiber.resume(runnable);
    }, 0);
    return fiber;
  };
  const onInterruptSignal = () => {
    if (!processAll) {
      target -= todos.length;
      todos = [];
    }
    interrupted3 = true;
    interruptAll3();
  };
  const stepOrExit = batching ? step : exit;
  const processingFiber = runFiber(async_((resume2) => {
    const pushResult = (res, index) => {
      if (res._op === "Blocked") {
        residual.push(res);
      } else {
        results.push({
          index,
          exit: res
        });
        if (res._op === "Failure" && !interrupted3) {
          onInterruptSignal();
        }
      }
    };
    const next2 = () => {
      if (todos.length > 0) {
        const a = todos.pop();
        let index = counter6++;
        const returnNextElement = () => {
          const a2 = todos.pop();
          index = counter6++;
          return flatMap4(yieldNow(), () => flatMap4(stepOrExit(restore(f(a2, index))), onRes));
        };
        const onRes = (res) => {
          if (todos.length > 0) {
            pushResult(res, index);
            if (todos.length > 0) {
              return returnNextElement();
            }
          }
          return succeed(res);
        };
        const todo = flatMap4(stepOrExit(restore(f(a, index))), onRes);
        const fiber = runFiber(todo);
        startOrder.push(fiber);
        fibers.add(fiber);
        if (interrupted3) {
          fiber.currentScheduler.scheduleTask(() => {
            fiber.unsafeInterruptAsFork(parent.id());
          }, 0);
        }
        fiber.addObserver((wrapped) => {
          let exit3;
          if (wrapped._op === "Failure") {
            exit3 = wrapped;
          } else {
            exit3 = wrapped.effect_instruction_i0;
          }
          joinOrder.push(fiber);
          fibers.delete(fiber);
          pushResult(exit3, index);
          if (results.length === target) {
            resume2(succeed(getOrElse(exitCollectAll(collectExits(), {
              parallel: true
            }), () => exitVoid)));
          } else if (residual.length + results.length === target) {
            const exits = collectExits();
            const requests = residual.map((blocked2) => blocked2.effect_instruction_i0).reduce(par);
            resume2(succeed(blocked(requests, forEachConcurrentDiscard([getOrElse(exitCollectAll(exits, {
              parallel: true
            }), () => exitVoid), ...residual.map((blocked2) => blocked2.effect_instruction_i1)], (i) => i, batching, true, n))));
          } else {
            next2();
          }
        });
      }
    };
    for (let i = 0; i < fibersCount; i++) {
      next2();
    }
  }));
  return asVoid(onExit(flatten4(restore(join2(processingFiber))), exitMatch({
    onFailure: (cause2) => {
      onInterruptSignal();
      const target2 = residual.length + 1;
      const concurrency = Math.min(typeof n === "number" ? n : residual.length, residual.length);
      const toPop = Array.from(residual);
      return async_((cb) => {
        const exits = [];
        let count = 0;
        let index = 0;
        const check = (index2, hitNext) => (exit3) => {
          exits[index2] = exit3;
          count++;
          if (count === target2) {
            cb(exitSucceed(exitFailCause(cause2)));
          }
          if (toPop.length > 0 && hitNext) {
            next2();
          }
        };
        const next2 = () => {
          runFiber(toPop.pop(), true).addObserver(check(index, true));
          index++;
        };
        processingFiber.addObserver(check(index, false));
        index++;
        for (let i = 0; i < concurrency; i++) {
          next2();
        }
      });
    },
    onSuccess: () => forEachSequential(joinOrder, (f2) => f2.inheritAll)
  })));
})));
var forEachParN = (self, n, f, batching) => suspend(() => {
  const as6 = fromIterable(self);
  const array2 = new Array(as6.length);
  const fn = (a, i) => map4(f(a, i), (b) => array2[i] = b);
  return zipRight(forEachConcurrentDiscard(as6, fn, batching, false, n), succeed(array2));
});
var fork2 = (self) => withFiberRuntime((state, status3) => succeed(unsafeFork2(self, state, status3.runtimeFlags)));
var forkDaemon2 = (self) => forkWithScopeOverride(self, globalScope);
var forkWithErrorHandler = dual(2, (self, handler) => fork2(onError(self, (cause2) => {
  const either3 = failureOrCause(cause2);
  switch (either3._tag) {
    case "Left":
      return handler(either3.left);
    case "Right":
      return failCause(either3.right);
  }
})));
var unsafeFork2 = (effect, parentFiber, parentRuntimeFlags, overrideScope = null) => {
  const childFiber = unsafeMakeChildFiber(effect, parentFiber, parentRuntimeFlags, overrideScope);
  childFiber.resume(effect);
  return childFiber;
};
var unsafeForkUnstarted = (effect, parentFiber, parentRuntimeFlags, overrideScope = null) => {
  const childFiber = unsafeMakeChildFiber(effect, parentFiber, parentRuntimeFlags, overrideScope);
  return childFiber;
};
var unsafeMakeChildFiber = (effect, parentFiber, parentRuntimeFlags, overrideScope = null) => {
  const childId = unsafeMake();
  const parentFiberRefs = parentFiber.getFiberRefs();
  const childFiberRefs = forkAs(parentFiberRefs, childId);
  const childFiber = new FiberRuntime(childId, childFiberRefs, parentRuntimeFlags);
  const childContext = getOrDefault(childFiberRefs, currentContext);
  const supervisor = childFiber.currentSupervisor;
  supervisor.onStart(childContext, effect, some2(parentFiber), childFiber);
  childFiber.addObserver((exit3) => supervisor.onEnd(exit3, childFiber));
  const parentScope = overrideScope !== null ? overrideScope : pipe(parentFiber.getFiberRef(currentForkScopeOverride), getOrElse(() => parentFiber.scope()));
  parentScope.add(parentRuntimeFlags, childFiber);
  return childFiber;
};
var forkWithScopeOverride = (self, scopeOverride) => withFiberRuntime((parentFiber, parentStatus) => succeed(unsafeFork2(self, parentFiber, parentStatus.runtimeFlags, scopeOverride)));
var mergeAll2 = dual((args2) => isFunction(args2[2]), (elements, zero2, f, options) => matchSimple(options?.concurrency, () => fromIterable(elements).reduce((acc, a, i) => zipWith2(acc, a, (acc2, a2) => f(acc2, a2, i)), succeed(zero2)), () => flatMap4(make14(zero2), (acc) => flatMap4(forEach7(elements, (effect, i) => flatMap4(effect, (a) => update3(acc, (b) => f(b, a, i))), options), () => get6(acc)))));
var partition2 = dual((args2) => isIterable(args2[0]), (elements, f, options) => pipe(forEach7(elements, (a, i) => either(f(a, i)), options), map4((chunk3) => partitionMap(chunk3, identity))));
var validateAll = dual((args2) => isIterable(args2[0]), (elements, f, options) => flatMap4(partition2(elements, f, {
  concurrency: options?.concurrency,
  batching: options?.batching,
  concurrentFinalizers: options?.concurrentFinalizers
}), ([es, bs]) => isNonEmptyArray(es) ? fail2(es) : options?.discard ? void_ : succeed(bs)));
var raceAll2 = (all6) => withFiberRuntime((state, status3) => async_((resume2) => {
  const fibers = /* @__PURE__ */ new Set();
  let winner;
  let failures3 = empty9;
  const interruptAll3 = () => {
    for (const fiber of fibers) {
      fiber.unsafeInterruptAsFork(state.id());
    }
  };
  let latch = false;
  let empty28 = true;
  for (const self of all6) {
    empty28 = false;
    const fiber = unsafeFork2(interruptible2(self), state, status3.runtimeFlags);
    fibers.add(fiber);
    fiber.addObserver((exit3) => {
      fibers.delete(fiber);
      if (!winner) {
        if (exit3._tag === "Success") {
          latch = true;
          winner = fiber;
          failures3 = empty9;
          interruptAll3();
        } else {
          failures3 = parallel(exit3.cause, failures3);
        }
      }
      if (latch && fibers.size === 0) {
        resume2(winner ? zipRight(inheritAll(winner), winner.unsafePoll()) : failCause(failures3));
      }
    });
    if (winner) break;
  }
  if (empty28) {
    return resume2(dieSync(() => new IllegalArgumentException(`Received an empty collection of effects`)));
  }
  latch = true;
  return interruptAllAs(fibers, state.id());
}));
var reduceEffect = dual((args2) => isIterable(args2[0]) && !isEffect(args2[0]), (elements, zero2, f, options) => matchSimple(options?.concurrency, () => fromIterable(elements).reduce((acc, a, i) => zipWith2(acc, a, (acc2, a2) => f(acc2, a2, i)), zero2), () => suspend(() => pipe(mergeAll2([zero2, ...elements], none2(), (acc, elem, i) => {
  switch (acc._tag) {
    case "None": {
      return some2(elem);
    }
    case "Some": {
      return some2(f(acc.value, elem, i));
    }
  }
}, options), map4((option3) => {
  switch (option3._tag) {
    case "None": {
      throw new Error("BUG: Effect.reduceEffect - please report an issue at https://github.com/Effect-TS/effect/issues");
    }
    case "Some": {
      return option3.value;
    }
  }
})))));
var parallelFinalizers = (self) => contextWithEffect((context4) => match(getOption(context4, scopeTag), {
  onNone: () => self,
  onSome: (scope4) => {
    switch (scope4.strategy._tag) {
      case "Parallel":
        return self;
      case "Sequential":
      case "ParallelN":
        return flatMap4(scopeFork(scope4, parallel3), (inner) => scopeExtend(self, inner));
    }
  }
}));
var parallelNFinalizers = (parallelism) => (self) => contextWithEffect((context4) => match(getOption(context4, scopeTag), {
  onNone: () => self,
  onSome: (scope4) => {
    if (scope4.strategy._tag === "ParallelN" && scope4.strategy.parallelism === parallelism) {
      return self;
    }
    return flatMap4(scopeFork(scope4, parallelN2(parallelism)), (inner) => scopeExtend(self, inner));
  }
}));
var finalizersMask = (strategy) => (self) => finalizersMaskInternal(strategy, true)(self);
var finalizersMaskInternal = (strategy, concurrentFinalizers) => (self) => contextWithEffect((context4) => match(getOption(context4, scopeTag), {
  onNone: () => self(identity),
  onSome: (scope4) => {
    if (concurrentFinalizers === true) {
      const patch7 = strategy._tag === "Parallel" ? parallelFinalizers : strategy._tag === "Sequential" ? sequentialFinalizers : parallelNFinalizers(strategy.parallelism);
      switch (scope4.strategy._tag) {
        case "Parallel":
          return patch7(self(parallelFinalizers));
        case "Sequential":
          return patch7(self(sequentialFinalizers));
        case "ParallelN":
          return patch7(self(parallelNFinalizers(scope4.strategy.parallelism)));
      }
    } else {
      return self(identity);
    }
  }
}));
var scopeWith = (f) => flatMap4(scopeTag, f);
var scopedWith = (f) => flatMap4(scopeMake2(), (scope4) => onExit(f(scope4), (exit3) => scope4.close(exit3)));
var scopedEffect = (effect) => flatMap4(scopeMake2(), (scope4) => scopeUse(effect, scope4));
var sequentialFinalizers = (self) => contextWithEffect((context4) => match(getOption(context4, scopeTag), {
  onNone: () => self,
  onSome: (scope4) => {
    switch (scope4.strategy._tag) {
      case "Sequential":
        return self;
      case "Parallel":
      case "ParallelN":
        return flatMap4(scopeFork(scope4, sequential3), (inner) => scopeExtend(self, inner));
    }
  }
}));
var tagMetricsScoped = (key, value2) => labelMetricsScoped([make20(key, value2)]);
var labelMetricsScoped = (labels) => fiberRefLocallyScopedWith(currentMetricLabels, (old) => union(old, labels));
var using = dual(2, (self, use2) => scopedWith((scope4) => flatMap4(scopeExtend(self, scope4), use2)));
var validate = dual((args2) => isEffect(args2[1]), (self, that, options) => validateWith(self, that, (a, b) => [a, b], options));
var validateWith = dual((args2) => isEffect(args2[1]), (self, that, f, options) => flatten4(zipWithOptions(exit(self), exit(that), (ea, eb) => exitZipWith(ea, eb, {
  onSuccess: f,
  onFailure: (ca, cb) => options?.concurrent ? parallel(ca, cb) : sequential(ca, cb)
}), options)));
var validateAllPar = dual(2, (elements, f) => flatMap4(partition2(elements, f), ([es, bs]) => es.length === 0 ? succeed(bs) : fail2(es)));
var validateAllParDiscard = dual(2, (elements, f) => flatMap4(partition2(elements, f), ([es, _]) => es.length === 0 ? void_ : fail2(es)));
var validateFirst = dual((args2) => isIterable(args2[0]), (elements, f, options) => flip(forEach7(elements, (a, i) => flip(f(a, i)), options)));
var withClockScoped = (c) => fiberRefLocallyScopedWith(currentServices, add(clockTag, c));
var withRandomScoped = (value2) => fiberRefLocallyScopedWith(currentServices, add(randomTag, value2));
var withConfigProviderScoped = (provider) => fiberRefLocallyScopedWith(currentServices, add(configProviderTag, provider));
var withEarlyRelease = (self) => scopeWith((parent) => flatMap4(scopeFork(parent, sequential2), (child) => pipe(self, scopeExtend(child), map4((value2) => [fiberIdWith((fiberId2) => scopeClose(child, exitInterrupt(fiberId2))), value2]))));
var zipOptions = dual((args2) => isEffect(args2[1]), (self, that, options) => zipWithOptions(self, that, (a, b) => [a, b], options));
var zipLeftOptions = dual((args2) => isEffect(args2[1]), (self, that, options) => {
  if (options?.concurrent !== true && (options?.batching === void 0 || options.batching === false)) {
    return zipLeft(self, that);
  }
  return zipWithOptions(self, that, (a, _) => a, options);
});
var zipRightOptions = dual((args2) => isEffect(args2[1]), (self, that, options) => {
  if (options?.concurrent !== true && (options?.batching === void 0 || options.batching === false)) {
    return zipRight(self, that);
  }
  return zipWithOptions(self, that, (_, b) => b, options);
});
var zipWithOptions = dual((args2) => isEffect(args2[1]), (self, that, f, options) => map4(all4([self, that], {
  concurrency: options?.concurrent ? 2 : 1,
  batching: options?.batching,
  concurrentFinalizers: options?.concurrentFinalizers
}), ([a, a2]) => f(a, a2)));
var withRuntimeFlagsScoped = (update6) => {
  if (update6 === empty7) {
    return void_;
  }
  return pipe(runtimeFlags, flatMap4((runtimeFlags2) => {
    const updatedRuntimeFlags = patch(runtimeFlags2, update6);
    const revertRuntimeFlags = diff(updatedRuntimeFlags, runtimeFlags2);
    return pipe(updateRuntimeFlags(update6), zipRight(addFinalizer2(() => updateRuntimeFlags(revertRuntimeFlags))), asVoid);
  }), uninterruptible);
};
var scopeTag = GenericTag("effect/Scope");
var scope2 = scopeTag;
var scopeUnsafeAddFinalizer = (scope4, fin) => {
  if (scope4.state._tag === "Open") {
    scope4.state.finalizers.set({}, fin);
  }
};
var ScopeImplProto = {
  [ScopeTypeId]: ScopeTypeId,
  [CloseableScopeTypeId]: CloseableScopeTypeId,
  pipe() {
    return pipeArguments(this, arguments);
  },
  fork(strategy) {
    return sync(() => {
      const newScope = scopeUnsafeMake2(strategy);
      if (this.state._tag === "Closed") {
        newScope.state = this.state;
        return newScope;
      }
      const key = {};
      const fin = (exit3) => newScope.close(exit3);
      this.state.finalizers.set(key, fin);
      scopeUnsafeAddFinalizer(newScope, (_) => sync(() => {
        if (this.state._tag === "Open") {
          this.state.finalizers.delete(key);
        }
      }));
      return newScope;
    });
  },
  close(exit3) {
    return suspend(() => {
      if (this.state._tag === "Closed") {
        return void_;
      }
      const finalizers = Array.from(this.state.finalizers.values()).reverse();
      this.state = {
        _tag: "Closed",
        exit: exit3
      };
      if (finalizers.length === 0) {
        return void_;
      }
      return isSequential(this.strategy) ? pipe(forEachSequential(finalizers, (fin) => exit(fin(exit3))), flatMap4((results) => pipe(exitCollectAll(results), map(exitAsVoid), getOrElse(() => exitVoid)))) : isParallel(this.strategy) ? pipe(forEachParUnbounded(finalizers, (fin) => exit(fin(exit3)), false), flatMap4((results) => pipe(exitCollectAll(results, {
        parallel: true
      }), map(exitAsVoid), getOrElse(() => exitVoid)))) : pipe(forEachParN(finalizers, this.strategy.parallelism, (fin) => exit(fin(exit3)), false), flatMap4((results) => pipe(exitCollectAll(results, {
        parallel: true
      }), map(exitAsVoid), getOrElse(() => exitVoid))));
    });
  },
  addFinalizer(fin) {
    return suspend(() => {
      if (this.state._tag === "Closed") {
        return fin(this.state.exit);
      }
      this.state.finalizers.set({}, fin);
      return void_;
    });
  }
};
var scopeUnsafeMake2 = (strategy = sequential2) => {
  const scope4 = Object.create(ScopeImplProto);
  scope4.strategy = strategy;
  scope4.state = {
    _tag: "Open",
    finalizers: /* @__PURE__ */ new Map()
  };
  return scope4;
};
var scopeMake2 = (strategy = sequential2) => sync(() => scopeUnsafeMake2(strategy));
var scopeExtend = dual(2, (effect, scope4) => mapInputContext(
  effect,
  // @ts-expect-error
  merge2(make2(scopeTag, scope4))
));
var scopeUse = dual(2, (effect, scope4) => pipe(effect, scopeExtend(scope4), onExit((exit3) => scope4.close(exit3))));
var fiberRefUnsafeMakeSupervisor = (initial) => fiberRefUnsafeMakePatch(initial, {
  differ,
  fork: empty20
});
var fiberRefLocallyScoped = dual(2, (self, value2) => asVoid(acquireRelease2(flatMap4(fiberRefGet(self), (oldValue) => as2(fiberRefSet(self, value2), oldValue)), (oldValue) => fiberRefSet(self, oldValue))));
var fiberRefLocallyScopedWith = dual(2, (self, f) => fiberRefGetWith(self, (a) => fiberRefLocallyScoped(self, f(a))));
var fiberRefMake = (initial, options) => fiberRefMakeWith(() => fiberRefUnsafeMake(initial, options));
var fiberRefMakeWith = (ref) => acquireRelease2(tap(sync(ref), (ref2) => fiberRefUpdate(ref2, identity)), (fiberRef) => fiberRefDelete(fiberRef));
var fiberRefMakeContext = (initial) => fiberRefMakeWith(() => fiberRefUnsafeMakeContext(initial));
var fiberRefMakeRuntimeFlags = (initial) => fiberRefMakeWith(() => fiberRefUnsafeMakeRuntimeFlags(initial));
var currentRuntimeFlags = fiberRefUnsafeMakeRuntimeFlags(none5);
var currentSupervisor = fiberRefUnsafeMakeSupervisor(none8);
var fiberAwaitAll = (fibers) => forEach7(fibers, _await);
var fiberAll = (fibers) => {
  const _fiberAll = {
    ...CommitPrototype2,
    commit() {
      return join2(this);
    },
    [FiberTypeId]: fiberVariance2,
    id: () => fromIterable(fibers).reduce((id3, fiber) => combine2(id3, fiber.id()), none4),
    await: exit(forEachParUnbounded(fibers, (fiber) => flatten4(fiber.await), false)),
    children: map4(forEachParUnbounded(fibers, (fiber) => fiber.children, false), flatten),
    inheritAll: forEachSequentialDiscard(fibers, (fiber) => fiber.inheritAll),
    poll: map4(forEachSequential(fibers, (fiber) => fiber.poll), reduceRight(some2(exitSucceed(new Array())), (optionB, optionA) => {
      switch (optionA._tag) {
        case "None": {
          return none2();
        }
        case "Some": {
          switch (optionB._tag) {
            case "None": {
              return none2();
            }
            case "Some": {
              return some2(exitZipWith(optionA.value, optionB.value, {
                onSuccess: (a, chunk3) => [a, ...chunk3],
                onFailure: parallel
              }));
            }
          }
        }
      }
    })),
    interruptAsFork: (fiberId2) => forEachSequentialDiscard(fibers, (fiber) => fiber.interruptAsFork(fiberId2))
  };
  return _fiberAll;
};
var fiberInterruptFork = (self) => asVoid(forkDaemon2(interruptFiber(self)));
var fiberJoinAll = (fibers) => join2(fiberAll(fibers));
var fiberScoped = (self) => acquireRelease2(succeed(self), interruptFiber);
var raceWith = dual(3, (self, other, options) => raceFibersWith(self, other, {
  onSelfWin: (winner, loser) => flatMap4(winner.await, (exit3) => {
    switch (exit3._tag) {
      case OP_SUCCESS: {
        return flatMap4(winner.inheritAll, () => options.onSelfDone(exit3, loser));
      }
      case OP_FAILURE: {
        return options.onSelfDone(exit3, loser);
      }
    }
  }),
  onOtherWin: (winner, loser) => flatMap4(winner.await, (exit3) => {
    switch (exit3._tag) {
      case OP_SUCCESS: {
        return flatMap4(winner.inheritAll, () => options.onOtherDone(exit3, loser));
      }
      case OP_FAILURE: {
        return options.onOtherDone(exit3, loser);
      }
    }
  })
}));
var disconnect = (self) => uninterruptibleMask((restore) => fiberIdWith((fiberId2) => flatMap4(forkDaemon2(restore(self)), (fiber) => pipe(restore(join2(fiber)), onInterrupt(() => pipe(fiber, interruptAsFork(fiberId2)))))));
var race2 = dual(2, (self, that) => fiberIdWith((parentFiberId) => raceWith(self, that, {
  onSelfDone: (exit3, right2) => exitMatchEffect(exit3, {
    onFailure: (cause2) => pipe(join2(right2), mapErrorCause2((cause22) => parallel(cause2, cause22))),
    onSuccess: (value2) => pipe(right2, interruptAsFiber(parentFiberId), as2(value2))
  }),
  onOtherDone: (exit3, left2) => exitMatchEffect(exit3, {
    onFailure: (cause2) => pipe(join2(left2), mapErrorCause2((cause22) => parallel(cause22, cause2))),
    onSuccess: (value2) => pipe(left2, interruptAsFiber(parentFiberId), as2(value2))
  })
})));
var raceFibersWith = dual(3, (self, other, options) => withFiberRuntime((parentFiber, parentStatus) => {
  const parentRuntimeFlags = parentStatus.runtimeFlags;
  const raceIndicator = make5(true);
  const leftFiber = unsafeMakeChildFiber(self, parentFiber, parentRuntimeFlags, options.selfScope);
  const rightFiber = unsafeMakeChildFiber(other, parentFiber, parentRuntimeFlags, options.otherScope);
  return async_((cb) => {
    leftFiber.addObserver(() => completeRace(leftFiber, rightFiber, options.onSelfWin, raceIndicator, cb));
    rightFiber.addObserver(() => completeRace(rightFiber, leftFiber, options.onOtherWin, raceIndicator, cb));
    leftFiber.startFork(self);
    rightFiber.startFork(other);
  }, combine2(leftFiber.id(), rightFiber.id()));
}));
var completeRace = (winner, loser, cont, ab, cb) => {
  if (compareAndSet(true, false)(ab)) {
    cb(cont(winner, loser));
  }
};
var ensuring2 = dual(2, (self, finalizer) => uninterruptibleMask((restore) => matchCauseEffect(restore(self), {
  onFailure: (cause1) => matchCauseEffect(finalizer, {
    onFailure: (cause2) => failCause(sequential(cause1, cause2)),
    onSuccess: () => failCause(cause1)
  }),
  onSuccess: (a) => as2(finalizer, a)
})));
var invokeWithInterrupt = (self, entries, onInterrupt3) => fiberIdWith((id3) => flatMap4(flatMap4(forkDaemon2(interruptible2(self)), (processing) => async_((cb) => {
  const counts = entries.map((_) => _.listeners.count);
  const checkDone = () => {
    if (counts.every((count) => count === 0)) {
      if (entries.every((_) => {
        if (_.result.state.current._tag === "Pending") {
          return true;
        } else if (_.result.state.current._tag === "Done" && exitIsExit(_.result.state.current.effect) && _.result.state.current.effect._tag === "Failure" && isInterrupted(_.result.state.current.effect.cause)) {
          return true;
        } else {
          return false;
        }
      })) {
        cleanup.forEach((f) => f());
        onInterrupt3?.();
        cb(interruptFiber(processing));
      }
    }
  };
  processing.addObserver((exit3) => {
    cleanup.forEach((f) => f());
    cb(exit3);
  });
  const cleanup = entries.map((r, i) => {
    const observer = (count) => {
      counts[i] = count;
      checkDone();
    };
    r.listeners.addObserver(observer);
    return () => r.listeners.removeObserver(observer);
  });
  checkDone();
  return sync(() => {
    cleanup.forEach((f) => f());
  });
})), () => suspend(() => {
  const residual = entries.flatMap((entry) => {
    if (!entry.state.completed) {
      return [entry];
    }
    return [];
  });
  return forEachSequentialDiscard(residual, (entry) => complete(entry.request, exitInterrupt(id3)));
})));
var interruptWhenPossible = dual(2, (self, all6) => fiberRefGetWith(currentRequestMap, (map15) => suspend(() => {
  const entries = fromIterable(all6).flatMap((_) => map15.has(_) ? [map15.get(_)] : []);
  return invokeWithInterrupt(self, entries);
})));
var makeSpanScoped = (name, options) => {
  options = addSpanStackTrace(options);
  return uninterruptible(withFiberRuntime((fiber) => {
    const scope4 = unsafeGet2(fiber.getFiberRef(currentContext), scopeTag);
    const span2 = unsafeMakeSpan(fiber, name, options);
    const timingEnabled = fiber.getFiberRef(currentTracerTimingEnabled);
    const clock_ = get(fiber.getFiberRef(currentServices), clockTag);
    return as2(scopeAddFinalizerExit(scope4, (exit3) => endSpan(span2, exit3, clock_, timingEnabled)), span2);
  }));
};
var withTracerScoped = (value2) => fiberRefLocallyScopedWith(currentServices, add(tracerTag, value2));
var withSpanScoped = function() {
  const dataFirst = typeof arguments[0] !== "string";
  const name = dataFirst ? arguments[1] : arguments[0];
  const options = addSpanStackTrace(dataFirst ? arguments[2] : arguments[1]);
  if (dataFirst) {
    const self = arguments[0];
    return flatMap4(makeSpanScoped(name, addSpanStackTrace(options)), (span2) => provideService2(self, spanTag, span2));
  }
  return (self) => flatMap4(makeSpanScoped(name, addSpanStackTrace(options)), (span2) => provideService2(self, spanTag, span2));
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Cause.js
var Cause_exports = {};
__export(Cause_exports, {
  CauseTypeId: () => CauseTypeId2,
  ExceededCapacityException: () => ExceededCapacityException2,
  ExceededCapacityExceptionTypeId: () => ExceededCapacityExceptionTypeId2,
  IllegalArgumentException: () => IllegalArgumentException2,
  IllegalArgumentExceptionTypeId: () => IllegalArgumentExceptionTypeId2,
  InterruptedException: () => InterruptedException2,
  InterruptedExceptionTypeId: () => InterruptedExceptionTypeId2,
  InvalidPubSubCapacityExceptionTypeId: () => InvalidPubSubCapacityExceptionTypeId2,
  NoSuchElementException: () => NoSuchElementException3,
  NoSuchElementExceptionTypeId: () => NoSuchElementExceptionTypeId2,
  RuntimeException: () => RuntimeException2,
  RuntimeExceptionTypeId: () => RuntimeExceptionTypeId2,
  TimeoutException: () => TimeoutException3,
  TimeoutExceptionTypeId: () => TimeoutExceptionTypeId2,
  UnknownException: () => UnknownException2,
  UnknownExceptionTypeId: () => UnknownExceptionTypeId2,
  YieldableError: () => YieldableError3,
  andThen: () => andThen4,
  as: () => as5,
  contains: () => contains3,
  defects: () => defects2,
  die: () => die5,
  dieOption: () => dieOption2,
  empty: () => empty21,
  fail: () => fail7,
  failureOption: () => failureOption2,
  failureOrCause: () => failureOrCause2,
  failures: () => failures2,
  filter: () => filter5,
  find: () => find2,
  flatMap: () => flatMap8,
  flatten: () => flatten7,
  flipCauseOption: () => flipCauseOption2,
  interrupt: () => interrupt5,
  interruptOption: () => interruptOption2,
  interruptors: () => interruptors2,
  isCause: () => isCause2,
  isDie: () => isDie2,
  isDieType: () => isDieType2,
  isEmpty: () => isEmpty4,
  isEmptyType: () => isEmptyType2,
  isExceededCapacityException: () => isExceededCapacityException2,
  isFailType: () => isFailType2,
  isFailure: () => isFailure4,
  isIllegalArgumentException: () => isIllegalArgumentException2,
  isInterruptType: () => isInterruptType2,
  isInterrupted: () => isInterrupted3,
  isInterruptedException: () => isInterruptedException2,
  isInterruptedOnly: () => isInterruptedOnly2,
  isNoSuchElementException: () => isNoSuchElementException2,
  isParallelType: () => isParallelType2,
  isRuntimeException: () => isRuntimeException2,
  isSequentialType: () => isSequentialType2,
  isTimeoutException: () => isTimeoutException2,
  isUnknownException: () => isUnknownException2,
  keepDefects: () => keepDefects2,
  linearize: () => linearize2,
  map: () => map12,
  match: () => match12,
  originalError: () => originalError,
  parallel: () => parallel4,
  pretty: () => pretty3,
  prettyErrors: () => prettyErrors2,
  reduce: () => reduce8,
  reduceWithContext: () => reduceWithContext3,
  sequential: () => sequential4,
  size: () => size8,
  squash: () => squash,
  squashWith: () => squashWith,
  stripFailures: () => stripFailures2,
  stripSomeDefects: () => stripSomeDefects2
});
var CauseTypeId2 = CauseTypeId;
var RuntimeExceptionTypeId2 = RuntimeExceptionTypeId;
var InterruptedExceptionTypeId2 = InterruptedExceptionTypeId;
var IllegalArgumentExceptionTypeId2 = IllegalArgumentExceptionTypeId;
var NoSuchElementExceptionTypeId2 = NoSuchElementExceptionTypeId;
var InvalidPubSubCapacityExceptionTypeId2 = InvalidPubSubCapacityExceptionTypeId;
var ExceededCapacityExceptionTypeId2 = ExceededCapacityExceptionTypeId;
var TimeoutExceptionTypeId2 = TimeoutExceptionTypeId;
var UnknownExceptionTypeId2 = UnknownExceptionTypeId;
var YieldableError3 = YieldableError;
var empty21 = empty9;
var fail7 = fail;
var die5 = die;
var interrupt5 = interrupt;
var parallel4 = parallel;
var sequential4 = sequential;
var isCause2 = isCause;
var isEmptyType2 = isEmptyType;
var isFailType2 = isFailType;
var isDieType2 = isDieType;
var isInterruptType2 = isInterruptType;
var isSequentialType2 = isSequentialType;
var isParallelType2 = isParallelType;
var size8 = size3;
var isEmpty4 = isEmpty2;
var isFailure4 = isFailure;
var isDie2 = isDie;
var isInterrupted3 = isInterrupted;
var isInterruptedOnly2 = isInterruptedOnly;
var failures2 = failures;
var defects2 = defects;
var interruptors2 = interruptors;
var failureOption2 = failureOption;
var failureOrCause2 = failureOrCause;
var flipCauseOption2 = flipCauseOption;
var dieOption2 = dieOption;
var interruptOption2 = interruptOption;
var keepDefects2 = keepDefects;
var linearize2 = linearize;
var stripFailures2 = stripFailures;
var stripSomeDefects2 = stripSomeDefects;
var as5 = as;
var map12 = map3;
var flatMap8 = flatMap3;
var andThen4 = andThen;
var flatten7 = flatten3;
var contains3 = contains2;
var squash = causeSquash;
var squashWith = causeSquashWith;
var find2 = find;
var filter5 = filter;
var match12 = match3;
var reduce8 = reduce4;
var reduceWithContext3 = reduceWithContext;
var InterruptedException2 = InterruptedException;
var isInterruptedException2 = isInterruptedException;
var IllegalArgumentException2 = IllegalArgumentException;
var isIllegalArgumentException2 = isIllegalArgumentException;
var NoSuchElementException3 = NoSuchElementException;
var isNoSuchElementException2 = isNoSuchElementException;
var RuntimeException2 = RuntimeException;
var isRuntimeException2 = isRuntimeException;
var TimeoutException3 = TimeoutException;
var isTimeoutException2 = isTimeoutException;
var UnknownException2 = UnknownException;
var isUnknownException2 = isUnknownException;
var ExceededCapacityException2 = ExceededCapacityException;
var isExceededCapacityException2 = isExceededCapacityException;
var pretty3 = pretty;
var prettyErrors2 = prettyErrors;
var originalError = originalInstance;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ScheduleInterval.js
var ScheduleInterval_exports = {};
__export(ScheduleInterval_exports, {
  IntervalTypeId: () => IntervalTypeId2,
  after: () => after2,
  before: () => before2,
  empty: () => empty23,
  intersect: () => intersect2,
  isEmpty: () => isEmpty6,
  isNonEmpty: () => isNonEmpty3,
  lessThan: () => lessThan7,
  make: () => make31,
  max: () => max4,
  min: () => min4,
  size: () => size10,
  union: () => union5
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/schedule/interval.js
var IntervalSymbolKey = "effect/ScheduleInterval";
var IntervalTypeId = Symbol.for(IntervalSymbolKey);
var empty22 = {
  [IntervalTypeId]: IntervalTypeId,
  startMillis: 0,
  endMillis: 0
};
var make30 = (startMillis, endMillis) => {
  if (startMillis > endMillis) {
    return empty22;
  }
  return {
    [IntervalTypeId]: IntervalTypeId,
    startMillis,
    endMillis
  };
};
var lessThan6 = dual(2, (self, that) => min3(self, that) === self);
var min3 = dual(2, (self, that) => {
  if (self.endMillis <= that.startMillis) return self;
  if (that.endMillis <= self.startMillis) return that;
  if (self.startMillis < that.startMillis) return self;
  if (that.startMillis < self.startMillis) return that;
  if (self.endMillis <= that.endMillis) return self;
  return that;
});
var max3 = dual(2, (self, that) => min3(self, that) === self ? that : self);
var isEmpty5 = (self) => {
  return self.startMillis >= self.endMillis;
};
var isNonEmpty2 = (self) => {
  return !isEmpty5(self);
};
var intersect = dual(2, (self, that) => {
  const start3 = Math.max(self.startMillis, that.startMillis);
  const end3 = Math.min(self.endMillis, that.endMillis);
  return make30(start3, end3);
});
var size9 = (self) => {
  return millis(self.endMillis - self.startMillis);
};
var union4 = dual(2, (self, that) => {
  const start3 = Math.max(self.startMillis, that.startMillis);
  const end3 = Math.min(self.endMillis, that.endMillis);
  return start3 < end3 ? none2() : some2(make30(start3, end3));
});
var after = (startMilliseconds) => {
  return make30(startMilliseconds, Number.POSITIVE_INFINITY);
};
var before = (endMilliseconds) => {
  return make30(Number.NEGATIVE_INFINITY, endMilliseconds);
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ScheduleInterval.js
var IntervalTypeId2 = IntervalTypeId;
var make31 = make30;
var empty23 = empty22;
var lessThan7 = lessThan6;
var min4 = min3;
var max4 = max3;
var isEmpty6 = isEmpty5;
var isNonEmpty3 = isNonEmpty2;
var intersect2 = intersect;
var size10 = size9;
var union5 = union4;
var after2 = after;
var before2 = before;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ScheduleIntervals.js
var ScheduleIntervals_exports = {};
__export(ScheduleIntervals_exports, {
  IntervalsTypeId: () => IntervalsTypeId2,
  empty: () => empty25,
  end: () => end2,
  fromIterable: () => fromIterable11,
  intersect: () => intersect4,
  isNonEmpty: () => isNonEmpty5,
  lessThan: () => lessThan9,
  make: () => make33,
  max: () => max6,
  start: () => start2,
  union: () => union7
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/schedule/intervals.js
var IntervalsSymbolKey = "effect/ScheduleIntervals";
var IntervalsTypeId = Symbol.for(IntervalsSymbolKey);
var make32 = (intervals) => {
  return {
    [IntervalsTypeId]: IntervalsTypeId,
    intervals
  };
};
var empty24 = make32(empty2());
var fromIterable10 = (intervals) => Array.from(intervals).reduce((intervals2, interval) => pipe(intervals2, union6(make32(of2(interval)))), empty24);
var union6 = dual(2, (self, that) => {
  if (!isNonEmpty(that.intervals)) {
    return self;
  }
  if (!isNonEmpty(self.intervals)) {
    return that;
  }
  if (headNonEmpty2(self.intervals).startMillis < headNonEmpty2(that.intervals).startMillis) {
    return unionLoop(tailNonEmpty2(self.intervals), that.intervals, headNonEmpty2(self.intervals), empty2());
  }
  return unionLoop(self.intervals, tailNonEmpty2(that.intervals), headNonEmpty2(that.intervals), empty2());
});
var unionLoop = (_self, _that, _interval, _acc) => {
  let self = _self;
  let that = _that;
  let interval = _interval;
  let acc = _acc;
  while (isNonEmpty(self) || isNonEmpty(that)) {
    if (!isNonEmpty(self) && isNonEmpty(that)) {
      if (interval.endMillis < headNonEmpty2(that).startMillis) {
        acc = pipe(acc, prepend2(interval));
        interval = headNonEmpty2(that);
        that = tailNonEmpty2(that);
        self = empty2();
      } else {
        interval = make31(interval.startMillis, headNonEmpty2(that).endMillis);
        that = tailNonEmpty2(that);
        self = empty2();
      }
    } else if (isNonEmpty(self) && isEmpty(that)) {
      if (interval.endMillis < headNonEmpty2(self).startMillis) {
        acc = pipe(acc, prepend2(interval));
        interval = headNonEmpty2(self);
        that = empty2();
        self = tailNonEmpty2(self);
      } else {
        interval = make31(interval.startMillis, headNonEmpty2(self).endMillis);
        that = empty2();
        self = tailNonEmpty2(self);
      }
    } else if (isNonEmpty(self) && isNonEmpty(that)) {
      if (headNonEmpty2(self).startMillis < headNonEmpty2(that).startMillis) {
        if (interval.endMillis < headNonEmpty2(self).startMillis) {
          acc = pipe(acc, prepend2(interval));
          interval = headNonEmpty2(self);
          self = tailNonEmpty2(self);
        } else {
          interval = make31(interval.startMillis, headNonEmpty2(self).endMillis);
          self = tailNonEmpty2(self);
        }
      } else if (interval.endMillis < headNonEmpty2(that).startMillis) {
        acc = pipe(acc, prepend2(interval));
        interval = headNonEmpty2(that);
        that = tailNonEmpty2(that);
      } else {
        interval = make31(interval.startMillis, headNonEmpty2(that).endMillis);
        that = tailNonEmpty2(that);
      }
    } else {
      throw new Error(getBugErrorMessage("Intervals.unionLoop"));
    }
  }
  return make32(pipe(acc, prepend2(interval), reverse));
};
var intersect3 = dual(2, (self, that) => intersectLoop(self.intervals, that.intervals, empty2()));
var intersectLoop = (_left, _right, _acc) => {
  let left2 = _left;
  let right2 = _right;
  let acc = _acc;
  while (isNonEmpty(left2) && isNonEmpty(right2)) {
    const interval = pipe(headNonEmpty2(left2), intersect2(headNonEmpty2(right2)));
    const intervals = isEmpty6(interval) ? acc : pipe(acc, prepend2(interval));
    if (pipe(headNonEmpty2(left2), lessThan7(headNonEmpty2(right2)))) {
      left2 = tailNonEmpty2(left2);
    } else {
      right2 = tailNonEmpty2(right2);
    }
    acc = intervals;
  }
  return make32(reverse(acc));
};
var start = (self) => {
  return pipe(self.intervals, head2, getOrElse(() => empty23)).startMillis;
};
var end = (self) => {
  return pipe(self.intervals, head2, getOrElse(() => empty23)).endMillis;
};
var lessThan8 = dual(2, (self, that) => start(self) < start(that));
var isNonEmpty4 = (self) => {
  return isNonEmpty(self.intervals);
};
var max5 = dual(2, (self, that) => lessThan8(self, that) ? that : self);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ScheduleIntervals.js
var IntervalsTypeId2 = IntervalsTypeId;
var make33 = make32;
var empty25 = empty24;
var fromIterable11 = fromIterable10;
var union7 = union6;
var intersect4 = intersect3;
var start2 = start;
var end2 = end;
var lessThan9 = lessThan8;
var isNonEmpty5 = isNonEmpty4;
var max6 = max5;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ScheduleDecision.js
var ScheduleDecision_exports = {};
__export(ScheduleDecision_exports, {
  continue: () => _continue2,
  continueWith: () => continueWith2,
  done: () => done5,
  isContinue: () => isContinue2,
  isDone: () => isDone4
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/schedule/decision.js
var OP_CONTINUE = "Continue";
var OP_DONE2 = "Done";
var _continue = (intervals) => {
  return {
    _tag: OP_CONTINUE,
    intervals
  };
};
var continueWith = (interval) => {
  return {
    _tag: OP_CONTINUE,
    intervals: make33(of2(interval))
  };
};
var done4 = {
  _tag: OP_DONE2
};
var isContinue = (self) => {
  return self._tag === OP_CONTINUE;
};
var isDone3 = (self) => {
  return self._tag === OP_DONE2;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/ScheduleDecision.js
var _continue2 = _continue;
var continueWith2 = continueWith;
var done5 = done4;
var isContinue2 = isContinue;
var isDone4 = isDone3;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Scope.js
var Scope_exports = {};
__export(Scope_exports, {
  CloseableScopeTypeId: () => CloseableScopeTypeId2,
  Scope: () => Scope,
  ScopeTypeId: () => ScopeTypeId2,
  addFinalizer: () => addFinalizer3,
  addFinalizerExit: () => addFinalizerExit,
  close: () => close,
  extend: () => extend2,
  fork: () => fork3,
  make: () => make34,
  use: () => use
});
var ScopeTypeId2 = ScopeTypeId;
var CloseableScopeTypeId2 = CloseableScopeTypeId;
var Scope = scopeTag;
var addFinalizer3 = scopeAddFinalizer;
var addFinalizerExit = scopeAddFinalizerExit;
var close = scopeClose;
var extend2 = scopeExtend;
var fork3 = scopeFork;
var use = scopeUse;
var make34 = scopeMake2;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/effect/circular.js
var Semaphore = class {
  permits;
  waiters = /* @__PURE__ */ new Set();
  taken = 0;
  constructor(permits) {
    this.permits = permits;
  }
  get free() {
    return this.permits - this.taken;
  }
  take = (n) => asyncInterrupt((resume2) => {
    if (this.free < n) {
      const observer = () => {
        if (this.free < n) return;
        this.waiters.delete(observer);
        resume2(suspend(() => {
          if (this.free < n) return this.take(n);
          this.taken += n;
          return succeed(n);
        }));
      };
      this.waiters.add(observer);
      return sync(() => {
        this.waiters.delete(observer);
      });
    }
    resume2(suspend(() => {
      if (this.free < n) return this.take(n);
      this.taken += n;
      return succeed(n);
    }));
  });
  updateTakenUnsafe(fiber, f) {
    this.taken = f(this.taken);
    if (this.waiters.size > 0) {
      fiber.getFiberRef(currentScheduler).scheduleTask(() => {
        const iter = this.waiters.values();
        let item = iter.next();
        while (item.done === false && this.free > 0) {
          item.value();
          item = iter.next();
        }
      }, fiber.getFiberRef(currentSchedulingPriority));
    }
    return succeed(this.free);
  }
  updateTaken(f) {
    return withFiberRuntime((fiber) => this.updateTakenUnsafe(fiber, f));
  }
  resize = (permits) => asVoid(withFiberRuntime((fiber) => {
    this.permits = permits;
    if (this.free < 0) {
      return void_;
    }
    return this.updateTakenUnsafe(fiber, (taken) => taken);
  }));
  release = (n) => this.updateTaken((taken) => taken - n);
  releaseAll = this.updateTaken((_) => 0);
  withPermits = (n) => (self) => uninterruptibleMask((restore) => flatMap4(restore(this.take(n)), (permits) => ensuring2(restore(self), this.release(permits))));
  withPermitsIfAvailable = (n) => (self) => uninterruptibleMask((restore) => suspend(() => {
    if (this.free < n) {
      return succeedNone2;
    }
    this.taken += n;
    return ensuring2(restore(asSome2(self)), this.release(n));
  }));
};
var unsafeMakeSemaphore = (permits) => new Semaphore(permits);
var makeSemaphore = (permits) => sync(() => unsafeMakeSemaphore(permits));
var Latch = class extends Class {
  isOpen;
  waiters = [];
  scheduled = false;
  constructor(isOpen) {
    super();
    this.isOpen = isOpen;
  }
  commit() {
    return this.await;
  }
  unsafeSchedule(fiber) {
    if (this.scheduled || this.waiters.length === 0) {
      return void_;
    }
    this.scheduled = true;
    fiber.currentScheduler.scheduleTask(this.flushWaiters, fiber.getFiberRef(currentSchedulingPriority));
    return void_;
  }
  flushWaiters = () => {
    this.scheduled = false;
    const waiters = this.waiters;
    this.waiters = [];
    for (let i = 0; i < waiters.length; i++) {
      waiters[i](exitVoid);
    }
  };
  open = withFiberRuntime((fiber) => {
    if (this.isOpen) {
      return void_;
    }
    this.isOpen = true;
    return this.unsafeSchedule(fiber);
  });
  unsafeOpen() {
    if (this.isOpen) return;
    this.isOpen = true;
    this.flushWaiters();
  }
  release = withFiberRuntime((fiber) => {
    if (this.isOpen) {
      return void_;
    }
    return this.unsafeSchedule(fiber);
  });
  await = asyncInterrupt((resume2) => {
    if (this.isOpen) {
      return resume2(void_);
    }
    this.waiters.push(resume2);
    return sync(() => {
      const index = this.waiters.indexOf(resume2);
      if (index !== -1) {
        this.waiters.splice(index, 1);
      }
    });
  });
  unsafeClose() {
    this.isOpen = false;
  }
  close = sync(() => {
    this.isOpen = false;
  });
  whenOpen = (self) => {
    return zipRight(this.await, self);
  };
};
var unsafeMakeLatch = (open) => new Latch(open ?? false);
var makeLatch = (open) => sync(() => unsafeMakeLatch(open));
var awaitAllChildren = (self) => ensuringChildren(self, fiberAwaitAll);
var cached2 = dual(2, (self, timeToLive) => map4(cachedInvalidateWithTTL(self, timeToLive), (tuple2) => tuple2[0]));
var cachedInvalidateWithTTL = dual(2, (self, timeToLive) => {
  const duration = decode(timeToLive);
  return flatMap4(context(), (env) => map4(makeSynchronized(none2()), (cache) => [provideContext(getCachedValue(self, duration, cache), env), invalidateCache(cache)]));
});
var computeCachedValue = (self, timeToLive, start3) => {
  const timeToLiveMillis = toMillis(decode(timeToLive));
  return pipe(deferredMake(), tap((deferred) => intoDeferred(self, deferred)), map4((deferred) => some2([start3 + timeToLiveMillis, deferred])));
};
var getCachedValue = (self, timeToLive, cache) => uninterruptibleMask((restore) => pipe(clockWith3((clock2) => clock2.currentTimeMillis), flatMap4((time) => updateSomeAndGetEffectSynchronized(cache, (option3) => {
  switch (option3._tag) {
    case "None": {
      return some2(computeCachedValue(self, timeToLive, time));
    }
    case "Some": {
      const [end3] = option3.value;
      return end3 - time <= 0 ? some2(computeCachedValue(self, timeToLive, time)) : none2();
    }
  }
})), flatMap4((option3) => isNone(option3) ? dieMessage("BUG: Effect.cachedInvalidate - please report an issue at https://github.com/Effect-TS/effect/issues") : restore(deferredAwait(option3.value[1])))));
var invalidateCache = (cache) => set3(cache, none2());
var ensuringChild = dual(2, (self, f) => ensuringChildren(self, (children3) => f(fiberAll(children3))));
var ensuringChildren = dual(2, (self, children3) => flatMap4(track, (supervisor) => pipe(supervised(self, supervisor), ensuring2(flatMap4(supervisor.value, children3)))));
var forkAll = dual((args2) => isIterable(args2[0]), (effects, options) => options?.discard ? forEachSequentialDiscard(effects, fork2) : map4(forEachSequential(effects, fork2), fiberAll));
var forkIn2 = dual(2, (self, scope4) => withFiberRuntime((parent, parentStatus) => {
  const scopeImpl = scope4;
  const fiber = unsafeFork2(self, parent, parentStatus.runtimeFlags, globalScope);
  if (scopeImpl.state._tag === "Open") {
    const finalizer = () => fiberIdWith((fiberId2) => equals(fiberId2, fiber.id()) ? void_ : asVoid(interruptFiber(fiber)));
    const key = {};
    scopeImpl.state.finalizers.set(key, finalizer);
    fiber.addObserver(() => {
      if (scopeImpl.state._tag === "Closed") return;
      scopeImpl.state.finalizers.delete(key);
    });
  } else {
    fiber.unsafeInterruptAsFork(parent.id());
  }
  return succeed(fiber);
}));
var forkScoped2 = (self) => scopeWith((scope4) => forkIn2(self, scope4));
var fromFiber = (fiber) => join2(fiber);
var fromFiberEffect = (fiber) => suspend(() => flatMap4(fiber, join2));
var memoKeySymbol = Symbol.for("effect/Effect/memoizeFunction.key");
var Key = class {
  a;
  eq;
  [memoKeySymbol] = memoKeySymbol;
  constructor(a, eq) {
    this.a = a;
    this.eq = eq;
  }
  [symbol2](that) {
    if (hasProperty(that, memoKeySymbol)) {
      if (this.eq) {
        return this.eq(this.a, that.a);
      } else {
        return equals(this.a, that.a);
      }
    }
    return false;
  }
  [symbol]() {
    return this.eq ? 0 : cached(this, hash(this.a));
  }
};
var cachedFunction = (f, eq) => {
  return pipe(sync(() => empty15()), flatMap4(makeSynchronized), map4((ref) => (a) => pipe(ref.modifyEffect((map15) => {
    const result = pipe(map15, get7(new Key(a, eq)));
    if (isNone(result)) {
      return pipe(deferredMake(), tap((deferred) => pipe(diffFiberRefs(f(a)), intoDeferred(deferred), fork2)), map4((deferred) => [deferred, pipe(map15, set5(new Key(a, eq), deferred))]));
    }
    return succeed([result.value, map15]);
  }), flatMap4(deferredAwait), flatMap4(([patch7, b]) => pipe(patchFiberRefs(patch7), as2(b))))));
};
var raceFirst2 = dual(2, (self, that) => pipe(exit(self), race2(exit(that)), (effect) => flatten4(effect)));
var supervised = dual(2, (self, supervisor) => {
  const supervise = fiberRefLocallyWith(currentSupervisor, (s) => s.zip(supervisor));
  return supervise(self);
});
var timeout2 = dual(2, (self, duration) => timeoutFail(self, {
  onTimeout: () => timeoutExceptionFromDuration(duration),
  duration
}));
var timeoutFail = dual(2, (self, {
  duration,
  onTimeout
}) => flatten4(timeoutTo(self, {
  onTimeout: () => failSync(onTimeout),
  onSuccess: succeed,
  duration
})));
var timeoutFailCause = dual(2, (self, {
  duration,
  onTimeout
}) => flatten4(timeoutTo(self, {
  onTimeout: () => failCauseSync(onTimeout),
  onSuccess: succeed,
  duration
})));
var timeoutOption2 = dual(2, (self, duration) => timeoutTo(self, {
  duration,
  onSuccess: some2,
  onTimeout: none2
}));
var timeoutTo = dual(2, (self, {
  duration,
  onSuccess,
  onTimeout
}) => fiberIdWith((parentFiberId) => uninterruptibleMask((restore) => raceFibersWith(restore(self), interruptible2(sleep4(duration)), {
  onSelfWin: (winner, loser) => flatMap4(winner.await, (exit3) => {
    if (exit3._tag === "Success") {
      return flatMap4(winner.inheritAll, () => as2(interruptAsFiber(loser, parentFiberId), onSuccess(exit3.value)));
    } else {
      return flatMap4(interruptAsFiber(loser, parentFiberId), () => exitFailCause(exit3.cause));
    }
  }),
  onOtherWin: (winner, loser) => flatMap4(winner.await, (exit3) => {
    if (exit3._tag === "Success") {
      return flatMap4(winner.inheritAll, () => as2(interruptAsFiber(loser, parentFiberId), onTimeout()));
    } else {
      return flatMap4(interruptAsFiber(loser, parentFiberId), () => exitFailCause(exit3.cause));
    }
  }),
  otherScope: globalScope
}))));
var SynchronizedSymbolKey = "effect/Ref/SynchronizedRef";
var SynchronizedTypeId = Symbol.for(SynchronizedSymbolKey);
var synchronizedVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var SynchronizedImpl = class extends Class {
  ref;
  withLock;
  [SynchronizedTypeId] = synchronizedVariance;
  [RefTypeId] = refVariance;
  [TypeId3] = TypeId3;
  constructor(ref, withLock) {
    super();
    this.ref = ref;
    this.withLock = withLock;
    this.get = get5(this.ref);
  }
  get;
  commit() {
    return this.get;
  }
  modify(f) {
    return this.modifyEffect((a) => succeed(f(a)));
  }
  modifyEffect(f) {
    return this.withLock(pipe(flatMap4(get5(this.ref), f), flatMap4(([b, a]) => as2(set3(this.ref, a), b))));
  }
};
var makeSynchronized = (value2) => sync(() => unsafeMakeSynchronized(value2));
var unsafeMakeSynchronized = (value2) => {
  const ref = unsafeMake4(value2);
  const sem = unsafeMakeSemaphore(1);
  return new SynchronizedImpl(ref, sem.withPermits(1));
};
var updateSomeAndGetEffectSynchronized = dual(2, (self, pf) => self.modifyEffect((value2) => {
  const result = pf(value2);
  switch (result._tag) {
    case "None": {
      return succeed([value2, value2]);
    }
    case "Some": {
      return map4(result.value, (a) => [a, a]);
    }
  }
}));
var zipFiber = dual(2, (self, that) => zipWithFiber(self, that, (a, b) => [a, b]));
var zipLeftFiber = dual(2, (self, that) => zipWithFiber(self, that, (a, _) => a));
var zipRightFiber = dual(2, (self, that) => zipWithFiber(self, that, (_, b) => b));
var zipWithFiber = dual(3, (self, that, f) => ({
  ...CommitPrototype2,
  commit() {
    return join2(this);
  },
  [FiberTypeId]: fiberVariance2,
  id: () => pipe(self.id(), getOrElse2(that.id())),
  await: pipe(self.await, flatten4, zipWithOptions(flatten4(that.await), f, {
    concurrent: true
  }), exit),
  children: self.children,
  inheritAll: zipRight(that.inheritAll, self.inheritAll),
  poll: zipWith2(self.poll, that.poll, (optionA, optionB) => pipe(optionA, flatMap((exitA) => pipe(optionB, map((exitB) => zipWith4(exitA, exitB, {
    onSuccess: f,
    onFailure: parallel
  })))))),
  interruptAsFork: (id3) => zipRight(self.interruptAsFork(id3), that.interruptAsFork(id3)),
  pipe() {
    return pipeArguments(this, arguments);
  }
}));
var bindAll = dual((args2) => isEffect(args2[0]), (self, f, options) => flatMap4(self, (a) => all4(f(a), options).pipe(map4((record) => Object.assign({}, a, record)))));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Fiber.js
var Fiber_exports = {};
__export(Fiber_exports, {
  FiberTypeId: () => FiberTypeId2,
  Order: () => Order5,
  RuntimeFiberTypeId: () => RuntimeFiberTypeId2,
  all: () => all5,
  await: () => _await2,
  awaitAll: () => awaitAll,
  children: () => children2,
  done: () => done6,
  dump: () => dump2,
  dumpAll: () => dumpAll2,
  fail: () => fail8,
  failCause: () => failCause6,
  fromEffect: () => fromEffect3,
  getCurrentFiber: () => getCurrentFiber2,
  id: () => id2,
  inheritAll: () => inheritAll2,
  interrupt: () => interrupt6,
  interruptAll: () => interruptAll2,
  interruptAllAs: () => interruptAllAs2,
  interruptAs: () => interruptAs,
  interruptAsFork: () => interruptAsFork2,
  interruptFork: () => interruptFork,
  interrupted: () => interrupted2,
  isFiber: () => isFiber2,
  isRuntimeFiber: () => isRuntimeFiber2,
  join: () => join3,
  joinAll: () => joinAll,
  map: () => map13,
  mapEffect: () => mapEffect3,
  mapFiber: () => mapFiber2,
  match: () => match13,
  never: () => never4,
  orElse: () => orElse4,
  orElseEither: () => orElseEither4,
  poll: () => poll2,
  pretty: () => pretty4,
  roots: () => roots2,
  scoped: () => scoped2,
  status: () => status2,
  succeed: () => succeed8,
  unsafeRoots: () => unsafeRoots2,
  void: () => void_5,
  zip: () => zip9,
  zipLeft: () => zipLeft4,
  zipRight: () => zipRight4,
  zipWith: () => zipWith5
});
var FiberTypeId2 = FiberTypeId;
var RuntimeFiberTypeId2 = RuntimeFiberTypeId;
var Order5 = Order4;
var isFiber2 = isFiber;
var isRuntimeFiber2 = isRuntimeFiber;
var id2 = id;
var _await2 = _await;
var awaitAll = fiberAwaitAll;
var children2 = children;
var all5 = fiberAll;
var done6 = done3;
var dump2 = dump;
var dumpAll2 = dumpAll;
var fail8 = fail5;
var failCause6 = failCause4;
var fromEffect3 = fromEffect;
var getCurrentFiber2 = getCurrentFiber;
var inheritAll2 = inheritAll;
var interrupt6 = interruptFiber;
var interrupted2 = interrupted;
var interruptAs = interruptAsFiber;
var interruptAsFork2 = interruptAsFork;
var interruptAll2 = interruptAll;
var interruptAllAs2 = interruptAllAs;
var interruptFork = fiberInterruptFork;
var join3 = join2;
var joinAll = fiberJoinAll;
var map13 = map9;
var mapEffect3 = mapEffect2;
var mapFiber2 = mapFiber;
var match13 = match11;
var never4 = never3;
var orElse4 = orElse3;
var orElseEither4 = orElseEither3;
var poll2 = poll;
var pretty4 = pretty2;
var roots2 = roots;
var unsafeRoots2 = unsafeRoots;
var scoped2 = fiberScoped;
var status2 = status;
var succeed8 = succeed4;
var void_5 = void_4;
var zip9 = zipFiber;
var zipLeft4 = zipLeftFiber;
var zipRight4 = zipRightFiber;
var zipWith5 = zipWithFiber;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/runtime.js
var makeDual = (f) => function() {
  if (arguments.length === 1) {
    const runtime2 = arguments[0];
    return (effect, ...args2) => f(runtime2, effect, ...args2);
  }
  return f.apply(this, arguments);
};
var unsafeFork3 = makeDual((runtime2, self, options) => {
  const fiberId2 = unsafeMake();
  const fiberRefUpdates = [[currentContext, [[fiberId2, runtime2.context]]]];
  if (options?.scheduler) {
    fiberRefUpdates.push([currentScheduler, [[fiberId2, options.scheduler]]]);
  }
  let fiberRefs4 = updateManyAs2(runtime2.fiberRefs, {
    entries: fiberRefUpdates,
    forkAs: fiberId2
  });
  if (options?.updateRefs) {
    fiberRefs4 = options.updateRefs(fiberRefs4, fiberId2);
  }
  const fiberRuntime = new FiberRuntime(fiberId2, fiberRefs4, runtime2.runtimeFlags);
  let effect = self;
  if (options?.scope) {
    effect = flatMap4(fork3(options.scope, sequential2), (closeableScope) => zipRight(scopeAddFinalizer(closeableScope, fiberIdWith((id3) => equals(id3, fiberRuntime.id()) ? void_ : interruptAsFiber(fiberRuntime, id3))), onExit(self, (exit3) => close(closeableScope, exit3))));
  }
  const supervisor = fiberRuntime.currentSupervisor;
  if (supervisor !== none8) {
    supervisor.onStart(runtime2.context, effect, none2(), fiberRuntime);
    fiberRuntime.addObserver((exit3) => supervisor.onEnd(exit3, fiberRuntime));
  }
  globalScope.add(runtime2.runtimeFlags, fiberRuntime);
  if (options?.immediate === false) {
    fiberRuntime.resume(effect);
  } else {
    fiberRuntime.start(effect);
  }
  return fiberRuntime;
});
var unsafeRunCallback = makeDual((runtime2, effect, options = {}) => {
  const fiberRuntime = unsafeFork3(runtime2, effect, options);
  if (options.onExit) {
    fiberRuntime.addObserver((exit3) => {
      options.onExit(exit3);
    });
  }
  return (id3, cancelOptions) => unsafeRunCallback(runtime2)(pipe(fiberRuntime, interruptAs(id3 ?? none4)), {
    ...cancelOptions,
    onExit: cancelOptions?.onExit ? (exit3) => cancelOptions.onExit(flatten6(exit3)) : void 0
  });
});
var unsafeRunSync = makeDual((runtime2, effect) => {
  const result = unsafeRunSyncExit(runtime2)(effect);
  if (result._tag === "Failure") {
    throw fiberFailure(result.effect_instruction_i0);
  }
  return result.effect_instruction_i0;
});
var AsyncFiberExceptionImpl = class extends Error {
  fiber;
  _tag = "AsyncFiberException";
  constructor(fiber) {
    super(`Fiber #${fiber.id().id} cannot be resolved synchronously. This is caused by using runSync on an effect that performs async work`);
    this.fiber = fiber;
    this.name = this._tag;
    this.stack = this.message;
  }
};
var asyncFiberException = (fiber) => {
  const limit = Error.stackTraceLimit;
  Error.stackTraceLimit = 0;
  const error = new AsyncFiberExceptionImpl(fiber);
  Error.stackTraceLimit = limit;
  return error;
};
var isAsyncFiberException = (u) => isTagged(u, "AsyncFiberException") && "fiber" in u;
var FiberFailureId = Symbol.for("effect/Runtime/FiberFailure");
var FiberFailureCauseId = Symbol.for("effect/Runtime/FiberFailure/Cause");
var FiberFailureImpl = class extends Error {
  [FiberFailureId];
  [FiberFailureCauseId];
  constructor(cause2) {
    const head5 = prettyErrors(cause2)[0];
    super(head5?.message || "An error has occurred");
    this[FiberFailureId] = FiberFailureId;
    this[FiberFailureCauseId] = cause2;
    this.name = head5 ? `(FiberFailure) ${head5.name}` : "FiberFailure";
    if (head5?.stack) {
      this.stack = head5.stack;
    }
  }
  toJSON() {
    return {
      _id: "FiberFailure",
      cause: this[FiberFailureCauseId].toJSON()
    };
  }
  toString() {
    return "(FiberFailure) " + pretty(this[FiberFailureCauseId], {
      renderErrorCause: true
    });
  }
  [NodeInspectSymbol]() {
    return this.toString();
  }
};
var fiberFailure = (cause2) => {
  const limit = Error.stackTraceLimit;
  Error.stackTraceLimit = 0;
  const error = new FiberFailureImpl(cause2);
  Error.stackTraceLimit = limit;
  return error;
};
var isFiberFailure = (u) => hasProperty(u, FiberFailureId);
var fastPath = (effect) => {
  const op = effect;
  switch (op._op) {
    case "Failure":
    case "Success": {
      return op;
    }
    case "Left": {
      return exitFail(op.left);
    }
    case "Right": {
      return exitSucceed(op.right);
    }
    case "Some": {
      return exitSucceed(op.value);
    }
    case "None": {
      return exitFail(new NoSuchElementException());
    }
  }
};
var unsafeRunSyncExit = makeDual((runtime2, effect) => {
  const op = fastPath(effect);
  if (op) {
    return op;
  }
  const scheduler = new SyncScheduler();
  const fiberRuntime = unsafeFork3(runtime2)(effect, {
    scheduler
  });
  scheduler.flush();
  const result = fiberRuntime.unsafePoll();
  if (result) {
    return result;
  }
  return exitDie(capture(asyncFiberException(fiberRuntime), currentSpanFromFiber(fiberRuntime)));
});
var unsafeRunPromise = makeDual((runtime2, effect, options) => unsafeRunPromiseExit(runtime2, effect, options).then((result) => {
  switch (result._tag) {
    case OP_SUCCESS: {
      return result.effect_instruction_i0;
    }
    case OP_FAILURE: {
      throw fiberFailure(result.effect_instruction_i0);
    }
  }
}));
var unsafeRunPromiseExit = makeDual((runtime2, effect, options) => new Promise((resolve) => {
  const op = fastPath(effect);
  if (op) {
    resolve(op);
  }
  const fiber = unsafeFork3(runtime2)(effect);
  fiber.addObserver((exit3) => {
    resolve(exit3);
  });
  if (options?.signal !== void 0) {
    if (options.signal.aborted) {
      fiber.unsafeInterruptAsFork(fiber.id());
    } else {
      options.signal.addEventListener("abort", () => {
        fiber.unsafeInterruptAsFork(fiber.id());
      }, {
        once: true
      });
    }
  }
}));
var RuntimeImpl = class {
  context;
  runtimeFlags;
  fiberRefs;
  constructor(context4, runtimeFlags2, fiberRefs4) {
    this.context = context4;
    this.runtimeFlags = runtimeFlags2;
    this.fiberRefs = fiberRefs4;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make35 = (options) => new RuntimeImpl(options.context, options.runtimeFlags, options.fiberRefs);
var runtime = () => withFiberRuntime((state, status3) => succeed(new RuntimeImpl(state.getFiberRef(currentContext), status3.runtimeFlags, state.getFiberRefs())));
var defaultRuntimeFlags = make7(Interruption, CooperativeYielding, RuntimeMetrics);
var defaultRuntime = make35({
  context: empty3(),
  runtimeFlags: defaultRuntimeFlags,
  fiberRefs: empty12()
});
var updateRuntimeFlags2 = dual(2, (self, f) => make35({
  context: self.context,
  runtimeFlags: f(self.runtimeFlags),
  fiberRefs: self.fiberRefs
}));
var disableRuntimeFlag = dual(2, (self, flag) => updateRuntimeFlags2(self, disable(flag)));
var enableRuntimeFlag = dual(2, (self, flag) => updateRuntimeFlags2(self, enable(flag)));
var updateContext2 = dual(2, (self, f) => make35({
  context: f(self.context),
  runtimeFlags: self.runtimeFlags,
  fiberRefs: self.fiberRefs
}));
var provideService3 = dual(3, (self, tag, service3) => updateContext2(self, add(tag, service3)));
var updateFiberRefs2 = dual(2, (self, f) => make35({
  context: self.context,
  runtimeFlags: self.runtimeFlags,
  fiberRefs: f(self.fiberRefs)
}));
var setFiberRef = dual(3, (self, fiberRef, value2) => updateFiberRefs2(self, updateAs2({
  fiberId: none4,
  fiberRef,
  value: value2
})));
var deleteFiberRef = dual(2, (self, fiberRef) => updateFiberRefs2(self, delete_2(fiberRef)));
var unsafeRunEffect = unsafeRunCallback(defaultRuntime);
var unsafeForkEffect = unsafeFork3(defaultRuntime);
var unsafeRunPromiseEffect = unsafeRunPromise(defaultRuntime);
var unsafeRunPromiseExitEffect = unsafeRunPromiseExit(defaultRuntime);
var unsafeRunSyncEffect = unsafeRunSync(defaultRuntime);
var unsafeRunSyncExitEffect = unsafeRunSyncExit(defaultRuntime);
var asyncEffect = (register) => suspend(() => {
  let cleanup = void 0;
  return flatMap4(deferredMake(), (deferred) => flatMap4(runtime(), (runtime2) => uninterruptibleMask((restore) => zipRight(fork2(restore(matchCauseEffect(register((cb) => unsafeRunCallback(runtime2)(intoDeferred(cb, deferred))), {
    onFailure: (cause2) => deferredFailCause(deferred, cause2),
    onSuccess: (cleanup_) => {
      cleanup = cleanup_;
      return void_;
    }
  }))), restore(onInterrupt(deferredAwait(deferred), () => cleanup ?? void_))))));
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/managedRuntime/circular.js
var TypeId7 = Symbol.for("effect/ManagedRuntime");

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/layer.js
var OP_EXTEND_SCOPE = "ExtendScope";
var OP_FOLD = "Fold";
var OP_FRESH = "Fresh";
var OP_FROM_EFFECT = "FromEffect";
var OP_SCOPED = "Scoped";
var OP_SUSPEND = "Suspend";
var OP_PROVIDE = "Provide";
var OP_PROVIDE_MERGE = "ProvideMerge";
var OP_MERGE_ALL = "MergeAll";
var OP_ZIP_WITH2 = "ZipWith";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/synchronizedRef.js
var getAndUpdateEffect = dual(2, (self, f) => self.modifyEffect((value2) => map4(f(value2), (result) => [value2, result])));
var getAndUpdateSomeEffect = dual(2, (self, pf) => self.modifyEffect((value2) => {
  const result = pf(value2);
  switch (result._tag) {
    case "None": {
      return succeed([value2, value2]);
    }
    case "Some": {
      return map4(result.value, (newValue) => [value2, newValue]);
    }
  }
}));
var modify5 = dual(2, (self, f) => self.modify(f));
var modifyEffect = dual(2, (self, f) => self.modifyEffect(f));
var modifySomeEffect = dual(3, (self, fallback, pf) => self.modifyEffect((value2) => pipe(pf(value2), getOrElse(() => succeed([fallback, value2])))));
var updateEffect = dual(2, (self, f) => self.modifyEffect((value2) => map4(f(value2), (result) => [void 0, result])));
var updateAndGetEffect = dual(2, (self, f) => self.modifyEffect((value2) => map4(f(value2), (result) => [result, result])));
var updateSomeEffect = dual(2, (self, pf) => self.modifyEffect((value2) => {
  const result = pf(value2);
  switch (result._tag) {
    case "None": {
      return succeed([void 0, value2]);
    }
    case "Some": {
      return map4(result.value, (a) => [void 0, a]);
    }
  }
}));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/layer.js
var LayerSymbolKey = "effect/Layer";
var LayerTypeId = Symbol.for(LayerSymbolKey);
var layerVariance = {
  /* c8 ignore next */
  _RIn: (_) => _,
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _ROut: (_) => _
};
var proto2 = {
  [LayerTypeId]: layerVariance,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var MemoMapTypeIdKey = "effect/Layer/MemoMap";
var MemoMapTypeId = Symbol.for(MemoMapTypeIdKey);
var CurrentMemoMap = Reference()("effect/Layer/CurrentMemoMap", {
  defaultValue: () => unsafeMakeMemoMap()
});
var isLayer = (u) => hasProperty(u, LayerTypeId);
var isFresh = (self) => {
  return self._op_layer === OP_FRESH;
};
var MemoMapImpl = class {
  ref;
  [MemoMapTypeId];
  constructor(ref) {
    this.ref = ref;
    this[MemoMapTypeId] = MemoMapTypeId;
  }
  /**
   * Checks the memo map to see if a layer exists. If it is, immediately
   * returns it. Otherwise, obtains the layer, stores it in the memo map,
   * and adds a finalizer to the `Scope`.
   */
  getOrElseMemoize(layer, scope4) {
    return pipe(modifyEffect(this.ref, (map15) => {
      const inMap = map15.get(layer);
      if (inMap !== void 0) {
        const [acquire, release] = inMap;
        const cached3 = pipe(acquire, flatMap4(([patch7, b]) => pipe(patchFiberRefs(patch7), as2(b))), onExit(exitMatch({
          onFailure: () => void_,
          onSuccess: () => scopeAddFinalizerExit(scope4, release)
        })));
        return succeed([cached3, map15]);
      }
      return pipe(make13(0), flatMap4((observers) => pipe(deferredMake(), flatMap4((deferred) => pipe(make13(() => void_), map4((finalizerRef) => {
        const resource = uninterruptibleMask((restore) => pipe(scopeMake2(), flatMap4((innerScope) => pipe(restore(flatMap4(makeBuilder(layer, innerScope, true), (f) => diffFiberRefs(f(this)))), exit, flatMap4((exit3) => {
          switch (exit3._tag) {
            case OP_FAILURE: {
              return pipe(deferredFailCause(deferred, exit3.effect_instruction_i0), zipRight(scopeClose(innerScope, exit3)), zipRight(failCause(exit3.effect_instruction_i0)));
            }
            case OP_SUCCESS: {
              return pipe(set3(finalizerRef, (exit4) => pipe(scopeClose(innerScope, exit4), whenEffect(modify(observers, (n) => [n === 1, n - 1])), asVoid)), zipRight(update2(observers, (n) => n + 1)), zipRight(scopeAddFinalizerExit(scope4, (exit4) => pipe(sync(() => map15.delete(layer)), zipRight(get5(finalizerRef)), flatMap4((finalizer) => finalizer(exit4))))), zipRight(deferredSucceed(deferred, exit3.effect_instruction_i0)), as2(exit3.effect_instruction_i0[1]));
            }
          }
        })))));
        const memoized = [pipe(deferredAwait(deferred), onExit(exitMatchEffect({
          onFailure: () => void_,
          onSuccess: () => update2(observers, (n) => n + 1)
        }))), (exit3) => pipe(get5(finalizerRef), flatMap4((finalizer) => finalizer(exit3)))];
        return [resource, isFresh(layer) ? map15 : map15.set(layer, memoized)];
      }))))));
    }), flatten4);
  }
};
var makeMemoMap = suspend(() => map4(makeSynchronized(/* @__PURE__ */ new Map()), (ref) => new MemoMapImpl(ref)));
var unsafeMakeMemoMap = () => new MemoMapImpl(unsafeMakeSynchronized(/* @__PURE__ */ new Map()));
var build = (self) => scopeWith((scope4) => buildWithScope(self, scope4));
var buildWithScope = dual(2, (self, scope4) => flatMap4(makeMemoMap, (memoMap) => buildWithMemoMap(self, memoMap, scope4)));
var buildWithMemoMap = dual(3, (self, memoMap, scope4) => flatMap4(makeBuilder(self, scope4), (run) => provideService2(run(memoMap), CurrentMemoMap, memoMap)));
var makeBuilder = (self, scope4, inMemoMap = false) => {
  const op = self;
  switch (op._op_layer) {
    case "Locally": {
      return sync(() => (memoMap) => op.f(memoMap.getOrElseMemoize(op.self, scope4)));
    }
    case "ExtendScope": {
      return sync(() => (memoMap) => scopeWith((scope5) => memoMap.getOrElseMemoize(op.layer, scope5)));
    }
    case "Fold": {
      return sync(() => (memoMap) => pipe(memoMap.getOrElseMemoize(op.layer, scope4), matchCauseEffect({
        onFailure: (cause2) => memoMap.getOrElseMemoize(op.failureK(cause2), scope4),
        onSuccess: (value2) => memoMap.getOrElseMemoize(op.successK(value2), scope4)
      })));
    }
    case "Fresh": {
      return sync(() => (_) => pipe(op.layer, buildWithScope(scope4)));
    }
    case "FromEffect": {
      return inMemoMap ? sync(() => (_) => op.effect) : sync(() => (memoMap) => memoMap.getOrElseMemoize(self, scope4));
    }
    case "Provide": {
      return sync(() => (memoMap) => pipe(memoMap.getOrElseMemoize(op.first, scope4), flatMap4((env) => pipe(memoMap.getOrElseMemoize(op.second, scope4), provideContext(env)))));
    }
    case "Scoped": {
      return inMemoMap ? sync(() => (_) => scopeExtend(op.effect, scope4)) : sync(() => (memoMap) => memoMap.getOrElseMemoize(self, scope4));
    }
    case "Suspend": {
      return sync(() => (memoMap) => memoMap.getOrElseMemoize(op.evaluate(), scope4));
    }
    case "ProvideMerge": {
      return sync(() => (memoMap) => pipe(memoMap.getOrElseMemoize(op.first, scope4), zipWith2(memoMap.getOrElseMemoize(op.second, scope4), op.zipK)));
    }
    case "ZipWith": {
      return gen(function* () {
        const parallelScope = yield* scopeFork(scope4, parallel2);
        const firstScope = yield* scopeFork(parallelScope, sequential2);
        const secondScope = yield* scopeFork(parallelScope, sequential2);
        return (memoMap) => pipe(memoMap.getOrElseMemoize(op.first, firstScope), zipWithOptions(memoMap.getOrElseMemoize(op.second, secondScope), op.zipK, {
          concurrent: true
        }));
      });
    }
    case "MergeAll": {
      const layers = op.layers;
      return map4(scopeFork(scope4, parallel2), (parallelScope) => (memoMap) => {
        const contexts = new Array(layers.length);
        return map4(forEachConcurrentDiscard(layers, fnUntraced(function* (layer, i) {
          const scope5 = yield* scopeFork(parallelScope, sequential2);
          const context4 = yield* memoMap.getOrElseMemoize(layer, scope5);
          contexts[i] = context4;
        }), false, false), () => mergeAll(...contexts));
      });
    }
  }
};
var catchAll3 = dual(2, (self, onFailure) => match14(self, {
  onFailure,
  onSuccess: succeedContext
}));
var catchAllCause3 = dual(2, (self, onFailure) => matchCause3(self, {
  onFailure,
  onSuccess: succeedContext
}));
var die6 = (defect) => failCause7(die5(defect));
var dieSync2 = (evaluate2) => failCauseSync3(() => die5(evaluate2()));
var discard = (self) => map14(self, () => empty3());
var context3 = () => fromEffectContext(context());
var extendScope = (self) => {
  const extendScope2 = Object.create(proto2);
  extendScope2._op_layer = OP_EXTEND_SCOPE;
  extendScope2.layer = self;
  return extendScope2;
};
var fail9 = (error) => failCause7(fail7(error));
var failSync3 = (evaluate2) => failCauseSync3(() => fail7(evaluate2()));
var failCause7 = (cause2) => fromEffectContext(failCause(cause2));
var failCauseSync3 = (evaluate2) => fromEffectContext(failCauseSync(evaluate2));
var flatMap9 = dual(2, (self, f) => match14(self, {
  onFailure: fail9,
  onSuccess: f
}));
var flatten8 = dual(2, (self, tag) => flatMap9(self, get(tag)));
var fresh = (self) => {
  const fresh2 = Object.create(proto2);
  fresh2._op_layer = OP_FRESH;
  fresh2.layer = self;
  return fresh2;
};
var fromEffect4 = dual(2, (a, b) => {
  const tagFirst = isTag(a);
  const tag = tagFirst ? a : b;
  const effect = tagFirst ? b : a;
  return fromEffectContext(map4(effect, (service3) => make2(tag, service3)));
});
var fromEffectDiscard = (effect) => fromEffectContext(map4(effect, () => empty3()));
function fromEffectContext(effect) {
  const fromEffect5 = Object.create(proto2);
  fromEffect5._op_layer = OP_FROM_EFFECT;
  fromEffect5.effect = effect;
  return fromEffect5;
}
var fiberRefLocally2 = dual(3, (self, ref, value2) => locallyEffect(self, fiberRefLocally(ref, value2)));
var locallyEffect = dual(2, (self, f) => {
  const locally2 = Object.create(proto2);
  locally2._op_layer = "Locally";
  locally2.self = self;
  locally2.f = f;
  return locally2;
});
var fiberRefLocallyWith2 = dual(3, (self, ref, value2) => locallyEffect(self, fiberRefLocallyWith(ref, value2)));
var fiberRefLocallyScoped2 = (self, value2) => scopedDiscard(fiberRefLocallyScoped(self, value2));
var fiberRefLocallyScopedWith2 = (self, value2) => scopedDiscard(fiberRefLocallyScopedWith(self, value2));
var fromFunction = (tagA, tagB, f) => fromEffectContext(map4(tagA, (a) => make2(tagB, f(a))));
var launch = (self) => scopedEffect(zipRight(scopeWith((scope4) => pipe(self, buildWithScope(scope4))), never));
var mock = function() {
  if (arguments.length === 1) {
    return (service3) => mockImpl(arguments[0], service3);
  }
  return mockImpl(arguments[0], arguments[1]);
};
var mockImpl = (tag, service3) => succeed9(tag, new Proxy({
  ...service3
}, {
  get(target, prop, _receiver) {
    if (prop in target) {
      return target[prop];
    }
    const prevLimit = Error.stackTraceLimit;
    Error.stackTraceLimit = 2;
    const error = new Error(`${tag.key}: Unimplemented method "${prop.toString()}"`);
    Error.stackTraceLimit = prevLimit;
    error.name = "UnimplementedError";
    return makeUnimplemented(error);
  },
  has: constTrue
}));
var makeUnimplemented = (error) => {
  const dead = die2(error);
  function unimplemented() {
    return dead;
  }
  Object.assign(unimplemented, dead);
  Object.setPrototypeOf(unimplemented, Object.getPrototypeOf(dead));
  return unimplemented;
};
var map14 = dual(2, (self, f) => flatMap9(self, (context4) => succeedContext(f(context4))));
var mapError4 = dual(2, (self, f) => catchAll3(self, (error) => failSync3(() => f(error))));
var matchCause3 = dual(2, (self, {
  onFailure,
  onSuccess
}) => {
  const fold = Object.create(proto2);
  fold._op_layer = OP_FOLD;
  fold.layer = self;
  fold.failureK = onFailure;
  fold.successK = onSuccess;
  return fold;
});
var match14 = dual(2, (self, {
  onFailure,
  onSuccess
}) => matchCause3(self, {
  onFailure: (cause2) => {
    const failureOrCause3 = failureOrCause2(cause2);
    switch (failureOrCause3._tag) {
      case "Left": {
        return onFailure(failureOrCause3.left);
      }
      case "Right": {
        return failCause7(failureOrCause3.right);
      }
    }
  },
  onSuccess
}));
var memoize2 = (self) => scopeWith((scope4) => map4(memoize(buildWithScope(self, scope4)), fromEffectContext));
var merge4 = dual(2, (self, that) => zipWith6(self, that, (a, b) => merge2(a, b)));
var mergeAll3 = (...layers) => {
  const mergeAll4 = Object.create(proto2);
  mergeAll4._op_layer = OP_MERGE_ALL;
  mergeAll4.layers = layers;
  return mergeAll4;
};
var orDie3 = (self) => catchAll3(self, (defect) => die6(defect));
var orElse5 = dual(2, (self, that) => catchAll3(self, that));
var passthrough = (self) => merge4(context3(), self);
var project = dual(4, (self, tagA, tagB, f) => map14(self, (context4) => make2(tagB, f(unsafeGet2(context4, tagA)))));
var retry2 = dual(2, (self, schedule) => suspend3(() => {
  const stateTag = GenericTag("effect/Layer/retry/{ state: unknown }");
  return pipe(succeed9(stateTag, {
    state: schedule.initial
  }), flatMap9((env) => retryLoop(self, schedule, stateTag, pipe(env, get(stateTag)).state)));
}));
var retryLoop = (self, schedule, stateTag, state) => {
  return pipe(self, catchAll3((error) => pipe(retryUpdate(schedule, stateTag, error, state), flatMap9((env) => fresh(retryLoop(self, schedule, stateTag, pipe(env, get(stateTag)).state))))));
};
var retryUpdate = (schedule, stateTag, error, state) => {
  return fromEffect4(stateTag, pipe(currentTimeMillis2, flatMap4((now) => pipe(schedule.step(now, error, state), flatMap4(([state2, _, decision]) => isDone4(decision) ? fail2(error) : pipe(sleep3(millis(start2(decision.intervals) - now)), as2({
    state: state2
  })))))));
};
var scoped3 = dual(2, (a, b) => {
  const tagFirst = isTag(a);
  const tag = tagFirst ? a : b;
  const effect = tagFirst ? b : a;
  return scopedContext(map4(effect, (service3) => make2(tag, service3)));
});
var scopedDiscard = (effect) => scopedContext(pipe(effect, as2(empty3())));
var scopedContext = (effect) => {
  const scoped4 = Object.create(proto2);
  scoped4._op_layer = OP_SCOPED;
  scoped4.effect = effect;
  return scoped4;
};
var scope3 = scopedContext(map4(acquireRelease2(scopeMake2(), (scope4, exit3) => scope4.close(exit3)), (scope4) => make2(Scope, scope4)));
var service2 = (tag) => fromEffect4(tag, tag);
var succeed9 = dual(2, (a, b) => {
  const tagFirst = isTag(a);
  const tag = tagFirst ? a : b;
  const resource = tagFirst ? b : a;
  return fromEffectContext(succeed(make2(tag, resource)));
});
var succeedContext = (context4) => {
  return fromEffectContext(succeed(context4));
};
var empty26 = succeedContext(empty3());
var suspend3 = (evaluate2) => {
  const suspend4 = Object.create(proto2);
  suspend4._op_layer = OP_SUSPEND;
  suspend4.evaluate = evaluate2;
  return suspend4;
};
var sync5 = dual(2, (a, b) => {
  const tagFirst = isTag(a);
  const tag = tagFirst ? a : b;
  const evaluate2 = tagFirst ? b : a;
  return fromEffectContext(sync(() => make2(tag, evaluate2())));
});
var syncContext = (evaluate2) => {
  return fromEffectContext(sync(evaluate2));
};
var tap3 = dual(2, (self, f) => flatMap9(self, (context4) => fromEffectContext(as2(f(context4), context4))));
var tapError3 = dual(2, (self, f) => catchAll3(self, (e) => fromEffectContext(flatMap4(f(e), () => fail2(e)))));
var tapErrorCause3 = dual(2, (self, f) => catchAllCause3(self, (cause2) => fromEffectContext(flatMap4(f(cause2), () => failCause(cause2)))));
var toRuntime = (self) => pipe(scopeWith((scope4) => buildWithScope(self, scope4)), flatMap4((context4) => pipe(runtime(), provideContext(context4))));
var toRuntimeWithMemoMap = dual(2, (self, memoMap) => flatMap4(scopeWith((scope4) => buildWithMemoMap(self, memoMap, scope4)), (context4) => pipe(runtime(), provideContext(context4))));
var provide = dual(2, (self, that) => suspend3(() => {
  const provideTo = Object.create(proto2);
  provideTo._op_layer = OP_PROVIDE;
  provideTo.first = Object.create(proto2, {
    _op_layer: {
      value: OP_PROVIDE_MERGE,
      enumerable: true
    },
    first: {
      value: context3(),
      enumerable: true
    },
    second: {
      value: Array.isArray(that) ? mergeAll3(...that) : that
    },
    zipK: {
      value: (a, b) => pipe(a, merge2(b))
    }
  });
  provideTo.second = self;
  return provideTo;
}));
var provideMerge = dual(2, (that, self) => {
  const zipWith7 = Object.create(proto2);
  zipWith7._op_layer = OP_PROVIDE_MERGE;
  zipWith7.first = self;
  zipWith7.second = provide(that, self);
  zipWith7.zipK = (a, b) => {
    return pipe(a, merge2(b));
  };
  return zipWith7;
});
var zipWith6 = dual(3, (self, that, f) => suspend3(() => {
  const zipWith7 = Object.create(proto2);
  zipWith7._op_layer = OP_ZIP_WITH2;
  zipWith7.first = self;
  zipWith7.second = that;
  zipWith7.zipK = f;
  return zipWith7;
}));
var unwrapEffect = (self) => {
  const tag = GenericTag("effect/Layer/unwrapEffect/Layer.Layer<R1, E1, A>");
  return flatMap9(fromEffect4(tag, self), (context4) => get(context4, tag));
};
var unwrapScoped = (self) => {
  const tag = GenericTag("effect/Layer/unwrapScoped/Layer.Layer<R1, E1, A>");
  return flatMap9(scoped3(tag, self), (context4) => get(context4, tag));
};
var annotateLogs2 = dual((args2) => isLayer(args2[0]), function() {
  const args2 = arguments;
  return fiberRefLocallyWith2(args2[0], currentLogAnnotations, typeof args2[1] === "string" ? set2(args2[1], args2[2]) : (annotations) => Object.entries(args2[1]).reduce((acc, [key, value2]) => set2(acc, key, value2), annotations));
});
var annotateSpans2 = dual((args2) => isLayer(args2[0]), function() {
  const args2 = arguments;
  return fiberRefLocallyWith2(args2[0], currentTracerSpanAnnotations, typeof args2[1] === "string" ? set2(args2[1], args2[2]) : (annotations) => Object.entries(args2[1]).reduce((acc, [key, value2]) => set2(acc, key, value2), annotations));
});
var withSpan2 = function() {
  const dataFirst = typeof arguments[0] !== "string";
  const name = dataFirst ? arguments[1] : arguments[0];
  const options = addSpanStackTrace(dataFirst ? arguments[2] : arguments[1]);
  if (dataFirst) {
    const self = arguments[0];
    return unwrapScoped(map4(options?.onEnd ? tap(makeSpanScoped(name, options), (span2) => addFinalizer2((exit3) => options.onEnd(span2, exit3))) : makeSpanScoped(name, options), (span2) => withParentSpan2(self, span2)));
  }
  return (self) => unwrapScoped(map4(options?.onEnd ? tap(makeSpanScoped(name, options), (span2) => addFinalizer2((exit3) => options.onEnd(span2, exit3))) : makeSpanScoped(name, options), (span2) => withParentSpan2(self, span2)));
};
var withParentSpan2 = dual(2, (self, span2) => provide(self, succeedContext(make2(spanTag, span2))));
var provideSomeLayer = dual(2, (self, layer) => scopedWith((scope4) => flatMap4(buildWithScope(layer, scope4), (context4) => provideSomeContext(self, context4))));
var provideSomeRuntime = dual(2, (self, rt) => {
  const patchRefs = diff3(defaultRuntime.fiberRefs, rt.fiberRefs);
  const patchFlags = diff(defaultRuntime.runtimeFlags, rt.runtimeFlags);
  return uninterruptibleMask((restore) => withFiberRuntime((fiber) => {
    const oldContext = fiber.getFiberRef(currentContext);
    const oldRefs = fiber.getFiberRefs();
    const newRefs = patch4(fiber.id(), oldRefs)(patchRefs);
    const oldFlags = fiber.currentRuntimeFlags;
    const newFlags = patch(patchFlags)(oldFlags);
    const rollbackRefs = diff3(newRefs, oldRefs);
    const rollbackFlags = diff(newFlags, oldFlags);
    fiber.setFiberRefs(newRefs);
    fiber.currentRuntimeFlags = newFlags;
    return ensuring2(provideSomeContext(restore(self), merge2(oldContext, rt.context)), withFiberRuntime((fiber2) => {
      fiber2.setFiberRefs(patch4(fiber2.id(), fiber2.getFiberRefs())(rollbackRefs));
      fiber2.currentRuntimeFlags = patch(rollbackFlags)(fiber2.currentRuntimeFlags);
      return void_;
    }));
  }));
});
var effect_provide = dual(2, (self, source) => {
  if (Array.isArray(source)) {
    return provideSomeLayer(self, mergeAll3(...source));
  } else if (isLayer(source)) {
    return provideSomeLayer(self, source);
  } else if (isContext(source)) {
    return provideSomeContext(self, source);
  } else if (TypeId7 in source) {
    return flatMap4(source.runtimeEffect, (rt) => provideSomeRuntime(self, rt));
  } else {
    return provideSomeRuntime(self, source);
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Deferred.js
var Deferred_exports = {};
__export(Deferred_exports, {
  DeferredTypeId: () => DeferredTypeId2,
  await: () => _await3,
  complete: () => complete2,
  completeWith: () => completeWith,
  die: () => die7,
  dieSync: () => dieSync3,
  done: () => done7,
  fail: () => fail10,
  failCause: () => failCause8,
  failCauseSync: () => failCauseSync4,
  failSync: () => failSync4,
  interrupt: () => interrupt7,
  interruptWith: () => interruptWith,
  isDone: () => isDone5,
  make: () => make36,
  makeAs: () => makeAs,
  poll: () => poll3,
  succeed: () => succeed10,
  sync: () => sync6,
  unsafeDone: () => unsafeDone,
  unsafeMake: () => unsafeMake8
});
var DeferredTypeId2 = DeferredTypeId;
var make36 = deferredMake;
var makeAs = deferredMakeAs;
var _await3 = deferredAwait;
var complete2 = deferredComplete;
var completeWith = deferredCompleteWith;
var done7 = deferredDone;
var fail10 = deferredFail;
var failSync4 = deferredFailSync;
var failCause8 = deferredFailCause;
var failCauseSync4 = deferredFailCauseSync;
var die7 = deferredDie;
var dieSync3 = deferredDieSync;
var interrupt7 = deferredInterrupt;
var interruptWith = deferredInterruptWith;
var isDone5 = deferredIsDone;
var poll3 = deferredPoll;
var succeed10 = deferredSucceed;
var sync6 = deferredSync;
var unsafeMake8 = deferredUnsafeMake;
var unsafeDone = deferredUnsafeDone;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MutableList.js
var MutableList_exports = {};
__export(MutableList_exports, {
  append: () => append3,
  empty: () => empty27,
  forEach: () => forEach8,
  fromIterable: () => fromIterable12,
  head: () => head4,
  isEmpty: () => isEmpty7,
  length: () => length,
  make: () => make37,
  pop: () => pop,
  prepend: () => prepend4,
  reset: () => reset,
  shift: () => shift,
  tail: () => tail
});
var TypeId8 = Symbol.for("effect/MutableList");
var MutableListProto = {
  [TypeId8]: TypeId8,
  [Symbol.iterator]() {
    let done8 = false;
    let head5 = this.head;
    return {
      next() {
        if (done8) {
          return this.return();
        }
        if (head5 == null) {
          done8 = true;
          return this.return();
        }
        const value2 = head5.value;
        head5 = head5.next;
        return {
          done: done8,
          value: value2
        };
      },
      return(value2) {
        if (!done8) {
          done8 = true;
        }
        return {
          done: true,
          value: value2
        };
      }
    };
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "MutableList",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeNode = (value2) => ({
  value: value2,
  removed: false,
  prev: void 0,
  next: void 0
});
var empty27 = () => {
  const list = Object.create(MutableListProto);
  list.head = void 0;
  list.tail = void 0;
  list._length = 0;
  return list;
};
var fromIterable12 = (iterable) => {
  const list = empty27();
  for (const element of iterable) {
    append3(list, element);
  }
  return list;
};
var make37 = (...elements) => fromIterable12(elements);
var isEmpty7 = (self) => length(self) === 0;
var length = (self) => self._length;
var tail = (self) => self.tail === void 0 ? void 0 : self.tail.value;
var head4 = (self) => self.head === void 0 ? void 0 : self.head.value;
var forEach8 = dual(2, (self, f) => {
  let current = self.head;
  while (current !== void 0) {
    f(current.value);
    current = current.next;
  }
});
var reset = (self) => {
  ;
  self._length = 0;
  self.head = void 0;
  self.tail = void 0;
  return self;
};
var append3 = dual(2, (self, value2) => {
  const node = makeNode(value2);
  if (self.head === void 0) {
    self.head = node;
  }
  if (self.tail === void 0) {
    self.tail = node;
  } else {
    self.tail.next = node;
    node.prev = self.tail;
    self.tail = node;
  }
  ;
  self._length += 1;
  return self;
});
var shift = (self) => {
  const head5 = self.head;
  if (head5 !== void 0) {
    remove3(self, head5);
    return head5.value;
  }
  return void 0;
};
var pop = (self) => {
  const tail2 = self.tail;
  if (tail2 !== void 0) {
    remove3(self, tail2);
    return tail2.value;
  }
  return void 0;
};
var prepend4 = dual(2, (self, value2) => {
  const node = makeNode(value2);
  node.next = self.head;
  if (self.head !== void 0) {
    self.head.prev = node;
  }
  self.head = node;
  if (self.tail === void 0) {
    self.tail = node;
  }
  ;
  self._length += 1;
  return self;
});
var remove3 = (self, node) => {
  if (node.removed) {
    return;
  }
  node.removed = true;
  if (node.prev !== void 0 && node.next !== void 0) {
    node.prev.next = node.next;
    node.next.prev = node.prev;
  } else if (node.prev !== void 0) {
    self.tail = node.prev;
    node.prev.next = void 0;
  } else if (node.next !== void 0) {
    self.head = node.next;
    node.next.prev = void 0;
  } else {
    self.tail = void 0;
    self.head = void 0;
  }
  if (self._length > 0) {
    ;
    self._length -= 1;
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MutableQueue.js
var MutableQueue_exports = {};
__export(MutableQueue_exports, {
  EmptyMutableQueue: () => EmptyMutableQueue,
  bounded: () => bounded,
  capacity: () => capacity,
  isEmpty: () => isEmpty8,
  isFull: () => isFull,
  length: () => length2,
  offer: () => offer,
  offerAll: () => offerAll,
  poll: () => poll4,
  pollUpTo: () => pollUpTo,
  unbounded: () => unbounded
});
var TypeId9 = Symbol.for("effect/MutableQueue");
var EmptyMutableQueue = Symbol.for("effect/mutable/MutableQueue/Empty");
var MutableQueueProto = {
  [TypeId9]: TypeId9,
  [Symbol.iterator]() {
    return Array.from(this.queue)[Symbol.iterator]();
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "MutableQueue",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make38 = (capacity2) => {
  const queue = Object.create(MutableQueueProto);
  queue.queue = empty27();
  queue.capacity = capacity2;
  return queue;
};
var bounded = (capacity2) => make38(capacity2);
var unbounded = () => make38(void 0);
var length2 = (self) => length(self.queue);
var isEmpty8 = (self) => isEmpty7(self.queue);
var isFull = (self) => self.capacity === void 0 ? false : length(self.queue) === self.capacity;
var capacity = (self) => self.capacity === void 0 ? Infinity : self.capacity;
var offer = dual(2, (self, value2) => {
  const queueLength = length(self.queue);
  if (self.capacity !== void 0 && queueLength === self.capacity) {
    return false;
  }
  append3(value2)(self.queue);
  return true;
});
var offerAll = dual(2, (self, values5) => {
  const iterator = values5[Symbol.iterator]();
  let next2;
  let remainder2 = empty2();
  let offering = true;
  while (offering && (next2 = iterator.next()) && !next2.done) {
    offering = offer(next2.value)(self);
  }
  while (next2 != null && !next2.done) {
    remainder2 = prepend2(next2.value)(remainder2);
    next2 = iterator.next();
  }
  return reverse(remainder2);
});
var poll4 = dual(2, (self, def) => {
  if (isEmpty7(self.queue)) {
    return def;
  }
  return shift(self.queue);
});
var pollUpTo = dual(2, (self, n) => {
  let result = empty2();
  let count = 0;
  while (count < n) {
    const element = poll4(EmptyMutableQueue)(self);
    if (element === EmptyMutableQueue) {
      break;
    }
    result = prepend2(element)(result);
    count += 1;
  }
  return reverse(result);
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/cache.js
var complete3 = (key, exit3, entryStats, timeToLiveMillis) => struct({
  _tag: "Complete",
  key,
  exit: exit3,
  entryStats,
  timeToLiveMillis
});
var pending = (key, deferred) => struct({
  _tag: "Pending",
  key,
  deferred
});
var refreshing = (deferred, complete4) => struct({
  _tag: "Refreshing",
  deferred,
  complete: complete4
});
var MapKeyTypeId = Symbol.for("effect/Cache/MapKey");
var MapKeyImpl = class {
  current;
  [MapKeyTypeId] = MapKeyTypeId;
  previous = void 0;
  next = void 0;
  constructor(current) {
    this.current = current;
  }
  [symbol]() {
    return pipe(hash(this.current), combine(hash(this.previous)), combine(hash(this.next)), cached(this));
  }
  [symbol2](that) {
    if (this === that) {
      return true;
    }
    return isMapKey(that) && equals(this.current, that.current) && equals(this.previous, that.previous) && equals(this.next, that.next);
  }
};
var makeMapKey = (current) => new MapKeyImpl(current);
var isMapKey = (u) => hasProperty(u, MapKeyTypeId);
var KeySetImpl = class {
  head = void 0;
  tail = void 0;
  add(key) {
    if (key !== this.tail) {
      if (this.tail === void 0) {
        this.head = key;
        this.tail = key;
      } else {
        const previous = key.previous;
        const next2 = key.next;
        if (next2 !== void 0) {
          key.next = void 0;
          if (previous !== void 0) {
            previous.next = next2;
            next2.previous = previous;
          } else {
            this.head = next2;
            this.head.previous = void 0;
          }
        }
        this.tail.next = key;
        key.previous = this.tail;
        this.tail = key;
      }
    }
  }
  remove() {
    const key = this.head;
    if (key !== void 0) {
      const next2 = key.next;
      if (next2 !== void 0) {
        key.next = void 0;
        this.head = next2;
        this.head.previous = void 0;
      } else {
        this.head = void 0;
        this.tail = void 0;
      }
    }
    return key;
  }
};
var makeKeySet = () => new KeySetImpl();
var makeCacheState = (map15, keys4, accesses, updating, hits, misses) => ({
  map: map15,
  keys: keys4,
  accesses,
  updating,
  hits,
  misses
});
var initialCacheState = () => makeCacheState(empty15(), makeKeySet(), unbounded(), make5(false), 0, 0);
var CacheSymbolKey = "effect/Cache";
var CacheTypeId = Symbol.for(CacheSymbolKey);
var cacheVariance = {
  /* c8 ignore next */
  _Key: (_) => _,
  /* c8 ignore next */
  _Error: (_) => _,
  /* c8 ignore next */
  _Value: (_) => _
};
var ConsumerCacheSymbolKey = "effect/ConsumerCache";
var ConsumerCacheTypeId = Symbol.for(ConsumerCacheSymbolKey);
var consumerCacheVariance = {
  /* c8 ignore next */
  _Key: (_) => _,
  /* c8 ignore next */
  _Error: (_) => _,
  /* c8 ignore next */
  _Value: (_) => _
};
var makeCacheStats = (options) => options;
var makeEntryStats = (loadedMillis) => ({
  loadedMillis
});
var CacheImpl = class {
  capacity;
  context;
  fiberId;
  lookup;
  timeToLive;
  [CacheTypeId] = cacheVariance;
  [ConsumerCacheTypeId] = consumerCacheVariance;
  cacheState;
  constructor(capacity2, context4, fiberId2, lookup, timeToLive) {
    this.capacity = capacity2;
    this.context = context4;
    this.fiberId = fiberId2;
    this.lookup = lookup;
    this.timeToLive = timeToLive;
    this.cacheState = initialCacheState();
  }
  get(key) {
    return map4(this.getEither(key), merge);
  }
  get cacheStats() {
    return sync(() => makeCacheStats({
      hits: this.cacheState.hits,
      misses: this.cacheState.misses,
      size: size4(this.cacheState.map)
    }));
  }
  getOption(key) {
    return suspend(() => match(get7(this.cacheState.map, key), {
      onNone: () => {
        const mapKey = makeMapKey(key);
        this.trackAccess(mapKey);
        this.trackMiss();
        return succeed(none2());
      },
      onSome: (value2) => this.resolveMapValue(value2)
    }));
  }
  getOptionComplete(key) {
    return suspend(() => match(get7(this.cacheState.map, key), {
      onNone: () => {
        const mapKey = makeMapKey(key);
        this.trackAccess(mapKey);
        this.trackMiss();
        return succeed(none2());
      },
      onSome: (value2) => this.resolveMapValue(value2, true)
    }));
  }
  contains(key) {
    return sync(() => has(this.cacheState.map, key));
  }
  entryStats(key) {
    return sync(() => {
      const option3 = get7(this.cacheState.map, key);
      if (isSome(option3)) {
        switch (option3.value._tag) {
          case "Complete": {
            const loaded = option3.value.entryStats.loadedMillis;
            return some2(makeEntryStats(loaded));
          }
          case "Pending": {
            return none2();
          }
          case "Refreshing": {
            const loaded = option3.value.complete.entryStats.loadedMillis;
            return some2(makeEntryStats(loaded));
          }
        }
      }
      return none2();
    });
  }
  getEither(key) {
    return suspend(() => {
      const k = key;
      let mapKey = void 0;
      let deferred = void 0;
      let value2 = getOrUndefined(get7(this.cacheState.map, k));
      if (value2 === void 0) {
        deferred = unsafeMake8(this.fiberId);
        mapKey = makeMapKey(k);
        if (has(this.cacheState.map, k)) {
          value2 = getOrUndefined(get7(this.cacheState.map, k));
        } else {
          set5(this.cacheState.map, k, pending(mapKey, deferred));
        }
      }
      if (value2 === void 0) {
        this.trackAccess(mapKey);
        this.trackMiss();
        return map4(this.lookupValueOf(key, deferred), right);
      } else {
        return flatMap4(this.resolveMapValue(value2), match({
          onNone: () => this.getEither(key),
          onSome: (value3) => succeed(left(value3))
        }));
      }
    });
  }
  invalidate(key) {
    return sync(() => {
      remove(this.cacheState.map, key);
    });
  }
  invalidateWhen(key, when3) {
    return sync(() => {
      const value2 = get7(this.cacheState.map, key);
      if (isSome(value2) && value2.value._tag === "Complete") {
        if (value2.value.exit._tag === "Success") {
          if (when3(value2.value.exit.value)) {
            remove(this.cacheState.map, key);
          }
        }
      }
    });
  }
  get invalidateAll() {
    return sync(() => {
      this.cacheState.map = empty15();
    });
  }
  refresh(key) {
    return clockWith3((clock2) => suspend(() => {
      const k = key;
      const deferred = unsafeMake8(this.fiberId);
      let value2 = getOrUndefined(get7(this.cacheState.map, k));
      if (value2 === void 0) {
        if (has(this.cacheState.map, k)) {
          value2 = getOrUndefined(get7(this.cacheState.map, k));
        } else {
          set5(this.cacheState.map, k, pending(makeMapKey(k), deferred));
        }
      }
      if (value2 === void 0) {
        return asVoid(this.lookupValueOf(key, deferred));
      } else {
        switch (value2._tag) {
          case "Complete": {
            if (this.hasExpired(clock2, value2.timeToLiveMillis)) {
              const found = getOrUndefined(get7(this.cacheState.map, k));
              if (equals(found, value2)) {
                remove(this.cacheState.map, k);
              }
              return asVoid(this.get(key));
            }
            return pipe(this.lookupValueOf(key, deferred), when2(() => {
              const current = getOrUndefined(get7(this.cacheState.map, k));
              if (equals(current, value2)) {
                const mapValue = refreshing(deferred, value2);
                set5(this.cacheState.map, k, mapValue);
                return true;
              }
              return false;
            }), asVoid);
          }
          case "Pending": {
            return _await3(value2.deferred);
          }
          case "Refreshing": {
            return _await3(value2.deferred);
          }
        }
      }
    }));
  }
  set(key, value2) {
    return clockWith3((clock2) => sync(() => {
      const now = clock2.unsafeCurrentTimeMillis();
      const k = key;
      const lookupResult = succeed3(value2);
      const mapValue = complete3(makeMapKey(k), lookupResult, makeEntryStats(now), now + toMillis(decode(this.timeToLive(lookupResult))));
      set5(this.cacheState.map, k, mapValue);
    }));
  }
  get size() {
    return sync(() => {
      return size4(this.cacheState.map);
    });
  }
  get values() {
    return sync(() => {
      const values5 = [];
      for (const entry of this.cacheState.map) {
        if (entry[1]._tag === "Complete" && entry[1].exit._tag === "Success") {
          values5.push(entry[1].exit.value);
        }
      }
      return values5;
    });
  }
  get entries() {
    return sync(() => {
      const values5 = [];
      for (const entry of this.cacheState.map) {
        if (entry[1]._tag === "Complete" && entry[1].exit._tag === "Success") {
          values5.push([entry[0], entry[1].exit.value]);
        }
      }
      return values5;
    });
  }
  get keys() {
    return sync(() => {
      const keys4 = [];
      for (const entry of this.cacheState.map) {
        if (entry[1]._tag === "Complete" && entry[1].exit._tag === "Success") {
          keys4.push(entry[0]);
        }
      }
      return keys4;
    });
  }
  resolveMapValue(value2, ignorePending = false) {
    return clockWith3((clock2) => {
      switch (value2._tag) {
        case "Complete": {
          this.trackAccess(value2.key);
          if (this.hasExpired(clock2, value2.timeToLiveMillis)) {
            remove(this.cacheState.map, value2.key.current);
            return succeed(none2());
          }
          this.trackHit();
          return map4(value2.exit, some2);
        }
        case "Pending": {
          this.trackAccess(value2.key);
          this.trackHit();
          if (ignorePending) {
            return succeed(none2());
          }
          return map4(_await3(value2.deferred), some2);
        }
        case "Refreshing": {
          this.trackAccess(value2.complete.key);
          this.trackHit();
          if (this.hasExpired(clock2, value2.complete.timeToLiveMillis)) {
            if (ignorePending) {
              return succeed(none2());
            }
            return map4(_await3(value2.deferred), some2);
          }
          return map4(value2.complete.exit, some2);
        }
      }
    });
  }
  trackHit() {
    this.cacheState.hits = this.cacheState.hits + 1;
  }
  trackMiss() {
    this.cacheState.misses = this.cacheState.misses + 1;
  }
  trackAccess(key) {
    offer(this.cacheState.accesses, key);
    if (compareAndSet(this.cacheState.updating, false, true)) {
      let loop2 = true;
      while (loop2) {
        const key2 = poll4(this.cacheState.accesses, EmptyMutableQueue);
        if (key2 === EmptyMutableQueue) {
          loop2 = false;
        } else {
          this.cacheState.keys.add(key2);
        }
      }
      let size11 = size4(this.cacheState.map);
      loop2 = size11 > this.capacity;
      while (loop2) {
        const key2 = this.cacheState.keys.remove();
        if (key2 !== void 0) {
          if (has(this.cacheState.map, key2.current)) {
            remove(this.cacheState.map, key2.current);
            size11 = size11 - 1;
            loop2 = size11 > this.capacity;
          }
        } else {
          loop2 = false;
        }
      }
      set(this.cacheState.updating, false);
    }
  }
  hasExpired(clock2, timeToLiveMillis) {
    return clock2.unsafeCurrentTimeMillis() > timeToLiveMillis;
  }
  lookupValueOf(input, deferred) {
    return clockWith3((clock2) => suspend(() => {
      const key = input;
      return pipe(this.lookup(input), provideContext(this.context), exit, flatMap4((exit3) => {
        const now = clock2.unsafeCurrentTimeMillis();
        const stats = makeEntryStats(now);
        const value2 = complete3(makeMapKey(key), exit3, stats, now + toMillis(decode(this.timeToLive(exit3))));
        set5(this.cacheState.map, key, value2);
        return zipRight(done7(deferred, exit3), exit3);
      }), onInterrupt(() => zipRight(interrupt7(deferred), sync(() => {
        remove(this.cacheState.map, key);
      }))));
    }));
  }
};
var make39 = (options) => {
  const timeToLive = decode(options.timeToLive);
  return makeWith({
    capacity: options.capacity,
    lookup: options.lookup,
    timeToLive: () => timeToLive
  });
};
var makeWith = (options) => map4(all4([context(), fiberId]), ([context4, fiberId2]) => new CacheImpl(options.capacity, context4, fiberId2, options.lookup, (exit3) => decode(options.timeToLive(exit3))));
var unsafeMakeWith = (capacity2, lookup, timeToLive) => new CacheImpl(capacity2, empty3(), none3, lookup, (exit3) => decode(timeToLive(exit3)));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/query.js
var currentCache = globalValue(Symbol.for("effect/FiberRef/currentCache"), () => fiberRefUnsafeMake(unsafeMakeWith(65536, () => map4(deferredMake(), (handle) => ({
  listeners: new Listeners(),
  handle
})), () => seconds(60))));
var currentCacheEnabled = globalValue(Symbol.for("effect/FiberRef/currentCacheEnabled"), () => fiberRefUnsafeMake(false));
var fromRequest = (request, dataSource) => flatMap4(isEffect(dataSource) ? dataSource : succeed(dataSource), (ds) => fiberIdWith((id3) => {
  const proxy = new Proxy(request, {});
  return fiberRefGetWith(currentCacheEnabled, (cacheEnabled) => {
    if (cacheEnabled) {
      const cached3 = fiberRefGetWith(currentCache, (cache) => flatMap4(cache.getEither(proxy), (orNew) => {
        switch (orNew._tag) {
          case "Left": {
            if (orNew.left.listeners.interrupted) {
              return flatMap4(cache.invalidateWhen(proxy, (entry) => entry.handle === orNew.left.handle), () => cached3);
            }
            orNew.left.listeners.increment();
            return uninterruptibleMask((restore) => flatMap4(exit(blocked(empty8, restore(deferredAwait(orNew.left.handle)))), (exit3) => {
              orNew.left.listeners.decrement();
              return exit3;
            }));
          }
          case "Right": {
            orNew.right.listeners.increment();
            return uninterruptibleMask((restore) => flatMap4(exit(blocked(single(ds, makeEntry({
              request: proxy,
              result: orNew.right.handle,
              listeners: orNew.right.listeners,
              ownerId: id3,
              state: {
                completed: false
              }
            })), restore(deferredAwait(orNew.right.handle)))), () => {
              orNew.right.listeners.decrement();
              return deferredAwait(orNew.right.handle);
            }));
          }
        }
      }));
      return cached3;
    }
    const listeners = new Listeners();
    listeners.increment();
    return flatMap4(deferredMake(), (ref) => ensuring2(blocked(single(ds, makeEntry({
      request: proxy,
      result: ref,
      listeners,
      ownerId: id3,
      state: {
        completed: false
      }
    })), deferredAwait(ref)), sync(() => listeners.decrement())));
  });
}));
var cacheRequest = (request, result) => {
  return fiberRefGetWith(currentCacheEnabled, (cacheEnabled) => {
    if (cacheEnabled) {
      return fiberRefGetWith(currentCache, (cache) => flatMap4(cache.getEither(request), (orNew) => {
        switch (orNew._tag) {
          case "Left": {
            return void_;
          }
          case "Right": {
            return deferredComplete(orNew.right.handle, result);
          }
        }
      }));
    }
    return void_;
  });
};
var withRequestCaching = dual(2, (self, strategy) => fiberRefLocally(self, currentCacheEnabled, strategy));
var withRequestCache = dual(
  2,
  // @ts-expect-error
  (self, cache) => fiberRefLocally(self, currentCache, cache)
);

export {
  ClockTypeId,
  clockTag,
  Order,
  greaterThanOrEqualTo3 as greaterThanOrEqualTo,
  clamp2 as clamp,
  sign,
  remainder,
  nextPow2,
  parse,
  Number_exports,
  escape,
  RegExp_exports,
  ConfigErrorTypeId,
  And,
  Or,
  InvalidData,
  MissingData,
  SourceUnavailable,
  Unsupported,
  isConfigError,
  isAnd,
  isOr,
  isInvalidData,
  isMissingData,
  isSourceUnavailable,
  isUnsupported,
  prefixed,
  reduceWithContext2 as reduceWithContext,
  isMissingDataOnly,
  empty10 as empty,
  andThen2 as andThen,
  mapName,
  nested,
  unnested,
  OP_CONSTANT,
  OP_FAIL,
  OP_FALLBACK,
  OP_DESCRIBED,
  OP_LAZY,
  OP_MAP_OR_FAIL,
  OP_NESTED,
  OP_PRIMITIVE,
  OP_SEQUENCE,
  OP_HASHMAP,
  OP_ZIP_WITH,
  ConfigProviderTypeId,
  configProviderTag,
  FlatConfigProviderTypeId,
  make11 as make,
  makeFlat,
  fromFlat,
  fromEnv,
  fromMap,
  mapInputPath,
  nested2,
  unnested2,
  orElse2 as orElse,
  constantCase2 as constantCase,
  kebabCase2 as kebabCase,
  lowerCase2 as lowerCase,
  snakeCase2 as snakeCase,
  upperCase2 as upperCase,
  within,
  fromJson,
  TypeId,
  consoleTag,
  RandomTypeId,
  randomTag,
  make9 as make2,
  fixed,
  spanTag,
  addSpanStackTrace,
  DisablePropagation,
  liveServices,
  currentServices,
  clockWith,
  withClock,
  withConfigProvider,
  configProviderWith,
  config,
  randomWith,
  withRandom,
  next,
  nextInt,
  nextBoolean,
  nextRange,
  nextIntBetween,
  shuffle,
  choice,
  tracerWith,
  withTracer,
  not,
  Boolean_exports,
  CommitPrototype2 as CommitPrototype,
  Class,
  StructuralClass,
  Effectable_exports,
  sequential2 as sequential,
  sequential3 as sequential2,
  ExecutionStrategy_exports,
  empty11 as empty2,
  getOrDefault2 as getOrDefault,
  updateAs2 as updateAs,
  FiberRefs_exports,
  diff3 as diff,
  patch4 as patch,
  FiberRefsPatch_exports,
  isDone2 as isDone,
  isSuspended2 as isSuspended,
  FiberStatus_exports,
  LogLevel_exports,
  Micro_exports,
  TypeId3 as TypeId2,
  Readable_exports,
  refVariance,
  unsafeMake4 as unsafeMake,
  make13 as make3,
  get5 as get,
  set3 as set,
  getAndSet,
  getAndUpdate,
  getAndUpdateSome,
  setAndGet,
  modify,
  modifySome,
  update2 as update,
  updateAndGet,
  updateSome,
  updateSomeAndGet,
  unsafeGet3 as unsafeGet,
  RefTypeId2 as RefTypeId,
  make14 as make4,
  get6 as get2,
  getAndSet2,
  getAndUpdate2,
  getAndUpdateSome2,
  modify2,
  modifySome2,
  set4 as set2,
  setAndGet2,
  update3 as update2,
  updateAndGet2,
  updateSome2,
  updateSomeAndGet2,
  Ref_exports,
  SyncScheduler,
  currentScheduler,
  withScheduler,
  Scheduler_exports,
  sleep3 as sleep,
  currentTimeMillis2 as currentTimeMillis,
  Clock_exports,
  LogSpan_exports,
  ParentSpan,
  externalSpan2 as externalSpan,
  Tracer_exports,
  MetricLabelTypeId,
  make20 as make5,
  isMetricLabel,
  annotateLogs,
  asSome2 as asSome,
  asSomeError,
  try_2 as try_,
  _catch,
  catchAllDefect2 as catchAllDefect,
  catchSomeCause,
  catchSomeDefect,
  catchTag2 as catchTag,
  catchTags,
  cause,
  clockWith3 as clockWith2,
  clock,
  delay2 as delay,
  descriptorWith,
  allowInterrupt,
  descriptor,
  diffFiberRefs,
  Do2 as Do,
  bind3 as bind,
  bindTo3 as bindTo,
  let_3 as let_,
  dropUntil,
  dropWhile,
  contextWith,
  eventually,
  filterMap3 as filterMap,
  filterOrDie,
  filterOrDieMessage,
  filterOrElse,
  liftPredicate,
  filterOrFail2 as filterOrFail,
  findFirst,
  firstSuccessOf,
  flipWith,
  match8 as match,
  every2 as every,
  forever2 as forever,
  fiberRefs3 as fiberRefs,
  head3 as head,
  ignore2 as ignore,
  ignoreLogged2 as ignoreLogged,
  inheritFiberRefs,
  isFailure2 as isFailure,
  isSuccess,
  iterate,
  logWithLevel,
  log,
  logTrace,
  logDebug,
  logInfo,
  logWarning,
  logError,
  logFatal,
  withLogSpan,
  logAnnotations,
  loop,
  mapAccum,
  mapErrorCause2 as mapErrorCause,
  memoize,
  merge3 as merge,
  negate2 as negate,
  none6 as none,
  once,
  option2 as option,
  orElseFail,
  orElseSucceed2 as orElseSucceed,
  parallelErrors,
  patchFiberRefs,
  promise2 as promise,
  provideService2 as provideService,
  provideServiceEffect2 as provideServiceEffect,
  random,
  reduce5 as reduce,
  reduceRight2 as reduceRight,
  reduceWhile,
  repeatN,
  sandbox2 as sandbox,
  setFiberRefs,
  sleep4 as sleep2,
  succeedNone2 as succeedNone,
  succeedSome2 as succeedSome,
  summarized,
  tagMetrics,
  labelMetrics,
  takeUntil,
  takeWhile,
  tapBoth,
  tapDefect2 as tapDefect,
  tapError2 as tapError,
  tapErrorTag,
  tapErrorCause2 as tapErrorCause,
  timed,
  timedWith,
  tracer,
  tryPromise2 as tryPromise,
  tryMap,
  tryMapPromise,
  unless,
  unlessEffect,
  unsandbox,
  updateFiberRefs,
  updateService2 as updateService,
  when2 as when,
  whenFiberRef,
  whenRef,
  withMetric,
  serviceFunctionEffect,
  serviceFunction,
  serviceFunctions,
  serviceConstants,
  serviceMembers,
  serviceOption2 as serviceOption,
  serviceOptional,
  annotateCurrentSpan,
  linkSpanCurrent,
  annotateSpans,
  currentParentSpan,
  currentSpan,
  linkSpans,
  makeSpan,
  spanAnnotations,
  spanLinks,
  endSpan,
  useSpan,
  withParentSpan,
  withSpan,
  functionWithSpan,
  fromNullable2 as fromNullable,
  optionFromOptional,
  isExit,
  isFailure3 as isFailure2,
  isSuccess2,
  asVoid3 as asVoid,
  all3 as all,
  die4 as die,
  fail4 as fail,
  failCause3 as failCause,
  forEachEffect,
  interrupt4 as interrupt,
  map7 as map,
  mapBoth2 as mapBoth,
  match9 as match2,
  matchEffect3 as matchEffect,
  succeed3 as succeed,
  void_3 as void_,
  zip5 as zip,
  zipRight2 as zipRight,
  Exit_exports,
  Order4 as Order2,
  LoggerTypeId,
  makeLogger,
  mapInput2 as mapInput,
  mapInputOptions,
  filterLogLevel,
  map10 as map2,
  none7 as none2,
  simple,
  succeed5 as succeed2,
  sync3 as sync,
  zip7 as zip2,
  zipLeft3 as zipLeft,
  zipRight3 as zipRight2,
  stringLogger,
  logfmtLogger,
  structuredLogger,
  jsonLogger,
  isLogger,
  prettyLogger,
  prettyLoggerDefault,
  MetricBoundariesTypeId,
  isMetricBoundaries,
  fromIterable9 as fromIterable,
  linear,
  exponential,
  MetricKeyTypeTypeId,
  CounterKeyTypeTypeId,
  FrequencyKeyTypeTypeId,
  GaugeKeyTypeTypeId,
  HistogramKeyTypeTypeId,
  SummaryKeyTypeTypeId,
  counter,
  frequency,
  gauge,
  histogram,
  summary,
  isMetricKeyType,
  isCounterKey,
  isFrequencyKey,
  isGaugeKey,
  isHistogramKey,
  isSummaryKey,
  MetricKeyTypeId,
  isMetricKey,
  counter2,
  frequency2,
  gauge2,
  histogram2,
  summary2,
  tagged,
  taggedWithLabels,
  empty15 as empty3,
  fromIterable5 as fromIterable2,
  get7 as get3,
  keys,
  has,
  set5 as set3,
  remove,
  clear,
  size4 as size,
  MutableHashMap_exports,
  MetricStateTypeId,
  CounterStateTypeId,
  FrequencyStateTypeId,
  GaugeStateTypeId,
  HistogramStateTypeId,
  SummaryStateTypeId,
  counter3,
  frequency3,
  gauge3,
  histogram3,
  summary3,
  isMetricState,
  isCounterState,
  isFrequencyState,
  isGaugeState,
  isHistogramState,
  isSummaryState,
  MetricHookTypeId,
  make26 as make6,
  onModify,
  onUpdate,
  counter4,
  frequency4,
  gauge4,
  histogram4,
  summary4,
  MetricPairTypeId,
  make27 as make7,
  unsafeMake7 as unsafeMake2,
  MetricRegistryTypeId,
  make28 as make8,
  MetricTypeId,
  globalMetricRegistry,
  make29 as make9,
  mapInput3 as mapInput2,
  counter5,
  frequency5,
  withConstantInput,
  fromMetricKey,
  gauge5,
  histogram5,
  increment2 as increment,
  incrementBy,
  map11 as map3,
  mapType,
  modify4 as modify3,
  set6 as set4,
  succeed6 as succeed3,
  sync4 as sync2,
  summary5,
  summaryTimestamp,
  tagged2,
  taggedWithLabelsInput,
  taggedWithLabels2,
  timer2 as timer,
  timerWithBoundaries,
  trackAll,
  trackDefect,
  trackDefectWith,
  trackDuration,
  trackDurationWith,
  trackError,
  trackErrorWith,
  trackSuccess,
  trackSuccessWith,
  update5 as update3,
  value,
  withNow,
  zip8 as zip3,
  unsafeSnapshot,
  snapshot,
  RequestTypeId,
  isRequest,
  of4 as of,
  tagged3,
  Class2,
  TaggedClass,
  complete,
  completeEffect,
  fail6 as fail2,
  failCause5 as failCause2,
  succeed7 as succeed4,
  Listeners,
  empty17 as empty4,
  fromIterable7 as fromIterable3,
  findFirst3 as findFirst2,
  first2 as first,
  getOrder2 as getOrder,
  has3 as has2,
  insert2 as insert,
  keys3 as keys2,
  last3 as last,
  reduce7 as reduce2,
  removeFirst2 as removeFirst,
  size6 as size2,
  values3 as values,
  RedBlackTree_exports,
  isSortedSet,
  empty18 as empty5,
  fromIterable8 as fromIterable4,
  filter3 as filter,
  union3 as union,
  values4 as values2,
  getEquivalence2 as getEquivalence,
  SortedSet_exports,
  SupervisorTypeId,
  supervisorVariance,
  ProxySupervisor,
  Zip,
  unsafeTrack,
  track,
  fromEffect2 as fromEffect,
  none8 as none3,
  fibersIn,
  Differ_exports,
  fiberStarted,
  fiberActive,
  fiberSuccesses,
  fiberFailures,
  fiberLifetimes,
  currentMinimumLogLevel,
  loggerWithConsoleLog,
  loggerWithLeveledLog,
  loggerWithConsoleError,
  defaultLogger,
  jsonLogger2,
  logFmtLogger,
  prettyLogger2,
  structuredLogger2,
  tracerLogger,
  loggerWithSpanAnnotations,
  currentLoggers,
  batchedLogger,
  annotateLogsScoped,
  whenLogLevel,
  acquireRelease2 as acquireRelease,
  acquireReleaseInterruptible,
  addFinalizer2 as addFinalizer,
  daemonChildren,
  exists2 as exists,
  filter4 as filter2,
  all4 as all2,
  allWith,
  allSuccesses,
  replicate2 as replicate,
  replicateEffect2 as replicateEffect,
  forEach7 as forEach,
  forEachParUnbounded,
  forEachConcurrentDiscard,
  fork2 as fork,
  forkDaemon2 as forkDaemon,
  forkWithErrorHandler,
  mergeAll2 as mergeAll,
  partition2 as partition,
  validateAll,
  raceAll2 as raceAll,
  reduceEffect,
  parallelFinalizers,
  finalizersMask,
  scopeWith,
  scopedWith,
  scopedEffect,
  sequentialFinalizers,
  tagMetricsScoped,
  labelMetricsScoped,
  using,
  validate,
  validateWith,
  validateFirst,
  withClockScoped,
  withRandomScoped,
  withConfigProviderScoped,
  withEarlyRelease,
  zipOptions,
  zipLeftOptions,
  zipRightOptions,
  zipWithOptions,
  withRuntimeFlagsScoped,
  scopeTag,
  scope2 as scope,
  scopeMake2 as scopeMake,
  scopeExtend,
  fiberRefUnsafeMakeSupervisor,
  fiberRefLocallyScoped,
  fiberRefLocallyScopedWith,
  fiberRefMake,
  fiberRefMakeWith,
  fiberRefMakeContext,
  fiberRefMakeRuntimeFlags,
  currentRuntimeFlags,
  currentSupervisor,
  raceWith,
  disconnect,
  race2 as race,
  ensuring2 as ensuring,
  invokeWithInterrupt,
  interruptWhenPossible,
  makeSpanScoped,
  withTracerScoped,
  withSpanScoped,
  TimeoutExceptionTypeId2 as TimeoutExceptionTypeId,
  empty21 as empty6,
  fail7 as fail3,
  die5 as die2,
  interrupt5 as interrupt2,
  parallel4 as parallel,
  sequential4 as sequential3,
  isCause2 as isCause,
  isEmptyType2 as isEmptyType,
  isFailType2 as isFailType,
  isDieType2 as isDieType,
  isInterruptType2 as isInterruptType,
  isInterrupted3 as isInterrupted,
  isInterruptedOnly2 as isInterruptedOnly,
  interruptors2 as interruptors,
  failureOption2 as failureOption,
  failureOrCause2 as failureOrCause,
  flipCauseOption2 as flipCauseOption,
  dieOption2 as dieOption,
  stripSomeDefects2 as stripSomeDefects,
  map12 as map4,
  squash,
  reduce8 as reduce3,
  reduceWithContext3 as reduceWithContext2,
  IllegalArgumentException2 as IllegalArgumentException,
  NoSuchElementException3 as NoSuchElementException,
  isNoSuchElementException2 as isNoSuchElementException,
  RuntimeException2 as RuntimeException,
  isRuntimeException2 as isRuntimeException,
  pretty3 as pretty,
  Cause_exports,
  make31 as make10,
  size10 as size3,
  after2 as after,
  ScheduleInterval_exports,
  union7 as union2,
  intersect4 as intersect,
  start2 as start,
  end2 as end,
  lessThan9 as lessThan,
  isNonEmpty5 as isNonEmpty,
  max6 as max,
  ScheduleIntervals_exports,
  _continue2 as _continue,
  continueWith2 as continueWith,
  done5 as done,
  isContinue2 as isContinue,
  isDone4 as isDone2,
  ScheduleDecision_exports,
  Scope,
  addFinalizer3 as addFinalizer2,
  addFinalizerExit,
  close,
  extend2 as extend,
  fork3 as fork2,
  make34 as make11,
  Scope_exports,
  unsafeMakeSemaphore,
  makeSemaphore,
  unsafeMakeLatch,
  makeLatch,
  awaitAllChildren,
  cached2 as cached,
  cachedInvalidateWithTTL,
  ensuringChild,
  ensuringChildren,
  forkAll,
  forkIn2 as forkIn,
  forkScoped2 as forkScoped,
  fromFiber,
  fromFiberEffect,
  cachedFunction,
  raceFirst2 as raceFirst,
  supervised,
  timeout2 as timeout,
  timeoutFail,
  timeoutFailCause,
  timeoutOption2 as timeoutOption,
  timeoutTo,
  SynchronizedTypeId,
  synchronizedVariance,
  makeSynchronized,
  unsafeMakeSynchronized,
  updateSomeAndGetEffectSynchronized,
  bindAll,
  TypeId7 as TypeId3,
  _await2 as _await,
  getCurrentFiber2 as getCurrentFiber,
  inheritAll2 as inheritAll,
  interrupt6 as interrupt3,
  interruptAs,
  interruptAllAs2 as interruptAllAs,
  join3 as join,
  poll2 as poll,
  Fiber_exports,
  unsafeFork3 as unsafeFork,
  unsafeRunCallback,
  unsafeRunSync,
  isAsyncFiberException,
  FiberFailureCauseId,
  fiberFailure,
  isFiberFailure,
  unsafeRunSyncExit,
  unsafeRunPromise,
  unsafeRunPromiseExit,
  make35 as make12,
  runtime,
  defaultRuntimeFlags,
  defaultRuntime,
  updateRuntimeFlags2 as updateRuntimeFlags,
  disableRuntimeFlag,
  enableRuntimeFlag,
  updateContext2 as updateContext,
  provideService3 as provideService2,
  updateFiberRefs2,
  setFiberRef,
  deleteFiberRef,
  unsafeRunEffect,
  unsafeForkEffect,
  unsafeRunPromiseEffect,
  unsafeRunPromiseExitEffect,
  unsafeRunSyncEffect,
  unsafeRunSyncExitEffect,
  asyncEffect,
  getAndUpdateEffect,
  getAndUpdateSomeEffect,
  modify5 as modify4,
  modifyEffect,
  modifySomeEffect,
  updateEffect,
  updateAndGetEffect,
  updateSomeEffect,
  LayerTypeId,
  MemoMapTypeId,
  CurrentMemoMap,
  isLayer,
  isFresh,
  makeMemoMap,
  unsafeMakeMemoMap,
  build,
  buildWithScope,
  buildWithMemoMap,
  catchAll3 as catchAll,
  catchAllCause3 as catchAllCause,
  die6 as die3,
  dieSync2 as dieSync,
  discard,
  context3 as context,
  extendScope,
  fail9 as fail4,
  failSync3 as failSync,
  failCause7 as failCause3,
  failCauseSync3 as failCauseSync,
  flatMap9 as flatMap,
  flatten8 as flatten,
  fresh,
  fromEffect4 as fromEffect2,
  fromEffectDiscard,
  fromEffectContext,
  fiberRefLocally2 as fiberRefLocally,
  locallyEffect,
  fiberRefLocallyWith2 as fiberRefLocallyWith,
  fiberRefLocallyScoped2,
  fiberRefLocallyScopedWith2,
  fromFunction,
  launch,
  mock,
  map14 as map5,
  mapError4 as mapError,
  matchCause3 as matchCause,
  match14 as match3,
  memoize2,
  merge4 as merge2,
  mergeAll3 as mergeAll2,
  orDie3 as orDie,
  orElse5 as orElse2,
  passthrough,
  project,
  retry2 as retry,
  scoped3 as scoped,
  scopedDiscard,
  scopedContext,
  scope3 as scope2,
  service2 as service,
  succeed9 as succeed5,
  succeedContext,
  empty26 as empty7,
  suspend3 as suspend,
  sync5 as sync3,
  syncContext,
  tap3 as tap,
  tapError3 as tapError2,
  tapErrorCause3 as tapErrorCause2,
  toRuntime,
  toRuntimeWithMemoMap,
  provide,
  provideMerge,
  zipWith6 as zipWith,
  unwrapEffect,
  unwrapScoped,
  annotateLogs2,
  annotateSpans2,
  withSpan2,
  withParentSpan2,
  effect_provide,
  DeferredTypeId2 as DeferredTypeId,
  make36 as make13,
  _await3 as _await2,
  complete2,
  done7 as done2,
  fail10 as fail5,
  failCause8 as failCause4,
  interrupt7 as interrupt4,
  isDone5 as isDone3,
  poll3 as poll2,
  succeed10 as succeed6,
  unsafeMake8 as unsafeMake3,
  unsafeDone,
  Deferred_exports,
  MutableList_exports,
  EmptyMutableQueue,
  bounded,
  unbounded,
  length2 as length,
  isEmpty8 as isEmpty,
  capacity,
  offer,
  offerAll,
  poll4 as poll3,
  pollUpTo,
  MutableQueue_exports,
  makeMapKey,
  makeKeySet,
  CacheTypeId,
  ConsumerCacheTypeId,
  makeCacheStats,
  makeEntryStats,
  make39 as make14,
  makeWith,
  currentCache,
  currentCacheEnabled,
  fromRequest,
  cacheRequest,
  withRequestCaching,
  withRequestCache
};
//# sourceMappingURL=chunk-UYCMDTGT.js.map
