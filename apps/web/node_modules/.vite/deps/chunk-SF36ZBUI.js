import {
  ChannelException,
  ChannelExceptionTypeId,
  ChannelTypeId,
  SinkTypeId,
  acquireReleaseOut,
  acquireUseRelease as acquireUseRelease2,
  as as as2,
  as2 as as3,
  asVoid,
  buffer,
  bufferChunk,
  catchAll,
  catchAllCause as catchAllCause2,
  channelToSink,
  channelToStream,
  collect,
  collectAll,
  collectAllFrom,
  collectAllN,
  collectAllToMap,
  collectAllToMapN,
  collectAllToSet,
  collectAllToSetN,
  collectAllUntil,
  collectAllUntilEffect,
  collectAllWhile,
  collectAllWhileEffect,
  collectAllWhileWith,
  collectLeftover,
  concatAll,
  concatAllWith,
  concatMap,
  concatMapWith,
  concatMapWithCustom,
  concatOut,
  context as context2,
  context2 as context3,
  contextWith,
  contextWith2,
  contextWithChannel,
  contextWithEffect,
  contextWithEffect2,
  contextWithSink,
  count,
  die,
  dieMessage,
  dieSync,
  dimap,
  dimapChunks,
  dimapChunksEffect,
  dimapEffect,
  doneCollect,
  drain,
  drain2,
  drop as drop2,
  dropUntil,
  dropUntilEffect,
  dropWhile,
  dropWhileEffect,
  embedInput,
  emitCollect,
  ensuring as ensuring2,
  ensuring2 as ensuring3,
  ensuringWith,
  ensuringWith2,
  every,
  fail,
  fail2,
  failCause,
  failCause2,
  failCauseSync,
  failCauseSync2,
  failSync,
  failSync2,
  filterInput,
  filterInputEffect,
  findEffect,
  flatMap as flatMap3,
  flatMap2 as flatMap4,
  flatten as flatten2,
  fold,
  foldCauseChannel,
  foldChannel,
  foldChunks,
  foldChunksEffect,
  foldEffect,
  foldLeft,
  foldLeftChunks,
  foldLeftChunksEffect,
  foldLeftEffect,
  foldSink,
  foldUntil,
  foldUntilEffect,
  foldWeighted,
  foldWeightedDecompose,
  foldWeightedDecomposeEffect,
  foldWeightedEffect,
  forEach as forEach2,
  forEachChunk,
  forEachChunkWhile,
  forEachWhile,
  fromChannel,
  fromChannel2,
  fromEffect,
  fromEffect2,
  fromEither,
  fromInput,
  fromOption,
  fromPubSub,
  fromPubSub2,
  fromPubSubScoped,
  fromPush,
  fromQueue,
  fromQueue2,
  head as head2,
  identityChannel,
  ignoreLeftover,
  interruptWhen,
  interruptWhenDeferred,
  isChannel,
  isChannelException,
  isStream,
  last,
  leftover,
  map as map3,
  map2 as map4,
  mapEffect,
  mapEffect2,
  mapError,
  mapError2,
  mapErrorCause,
  mapInput,
  mapInput2,
  mapInputChunks,
  mapInputChunksEffect,
  mapInputContext as mapInputContext2,
  mapInputEffect,
  mapInputEffect2,
  mapInputError,
  mapInputErrorEffect,
  mapInputIn,
  mapInputInEffect,
  mapLeftover,
  mapOut,
  mapOutEffect,
  mapOutEffectPar,
  mergeAll,
  mergeAllUnbounded,
  mergeAllUnboundedWith,
  mergeAllWith,
  mergeMap,
  mergeOut,
  mergeOutWith,
  mergeWith,
  mkString,
  never,
  never2,
  orDie,
  orDieWith,
  orElse,
  orElse2,
  pipeTo,
  pipeToOrFail,
  provideContext,
  provideContext2,
  provideLayer,
  provideService,
  provideSomeLayer,
  race,
  raceBoth,
  raceWith,
  read,
  readOrFail,
  readWith,
  readWithCause,
  refineOrDie,
  refineOrDieWith,
  repeated,
  run,
  runCollect,
  runDrain,
  runFork as runFork2,
  runIn,
  runScoped,
  scoped,
  scopedWith,
  some as some2,
  splitLines,
  splitWhere,
  succeed as succeed2,
  succeed2 as succeed3,
  sum,
  summarized,
  suspend as suspend2,
  suspend2 as suspend3,
  sync as sync3,
  sync2 as sync4,
  take as take2,
  timed,
  toChannel,
  toChannel2,
  toPubSub,
  toPull,
  toPullIn,
  toQueue,
  unbounded,
  unwrap,
  unwrap2,
  unwrapScoped,
  unwrapScoped2,
  unwrapScopedWith,
  unwrapScopedWith2,
  updateService,
  void_ as void_2,
  withDuration,
  withSpan,
  write,
  writeAll,
  writeChunk,
  zip,
  zip2,
  zipLeft as zipLeft2,
  zipLeft2 as zipLeft3,
  zipRight as zipRight2,
  zipRight2 as zipRight3,
  zipWith
} from "./chunk-HVF2XBK2.js";
import {
  _void,
  acquireRelease as acquireRelease2,
  fiberIdWith,
  flatMap as flatMap2,
  forEach,
  forkDaemon as forkDaemon2,
  interrupt as interrupt2,
  intoDeferred,
  isEffect,
  map2,
  runFork,
  runtime,
  sync2,
  tap as tap2,
  whileLoop,
  withFiberRuntime as withFiberRuntime2
} from "./chunk-LXSJW2UJ.js";
import {
  Class,
  NoSuchElementException,
  _await,
  _await2,
  acquireRelease,
  clockWith,
  delay,
  ensuring,
  forever,
  forkDaemon,
  forkIn,
  interruptAllAs,
  interruptAs,
  isFailure2 as isFailure,
  isInterruptedOnly,
  isSuccess2 as isSuccess,
  make13 as make2,
  reduceWithContext2 as reduceWithContext,
  replicateEffect,
  scopeExtend,
  scopeMake,
  scopeTag,
  scopeWith,
  squash,
  succeedNone,
  unsafeDone,
  unsafeMakeLatch,
  unsafeMakeSemaphore
} from "./chunk-UYCMDTGT.js";
import {
  acquireUseRelease,
  as2 as as,
  asyncInterrupt,
  catchAllCause,
  combine,
  context,
  currentUnhandledErrorLogLevel,
  exit,
  exitAs,
  exitFail,
  exitFailCause,
  exitSucceed,
  exitVoid,
  exitZipRight,
  flatMap2 as flatMap,
  forEachSequentialDiscard,
  has,
  ids,
  interrupt2 as interrupt,
  interruptFiber,
  interruptible2 as interruptible,
  make4 as make,
  map2 as map,
  mapInputContext,
  matchCauseEffect,
  none2,
  onInterrupt,
  succeed,
  suspend,
  sync,
  tap,
  toMillis,
  uninterruptible,
  uninterruptibleMask,
  void_,
  withFiberRuntime,
  zipLeft,
  zipRight
} from "./chunk-7GS5HKXM.js";
import {
  appendAll2 as appendAll,
  drop2 as drop,
  empty,
  empty4 as empty2,
  filter,
  flatten,
  fromIterable,
  fromIterable2,
  getOrThrowWith,
  head,
  isChunk,
  isLeft,
  isRight,
  left,
  match,
  of2 as of,
  right,
  take,
  takeRight,
  unsafeFromArray,
  unsafeHead,
  unsafeHead2
} from "./chunk-JDLTRRA5.js";
import {
  get,
  merge
} from "./chunk-RG46YT3R.js";
import {
  getRight2 as getRight,
  match as match2,
  none,
  some
} from "./chunk-5XJTUU64.js";
import {
  NodeInspectSymbol,
  format,
  hasProperty,
  pipeArguments
} from "./chunk-SPGJQP32.js";
import {
  constFalse,
  constVoid,
  dual,
  identity,
  unsafeCoerce
} from "./chunk-LX4DLJUA.js";
import {
  __export
} from "./chunk-G3PMV62Z.js";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Mailbox.js
var Mailbox_exports = {};
__export(Mailbox_exports, {
  ReadonlyTypeId: () => ReadonlyTypeId2,
  TypeId: () => TypeId2,
  fromStream: () => fromStream2,
  into: () => into2,
  isMailbox: () => isMailbox,
  isReadonlyMailbox: () => isReadonlyMailbox,
  make: () => make4,
  toChannel: () => toChannel4,
  toStream: () => toStream2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/mailbox.js
var TypeId = Symbol.for("effect/Mailbox");
var ReadonlyTypeId = Symbol.for("effect/Mailbox/ReadonlyMailbox");
var empty3 = empty2();
var exitEmpty = exitSucceed(empty3);
var exitFalse = exitSucceed(false);
var exitTrue = exitSucceed(true);
var constDone = [empty3, true];
var MailboxImpl = class extends Class {
  scheduler;
  capacity;
  strategy;
  [TypeId] = TypeId;
  [ReadonlyTypeId] = ReadonlyTypeId;
  state = {
    _tag: "Open",
    takers: /* @__PURE__ */ new Set(),
    offers: /* @__PURE__ */ new Set(),
    awaiters: /* @__PURE__ */ new Set()
  };
  messages = [];
  messagesChunk = empty2();
  constructor(scheduler, capacity, strategy) {
    super();
    this.scheduler = scheduler;
    this.capacity = capacity;
    this.strategy = strategy;
  }
  offer(message) {
    return suspend(() => {
      if (this.state._tag !== "Open") {
        return exitFalse;
      } else if (this.messages.length + this.messagesChunk.length >= this.capacity) {
        switch (this.strategy) {
          case "dropping":
            return exitFalse;
          case "suspend":
            if (this.capacity <= 0 && this.state.takers.size > 0) {
              this.messages.push(message);
              this.releaseTaker();
              return exitTrue;
            }
            return this.offerRemainingSingle(message);
          case "sliding":
            this.unsafeTake();
            this.messages.push(message);
            return exitTrue;
        }
      }
      this.messages.push(message);
      this.scheduleReleaseTaker();
      return exitTrue;
    });
  }
  unsafeOffer(message) {
    if (this.state._tag !== "Open") {
      return false;
    } else if (this.messages.length + this.messagesChunk.length >= this.capacity) {
      if (this.strategy === "sliding") {
        this.unsafeTake();
        this.messages.push(message);
        return true;
      } else if (this.capacity <= 0 && this.state.takers.size > 0) {
        this.messages.push(message);
        this.releaseTaker();
        return true;
      }
      return false;
    }
    this.messages.push(message);
    this.scheduleReleaseTaker();
    return true;
  }
  offerAll(messages) {
    return suspend(() => {
      if (this.state._tag !== "Open") {
        return succeed(fromIterable2(messages));
      }
      const remaining = this.unsafeOfferAllArray(messages);
      if (remaining.length === 0) {
        return exitEmpty;
      } else if (this.strategy === "dropping") {
        return succeed(unsafeFromArray(remaining));
      }
      return this.offerRemainingArray(remaining);
    });
  }
  unsafeOfferAll(messages) {
    return unsafeFromArray(this.unsafeOfferAllArray(messages));
  }
  unsafeOfferAllArray(messages) {
    if (this.state._tag !== "Open") {
      return fromIterable(messages);
    } else if (this.capacity === Number.POSITIVE_INFINITY || this.strategy === "sliding") {
      if (this.messages.length > 0) {
        this.messagesChunk = appendAll(this.messagesChunk, unsafeFromArray(this.messages));
      }
      if (this.strategy === "sliding") {
        this.messagesChunk = this.messagesChunk.pipe(appendAll(fromIterable2(messages)), takeRight(this.capacity));
      } else if (isChunk(messages)) {
        this.messagesChunk = appendAll(this.messagesChunk, messages);
      } else {
        this.messages = fromIterable(messages);
      }
      this.scheduleReleaseTaker();
      return [];
    }
    const free = this.capacity <= 0 ? this.state.takers.size : this.capacity - this.messages.length - this.messagesChunk.length;
    if (free === 0) {
      return fromIterable(messages);
    }
    const remaining = [];
    let i = 0;
    for (const message of messages) {
      if (i < free) {
        this.messages.push(message);
      } else {
        remaining.push(message);
      }
      i++;
    }
    this.scheduleReleaseTaker();
    return remaining;
  }
  fail(error2) {
    return this.done(exitFail(error2));
  }
  failCause(cause) {
    return this.done(exitFailCause(cause));
  }
  unsafeDone(exit2) {
    if (this.state._tag !== "Open") {
      return false;
    } else if (this.state.offers.size === 0 && this.messages.length === 0 && this.messagesChunk.length === 0) {
      this.finalize(exit2);
      return true;
    }
    this.state = {
      ...this.state,
      _tag: "Closing",
      exit: exit2
    };
    return true;
  }
  shutdown = sync(() => {
    if (this.state._tag === "Done") {
      return true;
    }
    this.messages = [];
    this.messagesChunk = empty3;
    const offers = this.state.offers;
    this.finalize(this.state._tag === "Open" ? exitVoid : this.state.exit);
    if (offers.size > 0) {
      for (const entry of offers) {
        if (entry._tag === "Single") {
          entry.resume(exitFalse);
        } else {
          entry.resume(exitSucceed(unsafeFromArray(entry.remaining.slice(entry.offset))));
        }
      }
      offers.clear();
    }
    return true;
  });
  done(exit2) {
    return sync(() => this.unsafeDone(exit2));
  }
  end = this.done(exitVoid);
  clear = suspend(() => {
    if (this.state._tag === "Done") {
      return exitAs(this.state.exit, empty3);
    }
    const messages = this.unsafeTakeAll();
    this.releaseCapacity();
    return succeed(messages);
  });
  takeAll = suspend(() => {
    if (this.state._tag === "Done") {
      return exitAs(this.state.exit, constDone);
    }
    const messages = this.unsafeTakeAll();
    if (messages.length === 0) {
      return zipRight(this.awaitTake, this.takeAll);
    }
    return succeed([messages, this.releaseCapacity()]);
  });
  takeN(n) {
    return suspend(() => {
      if (this.state._tag === "Done") {
        return exitAs(this.state.exit, constDone);
      } else if (n <= 0) {
        return succeed([empty3, false]);
      }
      n = Math.min(n, this.capacity);
      let messages;
      if (n <= this.messagesChunk.length) {
        messages = take(this.messagesChunk, n);
        this.messagesChunk = drop(this.messagesChunk, n);
      } else if (n <= this.messages.length + this.messagesChunk.length) {
        this.messagesChunk = appendAll(this.messagesChunk, unsafeFromArray(this.messages));
        this.messages = [];
        messages = take(this.messagesChunk, n);
        this.messagesChunk = drop(this.messagesChunk, n);
      } else {
        return zipRight(this.awaitTake, this.takeN(n));
      }
      return succeed([messages, this.releaseCapacity()]);
    });
  }
  unsafeTake() {
    if (this.state._tag === "Done") {
      return exitZipRight(this.state.exit, exitFail(new NoSuchElementException()));
    }
    let message;
    if (this.messagesChunk.length > 0) {
      message = unsafeHead2(this.messagesChunk);
      this.messagesChunk = drop(this.messagesChunk, 1);
    } else if (this.messages.length > 0) {
      message = this.messages[0];
      this.messagesChunk = drop(unsafeFromArray(this.messages), 1);
      this.messages = [];
    } else if (this.capacity <= 0 && this.state.offers.size > 0) {
      this.capacity = 1;
      this.releaseCapacity();
      this.capacity = 0;
      return this.messages.length > 0 ? exitSucceed(this.messages.pop()) : void 0;
    } else {
      return void 0;
    }
    this.releaseCapacity();
    return exitSucceed(message);
  }
  take = suspend(() => this.unsafeTake() ?? zipRight(this.awaitTake, this.take));
  await = asyncInterrupt((resume) => {
    if (this.state._tag === "Done") {
      return resume(this.state.exit);
    }
    this.state.awaiters.add(resume);
    return sync(() => {
      if (this.state._tag !== "Done") {
        this.state.awaiters.delete(resume);
      }
    });
  });
  unsafeSize() {
    const size2 = this.messages.length + this.messagesChunk.length;
    return this.state._tag === "Done" ? none() : some(size2);
  }
  size = sync(() => this.unsafeSize());
  commit() {
    return this.takeAll;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  toJSON() {
    return {
      _id: "effect/Mailbox",
      state: this.state._tag,
      size: this.unsafeSize().toJSON()
    };
  }
  toString() {
    return format(this);
  }
  [NodeInspectSymbol]() {
    return format(this);
  }
  offerRemainingSingle(message) {
    return asyncInterrupt((resume) => {
      if (this.state._tag !== "Open") {
        return resume(exitFalse);
      }
      const entry = {
        _tag: "Single",
        message,
        resume
      };
      this.state.offers.add(entry);
      return sync(() => {
        if (this.state._tag === "Open") {
          this.state.offers.delete(entry);
        }
      });
    });
  }
  offerRemainingArray(remaining) {
    return asyncInterrupt((resume) => {
      if (this.state._tag !== "Open") {
        return resume(exitSucceed(unsafeFromArray(remaining)));
      }
      const entry = {
        _tag: "Array",
        remaining,
        offset: 0,
        resume
      };
      this.state.offers.add(entry);
      return sync(() => {
        if (this.state._tag === "Open") {
          this.state.offers.delete(entry);
        }
      });
    });
  }
  releaseCapacity() {
    if (this.state._tag === "Done") {
      return this.state.exit._tag === "Success";
    } else if (this.state.offers.size === 0) {
      if (this.state._tag === "Closing" && this.messages.length === 0 && this.messagesChunk.length === 0) {
        this.finalize(this.state.exit);
        return this.state.exit._tag === "Success";
      }
      return false;
    }
    let n = this.capacity - this.messages.length - this.messagesChunk.length;
    for (const entry of this.state.offers) {
      if (n === 0) return false;
      else if (entry._tag === "Single") {
        this.messages.push(entry.message);
        n--;
        entry.resume(exitTrue);
        this.state.offers.delete(entry);
      } else {
        for (; entry.offset < entry.remaining.length; entry.offset++) {
          if (n === 0) return false;
          this.messages.push(entry.remaining[entry.offset]);
          n--;
        }
        entry.resume(exitEmpty);
        this.state.offers.delete(entry);
      }
    }
    return false;
  }
  awaitTake = asyncInterrupt((resume) => {
    if (this.state._tag === "Done") {
      return resume(this.state.exit);
    }
    this.state.takers.add(resume);
    return sync(() => {
      if (this.state._tag !== "Done") {
        this.state.takers.delete(resume);
      }
    });
  });
  scheduleRunning = false;
  scheduleReleaseTaker() {
    if (this.scheduleRunning) {
      return;
    }
    this.scheduleRunning = true;
    this.scheduler.scheduleTask(this.releaseTaker, 0);
  }
  releaseTaker = () => {
    this.scheduleRunning = false;
    if (this.state._tag === "Done") {
      return;
    } else if (this.state.takers.size === 0) {
      return;
    }
    for (const taker of this.state.takers) {
      this.state.takers.delete(taker);
      taker(exitVoid);
      if (this.messages.length + this.messagesChunk.length === 0) {
        break;
      }
    }
  };
  unsafeTakeAll() {
    if (this.messagesChunk.length > 0) {
      const messages = this.messages.length > 0 ? appendAll(this.messagesChunk, unsafeFromArray(this.messages)) : this.messagesChunk;
      this.messagesChunk = empty3;
      this.messages = [];
      return messages;
    } else if (this.messages.length > 0) {
      const messages = unsafeFromArray(this.messages);
      this.messages = [];
      return messages;
    } else if (this.state._tag !== "Done" && this.state.offers.size > 0) {
      this.capacity = 1;
      this.releaseCapacity();
      this.capacity = 0;
      return of(this.messages.pop());
    }
    return empty3;
  }
  finalize(exit2) {
    if (this.state._tag === "Done") {
      return;
    }
    const openState = this.state;
    this.state = {
      _tag: "Done",
      exit: exit2
    };
    for (const taker of openState.takers) {
      taker(exit2);
    }
    openState.takers.clear();
    for (const awaiter of openState.awaiters) {
      awaiter(exit2);
    }
    openState.awaiters.clear();
  }
};
var make3 = (capacity) => withFiberRuntime((fiber) => succeed(new MailboxImpl(fiber.currentScheduler, typeof capacity === "number" ? capacity : capacity?.capacity ?? Number.POSITIVE_INFINITY, typeof capacity === "number" ? "suspend" : capacity?.strategy ?? "suspend")));
var into = dual(2, (effect, self) => uninterruptibleMask((restore) => matchCauseEffect(restore(effect), {
  onFailure: (cause) => self.failCause(cause),
  onSuccess: (_) => self.end
})));
var toChannel3 = (self) => {
  const loop = flatMap3(self.takeAll, ([messages, done]) => done ? messages.length === 0 ? void_2 : write(messages) : zipRight2(write(messages), loop));
  return loop;
};
var toStream = (self) => fromChannel2(toChannel3(self));
var fromStream = dual((args) => isStream(args[0]), (self, options) => tap(acquireRelease(make3(options), (mailbox) => mailbox.shutdown), (mailbox) => {
  const writer = readWithCause({
    onInput: (input) => flatMap3(mailbox.offerAll(input), () => writer),
    onFailure: (cause) => mailbox.failCause(cause),
    onDone: () => mailbox.end
  });
  return scopeWith((scope) => toChannel2(self).pipe(pipeTo(writer), runIn(scope), forkIn(scope)));
}));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Mailbox.js
var TypeId2 = TypeId;
var ReadonlyTypeId2 = ReadonlyTypeId;
var isMailbox = (u) => hasProperty(u, TypeId2);
var isReadonlyMailbox = (u) => hasProperty(u, ReadonlyTypeId2);
var make4 = make3;
var into2 = into;
var toChannel4 = toChannel3;
var toStream2 = toStream;
var fromStream2 = fromStream;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Brand.js
var Brand_exports = {};
__export(Brand_exports, {
  BrandTypeId: () => BrandTypeId,
  RefinedConstructorsTypeId: () => RefinedConstructorsTypeId,
  all: () => all,
  error: () => error,
  errors: () => errors,
  nominal: () => nominal,
  refined: () => refined,
  unbranded: () => unbranded
});
var BrandTypeId = Symbol.for("effect/Brand");
var RefinedConstructorsTypeId = Symbol.for("effect/Brand/Refined");
var error = (message, meta) => [{
  message,
  meta
}];
var errors = (...errors2) => flatten(errors2);
function refined(...args) {
  const either = args.length === 2 ? (unbranded2) => args[0](unbranded2) ? right(unbranded2) : left(args[1](unbranded2)) : (unbranded2) => {
    return match2(args[0](unbranded2), {
      onNone: () => right(unbranded2),
      onSome: left
    });
  };
  return Object.assign((unbranded2) => getOrThrowWith(either(unbranded2), identity), {
    [RefinedConstructorsTypeId]: RefinedConstructorsTypeId,
    option: (args2) => getRight(either(args2)),
    either,
    is: (args2) => isRight(either(args2))
  });
}
var nominal = () => {
  return Object.assign((args) => args, {
    [RefinedConstructorsTypeId]: RefinedConstructorsTypeId,
    option: (args) => some(args),
    either: (args) => right(args),
    is: (_args) => true
  });
};
var all = (...brands) => {
  const either = (args) => {
    let result = right(args);
    for (const brand of brands) {
      const nextResult = brand.either(args);
      if (isLeft(result) && isLeft(nextResult)) {
        result = left([...result.left, ...nextResult.left]);
      } else {
        result = isLeft(result) ? result : nextResult;
      }
    }
    return result;
  };
  return Object.assign((args) => match(either(args), {
    onLeft: (e) => {
      throw e;
    },
    onRight: identity
  }), {
    [RefinedConstructorsTypeId]: RefinedConstructorsTypeId,
    option: (args) => getRight(either(args)),
    either,
    is: (args) => isRight(either(args))
  });
};
var unbranded = unsafeCoerce;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Channel.js
var Channel_exports = {};
__export(Channel_exports, {
  ChannelException: () => ChannelException2,
  ChannelExceptionTypeId: () => ChannelExceptionTypeId2,
  ChannelTypeId: () => ChannelTypeId2,
  acquireReleaseOut: () => acquireReleaseOut2,
  acquireUseRelease: () => acquireUseRelease3,
  as: () => as4,
  asVoid: () => asVoid2,
  buffer: () => buffer2,
  bufferChunk: () => bufferChunk2,
  catchAll: () => catchAll2,
  catchAllCause: () => catchAllCause3,
  collect: () => collect2,
  concatAll: () => concatAll2,
  concatAllWith: () => concatAllWith2,
  concatMap: () => concatMap2,
  concatMapWith: () => concatMapWith2,
  concatMapWithCustom: () => concatMapWithCustom2,
  concatOut: () => concatOut2,
  context: () => context4,
  contextWith: () => contextWith3,
  contextWithChannel: () => contextWithChannel2,
  contextWithEffect: () => contextWithEffect3,
  doneCollect: () => doneCollect2,
  drain: () => drain3,
  embedInput: () => embedInput2,
  emitCollect: () => emitCollect2,
  ensuring: () => ensuring4,
  ensuringWith: () => ensuringWith3,
  fail: () => fail3,
  failCause: () => failCause3,
  failCauseSync: () => failCauseSync3,
  failSync: () => failSync3,
  flatMap: () => flatMap5,
  flatten: () => flatten3,
  foldCauseChannel: () => foldCauseChannel2,
  foldChannel: () => foldChannel2,
  fromEffect: () => fromEffect3,
  fromEither: () => fromEither2,
  fromInput: () => fromInput2,
  fromOption: () => fromOption2,
  fromPubSub: () => fromPubSub3,
  fromPubSubScoped: () => fromPubSubScoped2,
  fromQueue: () => fromQueue3,
  identity: () => identity2,
  interruptWhen: () => interruptWhen2,
  interruptWhenDeferred: () => interruptWhenDeferred2,
  isChannel: () => isChannel2,
  isChannelException: () => isChannelException2,
  map: () => map5,
  mapEffect: () => mapEffect3,
  mapError: () => mapError3,
  mapErrorCause: () => mapErrorCause2,
  mapInput: () => mapInput3,
  mapInputContext: () => mapInputContext3,
  mapInputEffect: () => mapInputEffect3,
  mapInputError: () => mapInputError2,
  mapInputErrorEffect: () => mapInputErrorEffect2,
  mapInputIn: () => mapInputIn2,
  mapInputInEffect: () => mapInputInEffect2,
  mapOut: () => mapOut2,
  mapOutEffect: () => mapOutEffect2,
  mapOutEffectPar: () => mapOutEffectPar2,
  mergeAll: () => mergeAll2,
  mergeAllUnbounded: () => mergeAllUnbounded2,
  mergeAllUnboundedWith: () => mergeAllUnboundedWith2,
  mergeAllWith: () => mergeAllWith2,
  mergeMap: () => mergeMap2,
  mergeOut: () => mergeOut2,
  mergeOutWith: () => mergeOutWith2,
  mergeWith: () => mergeWith2,
  never: () => never3,
  orDie: () => orDie2,
  orDieWith: () => orDieWith2,
  orElse: () => orElse3,
  pipeTo: () => pipeTo2,
  pipeToOrFail: () => pipeToOrFail2,
  provideContext: () => provideContext3,
  provideLayer: () => provideLayer2,
  provideService: () => provideService2,
  provideSomeLayer: () => provideSomeLayer2,
  read: () => read2,
  readOrFail: () => readOrFail2,
  readWith: () => readWith2,
  readWithCause: () => readWithCause2,
  repeated: () => repeated2,
  run: () => run2,
  runCollect: () => runCollect2,
  runDrain: () => runDrain2,
  runScoped: () => runScoped2,
  scoped: () => scoped2,
  scopedWith: () => scopedWith2,
  splitLines: () => splitLines2,
  succeed: () => succeed4,
  suspend: () => suspend4,
  sync: () => sync5,
  toPubSub: () => toPubSub2,
  toPull: () => toPull2,
  toPullIn: () => toPullIn2,
  toQueue: () => toQueue2,
  toSink: () => toSink,
  toStream: () => toStream3,
  unwrap: () => unwrap3,
  unwrapScoped: () => unwrapScoped3,
  unwrapScopedWith: () => unwrapScopedWith3,
  updateService: () => updateService2,
  void: () => void_3,
  withSpan: () => withSpan2,
  write: () => write2,
  writeAll: () => writeAll2,
  writeChunk: () => writeChunk2,
  zip: () => zip3,
  zipLeft: () => zipLeft4,
  zipRight: () => zipRight4
});
var ChannelTypeId2 = ChannelTypeId;
var ChannelExceptionTypeId2 = ChannelExceptionTypeId;
var isChannel2 = isChannel;
var acquireUseRelease3 = acquireUseRelease2;
var acquireReleaseOut2 = acquireReleaseOut;
var as4 = as2;
var asVoid2 = asVoid;
var buffer2 = buffer;
var bufferChunk2 = bufferChunk;
var catchAll2 = catchAll;
var catchAllCause3 = catchAllCause2;
var concatAll2 = concatAll;
var concatAllWith2 = concatAllWith;
var concatMap2 = concatMap;
var concatMapWith2 = concatMapWith;
var concatMapWithCustom2 = concatMapWithCustom;
var collect2 = collect;
var concatOut2 = concatOut;
var mapInput3 = mapInput;
var mapInputEffect3 = mapInputEffect;
var mapInputError2 = mapInputError;
var mapInputErrorEffect2 = mapInputErrorEffect;
var mapInputIn2 = mapInputIn;
var mapInputInEffect2 = mapInputInEffect;
var doneCollect2 = doneCollect;
var drain3 = drain;
var embedInput2 = embedInput;
var emitCollect2 = emitCollect;
var ensuring4 = ensuring2;
var ensuringWith3 = ensuringWith;
var context4 = context2;
var contextWith3 = contextWith;
var contextWithChannel2 = contextWithChannel;
var contextWithEffect3 = contextWithEffect;
var fail3 = fail;
var failSync3 = failSync;
var failCause3 = failCause;
var failCauseSync3 = failCauseSync;
var flatMap5 = flatMap3;
var flatten3 = flatten2;
var foldChannel2 = foldChannel;
var foldCauseChannel2 = foldCauseChannel;
var fromEffect3 = fromEffect;
var fromEither2 = fromEither;
var fromInput2 = fromInput;
var fromPubSub3 = fromPubSub;
var fromPubSubScoped2 = fromPubSubScoped;
var fromOption2 = fromOption;
var fromQueue3 = fromQueue;
var identity2 = identityChannel;
var interruptWhen2 = interruptWhen;
var interruptWhenDeferred2 = interruptWhenDeferred;
var map5 = map3;
var mapEffect3 = mapEffect;
var mapError3 = mapError;
var mapErrorCause2 = mapErrorCause;
var mapOut2 = mapOut;
var mapOutEffect2 = mapOutEffect;
var mapOutEffectPar2 = mapOutEffectPar;
var mergeAll2 = mergeAll;
var mergeAllUnbounded2 = mergeAllUnbounded;
var mergeAllUnboundedWith2 = mergeAllUnboundedWith;
var mergeAllWith2 = mergeAllWith;
var mergeMap2 = mergeMap;
var mergeOut2 = mergeOut;
var mergeOutWith2 = mergeOutWith;
var mergeWith2 = mergeWith;
var never3 = never;
var orDie2 = orDie;
var orDieWith2 = orDieWith;
var orElse3 = orElse;
var pipeTo2 = pipeTo;
var pipeToOrFail2 = pipeToOrFail;
var provideContext3 = provideContext;
var provideLayer2 = provideLayer;
var mapInputContext3 = mapInputContext2;
var provideSomeLayer2 = provideSomeLayer;
var provideService2 = provideService;
var read2 = read;
var readOrFail2 = readOrFail;
var readWith2 = readWith;
var readWithCause2 = readWithCause;
var repeated2 = repeated;
var run2 = run;
var runCollect2 = runCollect;
var runDrain2 = runDrain;
var runScoped2 = runScoped;
var scoped2 = scoped;
var scopedWith2 = scopedWith;
var splitLines2 = splitLines;
var succeed4 = succeed2;
var suspend4 = suspend2;
var sync5 = sync3;
var toPubSub2 = toPubSub;
var toPull2 = toPull;
var toPullIn2 = toPullIn;
var toQueue2 = toQueue;
var toSink = channelToSink;
var toStream3 = channelToStream;
var void_3 = void_2;
var unwrap3 = unwrap;
var unwrapScoped3 = unwrapScoped;
var unwrapScopedWith3 = unwrapScopedWith;
var updateService2 = updateService;
var withSpan2 = withSpan;
var write2 = write;
var writeAll2 = writeAll;
var writeChunk2 = writeChunk;
var zip3 = zip;
var zipLeft4 = zipLeft2;
var zipRight4 = zipRight2;
var ChannelException2 = ChannelException;
var isChannelException2 = isChannelException;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Sink.js
var Sink_exports = {};
__export(Sink_exports, {
  SinkTypeId: () => SinkTypeId2,
  as: () => as5,
  collectAll: () => collectAll2,
  collectAllFrom: () => collectAllFrom2,
  collectAllN: () => collectAllN2,
  collectAllToMap: () => collectAllToMap2,
  collectAllToMapN: () => collectAllToMapN2,
  collectAllToSet: () => collectAllToSet2,
  collectAllToSetN: () => collectAllToSetN2,
  collectAllUntil: () => collectAllUntil2,
  collectAllUntilEffect: () => collectAllUntilEffect2,
  collectAllWhile: () => collectAllWhile2,
  collectAllWhileEffect: () => collectAllWhileEffect2,
  collectAllWhileWith: () => collectAllWhileWith2,
  collectLeftover: () => collectLeftover2,
  context: () => context5,
  contextWith: () => contextWith4,
  contextWithEffect: () => contextWithEffect4,
  contextWithSink: () => contextWithSink2,
  count: () => count2,
  die: () => die2,
  dieMessage: () => dieMessage2,
  dieSync: () => dieSync2,
  dimap: () => dimap2,
  dimapChunks: () => dimapChunks2,
  dimapChunksEffect: () => dimapChunksEffect2,
  dimapEffect: () => dimapEffect2,
  drain: () => drain4,
  drop: () => drop3,
  dropUntil: () => dropUntil2,
  dropUntilEffect: () => dropUntilEffect2,
  dropWhile: () => dropWhile2,
  dropWhileEffect: () => dropWhileEffect2,
  ensuring: () => ensuring5,
  ensuringWith: () => ensuringWith4,
  every: () => every2,
  fail: () => fail4,
  failCause: () => failCause4,
  failCauseSync: () => failCauseSync4,
  failSync: () => failSync4,
  filterInput: () => filterInput2,
  filterInputEffect: () => filterInputEffect2,
  findEffect: () => findEffect2,
  flatMap: () => flatMap6,
  fold: () => fold2,
  foldChunks: () => foldChunks2,
  foldChunksEffect: () => foldChunksEffect2,
  foldEffect: () => foldEffect2,
  foldLeft: () => foldLeft2,
  foldLeftChunks: () => foldLeftChunks2,
  foldLeftChunksEffect: () => foldLeftChunksEffect2,
  foldLeftEffect: () => foldLeftEffect2,
  foldSink: () => foldSink2,
  foldUntil: () => foldUntil2,
  foldUntilEffect: () => foldUntilEffect2,
  foldWeighted: () => foldWeighted2,
  foldWeightedDecompose: () => foldWeightedDecompose2,
  foldWeightedDecomposeEffect: () => foldWeightedDecomposeEffect2,
  foldWeightedEffect: () => foldWeightedEffect2,
  forEach: () => forEach3,
  forEachChunk: () => forEachChunk2,
  forEachChunkWhile: () => forEachChunkWhile2,
  forEachWhile: () => forEachWhile2,
  fromChannel: () => fromChannel3,
  fromEffect: () => fromEffect4,
  fromPubSub: () => fromPubSub4,
  fromPush: () => fromPush2,
  fromQueue: () => fromQueue4,
  head: () => head3,
  ignoreLeftover: () => ignoreLeftover2,
  last: () => last2,
  leftover: () => leftover2,
  map: () => map6,
  mapEffect: () => mapEffect4,
  mapError: () => mapError4,
  mapInput: () => mapInput4,
  mapInputChunks: () => mapInputChunks2,
  mapInputChunksEffect: () => mapInputChunksEffect2,
  mapInputEffect: () => mapInputEffect4,
  mapLeftover: () => mapLeftover2,
  mkString: () => mkString2,
  never: () => never4,
  orElse: () => orElse4,
  provideContext: () => provideContext4,
  race: () => race2,
  raceBoth: () => raceBoth2,
  raceWith: () => raceWith2,
  refineOrDie: () => refineOrDie2,
  refineOrDieWith: () => refineOrDieWith2,
  some: () => some3,
  splitWhere: () => splitWhere2,
  succeed: () => succeed5,
  sum: () => sum2,
  summarized: () => summarized2,
  suspend: () => suspend5,
  sync: () => sync6,
  take: () => take3,
  timed: () => timed2,
  toChannel: () => toChannel5,
  unwrap: () => unwrap4,
  unwrapScoped: () => unwrapScoped4,
  unwrapScopedWith: () => unwrapScopedWith4,
  withDuration: () => withDuration2,
  zip: () => zip4,
  zipLeft: () => zipLeft5,
  zipRight: () => zipRight5,
  zipWith: () => zipWith2
});
var SinkTypeId2 = SinkTypeId;
var as5 = as3;
var collectAll2 = collectAll;
var collectAllN2 = collectAllN;
var collectAllFrom2 = collectAllFrom;
var collectAllToMap2 = collectAllToMap;
var collectAllToMapN2 = collectAllToMapN;
var collectAllToSet2 = collectAllToSet;
var collectAllToSetN2 = collectAllToSetN;
var collectAllUntil2 = collectAllUntil;
var collectAllUntilEffect2 = collectAllUntilEffect;
var collectAllWhile2 = collectAllWhile;
var collectAllWhileEffect2 = collectAllWhileEffect;
var collectAllWhileWith2 = collectAllWhileWith;
var collectLeftover2 = collectLeftover;
var mapInput4 = mapInput2;
var mapInputEffect4 = mapInputEffect2;
var mapInputChunks2 = mapInputChunks;
var mapInputChunksEffect2 = mapInputChunksEffect;
var count2 = count;
var die2 = die;
var dieMessage2 = dieMessage;
var dieSync2 = dieSync;
var dimap2 = dimap;
var dimapEffect2 = dimapEffect;
var dimapChunks2 = dimapChunks;
var dimapChunksEffect2 = dimapChunksEffect;
var drain4 = drain2;
var drop3 = drop2;
var dropUntil2 = dropUntil;
var dropUntilEffect2 = dropUntilEffect;
var dropWhile2 = dropWhile;
var dropWhileEffect2 = dropWhileEffect;
var ensuring5 = ensuring3;
var ensuringWith4 = ensuringWith2;
var context5 = context3;
var contextWith4 = contextWith2;
var contextWithEffect4 = contextWithEffect2;
var contextWithSink2 = contextWithSink;
var every2 = every;
var fail4 = fail2;
var failSync4 = failSync2;
var failCause4 = failCause2;
var failCauseSync4 = failCauseSync2;
var filterInput2 = filterInput;
var filterInputEffect2 = filterInputEffect;
var findEffect2 = findEffect;
var fold2 = fold;
var foldSink2 = foldSink;
var foldChunks2 = foldChunks;
var foldChunksEffect2 = foldChunksEffect;
var foldEffect2 = foldEffect;
var foldLeft2 = foldLeft;
var foldLeftChunks2 = foldLeftChunks;
var foldLeftChunksEffect2 = foldLeftChunksEffect;
var foldLeftEffect2 = foldLeftEffect;
var foldUntil2 = foldUntil;
var foldUntilEffect2 = foldUntilEffect;
var foldWeighted2 = foldWeighted;
var foldWeightedDecompose2 = foldWeightedDecompose;
var foldWeightedDecomposeEffect2 = foldWeightedDecomposeEffect;
var foldWeightedEffect2 = foldWeightedEffect;
var forEach3 = forEach2;
var forEachChunk2 = forEachChunk;
var forEachChunkWhile2 = forEachChunkWhile;
var forEachWhile2 = forEachWhile;
var flatMap6 = flatMap4;
var fromChannel3 = fromChannel;
var toChannel5 = toChannel;
var fromEffect4 = fromEffect2;
var fromPubSub4 = fromPubSub2;
var fromPush2 = fromPush;
var fromQueue4 = fromQueue2;
var head3 = head2;
var ignoreLeftover2 = ignoreLeftover;
var last2 = last;
var leftover2 = leftover;
var map6 = map4;
var mapEffect4 = mapEffect2;
var mapError4 = mapError2;
var mapLeftover2 = mapLeftover;
var mkString2 = mkString;
var never4 = never2;
var orElse4 = orElse2;
var provideContext4 = provideContext2;
var race2 = race;
var raceBoth2 = raceBoth;
var raceWith2 = raceWith;
var refineOrDie2 = refineOrDie;
var refineOrDieWith2 = refineOrDieWith;
var some3 = some2;
var splitWhere2 = splitWhere;
var succeed5 = succeed3;
var sum2 = sum;
var summarized2 = summarized;
var suspend5 = suspend3;
var sync6 = sync4;
var take3 = take2;
var timed2 = timed;
var unwrap4 = unwrap2;
var unwrapScoped4 = unwrapScoped2;
var unwrapScopedWith4 = unwrapScopedWith2;
var withDuration2 = withDuration;
var zip4 = zip2;
var zipLeft5 = zipLeft3;
var zipRight5 = zipRight3;
var zipWith2 = zipWith;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberSet.js
var FiberSet_exports = {};
__export(FiberSet_exports, {
  TypeId: () => TypeId3,
  add: () => add,
  awaitEmpty: () => awaitEmpty,
  clear: () => clear,
  isFiberSet: () => isFiberSet,
  join: () => join,
  make: () => make5,
  makeRuntime: () => makeRuntime,
  makeRuntimePromise: () => makeRuntimePromise,
  run: () => run3,
  runtime: () => runtime2,
  runtimePromise: () => runtimePromise,
  size: () => size,
  unsafeAdd: () => unsafeAdd
});
var TypeId3 = Symbol.for("effect/FiberSet");
var isFiberSet = (u) => hasProperty(u, TypeId3);
var Proto = {
  [TypeId3]: TypeId3,
  [Symbol.iterator]() {
    if (this.state._tag === "Closed") {
      return empty();
    }
    return this.state.backing[Symbol.iterator]();
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "FiberMap",
      state: this.state
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var unsafeMake = (backing, deferred) => {
  const self = Object.create(Proto);
  self.state = {
    _tag: "Open",
    backing
  };
  self.deferred = deferred;
  return self;
};
var make5 = () => acquireRelease2(map2(make2(), (deferred) => unsafeMake(/* @__PURE__ */ new Set(), deferred)), (set) => withFiberRuntime2((parent) => {
  const state = set.state;
  if (state._tag === "Closed") return _void;
  set.state = {
    _tag: "Closed"
  };
  const fibers = state.backing;
  return interruptAllAs(fibers, combine(parent.id(), internalFiberId)).pipe(intoDeferred(set.deferred));
}));
var makeRuntime = () => flatMap2(make5(), (self) => runtime2(self)());
var makeRuntimePromise = () => flatMap2(make5(), (self) => runtimePromise(self)());
var internalFiberIdId = -1;
var internalFiberId = make(internalFiberIdId, 0);
var isInternalInterruption = reduceWithContext(void 0, {
  emptyCase: constFalse,
  failCase: constFalse,
  dieCase: constFalse,
  interruptCase: (_, fiberId) => has(ids(fiberId), internalFiberIdId),
  sequentialCase: (_, left2, right2) => left2 || right2,
  parallelCase: (_, left2, right2) => left2 || right2
});
var unsafeAdd = dual((args) => isFiberSet(args[0]), (self, fiber, options) => {
  if (self.state._tag === "Closed") {
    fiber.unsafeInterruptAsFork(combine(options?.interruptAs ?? none2, internalFiberId));
    return;
  } else if (self.state.backing.has(fiber)) {
    return;
  }
  self.state.backing.add(fiber);
  fiber.addObserver((exit2) => {
    if (self.state._tag === "Closed") {
      return;
    }
    self.state.backing.delete(fiber);
    if (isFailure(exit2) && (options?.propagateInterruption === true ? !isInternalInterruption(exit2.cause) : !isInterruptedOnly(exit2.cause))) {
      unsafeDone(self.deferred, exit2);
    }
  });
});
var add = dual((args) => isFiberSet(args[0]), (self, fiber, options) => fiberIdWith((fiberId) => sync2(() => unsafeAdd(self, fiber, {
  ...options,
  interruptAs: fiberId
}))));
var clear = (self) => withFiberRuntime2((clearFiber) => {
  if (self.state._tag === "Closed") {
    return _void;
  }
  return forEach(self.state.backing, (fiber) => (
    // will be removed by the observer
    interruptAs(fiber, combine(clearFiber.id(), internalFiberId))
  ));
});
var constInterruptedFiber = /* @__PURE__ */ (function() {
  let fiber = void 0;
  return () => {
    if (fiber === void 0) {
      fiber = runFork(interrupt2);
    }
    return fiber;
  };
})();
var run3 = function() {
  const self = arguments[0];
  if (!isEffect(arguments[1])) {
    const options = arguments[1];
    return (effect) => runImpl(self, effect, options);
  }
  return runImpl(self, arguments[1], arguments[2]);
};
var runImpl = (self, effect, options) => fiberIdWith((fiberId) => {
  if (self.state._tag === "Closed") {
    return sync2(constInterruptedFiber);
  }
  return tap2(forkDaemon2(effect), (fiber) => unsafeAdd(self, fiber, {
    ...options,
    interruptAs: fiberId
  }));
});
var runtime2 = (self) => () => map2(runtime(), (runtime3) => {
  const runFork3 = runFork2(runtime3);
  return (effect, options) => {
    if (self.state._tag === "Closed") {
      return constInterruptedFiber();
    }
    const fiber = runFork3(effect, options);
    unsafeAdd(self, fiber);
    return fiber;
  };
});
var runtimePromise = (self) => () => map2(runtime2(self)(), (runFork3) => (effect, options) => new Promise((resolve, reject) => runFork3(effect, options).addObserver((exit2) => {
  if (isSuccess(exit2)) {
    resolve(exit2.value);
  } else {
    reject(squash(exit2.cause));
  }
})));
var size = (self) => sync2(() => self.state._tag === "Closed" ? 0 : self.state.backing.size);
var join = (self) => _await2(self.deferred);
var awaitEmpty = (self) => whileLoop({
  while: () => self.state._tag === "Open" && self.state.backing.size > 0,
  body: () => _await(unsafeHead(self)),
  step: constVoid
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Pool.js
var Pool_exports = {};
__export(Pool_exports, {
  PoolTypeId: () => PoolTypeId2,
  get: () => get3,
  invalidate: () => invalidate2,
  isPool: () => isPool2,
  make: () => make7,
  makeWithTTL: () => makeWithTTL2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/pool.js
var PoolTypeId = Symbol.for("effect/Pool");
var poolVariance = {
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _A: (_) => _
};
var isPool = (u) => hasProperty(u, PoolTypeId);
var makeWith = (options) => uninterruptibleMask((restore) => flatMap(context(), (context6) => {
  const scope = get(context6, scopeTag);
  const acquire = mapInputContext(options.acquire, (input) => merge(context6, input));
  const pool = new PoolImpl(scope, acquire, options.concurrency ?? 1, options.min, options.max, options.strategy, Math.min(Math.max(options.targetUtilization ?? 1, 0.1), 1));
  const initialize = tap(forkDaemon(restore(pool.resize)), (fiber) => scope.addFinalizer(() => interruptFiber(fiber)));
  const runStrategy = tap(forkDaemon(restore(options.strategy.run(pool))), (fiber) => scope.addFinalizer(() => interruptFiber(fiber)));
  return succeed(pool).pipe(zipLeft(scope.addFinalizer(() => pool.shutdown)), zipLeft(initialize), zipLeft(runStrategy));
}));
var make6 = (options) => makeWith({
  ...options,
  min: options.size,
  max: options.size,
  strategy: strategyNoop()
});
var makeWithTTL = (options) => flatMap(options.timeToLiveStrategy === "creation" ? strategyCreationTTL(options.timeToLive) : strategyUsageTTL(options.timeToLive), (strategy) => makeWith({
  ...options,
  strategy
}));
var get2 = (self) => self.get;
var invalidate = dual(2, (self, item) => self.invalidate(item));
var PoolImpl = class extends Class {
  scope;
  acquire;
  concurrency;
  minSize;
  maxSize;
  strategy;
  targetUtilization;
  [PoolTypeId];
  isShuttingDown = false;
  semaphore;
  items = /* @__PURE__ */ new Set();
  available = /* @__PURE__ */ new Set();
  availableLatch = unsafeMakeLatch(false);
  invalidated = /* @__PURE__ */ new Set();
  waiters = 0;
  constructor(scope, acquire, concurrency, minSize, maxSize, strategy, targetUtilization) {
    super();
    this.scope = scope;
    this.acquire = acquire;
    this.concurrency = concurrency;
    this.minSize = minSize;
    this.maxSize = maxSize;
    this.strategy = strategy;
    this.targetUtilization = targetUtilization;
    this[PoolTypeId] = poolVariance;
    this.semaphore = unsafeMakeSemaphore(concurrency * maxSize);
  }
  allocate = acquireUseRelease(scopeMake(), (scope) => this.acquire.pipe(scopeExtend(scope), exit, flatMap((exit2) => {
    const item = {
      exit: exit2,
      finalizer: catchAllCause(scope.close(exit2), reportUnhandledError),
      refCount: 0,
      disableReclaim: false
    };
    this.items.add(item);
    this.available.add(item);
    return as(exit2._tag === "Success" ? this.strategy.onAcquire(item) : zipRight(item.finalizer, this.strategy.onAcquire(item)), item);
  })), (scope, exit2) => exit2._tag === "Failure" ? scope.close(exit2) : void_);
  get currentUsage() {
    let count3 = this.waiters;
    for (const item of this.items) {
      count3 += item.refCount;
    }
    return count3;
  }
  get targetSize() {
    if (this.isShuttingDown) return 0;
    const utilization = this.currentUsage / this.targetUtilization;
    const target = Math.ceil(utilization / this.concurrency);
    return Math.min(Math.max(this.minSize, target), this.maxSize);
  }
  get activeSize() {
    return this.items.size - this.invalidated.size;
  }
  resizeLoop = suspend(() => {
    if (this.activeSize >= this.targetSize) {
      return void_;
    }
    const toAcquire = this.targetSize - this.activeSize;
    return this.strategy.reclaim(this).pipe(flatMap(match2({
      onNone: () => this.allocate,
      onSome: succeed
    })), replicateEffect(toAcquire, {
      concurrency: toAcquire
    }), zipLeft(this.availableLatch.open), flatMap((items) => items.some((_) => _.exit._tag === "Failure") ? void_ : this.resizeLoop));
  });
  resizeSemaphore = unsafeMakeSemaphore(1);
  resize = this.resizeSemaphore.withPermits(1)(this.resizeLoop);
  getPoolItem = uninterruptibleMask((restore) => restore(this.semaphore.take(1)).pipe(zipRight(scopeTag), flatMap((scope) => suspend(() => {
    this.waiters++;
    if (this.isShuttingDown) {
      return interrupt;
    } else if (this.targetSize > this.activeSize) {
      const self = this;
      return flatMap(this.resizeSemaphore.withPermitsIfAvailable(1)(forkIn(interruptible(this.resize), this.scope)), function loop() {
        if (self.isShuttingDown) {
          return interrupt;
        } else if (self.available.size > 0) {
          return succeed(unsafeHead(self.available));
        }
        self.availableLatch.unsafeClose();
        return flatMap(self.availableLatch.await, loop);
      });
    }
    return succeed(unsafeHead(this.available));
  }).pipe(ensuring(sync(() => this.waiters--)), tap((item) => {
    if (item.exit._tag === "Failure") {
      this.items.delete(item);
      this.invalidated.delete(item);
      this.available.delete(item);
      return this.semaphore.release(1);
    }
    item.refCount++;
    this.available.delete(item);
    if (item.refCount < this.concurrency) {
      this.available.add(item);
    }
    return scope.addFinalizer(() => zipRight(suspend(() => {
      item.refCount--;
      if (this.invalidated.has(item)) {
        return this.invalidatePoolItem(item);
      }
      this.available.add(item);
      return exitVoid;
    }), this.semaphore.release(1)));
  }), onInterrupt(() => this.semaphore.release(1))))));
  commit() {
    return this.get;
  }
  get = flatMap(suspend(() => this.isShuttingDown ? interrupt : this.getPoolItem), (_) => _.exit);
  invalidate(item) {
    return suspend(() => {
      if (this.isShuttingDown) return void_;
      for (const poolItem of this.items) {
        if (poolItem.exit._tag === "Success" && poolItem.exit.value === item) {
          poolItem.disableReclaim = true;
          return uninterruptible(this.invalidatePoolItem(poolItem));
        }
      }
      return void_;
    });
  }
  invalidatePoolItem(poolItem) {
    return suspend(() => {
      if (!this.items.has(poolItem)) {
        return void_;
      } else if (poolItem.refCount === 0) {
        this.items.delete(poolItem);
        this.available.delete(poolItem);
        this.invalidated.delete(poolItem);
        return zipRight(poolItem.finalizer, forkIn(interruptible(this.resize), this.scope));
      }
      this.invalidated.add(poolItem);
      this.available.delete(poolItem);
      return void_;
    });
  }
  get shutdown() {
    return suspend(() => {
      if (this.isShuttingDown) return void_;
      this.isShuttingDown = true;
      const size2 = this.items.size;
      const semaphore = unsafeMakeSemaphore(size2);
      return forEachSequentialDiscard(this.items, (item) => {
        if (item.refCount > 0) {
          item.finalizer = zipLeft(item.finalizer, semaphore.release(1));
          this.invalidated.add(item);
          return semaphore.take(1);
        }
        this.items.delete(item);
        this.available.delete(item);
        this.invalidated.delete(item);
        return item.finalizer;
      }).pipe(zipRight(this.semaphore.releaseAll), zipRight(this.availableLatch.open), zipRight(semaphore.take(size2)));
    });
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var strategyNoop = () => ({
  run: (_) => void_,
  onAcquire: (_) => void_,
  reclaim: (_) => succeedNone
});
var strategyCreationTTL = (ttl) => clockWith((clock) => map(unbounded(), (queue) => {
  const ttlMillis = toMillis(ttl);
  const creationTimes = /* @__PURE__ */ new WeakMap();
  return identity({
    run: (pool) => {
      const process = (item) => suspend(() => {
        if (!pool.items.has(item) || pool.invalidated.has(item)) {
          return void_;
        }
        const now = clock.unsafeCurrentTimeMillis();
        const created = creationTimes.get(item);
        const remaining = ttlMillis - (now - created);
        return remaining > 0 ? delay(process(item), remaining) : pool.invalidatePoolItem(item);
      });
      return queue.take.pipe(tap(process), forever);
    },
    onAcquire: (item) => suspend(() => {
      creationTimes.set(item, clock.unsafeCurrentTimeMillis());
      return queue.offer(item);
    }),
    reclaim: (_) => succeedNone
  });
}));
var strategyUsageTTL = (ttl) => map(unbounded(), (queue) => {
  return identity({
    run: (pool) => {
      const process = suspend(() => {
        const excess = pool.activeSize - pool.targetSize;
        if (excess <= 0) return void_;
        return queue.take.pipe(tap((item) => pool.invalidatePoolItem(item)), zipRight(process));
      });
      return process.pipe(delay(ttl), forever);
    },
    onAcquire: (item) => queue.offer(item),
    reclaim(pool) {
      return suspend(() => {
        if (pool.invalidated.size === 0) {
          return succeedNone;
        }
        const item = head(filter(pool.invalidated, (item2) => !item2.disableReclaim));
        if (item._tag === "None") {
          return succeedNone;
        }
        pool.invalidated.delete(item.value);
        if (item.value.refCount < pool.concurrency) {
          pool.available.add(item.value);
        }
        return as(queue.offer(item.value), item);
      });
    }
  });
});
var reportUnhandledError = (cause) => withFiberRuntime((fiber) => {
  const unhandledLogLevel = fiber.getFiberRef(currentUnhandledErrorLogLevel);
  if (unhandledLogLevel._tag === "Some") {
    fiber.log("Unhandled error in pool finalizer", cause, unhandledLogLevel);
  }
  return void_;
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Pool.js
var PoolTypeId2 = PoolTypeId;
var isPool2 = isPool;
var make7 = make6;
var makeWithTTL2 = makeWithTTL;
var get3 = get2;
var invalidate2 = invalidate;

export {
  isMailbox,
  make4 as make,
  into2 as into,
  toChannel4 as toChannel,
  toStream2 as toStream,
  Mailbox_exports,
  nominal,
  Brand_exports,
  isChannel2 as isChannel,
  acquireUseRelease3 as acquireUseRelease,
  embedInput2 as embedInput,
  failCause3 as failCause,
  flatMap5 as flatMap,
  mapOut2 as mapOut,
  mapOutEffect2 as mapOutEffect,
  pipeTo2 as pipeTo,
  readWithCause2 as readWithCause,
  run2 as run,
  runDrain2 as runDrain,
  splitLines2 as splitLines,
  succeed4 as succeed,
  suspend4 as suspend,
  sync5 as sync,
  toPull2 as toPull,
  void_3 as void_,
  unwrap3 as unwrap,
  unwrapScoped3 as unwrapScoped,
  write2 as write,
  zipRight4 as zipRight,
  Channel_exports,
  fail4 as fail,
  foldLeftChunks2 as foldLeftChunks,
  forEach3 as forEach,
  unwrapScoped4 as unwrapScoped2,
  Sink_exports,
  make5 as make2,
  unsafeAdd,
  run3 as run2,
  runtime2 as runtime,
  join,
  FiberSet_exports,
  makeWithTTL,
  get2 as get,
  make7 as make3,
  makeWithTTL2,
  Pool_exports
};
//# sourceMappingURL=chunk-SF36ZBUI.js.map
