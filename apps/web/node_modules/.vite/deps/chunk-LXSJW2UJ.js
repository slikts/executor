import {
  Class2 as Class,
  DisablePropagation,
  Do,
  IllegalArgumentException as IllegalArgumentException2,
  Listeners,
  Order,
  RandomTypeId,
  RequestTypeId,
  TaggedClass,
  _catch,
  _continue,
  acquireRelease,
  acquireReleaseInterruptible,
  addFinalizer,
  after,
  all2,
  allSuccesses,
  allWith,
  allowInterrupt,
  annotateCurrentSpan,
  annotateLogs,
  annotateLogsScoped,
  annotateSpans,
  asSome,
  asSomeError,
  asyncEffect,
  awaitAllChildren,
  bind,
  bindAll,
  bindTo,
  cacheRequest,
  cached as cached2,
  cachedFunction,
  cachedInvalidateWithTTL,
  catchAllDefect,
  catchSomeCause,
  catchSomeDefect,
  catchTag,
  catchTags,
  cause,
  choice,
  clock,
  clockWith2 as clockWith,
  complete,
  completeEffect,
  configProviderWith,
  consoleTag,
  contextWith,
  continueWith,
  currentParentSpan,
  currentServices,
  currentSpan,
  currentTimeMillis,
  daemonChildren,
  delay,
  descriptor,
  descriptorWith,
  diffFiberRefs,
  disconnect,
  done,
  dropUntil,
  dropWhile,
  effect_provide,
  end,
  ensuring,
  ensuringChild,
  ensuringChildren,
  eventually,
  every,
  exists,
  fail2 as fail3,
  failCause2,
  fiberRefLocallyScoped,
  fiberRefLocallyScopedWith,
  fiberRefs,
  filter2,
  filterMap,
  filterOrDie,
  filterOrDieMessage,
  filterOrElse,
  filterOrFail,
  finalizersMask,
  findFirst,
  firstSuccessOf,
  fixed,
  flipWith,
  forEach,
  forever,
  fork,
  forkAll,
  forkDaemon,
  forkIn,
  forkScoped,
  forkWithErrorHandler,
  fromEffect2 as fromEffect,
  fromFiber,
  fromFiberEffect,
  fromNullable as fromNullable2,
  fromRequest,
  functionWithSpan,
  get as get2,
  greaterThanOrEqualTo as greaterThanOrEqualTo3,
  head,
  ignore,
  ignoreLogged,
  inheritFiberRefs,
  interruptWhenPossible,
  intersect,
  isContinue,
  isDone2 as isDone,
  isFailure,
  isNonEmpty as isNonEmpty2,
  isRequest,
  isSuccess,
  iterate,
  labelMetrics,
  labelMetricsScoped,
  lessThan as lessThan3,
  let_,
  liftPredicate,
  linkSpanCurrent,
  linkSpans,
  log,
  logAnnotations,
  logDebug,
  logError,
  logFatal,
  logInfo,
  logTrace,
  logWarning,
  logWithLevel,
  loop,
  make10 as make5,
  make14 as make6,
  make2 as make3,
  make3 as make4,
  makeLatch,
  makeSemaphore,
  makeSpan,
  makeSpanScoped,
  mapAccum,
  mapErrorCause,
  match as match2,
  max as max2,
  memoize,
  merge as merge2,
  mergeAll,
  negate,
  next,
  nextBoolean,
  nextInt,
  nextIntBetween,
  nextRange,
  none as none2,
  of,
  once,
  option,
  optionFromOptional,
  orElseFail,
  orElseSucceed,
  parallelErrors,
  parallelFinalizers,
  partition,
  patchFiberRefs,
  promise,
  provide,
  provideService,
  provideServiceEffect,
  race,
  raceAll,
  raceFirst,
  raceWith,
  random,
  randomTag,
  randomWith,
  reduce,
  reduceEffect,
  reduceRight,
  reduceWhile,
  repeatN,
  replicate,
  replicateEffect,
  runtime,
  sandbox,
  scope,
  scopeWith,
  scoped,
  scopedDiscard,
  scopedEffect,
  scopedWith,
  sequentialFinalizers,
  serviceConstants,
  serviceFunction,
  serviceFunctionEffect,
  serviceFunctions,
  serviceMembers,
  serviceOption,
  serviceOptional,
  set,
  setFiberRefs,
  shuffle,
  sign,
  size3 as size,
  sleep2 as sleep,
  spanAnnotations,
  spanLinks,
  start,
  succeed4 as succeed2,
  succeed5 as succeed3,
  succeedNone,
  succeedSome,
  summarized,
  supervised,
  sync3 as sync2,
  tagMetrics,
  tagMetricsScoped,
  tagged3 as tagged,
  takeUntil,
  takeWhile,
  tapBoth,
  tapDefect,
  tapError,
  tapErrorCause,
  tapErrorTag,
  timed,
  timedWith,
  timeout,
  timeoutFail,
  timeoutFailCause,
  timeoutOption,
  timeoutTo,
  tracer,
  tracerWith,
  tryMap,
  tryMapPromise,
  tryPromise,
  try_,
  union2 as union,
  unless,
  unlessEffect,
  unsafeForkEffect,
  unsafeMake,
  unsafeMakeLatch,
  unsafeMakeSemaphore,
  unsafeRunEffect,
  unsafeRunPromiseEffect,
  unsafeRunPromiseExitEffect,
  unsafeRunSyncEffect,
  unsafeRunSyncExitEffect,
  unsandbox,
  update,
  updateFiberRefs,
  updateService,
  useSpan,
  using,
  validate,
  validateAll,
  validateFirst,
  validateWith,
  when,
  whenFiberRef,
  whenLogLevel,
  whenRef,
  withClock,
  withClockScoped,
  withConfigProvider,
  withConfigProviderScoped,
  withEarlyRelease,
  withLogSpan,
  withMetric,
  withParentSpan,
  withRandom,
  withRandomScoped,
  withRequestCache,
  withRequestCaching,
  withRuntimeFlagsScoped,
  withScheduler,
  withSpan,
  withSpanScoped,
  withTracer,
  withTracerScoped,
  zipLeftOptions,
  zipOptions,
  zipRightOptions,
  zipWithOptions
} from "./chunk-UYCMDTGT.js";
import {
  TaggedError
} from "./chunk-B5FOTKVA.js";
import {
  EffectTypeId,
  IllegalArgumentException,
  NoSuchElementException,
  acquireUseRelease,
  andThen2 as andThen,
  as2 as as,
  asVoid,
  async_,
  blocked,
  catchAll,
  catchAllCause,
  catchIf,
  catchSome,
  checkInterruptible,
  context,
  contextWithEffect,
  custom,
  decode,
  deferredMake,
  die,
  die2,
  dieMessage,
  dieSync,
  either,
  exit,
  fail,
  fail2,
  failCause,
  failCauseSync,
  failSync,
  fiberId,
  fiberIdWith,
  fiberRefGet,
  fiberRefGetWith,
  fiberRefLocally,
  fiberRefLocallyWith,
  filterEffectOrElse,
  filterEffectOrFail,
  find,
  flatMap2 as flatMap,
  flatten3 as flatten,
  flip,
  fnUntraced,
  fromIterator,
  gen,
  greaterThanOrEqualTo as greaterThanOrEqualTo2,
  if_,
  interrupt2 as interrupt,
  interruptWith,
  interruptible2 as interruptible,
  interruptibleMask,
  intoDeferred,
  isDieType,
  isEffect,
  isEntry,
  lessThan as lessThan2,
  makeEntry,
  map2 as map3,
  mapBoth,
  mapError,
  mapInputContext,
  matchCause,
  matchCauseEffect,
  matchEffect,
  metricLabels,
  millis,
  never,
  onError,
  onExit,
  onInterrupt,
  orDie,
  orDieWith,
  orElse,
  provideContext,
  runRequestBlock,
  runtimeFlags,
  sequential,
  step,
  succeed,
  sum,
  suspend,
  sync,
  tap,
  times,
  toMillis,
  transplant,
  uninterruptible,
  uninterruptibleMask,
  updateRuntimeFlags,
  void_,
  whenEffect,
  whileLoop,
  withConcurrency,
  withFiberRuntime,
  withMaxOpsBeforeYield,
  withRequestBatching,
  withRuntimeFlags,
  withSchedulingPriority,
  withTracerEnabled,
  withTracerTiming,
  withUnhandledErrorLogLevel,
  yieldNow,
  zero,
  zipLeft,
  zipRight,
  zipWith
} from "./chunk-7GS5HKXM.js";
import {
  all,
  append2 as append,
  empty4 as empty,
  fromIterable,
  fromIterable2,
  fromOption,
  getOrThrowWith,
  headNonEmpty2 as headNonEmpty,
  isLeft,
  isNonEmpty,
  isNonEmptyArray,
  left,
  map,
  merge,
  prepend2 as prepend,
  reverse2 as reverse,
  right,
  sort,
  tailNonEmpty2 as tailNonEmpty
} from "./chunk-JDLTRRA5.js";
import {
  Reference,
  TagProto,
  add,
  get
} from "./chunk-RG46YT3R.js";
import {
  array,
  between,
  clamp,
  filter,
  fromNullable,
  getOrUndefined,
  greaterThan,
  greaterThanOrEqualTo,
  isNone as isNone2,
  isSome,
  lessThan,
  lessThanOrEqualTo,
  liftThrowable,
  make,
  make2,
  map as map2,
  match,
  max,
  min,
  none,
  number,
  some as some2,
  string,
  string2 as string3
} from "./chunk-5XJTUU64.js";
import {
  NodeInspectSymbol,
  array as array2,
  cached,
  combine,
  equals,
  format,
  globalValue,
  hasProperty,
  hash,
  isGeneratorFunction,
  isNone,
  isString,
  number as number2,
  pipeArguments,
  some,
  string as string2,
  symbol,
  symbol2
} from "./chunk-SPGJQP32.js";
import {
  constVoid,
  dual,
  identity,
  pipe
} from "./chunk-LX4DLJUA.js";
import {
  __export
} from "./chunk-G3PMV62Z.js";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Effect.js
var Effect_exports = {};
__export(Effect_exports, {
  Do: () => Do2,
  EffectTypeId: () => EffectTypeId2,
  Service: () => Service,
  Tag: () => Tag,
  acquireRelease: () => acquireRelease2,
  acquireReleaseInterruptible: () => acquireReleaseInterruptible2,
  acquireUseRelease: () => acquireUseRelease2,
  addFinalizer: () => addFinalizer2,
  all: () => all3,
  allSuccesses: () => allSuccesses2,
  allWith: () => allWith2,
  allowInterrupt: () => allowInterrupt2,
  andThen: () => andThen3,
  annotateCurrentSpan: () => annotateCurrentSpan2,
  annotateLogs: () => annotateLogs2,
  annotateLogsScoped: () => annotateLogsScoped2,
  annotateSpans: () => annotateSpans2,
  ap: () => ap,
  as: () => as3,
  asSome: () => asSome2,
  asSomeError: () => asSomeError2,
  asVoid: () => asVoid3,
  async: () => async,
  asyncEffect: () => asyncEffect2,
  awaitAllChildren: () => awaitAllChildren2,
  bind: () => bind2,
  bindAll: () => bindAll2,
  bindTo: () => bindTo2,
  blocked: () => blocked2,
  cacheRequestResult: () => cacheRequestResult,
  cached: () => cached3,
  cachedFunction: () => cachedFunction2,
  cachedInvalidateWithTTL: () => cachedInvalidateWithTTL2,
  cachedWithTTL: () => cachedWithTTL,
  catch: () => _catch2,
  catchAll: () => catchAll2,
  catchAllCause: () => catchAllCause2,
  catchAllDefect: () => catchAllDefect2,
  catchIf: () => catchIf2,
  catchSome: () => catchSome2,
  catchSomeCause: () => catchSomeCause2,
  catchSomeDefect: () => catchSomeDefect2,
  catchTag: () => catchTag2,
  catchTags: () => catchTags2,
  cause: () => cause2,
  checkInterruptible: () => checkInterruptible2,
  clock: () => clock2,
  clockWith: () => clockWith2,
  configProviderWith: () => configProviderWith2,
  console: () => console2,
  consoleWith: () => consoleWith2,
  context: () => context2,
  contextWith: () => contextWith2,
  contextWithEffect: () => contextWithEffect2,
  currentParentSpan: () => currentParentSpan2,
  currentSpan: () => currentSpan2,
  custom: () => custom2,
  daemonChildren: () => daemonChildren2,
  delay: () => delay2,
  descriptor: () => descriptor2,
  descriptorWith: () => descriptorWith2,
  die: () => die3,
  dieMessage: () => dieMessage2,
  dieSync: () => dieSync2,
  diffFiberRefs: () => diffFiberRefs2,
  disconnect: () => disconnect2,
  dropUntil: () => dropUntil2,
  dropWhile: () => dropWhile2,
  either: () => either3,
  ensureErrorType: () => ensureErrorType,
  ensureRequirementsType: () => ensureRequirementsType,
  ensureSuccessType: () => ensureSuccessType,
  ensuring: () => ensuring3,
  ensuringChild: () => ensuringChild2,
  ensuringChildren: () => ensuringChildren2,
  eventually: () => eventually2,
  every: () => every2,
  exists: () => exists2,
  exit: () => exit2,
  fail: () => fail5,
  failCause: () => failCause4,
  failCauseSync: () => failCauseSync2,
  failSync: () => failSync2,
  fiberId: () => fiberId2,
  fiberIdWith: () => fiberIdWith2,
  filter: () => filter3,
  filterEffectOrElse: () => filterEffectOrElse2,
  filterEffectOrFail: () => filterEffectOrFail2,
  filterMap: () => filterMap2,
  filterOrDie: () => filterOrDie2,
  filterOrDieMessage: () => filterOrDieMessage2,
  filterOrElse: () => filterOrElse2,
  filterOrFail: () => filterOrFail2,
  finalizersMask: () => finalizersMask2,
  findFirst: () => findFirst2,
  firstSuccessOf: () => firstSuccessOf2,
  flatMap: () => flatMap2,
  flatten: () => flatten2,
  flip: () => flip2,
  flipWith: () => flipWith2,
  fn: () => fn,
  fnUntraced: () => fnUntraced2,
  forEach: () => forEach2,
  forever: () => forever3,
  fork: () => fork2,
  forkAll: () => forkAll2,
  forkDaemon: () => forkDaemon2,
  forkIn: () => forkIn2,
  forkScoped: () => forkScoped2,
  forkWithErrorHandler: () => forkWithErrorHandler2,
  fromFiber: () => fromFiber2,
  fromFiberEffect: () => fromFiberEffect2,
  fromNullable: () => fromNullable3,
  functionWithSpan: () => functionWithSpan2,
  gen: () => gen2,
  getFiberRefs: () => getFiberRefs,
  getRuntimeFlags: () => getRuntimeFlags,
  head: () => head2,
  if: () => if_2,
  ignore: () => ignore2,
  ignoreLogged: () => ignoreLogged2,
  inheritFiberRefs: () => inheritFiberRefs2,
  interrupt: () => interrupt2,
  interruptWith: () => interruptWith2,
  interruptible: () => interruptible2,
  interruptibleMask: () => interruptibleMask2,
  intoDeferred: () => intoDeferred2,
  isEffect: () => isEffect2,
  isFailure: () => isFailure2,
  isSuccess: () => isSuccess2,
  iterate: () => iterate2,
  labelMetrics: () => labelMetrics2,
  labelMetricsScoped: () => labelMetricsScoped2,
  let: () => let_2,
  liftPredicate: () => liftPredicate2,
  linkSpanCurrent: () => linkSpanCurrent2,
  linkSpans: () => linkSpans2,
  locally: () => locally,
  locallyScoped: () => locallyScoped,
  locallyScopedWith: () => locallyScopedWith,
  locallyWith: () => locallyWith,
  log: () => log3,
  logAnnotations: () => logAnnotations2,
  logDebug: () => logDebug2,
  logError: () => logError2,
  logFatal: () => logFatal2,
  logInfo: () => logInfo2,
  logTrace: () => logTrace2,
  logWarning: () => logWarning2,
  logWithLevel: () => logWithLevel2,
  loop: () => loop2,
  makeLatch: () => makeLatch2,
  makeSemaphore: () => makeSemaphore2,
  makeSpan: () => makeSpan2,
  makeSpanScoped: () => makeSpanScoped2,
  map: () => map5,
  mapAccum: () => mapAccum2,
  mapBoth: () => mapBoth3,
  mapError: () => mapError2,
  mapErrorCause: () => mapErrorCause2,
  mapInputContext: () => mapInputContext3,
  match: () => match6,
  matchCause: () => matchCause2,
  matchCauseEffect: () => matchCauseEffect2,
  matchEffect: () => matchEffect2,
  merge: () => merge3,
  mergeAll: () => mergeAll2,
  metricLabels: () => metricLabels2,
  negate: () => negate2,
  never: () => never2,
  none: () => none3,
  onError: () => onError2,
  onExit: () => onExit2,
  onInterrupt: () => onInterrupt2,
  once: () => once3,
  option: () => option2,
  optionFromOptional: () => optionFromOptional2,
  orDie: () => orDie2,
  orDieWith: () => orDieWith2,
  orElse: () => orElse2,
  orElseFail: () => orElseFail2,
  orElseSucceed: () => orElseSucceed2,
  parallelErrors: () => parallelErrors2,
  parallelFinalizers: () => parallelFinalizers2,
  partition: () => partition2,
  patchFiberRefs: () => patchFiberRefs2,
  patchRuntimeFlags: () => patchRuntimeFlags,
  promise: () => promise2,
  provide: () => provide2,
  provideService: () => provideService3,
  provideServiceEffect: () => provideServiceEffect2,
  race: () => race2,
  raceAll: () => raceAll2,
  raceFirst: () => raceFirst2,
  raceWith: () => raceWith2,
  random: () => random2,
  randomWith: () => randomWith3,
  reduce: () => reduce3,
  reduceEffect: () => reduceEffect3,
  reduceRight: () => reduceRight2,
  reduceWhile: () => reduceWhile2,
  repeat: () => repeat2,
  repeatN: () => repeatN2,
  repeatOrElse: () => repeatOrElse,
  replicate: () => replicate2,
  replicateEffect: () => replicateEffect2,
  request: () => request,
  retry: () => retry,
  retryOrElse: () => retryOrElse,
  runCallback: () => runCallback,
  runFork: () => runFork,
  runPromise: () => runPromise,
  runPromiseExit: () => runPromiseExit,
  runRequestBlock: () => runRequestBlock2,
  runSync: () => runSync,
  runSyncExit: () => runSyncExit,
  runtime: () => runtime2,
  sandbox: () => sandbox2,
  schedule: () => schedule,
  scheduleForked: () => scheduleForked2,
  scheduleFrom: () => scheduleFrom,
  scope: () => scope2,
  scopeWith: () => scopeWith2,
  scoped: () => scoped2,
  scopedWith: () => scopedWith2,
  sequentialFinalizers: () => sequentialFinalizers2,
  serviceConstants: () => serviceConstants2,
  serviceFunction: () => serviceFunction2,
  serviceFunctionEffect: () => serviceFunctionEffect2,
  serviceFunctions: () => serviceFunctions2,
  serviceMembers: () => serviceMembers2,
  serviceOption: () => serviceOption2,
  serviceOptional: () => serviceOptional2,
  setFiberRefs: () => setFiberRefs2,
  sleep: () => sleep2,
  spanAnnotations: () => spanAnnotations2,
  spanLinks: () => spanLinks2,
  step: () => step2,
  succeed: () => succeed6,
  succeedNone: () => succeedNone2,
  succeedSome: () => succeedSome2,
  summarized: () => summarized2,
  supervised: () => supervised2,
  suspend: () => suspend2,
  sync: () => sync4,
  tagMetrics: () => tagMetrics2,
  tagMetricsScoped: () => tagMetricsScoped2,
  takeUntil: () => takeUntil2,
  takeWhile: () => takeWhile2,
  tap: () => tap2,
  tapBoth: () => tapBoth2,
  tapDefect: () => tapDefect2,
  tapError: () => tapError2,
  tapErrorCause: () => tapErrorCause2,
  tapErrorTag: () => tapErrorTag2,
  timed: () => timed2,
  timedWith: () => timedWith2,
  timeout: () => timeout2,
  timeoutFail: () => timeoutFail2,
  timeoutFailCause: () => timeoutFailCause2,
  timeoutOption: () => timeoutOption2,
  timeoutTo: () => timeoutTo2,
  tracer: () => tracer2,
  tracerWith: () => tracerWith2,
  transplant: () => transplant2,
  transposeMapOption: () => transposeMapOption,
  transposeOption: () => transposeOption,
  try: () => try_2,
  tryMap: () => tryMap2,
  tryMapPromise: () => tryMapPromise2,
  tryPromise: () => tryPromise2,
  uninterruptible: () => uninterruptible2,
  uninterruptibleMask: () => uninterruptibleMask2,
  unless: () => unless2,
  unlessEffect: () => unlessEffect2,
  unsafeMakeLatch: () => unsafeMakeLatch2,
  unsafeMakeSemaphore: () => unsafeMakeSemaphore2,
  unsandbox: () => unsandbox2,
  updateFiberRefs: () => updateFiberRefs2,
  updateService: () => updateService2,
  useSpan: () => useSpan2,
  using: () => using2,
  validate: () => validate2,
  validateAll: () => validateAll2,
  validateFirst: () => validateFirst2,
  validateWith: () => validateWith2,
  void: () => _void,
  when: () => when2,
  whenEffect: () => whenEffect2,
  whenFiberRef: () => whenFiberRef2,
  whenLogLevel: () => whenLogLevel2,
  whenRef: () => whenRef2,
  whileLoop: () => whileLoop2,
  withClock: () => withClock2,
  withClockScoped: () => withClockScoped2,
  withConcurrency: () => withConcurrency2,
  withConfigProvider: () => withConfigProvider2,
  withConfigProviderScoped: () => withConfigProviderScoped2,
  withConsole: () => withConsole2,
  withConsoleScoped: () => withConsoleScoped2,
  withEarlyRelease: () => withEarlyRelease2,
  withExecutionPlan: () => withExecutionPlan2,
  withFiberRuntime: () => withFiberRuntime2,
  withLogSpan: () => withLogSpan2,
  withMaxOpsBeforeYield: () => withMaxOpsBeforeYield2,
  withMetric: () => withMetric2,
  withParentSpan: () => withParentSpan2,
  withRandom: () => withRandom2,
  withRandomFixed: () => withRandomFixed,
  withRandomScoped: () => withRandomScoped2,
  withRequestBatching: () => withRequestBatching2,
  withRequestCache: () => withRequestCache2,
  withRequestCaching: () => withRequestCaching2,
  withRuntimeFlagsPatch: () => withRuntimeFlagsPatch,
  withRuntimeFlagsPatchScoped: () => withRuntimeFlagsPatchScoped,
  withScheduler: () => withScheduler2,
  withSchedulingPriority: () => withSchedulingPriority2,
  withSpan: () => withSpan2,
  withSpanScoped: () => withSpanScoped2,
  withTracer: () => withTracer2,
  withTracerEnabled: () => withTracerEnabled2,
  withTracerScoped: () => withTracerScoped2,
  withTracerTiming: () => withTracerTiming2,
  withUnhandledErrorLogLevel: () => withUnhandledErrorLogLevel2,
  yieldNow: () => yieldNow2,
  zip: () => zip,
  zipLeft: () => zipLeft3,
  zipRight: () => zipRight3,
  zipWith: () => zipWith3
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/console.js
var console = map3(fiberRefGet(currentServices), get(consoleTag));
var consoleWith = (f) => fiberRefGetWith(currentServices, (services) => f(get(services, consoleTag)));
var withConsole = dual(2, (effect, value) => fiberRefLocallyWith(effect, currentServices, add(consoleTag, value)));
var withConsoleScoped = (console3) => fiberRefLocallyScopedWith(currentServices, add(consoleTag, console3));
var setConsole = (console3) => scopedDiscard(fiberRefLocallyScopedWith(currentServices, add(consoleTag, console3)));
var assert = (condition, ...args) => consoleWith((_) => _.assert(condition, ...args));
var clear = consoleWith((_) => _.clear);
var count = (label) => consoleWith((_) => _.count(label));
var countReset = (label) => consoleWith((_) => _.countReset(label));
var debug = (...args) => consoleWith((_) => _.debug(...args));
var dir = (item, options) => consoleWith((_) => _.dir(item, options));
var dirxml = (...args) => consoleWith((_) => _.dirxml(...args));
var error = (...args) => consoleWith((_) => _.error(...args));
var group = (options) => consoleWith((_) => acquireRelease(_.group(options), () => _.groupEnd));
var info = (...args) => consoleWith((_) => _.info(...args));
var log2 = (...args) => consoleWith((_) => _.log(...args));
var table = (tabularData, properties) => consoleWith((_) => _.table(tabularData, properties));
var time = (label) => consoleWith((_) => acquireRelease(_.time(label), () => _.timeEnd(label)));
var timeLog = (label, ...args) => consoleWith((_) => _.timeLog(label, ...args));
var trace = (...args) => consoleWith((_) => _.trace(...args));
var warn = (...args) => consoleWith((_) => _.warn(...args));
var withGroup = dual((args) => isEffect(args[0]), (self, options) => consoleWith((_) => acquireUseRelease(_.group(options), () => self, () => _.groupEnd)));
var withTime = dual((args) => isEffect(args[0]), (self, label) => consoleWith((_) => acquireUseRelease(_.time(label), () => self, () => _.timeEnd(label))));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Cron.js
var Cron_exports = {};
__export(Cron_exports, {
  Equivalence: () => Equivalence3,
  ParseError: () => ParseError,
  ParseErrorTypeId: () => ParseErrorTypeId,
  TypeId: () => TypeId2,
  equals: () => equals2,
  isCron: () => isCron,
  isParseError: () => isParseError,
  make: () => make8,
  match: () => match5,
  next: () => next2,
  parse: () => parse,
  sequence: () => sequence,
  unsafeParse: () => unsafeParse
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/dateTime.js
var TypeId = Symbol.for("effect/DateTime");
var TimeZoneTypeId = Symbol.for("effect/DateTime/TimeZone");
var Proto = {
  [TypeId]: TypeId,
  pipe() {
    return pipeArguments(this, arguments);
  },
  [NodeInspectSymbol]() {
    return this.toString();
  },
  toJSON() {
    return toDateUtc(this).toJSON();
  }
};
var ProtoUtc = {
  ...Proto,
  _tag: "Utc",
  [symbol]() {
    return cached(this, number2(this.epochMillis));
  },
  [symbol2](that) {
    return isDateTime(that) && that._tag === "Utc" && this.epochMillis === that.epochMillis;
  },
  toString() {
    return `DateTime.Utc(${toDateUtc(this).toJSON()})`;
  }
};
var ProtoZoned = {
  ...Proto,
  _tag: "Zoned",
  [symbol]() {
    return pipe(number2(this.epochMillis), combine(hash(this.zone)), cached(this));
  },
  [symbol2](that) {
    return isDateTime(that) && that._tag === "Zoned" && this.epochMillis === that.epochMillis && equals(this.zone, that.zone);
  },
  toString() {
    return `DateTime.Zoned(${formatIsoZoned(this)})`;
  }
};
var ProtoTimeZone = {
  [TimeZoneTypeId]: TimeZoneTypeId,
  [NodeInspectSymbol]() {
    return this.toString();
  }
};
var ProtoTimeZoneNamed = {
  ...ProtoTimeZone,
  _tag: "Named",
  [symbol]() {
    return cached(this, string2(`Named:${this.id}`));
  },
  [symbol2](that) {
    return isTimeZone(that) && that._tag === "Named" && this.id === that.id;
  },
  toString() {
    return `TimeZone.Named(${this.id})`;
  },
  toJSON() {
    return {
      _id: "TimeZone",
      _tag: "Named",
      id: this.id
    };
  }
};
var ProtoTimeZoneOffset = {
  ...ProtoTimeZone,
  _tag: "Offset",
  [symbol]() {
    return cached(this, string2(`Offset:${this.offset}`));
  },
  [symbol2](that) {
    return isTimeZone(that) && that._tag === "Offset" && this.offset === that.offset;
  },
  toString() {
    return `TimeZone.Offset(${offsetToString(this.offset)})`;
  },
  toJSON() {
    return {
      _id: "TimeZone",
      _tag: "Offset",
      offset: this.offset
    };
  }
};
var makeZonedProto = (epochMillis, zone, partsUtc) => {
  const self = Object.create(ProtoZoned);
  self.epochMillis = epochMillis;
  self.zone = zone;
  Object.defineProperty(self, "partsUtc", {
    value: partsUtc,
    enumerable: false,
    writable: true
  });
  Object.defineProperty(self, "adjustedEpochMillis", {
    value: void 0,
    enumerable: false,
    writable: true
  });
  Object.defineProperty(self, "partsAdjusted", {
    value: void 0,
    enumerable: false,
    writable: true
  });
  return self;
};
var isDateTime = (u) => hasProperty(u, TypeId);
var isDateTimeArgs = (args) => isDateTime(args[0]);
var isTimeZone = (u) => hasProperty(u, TimeZoneTypeId);
var isTimeZoneOffset = (u) => isTimeZone(u) && u._tag === "Offset";
var isTimeZoneNamed = (u) => isTimeZone(u) && u._tag === "Named";
var isUtc = (self) => self._tag === "Utc";
var isZoned = (self) => self._tag === "Zoned";
var Equivalence = make((a, b) => a.epochMillis === b.epochMillis);
var Order2 = make2((self, that) => self.epochMillis < that.epochMillis ? -1 : self.epochMillis > that.epochMillis ? 1 : 0);
var clamp2 = clamp(Order2);
var makeUtc = (epochMillis) => {
  const self = Object.create(ProtoUtc);
  self.epochMillis = epochMillis;
  Object.defineProperty(self, "partsUtc", {
    value: void 0,
    enumerable: false,
    writable: true
  });
  return self;
};
var unsafeFromDate = (date) => {
  const epochMillis = date.getTime();
  if (Number.isNaN(epochMillis)) {
    throw new IllegalArgumentException2("Invalid date");
  }
  return makeUtc(epochMillis);
};
var unsafeMake2 = (input) => {
  if (isDateTime(input)) {
    return input;
  } else if (input instanceof Date) {
    return unsafeFromDate(input);
  } else if (typeof input === "object") {
    const date = /* @__PURE__ */ new Date(0);
    setPartsDate(date, input);
    return unsafeFromDate(date);
  } else if (typeof input === "string" && !hasZone(input)) {
    return unsafeFromDate(/* @__PURE__ */ new Date(input + "Z"));
  }
  return unsafeFromDate(new Date(input));
};
var hasZone = (input) => /Z|[+-]\d{2}$|[+-]\d{2}:?\d{2}$|\]$/.test(input);
var minEpochMillis = -864e13 + 12 * 60 * 60 * 1e3;
var maxEpochMillis = 864e13 - 14 * 60 * 60 * 1e3;
var unsafeMakeZoned = (input, options) => {
  if (options?.timeZone === void 0 && isDateTime(input) && isZoned(input)) {
    return input;
  }
  const self = unsafeMake2(input);
  if (self.epochMillis < minEpochMillis || self.epochMillis > maxEpochMillis) {
    throw new RangeError(`Epoch millis out of range: ${self.epochMillis}`);
  }
  let zone;
  if (options?.timeZone === void 0) {
    const offset = new Date(self.epochMillis).getTimezoneOffset() * -60 * 1e3;
    zone = zoneMakeOffset(offset);
  } else if (isTimeZone(options?.timeZone)) {
    zone = options.timeZone;
  } else if (typeof options?.timeZone === "number") {
    zone = zoneMakeOffset(options.timeZone);
  } else {
    const parsedZone = zoneFromString(options.timeZone);
    if (isNone2(parsedZone)) {
      throw new IllegalArgumentException2(`Invalid time zone: ${options.timeZone}`);
    }
    zone = parsedZone.value;
  }
  if (options?.adjustForTimeZone !== true) {
    return makeZonedProto(self.epochMillis, zone, self.partsUtc);
  }
  return makeZonedFromAdjusted(self.epochMillis, zone, options?.disambiguation ?? "compatible");
};
var makeZoned = liftThrowable(unsafeMakeZoned);
var make7 = liftThrowable(unsafeMake2);
var zonedStringRegex = /^(.{17,35})\[(.+)\]$/;
var makeZonedFromString = (input) => {
  const match7 = zonedStringRegex.exec(input);
  if (match7 === null) {
    const offset = parseOffset(input);
    return offset !== null ? makeZoned(input, {
      timeZone: offset
    }) : none();
  }
  const [, isoString, timeZone] = match7;
  return makeZoned(isoString, {
    timeZone
  });
};
var now = map3(currentTimeMillis, makeUtc);
var nowAsDate = map3(currentTimeMillis, (millis2) => new Date(millis2));
var unsafeNow = () => makeUtc(Date.now());
var toUtc = (self) => makeUtc(self.epochMillis);
var setZone = dual(isDateTimeArgs, (self, zone, options) => options?.adjustForTimeZone === true ? makeZonedFromAdjusted(self.epochMillis, zone, options?.disambiguation ?? "compatible") : makeZonedProto(self.epochMillis, zone, self.partsUtc));
var setZoneOffset = dual(isDateTimeArgs, (self, offset, options) => setZone(self, zoneMakeOffset(offset), options));
var validZoneCache = globalValue("effect/DateTime/validZoneCache", () => /* @__PURE__ */ new Map());
var formatOptions = {
  day: "numeric",
  month: "numeric",
  year: "numeric",
  hour: "numeric",
  minute: "numeric",
  second: "numeric",
  timeZoneName: "longOffset",
  fractionalSecondDigits: 3,
  hourCycle: "h23"
};
var zoneMakeIntl = (format3) => {
  const zoneId = format3.resolvedOptions().timeZone;
  if (validZoneCache.has(zoneId)) {
    return validZoneCache.get(zoneId);
  }
  const zone = Object.create(ProtoTimeZoneNamed);
  zone.id = zoneId;
  zone.format = format3;
  validZoneCache.set(zoneId, zone);
  return zone;
};
var zoneUnsafeMakeNamed = (zoneId) => {
  if (validZoneCache.has(zoneId)) {
    return validZoneCache.get(zoneId);
  }
  try {
    return zoneMakeIntl(new Intl.DateTimeFormat("en-US", {
      ...formatOptions,
      timeZone: zoneId
    }));
  } catch {
    throw new IllegalArgumentException2(`Invalid time zone: ${zoneId}`);
  }
};
var zoneMakeOffset = (offset) => {
  const zone = Object.create(ProtoTimeZoneOffset);
  zone.offset = offset;
  return zone;
};
var zoneMakeNamed = liftThrowable(zoneUnsafeMakeNamed);
var zoneMakeNamedEffect = (zoneId) => try_({
  try: () => zoneUnsafeMakeNamed(zoneId),
  catch: (e) => e
});
var zoneMakeLocal = () => zoneMakeIntl(new Intl.DateTimeFormat("en-US", formatOptions));
var offsetZoneRegex = /^(?:GMT|[+-])/;
var zoneFromString = (zone) => {
  if (offsetZoneRegex.test(zone)) {
    const offset = parseOffset(zone);
    return offset === null ? none() : some2(zoneMakeOffset(offset));
  }
  return zoneMakeNamed(zone);
};
var zoneToString = (self) => {
  if (self._tag === "Offset") {
    return offsetToString(self.offset);
  }
  return self.id;
};
var setZoneNamed = dual(isDateTimeArgs, (self, zoneId, options) => map2(zoneMakeNamed(zoneId), (zone) => setZone(self, zone, options)));
var unsafeSetZoneNamed = dual(isDateTimeArgs, (self, zoneId, options) => setZone(self, zoneUnsafeMakeNamed(zoneId), options));
var distance = dual(2, (self, other) => toEpochMillis(other) - toEpochMillis(self));
var distanceDurationEither = dual(2, (self, other) => {
  const diffMillis = distance(self, other);
  return diffMillis > 0 ? right(millis(diffMillis)) : left(millis(-diffMillis));
});
var distanceDuration = dual(2, (self, other) => millis(Math.abs(distance(self, other))));
var min2 = min(Order2);
var max3 = max(Order2);
var greaterThan2 = greaterThan(Order2);
var greaterThanOrEqualTo4 = greaterThanOrEqualTo(Order2);
var lessThan4 = lessThan(Order2);
var lessThanOrEqualTo2 = lessThanOrEqualTo(Order2);
var between2 = between(Order2);
var isFuture = (self) => map3(now, lessThan4(self));
var unsafeIsFuture = (self) => lessThan4(unsafeNow(), self);
var isPast = (self) => map3(now, greaterThan2(self));
var unsafeIsPast = (self) => greaterThan2(unsafeNow(), self);
var toDateUtc = (self) => new Date(self.epochMillis);
var toDate = (self) => {
  if (self._tag === "Utc") {
    return new Date(self.epochMillis);
  } else if (self.zone._tag === "Offset") {
    return new Date(self.epochMillis + self.zone.offset);
  } else if (self.adjustedEpochMillis !== void 0) {
    return new Date(self.adjustedEpochMillis);
  }
  const parts = self.zone.format.formatToParts(self.epochMillis).filter((_) => _.type !== "literal");
  const date = /* @__PURE__ */ new Date(0);
  date.setUTCFullYear(Number(parts[2].value), Number(parts[0].value) - 1, Number(parts[1].value));
  date.setUTCHours(Number(parts[3].value), Number(parts[4].value), Number(parts[5].value), Number(parts[6].value));
  self.adjustedEpochMillis = date.getTime();
  return date;
};
var zonedOffset = (self) => {
  const date = toDate(self);
  return date.getTime() - toEpochMillis(self);
};
var offsetToString = (offset) => {
  const abs = Math.abs(offset);
  let hours = Math.floor(abs / (60 * 60 * 1e3));
  let minutes = Math.round(abs % (60 * 60 * 1e3) / (60 * 1e3));
  if (minutes === 60) {
    hours += 1;
    minutes = 0;
  }
  return `${offset < 0 ? "-" : "+"}${String(hours).padStart(2, "0")}:${String(minutes).padStart(2, "0")}`;
};
var zonedOffsetIso = (self) => offsetToString(zonedOffset(self));
var toEpochMillis = (self) => self.epochMillis;
var removeTime = (self) => withDate(self, (date) => {
  date.setUTCHours(0, 0, 0, 0);
  return makeUtc(date.getTime());
});
var dateToParts = (date) => ({
  millis: date.getUTCMilliseconds(),
  seconds: date.getUTCSeconds(),
  minutes: date.getUTCMinutes(),
  hours: date.getUTCHours(),
  day: date.getUTCDate(),
  weekDay: date.getUTCDay(),
  month: date.getUTCMonth() + 1,
  year: date.getUTCFullYear()
});
var toParts = (self) => {
  if (self._tag === "Utc") {
    return toPartsUtc(self);
  } else if (self.partsAdjusted !== void 0) {
    return self.partsAdjusted;
  }
  self.partsAdjusted = withDate(self, dateToParts);
  return self.partsAdjusted;
};
var toPartsUtc = (self) => {
  if (self.partsUtc !== void 0) {
    return self.partsUtc;
  }
  self.partsUtc = withDateUtc(self, dateToParts);
  return self.partsUtc;
};
var getPartUtc = dual(2, (self, part) => toPartsUtc(self)[part]);
var getPart = dual(2, (self, part) => toParts(self)[part]);
var setPartsDate = (date, parts) => {
  if (parts.year !== void 0) {
    date.setUTCFullYear(parts.year);
  }
  if (parts.month !== void 0) {
    date.setUTCMonth(parts.month - 1);
  }
  if (parts.day !== void 0) {
    date.setUTCDate(parts.day);
  }
  if (parts.weekDay !== void 0) {
    const diff = parts.weekDay - date.getUTCDay();
    date.setUTCDate(date.getUTCDate() + diff);
  }
  if (parts.hours !== void 0) {
    date.setUTCHours(parts.hours);
  }
  if (parts.minutes !== void 0) {
    date.setUTCMinutes(parts.minutes);
  }
  if (parts.seconds !== void 0) {
    date.setUTCSeconds(parts.seconds);
  }
  if (parts.millis !== void 0) {
    date.setUTCMilliseconds(parts.millis);
  }
};
var setParts = dual(2, (self, parts) => mutate(self, (date) => setPartsDate(date, parts)));
var setPartsUtc = dual(2, (self, parts) => mutateUtc(self, (date) => setPartsDate(date, parts)));
var constDayMillis = 24 * 60 * 60 * 1e3;
var makeZonedFromAdjusted = (adjustedMillis, zone, disambiguation) => {
  if (zone._tag === "Offset") {
    return makeZonedProto(adjustedMillis - zone.offset, zone);
  }
  const beforeOffset = calculateNamedOffset(adjustedMillis - constDayMillis, adjustedMillis, zone);
  const afterOffset = calculateNamedOffset(adjustedMillis + constDayMillis, adjustedMillis, zone);
  if (beforeOffset === afterOffset) {
    return makeZonedProto(adjustedMillis - beforeOffset, zone);
  }
  const isForwards = beforeOffset < afterOffset;
  const transitionMillis = beforeOffset - afterOffset;
  if (isForwards) {
    const currentAfterOffset = calculateNamedOffset(adjustedMillis - afterOffset, adjustedMillis, zone);
    if (currentAfterOffset === afterOffset) {
      return makeZonedProto(adjustedMillis - afterOffset, zone);
    }
    const before = makeZonedProto(adjustedMillis - beforeOffset, zone);
    const beforeAdjustedMillis = toDate(before).getTime();
    if (adjustedMillis !== beforeAdjustedMillis) {
      switch (disambiguation) {
        case "reject": {
          const formatted = new Date(adjustedMillis).toISOString();
          throw new RangeError(`Gap time: ${formatted} does not exist in time zone ${zone.id}`);
        }
        case "earlier":
          return makeZonedProto(adjustedMillis - afterOffset, zone);
        case "compatible":
        case "later":
          return before;
      }
    }
    return before;
  }
  const currentBeforeOffset = calculateNamedOffset(adjustedMillis - beforeOffset, adjustedMillis, zone);
  if (currentBeforeOffset === beforeOffset) {
    if (disambiguation === "earlier" || disambiguation === "compatible") {
      return makeZonedProto(adjustedMillis - beforeOffset, zone);
    }
    const laterOffset = calculateNamedOffset(adjustedMillis - beforeOffset + transitionMillis, adjustedMillis + transitionMillis, zone);
    if (laterOffset === beforeOffset) {
      return makeZonedProto(adjustedMillis - beforeOffset, zone);
    }
    if (disambiguation === "reject") {
      const formatted = new Date(adjustedMillis).toISOString();
      throw new RangeError(`Ambiguous time: ${formatted} occurs twice in time zone ${zone.id}`);
    }
  }
  return makeZonedProto(adjustedMillis - afterOffset, zone);
};
var offsetRegex = /([+-])(\d{2}):(\d{2})$/;
var parseOffset = (offset) => {
  const match7 = offsetRegex.exec(offset);
  if (match7 === null) {
    return null;
  }
  const [, sign2, hours, minutes] = match7;
  return (sign2 === "+" ? 1 : -1) * (Number(hours) * 60 + Number(minutes)) * 60 * 1e3;
};
var calculateNamedOffset = (utcMillis, adjustedMillis, zone) => {
  const offset = zone.format.formatToParts(utcMillis).find((_) => _.type === "timeZoneName")?.value ?? "";
  if (offset === "GMT") {
    return 0;
  }
  const result = parseOffset(offset);
  if (result === null) {
    return zonedOffset(makeZonedProto(adjustedMillis, zone));
  }
  return result;
};
var mutate = dual(isDateTimeArgs, (self, f, options) => {
  if (self._tag === "Utc") {
    const date = toDateUtc(self);
    f(date);
    return makeUtc(date.getTime());
  }
  const adjustedDate = toDate(self);
  const newAdjustedDate = new Date(adjustedDate.getTime());
  f(newAdjustedDate);
  return makeZonedFromAdjusted(newAdjustedDate.getTime(), self.zone, options?.disambiguation ?? "compatible");
});
var mutateUtc = dual(2, (self, f) => mapEpochMillis(self, (millis2) => {
  const date = new Date(millis2);
  f(date);
  return date.getTime();
}));
var mapEpochMillis = dual(2, (self, f) => {
  const millis2 = f(toEpochMillis(self));
  return self._tag === "Utc" ? makeUtc(millis2) : makeZonedProto(millis2, self.zone);
});
var withDate = dual(2, (self, f) => f(toDate(self)));
var withDateUtc = dual(2, (self, f) => f(toDateUtc(self)));
var match3 = dual(2, (self, options) => self._tag === "Utc" ? options.onUtc(self) : options.onZoned(self));
var addDuration = dual(2, (self, duration2) => mapEpochMillis(self, (millis2) => millis2 + toMillis(duration2)));
var subtractDuration = dual(2, (self, duration2) => mapEpochMillis(self, (millis2) => millis2 - toMillis(duration2)));
var addMillis = (date, amount) => {
  date.setTime(date.getTime() + amount);
};
var add2 = dual(2, (self, parts) => mutate(self, (date) => {
  if (parts.millis) {
    addMillis(date, parts.millis);
  }
  if (parts.seconds) {
    addMillis(date, parts.seconds * 1e3);
  }
  if (parts.minutes) {
    addMillis(date, parts.minutes * 60 * 1e3);
  }
  if (parts.hours) {
    addMillis(date, parts.hours * 60 * 60 * 1e3);
  }
  if (parts.days) {
    date.setUTCDate(date.getUTCDate() + parts.days);
  }
  if (parts.weeks) {
    date.setUTCDate(date.getUTCDate() + parts.weeks * 7);
  }
  if (parts.months) {
    const day = date.getUTCDate();
    date.setUTCMonth(date.getUTCMonth() + parts.months + 1, 0);
    if (day < date.getUTCDate()) {
      date.setUTCDate(day);
    }
  }
  if (parts.years) {
    const day = date.getUTCDate();
    const month = date.getUTCMonth();
    date.setUTCFullYear(date.getUTCFullYear() + parts.years, month + 1, 0);
    if (day < date.getUTCDate()) {
      date.setUTCDate(day);
    }
  }
}));
var subtract = dual(2, (self, parts) => {
  const newParts = {};
  for (const key in parts) {
    newParts[key] = -1 * parts[key];
  }
  return add2(self, newParts);
});
var startOfDate = (date, part, options) => {
  switch (part) {
    case "second": {
      date.setUTCMilliseconds(0);
      break;
    }
    case "minute": {
      date.setUTCSeconds(0, 0);
      break;
    }
    case "hour": {
      date.setUTCMinutes(0, 0, 0);
      break;
    }
    case "day": {
      date.setUTCHours(0, 0, 0, 0);
      break;
    }
    case "week": {
      const weekStartsOn = options?.weekStartsOn ?? 0;
      const day = date.getUTCDay();
      const diff = (day - weekStartsOn + 7) % 7;
      date.setUTCDate(date.getUTCDate() - diff);
      date.setUTCHours(0, 0, 0, 0);
      break;
    }
    case "month": {
      date.setUTCDate(1);
      date.setUTCHours(0, 0, 0, 0);
      break;
    }
    case "year": {
      date.setUTCMonth(0, 1);
      date.setUTCHours(0, 0, 0, 0);
      break;
    }
  }
};
var startOf = dual(isDateTimeArgs, (self, part, options) => mutate(self, (date) => startOfDate(date, part, options)));
var endOfDate = (date, part, options) => {
  switch (part) {
    case "second": {
      date.setUTCMilliseconds(999);
      break;
    }
    case "minute": {
      date.setUTCSeconds(59, 999);
      break;
    }
    case "hour": {
      date.setUTCMinutes(59, 59, 999);
      break;
    }
    case "day": {
      date.setUTCHours(23, 59, 59, 999);
      break;
    }
    case "week": {
      const weekStartsOn = options?.weekStartsOn ?? 0;
      const day = date.getUTCDay();
      const diff = (day - weekStartsOn + 7) % 7;
      date.setUTCDate(date.getUTCDate() - diff + 6);
      date.setUTCHours(23, 59, 59, 999);
      break;
    }
    case "month": {
      date.setUTCMonth(date.getUTCMonth() + 1, 0);
      date.setUTCHours(23, 59, 59, 999);
      break;
    }
    case "year": {
      date.setUTCMonth(11, 31);
      date.setUTCHours(23, 59, 59, 999);
      break;
    }
  }
};
var endOf = dual(isDateTimeArgs, (self, part, options) => mutate(self, (date) => endOfDate(date, part, options)));
var nearest = dual(isDateTimeArgs, (self, part, options) => mutate(self, (date) => {
  if (part === "milli") return;
  const millis2 = date.getTime();
  const start2 = new Date(millis2);
  startOfDate(start2, part, options);
  const startMillis = start2.getTime();
  const end2 = new Date(millis2);
  endOfDate(end2, part, options);
  const endMillis = end2.getTime() + 1;
  const diffStart = millis2 - startMillis;
  const diffEnd = endMillis - millis2;
  if (diffStart < diffEnd) {
    date.setTime(startMillis);
  } else {
    date.setTime(endMillis);
  }
}));
var intlTimeZone = (self) => {
  if (self._tag === "Named") {
    return self.id;
  }
  return offsetToString(self.offset);
};
var format2 = dual(isDateTimeArgs, (self, options) => {
  try {
    return new Intl.DateTimeFormat(options?.locale, {
      timeZone: self._tag === "Utc" ? "UTC" : intlTimeZone(self.zone),
      ...options
    }).format(self.epochMillis);
  } catch {
    return new Intl.DateTimeFormat(options?.locale, {
      timeZone: "UTC",
      ...options
    }).format(toDate(self));
  }
});
var formatLocal = dual(isDateTimeArgs, (self, options) => new Intl.DateTimeFormat(options?.locale, options).format(self.epochMillis));
var formatUtc = dual(isDateTimeArgs, (self, options) => new Intl.DateTimeFormat(options?.locale, {
  ...options,
  timeZone: "UTC"
}).format(self.epochMillis));
var formatIntl = dual(2, (self, format3) => format3.format(self.epochMillis));
var formatIso = (self) => toDateUtc(self).toISOString();
var formatIsoDate = (self) => toDate(self).toISOString().slice(0, 10);
var formatIsoDateUtc = (self) => toDateUtc(self).toISOString().slice(0, 10);
var formatIsoOffset = (self) => {
  const date = toDate(self);
  return self._tag === "Utc" ? date.toISOString() : `${date.toISOString().slice(0, -1)}${zonedOffsetIso(self)}`;
};
var formatIsoZoned = (self) => self.zone._tag === "Offset" ? formatIsoOffset(self) : `${formatIsoOffset(self)}[${self.zone.id}]`;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/String.js
var String_exports = {};
__export(String_exports, {
  Equivalence: () => Equivalence2,
  Order: () => Order3,
  at: () => at,
  camelToSnake: () => camelToSnake,
  capitalize: () => capitalize,
  charAt: () => charAt,
  charCodeAt: () => charCodeAt,
  codePointAt: () => codePointAt,
  concat: () => concat,
  empty: () => empty2,
  endsWith: () => endsWith,
  includes: () => includes,
  indexOf: () => indexOf,
  isEmpty: () => isEmpty,
  isNonEmpty: () => isNonEmpty3,
  isString: () => isString2,
  kebabToSnake: () => kebabToSnake,
  lastIndexOf: () => lastIndexOf,
  length: () => length,
  linesIterator: () => linesIterator,
  linesWithSeparators: () => linesWithSeparators,
  localeCompare: () => localeCompare,
  match: () => match4,
  matchAll: () => matchAll,
  normalize: () => normalize,
  padEnd: () => padEnd,
  padStart: () => padStart,
  pascalToSnake: () => pascalToSnake,
  repeat: () => repeat,
  replace: () => replace,
  replaceAll: () => replaceAll,
  search: () => search,
  slice: () => slice,
  snakeToCamel: () => snakeToCamel,
  snakeToKebab: () => snakeToKebab,
  snakeToPascal: () => snakeToPascal,
  split: () => split,
  startsWith: () => startsWith,
  stripMargin: () => stripMargin,
  stripMarginWith: () => stripMarginWith,
  substring: () => substring,
  takeLeft: () => takeLeft,
  takeRight: () => takeRight,
  toLocaleLowerCase: () => toLocaleLowerCase,
  toLocaleUpperCase: () => toLocaleUpperCase,
  toLowerCase: () => toLowerCase,
  toUpperCase: () => toUpperCase,
  trim: () => trim,
  trimEnd: () => trimEnd,
  trimStart: () => trimStart,
  uncapitalize: () => uncapitalize
});
var isString2 = isString;
var Equivalence2 = string;
var Order3 = string3;
var empty2 = "";
var concat = dual(2, (self, that) => self + that);
var toUpperCase = (self) => self.toUpperCase();
var toLowerCase = (self) => self.toLowerCase();
var capitalize = (self) => {
  if (self.length === 0) return self;
  return toUpperCase(self[0]) + self.slice(1);
};
var uncapitalize = (self) => {
  if (self.length === 0) return self;
  return toLowerCase(self[0]) + self.slice(1);
};
var replace = (searchValue, replaceValue) => (self) => self.replace(searchValue, replaceValue);
var trim = (self) => self.trim();
var trimStart = (self) => self.trimStart();
var trimEnd = (self) => self.trimEnd();
var slice = (start2, end2) => (self) => self.slice(start2, end2);
var isEmpty = (self) => self.length === 0;
var isNonEmpty3 = (self) => self.length > 0;
var length = (self) => self.length;
var split = dual(2, (self, separator) => {
  const out = self.split(separator);
  return isNonEmptyArray(out) ? out : [self];
});
var includes = (searchString, position) => (self) => self.includes(searchString, position);
var startsWith = (searchString, position) => (self) => self.startsWith(searchString, position);
var endsWith = (searchString, position) => (self) => self.endsWith(searchString, position);
var charCodeAt = dual(2, (self, index) => filter(some2(self.charCodeAt(index)), (charCode) => !isNaN(charCode)));
var substring = (start2, end2) => (self) => self.substring(start2, end2);
var at = dual(2, (self, index) => fromNullable(self.at(index)));
var charAt = dual(2, (self, index) => filter(some2(self.charAt(index)), isNonEmpty3));
var codePointAt = dual(2, (self, index) => fromNullable(self.codePointAt(index)));
var indexOf = (searchString) => (self) => filter(some2(self.indexOf(searchString)), greaterThanOrEqualTo3(0));
var lastIndexOf = (searchString) => (self) => filter(some2(self.lastIndexOf(searchString)), greaterThanOrEqualTo3(0));
var localeCompare = (that, locales, options) => (self) => sign(self.localeCompare(that, locales, options));
var match4 = (regexp) => (self) => fromNullable(self.match(regexp));
var matchAll = (regexp) => (self) => self.matchAll(regexp);
var normalize = (form) => (self) => self.normalize(form);
var padEnd = (maxLength, fillString) => (self) => self.padEnd(maxLength, fillString);
var padStart = (maxLength, fillString) => (self) => self.padStart(maxLength, fillString);
var repeat = (count3) => (self) => self.repeat(count3);
var replaceAll = (searchValue, replaceValue) => (self) => self.replaceAll(searchValue, replaceValue);
var search = dual(2, (self, regexp) => filter(some2(self.search(regexp)), greaterThanOrEqualTo3(0)));
var toLocaleLowerCase = (locale) => (self) => self.toLocaleLowerCase(locale);
var toLocaleUpperCase = (locale) => (self) => self.toLocaleUpperCase(locale);
var takeLeft = dual(2, (self, n) => self.slice(0, Math.max(n, 0)));
var takeRight = dual(2, (self, n) => self.slice(Math.max(0, self.length - Math.floor(n)), Infinity));
var CR = 13;
var LF = 10;
var linesIterator = (self) => linesSeparated(self, true);
var linesWithSeparators = (s) => linesSeparated(s, false);
var stripMarginWith = dual(2, (self, marginChar) => {
  let out = "";
  for (const line of linesWithSeparators(self)) {
    let index = 0;
    while (index < line.length && line.charAt(index) <= " ") {
      index = index + 1;
    }
    const stripped = index < line.length && line.charAt(index) === marginChar ? line.substring(index + 1) : line;
    out = out + stripped;
  }
  return out;
});
var stripMargin = (self) => stripMarginWith(self, "|");
var snakeToCamel = (self) => {
  let str = self[0];
  for (let i = 1; i < self.length; i++) {
    str += self[i] === "_" ? self[++i].toUpperCase() : self[i];
  }
  return str;
};
var snakeToPascal = (self) => {
  let str = self[0].toUpperCase();
  for (let i = 1; i < self.length; i++) {
    str += self[i] === "_" ? self[++i].toUpperCase() : self[i];
  }
  return str;
};
var snakeToKebab = (self) => self.replace(/_/g, "-");
var camelToSnake = (self) => self.replace(/([A-Z])/g, "_$1").toLowerCase();
var pascalToSnake = (self) => (self.slice(0, 1) + self.slice(1).replace(/([A-Z])/g, "_$1")).toLowerCase();
var kebabToSnake = (self) => self.replace(/-/g, "_");
var LinesIterator = class _LinesIterator {
  s;
  stripped;
  index;
  length;
  constructor(s, stripped = false) {
    this.s = s;
    this.stripped = stripped;
    this.index = 0;
    this.length = s.length;
  }
  next() {
    if (this.done) {
      return {
        done: true,
        value: void 0
      };
    }
    const start2 = this.index;
    while (!this.done && !isLineBreak(this.s[this.index])) {
      this.index = this.index + 1;
    }
    let end2 = this.index;
    if (!this.done) {
      const char = this.s[this.index];
      this.index = this.index + 1;
      if (!this.done && isLineBreak2(char, this.s[this.index])) {
        this.index = this.index + 1;
      }
      if (!this.stripped) {
        end2 = this.index;
      }
    }
    return {
      done: false,
      value: this.s.substring(start2, end2)
    };
  }
  [Symbol.iterator]() {
    return new _LinesIterator(this.s, this.stripped);
  }
  get done() {
    return this.index >= this.length;
  }
};
var isLineBreak = (char) => {
  const code = char.charCodeAt(0);
  return code === CR || code === LF;
};
var isLineBreak2 = (char0, char1) => char0.charCodeAt(0) === CR && char1.charCodeAt(0) === LF;
var linesSeparated = (self, stripped) => new LinesIterator(self, stripped);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Cron.js
var TypeId2 = Symbol.for("effect/Cron");
var CronProto = {
  [TypeId2]: TypeId2,
  [symbol2](that) {
    return isCron(that) && equals2(this, that);
  },
  [symbol]() {
    return pipe(hash(this.tz), combine(array2(fromIterable(this.seconds))), combine(array2(fromIterable(this.minutes))), combine(array2(fromIterable(this.hours))), combine(array2(fromIterable(this.days))), combine(array2(fromIterable(this.months))), combine(array2(fromIterable(this.weekdays))), cached(this));
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "Cron",
      tz: this.tz,
      seconds: fromIterable(this.seconds),
      minutes: fromIterable(this.minutes),
      hours: fromIterable(this.hours),
      days: fromIterable(this.days),
      months: fromIterable(this.months),
      weekdays: fromIterable(this.weekdays)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isCron = (u) => hasProperty(u, TypeId2);
var make8 = (values) => {
  const o = Object.create(CronProto);
  o.seconds = new Set(sort(values.seconds ?? [0], Order));
  o.minutes = new Set(sort(values.minutes, Order));
  o.hours = new Set(sort(values.hours, Order));
  o.days = new Set(sort(values.days, Order));
  o.months = new Set(sort(values.months, Order));
  o.weekdays = new Set(sort(values.weekdays, Order));
  o.tz = fromNullable(values.tz);
  const seconds = Array.from(o.seconds);
  const minutes = Array.from(o.minutes);
  const hours = Array.from(o.hours);
  const days = Array.from(o.days);
  const months = Array.from(o.months);
  const weekdays = Array.from(o.weekdays);
  o.first = {
    second: seconds[0] ?? 0,
    minute: minutes[0] ?? 0,
    hour: hours[0] ?? 0,
    day: days[0] ?? 1,
    month: (months[0] ?? 1) - 1,
    weekday: weekdays[0] ?? 0
  };
  o.next = {
    second: nextLookupTable(seconds, 60),
    minute: nextLookupTable(minutes, 60),
    hour: nextLookupTable(hours, 24),
    day: nextLookupTable(days, 32),
    month: nextLookupTable(months, 13),
    weekday: nextLookupTable(weekdays, 7)
  };
  return o;
};
var nextLookupTable = (values, size2) => {
  const result = new Array(size2).fill(void 0);
  if (values.length === 0) {
    return result;
  }
  let current = void 0;
  let index = values.length - 1;
  for (let i = size2 - 1; i >= 0; i--) {
    while (index >= 0 && values[index] >= i) {
      current = values[index--];
    }
    result[i] = current;
  }
  return result;
};
var ParseErrorTypeId = Symbol.for("effect/Cron/errors/ParseError");
var ParseError = class extends TaggedError("CronParseError") {
  /**
   * @since 2.0.0
   */
  [ParseErrorTypeId] = ParseErrorTypeId;
};
var isParseError = (u) => hasProperty(u, ParseErrorTypeId);
var parse = (cron2, tz) => {
  const segments = cron2.split(" ").filter(isNonEmpty3);
  if (segments.length !== 5 && segments.length !== 6) {
    return left(new ParseError({
      message: `Invalid number of segments in cron expression`,
      input: cron2
    }));
  }
  if (segments.length === 5) {
    segments.unshift("0");
  }
  const [seconds, minutes, hours, days, months, weekdays] = segments;
  const zone = tz === void 0 || isTimeZone(tz) ? right(tz) : fromOption(zoneFromString(tz), () => new ParseError({
    message: `Invalid time zone in cron expression`,
    input: tz
  }));
  return all({
    tz: zone,
    seconds: parseSegment(seconds, secondOptions),
    minutes: parseSegment(minutes, minuteOptions),
    hours: parseSegment(hours, hourOptions),
    days: parseSegment(days, dayOptions),
    months: parseSegment(months, monthOptions),
    weekdays: parseSegment(weekdays, weekdayOptions)
  }).pipe(map(make8));
};
var unsafeParse = (cron2, tz) => getOrThrowWith(parse(cron2, tz), identity);
var match5 = (cron2, date) => {
  const parts = unsafeMakeZoned(date, {
    timeZone: getOrUndefined(cron2.tz)
  }).pipe(toParts);
  if (cron2.seconds.size !== 0 && !cron2.seconds.has(parts.seconds)) {
    return false;
  }
  if (cron2.minutes.size !== 0 && !cron2.minutes.has(parts.minutes)) {
    return false;
  }
  if (cron2.hours.size !== 0 && !cron2.hours.has(parts.hours)) {
    return false;
  }
  if (cron2.months.size !== 0 && !cron2.months.has(parts.month)) {
    return false;
  }
  if (cron2.days.size === 0 && cron2.weekdays.size === 0) {
    return true;
  }
  if (cron2.weekdays.size === 0) {
    return cron2.days.has(parts.day);
  }
  if (cron2.days.size === 0) {
    return cron2.weekdays.has(parts.weekDay);
  }
  return cron2.days.has(parts.day) || cron2.weekdays.has(parts.weekDay);
};
var daysInMonth = (date) => new Date(Date.UTC(date.getUTCFullYear(), date.getUTCMonth() + 1, 0)).getUTCDate();
var next2 = (cron2, startFrom) => {
  const tz = getOrUndefined(cron2.tz);
  const zoned = unsafeMakeZoned(startFrom ?? /* @__PURE__ */ new Date(), {
    timeZone: tz
  });
  const utc = tz !== void 0 && isTimeZoneNamed(tz) && tz.id === "UTC";
  const adjustDst = utc ? constVoid : (current) => {
    const adjusted = unsafeMakeZoned(current, {
      timeZone: zoned.zone,
      adjustForTimeZone: true
    }).pipe(toDate);
    const drift = current.getTime() - adjusted.getTime();
    if (drift > 0) {
      current.setTime(current.getTime() + drift);
    }
  };
  const result = mutate(zoned, (current) => {
    current.setUTCSeconds(current.getUTCSeconds() + 1, 0);
    for (let i = 0; i < 1e4; i++) {
      if (cron2.seconds.size !== 0) {
        const currentSecond = current.getUTCSeconds();
        const nextSecond2 = cron2.next.second[currentSecond];
        if (nextSecond2 === void 0) {
          current.setUTCMinutes(current.getUTCMinutes() + 1, cron2.first.second);
          adjustDst(current);
          continue;
        }
        if (nextSecond2 > currentSecond) {
          current.setUTCSeconds(nextSecond2);
          adjustDst(current);
          continue;
        }
      }
      if (cron2.minutes.size !== 0) {
        const currentMinute = current.getUTCMinutes();
        const nextMinute2 = cron2.next.minute[currentMinute];
        if (nextMinute2 === void 0) {
          current.setUTCHours(current.getUTCHours() + 1, cron2.first.minute, cron2.first.second);
          adjustDst(current);
          continue;
        }
        if (nextMinute2 > currentMinute) {
          current.setUTCMinutes(nextMinute2, cron2.first.second);
          adjustDst(current);
          continue;
        }
      }
      if (cron2.hours.size !== 0) {
        const currentHour = current.getUTCHours();
        const nextHour2 = cron2.next.hour[currentHour];
        if (nextHour2 === void 0) {
          current.setUTCDate(current.getUTCDate() + 1);
          current.setUTCHours(cron2.first.hour, cron2.first.minute, cron2.first.second);
          adjustDst(current);
          continue;
        }
        if (nextHour2 > currentHour) {
          current.setUTCHours(nextHour2, cron2.first.minute, cron2.first.second);
          adjustDst(current);
          continue;
        }
      }
      if (cron2.weekdays.size !== 0 || cron2.days.size !== 0) {
        let a = Infinity;
        let b = Infinity;
        if (cron2.weekdays.size !== 0) {
          const currentWeekday = current.getUTCDay();
          const nextWeekday = cron2.next.weekday[currentWeekday];
          a = nextWeekday === void 0 ? 7 - currentWeekday + cron2.first.weekday : nextWeekday - currentWeekday;
        }
        if (cron2.days.size !== 0 && a !== 0) {
          const currentDay = current.getUTCDate();
          const nextDay2 = cron2.next.day[currentDay];
          b = nextDay2 === void 0 ? daysInMonth(current) - currentDay + cron2.first.day : nextDay2 - currentDay;
        }
        const addDays = Math.min(a, b);
        if (addDays !== 0) {
          current.setUTCDate(current.getUTCDate() + addDays);
          current.setUTCHours(cron2.first.hour, cron2.first.minute, cron2.first.second);
          adjustDst(current);
          continue;
        }
      }
      if (cron2.months.size !== 0) {
        const currentMonth = current.getUTCMonth() + 1;
        const nextMonth = cron2.next.month[currentMonth];
        if (nextMonth === void 0) {
          current.setUTCFullYear(current.getUTCFullYear() + 1);
          current.setUTCMonth(cron2.first.month, cron2.first.day);
          current.setUTCHours(cron2.first.hour, cron2.first.minute, cron2.first.second);
          adjustDst(current);
          continue;
        }
        if (nextMonth > currentMonth) {
          current.setUTCMonth(nextMonth - 1, cron2.first.day);
          current.setUTCHours(cron2.first.hour, cron2.first.minute, cron2.first.second);
          adjustDst(current);
          continue;
        }
      }
      return;
    }
    throw new Error("Unable to find next cron date");
  });
  return toDateUtc(result);
};
var sequence = function* (cron2, startFrom) {
  while (true) {
    yield startFrom = next2(cron2, startFrom);
  }
};
var Equivalence3 = make((self, that) => restrictionsEquals(self.seconds, that.seconds) && restrictionsEquals(self.minutes, that.minutes) && restrictionsEquals(self.hours, that.hours) && restrictionsEquals(self.days, that.days) && restrictionsEquals(self.months, that.months) && restrictionsEquals(self.weekdays, that.weekdays));
var restrictionsArrayEquals = array(number);
var restrictionsEquals = (self, that) => restrictionsArrayEquals(fromIterable(self), fromIterable(that));
var equals2 = dual(2, (self, that) => Equivalence3(self, that));
var secondOptions = {
  min: 0,
  max: 59
};
var minuteOptions = {
  min: 0,
  max: 59
};
var hourOptions = {
  min: 0,
  max: 23
};
var dayOptions = {
  min: 1,
  max: 31
};
var monthOptions = {
  min: 1,
  max: 12,
  aliases: {
    jan: 1,
    feb: 2,
    mar: 3,
    apr: 4,
    may: 5,
    jun: 6,
    jul: 7,
    aug: 8,
    sep: 9,
    oct: 10,
    nov: 11,
    dec: 12
  }
};
var weekdayOptions = {
  min: 0,
  max: 6,
  aliases: {
    sun: 0,
    mon: 1,
    tue: 2,
    wed: 3,
    thu: 4,
    fri: 5,
    sat: 6
  }
};
var parseSegment = (input, options) => {
  const capacity = options.max - options.min + 1;
  const values = /* @__PURE__ */ new Set();
  const fields = input.split(",");
  for (const field of fields) {
    const [raw, step3] = splitStep(field);
    if (raw === "*" && step3 === void 0) {
      return right(/* @__PURE__ */ new Set());
    }
    if (step3 !== void 0) {
      if (!Number.isInteger(step3)) {
        return left(new ParseError({
          message: `Expected step value to be a positive integer`,
          input
        }));
      }
      if (step3 < 1) {
        return left(new ParseError({
          message: `Expected step value to be greater than 0`,
          input
        }));
      }
      if (step3 > options.max) {
        return left(new ParseError({
          message: `Expected step value to be less than ${options.max}`,
          input
        }));
      }
    }
    if (raw === "*") {
      for (let i = options.min; i <= options.max; i += step3 ?? 1) {
        values.add(i);
      }
    } else {
      const [left2, right2] = splitRange(raw, options.aliases);
      if (!Number.isInteger(left2)) {
        return left(new ParseError({
          message: `Expected a positive integer`,
          input
        }));
      }
      if (left2 < options.min || left2 > options.max) {
        return left(new ParseError({
          message: `Expected a value between ${options.min} and ${options.max}`,
          input
        }));
      }
      if (right2 === void 0) {
        values.add(left2);
      } else {
        if (!Number.isInteger(right2)) {
          return left(new ParseError({
            message: `Expected a positive integer`,
            input
          }));
        }
        if (right2 < options.min || right2 > options.max) {
          return left(new ParseError({
            message: `Expected a value between ${options.min} and ${options.max}`,
            input
          }));
        }
        if (left2 > right2) {
          return left(new ParseError({
            message: `Invalid value range`,
            input
          }));
        }
        for (let i = left2; i <= right2; i += step3 ?? 1) {
          values.add(i);
        }
      }
    }
    if (values.size >= capacity) {
      return right(/* @__PURE__ */ new Set());
    }
  }
  return right(values);
};
var splitStep = (input) => {
  const seperator = input.indexOf("/");
  if (seperator !== -1) {
    return [input.slice(0, seperator), Number(input.slice(seperator + 1))];
  }
  return [input, void 0];
};
var splitRange = (input, aliases) => {
  const seperator = input.indexOf("-");
  if (seperator !== -1) {
    return [aliasOrValue(input.slice(0, seperator), aliases), aliasOrValue(input.slice(seperator + 1), aliases)];
  }
  return [aliasOrValue(input, aliases), void 0];
};
function aliasOrValue(field, aliases) {
  return aliases?.[field.toLocaleLowerCase()] ?? Number(field);
}

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Random.js
var Random_exports = {};
__export(Random_exports, {
  Random: () => Random,
  RandomTypeId: () => RandomTypeId2,
  choice: () => choice2,
  fixed: () => fixed2,
  make: () => make9,
  next: () => next3,
  nextBoolean: () => nextBoolean2,
  nextInt: () => nextInt2,
  nextIntBetween: () => nextIntBetween2,
  nextRange: () => nextRange2,
  randomWith: () => randomWith2,
  shuffle: () => shuffle2
});
var RandomTypeId2 = RandomTypeId;
var next3 = next;
var nextInt2 = nextInt;
var nextBoolean2 = nextBoolean;
var nextRange2 = nextRange;
var nextIntBetween2 = nextIntBetween;
var shuffle2 = shuffle;
var choice2 = choice;
var randomWith2 = randomWith;
var Random = randomTag;
var make9 = make3;
var fixed2 = fixed;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/schedule.js
var ScheduleSymbolKey = "effect/Schedule";
var ScheduleTypeId = Symbol.for(ScheduleSymbolKey);
var isSchedule = (u) => hasProperty(u, ScheduleTypeId);
var ScheduleDriverSymbolKey = "effect/ScheduleDriver";
var ScheduleDriverTypeId = Symbol.for(ScheduleDriverSymbolKey);
var defaultIterationMetadata = {
  start: 0,
  now: 0,
  input: void 0,
  output: void 0,
  elapsed: zero,
  elapsedSincePrevious: zero,
  recurrence: 0
};
var CurrentIterationMetadata = Reference()("effect/Schedule/CurrentIterationMetadata", {
  defaultValue: () => defaultIterationMetadata
});
var scheduleVariance = {
  /* c8 ignore next */
  _Out: (_) => _,
  /* c8 ignore next */
  _In: (_) => _,
  /* c8 ignore next */
  _R: (_) => _
};
var scheduleDriverVariance = {
  /* c8 ignore next */
  _Out: (_) => _,
  /* c8 ignore next */
  _In: (_) => _,
  /* c8 ignore next */
  _R: (_) => _
};
var ScheduleImpl = class {
  initial;
  step;
  [ScheduleTypeId] = scheduleVariance;
  constructor(initial, step3) {
    this.initial = initial;
    this.step = step3;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var updateInfo = (iterationMetaRef, now2, input, output) => update(iterationMetaRef, (prev) => prev.recurrence === 0 ? {
  now: now2,
  input,
  output,
  recurrence: prev.recurrence + 1,
  elapsed: zero,
  elapsedSincePrevious: zero,
  start: now2
} : {
  now: now2,
  input,
  output,
  recurrence: prev.recurrence + 1,
  elapsed: millis(now2 - prev.start),
  elapsedSincePrevious: millis(now2 - prev.now),
  start: prev.start
});
var ScheduleDriverImpl = class {
  schedule;
  ref;
  [ScheduleDriverTypeId] = scheduleDriverVariance;
  constructor(schedule2, ref) {
    this.schedule = schedule2;
    this.ref = ref;
  }
  get state() {
    return map3(get2(this.ref), (tuple) => tuple[1]);
  }
  get last() {
    return flatMap(get2(this.ref), ([element, _]) => {
      switch (element._tag) {
        case "None": {
          return failSync(() => new NoSuchElementException());
        }
        case "Some": {
          return succeed(element.value);
        }
      }
    });
  }
  iterationMeta = unsafeMake(defaultIterationMetadata);
  get reset() {
    return set(this.ref, [none(), this.schedule.initial]).pipe(zipLeft(set(this.iterationMeta, defaultIterationMetadata)));
  }
  next(input) {
    return pipe(map3(get2(this.ref), (tuple) => tuple[1]), flatMap((state) => pipe(currentTimeMillis, flatMap((now2) => pipe(suspend(() => this.schedule.step(now2, input, state)), flatMap(([state2, out, decision]) => {
      const setState = set(this.ref, [some2(out), state2]);
      if (isDone(decision)) {
        return setState.pipe(zipRight(fail2(none())));
      }
      const millis2 = start(decision.intervals) - now2;
      if (millis2 <= 0) {
        return setState.pipe(zipRight(updateInfo(this.iterationMeta, now2, input, out)), as(out));
      }
      const duration2 = millis(millis2);
      return pipe(setState, zipRight(updateInfo(this.iterationMeta, now2, input, out)), zipRight(sleep(duration2)), as(out));
    }))))));
  }
};
var makeWithState = (initial, step3) => new ScheduleImpl(initial, step3);
var addDelay = dual(2, (self, f) => addDelayEffect(self, (out) => sync(() => f(out))));
var addDelayEffect = dual(2, (self, f) => modifyDelayEffect(self, (out, duration2) => map3(f(out), (delay3) => sum(duration2, decode(delay3)))));
var andThen2 = dual(2, (self, that) => map4(andThenEither(self, that), merge));
var andThenEither = dual(2, (self, that) => makeWithState([self.initial, that.initial, true], (now2, input, state) => state[2] ? flatMap(self.step(now2, input, state[0]), ([lState, out, decision]) => {
  if (isDone(decision)) {
    return map3(that.step(now2, input, state[1]), ([rState, out2, decision2]) => [[lState, rState, false], right(out2), decision2]);
  }
  return succeed([[lState, state[1], true], left(out), decision]);
}) : map3(that.step(now2, input, state[1]), ([rState, out, decision]) => [[state[0], rState, false], right(out), decision])));
var as2 = dual(2, (self, out) => map4(self, () => out));
var asVoid2 = (self) => map4(self, constVoid);
var bothInOut = dual(2, (self, that) => makeWithState([self.initial, that.initial], (now2, [in1, in2], state) => zipWith(self.step(now2, in1, state[0]), that.step(now2, in2, state[1]), ([lState, out, lDecision], [rState, out2, rDecision]) => {
  if (isContinue(lDecision) && isContinue(rDecision)) {
    const interval = pipe(lDecision.intervals, union(rDecision.intervals));
    return [[lState, rState], [out, out2], _continue(interval)];
  }
  return [[lState, rState], [out, out2], done];
})));
var check = dual(2, (self, test) => checkEffect(self, (input, out) => sync(() => test(input, out))));
var checkEffect = dual(2, (self, test) => makeWithState(self.initial, (now2, input, state) => flatMap(self.step(now2, input, state), ([state2, out, decision]) => {
  if (isDone(decision)) {
    return succeed([state2, out, done]);
  }
  return map3(test(input, out), (cont) => cont ? [state2, out, decision] : [state2, out, done]);
})));
var collectAllInputs = () => collectAllOutputs(identity2());
var collectAllOutputs = (self) => reduce2(self, empty(), (outs, out) => pipe(outs, append(out)));
var collectUntil = (f) => collectAllOutputs(recurUntil(f));
var collectUntilEffect = (f) => collectAllOutputs(recurUntilEffect(f));
var collectWhile = (f) => collectAllOutputs(recurWhile(f));
var collectWhileEffect = (f) => collectAllOutputs(recurWhileEffect(f));
var compose = dual(2, (self, that) => makeWithState([self.initial, that.initial], (now2, input, state) => flatMap(self.step(now2, input, state[0]), ([lState, out, lDecision]) => map3(that.step(now2, out, state[1]), ([rState, out2, rDecision]) => isDone(lDecision) ? [[lState, rState], out2, done] : isDone(rDecision) ? [[lState, rState], out2, done] : [[lState, rState], out2, _continue(pipe(lDecision.intervals, max2(rDecision.intervals)))]))));
var mapInput = dual(2, (self, f) => mapInputEffect(self, (input2) => sync(() => f(input2))));
var mapInputContext2 = dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => mapInputContext(self.step(now2, input, state), f)));
var mapInputEffect = dual(2, (self, f) => makeWithState(self.initial, (now2, input2, state) => flatMap(f(input2), (input) => self.step(now2, input, state))));
var cron = (expression, tz) => {
  const parsed = isCron(expression) ? right(expression) : parse(expression, tz);
  return makeWithState([true, [Number.MIN_SAFE_INTEGER, 0, 0]], (now2, _, [initial, previous]) => {
    if (now2 < previous[0]) {
      return succeed([[false, previous], [previous[1], previous[2]], continueWith(make5(previous[1], previous[2]))]);
    }
    if (isLeft(parsed)) {
      return die2(parsed.left);
    }
    const cron2 = parsed.right;
    const date = new Date(now2);
    let next4;
    if (initial && match5(cron2, date)) {
      next4 = now2;
    }
    next4 = next2(cron2, date).getTime();
    const start2 = beginningOfSecond(next4);
    const end2 = endOfSecond(next4);
    return succeed([[false, [next4, start2, end2]], [start2, end2], continueWith(make5(start2, end2))]);
  });
};
var dayOfMonth = (day) => {
  return makeWithState([Number.NEGATIVE_INFINITY, 0], (now2, _, state) => {
    if (!Number.isInteger(day) || day < 1 || 31 < day) {
      return dieSync(() => new IllegalArgumentException(`Invalid argument in: dayOfMonth(${day}). Must be in range 1...31`));
    }
    const n = state[1];
    const initial = n === 0;
    const day0 = nextDayOfMonth(now2, day, initial);
    const start2 = beginningOfDay(day0);
    const end2 = endOfDay(day0);
    const interval = make5(start2, end2);
    return succeed([[end2, n + 1], n, continueWith(interval)]);
  });
};
var dayOfWeek = (day) => {
  return makeWithState([Number.MIN_SAFE_INTEGER, 0], (now2, _, state) => {
    if (!Number.isInteger(day) || day < 1 || 7 < day) {
      return dieSync(() => new IllegalArgumentException(`Invalid argument in: dayOfWeek(${day}). Must be in range 1 (Monday)...7 (Sunday)`));
    }
    const n = state[1];
    const initial = n === 0;
    const day0 = nextDay(now2, day, initial);
    const start2 = beginningOfDay(day0);
    const end2 = endOfDay(day0);
    const interval = make5(start2, end2);
    return succeed([[end2, n + 1], n, continueWith(interval)]);
  });
};
var delayed = dual(2, (self, f) => delayedEffect(self, (duration2) => sync(() => f(duration2))));
var delayedEffect = dual(2, (self, f) => modifyDelayEffect(self, (_, delay3) => f(delay3)));
var delayedSchedule = (schedule2) => addDelay(schedule2, (x) => x);
var delays = (self) => makeWithState(self.initial, (now2, input, state) => pipe(self.step(now2, input, state), flatMap(([state2, _, decision]) => {
  if (isDone(decision)) {
    return succeed([state2, zero, decision]);
  }
  return succeed([state2, millis(start(decision.intervals) - now2), decision]);
})));
var mapBoth2 = dual(2, (self, {
  onInput,
  onOutput
}) => map4(mapInput(self, onInput), onOutput));
var mapBothEffect = dual(2, (self, {
  onInput,
  onOutput
}) => mapEffect(mapInputEffect(self, onInput), onOutput));
var driver = (self) => pipe(make4([none(), self.initial]), map3((ref) => new ScheduleDriverImpl(self, ref)));
var duration = (durationInput) => {
  const duration2 = decode(durationInput);
  const durationMillis = toMillis(duration2);
  return makeWithState(true, (now2, _, state) => succeed(state ? [false, duration2, continueWith(after(now2 + durationMillis))] : [false, zero, done]));
};
var either2 = dual(2, (self, that) => union2(self, that));
var eitherWith = dual(3, (self, that, f) => unionWith(self, that, f));
var ensuring2 = dual(2, (self, finalizer) => makeWithState(self.initial, (now2, input, state) => flatMap(self.step(now2, input, state), ([state2, out, decision]) => isDone(decision) ? as(finalizer, [state2, out, decision]) : succeed([state2, out, decision]))));
var exponential = (baseInput, factor = 2) => {
  const base = decode(baseInput);
  return delayedSchedule(map4(forever2, (i) => times(base, Math.pow(factor, i))));
};
var fibonacci = (oneInput) => {
  const one = decode(oneInput);
  return delayedSchedule(pipe(unfold([one, one], ([a, b]) => [b, sum(a, b)]), map4((out) => out[0])));
};
var fixed3 = (intervalInput) => {
  const interval = decode(intervalInput);
  const intervalMillis = toMillis(interval);
  return makeWithState([none(), 0], (now2, _, [option3, n]) => sync(() => {
    switch (option3._tag) {
      case "None": {
        return [[some2([now2, now2 + intervalMillis]), n + 1], n, continueWith(after(now2 + intervalMillis))];
      }
      case "Some": {
        const [startMillis, lastRun] = option3.value;
        const runningBehind = now2 > lastRun + intervalMillis;
        const boundary = equals(interval, zero) ? interval : millis(intervalMillis - (now2 - startMillis) % intervalMillis);
        const sleepTime = equals(boundary, zero) ? interval : boundary;
        const nextRun = runningBehind ? now2 : now2 + toMillis(sleepTime);
        return [[some2([startMillis, nextRun]), n + 1], n, continueWith(after(nextRun))];
      }
    }
  }));
};
var fromDelay = (delay3) => duration(delay3);
var fromDelays = (delay3, ...delays2) => makeWithState([[delay3, ...delays2].map((_) => decode(_)), true], (now2, _, [durations, cont]) => sync(() => {
  if (cont) {
    const x = durations[0];
    const interval = after(now2 + toMillis(x));
    if (durations.length >= 2) {
      return [[durations.slice(1), true], x, continueWith(interval)];
    }
    const y = durations.slice(1);
    return [[[x, ...y], false], x, continueWith(interval)];
  }
  return [[durations, false], zero, done];
}));
var fromFunction = (f) => map4(identity2(), f);
var hourOfDay = (hour) => makeWithState([Number.NEGATIVE_INFINITY, 0], (now2, _, state) => {
  if (!Number.isInteger(hour) || hour < 0 || 23 < hour) {
    return dieSync(() => new IllegalArgumentException(`Invalid argument in: hourOfDay(${hour}). Must be in range 0...23`));
  }
  const n = state[1];
  const initial = n === 0;
  const hour0 = nextHour(now2, hour, initial);
  const start2 = beginningOfHour(hour0);
  const end2 = endOfHour(hour0);
  const interval = make5(start2, end2);
  return succeed([[end2, n + 1], n, continueWith(interval)]);
});
var identity2 = () => makeWithState(void 0, (now2, input, state) => succeed([state, input, continueWith(after(now2))]));
var intersect2 = dual(2, (self, that) => intersectWith(self, that, intersect));
var intersectWith = dual(3, (self, that, f) => makeWithState([self.initial, that.initial], (now2, input, state) => pipe(zipWith(self.step(now2, input, state[0]), that.step(now2, input, state[1]), (a, b) => [a, b]), flatMap(([[lState, out, lDecision], [rState, out2, rDecision]]) => {
  if (isContinue(lDecision) && isContinue(rDecision)) {
    return intersectWithLoop(self, that, input, lState, out, lDecision.intervals, rState, out2, rDecision.intervals, f);
  }
  return succeed([[lState, rState], [out, out2], done]);
}))));
var intersectWithLoop = (self, that, input, lState, out, lInterval, rState, out2, rInterval, f) => {
  const combined = f(lInterval, rInterval);
  if (isNonEmpty2(combined)) {
    return succeed([[lState, rState], [out, out2], _continue(combined)]);
  }
  if (pipe(lInterval, lessThan3(rInterval))) {
    return flatMap(self.step(end(lInterval), input, lState), ([lState2, out3, decision]) => {
      if (isDone(decision)) {
        return succeed([[lState2, rState], [out3, out2], done]);
      }
      return intersectWithLoop(self, that, input, lState2, out3, decision.intervals, rState, out2, rInterval, f);
    });
  }
  return flatMap(that.step(end(rInterval), input, rState), ([rState2, out22, decision]) => {
    if (isDone(decision)) {
      return succeed([[lState, rState2], [out, out22], done]);
    }
    return intersectWithLoop(self, that, input, lState, out, lInterval, rState2, out22, decision.intervals, f);
  });
};
var jittered = (self) => jitteredWith(self, {
  min: 0.8,
  max: 1.2
});
var jitteredWith = dual(2, (self, options) => {
  const {
    max: max4,
    min: min3
  } = Object.assign({
    min: 0.8,
    max: 1.2
  }, options);
  return delayedEffect(self, (duration2) => map3(next3, (random3) => {
    const d = toMillis(duration2);
    const jittered2 = d * min3 * (1 - random3) + d * max4 * random3;
    return millis(jittered2);
  }));
});
var linear = (baseInput) => {
  const base = decode(baseInput);
  return delayedSchedule(map4(forever2, (i) => times(base, i + 1)));
};
var map4 = dual(2, (self, f) => mapEffect(self, (out) => sync(() => f(out))));
var mapEffect = dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => flatMap(self.step(now2, input, state), ([state2, out, decision]) => map3(f(out), (out2) => [state2, out2, decision]))));
var minuteOfHour = (minute) => makeWithState([Number.MIN_SAFE_INTEGER, 0], (now2, _, state) => {
  if (!Number.isInteger(minute) || minute < 0 || 59 < minute) {
    return dieSync(() => new IllegalArgumentException(`Invalid argument in: minuteOfHour(${minute}). Must be in range 0...59`));
  }
  const n = state[1];
  const initial = n === 0;
  const minute0 = nextMinute(now2, minute, initial);
  const start2 = beginningOfMinute(minute0);
  const end2 = endOfMinute(minute0);
  const interval = make5(start2, end2);
  return succeed([[end2, n + 1], n, continueWith(interval)]);
});
var modifyDelay = dual(2, (self, f) => modifyDelayEffect(self, (out, duration2) => sync(() => f(out, duration2))));
var modifyDelayEffect = dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => flatMap(self.step(now2, input, state), ([state2, out, decision]) => {
  if (isDone(decision)) {
    return succeed([state2, out, decision]);
  }
  const intervals = decision.intervals;
  const delay3 = size(make5(now2, start(intervals)));
  return map3(f(out, delay3), (durationInput) => {
    const duration2 = decode(durationInput);
    const oldStart = start(intervals);
    const newStart = now2 + toMillis(duration2);
    const delta = newStart - oldStart;
    const newEnd = Math.max(0, end(intervals) + delta);
    const newInterval = make5(newStart, newEnd);
    return [state2, out, continueWith(newInterval)];
  });
})));
var onDecision = dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => flatMap(self.step(now2, input, state), ([state2, out, decision]) => as(f(out, decision), [state2, out, decision]))));
var passthrough = (self) => makeWithState(self.initial, (now2, input, state) => pipe(self.step(now2, input, state), map3(([state2, _, decision]) => [state2, input, decision])));
var provideContext2 = dual(2, (self, context3) => makeWithState(self.initial, (now2, input, state) => provideContext(self.step(now2, input, state), context3)));
var provideService2 = dual(3, (self, tag, service) => makeWithState(self.initial, (now2, input, state) => contextWithEffect((env) => provideContext(
  // @ts-expect-error
  self.step(now2, input, state),
  add(env, tag, service)
))));
var recurUntil = (f) => untilInput(identity2(), f);
var recurUntilEffect = (f) => untilInputEffect(identity2(), f);
var recurUntilOption = (pf) => untilOutput(map4(identity2(), pf), isSome);
var recurUpTo = (durationInput) => {
  const duration2 = decode(durationInput);
  return whileOutput(elapsed, (elapsed2) => lessThan2(elapsed2, duration2));
};
var recurWhile = (f) => whileInput(identity2(), f);
var recurWhileEffect = (f) => whileInputEffect(identity2(), f);
var recurs = (n) => whileOutput(forever2, (out) => out < n);
var reduce2 = dual(3, (self, zero2, f) => reduceEffect2(self, zero2, (z, out) => sync(() => f(z, out))));
var reduceEffect2 = dual(3, (self, zero2, f) => makeWithState([self.initial, zero2], (now2, input, [s, z]) => flatMap(self.step(now2, input, s), ([s2, out, decision]) => isDone(decision) ? succeed([[s2, z], z, decision]) : map3(f(z, out), (z2) => [[s2, z2], z, decision]))));
var repetitions = (self) => reduce2(self, 0, (n, _) => n + 1);
var resetAfter = dual(2, (self, durationInput) => {
  const duration2 = decode(durationInput);
  return pipe(self, intersect2(elapsed), resetWhen(([, time2]) => greaterThanOrEqualTo2(time2, duration2)), map4((out) => out[0]));
});
var resetWhen = dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => flatMap(self.step(now2, input, state), ([state2, out, decision]) => f(out) ? self.step(now2, input, self.initial) : succeed([state2, out, decision]))));
var run = dual(3, (self, now2, input) => pipe(runLoop(self, now2, fromIterable2(input), self.initial, empty()), map3((list) => reverse(list))));
var runLoop = (self, now2, inputs, state, acc) => {
  if (!isNonEmpty(inputs)) {
    return succeed(acc);
  }
  const input = headNonEmpty(inputs);
  const nextInputs = tailNonEmpty(inputs);
  return flatMap(self.step(now2, input, state), ([state2, out, decision]) => {
    if (isDone(decision)) {
      return sync(() => pipe(acc, prepend(out)));
    }
    return runLoop(self, start(decision.intervals), nextInputs, state2, prepend(acc, out));
  });
};
var secondOfMinute = (second) => makeWithState([Number.NEGATIVE_INFINITY, 0], (now2, _, state) => {
  if (!Number.isInteger(second) || second < 0 || 59 < second) {
    return dieSync(() => new IllegalArgumentException(`Invalid argument in: secondOfMinute(${second}). Must be in range 0...59`));
  }
  const n = state[1];
  const initial = n === 0;
  const second0 = nextSecond(now2, second, initial);
  const start2 = beginningOfSecond(second0);
  const end2 = endOfSecond(second0);
  const interval = make5(start2, end2);
  return succeed([[end2, n + 1], n, continueWith(interval)]);
});
var spaced = (duration2) => addDelay(forever2, () => duration2);
var succeed4 = (value) => map4(forever2, () => value);
var sync3 = (evaluate) => map4(forever2, evaluate);
var tapInput = dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => zipRight(f(input), self.step(now2, input, state))));
var tapOutput = dual(2, (self, f) => makeWithState(self.initial, (now2, input, state) => tap(self.step(now2, input, state), ([, out]) => f(out))));
var unfold = (initial, f) => makeWithState(initial, (now2, _, state) => sync(() => [f(state), state, continueWith(after(now2))]));
var union2 = dual(2, (self, that) => unionWith(self, that, union));
var unionWith = dual(3, (self, that, f) => makeWithState([self.initial, that.initial], (now2, input, state) => zipWith(self.step(now2, input, state[0]), that.step(now2, input, state[1]), ([lState, l, lDecision], [rState, r, rDecision]) => {
  if (isDone(lDecision) && isDone(rDecision)) {
    return [[lState, rState], [l, r], done];
  }
  if (isDone(lDecision) && isContinue(rDecision)) {
    return [[lState, rState], [l, r], _continue(rDecision.intervals)];
  }
  if (isContinue(lDecision) && isDone(rDecision)) {
    return [[lState, rState], [l, r], _continue(lDecision.intervals)];
  }
  if (isContinue(lDecision) && isContinue(rDecision)) {
    const combined = f(lDecision.intervals, rDecision.intervals);
    return [[lState, rState], [l, r], _continue(combined)];
  }
  throw new Error("BUG: Schedule.unionWith - please report an issue at https://github.com/Effect-TS/effect/issues");
})));
var untilInput = dual(2, (self, f) => check(self, (input, _) => !f(input)));
var untilInputEffect = dual(2, (self, f) => checkEffect(self, (input, _) => negate(f(input))));
var untilOutput = dual(2, (self, f) => check(self, (_, out) => !f(out)));
var untilOutputEffect = dual(2, (self, f) => checkEffect(self, (_, out) => negate(f(out))));
var upTo = dual(2, (self, duration2) => zipLeft2(self, recurUpTo(duration2)));
var whileInput = dual(2, (self, f) => check(self, (input, _) => f(input)));
var whileInputEffect = dual(2, (self, f) => checkEffect(self, (input, _) => f(input)));
var whileOutput = dual(2, (self, f) => check(self, (_, out) => f(out)));
var whileOutputEffect = dual(2, (self, f) => checkEffect(self, (_, out) => f(out)));
var windowed = (intervalInput) => {
  const interval = decode(intervalInput);
  const millis2 = toMillis(interval);
  return makeWithState([none(), 0], (now2, _, [option3, n]) => {
    switch (option3._tag) {
      case "None": {
        return succeed([[some2(now2), n + 1], n, continueWith(after(now2 + millis2))]);
      }
      case "Some": {
        return succeed([[some2(option3.value), n + 1], n, continueWith(after(now2 + (millis2 - (now2 - option3.value) % millis2)))]);
      }
    }
  });
};
var zipLeft2 = dual(2, (self, that) => map4(intersect2(self, that), (out) => out[0]));
var zipRight2 = dual(2, (self, that) => map4(intersect2(self, that), (out) => out[1]));
var zipWith2 = dual(3, (self, that, f) => map4(intersect2(self, that), ([out, out2]) => f(out, out2)));
var beginningOfSecond = (now2) => {
  const date = new Date(now2);
  return new Date(date.getFullYear(), date.getMonth(), date.getDate(), date.getHours(), date.getMinutes(), date.getSeconds(), 0).getTime();
};
var endOfSecond = (now2) => {
  const date = new Date(beginningOfSecond(now2));
  return date.setSeconds(date.getSeconds() + 1);
};
var nextSecond = (now2, second, initial) => {
  const date = new Date(now2);
  if (date.getSeconds() === second && initial) {
    return now2;
  }
  if (date.getSeconds() < second) {
    return date.setSeconds(second);
  }
  const newDate = new Date(date.setSeconds(second));
  return newDate.setTime(newDate.getTime() + 1e3 * 60);
};
var beginningOfMinute = (now2) => {
  const date = new Date(now2);
  return new Date(date.getFullYear(), date.getMonth(), date.getDate(), date.getHours(), date.getMinutes(), 0, 0).getTime();
};
var endOfMinute = (now2) => {
  const date = new Date(beginningOfMinute(now2));
  return date.setMinutes(date.getMinutes() + 1);
};
var nextMinute = (now2, minute, initial) => {
  const date = new Date(now2);
  if (date.getMinutes() === minute && initial) {
    return now2;
  }
  if (date.getMinutes() < minute) {
    return date.setMinutes(minute);
  }
  const newDate = new Date(date.setMinutes(minute));
  return newDate.setTime(newDate.getTime() + 1e3 * 60 * 60);
};
var beginningOfHour = (now2) => {
  const date = new Date(now2);
  return new Date(date.getFullYear(), date.getMonth(), date.getDate(), date.getHours(), 0, 0, 0).getTime();
};
var endOfHour = (now2) => {
  const date = new Date(beginningOfHour(now2));
  return date.setHours(date.getHours() + 1);
};
var nextHour = (now2, hour, initial) => {
  const date = new Date(now2);
  if (date.getHours() === hour && initial) {
    return now2;
  }
  if (date.getHours() < hour) {
    return date.setHours(hour);
  }
  const newDate = new Date(date.setHours(hour));
  return newDate.setTime(newDate.getTime() + 1e3 * 60 * 60 * 24);
};
var beginningOfDay = (now2) => {
  const date = new Date(now2);
  return new Date(date.getFullYear(), date.getMonth(), date.getDate(), 0, 0, 0, 0).getTime();
};
var endOfDay = (now2) => {
  const date = new Date(beginningOfDay(now2));
  return date.setDate(date.getDate() + 1);
};
var nextDay = (now2, dayOfWeek2, initial) => {
  const date = new Date(now2);
  if (date.getDay() === dayOfWeek2 && initial) {
    return now2;
  }
  const nextDayOfWeek = (7 + dayOfWeek2 - date.getDay()) % 7;
  return date.setDate(date.getDate() + (nextDayOfWeek === 0 ? 7 : nextDayOfWeek));
};
var nextDayOfMonth = (now2, day, initial) => {
  const date = new Date(now2);
  if (date.getDate() === day && initial) {
    return now2;
  }
  if (date.getDate() < day) {
    return date.setDate(day);
  }
  return findNextMonth(now2, day, 1);
};
var findNextMonth = (now2, day, months) => {
  const d = new Date(now2);
  const tmp1 = new Date(d.setDate(day));
  const tmp2 = new Date(tmp1.setMonth(tmp1.getMonth() + months));
  if (tmp2.getDate() === day) {
    const d2 = new Date(now2);
    const tmp3 = new Date(d2.setDate(day));
    return tmp3.setMonth(tmp3.getMonth() + months);
  }
  return findNextMonth(now2, day, months + 1);
};
var ScheduleDefectTypeId = Symbol.for("effect/Schedule/ScheduleDefect");
var ScheduleDefect = class {
  error;
  [ScheduleDefectTypeId];
  constructor(error2) {
    this.error = error2;
    this[ScheduleDefectTypeId] = ScheduleDefectTypeId;
  }
};
var isScheduleDefect = (u) => hasProperty(u, ScheduleDefectTypeId);
var scheduleDefectWrap = (self) => catchAll(self, (e) => die2(new ScheduleDefect(e)));
var scheduleDefectRefailCause = (cause3) => match(find(cause3, (_) => isDieType(_) && isScheduleDefect(_.defect) ? some2(_.defect) : none()), {
  onNone: () => cause3,
  onSome: (error2) => fail(error2.error)
});
var scheduleDefectRefail = (effect) => catchAllCause(effect, (cause3) => failCause(scheduleDefectRefailCause(cause3)));
var repeat_Effect = dual(2, (self, schedule2) => repeatOrElse_Effect(self, schedule2, (e, _) => fail2(e)));
var repeat_combined = dual(2, (self, options) => {
  if (isSchedule(options)) {
    return repeat_Effect(self, options);
  }
  const base = options.schedule ?? passthrough(forever2);
  const withWhile = options.while ? whileInputEffect(base, (a) => {
    const applied = options.while(a);
    if (typeof applied === "boolean") {
      return succeed(applied);
    }
    return scheduleDefectWrap(applied);
  }) : base;
  const withUntil = options.until ? untilInputEffect(withWhile, (a) => {
    const applied = options.until(a);
    if (typeof applied === "boolean") {
      return succeed(applied);
    }
    return scheduleDefectWrap(applied);
  }) : withWhile;
  const withTimes = options.times ? intersect2(withUntil, recurs(options.times)).pipe(map4((intersectionPair) => intersectionPair[0])) : withUntil;
  return scheduleDefectRefail(repeat_Effect(self, withTimes));
});
var repeatOrElse_Effect = dual(3, (self, schedule2, orElse3) => flatMap(driver(schedule2), (driver2) => matchEffect(self, {
  onFailure: (error2) => orElse3(error2, none()),
  onSuccess: (value) => repeatOrElseEffectLoop(provideServiceEffect(self, CurrentIterationMetadata, get2(driver2.iterationMeta)), driver2, (error2, option3) => provideServiceEffect(orElse3(error2, option3), CurrentIterationMetadata, get2(driver2.iterationMeta)), value)
})));
var repeatOrElseEffectLoop = (self, driver2, orElse3, value) => matchEffect(driver2.next(value), {
  onFailure: () => orDie(driver2.last),
  onSuccess: (b) => matchEffect(self, {
    onFailure: (error2) => orElse3(error2, some2(b)),
    onSuccess: (value2) => repeatOrElseEffectLoop(self, driver2, orElse3, value2)
  })
});
var retry_Effect = dual(2, (self, policy) => retryOrElse_Effect(self, policy, (e, _) => fail2(e)));
var retry_combined = dual(2, (self, options) => {
  if (isSchedule(options)) {
    return retry_Effect(self, options);
  }
  return scheduleDefectRefail(retry_Effect(self, fromRetryOptions(options)));
});
var fromRetryOptions = (options) => {
  const base = options.schedule ?? forever2;
  const withWhile = options.while ? whileInputEffect(base, (e) => {
    const applied = options.while(e);
    if (typeof applied === "boolean") {
      return succeed(applied);
    }
    return scheduleDefectWrap(applied);
  }) : base;
  const withUntil = options.until ? untilInputEffect(withWhile, (e) => {
    const applied = options.until(e);
    if (typeof applied === "boolean") {
      return succeed(applied);
    }
    return scheduleDefectWrap(applied);
  }) : withWhile;
  return options.times !== void 0 ? intersect2(withUntil, recurs(options.times)) : withUntil;
};
var retryOrElse_Effect = dual(3, (self, policy, orElse3) => flatMap(driver(policy), (driver2) => retryOrElse_EffectLoop(provideServiceEffect(self, CurrentIterationMetadata, get2(driver2.iterationMeta)), driver2, (e, out) => provideServiceEffect(orElse3(e, out), CurrentIterationMetadata, get2(driver2.iterationMeta)))));
var retryOrElse_EffectLoop = (self, driver2, orElse3) => {
  return catchAll(self, (e) => matchEffect(driver2.next(e), {
    onFailure: () => pipe(driver2.last, orDie, flatMap((out) => orElse3(e, out))),
    onSuccess: () => retryOrElse_EffectLoop(self, driver2, orElse3)
  }));
};
var schedule_Effect = dual(2, (self, schedule2) => scheduleFrom_Effect(self, void 0, schedule2));
var scheduleFrom_Effect = dual(3, (self, initial, schedule2) => flatMap(driver(schedule2), (driver2) => scheduleFrom_EffectLoop(provideServiceEffect(self, CurrentIterationMetadata, get2(driver2.iterationMeta)), initial, driver2)));
var scheduleFrom_EffectLoop = (self, initial, driver2) => matchEffect(driver2.next(initial), {
  onFailure: () => orDie(driver2.last),
  onSuccess: () => flatMap(self, (a) => scheduleFrom_EffectLoop(self, a, driver2))
});
var count2 = unfold(0, (n) => n + 1);
var elapsed = makeWithState(none(), (now2, _, state) => {
  switch (state._tag) {
    case "None": {
      return succeed([some2(now2), zero, continueWith(after(now2))]);
    }
    case "Some": {
      return succeed([some2(state.value), millis(now2 - state.value), continueWith(after(now2))]);
    }
  }
});
var forever2 = unfold(0, (n) => n + 1);
var once2 = asVoid2(recurs(1));
var stop = asVoid2(recurs(0));
var scheduleForked = dual(2, (self, schedule2) => forkScoped(schedule_Effect(self, schedule2)));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/executionPlan.js
var TypeId3 = Symbol.for("effect/ExecutionPlan");
var isExecutionPlan = (u) => hasProperty(u, TypeId3);
var withExecutionPlan = dual(2, (effect, plan) => suspend(() => {
  let i = 0;
  let result;
  return flatMap(whileLoop({
    while: () => i < plan.steps.length && (result === void 0 || isLeft(result)),
    body: () => {
      const step3 = plan.steps[i];
      let nextEffect = effect_provide(effect, step3.provide);
      if (result) {
        let attempted = false;
        const wrapped = nextEffect;
        nextEffect = suspend(() => {
          if (attempted) return wrapped;
          attempted = true;
          return result;
        });
        nextEffect = scheduleDefectRefail(retry_Effect(nextEffect, scheduleFromStep(step3, false)));
      } else {
        const schedule2 = scheduleFromStep(step3, true);
        nextEffect = schedule2 ? scheduleDefectRefail(retry_Effect(nextEffect, schedule2)) : nextEffect;
      }
      return either(nextEffect);
    },
    step: (either4) => {
      result = either4;
      i++;
    }
  }), () => result);
}));
var scheduleFromStep = (step3, first) => {
  if (!first) {
    return fromRetryOptions({
      schedule: step3.schedule ? step3.schedule : step3.attempts ? void 0 : once2,
      times: step3.attempts,
      while: step3.while
    });
  } else if (step3.attempts === 1 || !(step3.schedule || step3.attempts)) {
    return void 0;
  }
  return fromRetryOptions({
    schedule: step3.schedule,
    while: step3.while,
    times: step3.attempts ? step3.attempts - 1 : void 0
  });
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Request.js
var Request_exports = {};
__export(Request_exports, {
  Class: () => Class2,
  EntryTypeId: () => EntryTypeId,
  RequestTypeId: () => RequestTypeId2,
  TaggedClass: () => TaggedClass2,
  complete: () => complete2,
  completeEffect: () => completeEffect2,
  fail: () => fail4,
  failCause: () => failCause3,
  interruptWhenPossible: () => interruptWhenPossible2,
  isEntry: () => isEntry2,
  isRequest: () => isRequest2,
  makeCache: () => makeCache,
  makeEntry: () => makeEntry2,
  of: () => of2,
  succeed: () => succeed5,
  tagged: () => tagged2
});
var RequestTypeId2 = RequestTypeId;
var isRequest2 = isRequest;
var of2 = of;
var tagged2 = tagged;
var Class2 = Class;
var TaggedClass2 = TaggedClass;
var complete2 = complete;
var interruptWhenPossible2 = interruptWhenPossible;
var completeEffect2 = completeEffect;
var fail4 = fail3;
var failCause3 = failCause2;
var succeed5 = succeed2;
var makeCache = (options) => make6({
  ...options,
  lookup: () => map3(deferredMake(), (handle) => ({
    listeners: new Listeners(),
    handle
  }))
});
var EntryTypeId = Symbol.for("effect/RequestBlock.Entry");
var isEntry2 = isEntry;
var makeEntry2 = makeEntry;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Effect.js
var EffectTypeId2 = EffectTypeId;
var isEffect2 = isEffect;
var cachedWithTTL = cached2;
var cachedInvalidateWithTTL2 = cachedInvalidateWithTTL;
var cached3 = memoize;
var cachedFunction2 = cachedFunction;
var once3 = once;
var all3 = all2;
var allWith2 = allWith;
var allSuccesses2 = allSuccesses;
var dropUntil2 = dropUntil;
var dropWhile2 = dropWhile;
var takeUntil2 = takeUntil;
var takeWhile2 = takeWhile;
var every2 = every;
var exists2 = exists;
var filter3 = filter2;
var filterMap2 = filterMap;
var findFirst2 = findFirst;
var forEach2 = forEach;
var head2 = head;
var mergeAll2 = mergeAll;
var partition2 = partition;
var reduce3 = reduce;
var reduceWhile2 = reduceWhile;
var reduceRight2 = reduceRight;
var reduceEffect3 = reduceEffect;
var replicate2 = replicate;
var replicateEffect2 = replicateEffect;
var validateAll2 = validateAll;
var validateFirst2 = validateFirst;
var async = async_;
var asyncEffect2 = asyncEffect;
var custom2 = custom;
var withFiberRuntime2 = withFiberRuntime;
var fail5 = fail2;
var failSync2 = failSync;
var failCause4 = failCause;
var failCauseSync2 = failCauseSync;
var die3 = die2;
var dieMessage2 = dieMessage;
var dieSync2 = dieSync;
var gen2 = gen;
var never2 = never;
var none3 = none2;
var promise2 = promise;
var succeed6 = succeed;
var succeedNone2 = succeedNone;
var succeedSome2 = succeedSome;
var suspend2 = suspend;
var sync4 = sync;
var _void = void_;
var yieldNow2 = yieldNow;
var _catch2 = _catch;
var catchAll2 = catchAll;
var catchAllCause2 = catchAllCause;
var catchAllDefect2 = catchAllDefect;
var catchIf2 = catchIf;
var catchSome2 = catchSome;
var catchSomeCause2 = catchSomeCause;
var catchSomeDefect2 = catchSomeDefect;
var catchTag2 = catchTag;
var catchTags2 = catchTags;
var cause2 = cause;
var eventually2 = eventually;
var ignore2 = ignore;
var ignoreLogged2 = ignoreLogged;
var parallelErrors2 = parallelErrors;
var sandbox2 = sandbox;
var retry = retry_combined;
var withExecutionPlan2 = withExecutionPlan;
var retryOrElse = retryOrElse_Effect;
var try_2 = try_;
var tryMap2 = tryMap;
var tryMapPromise2 = tryMapPromise;
var tryPromise2 = tryPromise;
var unsandbox2 = unsandbox;
var allowInterrupt2 = allowInterrupt;
var checkInterruptible2 = checkInterruptible;
var disconnect2 = disconnect;
var interrupt2 = interrupt;
var interruptWith2 = interruptWith;
var interruptible2 = interruptible;
var interruptibleMask2 = interruptibleMask;
var onInterrupt2 = onInterrupt;
var uninterruptible2 = uninterruptible;
var uninterruptibleMask2 = uninterruptibleMask;
var liftPredicate2 = liftPredicate;
var as3 = as;
var asSome2 = asSome;
var asSomeError2 = asSomeError;
var asVoid3 = asVoid;
var flip2 = flip;
var flipWith2 = flipWith;
var map5 = map3;
var mapAccum2 = mapAccum;
var mapBoth3 = mapBoth;
var mapError2 = mapError;
var mapErrorCause2 = mapErrorCause;
var merge3 = merge2;
var negate2 = negate;
var acquireRelease2 = acquireRelease;
var acquireReleaseInterruptible2 = acquireReleaseInterruptible;
var acquireUseRelease2 = acquireUseRelease;
var addFinalizer2 = addFinalizer;
var ensuring3 = ensuring;
var onError2 = onError;
var onExit2 = onExit;
var parallelFinalizers2 = parallelFinalizers;
var sequentialFinalizers2 = sequentialFinalizers;
var finalizersMask2 = finalizersMask;
var scope2 = scope;
var scopeWith2 = scopeWith;
var scopedWith2 = scopedWith;
var scoped2 = scopedEffect;
var using2 = using;
var withEarlyRelease2 = withEarlyRelease;
var awaitAllChildren2 = awaitAllChildren;
var daemonChildren2 = daemonChildren;
var descriptor2 = descriptor;
var descriptorWith2 = descriptorWith;
var diffFiberRefs2 = diffFiberRefs;
var ensuringChild2 = ensuringChild;
var ensuringChildren2 = ensuringChildren;
var fiberId2 = fiberId;
var fiberIdWith2 = fiberIdWith;
var fork2 = fork;
var forkDaemon2 = forkDaemon;
var forkAll2 = forkAll;
var forkIn2 = forkIn;
var forkScoped2 = forkScoped;
var forkWithErrorHandler2 = forkWithErrorHandler;
var fromFiber2 = fromFiber;
var fromFiberEffect2 = fromFiberEffect;
var supervised2 = supervised;
var transplant2 = transplant;
var withConcurrency2 = withConcurrency;
var withScheduler2 = withScheduler;
var withSchedulingPriority2 = withSchedulingPriority;
var withMaxOpsBeforeYield2 = withMaxOpsBeforeYield;
var clock2 = clock;
var clockWith2 = clockWith;
var withClockScoped2 = withClockScoped;
var withClock2 = withClock;
var console2 = console;
var consoleWith2 = consoleWith;
var withConsoleScoped2 = withConsoleScoped;
var withConsole2 = withConsole;
var delay2 = delay;
var sleep2 = sleep;
var timed2 = timed;
var timedWith2 = timedWith;
var timeout2 = timeout;
var timeoutOption2 = timeoutOption;
var timeoutFail2 = timeoutFail;
var timeoutFailCause2 = timeoutFailCause;
var timeoutTo2 = timeoutTo;
var configProviderWith2 = configProviderWith;
var withConfigProvider2 = withConfigProvider;
var withConfigProviderScoped2 = withConfigProviderScoped;
var context2 = context;
var contextWith2 = contextWith;
var contextWithEffect2 = contextWithEffect;
var mapInputContext3 = mapInputContext;
var provide2 = effect_provide;
var provideService3 = provideService;
var provideServiceEffect2 = provideServiceEffect;
var serviceFunction2 = serviceFunction;
var serviceFunctionEffect2 = serviceFunctionEffect;
var serviceFunctions2 = serviceFunctions;
var serviceConstants2 = serviceConstants;
var serviceMembers2 = serviceMembers;
var serviceOption2 = serviceOption;
var serviceOptional2 = serviceOptional;
var updateService2 = updateService;
var Do2 = Do;
var bind2 = bind;
var bindAll2 = bindAll;
var bindTo2 = bindTo;
var let_2 = let_;
var option2 = option;
var either3 = either;
var exit2 = exit;
var intoDeferred2 = intoDeferred;
var if_2 = if_;
var filterOrDie2 = filterOrDie;
var filterOrDieMessage2 = filterOrDieMessage;
var filterOrElse2 = filterOrElse;
var filterOrFail2 = filterOrFail;
var filterEffectOrElse2 = filterEffectOrElse;
var filterEffectOrFail2 = filterEffectOrFail;
var unless2 = unless;
var unlessEffect2 = unlessEffect;
var when2 = when;
var whenEffect2 = whenEffect;
var whenFiberRef2 = whenFiberRef;
var whenRef2 = whenRef;
var flatMap2 = flatMap;
var andThen3 = andThen;
var flatten2 = flatten;
var race2 = race;
var raceAll2 = raceAll;
var raceFirst2 = raceFirst;
var raceWith2 = raceWith;
var summarized2 = summarized;
var tap2 = tap;
var tapBoth2 = tapBoth;
var tapDefect2 = tapDefect;
var tapError2 = tapError;
var tapErrorTag2 = tapErrorTag;
var tapErrorCause2 = tapErrorCause;
var forever3 = forever;
var iterate2 = iterate;
var loop2 = loop;
var repeat2 = repeat_combined;
var repeatN2 = repeatN;
var repeatOrElse = repeatOrElse_Effect;
var schedule = schedule_Effect;
var scheduleForked2 = scheduleForked;
var scheduleFrom = scheduleFrom_Effect;
var whileLoop2 = whileLoop;
var getFiberRefs = fiberRefs;
var inheritFiberRefs2 = inheritFiberRefs;
var locally = fiberRefLocally;
var locallyWith = fiberRefLocallyWith;
var locallyScoped = fiberRefLocallyScoped;
var locallyScopedWith = fiberRefLocallyScopedWith;
var patchFiberRefs2 = patchFiberRefs;
var setFiberRefs2 = setFiberRefs;
var updateFiberRefs2 = updateFiberRefs;
var isFailure2 = isFailure;
var isSuccess2 = isSuccess;
var match6 = match2;
var matchCause2 = matchCause;
var matchCauseEffect2 = matchCauseEffect;
var matchEffect2 = matchEffect;
var log3 = log;
var logWithLevel2 = (level, ...message) => logWithLevel(level)(...message);
var logTrace2 = logTrace;
var logDebug2 = logDebug;
var logInfo2 = logInfo;
var logWarning2 = logWarning;
var logError2 = logError;
var logFatal2 = logFatal;
var withLogSpan2 = withLogSpan;
var annotateLogs2 = annotateLogs;
var annotateLogsScoped2 = annotateLogsScoped;
var logAnnotations2 = logAnnotations;
var withUnhandledErrorLogLevel2 = withUnhandledErrorLogLevel;
var whenLogLevel2 = whenLogLevel;
var orDie2 = orDie;
var orDieWith2 = orDieWith;
var orElse2 = orElse;
var orElseFail2 = orElseFail;
var orElseSucceed2 = orElseSucceed;
var firstSuccessOf2 = firstSuccessOf;
var random2 = random;
var randomWith3 = randomWith;
var withRandom2 = withRandom;
var withRandomFixed = dual(2, (effect, values) => withRandom2(effect, fixed2(values)));
var withRandomScoped2 = withRandomScoped;
var runtime2 = runtime;
var getRuntimeFlags = runtimeFlags;
var patchRuntimeFlags = updateRuntimeFlags;
var withRuntimeFlagsPatch = withRuntimeFlags;
var withRuntimeFlagsPatchScoped = withRuntimeFlagsScoped;
var tagMetrics2 = tagMetrics;
var labelMetrics2 = labelMetrics;
var tagMetricsScoped2 = tagMetricsScoped;
var labelMetricsScoped2 = labelMetricsScoped;
var metricLabels2 = metricLabels;
var withMetric2 = withMetric;
var unsafeMakeSemaphore2 = unsafeMakeSemaphore;
var makeSemaphore2 = makeSemaphore;
var unsafeMakeLatch2 = unsafeMakeLatch;
var makeLatch2 = makeLatch;
var runFork = unsafeForkEffect;
var runCallback = unsafeRunEffect;
var runPromise = unsafeRunPromiseEffect;
var runPromiseExit = unsafeRunPromiseExitEffect;
var runSync = unsafeRunSyncEffect;
var runSyncExit = unsafeRunSyncExitEffect;
var validate2 = validate;
var validateWith2 = validateWith;
var zip = zipOptions;
var zipLeft3 = zipLeftOptions;
var zipRight3 = zipRightOptions;
var zipWith3 = zipWithOptions;
var ap = dual(2, (self, that) => zipWith3(self, that, (f, a) => f(a)));
var blocked2 = blocked;
var runRequestBlock2 = runRequestBlock;
var step2 = step;
var request = dual((args) => isRequest2(args[0]), fromRequest);
var cacheRequestResult = cacheRequest;
var withRequestBatching2 = withRequestBatching;
var withRequestCaching2 = withRequestCaching;
var withRequestCache2 = withRequestCache;
var tracer2 = tracer;
var tracerWith2 = tracerWith;
var withTracer2 = withTracer;
var withTracerScoped2 = withTracerScoped;
var withTracerEnabled2 = withTracerEnabled;
var withTracerTiming2 = withTracerTiming;
var annotateSpans2 = annotateSpans;
var annotateCurrentSpan2 = annotateCurrentSpan;
var currentSpan2 = currentSpan;
var currentParentSpan2 = currentParentSpan;
var spanAnnotations2 = spanAnnotations;
var spanLinks2 = spanLinks;
var linkSpans2 = linkSpans;
var linkSpanCurrent2 = linkSpanCurrent;
var makeSpan2 = makeSpan;
var makeSpanScoped2 = makeSpanScoped;
var useSpan2 = useSpan;
var withSpan2 = withSpan;
var functionWithSpan2 = functionWithSpan;
var withSpanScoped2 = withSpanScoped;
var withParentSpan2 = withParentSpan;
var fromNullable3 = fromNullable2;
var optionFromOptional2 = optionFromOptional;
var transposeOption = (self) => {
  return isNone(self) ? succeedNone2 : map5(self.value, some);
};
var transposeMapOption = dual(2, (self, f) => isNone(self) ? succeedNone2 : map5(f(self.value), some));
var makeTagProxy = (TagClass) => {
  const cache = /* @__PURE__ */ new Map();
  return new Proxy(TagClass, {
    get(target, prop, receiver) {
      if (prop in target) {
        return Reflect.get(target, prop, receiver);
      }
      if (cache.has(prop)) {
        return cache.get(prop);
      }
      const fn2 = (...args) => andThen(target, (s) => {
        if (typeof s[prop] === "function") {
          cache.set(prop, (...args2) => andThen(target, (s2) => s2[prop](...args2)));
          return s[prop](...args);
        }
        cache.set(prop, andThen(target, (s2) => s2[prop]));
        return s[prop];
      });
      const cn = andThen(target, (s) => s[prop]);
      Object.assign(fn2, cn);
      const apply = fn2.apply;
      const bind3 = fn2.bind;
      const call = fn2.call;
      const proto = Object.setPrototypeOf({}, Object.getPrototypeOf(cn));
      proto.apply = apply;
      proto.bind = bind3;
      proto.call = call;
      Object.setPrototypeOf(fn2, proto);
      cache.set(prop, fn2);
      return fn2;
    }
  });
};
var Tag = (id) => () => {
  const limit = Error.stackTraceLimit;
  Error.stackTraceLimit = 2;
  const creationError = new Error();
  Error.stackTraceLimit = limit;
  function TagClass() {
  }
  Object.setPrototypeOf(TagClass, TagProto);
  TagClass.key = id;
  Object.defineProperty(TagClass, "use", {
    get() {
      return (body) => andThen(this, body);
    }
  });
  Object.defineProperty(TagClass, "stack", {
    get() {
      return creationError.stack;
    }
  });
  return makeTagProxy(TagClass);
};
var Service = function() {
  return function() {
    const [id, maker] = arguments;
    const proxy = "accessors" in maker ? maker["accessors"] : false;
    const limit = Error.stackTraceLimit;
    Error.stackTraceLimit = 2;
    const creationError = new Error();
    Error.stackTraceLimit = limit;
    let patchState = "unchecked";
    const TagClass = function(service) {
      if (patchState === "unchecked") {
        const proto = Object.getPrototypeOf(service);
        if (proto === Object.prototype || proto === null) {
          patchState = "plain";
        } else {
          const selfProto = Object.getPrototypeOf(this);
          Object.setPrototypeOf(selfProto, proto);
          patchState = "patched";
        }
      }
      if (patchState === "plain") {
        Object.assign(this, service);
      } else if (patchState === "patched") {
        Object.setPrototypeOf(service, Object.getPrototypeOf(this));
        return service;
      }
    };
    TagClass.prototype._tag = id;
    Object.defineProperty(TagClass, "make", {
      get() {
        return (service) => new this(service);
      }
    });
    Object.defineProperty(TagClass, "use", {
      get() {
        return (body) => andThen(this, body);
      }
    });
    TagClass.key = id;
    Object.assign(TagClass, TagProto);
    Object.defineProperty(TagClass, "stack", {
      get() {
        return creationError.stack;
      }
    });
    const hasDeps = "dependencies" in maker && maker.dependencies.length > 0;
    const layerName = hasDeps ? "DefaultWithoutDependencies" : "Default";
    let layerCache;
    let isFunction = false;
    if ("effect" in maker) {
      isFunction = typeof maker.effect === "function";
      Object.defineProperty(TagClass, layerName, {
        get() {
          if (isFunction) {
            return (function() {
              return fromEffect(TagClass, map5(maker.effect.apply(null, arguments), (_) => new this(_)));
            }).bind(this);
          }
          return layerCache ??= fromEffect(TagClass, map5(maker.effect, (_) => new this(_)));
        }
      });
    } else if ("scoped" in maker) {
      isFunction = typeof maker.scoped === "function";
      Object.defineProperty(TagClass, layerName, {
        get() {
          if (isFunction) {
            return (function() {
              return scoped(TagClass, map5(maker.scoped.apply(null, arguments), (_) => new this(_)));
            }).bind(this);
          }
          return layerCache ??= scoped(TagClass, map5(maker.scoped, (_) => new this(_)));
        }
      });
    } else if ("sync" in maker) {
      Object.defineProperty(TagClass, layerName, {
        get() {
          return layerCache ??= sync2(TagClass, () => new this(maker.sync()));
        }
      });
    } else {
      Object.defineProperty(TagClass, layerName, {
        get() {
          return layerCache ??= succeed3(TagClass, new this(maker.succeed));
        }
      });
    }
    if (hasDeps) {
      let layerWithDepsCache;
      Object.defineProperty(TagClass, "Default", {
        get() {
          if (isFunction) {
            return function() {
              return provide(this.DefaultWithoutDependencies.apply(null, arguments), maker.dependencies);
            };
          }
          return layerWithDepsCache ??= provide(this.DefaultWithoutDependencies, maker.dependencies);
        }
      });
    }
    return proxy === true ? makeTagProxy(TagClass) : TagClass;
  };
};
var fn = function(nameOrBody, ...pipeables) {
  const limit = Error.stackTraceLimit;
  Error.stackTraceLimit = 2;
  const errorDef = new Error();
  Error.stackTraceLimit = limit;
  if (typeof nameOrBody !== "string") {
    return defineLength(nameOrBody.length, function(...args) {
      const limit2 = Error.stackTraceLimit;
      Error.stackTraceLimit = 2;
      const errorCall = new Error();
      Error.stackTraceLimit = limit2;
      return fnApply({
        self: this,
        body: nameOrBody,
        args,
        pipeables,
        spanName: "<anonymous>",
        spanOptions: {
          context: DisablePropagation.context(true)
        },
        errorDef,
        errorCall
      });
    });
  }
  const name = nameOrBody;
  const options = pipeables[0];
  return (body, ...pipeables2) => defineLength(body.length, {
    [name](...args) {
      const limit2 = Error.stackTraceLimit;
      Error.stackTraceLimit = 2;
      const errorCall = new Error();
      Error.stackTraceLimit = limit2;
      return fnApply({
        self: this,
        body,
        args,
        pipeables: pipeables2,
        spanName: name,
        spanOptions: options,
        errorDef,
        errorCall
      });
    }
  }[name]);
};
function defineLength(length2, fn2) {
  return Object.defineProperty(fn2, "length", {
    value: length2,
    configurable: true
  });
}
function fnApply(options) {
  let effect;
  let fnError = void 0;
  if (isGeneratorFunction(options.body)) {
    effect = fromIterator(() => options.body.apply(options.self, options.args));
  } else {
    try {
      effect = options.body.apply(options.self, options.args);
    } catch (error2) {
      fnError = error2;
      effect = die3(error2);
    }
  }
  if (options.pipeables.length > 0) {
    try {
      for (const x of options.pipeables) {
        effect = x(effect, ...options.args);
      }
    } catch (error2) {
      effect = fnError ? failCause4(sequential(die(fnError), die(error2))) : die3(error2);
    }
  }
  let cache = false;
  const captureStackTrace = () => {
    if (cache !== false) {
      return cache;
    }
    if (options.errorCall.stack) {
      const stackDef = options.errorDef.stack.trim().split("\n");
      const stackCall = options.errorCall.stack.trim().split("\n");
      let endStackDef = stackDef.slice(2).join("\n").trim();
      if (!endStackDef.includes(`(`)) {
        endStackDef = endStackDef.replace(/at (.*)/, "at ($1)");
      }
      let endStackCall = stackCall.slice(2).join("\n").trim();
      if (!endStackCall.includes(`(`)) {
        endStackCall = endStackCall.replace(/at (.*)/, "at ($1)");
      }
      cache = `${endStackDef}
${endStackCall}`;
      return cache;
    }
  };
  const opts = options.spanOptions && "captureStackTrace" in options.spanOptions ? options.spanOptions : {
    captureStackTrace,
    ...options.spanOptions
  };
  return withSpan2(effect, options.spanName, opts);
}
var fnUntraced2 = fnUntraced;
var ensureSuccessType = () => (effect) => effect;
var ensureErrorType = () => (effect) => effect;
var ensureRequirementsType = () => (effect) => effect;

export {
  consoleWith,
  withConsole,
  setConsole,
  assert,
  clear,
  count,
  countReset,
  debug,
  dir,
  dirxml,
  error,
  group,
  info,
  log2 as log,
  table,
  time,
  timeLog,
  trace,
  warn,
  withGroup,
  withTime,
  TypeId,
  TimeZoneTypeId,
  isDateTime,
  isTimeZone,
  isTimeZoneOffset,
  isTimeZoneNamed,
  isUtc,
  isZoned,
  Equivalence,
  Order2 as Order,
  clamp2 as clamp,
  unsafeFromDate,
  unsafeMake2 as unsafeMake,
  unsafeMakeZoned,
  makeZoned,
  make7 as make,
  makeZonedFromString,
  now,
  nowAsDate,
  unsafeNow,
  toUtc,
  setZone,
  setZoneOffset,
  zoneUnsafeMakeNamed,
  zoneMakeOffset,
  zoneMakeNamed,
  zoneMakeNamedEffect,
  zoneMakeLocal,
  zoneFromString,
  zoneToString,
  setZoneNamed,
  unsafeSetZoneNamed,
  distance,
  distanceDurationEither,
  distanceDuration,
  min2 as min,
  max3 as max,
  greaterThan2 as greaterThan,
  greaterThanOrEqualTo4 as greaterThanOrEqualTo,
  lessThan4 as lessThan,
  lessThanOrEqualTo2 as lessThanOrEqualTo,
  between2 as between,
  isFuture,
  unsafeIsFuture,
  isPast,
  unsafeIsPast,
  toDateUtc,
  toDate,
  zonedOffset,
  zonedOffsetIso,
  toEpochMillis,
  removeTime,
  toParts,
  toPartsUtc,
  getPartUtc,
  getPart,
  setParts,
  setPartsUtc,
  mutate,
  mutateUtc,
  mapEpochMillis,
  withDate,
  withDateUtc,
  match3 as match,
  addDuration,
  subtractDuration,
  add2 as add,
  subtract,
  startOf,
  endOf,
  nearest,
  format2 as format,
  formatLocal,
  formatUtc,
  formatIntl,
  formatIso,
  formatIsoDate,
  formatIsoDateUtc,
  formatIsoOffset,
  formatIsoZoned,
  Equivalence2,
  capitalize,
  uncapitalize,
  isNonEmpty3 as isNonEmpty,
  String_exports,
  Cron_exports,
  Random_exports,
  ScheduleTypeId,
  isSchedule,
  ScheduleDriverTypeId,
  CurrentIterationMetadata,
  makeWithState,
  addDelay,
  addDelayEffect,
  andThen2 as andThen,
  andThenEither,
  as2 as as,
  asVoid2 as asVoid,
  bothInOut,
  check,
  checkEffect,
  collectAllInputs,
  collectAllOutputs,
  collectUntil,
  collectUntilEffect,
  collectWhile,
  collectWhileEffect,
  compose,
  mapInput,
  mapInputContext2 as mapInputContext,
  mapInputEffect,
  cron,
  dayOfMonth,
  dayOfWeek,
  delayed,
  delayedEffect,
  delayedSchedule,
  delays,
  mapBoth2 as mapBoth,
  mapBothEffect,
  driver,
  duration,
  either2 as either,
  eitherWith,
  ensuring2 as ensuring,
  exponential,
  fibonacci,
  fixed3 as fixed,
  fromDelay,
  fromDelays,
  fromFunction,
  hourOfDay,
  identity2 as identity,
  intersect2 as intersect,
  intersectWith,
  jittered,
  jitteredWith,
  linear,
  map4 as map,
  mapEffect,
  minuteOfHour,
  modifyDelay,
  modifyDelayEffect,
  onDecision,
  passthrough,
  provideContext2 as provideContext,
  provideService2 as provideService,
  recurUntil,
  recurUntilEffect,
  recurUntilOption,
  recurUpTo,
  recurWhile,
  recurWhileEffect,
  recurs,
  reduce2 as reduce,
  reduceEffect2 as reduceEffect,
  repetitions,
  resetAfter,
  resetWhen,
  run,
  secondOfMinute,
  spaced,
  succeed4 as succeed,
  sync3 as sync,
  tapInput,
  tapOutput,
  unfold,
  union2 as union,
  unionWith,
  untilInput,
  untilInputEffect,
  untilOutput,
  untilOutputEffect,
  upTo,
  whileInput,
  whileInputEffect,
  whileOutput,
  whileOutputEffect,
  windowed,
  zipLeft2 as zipLeft,
  zipRight2 as zipRight,
  zipWith2 as zipWith,
  scheduleDefectRefailCause,
  retry_Effect,
  schedule_Effect,
  count2,
  elapsed,
  forever2 as forever,
  once2 as once,
  stop,
  scheduleForked,
  TypeId3 as TypeId2,
  isExecutionPlan,
  scheduleFromStep,
  Class2 as Class,
  Request_exports,
  EffectTypeId2 as EffectTypeId,
  isEffect2 as isEffect,
  cachedWithTTL,
  cachedInvalidateWithTTL2 as cachedInvalidateWithTTL,
  cached3 as cached,
  cachedFunction2 as cachedFunction,
  once3 as once2,
  all3 as all,
  allWith2 as allWith,
  allSuccesses2 as allSuccesses,
  dropUntil2 as dropUntil,
  dropWhile2 as dropWhile,
  takeUntil2 as takeUntil,
  takeWhile2 as takeWhile,
  every2 as every,
  exists2 as exists,
  filter3 as filter,
  filterMap2 as filterMap,
  findFirst2 as findFirst,
  forEach2 as forEach,
  head2 as head,
  mergeAll2 as mergeAll,
  partition2 as partition,
  reduce3 as reduce2,
  reduceWhile2 as reduceWhile,
  reduceRight2 as reduceRight,
  reduceEffect3 as reduceEffect2,
  replicate2 as replicate,
  replicateEffect2 as replicateEffect,
  validateAll2 as validateAll,
  validateFirst2 as validateFirst,
  async,
  asyncEffect2 as asyncEffect,
  custom2 as custom,
  withFiberRuntime2 as withFiberRuntime,
  fail5 as fail,
  failSync2 as failSync,
  failCause4 as failCause,
  failCauseSync2 as failCauseSync,
  die3 as die,
  dieMessage2 as dieMessage,
  dieSync2 as dieSync,
  gen2 as gen,
  never2 as never,
  none3 as none,
  promise2 as promise,
  succeed6 as succeed2,
  succeedNone2 as succeedNone,
  succeedSome2 as succeedSome,
  suspend2 as suspend,
  sync4 as sync2,
  _void,
  yieldNow2 as yieldNow,
  _catch2 as _catch,
  catchAll2 as catchAll,
  catchAllCause2 as catchAllCause,
  catchAllDefect2 as catchAllDefect,
  catchIf2 as catchIf,
  catchSome2 as catchSome,
  catchSomeCause2 as catchSomeCause,
  catchSomeDefect2 as catchSomeDefect,
  catchTag2 as catchTag,
  catchTags2 as catchTags,
  cause2 as cause,
  eventually2 as eventually,
  ignore2 as ignore,
  ignoreLogged2 as ignoreLogged,
  parallelErrors2 as parallelErrors,
  sandbox2 as sandbox,
  retry,
  withExecutionPlan2 as withExecutionPlan,
  retryOrElse,
  try_2 as try_,
  tryMap2 as tryMap,
  tryMapPromise2 as tryMapPromise,
  tryPromise2 as tryPromise,
  unsandbox2 as unsandbox,
  allowInterrupt2 as allowInterrupt,
  checkInterruptible2 as checkInterruptible,
  disconnect2 as disconnect,
  interrupt2 as interrupt,
  interruptWith2 as interruptWith,
  interruptible2 as interruptible,
  interruptibleMask2 as interruptibleMask,
  onInterrupt2 as onInterrupt,
  uninterruptible2 as uninterruptible,
  uninterruptibleMask2 as uninterruptibleMask,
  liftPredicate2 as liftPredicate,
  as3 as as2,
  asSome2 as asSome,
  asSomeError2 as asSomeError,
  asVoid3 as asVoid2,
  flip2 as flip,
  flipWith2 as flipWith,
  map5 as map2,
  mapAccum2 as mapAccum,
  mapBoth3 as mapBoth2,
  mapError2 as mapError,
  mapErrorCause2 as mapErrorCause,
  merge3 as merge,
  negate2 as negate,
  acquireRelease2 as acquireRelease,
  acquireReleaseInterruptible2 as acquireReleaseInterruptible,
  acquireUseRelease2 as acquireUseRelease,
  addFinalizer2 as addFinalizer,
  ensuring3 as ensuring2,
  onError2 as onError,
  onExit2 as onExit,
  parallelFinalizers2 as parallelFinalizers,
  sequentialFinalizers2 as sequentialFinalizers,
  finalizersMask2 as finalizersMask,
  scope2 as scope,
  scopeWith2 as scopeWith,
  scopedWith2 as scopedWith,
  scoped2 as scoped,
  using2 as using,
  withEarlyRelease2 as withEarlyRelease,
  awaitAllChildren2 as awaitAllChildren,
  daemonChildren2 as daemonChildren,
  descriptor2 as descriptor,
  descriptorWith2 as descriptorWith,
  diffFiberRefs2 as diffFiberRefs,
  ensuringChild2 as ensuringChild,
  ensuringChildren2 as ensuringChildren,
  fiberId2 as fiberId,
  fiberIdWith2 as fiberIdWith,
  fork2 as fork,
  forkDaemon2 as forkDaemon,
  forkAll2 as forkAll,
  forkIn2 as forkIn,
  forkScoped2 as forkScoped,
  forkWithErrorHandler2 as forkWithErrorHandler,
  fromFiber2 as fromFiber,
  fromFiberEffect2 as fromFiberEffect,
  supervised2 as supervised,
  transplant2 as transplant,
  withConcurrency2 as withConcurrency,
  withScheduler2 as withScheduler,
  withSchedulingPriority2 as withSchedulingPriority,
  withMaxOpsBeforeYield2 as withMaxOpsBeforeYield,
  clock2 as clock,
  clockWith2 as clockWith,
  withClockScoped2 as withClockScoped,
  withClock2 as withClock,
  console2 as console,
  consoleWith2,
  withConsoleScoped2 as withConsoleScoped,
  withConsole2,
  delay2 as delay,
  sleep2 as sleep,
  timed2 as timed,
  timedWith2 as timedWith,
  timeout2 as timeout,
  timeoutOption2 as timeoutOption,
  timeoutFail2 as timeoutFail,
  timeoutFailCause2 as timeoutFailCause,
  timeoutTo2 as timeoutTo,
  configProviderWith2 as configProviderWith,
  withConfigProvider2 as withConfigProvider,
  withConfigProviderScoped2 as withConfigProviderScoped,
  context2 as context,
  contextWith2 as contextWith,
  contextWithEffect2 as contextWithEffect,
  mapInputContext3 as mapInputContext2,
  provide2 as provide,
  provideService3 as provideService2,
  provideServiceEffect2 as provideServiceEffect,
  serviceFunction2 as serviceFunction,
  serviceFunctionEffect2 as serviceFunctionEffect,
  serviceFunctions2 as serviceFunctions,
  serviceConstants2 as serviceConstants,
  serviceMembers2 as serviceMembers,
  serviceOption2 as serviceOption,
  serviceOptional2 as serviceOptional,
  updateService2 as updateService,
  Do2 as Do,
  bind2 as bind,
  bindAll2 as bindAll,
  bindTo2 as bindTo,
  let_2 as let_,
  option2 as option,
  either3 as either2,
  exit2 as exit,
  intoDeferred2 as intoDeferred,
  if_2 as if_,
  filterOrDie2 as filterOrDie,
  filterOrDieMessage2 as filterOrDieMessage,
  filterOrElse2 as filterOrElse,
  filterOrFail2 as filterOrFail,
  filterEffectOrElse2 as filterEffectOrElse,
  filterEffectOrFail2 as filterEffectOrFail,
  unless2 as unless,
  unlessEffect2 as unlessEffect,
  when2 as when,
  whenEffect2 as whenEffect,
  whenFiberRef2 as whenFiberRef,
  whenRef2 as whenRef,
  flatMap2 as flatMap,
  andThen3 as andThen2,
  flatten2 as flatten,
  race2 as race,
  raceAll2 as raceAll,
  raceFirst2 as raceFirst,
  raceWith2 as raceWith,
  summarized2 as summarized,
  tap2 as tap,
  tapBoth2 as tapBoth,
  tapDefect2 as tapDefect,
  tapError2 as tapError,
  tapErrorTag2 as tapErrorTag,
  tapErrorCause2 as tapErrorCause,
  forever3 as forever2,
  iterate2 as iterate,
  loop2 as loop,
  repeat2 as repeat,
  repeatN2 as repeatN,
  repeatOrElse,
  schedule,
  scheduleForked2,
  scheduleFrom,
  whileLoop2 as whileLoop,
  getFiberRefs,
  inheritFiberRefs2 as inheritFiberRefs,
  locally,
  locallyWith,
  locallyScoped,
  locallyScopedWith,
  patchFiberRefs2 as patchFiberRefs,
  setFiberRefs2 as setFiberRefs,
  updateFiberRefs2 as updateFiberRefs,
  isFailure2 as isFailure,
  isSuccess2 as isSuccess,
  match6 as match2,
  matchCause2 as matchCause,
  matchCauseEffect2 as matchCauseEffect,
  matchEffect2 as matchEffect,
  log3 as log2,
  logWithLevel2 as logWithLevel,
  logTrace2 as logTrace,
  logDebug2 as logDebug,
  logInfo2 as logInfo,
  logWarning2 as logWarning,
  logError2 as logError,
  logFatal2 as logFatal,
  withLogSpan2 as withLogSpan,
  annotateLogs2 as annotateLogs,
  annotateLogsScoped2 as annotateLogsScoped,
  logAnnotations2 as logAnnotations,
  withUnhandledErrorLogLevel2 as withUnhandledErrorLogLevel,
  whenLogLevel2 as whenLogLevel,
  orDie2 as orDie,
  orDieWith2 as orDieWith,
  orElse2 as orElse,
  orElseFail2 as orElseFail,
  orElseSucceed2 as orElseSucceed,
  firstSuccessOf2 as firstSuccessOf,
  random2 as random,
  randomWith3 as randomWith,
  withRandom2 as withRandom,
  withRandomFixed,
  withRandomScoped2 as withRandomScoped,
  runtime2 as runtime,
  getRuntimeFlags,
  patchRuntimeFlags,
  withRuntimeFlagsPatch,
  withRuntimeFlagsPatchScoped,
  tagMetrics2 as tagMetrics,
  labelMetrics2 as labelMetrics,
  tagMetricsScoped2 as tagMetricsScoped,
  labelMetricsScoped2 as labelMetricsScoped,
  metricLabels2 as metricLabels,
  withMetric2 as withMetric,
  unsafeMakeSemaphore2 as unsafeMakeSemaphore,
  makeSemaphore2 as makeSemaphore,
  unsafeMakeLatch2 as unsafeMakeLatch,
  makeLatch2 as makeLatch,
  runFork,
  runCallback,
  runPromise,
  runPromiseExit,
  runSync,
  runSyncExit,
  validate2 as validate,
  validateWith2 as validateWith,
  zip,
  zipLeft3 as zipLeft2,
  zipRight3 as zipRight2,
  zipWith3 as zipWith2,
  ap,
  blocked2 as blocked,
  runRequestBlock2 as runRequestBlock,
  step2 as step,
  request,
  cacheRequestResult,
  withRequestBatching2 as withRequestBatching,
  withRequestCaching2 as withRequestCaching,
  withRequestCache2 as withRequestCache,
  tracer2 as tracer,
  tracerWith2 as tracerWith,
  withTracer2 as withTracer,
  withTracerScoped2 as withTracerScoped,
  withTracerEnabled2 as withTracerEnabled,
  withTracerTiming2 as withTracerTiming,
  annotateSpans2 as annotateSpans,
  annotateCurrentSpan2 as annotateCurrentSpan,
  currentSpan2 as currentSpan,
  currentParentSpan2 as currentParentSpan,
  spanAnnotations2 as spanAnnotations,
  spanLinks2 as spanLinks,
  linkSpans2 as linkSpans,
  linkSpanCurrent2 as linkSpanCurrent,
  makeSpan2 as makeSpan,
  makeSpanScoped2 as makeSpanScoped,
  useSpan2 as useSpan,
  withSpan2 as withSpan,
  functionWithSpan2 as functionWithSpan,
  withSpanScoped2 as withSpanScoped,
  withParentSpan2 as withParentSpan,
  fromNullable3 as fromNullable,
  optionFromOptional2 as optionFromOptional,
  transposeOption,
  transposeMapOption,
  Tag,
  Service,
  fn,
  fnUntraced2 as fnUntraced,
  ensureSuccessType,
  ensureErrorType,
  ensureRequirementsType,
  Effect_exports
};
//# sourceMappingURL=chunk-LXSJW2UJ.js.map
