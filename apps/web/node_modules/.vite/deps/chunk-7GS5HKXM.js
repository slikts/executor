import {
  allocate,
  append2 as append,
  appendAll2 as appendAll,
  drop,
  drop2,
  empty3 as empty,
  empty4 as empty2,
  fromIterable,
  fromIterable2,
  getEquivalence3 as getEquivalence,
  head3 as head,
  headNonEmpty,
  headNonEmpty2,
  isEmpty,
  isLeft,
  isNonEmpty,
  isNonEmptyArray2 as isNonEmptyArray,
  left,
  make3 as make2,
  of,
  of2,
  partitionMap,
  prepend2 as prepend,
  reduce2 as reduce,
  reverse2 as reverse,
  right,
  tailNonEmpty,
  tailNonEmpty2,
  toReadonlyArray,
  unsafeFromArray,
  unsafeGet2 as unsafeGet,
  unsafeHead2 as unsafeHead
} from "./chunk-JDLTRRA5.js";
import {
  empty as empty3,
  makeContext,
  merge
} from "./chunk-RG46YT3R.js";
import {
  between,
  clamp,
  getOrElse,
  getOrThrow,
  getOrUndefined,
  isNone,
  isSome,
  liftThrowable,
  make2 as make,
  map,
  mapInput,
  mapInput2,
  match,
  max,
  mergeWith,
  min,
  none,
  orElseSome,
  some
} from "./chunk-5XJTUU64.js";
import {
  CommitPrototype,
  NodeInspectSymbol,
  OP_ASYNC,
  OP_COMMIT,
  OP_FAILURE,
  OP_ITERATOR,
  OP_ON_FAILURE,
  OP_ON_SUCCESS,
  OP_ON_SUCCESS_AND_FAILURE,
  OP_REVERT_FLAGS,
  OP_SUCCESS,
  OP_SYNC,
  OP_UPDATE_RUNTIME_FLAGS,
  OP_WHILE,
  OP_WITH_RUNTIME,
  OP_YIELD,
  StructuralCommitPrototype,
  StructuralPrototype,
  YieldWrap,
  array,
  cached,
  combine,
  effectVariance,
  equals,
  format,
  getBugErrorMessage,
  globalValue,
  hasProperty,
  hash,
  internalCall,
  isBigInt,
  isFunction,
  isNumber,
  isObject,
  isPromiseLike,
  isString,
  isTagged,
  pipeArguments,
  random,
  string,
  stringifyCircular,
  structure,
  symbol,
  symbol2,
  toJSON
} from "./chunk-SPGJQP32.js";
import {
  constFalse,
  constTrue,
  constant,
  dual,
  identity,
  pipe,
  unsafeCoerce
} from "./chunk-LX4DLJUA.js";
import {
  __export
} from "./chunk-G3PMV62Z.js";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Duration.js
var Duration_exports = {};
__export(Duration_exports, {
  Equivalence: () => Equivalence,
  Order: () => Order,
  between: () => between2,
  clamp: () => clamp2,
  days: () => days,
  decode: () => decode,
  decodeUnknown: () => decodeUnknown,
  divide: () => divide,
  equals: () => equals2,
  format: () => format2,
  formatIso: () => formatIso,
  fromIso: () => fromIso,
  greaterThan: () => greaterThan,
  greaterThanOrEqualTo: () => greaterThanOrEqualTo,
  hours: () => hours,
  infinity: () => infinity,
  isDuration: () => isDuration,
  isFinite: () => isFinite,
  isZero: () => isZero,
  lessThan: () => lessThan,
  lessThanOrEqualTo: () => lessThanOrEqualTo,
  match: () => match2,
  matchWith: () => matchWith,
  max: () => max2,
  micros: () => micros,
  millis: () => millis,
  min: () => min2,
  minutes: () => minutes,
  nanos: () => nanos,
  parts: () => parts,
  seconds: () => seconds,
  subtract: () => subtract,
  sum: () => sum,
  times: () => times,
  toDays: () => toDays,
  toHours: () => toHours,
  toHrTime: () => toHrTime,
  toMillis: () => toMillis,
  toMinutes: () => toMinutes,
  toNanos: () => toNanos,
  toSeconds: () => toSeconds,
  toWeeks: () => toWeeks,
  unsafeDivide: () => unsafeDivide,
  unsafeFormatIso: () => unsafeFormatIso,
  unsafeToNanos: () => unsafeToNanos,
  weeks: () => weeks,
  zero: () => zero
});
var TypeId = Symbol.for("effect/Duration");
var bigint0 = BigInt(0);
var bigint24 = BigInt(24);
var bigint60 = BigInt(60);
var bigint1e3 = BigInt(1e3);
var bigint1e6 = BigInt(1e6);
var bigint1e9 = BigInt(1e9);
var DURATION_REGEX = /^(-?\d+(?:\.\d+)?)\s+(nanos?|micros?|millis?|seconds?|minutes?|hours?|days?|weeks?)$/;
var decode = (input) => {
  if (isDuration(input)) {
    return input;
  } else if (isNumber(input)) {
    return millis(input);
  } else if (isBigInt(input)) {
    return nanos(input);
  } else if (Array.isArray(input) && input.length === 2 && input.every(isNumber)) {
    if (input[0] === -Infinity || input[1] === -Infinity || Number.isNaN(input[0]) || Number.isNaN(input[1])) {
      return zero;
    }
    if (input[0] === Infinity || input[1] === Infinity) {
      return infinity;
    }
    return nanos(BigInt(Math.round(input[0] * 1e9)) + BigInt(Math.round(input[1])));
  } else if (isString(input)) {
    const match4 = DURATION_REGEX.exec(input);
    if (match4) {
      const [_, valueStr, unit] = match4;
      const value = Number(valueStr);
      switch (unit) {
        case "nano":
        case "nanos":
          return nanos(BigInt(valueStr));
        case "micro":
        case "micros":
          return micros(BigInt(valueStr));
        case "milli":
        case "millis":
          return millis(value);
        case "second":
        case "seconds":
          return seconds(value);
        case "minute":
        case "minutes":
          return minutes(value);
        case "hour":
        case "hours":
          return hours(value);
        case "day":
        case "days":
          return days(value);
        case "week":
        case "weeks":
          return weeks(value);
      }
    }
  }
  throw new Error("Invalid DurationInput");
};
var decodeUnknown = liftThrowable(decode);
var zeroValue = {
  _tag: "Millis",
  millis: 0
};
var infinityValue = {
  _tag: "Infinity"
};
var DurationProto = {
  [TypeId]: TypeId,
  [symbol]() {
    return cached(this, structure(this.value));
  },
  [symbol2](that) {
    return isDuration(that) && equals2(this, that);
  },
  toString() {
    return `Duration(${format2(this)})`;
  },
  toJSON() {
    switch (this.value._tag) {
      case "Millis":
        return {
          _id: "Duration",
          _tag: "Millis",
          millis: this.value.millis
        };
      case "Nanos":
        return {
          _id: "Duration",
          _tag: "Nanos",
          hrtime: toHrTime(this)
        };
      case "Infinity":
        return {
          _id: "Duration",
          _tag: "Infinity"
        };
    }
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make3 = (input) => {
  const duration = Object.create(DurationProto);
  if (isNumber(input)) {
    if (isNaN(input) || input <= 0) {
      duration.value = zeroValue;
    } else if (!Number.isFinite(input)) {
      duration.value = infinityValue;
    } else if (!Number.isInteger(input)) {
      duration.value = {
        _tag: "Nanos",
        nanos: BigInt(Math.round(input * 1e6))
      };
    } else {
      duration.value = {
        _tag: "Millis",
        millis: input
      };
    }
  } else if (input <= bigint0) {
    duration.value = zeroValue;
  } else {
    duration.value = {
      _tag: "Nanos",
      nanos: input
    };
  }
  return duration;
};
var isDuration = (u) => hasProperty(u, TypeId);
var isFinite = (self) => self.value._tag !== "Infinity";
var isZero = (self) => {
  switch (self.value._tag) {
    case "Millis": {
      return self.value.millis === 0;
    }
    case "Nanos": {
      return self.value.nanos === bigint0;
    }
    case "Infinity": {
      return false;
    }
  }
};
var zero = make3(0);
var infinity = make3(Infinity);
var nanos = (nanos2) => make3(nanos2);
var micros = (micros2) => make3(micros2 * bigint1e3);
var millis = (millis2) => make3(millis2);
var seconds = (seconds2) => make3(seconds2 * 1e3);
var minutes = (minutes2) => make3(minutes2 * 6e4);
var hours = (hours2) => make3(hours2 * 36e5);
var days = (days2) => make3(days2 * 864e5);
var weeks = (weeks2) => make3(weeks2 * 6048e5);
var toMillis = (self) => match2(self, {
  onMillis: (millis2) => millis2,
  onNanos: (nanos2) => Number(nanos2) / 1e6
});
var toSeconds = (self) => match2(self, {
  onMillis: (millis2) => millis2 / 1e3,
  onNanos: (nanos2) => Number(nanos2) / 1e9
});
var toMinutes = (self) => match2(self, {
  onMillis: (millis2) => millis2 / 6e4,
  onNanos: (nanos2) => Number(nanos2) / 6e10
});
var toHours = (self) => match2(self, {
  onMillis: (millis2) => millis2 / 36e5,
  onNanos: (nanos2) => Number(nanos2) / 36e11
});
var toDays = (self) => match2(self, {
  onMillis: (millis2) => millis2 / 864e5,
  onNanos: (nanos2) => Number(nanos2) / 864e11
});
var toWeeks = (self) => match2(self, {
  onMillis: (millis2) => millis2 / 6048e5,
  onNanos: (nanos2) => Number(nanos2) / 6048e11
});
var toNanos = (self) => {
  const _self = decode(self);
  switch (_self.value._tag) {
    case "Infinity":
      return none();
    case "Nanos":
      return some(_self.value.nanos);
    case "Millis":
      return some(BigInt(Math.round(_self.value.millis * 1e6)));
  }
};
var unsafeToNanos = (self) => {
  const _self = decode(self);
  switch (_self.value._tag) {
    case "Infinity":
      throw new Error("Cannot convert infinite duration to nanos");
    case "Nanos":
      return _self.value.nanos;
    case "Millis":
      return BigInt(Math.round(_self.value.millis * 1e6));
  }
};
var toHrTime = (self) => {
  const _self = decode(self);
  switch (_self.value._tag) {
    case "Infinity":
      return [Infinity, 0];
    case "Nanos":
      return [Number(_self.value.nanos / bigint1e9), Number(_self.value.nanos % bigint1e9)];
    case "Millis":
      return [Math.floor(_self.value.millis / 1e3), Math.round(_self.value.millis % 1e3 * 1e6)];
  }
};
var match2 = dual(2, (self, options) => {
  const _self = decode(self);
  switch (_self.value._tag) {
    case "Nanos":
      return options.onNanos(_self.value.nanos);
    case "Infinity":
      return options.onMillis(Infinity);
    case "Millis":
      return options.onMillis(_self.value.millis);
  }
});
var matchWith = dual(3, (self, that, options) => {
  const _self = decode(self);
  const _that = decode(that);
  if (_self.value._tag === "Infinity" || _that.value._tag === "Infinity") {
    return options.onMillis(toMillis(_self), toMillis(_that));
  } else if (_self.value._tag === "Nanos" || _that.value._tag === "Nanos") {
    const selfNanos = _self.value._tag === "Nanos" ? _self.value.nanos : BigInt(Math.round(_self.value.millis * 1e6));
    const thatNanos = _that.value._tag === "Nanos" ? _that.value.nanos : BigInt(Math.round(_that.value.millis * 1e6));
    return options.onNanos(selfNanos, thatNanos);
  }
  return options.onMillis(_self.value.millis, _that.value.millis);
});
var Order = make((self, that) => matchWith(self, that, {
  onMillis: (self2, that2) => self2 < that2 ? -1 : self2 > that2 ? 1 : 0,
  onNanos: (self2, that2) => self2 < that2 ? -1 : self2 > that2 ? 1 : 0
}));
var between2 = between(mapInput2(Order, decode));
var Equivalence = (self, that) => matchWith(self, that, {
  onMillis: (self2, that2) => self2 === that2,
  onNanos: (self2, that2) => self2 === that2
});
var _min = min(Order);
var min2 = dual(2, (self, that) => _min(decode(self), decode(that)));
var _max = max(Order);
var max2 = dual(2, (self, that) => _max(decode(self), decode(that)));
var _clamp = clamp(Order);
var clamp2 = dual(2, (self, options) => _clamp(decode(self), {
  minimum: decode(options.minimum),
  maximum: decode(options.maximum)
}));
var divide = dual(2, (self, by) => match2(self, {
  onMillis: (millis2) => {
    if (by === 0 || isNaN(by) || !Number.isFinite(by)) {
      return none();
    }
    return some(make3(millis2 / by));
  },
  onNanos: (nanos2) => {
    if (isNaN(by) || by <= 0 || !Number.isFinite(by)) {
      return none();
    }
    try {
      return some(make3(nanos2 / BigInt(by)));
    } catch {
      return none();
    }
  }
}));
var unsafeDivide = dual(2, (self, by) => match2(self, {
  onMillis: (millis2) => make3(millis2 / by),
  onNanos: (nanos2) => {
    if (isNaN(by) || by < 0 || Object.is(by, -0)) {
      return zero;
    } else if (Object.is(by, 0) || !Number.isFinite(by)) {
      return infinity;
    }
    return make3(nanos2 / BigInt(by));
  }
}));
var times = dual(2, (self, times2) => match2(self, {
  onMillis: (millis2) => make3(millis2 * times2),
  onNanos: (nanos2) => make3(nanos2 * BigInt(times2))
}));
var subtract = dual(2, (self, that) => matchWith(self, that, {
  onMillis: (self2, that2) => make3(self2 - that2),
  onNanos: (self2, that2) => make3(self2 - that2)
}));
var sum = dual(2, (self, that) => matchWith(self, that, {
  onMillis: (self2, that2) => make3(self2 + that2),
  onNanos: (self2, that2) => make3(self2 + that2)
}));
var lessThan = dual(2, (self, that) => matchWith(self, that, {
  onMillis: (self2, that2) => self2 < that2,
  onNanos: (self2, that2) => self2 < that2
}));
var lessThanOrEqualTo = dual(2, (self, that) => matchWith(self, that, {
  onMillis: (self2, that2) => self2 <= that2,
  onNanos: (self2, that2) => self2 <= that2
}));
var greaterThan = dual(2, (self, that) => matchWith(self, that, {
  onMillis: (self2, that2) => self2 > that2,
  onNanos: (self2, that2) => self2 > that2
}));
var greaterThanOrEqualTo = dual(2, (self, that) => matchWith(self, that, {
  onMillis: (self2, that2) => self2 >= that2,
  onNanos: (self2, that2) => self2 >= that2
}));
var equals2 = dual(2, (self, that) => Equivalence(decode(self), decode(that)));
var parts = (self) => {
  const duration = decode(self);
  if (duration.value._tag === "Infinity") {
    return {
      days: Infinity,
      hours: Infinity,
      minutes: Infinity,
      seconds: Infinity,
      millis: Infinity,
      nanos: Infinity
    };
  }
  const nanos2 = unsafeToNanos(duration);
  const ms = nanos2 / bigint1e6;
  const sec = ms / bigint1e3;
  const min3 = sec / bigint60;
  const hr = min3 / bigint60;
  const days2 = hr / bigint24;
  return {
    days: Number(days2),
    hours: Number(hr % bigint24),
    minutes: Number(min3 % bigint60),
    seconds: Number(sec % bigint60),
    millis: Number(ms % bigint1e3),
    nanos: Number(nanos2 % bigint1e6)
  };
};
var format2 = (self) => {
  const duration = decode(self);
  if (duration.value._tag === "Infinity") {
    return "Infinity";
  }
  if (isZero(duration)) {
    return "0";
  }
  const fragments = parts(duration);
  const pieces = [];
  if (fragments.days !== 0) {
    pieces.push(`${fragments.days}d`);
  }
  if (fragments.hours !== 0) {
    pieces.push(`${fragments.hours}h`);
  }
  if (fragments.minutes !== 0) {
    pieces.push(`${fragments.minutes}m`);
  }
  if (fragments.seconds !== 0) {
    pieces.push(`${fragments.seconds}s`);
  }
  if (fragments.millis !== 0) {
    pieces.push(`${fragments.millis}ms`);
  }
  if (fragments.nanos !== 0) {
    pieces.push(`${fragments.nanos}ns`);
  }
  return pieces.join(" ");
};
var unsafeFormatIso = (self) => {
  const duration = decode(self);
  if (!isFinite(duration)) {
    throw new RangeError("Cannot format infinite duration");
  }
  const fragments = [];
  const {
    days: days2,
    hours: hours2,
    millis: millis2,
    minutes: minutes2,
    nanos: nanos2,
    seconds: seconds2
  } = parts(duration);
  let rest = days2;
  if (rest >= 365) {
    const years = Math.floor(rest / 365);
    rest %= 365;
    fragments.push(`${years}Y`);
  }
  if (rest >= 30) {
    const months = Math.floor(rest / 30);
    rest %= 30;
    fragments.push(`${months}M`);
  }
  if (rest >= 7) {
    const weeks2 = Math.floor(rest / 7);
    rest %= 7;
    fragments.push(`${weeks2}W`);
  }
  if (rest > 0) {
    fragments.push(`${rest}D`);
  }
  if (hours2 !== 0 || minutes2 !== 0 || seconds2 !== 0 || millis2 !== 0 || nanos2 !== 0) {
    fragments.push("T");
    if (hours2 !== 0) {
      fragments.push(`${hours2}H`);
    }
    if (minutes2 !== 0) {
      fragments.push(`${minutes2}M`);
    }
    if (seconds2 !== 0 || millis2 !== 0 || nanos2 !== 0) {
      const total = BigInt(seconds2) * bigint1e9 + BigInt(millis2) * bigint1e6 + BigInt(nanos2);
      const str = (Number(total) / 1e9).toFixed(9).replace(/\.?0+$/, "");
      fragments.push(`${str}S`);
    }
  }
  return `P${fragments.join("") || "T0S"}`;
};
var formatIso = (self) => {
  const duration = decode(self);
  return isFinite(duration) ? some(unsafeFormatIso(duration)) : none();
};
var fromIso = (iso) => {
  const result = DURATION_ISO_REGEX.exec(iso);
  if (result == null) {
    return none();
  }
  const [years, months, weeks2, days2, hours2, mins, secs] = result.slice(1, 8).map((_) => _ ? Number(_) : 0);
  const value = years * 365 * 24 * 60 * 60 + months * 30 * 24 * 60 * 60 + weeks2 * 7 * 24 * 60 * 60 + days2 * 24 * 60 * 60 + hours2 * 60 * 60 + mins * 60 + secs;
  return some(seconds(value));
};
var DURATION_ISO_REGEX = /^P(?!$)(?:(\d+)Y)?(?:(\d+)M)?(?:(\d+)W)?(?:(\d+)D)?(?:T(?!$)(?:(\d+)H)?(?:(\d+)M)?(?:(\d+(?:\.\d+)?)S)?)?$/;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/HashSet.js
var HashSet_exports = {};
__export(HashSet_exports, {
  add: () => add2,
  beginMutation: () => beginMutation3,
  difference: () => difference2,
  empty: () => empty6,
  endMutation: () => endMutation3,
  every: () => every3,
  filter: () => filter3,
  flatMap: () => flatMap3,
  forEach: () => forEach3,
  fromIterable: () => fromIterable5,
  has: () => has3,
  intersection: () => intersection2,
  isHashSet: () => isHashSet2,
  isSubset: () => isSubset2,
  make: () => make7,
  map: () => map4,
  mutate: () => mutate3,
  partition: () => partition2,
  reduce: () => reduce4,
  remove: () => remove3,
  size: () => size3,
  some: () => some4,
  toValues: () => toValues,
  toggle: () => toggle2,
  union: () => union3,
  values: () => values3
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/hashMap/config.js
var SIZE = 5;
var BUCKET_SIZE = Math.pow(2, SIZE);
var MASK = BUCKET_SIZE - 1;
var MAX_INDEX_NODE = BUCKET_SIZE / 2;
var MIN_ARRAY_NODE = BUCKET_SIZE / 4;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/hashMap/bitwise.js
function popcount(x) {
  x -= x >> 1 & 1431655765;
  x = (x & 858993459) + (x >> 2 & 858993459);
  x = x + (x >> 4) & 252645135;
  x += x >> 8;
  x += x >> 16;
  return x & 127;
}
function hashFragment(shift, h) {
  return h >>> shift & MASK;
}
function toBitmap(x) {
  return 1 << x;
}
function fromBitmap(bitmap, bit) {
  return popcount(bitmap & bit - 1);
}

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stack.js
var make4 = (value, previous) => ({
  value,
  previous
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/hashMap/array.js
function arrayUpdate(mutate5, at, v, arr) {
  let out = arr;
  if (!mutate5) {
    const len = arr.length;
    out = new Array(len);
    for (let i = 0; i < len; ++i) out[i] = arr[i];
  }
  out[at] = v;
  return out;
}
function arraySpliceOut(mutate5, at, arr) {
  const newLen = arr.length - 1;
  let i = 0;
  let g = 0;
  let out = arr;
  if (mutate5) {
    i = g = at;
  } else {
    out = new Array(newLen);
    while (i < at) out[g++] = arr[i++];
  }
  ++i;
  while (i <= newLen) out[g++] = arr[i++];
  if (mutate5) {
    out.length = newLen;
  }
  return out;
}
function arraySpliceIn(mutate5, at, v, arr) {
  const len = arr.length;
  if (mutate5) {
    let i2 = len;
    while (i2 >= at) arr[i2--] = arr[i2];
    arr[at] = v;
    return arr;
  }
  let i = 0, g = 0;
  const out = new Array(len + 1);
  while (i < at) out[g++] = arr[i++];
  out[at] = v;
  while (i < len) out[++g] = arr[i++];
  return out;
}

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/hashMap/node.js
var EmptyNode = class _EmptyNode {
  _tag = "EmptyNode";
  modify(edit, _shift, f, hash2, key, size7) {
    const v = f(none());
    if (isNone(v)) return new _EmptyNode();
    ++size7.value;
    return new LeafNode(edit, hash2, key, v);
  }
};
function isEmptyNode(a) {
  return isTagged(a, "EmptyNode");
}
function isLeafNode(node) {
  return isEmptyNode(node) || node._tag === "LeafNode" || node._tag === "CollisionNode";
}
function canEditNode(node, edit) {
  return isEmptyNode(node) ? false : edit === node.edit;
}
var LeafNode = class _LeafNode {
  edit;
  hash;
  key;
  value;
  _tag = "LeafNode";
  constructor(edit, hash2, key, value) {
    this.edit = edit;
    this.hash = hash2;
    this.key = key;
    this.value = value;
  }
  modify(edit, shift, f, hash2, key, size7) {
    if (equals(key, this.key)) {
      const v2 = f(this.value);
      if (v2 === this.value) return this;
      else if (isNone(v2)) {
        --size7.value;
        return new EmptyNode();
      }
      if (canEditNode(this, edit)) {
        this.value = v2;
        return this;
      }
      return new _LeafNode(edit, hash2, key, v2);
    }
    const v = f(none());
    if (isNone(v)) return this;
    ++size7.value;
    return mergeLeaves(edit, shift, this.hash, this, hash2, new _LeafNode(edit, hash2, key, v));
  }
};
var CollisionNode = class _CollisionNode {
  edit;
  hash;
  children;
  _tag = "CollisionNode";
  constructor(edit, hash2, children) {
    this.edit = edit;
    this.hash = hash2;
    this.children = children;
  }
  modify(edit, shift, f, hash2, key, size7) {
    if (hash2 === this.hash) {
      const canEdit = canEditNode(this, edit);
      const list = this.updateCollisionList(canEdit, edit, this.hash, this.children, f, key, size7);
      if (list === this.children) return this;
      return list.length > 1 ? new _CollisionNode(edit, this.hash, list) : list[0];
    }
    const v = f(none());
    if (isNone(v)) return this;
    ++size7.value;
    return mergeLeaves(edit, shift, this.hash, this, hash2, new LeafNode(edit, hash2, key, v));
  }
  updateCollisionList(mutate5, edit, hash2, list, f, key, size7) {
    const len = list.length;
    for (let i = 0; i < len; ++i) {
      const child = list[i];
      if ("key" in child && equals(key, child.key)) {
        const value = child.value;
        const newValue2 = f(value);
        if (newValue2 === value) return list;
        if (isNone(newValue2)) {
          --size7.value;
          return arraySpliceOut(mutate5, i, list);
        }
        return arrayUpdate(mutate5, i, new LeafNode(edit, hash2, key, newValue2), list);
      }
    }
    const newValue = f(none());
    if (isNone(newValue)) return list;
    ++size7.value;
    return arrayUpdate(mutate5, len, new LeafNode(edit, hash2, key, newValue), list);
  }
};
var IndexedNode = class _IndexedNode {
  edit;
  mask;
  children;
  _tag = "IndexedNode";
  constructor(edit, mask, children) {
    this.edit = edit;
    this.mask = mask;
    this.children = children;
  }
  modify(edit, shift, f, hash2, key, size7) {
    const mask = this.mask;
    const children = this.children;
    const frag = hashFragment(shift, hash2);
    const bit = toBitmap(frag);
    const indx = fromBitmap(mask, bit);
    const exists = mask & bit;
    const canEdit = canEditNode(this, edit);
    if (!exists) {
      const _newChild = new EmptyNode().modify(edit, shift + SIZE, f, hash2, key, size7);
      if (!_newChild) return this;
      return children.length >= MAX_INDEX_NODE ? expand(edit, frag, _newChild, mask, children) : new _IndexedNode(edit, mask | bit, arraySpliceIn(canEdit, indx, _newChild, children));
    }
    const current = children[indx];
    const child = current.modify(edit, shift + SIZE, f, hash2, key, size7);
    if (current === child) return this;
    let bitmap = mask;
    let newChildren;
    if (isEmptyNode(child)) {
      bitmap &= ~bit;
      if (!bitmap) return new EmptyNode();
      if (children.length <= 2 && isLeafNode(children[indx ^ 1])) {
        return children[indx ^ 1];
      }
      newChildren = arraySpliceOut(canEdit, indx, children);
    } else {
      newChildren = arrayUpdate(canEdit, indx, child, children);
    }
    if (canEdit) {
      this.mask = bitmap;
      this.children = newChildren;
      return this;
    }
    return new _IndexedNode(edit, bitmap, newChildren);
  }
};
var ArrayNode = class _ArrayNode {
  edit;
  size;
  children;
  _tag = "ArrayNode";
  constructor(edit, size7, children) {
    this.edit = edit;
    this.size = size7;
    this.children = children;
  }
  modify(edit, shift, f, hash2, key, size7) {
    let count = this.size;
    const children = this.children;
    const frag = hashFragment(shift, hash2);
    const child = children[frag];
    const newChild = (child || new EmptyNode()).modify(edit, shift + SIZE, f, hash2, key, size7);
    if (child === newChild) return this;
    const canEdit = canEditNode(this, edit);
    let newChildren;
    if (isEmptyNode(child) && !isEmptyNode(newChild)) {
      ++count;
      newChildren = arrayUpdate(canEdit, frag, newChild, children);
    } else if (!isEmptyNode(child) && isEmptyNode(newChild)) {
      --count;
      if (count <= MIN_ARRAY_NODE) {
        return pack(edit, count, frag, children);
      }
      newChildren = arrayUpdate(canEdit, frag, new EmptyNode(), children);
    } else {
      newChildren = arrayUpdate(canEdit, frag, newChild, children);
    }
    if (canEdit) {
      this.size = count;
      this.children = newChildren;
      return this;
    }
    return new _ArrayNode(edit, count, newChildren);
  }
};
function pack(edit, count, removed, elements) {
  const children = new Array(count - 1);
  let g = 0;
  let bitmap = 0;
  for (let i = 0, len = elements.length; i < len; ++i) {
    if (i !== removed) {
      const elem = elements[i];
      if (elem && !isEmptyNode(elem)) {
        children[g++] = elem;
        bitmap |= 1 << i;
      }
    }
  }
  return new IndexedNode(edit, bitmap, children);
}
function expand(edit, frag, child, bitmap, subNodes) {
  const arr = [];
  let bit = bitmap;
  let count = 0;
  for (let i = 0; bit; ++i) {
    if (bit & 1) arr[i] = subNodes[count++];
    bit >>>= 1;
  }
  arr[frag] = child;
  return new ArrayNode(edit, count + 1, arr);
}
function mergeLeavesInner(edit, shift, h1, n1, h2, n2) {
  if (h1 === h2) return new CollisionNode(edit, h1, [n2, n1]);
  const subH1 = hashFragment(shift, h1);
  const subH2 = hashFragment(shift, h2);
  if (subH1 === subH2) {
    return (child) => new IndexedNode(edit, toBitmap(subH1) | toBitmap(subH2), [child]);
  } else {
    const children = subH1 < subH2 ? [n1, n2] : [n2, n1];
    return new IndexedNode(edit, toBitmap(subH1) | toBitmap(subH2), children);
  }
}
function mergeLeaves(edit, shift, h1, n1, h2, n2) {
  let stack = void 0;
  let currentShift = shift;
  while (true) {
    const res = mergeLeavesInner(edit, currentShift, h1, n1, h2, n2);
    if (typeof res === "function") {
      stack = make4(res, stack);
      currentShift = currentShift + SIZE;
    } else {
      let final = res;
      while (stack != null) {
        final = stack.value(final);
        stack = stack.previous;
      }
      return final;
    }
  }
}

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/hashMap.js
var HashMapSymbolKey = "effect/HashMap";
var HashMapTypeId = Symbol.for(HashMapSymbolKey);
var HashMapProto = {
  [HashMapTypeId]: HashMapTypeId,
  [Symbol.iterator]() {
    return new HashMapIterator(this, (k, v) => [k, v]);
  },
  [symbol]() {
    let hash2 = hash(HashMapSymbolKey);
    for (const item of this) {
      hash2 ^= pipe(hash(item[0]), combine(hash(item[1])));
    }
    return cached(this, hash2);
  },
  [symbol2](that) {
    if (isHashMap(that)) {
      if (that._size !== this._size) {
        return false;
      }
      for (const item of this) {
        const elem = pipe(that, getHash(item[0], hash(item[0])));
        if (isNone(elem)) {
          return false;
        } else {
          if (!equals(item[1], elem.value)) {
            return false;
          }
        }
      }
      return true;
    }
    return false;
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "HashMap",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeImpl = (editable, edit, root, size7) => {
  const map9 = Object.create(HashMapProto);
  map9._editable = editable;
  map9._edit = edit;
  map9._root = root;
  map9._size = size7;
  return map9;
};
var HashMapIterator = class _HashMapIterator {
  map;
  f;
  v;
  constructor(map9, f) {
    this.map = map9;
    this.f = f;
    this.v = visitLazy(this.map._root, this.f, void 0);
  }
  next() {
    if (isNone(this.v)) {
      return {
        done: true,
        value: void 0
      };
    }
    const v0 = this.v.value;
    this.v = applyCont(v0.cont);
    return {
      done: false,
      value: v0.value
    };
  }
  [Symbol.iterator]() {
    return new _HashMapIterator(this.map, this.f);
  }
};
var applyCont = (cont) => cont ? visitLazyChildren(cont[0], cont[1], cont[2], cont[3], cont[4]) : none();
var visitLazy = (node, f, cont = void 0) => {
  switch (node._tag) {
    case "LeafNode": {
      if (isSome(node.value)) {
        return some({
          value: f(node.key, node.value.value),
          cont
        });
      }
      return applyCont(cont);
    }
    case "CollisionNode":
    case "ArrayNode":
    case "IndexedNode": {
      const children = node.children;
      return visitLazyChildren(children.length, children, 0, f, cont);
    }
    default: {
      return applyCont(cont);
    }
  }
};
var visitLazyChildren = (len, children, i, f, cont) => {
  while (i < len) {
    const child = children[i++];
    if (child && !isEmptyNode(child)) {
      return visitLazy(child, f, [len, children, i, f, cont]);
    }
  }
  return applyCont(cont);
};
var _empty = makeImpl(false, 0, new EmptyNode(), 0);
var empty4 = () => _empty;
var make5 = (...entries3) => fromIterable3(entries3);
var fromIterable3 = (entries3) => {
  const map9 = beginMutation(empty4());
  for (const entry of entries3) {
    set(map9, entry[0], entry[1]);
  }
  return endMutation(map9);
};
var isHashMap = (u) => hasProperty(u, HashMapTypeId);
var isEmpty2 = (self) => self && isEmptyNode(self._root);
var get = dual(2, (self, key) => getHash(self, key, hash(key)));
var getHash = dual(3, (self, key, hash2) => {
  let node = self._root;
  let shift = 0;
  while (true) {
    switch (node._tag) {
      case "LeafNode": {
        return equals(key, node.key) ? node.value : none();
      }
      case "CollisionNode": {
        if (hash2 === node.hash) {
          const children = node.children;
          for (let i = 0, len = children.length; i < len; ++i) {
            const child = children[i];
            if ("key" in child && equals(key, child.key)) {
              return child.value;
            }
          }
        }
        return none();
      }
      case "IndexedNode": {
        const frag = hashFragment(shift, hash2);
        const bit = toBitmap(frag);
        if (node.mask & bit) {
          node = node.children[fromBitmap(node.mask, bit)];
          shift += SIZE;
          break;
        }
        return none();
      }
      case "ArrayNode": {
        node = node.children[hashFragment(shift, hash2)];
        if (node) {
          shift += SIZE;
          break;
        }
        return none();
      }
      default:
        return none();
    }
  }
});
var unsafeGet2 = dual(2, (self, key) => {
  const element = getHash(self, key, hash(key));
  if (isNone(element)) {
    throw new Error("Expected map to contain key");
  }
  return element.value;
});
var has = dual(2, (self, key) => isSome(getHash(self, key, hash(key))));
var hasHash = dual(3, (self, key, hash2) => isSome(getHash(self, key, hash2)));
var hasBy = dual(2, (self, predicate) => isSome(findFirst(self, predicate)));
var set = dual(3, (self, key, value) => modifyAt(self, key, () => some(value)));
var setTree = dual(3, (self, newRoot, newSize) => {
  if (self._editable) {
    ;
    self._root = newRoot;
    self._size = newSize;
    return self;
  }
  return newRoot === self._root ? self : makeImpl(self._editable, self._edit, newRoot, newSize);
});
var keys = (self) => new HashMapIterator(self, (key) => key);
var values = (self) => new HashMapIterator(self, (_, value) => value);
var entries = (self) => new HashMapIterator(self, (key, value) => [key, value]);
var size = (self) => self._size;
var countBy = dual(2, (self, f) => {
  let count = 0;
  for (const [k, a] of self) {
    if (f(a, k)) {
      count++;
    }
  }
  return count;
});
var beginMutation = (self) => makeImpl(true, self._edit + 1, self._root, self._size);
var endMutation = (self) => {
  ;
  self._editable = false;
  return self;
};
var mutate = dual(2, (self, f) => {
  const transient = beginMutation(self);
  f(transient);
  return endMutation(transient);
});
var modifyAt = dual(3, (self, key, f) => modifyHash(self, key, hash(key), f));
var modifyHash = dual(4, (self, key, hash2, f) => {
  const size7 = {
    value: self._size
  };
  const newRoot = self._root.modify(self._editable ? self._edit : NaN, 0, f, hash2, key, size7);
  return pipe(self, setTree(newRoot, size7.value));
});
var modify = dual(3, (self, key, f) => modifyAt(self, key, map(f)));
var union = dual(2, (self, that) => {
  const result = beginMutation(self);
  forEach(that, (v, k) => set(result, k, v));
  return endMutation(result);
});
var remove = dual(2, (self, key) => modifyAt(self, key, none));
var removeMany = dual(2, (self, keys3) => mutate(self, (map9) => {
  for (const key of keys3) {
    remove(key)(map9);
  }
}));
var map2 = dual(2, (self, f) => reduce2(self, empty4(), (map9, value, key) => set(map9, key, f(value, key))));
var flatMap = dual(2, (self, f) => reduce2(self, empty4(), (zero2, value, key) => mutate(zero2, (map9) => forEach(f(value, key), (value2, key2) => set(map9, key2, value2)))));
var forEach = dual(2, (self, f) => reduce2(self, void 0, (_, value, key) => f(value, key)));
var reduce2 = dual(3, (self, zero2, f) => {
  const root = self._root;
  if (root._tag === "LeafNode") {
    return isSome(root.value) ? f(zero2, root.value.value, root.key) : zero2;
  }
  if (root._tag === "EmptyNode") {
    return zero2;
  }
  const toVisit = [root.children];
  let children;
  while (children = toVisit.pop()) {
    for (let i = 0, len = children.length; i < len; ) {
      const child = children[i++];
      if (child && !isEmptyNode(child)) {
        if (child._tag === "LeafNode") {
          if (isSome(child.value)) {
            zero2 = f(zero2, child.value.value, child.key);
          }
        } else {
          toVisit.push(child.children);
        }
      }
    }
  }
  return zero2;
});
var filter = dual(2, (self, f) => mutate(empty4(), (map9) => {
  for (const [k, a] of self) {
    if (f(a, k)) {
      set(map9, k, a);
    }
  }
}));
var compact = (self) => filterMap(self, identity);
var filterMap = dual(2, (self, f) => mutate(empty4(), (map9) => {
  for (const [k, a] of self) {
    const option = f(a, k);
    if (isSome(option)) {
      set(map9, k, option.value);
    }
  }
}));
var findFirst = dual(2, (self, predicate) => {
  for (const ka of self) {
    if (predicate(ka[1], ka[0])) {
      return some(ka);
    }
  }
  return none();
});
var some2 = dual(2, (self, predicate) => {
  for (const ka of self) {
    if (predicate(ka[1], ka[0])) {
      return true;
    }
  }
  return false;
});
var every = dual(2, (self, predicate) => !some2(self, (a, k) => !predicate(a, k)));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/hashSet.js
var HashSetSymbolKey = "effect/HashSet";
var HashSetTypeId = Symbol.for(HashSetSymbolKey);
var HashSetProto = {
  [HashSetTypeId]: HashSetTypeId,
  [Symbol.iterator]() {
    return keys(this._keyMap);
  },
  [symbol]() {
    return cached(this, combine(hash(this._keyMap))(hash(HashSetSymbolKey)));
  },
  [symbol2](that) {
    if (isHashSet(that)) {
      return size(this._keyMap) === size(that._keyMap) && equals(this._keyMap, that._keyMap);
    }
    return false;
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "HashSet",
      values: Array.from(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeImpl2 = (keyMap) => {
  const set4 = Object.create(HashSetProto);
  set4._keyMap = keyMap;
  return set4;
};
var isHashSet = (u) => hasProperty(u, HashSetTypeId);
var _empty2 = makeImpl2(empty4());
var empty5 = () => _empty2;
var fromIterable4 = (elements) => {
  const set4 = beginMutation2(empty5());
  for (const value of elements) {
    add(set4, value);
  }
  return endMutation2(set4);
};
var make6 = (...elements) => {
  const set4 = beginMutation2(empty5());
  for (const value of elements) {
    add(set4, value);
  }
  return endMutation2(set4);
};
var has2 = dual(2, (self, value) => has(self._keyMap, value));
var some3 = dual(2, (self, f) => {
  let found = false;
  for (const value of self) {
    found = f(value);
    if (found) {
      break;
    }
  }
  return found;
});
var every2 = dual(2, (self, refinement) => !some3(self, (a) => !refinement(a)));
var isSubset = dual(2, (self, that) => every2(self, (value) => has2(that, value)));
var values2 = (self) => keys(self._keyMap);
var size2 = (self) => size(self._keyMap);
var beginMutation2 = (self) => makeImpl2(beginMutation(self._keyMap));
var endMutation2 = (self) => {
  ;
  self._keyMap._editable = false;
  return self;
};
var mutate2 = dual(2, (self, f) => {
  const transient = beginMutation2(self);
  f(transient);
  return endMutation2(transient);
});
var add = dual(2, (self, value) => self._keyMap._editable ? (set(value, true)(self._keyMap), self) : makeImpl2(set(value, true)(self._keyMap)));
var remove2 = dual(2, (self, value) => self._keyMap._editable ? (remove(value)(self._keyMap), self) : makeImpl2(remove(value)(self._keyMap)));
var difference = dual(2, (self, that) => mutate2(self, (set4) => {
  for (const value of that) {
    remove2(set4, value);
  }
}));
var intersection = dual(2, (self, that) => mutate2(empty5(), (set4) => {
  for (const value of that) {
    if (has2(value)(self)) {
      add(value)(set4);
    }
  }
}));
var union2 = dual(2, (self, that) => mutate2(empty5(), (set4) => {
  forEach2(self, (value) => add(set4, value));
  for (const value of that) {
    add(set4, value);
  }
}));
var toggle = dual(2, (self, value) => has2(self, value) ? remove2(self, value) : add(self, value));
var map3 = dual(2, (self, f) => mutate2(empty5(), (set4) => {
  forEach2(self, (a) => {
    const b = f(a);
    if (!has2(set4, b)) {
      add(set4, b);
    }
  });
}));
var flatMap2 = dual(2, (self, f) => mutate2(empty5(), (set4) => {
  forEach2(self, (a) => {
    for (const b of f(a)) {
      if (!has2(set4, b)) {
        add(set4, b);
      }
    }
  });
}));
var forEach2 = dual(2, (self, f) => forEach(self._keyMap, (_, k) => f(k)));
var reduce3 = dual(3, (self, zero2, f) => reduce2(self._keyMap, zero2, (z, _, a) => f(z, a)));
var filter2 = dual(2, (self, f) => {
  return mutate2(empty5(), (set4) => {
    const iterator = values2(self);
    let next;
    while (!(next = iterator.next()).done) {
      const value = next.value;
      if (f(value)) {
        add(set4, value);
      }
    }
  });
});
var partition = dual(2, (self, predicate) => {
  const iterator = values2(self);
  let next;
  const right2 = beginMutation2(empty5());
  const left2 = beginMutation2(empty5());
  while (!(next = iterator.next()).done) {
    const value = next.value;
    if (predicate(value)) {
      add(right2, value);
    } else {
      add(left2, value);
    }
  }
  return [endMutation2(left2), endMutation2(right2)];
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/HashSet.js
var isHashSet2 = isHashSet;
var empty6 = empty5;
var fromIterable5 = fromIterable4;
var make7 = make6;
var has3 = has2;
var some4 = some3;
var every3 = every2;
var isSubset2 = isSubset;
var values3 = values2;
var toValues = (self) => Array.from(values3(self));
var size3 = size2;
var beginMutation3 = beginMutation2;
var endMutation3 = endMutation2;
var mutate3 = mutate2;
var add2 = add;
var remove3 = remove2;
var difference2 = difference;
var intersection2 = intersection;
var union3 = union2;
var toggle2 = toggle;
var map4 = map3;
var flatMap3 = flatMap2;
var forEach3 = forEach2;
var reduce4 = reduce3;
var filter3 = filter2;
var partition2 = partition;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MutableRef.js
var MutableRef_exports = {};
__export(MutableRef_exports, {
  compareAndSet: () => compareAndSet,
  decrement: () => decrement,
  decrementAndGet: () => decrementAndGet,
  get: () => get2,
  getAndDecrement: () => getAndDecrement,
  getAndIncrement: () => getAndIncrement,
  getAndSet: () => getAndSet,
  getAndUpdate: () => getAndUpdate,
  increment: () => increment,
  incrementAndGet: () => incrementAndGet,
  make: () => make8,
  set: () => set2,
  setAndGet: () => setAndGet,
  toggle: () => toggle3,
  update: () => update,
  updateAndGet: () => updateAndGet
});
var TypeId2 = Symbol.for("effect/MutableRef");
var MutableRefProto = {
  [TypeId2]: TypeId2,
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "MutableRef",
      current: toJSON(this.current)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make8 = (value) => {
  const ref = Object.create(MutableRefProto);
  ref.current = value;
  return ref;
};
var compareAndSet = dual(3, (self, oldValue, newValue) => {
  if (equals(oldValue, self.current)) {
    self.current = newValue;
    return true;
  }
  return false;
});
var decrement = (self) => update(self, (n) => n - 1);
var decrementAndGet = (self) => updateAndGet(self, (n) => n - 1);
var get2 = (self) => self.current;
var getAndDecrement = (self) => getAndUpdate(self, (n) => n - 1);
var getAndIncrement = (self) => getAndUpdate(self, (n) => n + 1);
var getAndSet = dual(2, (self, value) => {
  const ret = self.current;
  self.current = value;
  return ret;
});
var getAndUpdate = dual(2, (self, f) => getAndSet(self, f(get2(self))));
var increment = (self) => update(self, (n) => n + 1);
var incrementAndGet = (self) => updateAndGet(self, (n) => n + 1);
var set2 = dual(2, (self, value) => {
  self.current = value;
  return self;
});
var setAndGet = dual(2, (self, value) => {
  self.current = value;
  return self.current;
});
var update = dual(2, (self, f) => set2(self, f(get2(self))));
var updateAndGet = dual(2, (self, f) => setAndGet(self, f(get2(self))));
var toggle3 = (self) => update(self, (_) => !_);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberId.js
var FiberId_exports = {};
__export(FiberId_exports, {
  FiberIdTypeId: () => FiberIdTypeId2,
  combine: () => combine3,
  combineAll: () => combineAll2,
  composite: () => composite2,
  getOrElse: () => getOrElse3,
  ids: () => ids2,
  isComposite: () => isComposite2,
  isFiberId: () => isFiberId2,
  isNone: () => isNone3,
  isRuntime: () => isRuntime2,
  make: () => make10,
  none: () => none3,
  runtime: () => runtime2,
  threadName: () => threadName2,
  toOption: () => toOption2,
  toSet: () => toSet2,
  unsafeMake: () => unsafeMake2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/fiberId.js
var FiberIdSymbolKey = "effect/FiberId";
var FiberIdTypeId = Symbol.for(FiberIdSymbolKey);
var OP_NONE = "None";
var OP_RUNTIME = "Runtime";
var OP_COMPOSITE = "Composite";
var emptyHash = string(`${FiberIdSymbolKey}-${OP_NONE}`);
var None = class {
  [FiberIdTypeId] = FiberIdTypeId;
  _tag = OP_NONE;
  id = -1;
  startTimeMillis = -1;
  [symbol]() {
    return emptyHash;
  }
  [symbol2](that) {
    return isFiberId(that) && that._tag === OP_NONE;
  }
  toString() {
    return format(this.toJSON());
  }
  toJSON() {
    return {
      _id: "FiberId",
      _tag: this._tag
    };
  }
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
};
var Runtime = class {
  id;
  startTimeMillis;
  [FiberIdTypeId] = FiberIdTypeId;
  _tag = OP_RUNTIME;
  constructor(id, startTimeMillis) {
    this.id = id;
    this.startTimeMillis = startTimeMillis;
  }
  [symbol]() {
    return cached(this, string(`${FiberIdSymbolKey}-${this._tag}-${this.id}-${this.startTimeMillis}`));
  }
  [symbol2](that) {
    return isFiberId(that) && that._tag === OP_RUNTIME && this.id === that.id && this.startTimeMillis === that.startTimeMillis;
  }
  toString() {
    return format(this.toJSON());
  }
  toJSON() {
    return {
      _id: "FiberId",
      _tag: this._tag,
      id: this.id,
      startTimeMillis: this.startTimeMillis
    };
  }
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
};
var Composite = class {
  left;
  right;
  [FiberIdTypeId] = FiberIdTypeId;
  _tag = OP_COMPOSITE;
  constructor(left2, right2) {
    this.left = left2;
    this.right = right2;
  }
  _hash;
  [symbol]() {
    return pipe(string(`${FiberIdSymbolKey}-${this._tag}`), combine(hash(this.left)), combine(hash(this.right)), cached(this));
  }
  [symbol2](that) {
    return isFiberId(that) && that._tag === OP_COMPOSITE && equals(this.left, that.left) && equals(this.right, that.right);
  }
  toString() {
    return format(this.toJSON());
  }
  toJSON() {
    return {
      _id: "FiberId",
      _tag: this._tag,
      left: toJSON(this.left),
      right: toJSON(this.right)
    };
  }
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
};
var none2 = new None();
var runtime = (id, startTimeMillis) => {
  return new Runtime(id, startTimeMillis);
};
var composite = (left2, right2) => {
  return new Composite(left2, right2);
};
var isFiberId = (self) => hasProperty(self, FiberIdTypeId);
var isNone2 = (self) => {
  return self._tag === OP_NONE || pipe(toSet(self), every3((id) => isNone2(id)));
};
var isRuntime = (self) => {
  return self._tag === OP_RUNTIME;
};
var isComposite = (self) => {
  return self._tag === OP_COMPOSITE;
};
var combine2 = dual(2, (self, that) => {
  if (self._tag === OP_NONE) {
    return that;
  }
  if (that._tag === OP_NONE) {
    return self;
  }
  return new Composite(self, that);
});
var combineAll = (fiberIds) => {
  return pipe(fiberIds, reduce4(none2, (a, b) => combine2(b)(a)));
};
var getOrElse2 = dual(2, (self, that) => isNone2(self) ? that : self);
var ids = (self) => {
  switch (self._tag) {
    case OP_NONE: {
      return empty6();
    }
    case OP_RUNTIME: {
      return make7(self.id);
    }
    case OP_COMPOSITE: {
      return pipe(ids(self.left), union3(ids(self.right)));
    }
  }
};
var _fiberCounter = globalValue(Symbol.for("effect/Fiber/Id/_fiberCounter"), () => make8(0));
var make9 = (id, startTimeSeconds) => {
  return new Runtime(id, startTimeSeconds);
};
var threadName = (self) => {
  const identifiers = Array.from(ids(self)).map((n) => `#${n}`).join(",");
  return identifiers;
};
var toOption = (self) => {
  const fiberIds = toSet(self);
  if (size3(fiberIds) === 0) {
    return none();
  }
  let first = true;
  let acc;
  for (const fiberId2 of fiberIds) {
    if (first) {
      acc = fiberId2;
      first = false;
    } else {
      acc = pipe(acc, combine2(fiberId2));
    }
  }
  return some(acc);
};
var toSet = (self) => {
  switch (self._tag) {
    case OP_NONE: {
      return empty6();
    }
    case OP_RUNTIME: {
      return make7(self);
    }
    case OP_COMPOSITE: {
      return pipe(toSet(self.left), union3(toSet(self.right)));
    }
  }
};
var unsafeMake = () => {
  const id = get2(_fiberCounter);
  pipe(_fiberCounter, set2(id + 1));
  return new Runtime(id, Date.now());
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberId.js
var FiberIdTypeId2 = FiberIdTypeId;
var none3 = none2;
var runtime2 = runtime;
var composite2 = composite;
var isFiberId2 = isFiberId;
var isNone3 = isNone2;
var isRuntime2 = isRuntime;
var isComposite2 = isComposite;
var combine3 = combine2;
var combineAll2 = combineAll;
var getOrElse3 = getOrElse2;
var ids2 = ids;
var make10 = make9;
var threadName2 = threadName;
var toOption2 = toOption;
var toSet2 = toSet;
var unsafeMake2 = unsafeMake;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/HashMap.js
var HashMap_exports = {};
__export(HashMap_exports, {
  beginMutation: () => beginMutation4,
  compact: () => compact2,
  countBy: () => countBy2,
  empty: () => empty7,
  endMutation: () => endMutation4,
  entries: () => entries2,
  every: () => every4,
  filter: () => filter4,
  filterMap: () => filterMap2,
  findFirst: () => findFirst2,
  flatMap: () => flatMap4,
  forEach: () => forEach4,
  fromIterable: () => fromIterable6,
  get: () => get3,
  getHash: () => getHash2,
  has: () => has4,
  hasBy: () => hasBy2,
  hasHash: () => hasHash2,
  isEmpty: () => isEmpty3,
  isHashMap: () => isHashMap2,
  keySet: () => keySet2,
  keys: () => keys2,
  make: () => make11,
  map: () => map5,
  modify: () => modify2,
  modifyAt: () => modifyAt2,
  modifyHash: () => modifyHash2,
  mutate: () => mutate4,
  reduce: () => reduce5,
  remove: () => remove4,
  removeMany: () => removeMany2,
  set: () => set3,
  size: () => size4,
  some: () => some5,
  toEntries: () => toEntries,
  toValues: () => toValues2,
  union: () => union4,
  unsafeGet: () => unsafeGet3,
  values: () => values4
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/hashMap/keySet.js
function keySet(self) {
  return makeImpl2(self);
}

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/HashMap.js
var isHashMap2 = isHashMap;
var empty7 = empty4;
var make11 = make5;
var fromIterable6 = fromIterable3;
var isEmpty3 = isEmpty2;
var get3 = get;
var getHash2 = getHash;
var unsafeGet3 = unsafeGet2;
var has4 = has;
var hasHash2 = hasHash;
var hasBy2 = hasBy;
var set3 = set;
var keys2 = keys;
var keySet2 = keySet;
var values4 = values;
var toValues2 = (self) => Array.from(values4(self));
var entries2 = entries;
var toEntries = (self) => Array.from(entries2(self));
var size4 = size;
var countBy2 = countBy;
var beginMutation4 = beginMutation;
var endMutation4 = endMutation;
var mutate4 = mutate;
var modifyAt2 = modifyAt;
var modifyHash2 = modifyHash;
var modify2 = modify;
var union4 = union;
var remove4 = remove;
var removeMany2 = removeMany;
var map5 = map2;
var flatMap4 = flatMap;
var forEach4 = forEach;
var reduce5 = reduce2;
var filter4 = filter;
var compact2 = compact;
var filterMap2 = filterMap;
var findFirst2 = findFirst;
var some5 = some2;
var every4 = every;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/List.js
var List_exports = {};
__export(List_exports, {
  TypeId: () => TypeId3,
  append: () => append2,
  appendAll: () => appendAll2,
  compact: () => compact3,
  cons: () => cons,
  drop: () => drop3,
  empty: () => empty8,
  every: () => every5,
  filter: () => filter5,
  filterMap: () => filterMap3,
  findFirst: () => findFirst3,
  flatMap: () => flatMap5,
  forEach: () => forEach5,
  fromIterable: () => fromIterable7,
  getEquivalence: () => getEquivalence2,
  head: () => head2,
  isCons: () => isCons,
  isList: () => isList,
  isNil: () => isNil,
  last: () => last,
  make: () => make12,
  map: () => map6,
  nil: () => nil,
  of: () => of3,
  partition: () => partition3,
  partitionMap: () => partitionMap2,
  prepend: () => prepend2,
  prependAll: () => prependAll,
  prependAllReversed: () => prependAllReversed,
  reduce: () => reduce6,
  reduceRight: () => reduceRight,
  reverse: () => reverse2,
  size: () => size5,
  some: () => some6,
  splitAt: () => splitAt,
  tail: () => tail,
  take: () => take,
  toArray: () => toArray,
  toChunk: () => toChunk,
  unsafeHead: () => unsafeHead2,
  unsafeLast: () => unsafeLast,
  unsafeTail: () => unsafeTail
});
var TypeId3 = Symbol.for("effect/List");
var toArray = (self) => fromIterable(self);
var getEquivalence2 = (isEquivalent) => mapInput(getEquivalence(isEquivalent), toArray);
var _equivalence = getEquivalence2(equals);
var ConsProto = {
  [TypeId3]: TypeId3,
  _tag: "Cons",
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "List",
      _tag: "Cons",
      values: toArray(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  [symbol2](that) {
    return isList(that) && this._tag === that._tag && _equivalence(this, that);
  },
  [symbol]() {
    return cached(this, array(toArray(this)));
  },
  [Symbol.iterator]() {
    let done2 = false;
    let self = this;
    return {
      next() {
        if (done2) {
          return this.return();
        }
        if (self._tag === "Nil") {
          done2 = true;
          return this.return();
        }
        const value = self.head;
        self = self.tail;
        return {
          done: done2,
          value
        };
      },
      return(value) {
        if (!done2) {
          done2 = true;
        }
        return {
          done: true,
          value
        };
      }
    };
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeCons = (head3, tail2) => {
  const cons2 = Object.create(ConsProto);
  cons2.head = head3;
  cons2.tail = tail2;
  return cons2;
};
var NilHash = string("Nil");
var NilProto = {
  [TypeId3]: TypeId3,
  _tag: "Nil",
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "List",
      _tag: "Nil"
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  [symbol]() {
    return NilHash;
  },
  [symbol2](that) {
    return isList(that) && this._tag === that._tag;
  },
  [Symbol.iterator]() {
    return {
      next() {
        return {
          done: true,
          value: void 0
        };
      }
    };
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var _Nil = Object.create(NilProto);
var isList = (u) => hasProperty(u, TypeId3);
var isNil = (self) => self._tag === "Nil";
var isCons = (self) => self._tag === "Cons";
var size5 = (self) => {
  let these = self;
  let len = 0;
  while (!isNil(these)) {
    len += 1;
    these = these.tail;
  }
  return len;
};
var nil = () => _Nil;
var cons = (head3, tail2) => makeCons(head3, tail2);
var empty8 = nil;
var of3 = (value) => makeCons(value, _Nil);
var fromIterable7 = (prefix) => {
  const iterator = prefix[Symbol.iterator]();
  let next;
  if ((next = iterator.next()) && !next.done) {
    const result = makeCons(next.value, _Nil);
    let curr = result;
    while ((next = iterator.next()) && !next.done) {
      const temp = makeCons(next.value, _Nil);
      curr.tail = temp;
      curr = temp;
    }
    return result;
  } else {
    return _Nil;
  }
};
var make12 = (...elements) => fromIterable7(elements);
var append2 = dual(2, (self, element) => appendAll2(self, of3(element)));
var appendAll2 = dual(2, (self, that) => prependAll(that, self));
var prepend2 = dual(2, (self, element) => cons(element, self));
var prependAll = dual(2, (self, prefix) => {
  if (isNil(self)) {
    return prefix;
  } else if (isNil(prefix)) {
    return self;
  } else {
    const result = makeCons(prefix.head, self);
    let curr = result;
    let that = prefix.tail;
    while (!isNil(that)) {
      const temp = makeCons(that.head, self);
      curr.tail = temp;
      curr = temp;
      that = that.tail;
    }
    return result;
  }
});
var prependAllReversed = dual(2, (self, prefix) => {
  let out = self;
  let pres = prefix;
  while (isCons(pres)) {
    out = makeCons(pres.head, out);
    pres = pres.tail;
  }
  return out;
});
var drop3 = dual(2, (self, n) => {
  if (n <= 0) {
    return self;
  }
  if (n >= size5(self)) {
    return _Nil;
  }
  let these = self;
  let i = 0;
  while (!isNil(these) && i < n) {
    these = these.tail;
    i += 1;
  }
  return these;
});
var every5 = dual(2, (self, refinement) => {
  for (const a of self) {
    if (!refinement(a)) {
      return false;
    }
  }
  return true;
});
var some6 = dual(2, (self, predicate) => {
  let these = self;
  while (!isNil(these)) {
    if (predicate(these.head)) {
      return true;
    }
    these = these.tail;
  }
  return false;
});
var filter5 = dual(2, (self, predicate) => noneIn(self, predicate, false));
var noneIn = (self, predicate, isFlipped) => {
  while (true) {
    if (isNil(self)) {
      return _Nil;
    } else {
      if (predicate(self.head) !== isFlipped) {
        return allIn(self, self.tail, predicate, isFlipped);
      } else {
        self = self.tail;
      }
    }
  }
};
var allIn = (start, remaining, predicate, isFlipped) => {
  while (true) {
    if (isNil(remaining)) {
      return start;
    } else {
      if (predicate(remaining.head) !== isFlipped) {
        remaining = remaining.tail;
      } else {
        return partialFill(start, remaining, predicate, isFlipped);
      }
    }
  }
};
var partialFill = (origStart, firstMiss, predicate, isFlipped) => {
  const newHead = makeCons(unsafeHead2(origStart), _Nil);
  let toProcess = unsafeTail(origStart);
  let currentLast = newHead;
  while (!(toProcess === firstMiss)) {
    const newElem = makeCons(unsafeHead2(toProcess), _Nil);
    currentLast.tail = newElem;
    currentLast = unsafeCoerce(newElem);
    toProcess = unsafeCoerce(toProcess.tail);
  }
  let next = firstMiss.tail;
  let nextToCopy = unsafeCoerce(next);
  while (!isNil(next)) {
    const head3 = unsafeHead2(next);
    if (predicate(head3) !== isFlipped) {
      next = next.tail;
    } else {
      while (!(nextToCopy === next)) {
        const newElem = makeCons(unsafeHead2(nextToCopy), _Nil);
        currentLast.tail = newElem;
        currentLast = newElem;
        nextToCopy = unsafeCoerce(nextToCopy.tail);
      }
      nextToCopy = unsafeCoerce(next.tail);
      next = next.tail;
    }
  }
  if (!isNil(nextToCopy)) {
    currentLast.tail = nextToCopy;
  }
  return newHead;
};
var filterMap3 = dual(2, (self, f) => {
  const bs = [];
  for (const a of self) {
    const oa = f(a);
    if (isSome(oa)) {
      bs.push(oa.value);
    }
  }
  return fromIterable7(bs);
});
var compact3 = (self) => filterMap3(self, identity);
var findFirst3 = dual(2, (self, predicate) => {
  let these = self;
  while (!isNil(these)) {
    if (predicate(these.head)) {
      return some(these.head);
    }
    these = these.tail;
  }
  return none();
});
var flatMap5 = dual(2, (self, f) => {
  let rest = self;
  let head3 = void 0;
  let tail2 = void 0;
  while (!isNil(rest)) {
    let bs = f(rest.head);
    while (!isNil(bs)) {
      const next = makeCons(bs.head, _Nil);
      if (tail2 === void 0) {
        head3 = next;
      } else {
        tail2.tail = next;
      }
      tail2 = next;
      bs = bs.tail;
    }
    rest = rest.tail;
  }
  if (head3 === void 0) {
    return _Nil;
  }
  return head3;
});
var forEach5 = dual(2, (self, f) => {
  let these = self;
  while (!isNil(these)) {
    f(these.head);
    these = these.tail;
  }
});
var head2 = (self) => isNil(self) ? none() : some(self.head);
var last = (self) => isNil(self) ? none() : some(unsafeLast(self));
var map6 = dual(2, (self, f) => {
  if (isNil(self)) {
    return self;
  } else {
    let i = 0;
    const head3 = makeCons(f(self.head, i++), _Nil);
    let nextHead = head3;
    let rest = self.tail;
    while (!isNil(rest)) {
      const next = makeCons(f(rest.head, i++), _Nil);
      nextHead.tail = next;
      nextHead = next;
      rest = rest.tail;
    }
    return head3;
  }
});
var partition3 = dual(2, (self, predicate) => {
  const left2 = [];
  const right2 = [];
  for (const a of self) {
    if (predicate(a)) {
      right2.push(a);
    } else {
      left2.push(a);
    }
  }
  return [fromIterable7(left2), fromIterable7(right2)];
});
var partitionMap2 = dual(2, (self, f) => {
  const left2 = [];
  const right2 = [];
  for (const a of self) {
    const e = f(a);
    if (isLeft(e)) {
      left2.push(e.left);
    } else {
      right2.push(e.right);
    }
  }
  return [fromIterable7(left2), fromIterable7(right2)];
});
var reduce6 = dual(3, (self, zero2, f) => {
  let acc = zero2;
  let these = self;
  while (!isNil(these)) {
    acc = f(acc, these.head);
    these = these.tail;
  }
  return acc;
});
var reduceRight = dual(3, (self, zero2, f) => {
  let acc = zero2;
  let these = reverse2(self);
  while (!isNil(these)) {
    acc = f(acc, these.head);
    these = these.tail;
  }
  return acc;
});
var reverse2 = (self) => {
  let result = empty8();
  let these = self;
  while (!isNil(these)) {
    result = prepend2(result, these.head);
    these = these.tail;
  }
  return result;
};
var splitAt = dual(2, (self, n) => [take(self, n), drop3(self, n)]);
var tail = (self) => isNil(self) ? none() : some(self.tail);
var take = dual(2, (self, n) => {
  if (n <= 0) {
    return _Nil;
  }
  if (n >= size5(self)) {
    return self;
  }
  let these = make12(unsafeHead2(self));
  let current = unsafeTail(self);
  for (let i = 1; i < n; i++) {
    these = makeCons(unsafeHead2(current), these);
    current = unsafeTail(current);
  }
  return reverse2(these);
});
var toChunk = (self) => fromIterable2(self);
var getExpectedListToBeNonEmptyErrorMessage = "Expected List to be non-empty";
var unsafeHead2 = (self) => {
  if (isNil(self)) {
    throw new Error(getExpectedListToBeNonEmptyErrorMessage);
  }
  return self.head;
};
var unsafeLast = (self) => {
  if (isNil(self)) {
    throw new Error(getExpectedListToBeNonEmptyErrorMessage);
  }
  let these = self;
  let scout = self.tail;
  while (!isNil(scout)) {
    these = scout;
    scout = scout.tail;
  }
  return these.head;
};
var unsafeTail = (self) => {
  if (isNil(self)) {
    throw new Error(getExpectedListToBeNonEmptyErrorMessage);
  }
  return self.tail;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/data.js
var ArrayProto = Object.assign(Object.create(Array.prototype), {
  [symbol]() {
    return cached(this, array(this));
  },
  [symbol2](that) {
    if (Array.isArray(that) && this.length === that.length) {
      return this.every((v, i) => equals(v, that[i]));
    } else {
      return false;
    }
  }
});
var Structural = (function() {
  function Structural2(args) {
    if (args) {
      Object.assign(this, args);
    }
  }
  Structural2.prototype = StructuralPrototype;
  return Structural2;
})();
var struct = (as3) => Object.assign(Object.create(StructuralPrototype), as3);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RuntimeFlagsPatch.js
var RuntimeFlagsPatch_exports = {};
__export(RuntimeFlagsPatch_exports, {
  andThen: () => andThen2,
  both: () => both2,
  disable: () => disable3,
  disabledSet: () => disabledSet2,
  either: () => either2,
  empty: () => empty16,
  enable: () => enable3,
  enabledSet: () => enabledSet2,
  exclude: () => exclude2,
  includes: () => includes,
  inverse: () => inverse2,
  isActive: () => isActive2,
  isDisabled: () => isDisabled3,
  isEmpty: () => isEmpty5,
  isEnabled: () => isEnabled3,
  make: () => make16,
  render: () => render2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/differ/chunkPatch.js
var ChunkPatchTypeId = Symbol.for("effect/DifferChunkPatch");
function variance(a) {
  return a;
}
var PatchProto = {
  ...Structural.prototype,
  [ChunkPatchTypeId]: {
    _Value: variance,
    _Patch: variance
  }
};
var EmptyProto = Object.assign(Object.create(PatchProto), {
  _tag: "Empty"
});
var _empty3 = Object.create(EmptyProto);
var empty9 = () => _empty3;
var AndThenProto = Object.assign(Object.create(PatchProto), {
  _tag: "AndThen"
});
var makeAndThen = (first, second) => {
  const o = Object.create(AndThenProto);
  o.first = first;
  o.second = second;
  return o;
};
var AppendProto = Object.assign(Object.create(PatchProto), {
  _tag: "Append"
});
var makeAppend = (values5) => {
  const o = Object.create(AppendProto);
  o.values = values5;
  return o;
};
var SliceProto = Object.assign(Object.create(PatchProto), {
  _tag: "Slice"
});
var makeSlice = (from, until) => {
  const o = Object.create(SliceProto);
  o.from = from;
  o.until = until;
  return o;
};
var UpdateProto = Object.assign(Object.create(PatchProto), {
  _tag: "Update"
});
var makeUpdate = (index, patch8) => {
  const o = Object.create(UpdateProto);
  o.index = index;
  o.patch = patch8;
  return o;
};
var diff = (options) => {
  let i = 0;
  let patch8 = empty9();
  while (i < options.oldValue.length && i < options.newValue.length) {
    const oldElement = unsafeGet(i)(options.oldValue);
    const newElement = unsafeGet(i)(options.newValue);
    const valuePatch = options.differ.diff(oldElement, newElement);
    if (!equals(valuePatch, options.differ.empty)) {
      patch8 = pipe(patch8, combine4(makeUpdate(i, valuePatch)));
    }
    i = i + 1;
  }
  if (i < options.oldValue.length) {
    patch8 = pipe(patch8, combine4(makeSlice(0, i)));
  }
  if (i < options.newValue.length) {
    patch8 = pipe(patch8, combine4(makeAppend(drop2(i)(options.newValue))));
  }
  return patch8;
};
var combine4 = dual(2, (self, that) => makeAndThen(self, that));
var patch = dual(3, (self, oldValue, differ2) => {
  if (self._tag === "Empty") {
    return oldValue;
  }
  let chunk2 = oldValue;
  let patches = of2(self);
  while (isNonEmpty(patches)) {
    const head3 = headNonEmpty2(patches);
    const tail2 = tailNonEmpty2(patches);
    switch (head3._tag) {
      case "Empty": {
        patches = tail2;
        break;
      }
      case "AndThen": {
        patches = prepend(head3.first)(prepend(head3.second)(tail2));
        break;
      }
      case "Append": {
        chunk2 = appendAll(head3.values)(chunk2);
        patches = tail2;
        break;
      }
      case "Slice": {
        const array2 = toReadonlyArray(chunk2);
        chunk2 = unsafeFromArray(array2.slice(head3.from, head3.until));
        patches = tail2;
        break;
      }
      case "Update": {
        const array2 = toReadonlyArray(chunk2);
        array2[head3.index] = differ2.patch(head3.patch, array2[head3.index]);
        chunk2 = unsafeFromArray(array2);
        patches = tail2;
        break;
      }
    }
  }
  return chunk2;
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/differ/contextPatch.js
var ContextPatchTypeId = Symbol.for("effect/DifferContextPatch");
function variance2(a) {
  return a;
}
var PatchProto2 = {
  ...Structural.prototype,
  [ContextPatchTypeId]: {
    _Value: variance2,
    _Patch: variance2
  }
};
var EmptyProto2 = Object.assign(Object.create(PatchProto2), {
  _tag: "Empty"
});
var _empty4 = Object.create(EmptyProto2);
var empty10 = () => _empty4;
var AndThenProto2 = Object.assign(Object.create(PatchProto2), {
  _tag: "AndThen"
});
var makeAndThen2 = (first, second) => {
  const o = Object.create(AndThenProto2);
  o.first = first;
  o.second = second;
  return o;
};
var AddServiceProto = Object.assign(Object.create(PatchProto2), {
  _tag: "AddService"
});
var makeAddService = (key, service) => {
  const o = Object.create(AddServiceProto);
  o.key = key;
  o.service = service;
  return o;
};
var RemoveServiceProto = Object.assign(Object.create(PatchProto2), {
  _tag: "RemoveService"
});
var makeRemoveService = (key) => {
  const o = Object.create(RemoveServiceProto);
  o.key = key;
  return o;
};
var UpdateServiceProto = Object.assign(Object.create(PatchProto2), {
  _tag: "UpdateService"
});
var makeUpdateService = (key, update3) => {
  const o = Object.create(UpdateServiceProto);
  o.key = key;
  o.update = update3;
  return o;
};
var diff2 = (oldValue, newValue) => {
  const missingServices = new Map(oldValue.unsafeMap);
  let patch8 = empty10();
  for (const [tag, newService] of newValue.unsafeMap.entries()) {
    if (missingServices.has(tag)) {
      const old = missingServices.get(tag);
      missingServices.delete(tag);
      if (!equals(old, newService)) {
        patch8 = combine5(makeUpdateService(tag, () => newService))(patch8);
      }
    } else {
      missingServices.delete(tag);
      patch8 = combine5(makeAddService(tag, newService))(patch8);
    }
  }
  for (const [tag] of missingServices.entries()) {
    patch8 = combine5(makeRemoveService(tag))(patch8);
  }
  return patch8;
};
var combine5 = dual(2, (self, that) => makeAndThen2(self, that));
var patch2 = dual(2, (self, context2) => {
  if (self._tag === "Empty") {
    return context2;
  }
  let wasServiceUpdated = false;
  let patches = of2(self);
  const updatedContext = new Map(context2.unsafeMap);
  while (isNonEmpty(patches)) {
    const head3 = headNonEmpty2(patches);
    const tail2 = tailNonEmpty2(patches);
    switch (head3._tag) {
      case "Empty": {
        patches = tail2;
        break;
      }
      case "AddService": {
        updatedContext.set(head3.key, head3.service);
        patches = tail2;
        break;
      }
      case "AndThen": {
        patches = prepend(prepend(tail2, head3.second), head3.first);
        break;
      }
      case "RemoveService": {
        updatedContext.delete(head3.key);
        patches = tail2;
        break;
      }
      case "UpdateService": {
        updatedContext.set(head3.key, head3.update(updatedContext.get(head3.key)));
        wasServiceUpdated = true;
        patches = tail2;
        break;
      }
    }
  }
  if (!wasServiceUpdated) {
    return makeContext(updatedContext);
  }
  const map9 = /* @__PURE__ */ new Map();
  for (const [tag] of context2.unsafeMap) {
    if (updatedContext.has(tag)) {
      map9.set(tag, updatedContext.get(tag));
      updatedContext.delete(tag);
    }
  }
  for (const [tag, s] of updatedContext) {
    map9.set(tag, s);
  }
  return makeContext(map9);
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/differ/hashMapPatch.js
var HashMapPatchTypeId = Symbol.for("effect/DifferHashMapPatch");
function variance3(a) {
  return a;
}
var PatchProto3 = {
  ...Structural.prototype,
  [HashMapPatchTypeId]: {
    _Value: variance3,
    _Key: variance3,
    _Patch: variance3
  }
};
var EmptyProto3 = Object.assign(Object.create(PatchProto3), {
  _tag: "Empty"
});
var _empty5 = Object.create(EmptyProto3);
var empty11 = () => _empty5;
var AndThenProto3 = Object.assign(Object.create(PatchProto3), {
  _tag: "AndThen"
});
var makeAndThen3 = (first, second) => {
  const o = Object.create(AndThenProto3);
  o.first = first;
  o.second = second;
  return o;
};
var AddProto = Object.assign(Object.create(PatchProto3), {
  _tag: "Add"
});
var makeAdd = (key, value) => {
  const o = Object.create(AddProto);
  o.key = key;
  o.value = value;
  return o;
};
var RemoveProto = Object.assign(Object.create(PatchProto3), {
  _tag: "Remove"
});
var makeRemove = (key) => {
  const o = Object.create(RemoveProto);
  o.key = key;
  return o;
};
var UpdateProto2 = Object.assign(Object.create(PatchProto3), {
  _tag: "Update"
});
var makeUpdate2 = (key, patch8) => {
  const o = Object.create(UpdateProto2);
  o.key = key;
  o.patch = patch8;
  return o;
};
var diff3 = (options) => {
  const [removed, patch8] = reduce5([options.oldValue, empty11()], ([map9, patch9], newValue, key) => {
    const option = get3(key)(map9);
    switch (option._tag) {
      case "Some": {
        const valuePatch = options.differ.diff(option.value, newValue);
        if (equals(valuePatch, options.differ.empty)) {
          return [remove4(key)(map9), patch9];
        }
        return [remove4(key)(map9), combine6(makeUpdate2(key, valuePatch))(patch9)];
      }
      case "None": {
        return [map9, combine6(makeAdd(key, newValue))(patch9)];
      }
    }
  })(options.newValue);
  return reduce5(patch8, (patch9, _, key) => combine6(makeRemove(key))(patch9))(removed);
};
var combine6 = dual(2, (self, that) => makeAndThen3(self, that));
var patch3 = dual(3, (self, oldValue, differ2) => {
  if (self._tag === "Empty") {
    return oldValue;
  }
  let map9 = oldValue;
  let patches = of2(self);
  while (isNonEmpty(patches)) {
    const head3 = headNonEmpty2(patches);
    const tail2 = tailNonEmpty2(patches);
    switch (head3._tag) {
      case "Empty": {
        patches = tail2;
        break;
      }
      case "AndThen": {
        patches = prepend(head3.first)(prepend(head3.second)(tail2));
        break;
      }
      case "Add": {
        map9 = set3(head3.key, head3.value)(map9);
        patches = tail2;
        break;
      }
      case "Remove": {
        map9 = remove4(head3.key)(map9);
        patches = tail2;
        break;
      }
      case "Update": {
        const option = get3(head3.key)(map9);
        if (option._tag === "Some") {
          map9 = set3(head3.key, differ2.patch(head3.patch, option.value))(map9);
        }
        patches = tail2;
        break;
      }
    }
  }
  return map9;
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/differ/hashSetPatch.js
var HashSetPatchTypeId = Symbol.for("effect/DifferHashSetPatch");
function variance4(a) {
  return a;
}
var PatchProto4 = {
  ...Structural.prototype,
  [HashSetPatchTypeId]: {
    _Value: variance4,
    _Key: variance4,
    _Patch: variance4
  }
};
var EmptyProto4 = Object.assign(Object.create(PatchProto4), {
  _tag: "Empty"
});
var _empty6 = Object.create(EmptyProto4);
var empty12 = () => _empty6;
var AndThenProto4 = Object.assign(Object.create(PatchProto4), {
  _tag: "AndThen"
});
var makeAndThen4 = (first, second) => {
  const o = Object.create(AndThenProto4);
  o.first = first;
  o.second = second;
  return o;
};
var AddProto2 = Object.assign(Object.create(PatchProto4), {
  _tag: "Add"
});
var makeAdd2 = (value) => {
  const o = Object.create(AddProto2);
  o.value = value;
  return o;
};
var RemoveProto2 = Object.assign(Object.create(PatchProto4), {
  _tag: "Remove"
});
var makeRemove2 = (value) => {
  const o = Object.create(RemoveProto2);
  o.value = value;
  return o;
};
var diff4 = (oldValue, newValue) => {
  const [removed, patch8] = reduce4([oldValue, empty12()], ([set4, patch9], value) => {
    if (has3(value)(set4)) {
      return [remove3(value)(set4), patch9];
    }
    return [set4, combine7(makeAdd2(value))(patch9)];
  })(newValue);
  return reduce4(patch8, (patch9, value) => combine7(makeRemove2(value))(patch9))(removed);
};
var combine7 = dual(2, (self, that) => makeAndThen4(self, that));
var patch4 = dual(2, (self, oldValue) => {
  if (self._tag === "Empty") {
    return oldValue;
  }
  let set4 = oldValue;
  let patches = of2(self);
  while (isNonEmpty(patches)) {
    const head3 = headNonEmpty2(patches);
    const tail2 = tailNonEmpty2(patches);
    switch (head3._tag) {
      case "Empty": {
        patches = tail2;
        break;
      }
      case "AndThen": {
        patches = prepend(head3.first)(prepend(head3.second)(tail2));
        break;
      }
      case "Add": {
        set4 = add2(head3.value)(set4);
        patches = tail2;
        break;
      }
      case "Remove": {
        set4 = remove3(head3.value)(set4);
        patches = tail2;
      }
    }
  }
  return set4;
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/differ/orPatch.js
var OrPatchTypeId = Symbol.for("effect/DifferOrPatch");
function variance5(a) {
  return a;
}
var PatchProto5 = {
  ...Structural.prototype,
  [OrPatchTypeId]: {
    _Value: variance5,
    _Key: variance5,
    _Patch: variance5
  }
};
var EmptyProto5 = Object.assign(Object.create(PatchProto5), {
  _tag: "Empty"
});
var _empty7 = Object.create(EmptyProto5);
var empty13 = () => _empty7;
var AndThenProto5 = Object.assign(Object.create(PatchProto5), {
  _tag: "AndThen"
});
var makeAndThen5 = (first, second) => {
  const o = Object.create(AndThenProto5);
  o.first = first;
  o.second = second;
  return o;
};
var SetLeftProto = Object.assign(Object.create(PatchProto5), {
  _tag: "SetLeft"
});
var makeSetLeft = (value) => {
  const o = Object.create(SetLeftProto);
  o.value = value;
  return o;
};
var SetRightProto = Object.assign(Object.create(PatchProto5), {
  _tag: "SetRight"
});
var makeSetRight = (value) => {
  const o = Object.create(SetRightProto);
  o.value = value;
  return o;
};
var UpdateLeftProto = Object.assign(Object.create(PatchProto5), {
  _tag: "UpdateLeft"
});
var makeUpdateLeft = (patch8) => {
  const o = Object.create(UpdateLeftProto);
  o.patch = patch8;
  return o;
};
var UpdateRightProto = Object.assign(Object.create(PatchProto5), {
  _tag: "UpdateRight"
});
var makeUpdateRight = (patch8) => {
  const o = Object.create(UpdateRightProto);
  o.patch = patch8;
  return o;
};
var diff5 = (options) => {
  switch (options.oldValue._tag) {
    case "Left": {
      switch (options.newValue._tag) {
        case "Left": {
          const valuePatch = options.left.diff(options.oldValue.left, options.newValue.left);
          if (equals(valuePatch, options.left.empty)) {
            return empty13();
          }
          return makeUpdateLeft(valuePatch);
        }
        case "Right": {
          return makeSetRight(options.newValue.right);
        }
      }
    }
    case "Right": {
      switch (options.newValue._tag) {
        case "Left": {
          return makeSetLeft(options.newValue.left);
        }
        case "Right": {
          const valuePatch = options.right.diff(options.oldValue.right, options.newValue.right);
          if (equals(valuePatch, options.right.empty)) {
            return empty13();
          }
          return makeUpdateRight(valuePatch);
        }
      }
    }
  }
};
var combine8 = dual(2, (self, that) => makeAndThen5(self, that));
var patch5 = dual(2, (self, {
  left: left2,
  oldValue,
  right: right2
}) => {
  if (self._tag === "Empty") {
    return oldValue;
  }
  let patches = of2(self);
  let result = oldValue;
  while (isNonEmpty(patches)) {
    const head3 = headNonEmpty2(patches);
    const tail2 = tailNonEmpty2(patches);
    switch (head3._tag) {
      case "Empty": {
        patches = tail2;
        break;
      }
      case "AndThen": {
        patches = prepend(head3.first)(prepend(head3.second)(tail2));
        break;
      }
      case "UpdateLeft": {
        if (result._tag === "Left") {
          result = left(left2.patch(head3.patch, result.left));
        }
        patches = tail2;
        break;
      }
      case "UpdateRight": {
        if (result._tag === "Right") {
          result = right(right2.patch(head3.patch, result.right));
        }
        patches = tail2;
        break;
      }
      case "SetLeft": {
        result = left(head3.value);
        patches = tail2;
        break;
      }
      case "SetRight": {
        result = right(head3.value);
        patches = tail2;
        break;
      }
    }
  }
  return result;
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/differ/readonlyArrayPatch.js
var ReadonlyArrayPatchTypeId = Symbol.for("effect/DifferReadonlyArrayPatch");
function variance6(a) {
  return a;
}
var PatchProto6 = {
  ...Structural.prototype,
  [ReadonlyArrayPatchTypeId]: {
    _Value: variance6,
    _Patch: variance6
  }
};
var EmptyProto6 = Object.assign(Object.create(PatchProto6), {
  _tag: "Empty"
});
var _empty8 = Object.create(EmptyProto6);
var empty14 = () => _empty8;
var AndThenProto6 = Object.assign(Object.create(PatchProto6), {
  _tag: "AndThen"
});
var makeAndThen6 = (first, second) => {
  const o = Object.create(AndThenProto6);
  o.first = first;
  o.second = second;
  return o;
};
var AppendProto2 = Object.assign(Object.create(PatchProto6), {
  _tag: "Append"
});
var makeAppend2 = (values5) => {
  const o = Object.create(AppendProto2);
  o.values = values5;
  return o;
};
var SliceProto2 = Object.assign(Object.create(PatchProto6), {
  _tag: "Slice"
});
var makeSlice2 = (from, until) => {
  const o = Object.create(SliceProto2);
  o.from = from;
  o.until = until;
  return o;
};
var UpdateProto3 = Object.assign(Object.create(PatchProto6), {
  _tag: "Update"
});
var makeUpdate3 = (index, patch8) => {
  const o = Object.create(UpdateProto3);
  o.index = index;
  o.patch = patch8;
  return o;
};
var diff6 = (options) => {
  let i = 0;
  let patch8 = empty14();
  while (i < options.oldValue.length && i < options.newValue.length) {
    const oldElement = options.oldValue[i];
    const newElement = options.newValue[i];
    const valuePatch = options.differ.diff(oldElement, newElement);
    if (!equals(valuePatch, options.differ.empty)) {
      patch8 = combine9(patch8, makeUpdate3(i, valuePatch));
    }
    i = i + 1;
  }
  if (i < options.oldValue.length) {
    patch8 = combine9(patch8, makeSlice2(0, i));
  }
  if (i < options.newValue.length) {
    patch8 = combine9(patch8, makeAppend2(drop(i)(options.newValue)));
  }
  return patch8;
};
var combine9 = dual(2, (self, that) => makeAndThen6(self, that));
var patch6 = dual(3, (self, oldValue, differ2) => {
  if (self._tag === "Empty") {
    return oldValue;
  }
  let readonlyArray2 = oldValue.slice();
  let patches = of(self);
  while (isNonEmptyArray(patches)) {
    const head3 = headNonEmpty(patches);
    const tail2 = tailNonEmpty(patches);
    switch (head3._tag) {
      case "Empty": {
        patches = tail2;
        break;
      }
      case "AndThen": {
        tail2.unshift(head3.first, head3.second);
        patches = tail2;
        break;
      }
      case "Append": {
        for (const value of head3.values) {
          readonlyArray2.push(value);
        }
        patches = tail2;
        break;
      }
      case "Slice": {
        readonlyArray2 = readonlyArray2.slice(head3.from, head3.until);
        patches = tail2;
        break;
      }
      case "Update": {
        readonlyArray2[head3.index] = differ2.patch(head3.patch, readonlyArray2[head3.index]);
        patches = tail2;
        break;
      }
    }
  }
  return readonlyArray2;
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/differ.js
var DifferTypeId = Symbol.for("effect/Differ");
var DifferProto = {
  [DifferTypeId]: {
    _P: identity,
    _V: identity
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make13 = (params) => {
  const differ2 = Object.create(DifferProto);
  differ2.empty = params.empty;
  differ2.diff = params.diff;
  differ2.combine = params.combine;
  differ2.patch = params.patch;
  return differ2;
};
var environment = () => make13({
  empty: empty10(),
  combine: (first, second) => combine5(second)(first),
  diff: (oldValue, newValue) => diff2(oldValue, newValue),
  patch: (patch8, oldValue) => patch2(oldValue)(patch8)
});
var chunk = (differ2) => make13({
  empty: empty9(),
  combine: (first, second) => combine4(second)(first),
  diff: (oldValue, newValue) => diff({
    oldValue,
    newValue,
    differ: differ2
  }),
  patch: (patch8, oldValue) => patch(oldValue, differ2)(patch8)
});
var hashMap = (differ2) => make13({
  empty: empty11(),
  combine: (first, second) => combine6(second)(first),
  diff: (oldValue, newValue) => diff3({
    oldValue,
    newValue,
    differ: differ2
  }),
  patch: (patch8, oldValue) => patch3(oldValue, differ2)(patch8)
});
var hashSet = () => make13({
  empty: empty12(),
  combine: (first, second) => combine7(second)(first),
  diff: (oldValue, newValue) => diff4(oldValue, newValue),
  patch: (patch8, oldValue) => patch4(oldValue)(patch8)
});
var orElseEither = dual(2, (self, that) => make13({
  empty: empty13(),
  combine: (first, second) => combine8(first, second),
  diff: (oldValue, newValue) => diff5({
    oldValue,
    newValue,
    left: self,
    right: that
  }),
  patch: (patch8, oldValue) => patch5(patch8, {
    oldValue,
    left: self,
    right: that
  })
}));
var readonlyArray = (differ2) => make13({
  empty: empty14(),
  combine: (first, second) => combine9(first, second),
  diff: (oldValue, newValue) => diff6({
    oldValue,
    newValue,
    differ: differ2
  }),
  patch: (patch8, oldValue) => patch6(patch8, oldValue, differ2)
});
var transform = dual(2, (self, {
  toNew,
  toOld
}) => make13({
  empty: self.empty,
  combine: (first, second) => self.combine(first, second),
  diff: (oldValue, newValue) => self.diff(toOld(oldValue), toOld(newValue)),
  patch: (patch8, oldValue) => toNew(self.patch(patch8, toOld(oldValue)))
}));
var update2 = () => updateWith((_, a) => a);
var updateWith = (f) => make13({
  empty: identity,
  combine: (first, second) => {
    if (first === identity) {
      return second;
    }
    if (second === identity) {
      return first;
    }
    return (a) => second(first(a));
  },
  diff: (oldValue, newValue) => {
    if (equals(oldValue, newValue)) {
      return identity;
    }
    return constant(newValue);
  },
  patch: (patch8, oldValue) => f(oldValue, patch8(oldValue))
});
var zip = dual(2, (self, that) => make13({
  empty: [self.empty, that.empty],
  combine: (first, second) => [self.combine(first[0], second[0]), that.combine(first[1], second[1])],
  diff: (oldValue, newValue) => [self.diff(oldValue[0], newValue[0]), that.diff(oldValue[1], newValue[1])],
  patch: (patch8, oldValue) => [self.patch(patch8[0], oldValue[0]), that.patch(patch8[1], oldValue[1])]
}));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/runtimeFlagsPatch.js
var BIT_MASK = 255;
var BIT_SHIFT = 8;
var active = (patch8) => patch8 & BIT_MASK;
var enabled = (patch8) => patch8 >> BIT_SHIFT & BIT_MASK;
var make14 = (active2, enabled2) => (active2 & BIT_MASK) + ((enabled2 & active2 & BIT_MASK) << BIT_SHIFT);
var empty15 = make14(0, 0);
var enable = (flag) => make14(flag, flag);
var disable = (flag) => make14(flag, 0);
var isEmpty4 = (patch8) => patch8 === 0;
var isActive = dual(2, (self, flag) => (active(self) & flag) !== 0);
var isEnabled = dual(2, (self, flag) => (enabled(self) & flag) !== 0);
var isDisabled = dual(2, (self, flag) => (active(self) & flag) !== 0 && (enabled(self) & flag) === 0);
var exclude = dual(2, (self, flag) => make14(active(self) & ~flag, enabled(self)));
var both = dual(2, (self, that) => make14(active(self) | active(that), enabled(self) & enabled(that)));
var either = dual(2, (self, that) => make14(active(self) | active(that), enabled(self) | enabled(that)));
var andThen = dual(2, (self, that) => self | that);
var inverse = (patch8) => make14(enabled(patch8), invert(active(patch8)));
var invert = (n) => ~n >>> 0 & BIT_MASK;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/runtimeFlags.js
var None2 = 0;
var Interruption = 1 << 0;
var OpSupervision = 1 << 1;
var RuntimeMetrics = 1 << 2;
var WindDown = 1 << 4;
var CooperativeYielding = 1 << 5;
var allFlags = [None2, Interruption, OpSupervision, RuntimeMetrics, WindDown, CooperativeYielding];
var print = (flag) => {
  switch (flag) {
    case CooperativeYielding: {
      return "CooperativeYielding";
    }
    case WindDown: {
      return "WindDown";
    }
    case RuntimeMetrics: {
      return "RuntimeMetrics";
    }
    case OpSupervision: {
      return "OpSupervision";
    }
    case Interruption: {
      return "Interruption";
    }
    case None2: {
      return "None";
    }
  }
};
var cooperativeYielding = (self) => isEnabled2(self, CooperativeYielding);
var disable2 = dual(2, (self, flag) => self & ~flag);
var disableAll = dual(2, (self, flags) => self & ~flags);
var enable2 = dual(2, (self, flag) => self | flag);
var enableAll = dual(2, (self, flags) => self | flags);
var interruptible = (self) => interruption(self) && !windDown(self);
var interruption = (self) => isEnabled2(self, Interruption);
var isDisabled2 = dual(2, (self, flag) => !isEnabled2(self, flag));
var isEnabled2 = dual(2, (self, flag) => (self & flag) !== 0);
var make15 = (...flags) => flags.reduce((a, b) => a | b, 0);
var none4 = make15(None2);
var opSupervision = (self) => isEnabled2(self, OpSupervision);
var render = (self) => {
  const active2 = [];
  allFlags.forEach((flag) => {
    if (isEnabled2(self, flag)) {
      active2.push(`${print(flag)}`);
    }
  });
  return `RuntimeFlags(${active2.join(", ")})`;
};
var runtimeMetrics = (self) => isEnabled2(self, RuntimeMetrics);
var toSet3 = (self) => new Set(allFlags.filter((flag) => isEnabled2(self, flag)));
var windDown = (self) => isEnabled2(self, WindDown);
var enabledSet = (self) => toSet3(active(self) & enabled(self));
var disabledSet = (self) => toSet3(active(self) & ~enabled(self));
var diff7 = dual(2, (self, that) => make14(self ^ that, that));
var patch7 = dual(2, (self, patch8) => self & (invert(active(patch8)) | enabled(patch8)) | active(patch8) & enabled(patch8));
var renderPatch = (self) => {
  const enabled2 = Array.from(enabledSet(self)).map((flag) => print(flag)).join(", ");
  const disabled = Array.from(disabledSet(self)).map((flag) => print(flag)).join(", ");
  return `RuntimeFlagsPatch(enabled = (${enabled2}), disabled = (${disabled}))`;
};
var differ = make13({
  empty: empty15,
  diff: (oldValue, newValue) => diff7(oldValue, newValue),
  combine: (first, second) => andThen(second)(first),
  patch: (_patch, oldValue) => patch7(oldValue, _patch)
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RuntimeFlagsPatch.js
var empty16 = empty15;
var make16 = make14;
var enable3 = enable;
var disable3 = disable;
var isEmpty5 = isEmpty4;
var isActive2 = isActive;
var isEnabled3 = isEnabled;
var isDisabled3 = isDisabled;
var includes = isActive;
var andThen2 = andThen;
var both2 = both;
var either2 = either;
var exclude2 = exclude;
var inverse2 = inverse;
var enabledSet2 = enabledSet;
var disabledSet2 = disabledSet;
var render2 = renderPatch;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/cause.js
var OP_DIE = "Die";
var OP_EMPTY = "Empty";
var OP_FAIL = "Fail";
var OP_INTERRUPT = "Interrupt";
var OP_PARALLEL = "Parallel";
var OP_SEQUENTIAL = "Sequential";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/cause.js
var CauseSymbolKey = "effect/Cause";
var CauseTypeId = Symbol.for(CauseSymbolKey);
var variance7 = {
  /* c8 ignore next */
  _E: (_) => _
};
var proto = {
  [CauseTypeId]: variance7,
  [symbol]() {
    return pipe(hash(CauseSymbolKey), combine(hash(flattenCause(this))), cached(this));
  },
  [symbol2](that) {
    return isCause(that) && causeEquals(this, that);
  },
  pipe() {
    return pipeArguments(this, arguments);
  },
  toJSON() {
    switch (this._tag) {
      case "Empty":
        return {
          _id: "Cause",
          _tag: this._tag
        };
      case "Die":
        return {
          _id: "Cause",
          _tag: this._tag,
          defect: toJSON(this.defect)
        };
      case "Interrupt":
        return {
          _id: "Cause",
          _tag: this._tag,
          fiberId: this.fiberId.toJSON()
        };
      case "Fail":
        return {
          _id: "Cause",
          _tag: this._tag,
          failure: toJSON(this.error)
        };
      case "Sequential":
      case "Parallel":
        return {
          _id: "Cause",
          _tag: this._tag,
          left: toJSON(this.left),
          right: toJSON(this.right)
        };
    }
  },
  toString() {
    return pretty(this);
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
};
var empty17 = (() => {
  const o = Object.create(proto);
  o._tag = OP_EMPTY;
  return o;
})();
var fail = (error) => {
  const o = Object.create(proto);
  o._tag = OP_FAIL;
  o.error = error;
  return o;
};
var die = (defect) => {
  const o = Object.create(proto);
  o._tag = OP_DIE;
  o.defect = defect;
  return o;
};
var interrupt = (fiberId2) => {
  const o = Object.create(proto);
  o._tag = OP_INTERRUPT;
  o.fiberId = fiberId2;
  return o;
};
var parallel = (left2, right2) => {
  const o = Object.create(proto);
  o._tag = OP_PARALLEL;
  o.left = left2;
  o.right = right2;
  return o;
};
var sequential = (left2, right2) => {
  const o = Object.create(proto);
  o._tag = OP_SEQUENTIAL;
  o.left = left2;
  o.right = right2;
  return o;
};
var isCause = (u) => hasProperty(u, CauseTypeId);
var isEmptyType = (self) => self._tag === OP_EMPTY;
var isFailType = (self) => self._tag === OP_FAIL;
var isDieType = (self) => self._tag === OP_DIE;
var isInterruptType = (self) => self._tag === OP_INTERRUPT;
var isSequentialType = (self) => self._tag === OP_SEQUENTIAL;
var isParallelType = (self) => self._tag === OP_PARALLEL;
var size6 = (self) => reduceWithContext(self, void 0, SizeCauseReducer);
var isEmpty6 = (self) => {
  if (self._tag === OP_EMPTY) {
    return true;
  }
  return reduce7(self, true, (acc, cause) => {
    switch (cause._tag) {
      case OP_EMPTY: {
        return some(acc);
      }
      case OP_DIE:
      case OP_FAIL:
      case OP_INTERRUPT: {
        return some(false);
      }
      default: {
        return none();
      }
    }
  });
};
var isFailure = (self) => isSome(failureOption(self));
var isDie = (self) => isSome(dieOption(self));
var isInterrupted = (self) => isSome(interruptOption(self));
var isInterruptedOnly = (self) => reduceWithContext(void 0, IsInterruptedOnlyCauseReducer)(self);
var failures = (self) => reverse(reduce7(self, empty2(), (list, cause) => cause._tag === OP_FAIL ? some(pipe(list, prepend(cause.error))) : none()));
var defects = (self) => reverse(reduce7(self, empty2(), (list, cause) => cause._tag === OP_DIE ? some(pipe(list, prepend(cause.defect))) : none()));
var interruptors = (self) => reduce7(self, empty6(), (set4, cause) => cause._tag === OP_INTERRUPT ? some(pipe(set4, add2(cause.fiberId))) : none());
var failureOption = (self) => find(self, (cause) => cause._tag === OP_FAIL ? some(cause.error) : none());
var failureOrCause = (self) => {
  const option = failureOption(self);
  switch (option._tag) {
    case "None": {
      return right(self);
    }
    case "Some": {
      return left(option.value);
    }
  }
};
var dieOption = (self) => find(self, (cause) => cause._tag === OP_DIE ? some(cause.defect) : none());
var flipCauseOption = (self) => match3(self, {
  onEmpty: some(empty17),
  onFail: map(fail),
  onDie: (defect) => some(die(defect)),
  onInterrupt: (fiberId2) => some(interrupt(fiberId2)),
  onSequential: mergeWith(sequential),
  onParallel: mergeWith(parallel)
});
var interruptOption = (self) => find(self, (cause) => cause._tag === OP_INTERRUPT ? some(cause.fiberId) : none());
var keepDefects = (self) => match3(self, {
  onEmpty: none(),
  onFail: () => none(),
  onDie: (defect) => some(die(defect)),
  onInterrupt: () => none(),
  onSequential: mergeWith(sequential),
  onParallel: mergeWith(parallel)
});
var keepDefectsAndElectFailures = (self) => match3(self, {
  onEmpty: none(),
  onFail: (failure) => some(die(failure)),
  onDie: (defect) => some(die(defect)),
  onInterrupt: () => none(),
  onSequential: mergeWith(sequential),
  onParallel: mergeWith(parallel)
});
var linearize = (self) => match3(self, {
  onEmpty: empty6(),
  onFail: (error) => make7(fail(error)),
  onDie: (defect) => make7(die(defect)),
  onInterrupt: (fiberId2) => make7(interrupt(fiberId2)),
  onSequential: (leftSet, rightSet) => flatMap3(leftSet, (leftCause) => map4(rightSet, (rightCause) => sequential(leftCause, rightCause))),
  onParallel: (leftSet, rightSet) => flatMap3(leftSet, (leftCause) => map4(rightSet, (rightCause) => parallel(leftCause, rightCause)))
});
var stripFailures = (self) => match3(self, {
  onEmpty: empty17,
  onFail: () => empty17,
  onDie: die,
  onInterrupt: interrupt,
  onSequential: sequential,
  onParallel: parallel
});
var electFailures = (self) => match3(self, {
  onEmpty: empty17,
  onFail: die,
  onDie: die,
  onInterrupt: interrupt,
  onSequential: sequential,
  onParallel: parallel
});
var stripSomeDefects = dual(2, (self, pf) => match3(self, {
  onEmpty: some(empty17),
  onFail: (error) => some(fail(error)),
  onDie: (defect) => {
    const option = pf(defect);
    return isSome(option) ? none() : some(die(defect));
  },
  onInterrupt: (fiberId2) => some(interrupt(fiberId2)),
  onSequential: mergeWith(sequential),
  onParallel: mergeWith(parallel)
}));
var as = dual(2, (self, error) => map7(self, () => error));
var map7 = dual(2, (self, f) => flatMap6(self, (e) => fail(f(e))));
var flatMap6 = dual(2, (self, f) => match3(self, {
  onEmpty: empty17,
  onFail: (error) => f(error),
  onDie: (defect) => die(defect),
  onInterrupt: (fiberId2) => interrupt(fiberId2),
  onSequential: (left2, right2) => sequential(left2, right2),
  onParallel: (left2, right2) => parallel(left2, right2)
}));
var flatten = (self) => flatMap6(self, identity);
var andThen3 = dual(2, (self, f) => isFunction(f) ? flatMap6(self, f) : flatMap6(self, () => f));
var contains = dual(2, (self, that) => {
  if (that._tag === OP_EMPTY || self === that) {
    return true;
  }
  return reduce7(self, false, (accumulator, cause) => {
    return some(accumulator || causeEquals(cause, that));
  });
});
var causeEquals = (left2, right2) => {
  let leftStack = of2(left2);
  let rightStack = of2(right2);
  while (isNonEmpty(leftStack) && isNonEmpty(rightStack)) {
    const [leftParallel, leftSequential] = pipe(headNonEmpty2(leftStack), reduce7([empty6(), empty2()], ([parallel2, sequential2], cause) => {
      const [par2, seq2] = evaluateCause(cause);
      return some([pipe(parallel2, union3(par2)), pipe(sequential2, appendAll(seq2))]);
    }));
    const [rightParallel, rightSequential] = pipe(headNonEmpty2(rightStack), reduce7([empty6(), empty2()], ([parallel2, sequential2], cause) => {
      const [par2, seq2] = evaluateCause(cause);
      return some([pipe(parallel2, union3(par2)), pipe(sequential2, appendAll(seq2))]);
    }));
    if (!equals(leftParallel, rightParallel)) {
      return false;
    }
    leftStack = leftSequential;
    rightStack = rightSequential;
  }
  return true;
};
var flattenCause = (cause) => {
  return flattenCauseLoop(of2(cause), empty2());
};
var flattenCauseLoop = (causes, flattened) => {
  while (1) {
    const [parallel2, sequential2] = pipe(causes, reduce([empty6(), empty2()], ([parallel3, sequential3], cause) => {
      const [par2, seq2] = evaluateCause(cause);
      return [pipe(parallel3, union3(par2)), pipe(sequential3, appendAll(seq2))];
    }));
    const updated = size3(parallel2) > 0 ? pipe(flattened, prepend(parallel2)) : flattened;
    if (isEmpty(sequential2)) {
      return reverse(updated);
    }
    causes = sequential2;
    flattened = updated;
  }
  throw new Error(getBugErrorMessage("Cause.flattenCauseLoop"));
};
var find = dual(2, (self, pf) => {
  const stack = [self];
  while (stack.length > 0) {
    const item = stack.pop();
    const option = pf(item);
    switch (option._tag) {
      case "None": {
        switch (item._tag) {
          case OP_SEQUENTIAL:
          case OP_PARALLEL: {
            stack.push(item.right);
            stack.push(item.left);
            break;
          }
        }
        break;
      }
      case "Some": {
        return option;
      }
    }
  }
  return none();
});
var filter6 = dual(2, (self, predicate) => reduceWithContext(self, void 0, FilterCauseReducer(predicate)));
var evaluateCause = (self) => {
  let cause = self;
  const stack = [];
  let _parallel = empty6();
  let _sequential = empty2();
  while (cause !== void 0) {
    switch (cause._tag) {
      case OP_EMPTY: {
        if (stack.length === 0) {
          return [_parallel, _sequential];
        }
        cause = stack.pop();
        break;
      }
      case OP_FAIL: {
        _parallel = add2(_parallel, make2(cause._tag, cause.error));
        if (stack.length === 0) {
          return [_parallel, _sequential];
        }
        cause = stack.pop();
        break;
      }
      case OP_DIE: {
        _parallel = add2(_parallel, make2(cause._tag, cause.defect));
        if (stack.length === 0) {
          return [_parallel, _sequential];
        }
        cause = stack.pop();
        break;
      }
      case OP_INTERRUPT: {
        _parallel = add2(_parallel, make2(cause._tag, cause.fiberId));
        if (stack.length === 0) {
          return [_parallel, _sequential];
        }
        cause = stack.pop();
        break;
      }
      case OP_SEQUENTIAL: {
        switch (cause.left._tag) {
          case OP_EMPTY: {
            cause = cause.right;
            break;
          }
          case OP_SEQUENTIAL: {
            cause = sequential(cause.left.left, sequential(cause.left.right, cause.right));
            break;
          }
          case OP_PARALLEL: {
            cause = parallel(sequential(cause.left.left, cause.right), sequential(cause.left.right, cause.right));
            break;
          }
          default: {
            _sequential = prepend(_sequential, cause.right);
            cause = cause.left;
            break;
          }
        }
        break;
      }
      case OP_PARALLEL: {
        stack.push(cause.right);
        cause = cause.left;
        break;
      }
    }
  }
  throw new Error(getBugErrorMessage("Cause.evaluateCauseLoop"));
};
var SizeCauseReducer = {
  emptyCase: () => 0,
  failCase: () => 1,
  dieCase: () => 1,
  interruptCase: () => 1,
  sequentialCase: (_, left2, right2) => left2 + right2,
  parallelCase: (_, left2, right2) => left2 + right2
};
var IsInterruptedOnlyCauseReducer = {
  emptyCase: constTrue,
  failCase: constFalse,
  dieCase: constFalse,
  interruptCase: constTrue,
  sequentialCase: (_, left2, right2) => left2 && right2,
  parallelCase: (_, left2, right2) => left2 && right2
};
var FilterCauseReducer = (predicate) => ({
  emptyCase: () => empty17,
  failCase: (_, error) => fail(error),
  dieCase: (_, defect) => die(defect),
  interruptCase: (_, fiberId2) => interrupt(fiberId2),
  sequentialCase: (_, left2, right2) => {
    if (predicate(left2)) {
      if (predicate(right2)) {
        return sequential(left2, right2);
      }
      return left2;
    }
    if (predicate(right2)) {
      return right2;
    }
    return empty17;
  },
  parallelCase: (_, left2, right2) => {
    if (predicate(left2)) {
      if (predicate(right2)) {
        return parallel(left2, right2);
      }
      return left2;
    }
    if (predicate(right2)) {
      return right2;
    }
    return empty17;
  }
});
var OP_SEQUENTIAL_CASE = "SequentialCase";
var OP_PARALLEL_CASE = "ParallelCase";
var match3 = dual(2, (self, {
  onDie,
  onEmpty,
  onFail,
  onInterrupt: onInterrupt2,
  onParallel,
  onSequential
}) => {
  return reduceWithContext(self, void 0, {
    emptyCase: () => onEmpty,
    failCase: (_, error) => onFail(error),
    dieCase: (_, defect) => onDie(defect),
    interruptCase: (_, fiberId2) => onInterrupt2(fiberId2),
    sequentialCase: (_, left2, right2) => onSequential(left2, right2),
    parallelCase: (_, left2, right2) => onParallel(left2, right2)
  });
});
var reduce7 = dual(3, (self, zero2, pf) => {
  let accumulator = zero2;
  let cause = self;
  const causes = [];
  while (cause !== void 0) {
    const option = pf(accumulator, cause);
    accumulator = isSome(option) ? option.value : accumulator;
    switch (cause._tag) {
      case OP_SEQUENTIAL: {
        causes.push(cause.right);
        cause = cause.left;
        break;
      }
      case OP_PARALLEL: {
        causes.push(cause.right);
        cause = cause.left;
        break;
      }
      default: {
        cause = void 0;
        break;
      }
    }
    if (cause === void 0 && causes.length > 0) {
      cause = causes.pop();
    }
  }
  return accumulator;
});
var reduceWithContext = dual(3, (self, context2, reducer) => {
  const input = [self];
  const output = [];
  while (input.length > 0) {
    const cause = input.pop();
    switch (cause._tag) {
      case OP_EMPTY: {
        output.push(right(reducer.emptyCase(context2)));
        break;
      }
      case OP_FAIL: {
        output.push(right(reducer.failCase(context2, cause.error)));
        break;
      }
      case OP_DIE: {
        output.push(right(reducer.dieCase(context2, cause.defect)));
        break;
      }
      case OP_INTERRUPT: {
        output.push(right(reducer.interruptCase(context2, cause.fiberId)));
        break;
      }
      case OP_SEQUENTIAL: {
        input.push(cause.right);
        input.push(cause.left);
        output.push(left({
          _tag: OP_SEQUENTIAL_CASE
        }));
        break;
      }
      case OP_PARALLEL: {
        input.push(cause.right);
        input.push(cause.left);
        output.push(left({
          _tag: OP_PARALLEL_CASE
        }));
        break;
      }
    }
  }
  const accumulator = [];
  while (output.length > 0) {
    const either4 = output.pop();
    switch (either4._tag) {
      case "Left": {
        switch (either4.left._tag) {
          case OP_SEQUENTIAL_CASE: {
            const left2 = accumulator.pop();
            const right2 = accumulator.pop();
            const value = reducer.sequentialCase(context2, left2, right2);
            accumulator.push(value);
            break;
          }
          case OP_PARALLEL_CASE: {
            const left2 = accumulator.pop();
            const right2 = accumulator.pop();
            const value = reducer.parallelCase(context2, left2, right2);
            accumulator.push(value);
            break;
          }
        }
        break;
      }
      case "Right": {
        accumulator.push(either4.right);
        break;
      }
    }
  }
  if (accumulator.length === 0) {
    throw new Error("BUG: Cause.reduceWithContext - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  return accumulator.pop();
});
var pretty = (cause, options) => {
  if (isInterruptedOnly(cause)) {
    return "All fibers interrupted without errors.";
  }
  return prettyErrors(cause).map(function(e) {
    if (options?.renderErrorCause !== true || e.cause === void 0) {
      return e.stack;
    }
    return `${e.stack} {
${renderErrorCause(e.cause, "  ")}
}`;
  }).join("\n");
};
var renderErrorCause = (cause, prefix) => {
  const lines = cause.stack.split("\n");
  let stack = `${prefix}[cause]: ${lines[0]}`;
  for (let i = 1, len = lines.length; i < len; i++) {
    stack += `
${prefix}${lines[i]}`;
  }
  if (cause.cause) {
    stack += ` {
${renderErrorCause(cause.cause, `${prefix}  `)}
${prefix}}`;
  }
  return stack;
};
var PrettyError = class _PrettyError extends globalThis.Error {
  span = void 0;
  constructor(originalError) {
    const originalErrorIsObject = typeof originalError === "object" && originalError !== null;
    const prevLimit = Error.stackTraceLimit;
    Error.stackTraceLimit = 1;
    super(prettyErrorMessage(originalError), originalErrorIsObject && "cause" in originalError && typeof originalError.cause !== "undefined" ? {
      cause: new _PrettyError(originalError.cause)
    } : void 0);
    if (this.message === "") {
      this.message = "An error has occurred";
    }
    Error.stackTraceLimit = prevLimit;
    this.name = originalError instanceof Error ? originalError.name : "Error";
    if (originalErrorIsObject) {
      if (spanSymbol in originalError) {
        this.span = originalError[spanSymbol];
      }
      Object.keys(originalError).forEach((key) => {
        if (!(key in this)) {
          this[key] = originalError[key];
        }
      });
    }
    this.stack = prettyErrorStack(`${this.name}: ${this.message}`, originalError instanceof Error && originalError.stack ? originalError.stack : "", this.span);
  }
};
var prettyErrorMessage = (u) => {
  if (typeof u === "string") {
    return u;
  }
  if (typeof u === "object" && u !== null && u instanceof Error) {
    return u.message;
  }
  try {
    if (hasProperty(u, "toString") && isFunction(u["toString"]) && u["toString"] !== Object.prototype.toString && u["toString"] !== globalThis.Array.prototype.toString) {
      return u["toString"]();
    }
  } catch {
  }
  return stringifyCircular(u);
};
var locationRegex = /\((.*)\)/g;
var spanToTrace = globalValue("effect/Tracer/spanToTrace", () => /* @__PURE__ */ new WeakMap());
var prettyErrorStack = (message, stack, span) => {
  const out = [message];
  const lines = stack.startsWith(message) ? stack.slice(message.length).split("\n") : stack.split("\n");
  for (let i = 1; i < lines.length; i++) {
    if (lines[i].includes(" at new BaseEffectError") || lines[i].includes(" at new YieldableError")) {
      i++;
      continue;
    }
    if (lines[i].includes("Generator.next")) {
      break;
    }
    if (lines[i].includes("effect_internal_function")) {
      break;
    }
    out.push(lines[i].replace(/at .*effect_instruction_i.*\((.*)\)/, "at $1").replace(/EffectPrimitive\.\w+/, "<anonymous>"));
  }
  if (span) {
    let current = span;
    let i = 0;
    while (current && current._tag === "Span" && i < 10) {
      const stackFn = spanToTrace.get(current);
      if (typeof stackFn === "function") {
        const stack2 = stackFn();
        if (typeof stack2 === "string") {
          const locationMatchAll = stack2.matchAll(locationRegex);
          let match4 = false;
          for (const [, location] of locationMatchAll) {
            match4 = true;
            out.push(`    at ${current.name} (${location})`);
          }
          if (!match4) {
            out.push(`    at ${current.name} (${stack2.replace(/^at /, "")})`);
          }
        } else {
          out.push(`    at ${current.name}`);
        }
      } else {
        out.push(`    at ${current.name}`);
      }
      current = getOrUndefined(current.parent);
      i++;
    }
  }
  return out.join("\n");
};
var spanSymbol = Symbol.for("effect/SpanAnnotation");
var prettyErrors = (cause) => reduceWithContext(cause, void 0, {
  emptyCase: () => [],
  dieCase: (_, unknownError) => {
    return [new PrettyError(unknownError)];
  },
  failCase: (_, error) => {
    return [new PrettyError(error)];
  },
  interruptCase: () => [],
  parallelCase: (_, l, r) => [...l, ...r],
  sequentialCase: (_, l, r) => [...l, ...r]
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/blockedRequests.js
var empty18 = {
  _tag: "Empty"
};
var par = (self, that) => ({
  _tag: "Par",
  left: self,
  right: that
});
var seq = (self, that) => ({
  _tag: "Seq",
  left: self,
  right: that
});
var single = (dataSource, blockedRequest) => ({
  _tag: "Single",
  dataSource,
  blockedRequest
});
var MapRequestResolversReducer = (f) => ({
  emptyCase: () => empty18,
  parCase: (left2, right2) => par(left2, right2),
  seqCase: (left2, right2) => seq(left2, right2),
  singleCase: (dataSource, blockedRequest) => single(f(dataSource), blockedRequest)
});
var mapRequestResolvers = (self, f) => reduce8(self, MapRequestResolversReducer(f));
var reduce8 = (self, reducer) => {
  let input = of3(self);
  let output = empty8();
  while (isCons(input)) {
    const current = input.head;
    switch (current._tag) {
      case "Empty": {
        output = cons(right(reducer.emptyCase()), output);
        input = input.tail;
        break;
      }
      case "Par": {
        output = cons(left({
          _tag: "ParCase"
        }), output);
        input = cons(current.left, cons(current.right, input.tail));
        break;
      }
      case "Seq": {
        output = cons(left({
          _tag: "SeqCase"
        }), output);
        input = cons(current.left, cons(current.right, input.tail));
        break;
      }
      case "Single": {
        const result2 = reducer.singleCase(current.dataSource, current.blockedRequest);
        output = cons(right(result2), output);
        input = input.tail;
        break;
      }
    }
  }
  const result = reduce6(output, empty8(), (acc, current) => {
    switch (current._tag) {
      case "Left": {
        const left2 = unsafeHead2(acc);
        const right2 = unsafeHead2(unsafeTail(acc));
        const tail2 = unsafeTail(unsafeTail(acc));
        switch (current.left._tag) {
          case "ParCase": {
            return cons(reducer.parCase(left2, right2), tail2);
          }
          case "SeqCase": {
            return cons(reducer.seqCase(left2, right2), tail2);
          }
        }
      }
      case "Right": {
        return cons(current.right, acc);
      }
    }
  });
  if (isNil(result)) {
    throw new Error("BUG: BlockedRequests.reduce - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  return result.head;
};
var flatten2 = (self) => {
  let current = of3(self);
  let updated = empty8();
  while (1) {
    const [parallel2, sequential2] = reduce6(current, [parallelCollectionEmpty(), empty8()], ([parallel3, sequential3], blockedRequest) => {
      const [par2, seq2] = step(blockedRequest);
      return [parallelCollectionCombine(parallel3, par2), appendAll2(sequential3, seq2)];
    });
    updated = merge2(updated, parallel2);
    if (isNil(sequential2)) {
      return reverse2(updated);
    }
    current = sequential2;
  }
  throw new Error("BUG: BlockedRequests.flatten - please report an issue at https://github.com/Effect-TS/effect/issues");
};
var step = (requests) => {
  let current = requests;
  let parallel2 = parallelCollectionEmpty();
  let stack = empty8();
  let sequential2 = empty8();
  while (1) {
    switch (current._tag) {
      case "Empty": {
        if (isNil(stack)) {
          return [parallel2, sequential2];
        }
        current = stack.head;
        stack = stack.tail;
        break;
      }
      case "Par": {
        stack = cons(current.right, stack);
        current = current.left;
        break;
      }
      case "Seq": {
        const left2 = current.left;
        const right2 = current.right;
        switch (left2._tag) {
          case "Empty": {
            current = right2;
            break;
          }
          case "Par": {
            const l = left2.left;
            const r = left2.right;
            current = par(seq(l, right2), seq(r, right2));
            break;
          }
          case "Seq": {
            const l = left2.left;
            const r = left2.right;
            current = seq(l, seq(r, right2));
            break;
          }
          case "Single": {
            current = left2;
            sequential2 = cons(right2, sequential2);
            break;
          }
        }
        break;
      }
      case "Single": {
        parallel2 = parallelCollectionAdd(parallel2, current);
        if (isNil(stack)) {
          return [parallel2, sequential2];
        }
        current = stack.head;
        stack = stack.tail;
        break;
      }
    }
  }
  throw new Error("BUG: BlockedRequests.step - please report an issue at https://github.com/Effect-TS/effect/issues");
};
var merge2 = (sequential2, parallel2) => {
  if (isNil(sequential2)) {
    return of3(parallelCollectionToSequentialCollection(parallel2));
  }
  if (parallelCollectionIsEmpty(parallel2)) {
    return sequential2;
  }
  const seqHeadKeys = sequentialCollectionKeys(sequential2.head);
  const parKeys = parallelCollectionKeys(parallel2);
  if (seqHeadKeys.length === 1 && parKeys.length === 1 && equals(seqHeadKeys[0], parKeys[0])) {
    return cons(sequentialCollectionCombine(sequential2.head, parallelCollectionToSequentialCollection(parallel2)), sequential2.tail);
  }
  return cons(parallelCollectionToSequentialCollection(parallel2), sequential2);
};
var EntryTypeId = Symbol.for("effect/RequestBlock/Entry");
var EntryImpl = class {
  request;
  result;
  listeners;
  ownerId;
  state;
  [EntryTypeId] = blockedRequestVariance;
  constructor(request, result, listeners, ownerId, state) {
    this.request = request;
    this.result = result;
    this.listeners = listeners;
    this.ownerId = ownerId;
    this.state = state;
  }
};
var blockedRequestVariance = {
  /* c8 ignore next */
  _R: (_) => _
};
var isEntry = (u) => hasProperty(u, EntryTypeId);
var makeEntry = (options) => new EntryImpl(options.request, options.result, options.listeners, options.ownerId, options.state);
var RequestBlockParallelTypeId = Symbol.for("effect/RequestBlock/RequestBlockParallel");
var parallelVariance = {
  /* c8 ignore next */
  _R: (_) => _
};
var ParallelImpl = class {
  map;
  [RequestBlockParallelTypeId] = parallelVariance;
  constructor(map9) {
    this.map = map9;
  }
};
var parallelCollectionEmpty = () => new ParallelImpl(empty7());
var parallelCollectionAdd = (self, blockedRequest) => new ParallelImpl(modifyAt2(self.map, blockedRequest.dataSource, (_) => orElseSome(map(_, append(blockedRequest.blockedRequest)), () => of2(blockedRequest.blockedRequest))));
var parallelCollectionCombine = (self, that) => new ParallelImpl(reduce5(self.map, that.map, (map9, value, key) => set3(map9, key, match(get3(map9, key), {
  onNone: () => value,
  onSome: (other) => appendAll(value, other)
}))));
var parallelCollectionIsEmpty = (self) => isEmpty3(self.map);
var parallelCollectionKeys = (self) => Array.from(keys2(self.map));
var parallelCollectionToSequentialCollection = (self) => sequentialCollectionMake(map5(self.map, (x) => of2(x)));
var SequentialCollectionTypeId = Symbol.for("effect/RequestBlock/RequestBlockSequential");
var sequentialVariance = {
  /* c8 ignore next */
  _R: (_) => _
};
var SequentialImpl = class {
  map;
  [SequentialCollectionTypeId] = sequentialVariance;
  constructor(map9) {
    this.map = map9;
  }
};
var sequentialCollectionMake = (map9) => new SequentialImpl(map9);
var sequentialCollectionCombine = (self, that) => new SequentialImpl(reduce5(that.map, self.map, (map9, value, key) => set3(map9, key, match(get3(map9, key), {
  onNone: () => empty2(),
  onSome: (a) => appendAll(a, value)
}))));
var sequentialCollectionKeys = (self) => Array.from(keys2(self.map));
var sequentialCollectionToChunk = (self) => Array.from(self.map);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/deferred.js
var OP_STATE_PENDING = "Pending";
var OP_STATE_DONE = "Done";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/deferred.js
var DeferredSymbolKey = "effect/Deferred";
var DeferredTypeId = Symbol.for(DeferredSymbolKey);
var deferredVariance = {
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _A: (_) => _
};
var pending = (joiners) => {
  return {
    _tag: OP_STATE_PENDING,
    joiners
  };
};
var done = (effect) => {
  return {
    _tag: OP_STATE_DONE,
    effect
  };
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/singleShotGen.js
var SingleShotGen = class _SingleShotGen {
  self;
  called = false;
  constructor(self) {
    this.self = self;
  }
  next(a) {
    return this.called ? {
      value: a,
      done: true
    } : (this.called = true, {
      value: this.self,
      done: false
    });
  }
  return(a) {
    return {
      value: a,
      done: true
    };
  }
  throw(e) {
    throw e;
  }
  [Symbol.iterator]() {
    return new _SingleShotGen(this.self);
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/core.js
var blocked = (blockedRequests, _continue) => {
  const effect = new EffectPrimitive("Blocked");
  effect.effect_instruction_i0 = blockedRequests;
  effect.effect_instruction_i1 = _continue;
  return effect;
};
var runRequestBlock = (blockedRequests) => {
  const effect = new EffectPrimitive("RunBlocked");
  effect.effect_instruction_i0 = blockedRequests;
  return effect;
};
var EffectTypeId = Symbol.for("effect/Effect");
var RevertFlags = class {
  patch;
  op;
  _op = OP_REVERT_FLAGS;
  constructor(patch8, op) {
    this.patch = patch8;
    this.op = op;
  }
};
var EffectPrimitive = class {
  _op;
  effect_instruction_i0 = void 0;
  effect_instruction_i1 = void 0;
  effect_instruction_i2 = void 0;
  trace = void 0;
  [EffectTypeId] = effectVariance;
  constructor(_op) {
    this._op = _op;
  }
  [symbol2](that) {
    return this === that;
  }
  [symbol]() {
    return cached(this, random(this));
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  toJSON() {
    return {
      _id: "Effect",
      _op: this._op,
      effect_instruction_i0: toJSON(this.effect_instruction_i0),
      effect_instruction_i1: toJSON(this.effect_instruction_i1),
      effect_instruction_i2: toJSON(this.effect_instruction_i2)
    };
  }
  toString() {
    return format(this.toJSON());
  }
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Symbol.iterator]() {
    return new SingleShotGen(new YieldWrap(this));
  }
};
var EffectPrimitiveFailure = class {
  _op;
  effect_instruction_i0 = void 0;
  effect_instruction_i1 = void 0;
  effect_instruction_i2 = void 0;
  trace = void 0;
  [EffectTypeId] = effectVariance;
  constructor(_op) {
    this._op = _op;
    this._tag = _op;
  }
  [symbol2](that) {
    return exitIsExit(that) && that._op === "Failure" && // @ts-expect-error
    equals(this.effect_instruction_i0, that.effect_instruction_i0);
  }
  [symbol]() {
    return pipe(
      // @ts-expect-error
      string(this._tag),
      // @ts-expect-error
      combine(hash(this.effect_instruction_i0)),
      cached(this)
    );
  }
  get cause() {
    return this.effect_instruction_i0;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  toJSON() {
    return {
      _id: "Exit",
      _tag: this._op,
      cause: this.cause.toJSON()
    };
  }
  toString() {
    return format(this.toJSON());
  }
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Symbol.iterator]() {
    return new SingleShotGen(new YieldWrap(this));
  }
};
var EffectPrimitiveSuccess = class {
  _op;
  effect_instruction_i0 = void 0;
  effect_instruction_i1 = void 0;
  effect_instruction_i2 = void 0;
  trace = void 0;
  [EffectTypeId] = effectVariance;
  constructor(_op) {
    this._op = _op;
    this._tag = _op;
  }
  [symbol2](that) {
    return exitIsExit(that) && that._op === "Success" && // @ts-expect-error
    equals(this.effect_instruction_i0, that.effect_instruction_i0);
  }
  [symbol]() {
    return pipe(
      // @ts-expect-error
      string(this._tag),
      // @ts-expect-error
      combine(hash(this.effect_instruction_i0)),
      cached(this)
    );
  }
  get value() {
    return this.effect_instruction_i0;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  toJSON() {
    return {
      _id: "Exit",
      _tag: this._op,
      value: toJSON(this.value)
    };
  }
  toString() {
    return format(this.toJSON());
  }
  [NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Symbol.iterator]() {
    return new SingleShotGen(new YieldWrap(this));
  }
};
var isEffect = (u) => hasProperty(u, EffectTypeId);
var withFiberRuntime = (withRuntime) => {
  const effect = new EffectPrimitive(OP_WITH_RUNTIME);
  effect.effect_instruction_i0 = withRuntime;
  return effect;
};
var acquireUseRelease = dual(3, (acquire, use, release) => uninterruptibleMask((restore) => flatMap7(acquire, (a) => flatMap7(exit(suspend(() => restore(use(a)))), (exit2) => {
  return suspend(() => release(a, exit2)).pipe(matchCauseEffect({
    onFailure: (cause) => {
      switch (exit2._tag) {
        case OP_FAILURE:
          return failCause(sequential(exit2.effect_instruction_i0, cause));
        case OP_SUCCESS:
          return failCause(cause);
      }
    },
    onSuccess: () => exit2
  }));
}))));
var as2 = dual(2, (self, value) => flatMap7(self, () => succeed(value)));
var asVoid = (self) => as2(self, void 0);
var custom = function() {
  const wrapper = new EffectPrimitive(OP_COMMIT);
  switch (arguments.length) {
    case 2: {
      wrapper.effect_instruction_i0 = arguments[0];
      wrapper.commit = arguments[1];
      break;
    }
    case 3: {
      wrapper.effect_instruction_i0 = arguments[0];
      wrapper.effect_instruction_i1 = arguments[1];
      wrapper.commit = arguments[2];
      break;
    }
    case 4: {
      wrapper.effect_instruction_i0 = arguments[0];
      wrapper.effect_instruction_i1 = arguments[1];
      wrapper.effect_instruction_i2 = arguments[2];
      wrapper.commit = arguments[3];
      break;
    }
    default: {
      throw new Error(getBugErrorMessage("you're not supposed to end up here"));
    }
  }
  return wrapper;
};
var unsafeAsync = (register, blockingOn = none3) => {
  const effect = new EffectPrimitive(OP_ASYNC);
  let cancelerRef = void 0;
  effect.effect_instruction_i0 = (resume) => {
    cancelerRef = register(resume);
  };
  effect.effect_instruction_i1 = blockingOn;
  return onInterrupt(effect, (_) => isEffect(cancelerRef) ? cancelerRef : void_);
};
var asyncInterrupt = (register, blockingOn = none3) => suspend(() => unsafeAsync(register, blockingOn));
var async_ = (resume, blockingOn = none3) => {
  return custom(resume, function() {
    let backingResume = void 0;
    let pendingEffect = void 0;
    function proxyResume(effect2) {
      if (backingResume) {
        backingResume(effect2);
      } else if (pendingEffect === void 0) {
        pendingEffect = effect2;
      }
    }
    const effect = new EffectPrimitive(OP_ASYNC);
    effect.effect_instruction_i0 = (resume2) => {
      backingResume = resume2;
      if (pendingEffect) {
        resume2(pendingEffect);
      }
    };
    effect.effect_instruction_i1 = blockingOn;
    let cancelerRef = void 0;
    let controllerRef = void 0;
    if (this.effect_instruction_i0.length !== 1) {
      controllerRef = new AbortController();
      cancelerRef = internalCall(() => this.effect_instruction_i0(proxyResume, controllerRef.signal));
    } else {
      cancelerRef = internalCall(() => this.effect_instruction_i0(proxyResume));
    }
    return cancelerRef || controllerRef ? onInterrupt(effect, (_) => {
      if (controllerRef) {
        controllerRef.abort();
      }
      return cancelerRef ?? void_;
    }) : effect;
  });
};
var catchAllCause = dual(2, (self, f) => {
  const effect = new EffectPrimitive(OP_ON_FAILURE);
  effect.effect_instruction_i0 = self;
  effect.effect_instruction_i1 = f;
  return effect;
});
var catchAll = dual(2, (self, f) => matchEffect(self, {
  onFailure: f,
  onSuccess: succeed
}));
var catchIf = dual(3, (self, predicate, f) => catchAllCause(self, (cause) => {
  const either4 = failureOrCause(cause);
  switch (either4._tag) {
    case "Left":
      return predicate(either4.left) ? f(either4.left) : failCause(cause);
    case "Right":
      return failCause(either4.right);
  }
}));
var catchSome = dual(2, (self, pf) => catchAllCause(self, (cause) => {
  const either4 = failureOrCause(cause);
  switch (either4._tag) {
    case "Left":
      return pipe(pf(either4.left), getOrElse(() => failCause(cause)));
    case "Right":
      return failCause(either4.right);
  }
}));
var checkInterruptible = (f) => withFiberRuntime((_, status) => f(interruption(status.runtimeFlags)));
var originalSymbol = Symbol.for("effect/OriginalAnnotation");
var originalInstance = (obj) => {
  if (hasProperty(obj, originalSymbol)) {
    return obj[originalSymbol];
  }
  return obj;
};
var capture = (obj, span) => {
  if (isSome(span)) {
    return new Proxy(obj, {
      has(target, p) {
        return p === spanSymbol || p === originalSymbol || p in target;
      },
      get(target, p) {
        if (p === spanSymbol) {
          return span.value;
        }
        if (p === originalSymbol) {
          return obj;
        }
        return target[p];
      }
    });
  }
  return obj;
};
var die2 = (defect) => isObject(defect) && !(spanSymbol in defect) ? withFiberRuntime((fiber) => failCause(die(capture(defect, currentSpanFromFiber(fiber))))) : failCause(die(defect));
var dieMessage = (message) => failCauseSync(() => die(new RuntimeException(message)));
var dieSync = (evaluate) => flatMap7(sync(evaluate), die2);
var either3 = (self) => matchEffect(self, {
  onFailure: (e) => succeed(left(e)),
  onSuccess: (a) => succeed(right(a))
});
var exit = (self) => matchCause(self, {
  onFailure: exitFailCause,
  onSuccess: exitSucceed
});
var fail2 = (error) => isObject(error) && !(spanSymbol in error) ? withFiberRuntime((fiber) => failCause(fail(capture(error, currentSpanFromFiber(fiber))))) : failCause(fail(error));
var failSync = (evaluate) => flatMap7(sync(evaluate), fail2);
var failCause = (cause) => {
  const effect = new EffectPrimitiveFailure(OP_FAILURE);
  effect.effect_instruction_i0 = cause;
  return effect;
};
var failCauseSync = (evaluate) => flatMap7(sync(evaluate), failCause);
var fiberId = withFiberRuntime((state) => succeed(state.id()));
var fiberIdWith = (f) => withFiberRuntime((state) => f(state.id()));
var flatMap7 = dual(2, (self, f) => {
  const effect = new EffectPrimitive(OP_ON_SUCCESS);
  effect.effect_instruction_i0 = self;
  effect.effect_instruction_i1 = f;
  return effect;
});
var andThen4 = dual(2, (self, f) => flatMap7(self, (a) => {
  const b = typeof f === "function" ? f(a) : f;
  if (isEffect(b)) {
    return b;
  } else if (isPromiseLike(b)) {
    return unsafeAsync((resume) => {
      b.then((a2) => resume(succeed(a2)), (e) => resume(fail2(new UnknownException(e, "An unknown error occurred in Effect.andThen"))));
    });
  }
  return succeed(b);
}));
var step2 = (self) => {
  const effect = new EffectPrimitive("OnStep");
  effect.effect_instruction_i0 = self;
  return effect;
};
var flatten3 = (self) => flatMap7(self, identity);
var flip = (self) => matchEffect(self, {
  onFailure: succeed,
  onSuccess: fail2
});
var matchCause = dual(2, (self, options) => matchCauseEffect(self, {
  onFailure: (cause) => succeed(options.onFailure(cause)),
  onSuccess: (a) => succeed(options.onSuccess(a))
}));
var matchCauseEffect = dual(2, (self, options) => {
  const effect = new EffectPrimitive(OP_ON_SUCCESS_AND_FAILURE);
  effect.effect_instruction_i0 = self;
  effect.effect_instruction_i1 = options.onFailure;
  effect.effect_instruction_i2 = options.onSuccess;
  return effect;
});
var matchEffect = dual(2, (self, options) => matchCauseEffect(self, {
  onFailure: (cause) => {
    const defects2 = defects(cause);
    if (defects2.length > 0) {
      return failCause(electFailures(cause));
    }
    const failures2 = failures(cause);
    if (failures2.length > 0) {
      return options.onFailure(unsafeHead(failures2));
    }
    return failCause(cause);
  },
  onSuccess: options.onSuccess
}));
var forEachSequential = dual(2, (self, f) => suspend(() => {
  const arr = fromIterable(self);
  const ret = allocate(arr.length);
  let i = 0;
  return as2(whileLoop({
    while: () => i < arr.length,
    body: () => f(arr[i], i),
    step: (b) => {
      ret[i++] = b;
    }
  }), ret);
}));
var forEachSequentialDiscard = dual(2, (self, f) => suspend(() => {
  const arr = fromIterable(self);
  let i = 0;
  return whileLoop({
    while: () => i < arr.length,
    body: () => f(arr[i], i),
    step: () => {
      i++;
    }
  });
}));
var if_ = dual((args) => typeof args[0] === "boolean" || isEffect(args[0]), (self, options) => isEffect(self) ? flatMap7(self, (b) => b ? options.onTrue() : options.onFalse()) : self ? options.onTrue() : options.onFalse());
var interrupt2 = flatMap7(fiberId, (fiberId2) => interruptWith(fiberId2));
var interruptWith = (fiberId2) => failCause(interrupt(fiberId2));
var interruptible2 = (self) => {
  const effect = new EffectPrimitive(OP_UPDATE_RUNTIME_FLAGS);
  effect.effect_instruction_i0 = enable3(Interruption);
  effect.effect_instruction_i1 = () => self;
  return effect;
};
var interruptibleMask = (f) => custom(f, function() {
  const effect = new EffectPrimitive(OP_UPDATE_RUNTIME_FLAGS);
  effect.effect_instruction_i0 = enable3(Interruption);
  effect.effect_instruction_i1 = (oldFlags) => interruption(oldFlags) ? internalCall(() => this.effect_instruction_i0(interruptible2)) : internalCall(() => this.effect_instruction_i0(uninterruptible));
  return effect;
});
var intoDeferred = dual(2, (self, deferred) => uninterruptibleMask((restore) => flatMap7(exit(restore(self)), (exit2) => deferredDone(deferred, exit2))));
var map8 = dual(2, (self, f) => flatMap7(self, (a) => sync(() => f(a))));
var mapBoth = dual(2, (self, options) => matchEffect(self, {
  onFailure: (e) => failSync(() => options.onFailure(e)),
  onSuccess: (a) => sync(() => options.onSuccess(a))
}));
var mapError = dual(2, (self, f) => matchCauseEffect(self, {
  onFailure: (cause) => {
    const either4 = failureOrCause(cause);
    switch (either4._tag) {
      case "Left": {
        return failSync(() => f(either4.left));
      }
      case "Right": {
        return failCause(either4.right);
      }
    }
  },
  onSuccess: succeed
}));
var onError = dual(2, (self, cleanup) => onExit(self, (exit2) => exitIsSuccess(exit2) ? void_ : cleanup(exit2.effect_instruction_i0)));
var onExit = dual(2, (self, cleanup) => uninterruptibleMask((restore) => matchCauseEffect(restore(self), {
  onFailure: (cause1) => {
    const result = exitFailCause(cause1);
    return matchCauseEffect(cleanup(result), {
      onFailure: (cause2) => exitFailCause(sequential(cause1, cause2)),
      onSuccess: () => result
    });
  },
  onSuccess: (success) => {
    const result = exitSucceed(success);
    return zipRight(cleanup(result), result);
  }
})));
var onInterrupt = dual(2, (self, cleanup) => onExit(self, exitMatch({
  onFailure: (cause) => isInterruptedOnly(cause) ? asVoid(cleanup(interruptors(cause))) : void_,
  onSuccess: () => void_
})));
var orElse = dual(2, (self, that) => attemptOrElse(self, that, succeed));
var orDie = (self) => orDieWith(self, identity);
var orDieWith = dual(2, (self, f) => matchEffect(self, {
  onFailure: (e) => die2(f(e)),
  onSuccess: succeed
}));
var partitionMap3 = partitionMap;
var runtimeFlags = withFiberRuntime((_, status) => succeed(status.runtimeFlags));
var succeed = (value) => {
  const effect = new EffectPrimitiveSuccess(OP_SUCCESS);
  effect.effect_instruction_i0 = value;
  return effect;
};
var suspend = (evaluate) => {
  const effect = new EffectPrimitive(OP_COMMIT);
  effect.commit = evaluate;
  return effect;
};
var sync = (thunk) => {
  const effect = new EffectPrimitive(OP_SYNC);
  effect.effect_instruction_i0 = thunk;
  return effect;
};
var tap = dual((args) => args.length === 3 || args.length === 2 && !(isObject(args[1]) && "onlyEffect" in args[1]), (self, f) => flatMap7(self, (a) => {
  const b = typeof f === "function" ? f(a) : f;
  if (isEffect(b)) {
    return as2(b, a);
  } else if (isPromiseLike(b)) {
    return unsafeAsync((resume) => {
      b.then((_) => resume(succeed(a)), (e) => resume(fail2(new UnknownException(e, "An unknown error occurred in Effect.tap"))));
    });
  }
  return succeed(a);
}));
var transplant = (f) => withFiberRuntime((state) => {
  const scopeOverride = state.getFiberRef(currentForkScopeOverride);
  const scope = pipe(scopeOverride, getOrElse(() => state.scope()));
  return f(fiberRefLocally(currentForkScopeOverride, some(scope)));
});
var attemptOrElse = dual(3, (self, that, onSuccess) => matchCauseEffect(self, {
  onFailure: (cause) => {
    const defects2 = defects(cause);
    if (defects2.length > 0) {
      return failCause(getOrThrow(keepDefectsAndElectFailures(cause)));
    }
    return that();
  },
  onSuccess
}));
var uninterruptible = (self) => {
  const effect = new EffectPrimitive(OP_UPDATE_RUNTIME_FLAGS);
  effect.effect_instruction_i0 = disable3(Interruption);
  effect.effect_instruction_i1 = () => self;
  return effect;
};
var uninterruptibleMask = (f) => custom(f, function() {
  const effect = new EffectPrimitive(OP_UPDATE_RUNTIME_FLAGS);
  effect.effect_instruction_i0 = disable3(Interruption);
  effect.effect_instruction_i1 = (oldFlags) => interruption(oldFlags) ? internalCall(() => this.effect_instruction_i0(interruptible2)) : internalCall(() => this.effect_instruction_i0(uninterruptible));
  return effect;
});
var void_ = succeed(void 0);
var updateRuntimeFlags = (patch8) => {
  const effect = new EffectPrimitive(OP_UPDATE_RUNTIME_FLAGS);
  effect.effect_instruction_i0 = patch8;
  effect.effect_instruction_i1 = void 0;
  return effect;
};
var whenEffect = dual(2, (self, condition) => flatMap7(condition, (b) => {
  if (b) {
    return pipe(self, map8(some));
  }
  return succeed(none());
}));
var whileLoop = (options) => {
  const effect = new EffectPrimitive(OP_WHILE);
  effect.effect_instruction_i0 = options.while;
  effect.effect_instruction_i1 = options.body;
  effect.effect_instruction_i2 = options.step;
  return effect;
};
var fromIterator = (iterator) => suspend(() => {
  const effect = new EffectPrimitive(OP_ITERATOR);
  effect.effect_instruction_i0 = iterator();
  return effect;
});
var gen = function() {
  const f = arguments.length === 1 ? arguments[0] : arguments[1].bind(arguments[0]);
  return fromIterator(() => f(pipe));
};
var fnUntraced = (body, ...pipeables) => Object.defineProperty(pipeables.length === 0 ? function(...args) {
  return fromIterator(() => body.apply(this, args));
} : function(...args) {
  let effect = fromIterator(() => body.apply(this, args));
  for (const x of pipeables) {
    effect = x(effect, ...args);
  }
  return effect;
}, "length", {
  value: body.length,
  configurable: true
});
var withConcurrency = dual(2, (self, concurrency) => fiberRefLocally(self, currentConcurrency, concurrency));
var withRequestBatching = dual(2, (self, requestBatching) => fiberRefLocally(self, currentRequestBatching, requestBatching));
var withRuntimeFlags = dual(2, (self, update3) => {
  const effect = new EffectPrimitive(OP_UPDATE_RUNTIME_FLAGS);
  effect.effect_instruction_i0 = update3;
  effect.effect_instruction_i1 = () => self;
  return effect;
});
var withTracerEnabled = dual(2, (effect, enabled2) => fiberRefLocally(effect, currentTracerEnabled, enabled2));
var withTracerTiming = dual(2, (effect, enabled2) => fiberRefLocally(effect, currentTracerTimingEnabled, enabled2));
var yieldNow = (options) => {
  const effect = new EffectPrimitive(OP_YIELD);
  return typeof options?.priority !== "undefined" ? withSchedulingPriority(effect, options.priority) : effect;
};
var zip2 = dual(2, (self, that) => flatMap7(self, (a) => map8(that, (b) => [a, b])));
var zipFlatten = dual(2, (self, that) => flatMap7(self, (a) => map8(that, (b) => [...a, b])));
var zipLeft = dual(2, (self, that) => flatMap7(self, (a) => as2(that, a)));
var zipRight = dual(2, (self, that) => flatMap7(self, () => that));
var zipWith = dual(3, (self, that, f) => flatMap7(self, (a) => map8(that, (b) => f(a, b))));
var never = asyncInterrupt(() => {
  const interval = setInterval(() => {
  }, 2 ** 31 - 1);
  return sync(() => clearInterval(interval));
});
var interruptFiber = (self) => flatMap7(fiberId, (fiberId2) => pipe(self, interruptAsFiber(fiberId2)));
var interruptAsFiber = dual(2, (self, fiberId2) => flatMap7(self.interruptAsFork(fiberId2), () => self.await));
var logLevelAll = {
  _tag: "All",
  syslog: 0,
  label: "ALL",
  ordinal: Number.MIN_SAFE_INTEGER,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var logLevelFatal = {
  _tag: "Fatal",
  syslog: 2,
  label: "FATAL",
  ordinal: 5e4,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var logLevelError = {
  _tag: "Error",
  syslog: 3,
  label: "ERROR",
  ordinal: 4e4,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var logLevelWarning = {
  _tag: "Warning",
  syslog: 4,
  label: "WARN",
  ordinal: 3e4,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var logLevelInfo = {
  _tag: "Info",
  syslog: 6,
  label: "INFO",
  ordinal: 2e4,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var logLevelDebug = {
  _tag: "Debug",
  syslog: 7,
  label: "DEBUG",
  ordinal: 1e4,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var logLevelTrace = {
  _tag: "Trace",
  syslog: 7,
  label: "TRACE",
  ordinal: 0,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var logLevelNone = {
  _tag: "None",
  syslog: 7,
  label: "OFF",
  ordinal: Number.MAX_SAFE_INTEGER,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var allLogLevels = [logLevelAll, logLevelTrace, logLevelDebug, logLevelInfo, logLevelWarning, logLevelError, logLevelFatal, logLevelNone];
var FiberRefSymbolKey = "effect/FiberRef";
var FiberRefTypeId = Symbol.for(FiberRefSymbolKey);
var fiberRefVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var fiberRefGet = (self) => withFiberRuntime((fiber) => exitSucceed(fiber.getFiberRef(self)));
var fiberRefGetAndSet = dual(2, (self, value) => fiberRefModify(self, (v) => [v, value]));
var fiberRefGetAndUpdate = dual(2, (self, f) => fiberRefModify(self, (v) => [v, f(v)]));
var fiberRefGetAndUpdateSome = dual(2, (self, pf) => fiberRefModify(self, (v) => [v, getOrElse(pf(v), () => v)]));
var fiberRefGetWith = dual(2, (self, f) => flatMap7(fiberRefGet(self), f));
var fiberRefSet = dual(2, (self, value) => fiberRefModify(self, () => [void 0, value]));
var fiberRefDelete = (self) => withFiberRuntime((state) => {
  state.unsafeDeleteFiberRef(self);
  return void_;
});
var fiberRefReset = (self) => fiberRefSet(self, self.initial);
var fiberRefModify = dual(2, (self, f) => withFiberRuntime((state) => {
  const [b, a] = f(state.getFiberRef(self));
  state.setFiberRef(self, a);
  return succeed(b);
}));
var fiberRefModifySome = (self, def, f) => fiberRefModify(self, (v) => getOrElse(f(v), () => [def, v]));
var fiberRefUpdate = dual(2, (self, f) => fiberRefModify(self, (v) => [void 0, f(v)]));
var fiberRefUpdateSome = dual(2, (self, pf) => fiberRefModify(self, (v) => [void 0, getOrElse(pf(v), () => v)]));
var fiberRefUpdateAndGet = dual(2, (self, f) => fiberRefModify(self, (v) => {
  const result = f(v);
  return [result, result];
}));
var fiberRefUpdateSomeAndGet = dual(2, (self, pf) => fiberRefModify(self, (v) => {
  const result = getOrElse(pf(v), () => v);
  return [result, result];
}));
var RequestResolverSymbolKey = "effect/RequestResolver";
var RequestResolverTypeId = Symbol.for(RequestResolverSymbolKey);
var requestResolverVariance = {
  /* c8 ignore next */
  _A: (_) => _,
  /* c8 ignore next */
  _R: (_) => _
};
var RequestResolverImpl = class _RequestResolverImpl {
  runAll;
  target;
  [RequestResolverTypeId] = requestResolverVariance;
  constructor(runAll, target) {
    this.runAll = runAll;
    this.target = target;
  }
  [symbol]() {
    return cached(this, this.target ? hash(this.target) : random(this));
  }
  [symbol2](that) {
    return this.target ? isRequestResolver(that) && equals(this.target, that.target) : this === that;
  }
  identified(...ids3) {
    return new _RequestResolverImpl(this.runAll, fromIterable2(ids3));
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isRequestResolver = (u) => hasProperty(u, RequestResolverTypeId);
var resolverLocally = dual(3, (use, self, value) => new RequestResolverImpl((requests) => fiberRefLocally(use.runAll(requests), self, value), make2("Locally", use, self, value)));
var fiberRefLocally = dual(3, (use, self, value) => acquireUseRelease(zipLeft(fiberRefGet(self), fiberRefSet(self, value)), () => use, (oldValue) => fiberRefSet(self, oldValue)));
var fiberRefLocallyWith = dual(3, (use, self, f) => fiberRefGetWith(self, (a) => fiberRefLocally(use, self, f(a))));
var fiberRefUnsafeMake = (initial, options) => fiberRefUnsafeMakePatch(initial, {
  differ: update2(),
  fork: options?.fork ?? identity,
  join: options?.join
});
var fiberRefUnsafeMakeHashSet = (initial) => {
  const differ2 = hashSet();
  return fiberRefUnsafeMakePatch(initial, {
    differ: differ2,
    fork: differ2.empty
  });
};
var fiberRefUnsafeMakeReadonlyArray = (initial) => {
  const differ2 = readonlyArray(update2());
  return fiberRefUnsafeMakePatch(initial, {
    differ: differ2,
    fork: differ2.empty
  });
};
var fiberRefUnsafeMakeContext = (initial) => {
  const differ2 = environment();
  return fiberRefUnsafeMakePatch(initial, {
    differ: differ2,
    fork: differ2.empty
  });
};
var fiberRefUnsafeMakePatch = (initial, options) => {
  const _fiberRef = {
    ...CommitPrototype,
    [FiberRefTypeId]: fiberRefVariance,
    initial,
    commit() {
      return fiberRefGet(this);
    },
    diff: (oldValue, newValue) => options.differ.diff(oldValue, newValue),
    combine: (first, second) => options.differ.combine(first, second),
    patch: (patch8) => (oldValue) => options.differ.patch(patch8, oldValue),
    fork: options.fork,
    join: options.join ?? ((_, n) => n)
  };
  return _fiberRef;
};
var fiberRefUnsafeMakeRuntimeFlags = (initial) => fiberRefUnsafeMakePatch(initial, {
  differ,
  fork: differ.empty
});
var currentContext = globalValue(Symbol.for("effect/FiberRef/currentContext"), () => fiberRefUnsafeMakeContext(empty3()));
var currentSchedulingPriority = globalValue(Symbol.for("effect/FiberRef/currentSchedulingPriority"), () => fiberRefUnsafeMake(0));
var currentMaxOpsBeforeYield = globalValue(Symbol.for("effect/FiberRef/currentMaxOpsBeforeYield"), () => fiberRefUnsafeMake(2048));
var currentLogAnnotations = globalValue(Symbol.for("effect/FiberRef/currentLogAnnotation"), () => fiberRefUnsafeMake(empty7()));
var currentLogLevel = globalValue(Symbol.for("effect/FiberRef/currentLogLevel"), () => fiberRefUnsafeMake(logLevelInfo));
var currentLogSpan = globalValue(Symbol.for("effect/FiberRef/currentLogSpan"), () => fiberRefUnsafeMake(empty8()));
var withSchedulingPriority = dual(2, (self, scheduler) => fiberRefLocally(self, currentSchedulingPriority, scheduler));
var withMaxOpsBeforeYield = dual(2, (self, scheduler) => fiberRefLocally(self, currentMaxOpsBeforeYield, scheduler));
var currentConcurrency = globalValue(Symbol.for("effect/FiberRef/currentConcurrency"), () => fiberRefUnsafeMake("unbounded"));
var currentRequestBatching = globalValue(Symbol.for("effect/FiberRef/currentRequestBatching"), () => fiberRefUnsafeMake(true));
var currentUnhandledErrorLogLevel = globalValue(Symbol.for("effect/FiberRef/currentUnhandledErrorLogLevel"), () => fiberRefUnsafeMake(some(logLevelDebug)));
var currentVersionMismatchErrorLogLevel = globalValue(Symbol.for("effect/FiberRef/versionMismatchErrorLogLevel"), () => fiberRefUnsafeMake(some(logLevelWarning)));
var withUnhandledErrorLogLevel = dual(2, (self, level) => fiberRefLocally(self, currentUnhandledErrorLogLevel, level));
var currentMetricLabels = globalValue(Symbol.for("effect/FiberRef/currentMetricLabels"), () => fiberRefUnsafeMakeReadonlyArray(empty()));
var metricLabels = fiberRefGet(currentMetricLabels);
var currentForkScopeOverride = globalValue(Symbol.for("effect/FiberRef/currentForkScopeOverride"), () => fiberRefUnsafeMake(none(), {
  fork: () => none(),
  join: (parent, _) => parent
}));
var currentInterruptedCause = globalValue(Symbol.for("effect/FiberRef/currentInterruptedCause"), () => fiberRefUnsafeMake(empty17, {
  fork: () => empty17,
  join: (parent, _) => parent
}));
var currentTracerEnabled = globalValue(Symbol.for("effect/FiberRef/currentTracerEnabled"), () => fiberRefUnsafeMake(true));
var currentTracerTimingEnabled = globalValue(Symbol.for("effect/FiberRef/currentTracerTiming"), () => fiberRefUnsafeMake(true));
var currentTracerSpanAnnotations = globalValue(Symbol.for("effect/FiberRef/currentTracerSpanAnnotations"), () => fiberRefUnsafeMake(empty7()));
var currentTracerSpanLinks = globalValue(Symbol.for("effect/FiberRef/currentTracerSpanLinks"), () => fiberRefUnsafeMake(empty2()));
var ScopeTypeId = Symbol.for("effect/Scope");
var CloseableScopeTypeId = Symbol.for("effect/CloseableScope");
var scopeAddFinalizer = (self, finalizer) => self.addFinalizer(() => asVoid(finalizer));
var scopeAddFinalizerExit = (self, finalizer) => self.addFinalizer(finalizer);
var scopeClose = (self, exit2) => self.close(exit2);
var scopeFork = (self, strategy) => self.fork(strategy);
var causeSquash = (self) => {
  return causeSquashWith(identity)(self);
};
var causeSquashWith = dual(2, (self, f) => {
  const option = pipe(self, failureOption, map(f));
  switch (option._tag) {
    case "None": {
      return pipe(defects(self), head, match({
        onNone: () => {
          const interrupts = fromIterable(interruptors(self)).flatMap((fiberId2) => fromIterable(ids2(fiberId2)).map((id) => `#${id}`));
          return new InterruptedException(interrupts ? `Interrupted by fibers: ${interrupts.join(", ")}` : void 0);
        },
        onSome: identity
      }));
    }
    case "Some": {
      return option.value;
    }
  }
});
var YieldableError = (function() {
  class YieldableError2 extends globalThis.Error {
    commit() {
      return fail2(this);
    }
    toJSON() {
      const obj = {
        ...this
      };
      if (this.message) obj.message = this.message;
      if (this.cause) obj.cause = this.cause;
      return obj;
    }
    [NodeInspectSymbol]() {
      if (this.toString !== globalThis.Error.prototype.toString) {
        return this.stack ? `${this.toString()}
${this.stack.split("\n").slice(1).join("\n")}` : this.toString();
      } else if ("Bun" in globalThis) {
        return pretty(fail(this), {
          renderErrorCause: true
        });
      }
      return this;
    }
  }
  Object.assign(YieldableError2.prototype, StructuralCommitPrototype);
  return YieldableError2;
})();
var makeException = (proto2, tag) => {
  class Base extends YieldableError {
    _tag = tag;
  }
  Object.assign(Base.prototype, proto2);
  Base.prototype.name = tag;
  return Base;
};
var RuntimeExceptionTypeId = Symbol.for("effect/Cause/errors/RuntimeException");
var RuntimeException = makeException({
  [RuntimeExceptionTypeId]: RuntimeExceptionTypeId
}, "RuntimeException");
var isRuntimeException = (u) => hasProperty(u, RuntimeExceptionTypeId);
var InterruptedExceptionTypeId = Symbol.for("effect/Cause/errors/InterruptedException");
var InterruptedException = makeException({
  [InterruptedExceptionTypeId]: InterruptedExceptionTypeId
}, "InterruptedException");
var isInterruptedException = (u) => hasProperty(u, InterruptedExceptionTypeId);
var IllegalArgumentExceptionTypeId = Symbol.for("effect/Cause/errors/IllegalArgument");
var IllegalArgumentException = makeException({
  [IllegalArgumentExceptionTypeId]: IllegalArgumentExceptionTypeId
}, "IllegalArgumentException");
var isIllegalArgumentException = (u) => hasProperty(u, IllegalArgumentExceptionTypeId);
var NoSuchElementExceptionTypeId = Symbol.for("effect/Cause/errors/NoSuchElement");
var NoSuchElementException = makeException({
  [NoSuchElementExceptionTypeId]: NoSuchElementExceptionTypeId
}, "NoSuchElementException");
var isNoSuchElementException = (u) => hasProperty(u, NoSuchElementExceptionTypeId);
var InvalidPubSubCapacityExceptionTypeId = Symbol.for("effect/Cause/errors/InvalidPubSubCapacityException");
var InvalidPubSubCapacityException = makeException({
  [InvalidPubSubCapacityExceptionTypeId]: InvalidPubSubCapacityExceptionTypeId
}, "InvalidPubSubCapacityException");
var ExceededCapacityExceptionTypeId = Symbol.for("effect/Cause/errors/ExceededCapacityException");
var ExceededCapacityException = makeException({
  [ExceededCapacityExceptionTypeId]: ExceededCapacityExceptionTypeId
}, "ExceededCapacityException");
var isExceededCapacityException = (u) => hasProperty(u, ExceededCapacityExceptionTypeId);
var TimeoutExceptionTypeId = Symbol.for("effect/Cause/errors/Timeout");
var TimeoutException = makeException({
  [TimeoutExceptionTypeId]: TimeoutExceptionTypeId
}, "TimeoutException");
var timeoutExceptionFromDuration = (duration) => new TimeoutException(`Operation timed out after '${format2(duration)}'`);
var isTimeoutException = (u) => hasProperty(u, TimeoutExceptionTypeId);
var UnknownExceptionTypeId = Symbol.for("effect/Cause/errors/UnknownException");
var UnknownException = (function() {
  class UnknownException2 extends YieldableError {
    _tag = "UnknownException";
    error;
    constructor(cause, message) {
      super(message ?? "An unknown error occurred", {
        cause
      });
      this.error = cause;
    }
  }
  Object.assign(UnknownException2.prototype, {
    [UnknownExceptionTypeId]: UnknownExceptionTypeId,
    name: "UnknownException"
  });
  return UnknownException2;
})();
var isUnknownException = (u) => hasProperty(u, UnknownExceptionTypeId);
var exitIsExit = (u) => isEffect(u) && "_tag" in u && (u._tag === "Success" || u._tag === "Failure");
var exitIsFailure = (self) => self._tag === "Failure";
var exitIsSuccess = (self) => self._tag === "Success";
var exitIsInterrupted = (self) => {
  switch (self._tag) {
    case OP_FAILURE:
      return isInterrupted(self.effect_instruction_i0);
    case OP_SUCCESS:
      return false;
  }
};
var exitAs = dual(2, (self, value) => {
  switch (self._tag) {
    case OP_FAILURE: {
      return exitFailCause(self.effect_instruction_i0);
    }
    case OP_SUCCESS: {
      return exitSucceed(value);
    }
  }
});
var exitAsVoid = (self) => exitAs(self, void 0);
var exitCauseOption = (self) => {
  switch (self._tag) {
    case OP_FAILURE:
      return some(self.effect_instruction_i0);
    case OP_SUCCESS:
      return none();
  }
};
var exitCollectAll = (exits, options) => exitCollectAllInternal(exits, options?.parallel ? parallel : sequential);
var exitDie = (defect) => exitFailCause(die(defect));
var exitExists = dual(2, (self, refinement) => {
  switch (self._tag) {
    case OP_FAILURE:
      return false;
    case OP_SUCCESS:
      return refinement(self.effect_instruction_i0);
  }
});
var exitFail = (error) => exitFailCause(fail(error));
var exitFailCause = (cause) => {
  const effect = new EffectPrimitiveFailure(OP_FAILURE);
  effect.effect_instruction_i0 = cause;
  return effect;
};
var exitFlatMap = dual(2, (self, f) => {
  switch (self._tag) {
    case OP_FAILURE: {
      return exitFailCause(self.effect_instruction_i0);
    }
    case OP_SUCCESS: {
      return f(self.effect_instruction_i0);
    }
  }
});
var exitFlatMapEffect = dual(2, (self, f) => {
  switch (self._tag) {
    case OP_FAILURE: {
      return succeed(exitFailCause(self.effect_instruction_i0));
    }
    case OP_SUCCESS: {
      return f(self.effect_instruction_i0);
    }
  }
});
var exitFlatten = (self) => pipe(self, exitFlatMap(identity));
var exitForEachEffect = dual(2, (self, f) => {
  switch (self._tag) {
    case OP_FAILURE: {
      return succeed(exitFailCause(self.effect_instruction_i0));
    }
    case OP_SUCCESS: {
      return exit(f(self.effect_instruction_i0));
    }
  }
});
var exitFromEither = (either4) => {
  switch (either4._tag) {
    case "Left":
      return exitFail(either4.left);
    case "Right":
      return exitSucceed(either4.right);
  }
};
var exitFromOption = (option) => {
  switch (option._tag) {
    case "None":
      return exitFail(void 0);
    case "Some":
      return exitSucceed(option.value);
  }
};
var exitGetOrElse = dual(2, (self, orElse2) => {
  switch (self._tag) {
    case OP_FAILURE:
      return orElse2(self.effect_instruction_i0);
    case OP_SUCCESS:
      return self.effect_instruction_i0;
  }
});
var exitInterrupt = (fiberId2) => exitFailCause(interrupt(fiberId2));
var exitMap = dual(2, (self, f) => {
  switch (self._tag) {
    case OP_FAILURE:
      return exitFailCause(self.effect_instruction_i0);
    case OP_SUCCESS:
      return exitSucceed(f(self.effect_instruction_i0));
  }
});
var exitMapBoth = dual(2, (self, {
  onFailure,
  onSuccess
}) => {
  switch (self._tag) {
    case OP_FAILURE:
      return exitFailCause(pipe(self.effect_instruction_i0, map7(onFailure)));
    case OP_SUCCESS:
      return exitSucceed(onSuccess(self.effect_instruction_i0));
  }
});
var exitMapError = dual(2, (self, f) => {
  switch (self._tag) {
    case OP_FAILURE:
      return exitFailCause(pipe(self.effect_instruction_i0, map7(f)));
    case OP_SUCCESS:
      return exitSucceed(self.effect_instruction_i0);
  }
});
var exitMapErrorCause = dual(2, (self, f) => {
  switch (self._tag) {
    case OP_FAILURE:
      return exitFailCause(f(self.effect_instruction_i0));
    case OP_SUCCESS:
      return exitSucceed(self.effect_instruction_i0);
  }
});
var exitMatch = dual(2, (self, {
  onFailure,
  onSuccess
}) => {
  switch (self._tag) {
    case OP_FAILURE:
      return onFailure(self.effect_instruction_i0);
    case OP_SUCCESS:
      return onSuccess(self.effect_instruction_i0);
  }
});
var exitMatchEffect = dual(2, (self, {
  onFailure,
  onSuccess
}) => {
  switch (self._tag) {
    case OP_FAILURE:
      return onFailure(self.effect_instruction_i0);
    case OP_SUCCESS:
      return onSuccess(self.effect_instruction_i0);
  }
});
var exitSucceed = (value) => {
  const effect = new EffectPrimitiveSuccess(OP_SUCCESS);
  effect.effect_instruction_i0 = value;
  return effect;
};
var exitVoid = exitSucceed(void 0);
var exitZip = dual(2, (self, that) => exitZipWith(self, that, {
  onSuccess: (a, a2) => [a, a2],
  onFailure: sequential
}));
var exitZipLeft = dual(2, (self, that) => exitZipWith(self, that, {
  onSuccess: (a, _) => a,
  onFailure: sequential
}));
var exitZipRight = dual(2, (self, that) => exitZipWith(self, that, {
  onSuccess: (_, a2) => a2,
  onFailure: sequential
}));
var exitZipPar = dual(2, (self, that) => exitZipWith(self, that, {
  onSuccess: (a, a2) => [a, a2],
  onFailure: parallel
}));
var exitZipParLeft = dual(2, (self, that) => exitZipWith(self, that, {
  onSuccess: (a, _) => a,
  onFailure: parallel
}));
var exitZipParRight = dual(2, (self, that) => exitZipWith(self, that, {
  onSuccess: (_, a2) => a2,
  onFailure: parallel
}));
var exitZipWith = dual(3, (self, that, {
  onFailure,
  onSuccess
}) => {
  switch (self._tag) {
    case OP_FAILURE: {
      switch (that._tag) {
        case OP_SUCCESS:
          return exitFailCause(self.effect_instruction_i0);
        case OP_FAILURE: {
          return exitFailCause(onFailure(self.effect_instruction_i0, that.effect_instruction_i0));
        }
      }
    }
    case OP_SUCCESS: {
      switch (that._tag) {
        case OP_SUCCESS:
          return exitSucceed(onSuccess(self.effect_instruction_i0, that.effect_instruction_i0));
        case OP_FAILURE:
          return exitFailCause(that.effect_instruction_i0);
      }
    }
  }
});
var exitCollectAllInternal = (exits, combineCauses) => {
  const list = fromIterable2(exits);
  if (!isNonEmpty(list)) {
    return none();
  }
  return pipe(tailNonEmpty2(list), reduce(pipe(headNonEmpty2(list), exitMap(of2)), (accumulator, current) => pipe(accumulator, exitZipWith(current, {
    onSuccess: (list2, value) => pipe(list2, prepend(value)),
    onFailure: combineCauses
  }))), exitMap(reverse), exitMap((chunk2) => toReadonlyArray(chunk2)), some);
};
var deferredUnsafeMake = (fiberId2) => {
  const _deferred = {
    ...CommitPrototype,
    [DeferredTypeId]: deferredVariance,
    state: make8(pending([])),
    commit() {
      return deferredAwait(this);
    },
    blockingOn: fiberId2
  };
  return _deferred;
};
var deferredMake = () => flatMap7(fiberId, (id) => deferredMakeAs(id));
var deferredMakeAs = (fiberId2) => sync(() => deferredUnsafeMake(fiberId2));
var deferredAwait = (self) => asyncInterrupt((resume) => {
  const state = get2(self.state);
  switch (state._tag) {
    case OP_STATE_DONE: {
      return resume(state.effect);
    }
    case OP_STATE_PENDING: {
      state.joiners.push(resume);
      return deferredInterruptJoiner(self, resume);
    }
  }
}, self.blockingOn);
var deferredComplete = dual(2, (self, effect) => intoDeferred(effect, self));
var deferredCompleteWith = dual(2, (self, effect) => sync(() => {
  const state = get2(self.state);
  switch (state._tag) {
    case OP_STATE_DONE: {
      return false;
    }
    case OP_STATE_PENDING: {
      set2(self.state, done(effect));
      for (let i = 0, len = state.joiners.length; i < len; i++) {
        state.joiners[i](effect);
      }
      return true;
    }
  }
}));
var deferredDone = dual(2, (self, exit2) => deferredCompleteWith(self, exit2));
var deferredFail = dual(2, (self, error) => deferredCompleteWith(self, fail2(error)));
var deferredFailSync = dual(2, (self, evaluate) => deferredCompleteWith(self, failSync(evaluate)));
var deferredFailCause = dual(2, (self, cause) => deferredCompleteWith(self, failCause(cause)));
var deferredFailCauseSync = dual(2, (self, evaluate) => deferredCompleteWith(self, failCauseSync(evaluate)));
var deferredDie = dual(2, (self, defect) => deferredCompleteWith(self, die2(defect)));
var deferredDieSync = dual(2, (self, evaluate) => deferredCompleteWith(self, dieSync(evaluate)));
var deferredInterrupt = (self) => flatMap7(fiberId, (fiberId2) => deferredCompleteWith(self, interruptWith(fiberId2)));
var deferredInterruptWith = dual(2, (self, fiberId2) => deferredCompleteWith(self, interruptWith(fiberId2)));
var deferredIsDone = (self) => sync(() => get2(self.state)._tag === OP_STATE_DONE);
var deferredPoll = (self) => sync(() => {
  const state = get2(self.state);
  switch (state._tag) {
    case OP_STATE_DONE: {
      return some(state.effect);
    }
    case OP_STATE_PENDING: {
      return none();
    }
  }
});
var deferredSucceed = dual(2, (self, value) => deferredCompleteWith(self, succeed(value)));
var deferredSync = dual(2, (self, evaluate) => deferredCompleteWith(self, sync(evaluate)));
var deferredUnsafeDone = (self, effect) => {
  const state = get2(self.state);
  if (state._tag === OP_STATE_PENDING) {
    set2(self.state, done(effect));
    for (let i = 0, len = state.joiners.length; i < len; i++) {
      state.joiners[i](effect);
    }
  }
};
var deferredInterruptJoiner = (self, joiner) => sync(() => {
  const state = get2(self.state);
  if (state._tag === OP_STATE_PENDING) {
    const index = state.joiners.indexOf(joiner);
    if (index >= 0) {
      state.joiners.splice(index, 1);
    }
  }
});
var constContext = withFiberRuntime((fiber) => exitSucceed(fiber.currentContext));
var context = () => constContext;
var contextWithEffect = (f) => flatMap7(context(), f);
var provideContext = dual(2, (self, context2) => fiberRefLocally(currentContext, context2)(self));
var provideSomeContext = dual(2, (self, context2) => fiberRefLocallyWith(currentContext, (parent) => merge(parent, context2))(self));
var mapInputContext = dual(2, (self, f) => contextWithEffect((context2) => provideContext(self, f(context2))));
var filterEffectOrElse = dual(2, (self, options) => flatMap7(self, (a) => flatMap7(options.predicate(a), (pass) => pass ? succeed(a) : options.orElse(a))));
var filterEffectOrFail = dual(2, (self, options) => filterEffectOrElse(self, {
  predicate: options.predicate,
  orElse: (a) => fail2(options.orFailWith(a))
}));
var currentSpanFromFiber = (fiber) => {
  const span = fiber.currentSpan;
  return span !== void 0 && span._tag === "Span" ? some(span) : none();
};
var NoopSpanProto = {
  _tag: "Span",
  spanId: "noop",
  traceId: "noop",
  sampled: false,
  status: {
    _tag: "Ended",
    startTime: BigInt(0),
    endTime: BigInt(0),
    exit: exitVoid
  },
  attributes: /* @__PURE__ */ new Map(),
  links: [],
  kind: "internal",
  attribute() {
  },
  event() {
  },
  end() {
  },
  addLinks() {
  }
};
var noopSpan = (options) => Object.assign(Object.create(NoopSpanProto), options);

export {
  decode,
  decodeUnknown,
  isDuration,
  isFinite,
  zero,
  infinity,
  nanos,
  millis,
  seconds,
  toMillis,
  toSeconds,
  toNanos,
  between2 as between,
  Equivalence,
  clamp2 as clamp,
  times,
  sum,
  lessThan,
  lessThanOrEqualTo,
  greaterThan,
  greaterThanOrEqualTo,
  Duration_exports,
  make4 as make,
  isHashSet2 as isHashSet,
  empty6 as empty,
  fromIterable5 as fromIterable,
  make7 as make2,
  has3 as has,
  size3 as size,
  add2 as add,
  remove3 as remove,
  difference2 as difference,
  union3 as union,
  forEach3 as forEach,
  reduce4 as reduce,
  HashSet_exports,
  make8 as make3,
  compareAndSet,
  decrementAndGet,
  get2 as get,
  incrementAndGet,
  set2 as set,
  update,
  MutableRef_exports,
  none2 as none,
  threadName,
  none3 as none2,
  runtime2 as runtime,
  composite2 as composite,
  isFiberId2 as isFiberId,
  combine3 as combine,
  combineAll2 as combineAll,
  getOrElse3 as getOrElse,
  ids2 as ids,
  make10 as make4,
  threadName2,
  toSet2 as toSet,
  unsafeMake2 as unsafeMake,
  FiberId_exports,
  isHashMap2 as isHashMap,
  empty7 as empty2,
  make11 as make5,
  fromIterable6 as fromIterable2,
  isEmpty3 as isEmpty,
  get3 as get2,
  unsafeGet3 as unsafeGet,
  has4 as has2,
  set3 as set2,
  toValues2 as toValues,
  size4 as size2,
  mutate4 as mutate,
  union4 as union2,
  remove4 as remove2,
  forEach4 as forEach2,
  HashMap_exports,
  isList,
  isCons,
  cons,
  empty8 as empty3,
  of3 as of,
  fromIterable7 as fromIterable3,
  prepend2 as prepend,
  List_exports,
  ArrayProto,
  Structural,
  struct,
  DifferTypeId,
  make13 as make6,
  environment,
  chunk,
  hashMap,
  hashSet,
  orElseEither,
  readonlyArray,
  transform,
  update2,
  updateWith,
  zip,
  enable,
  disable,
  None2 as None,
  Interruption,
  OpSupervision,
  RuntimeMetrics,
  WindDown,
  CooperativeYielding,
  cooperativeYielding,
  disable2,
  disableAll,
  enable2,
  enableAll,
  interruptible,
  interruption,
  isDisabled2 as isDisabled,
  isEnabled2 as isEnabled,
  make15 as make7,
  none4 as none3,
  opSupervision,
  render,
  runtimeMetrics,
  toSet3 as toSet2,
  windDown,
  diff7 as diff,
  patch7 as patch,
  differ,
  empty16 as empty4,
  exclude2 as exclude,
  RuntimeFlagsPatch_exports,
  empty18 as empty5,
  par,
  seq,
  single,
  mapRequestResolvers,
  reduce8 as reduce2,
  flatten2 as flatten,
  isEntry,
  makeEntry,
  sequentialCollectionToChunk,
  CauseTypeId,
  empty17 as empty6,
  fail,
  die,
  interrupt,
  parallel,
  sequential,
  isCause,
  isEmptyType,
  isFailType,
  isDieType,
  isInterruptType,
  isSequentialType,
  isParallelType,
  size6 as size3,
  isEmpty6 as isEmpty2,
  isFailure,
  isDie,
  isInterrupted,
  isInterruptedOnly,
  failures,
  defects,
  interruptors,
  failureOption,
  failureOrCause,
  dieOption,
  flipCauseOption,
  interruptOption,
  keepDefects,
  linearize,
  stripFailures,
  stripSomeDefects,
  as,
  map7 as map,
  flatMap6 as flatMap,
  flatten as flatten2,
  andThen3 as andThen,
  contains,
  find,
  filter6 as filter,
  match3 as match,
  reduce7 as reduce3,
  reduceWithContext,
  pretty,
  prettyErrorMessage,
  spanToTrace,
  prettyErrors,
  DeferredTypeId,
  SingleShotGen,
  blocked,
  runRequestBlock,
  EffectTypeId,
  RevertFlags,
  isEffect,
  withFiberRuntime,
  acquireUseRelease,
  as2,
  asVoid,
  custom,
  unsafeAsync,
  asyncInterrupt,
  async_,
  catchAllCause,
  catchAll,
  catchIf,
  catchSome,
  checkInterruptible,
  originalInstance,
  capture,
  die2,
  dieMessage,
  dieSync,
  either3 as either,
  exit,
  fail2,
  failSync,
  failCause,
  failCauseSync,
  fiberId,
  fiberIdWith,
  flatMap7 as flatMap2,
  andThen4 as andThen2,
  step2 as step,
  flatten3,
  flip,
  matchCause,
  matchCauseEffect,
  matchEffect,
  forEachSequential,
  forEachSequentialDiscard,
  if_,
  interrupt2,
  interruptWith,
  interruptible2,
  interruptibleMask,
  intoDeferred,
  map8 as map2,
  mapBoth,
  mapError,
  onError,
  onExit,
  onInterrupt,
  orElse,
  orDie,
  orDieWith,
  partitionMap3 as partitionMap,
  runtimeFlags,
  succeed,
  suspend,
  sync,
  tap,
  transplant,
  uninterruptible,
  uninterruptibleMask,
  void_,
  updateRuntimeFlags,
  whenEffect,
  whileLoop,
  fromIterator,
  gen,
  fnUntraced,
  withConcurrency,
  withRequestBatching,
  withRuntimeFlags,
  withTracerEnabled,
  withTracerTiming,
  yieldNow,
  zip2,
  zipLeft,
  zipRight,
  zipWith,
  never,
  interruptFiber,
  interruptAsFiber,
  logLevelAll,
  logLevelFatal,
  logLevelError,
  logLevelWarning,
  logLevelInfo,
  logLevelDebug,
  logLevelTrace,
  logLevelNone,
  allLogLevels,
  FiberRefTypeId,
  fiberRefGet,
  fiberRefGetAndSet,
  fiberRefGetAndUpdate,
  fiberRefGetAndUpdateSome,
  fiberRefGetWith,
  fiberRefSet,
  fiberRefDelete,
  fiberRefReset,
  fiberRefModify,
  fiberRefModifySome,
  fiberRefUpdate,
  fiberRefUpdateSome,
  fiberRefUpdateAndGet,
  fiberRefUpdateSomeAndGet,
  RequestResolverTypeId,
  RequestResolverImpl,
  isRequestResolver,
  resolverLocally,
  fiberRefLocally,
  fiberRefLocallyWith,
  fiberRefUnsafeMake,
  fiberRefUnsafeMakeHashSet,
  fiberRefUnsafeMakeContext,
  fiberRefUnsafeMakePatch,
  fiberRefUnsafeMakeRuntimeFlags,
  currentContext,
  currentSchedulingPriority,
  currentMaxOpsBeforeYield,
  currentLogAnnotations,
  currentLogLevel,
  currentLogSpan,
  withSchedulingPriority,
  withMaxOpsBeforeYield,
  currentConcurrency,
  currentRequestBatching,
  currentUnhandledErrorLogLevel,
  currentVersionMismatchErrorLogLevel,
  withUnhandledErrorLogLevel,
  currentMetricLabels,
  metricLabels,
  currentForkScopeOverride,
  currentInterruptedCause,
  currentTracerEnabled,
  currentTracerTimingEnabled,
  currentTracerSpanAnnotations,
  currentTracerSpanLinks,
  ScopeTypeId,
  CloseableScopeTypeId,
  scopeAddFinalizer,
  scopeAddFinalizerExit,
  scopeClose,
  scopeFork,
  causeSquash,
  causeSquashWith,
  YieldableError,
  RuntimeExceptionTypeId,
  RuntimeException,
  isRuntimeException,
  InterruptedExceptionTypeId,
  InterruptedException,
  isInterruptedException,
  IllegalArgumentExceptionTypeId,
  IllegalArgumentException,
  isIllegalArgumentException,
  NoSuchElementExceptionTypeId,
  NoSuchElementException,
  isNoSuchElementException,
  InvalidPubSubCapacityExceptionTypeId,
  InvalidPubSubCapacityException,
  ExceededCapacityExceptionTypeId,
  ExceededCapacityException,
  isExceededCapacityException,
  TimeoutExceptionTypeId,
  TimeoutException,
  timeoutExceptionFromDuration,
  isTimeoutException,
  UnknownExceptionTypeId,
  UnknownException,
  isUnknownException,
  exitIsExit,
  exitIsFailure,
  exitIsSuccess,
  exitIsInterrupted,
  exitAs,
  exitAsVoid,
  exitCauseOption,
  exitCollectAll,
  exitDie,
  exitExists,
  exitFail,
  exitFailCause,
  exitFlatMap,
  exitFlatMapEffect,
  exitFlatten,
  exitForEachEffect,
  exitFromEither,
  exitFromOption,
  exitGetOrElse,
  exitInterrupt,
  exitMap,
  exitMapBoth,
  exitMapError,
  exitMapErrorCause,
  exitMatch,
  exitMatchEffect,
  exitSucceed,
  exitVoid,
  exitZip,
  exitZipLeft,
  exitZipRight,
  exitZipPar,
  exitZipParLeft,
  exitZipParRight,
  exitZipWith,
  deferredUnsafeMake,
  deferredMake,
  deferredMakeAs,
  deferredAwait,
  deferredComplete,
  deferredCompleteWith,
  deferredDone,
  deferredFail,
  deferredFailSync,
  deferredFailCause,
  deferredFailCauseSync,
  deferredDie,
  deferredDieSync,
  deferredInterrupt,
  deferredInterruptWith,
  deferredIsDone,
  deferredPoll,
  deferredSucceed,
  deferredSync,
  deferredUnsafeDone,
  context,
  contextWithEffect,
  provideContext,
  provideSomeContext,
  mapInputContext,
  filterEffectOrElse,
  filterEffectOrFail,
  currentSpanFromFiber,
  noopSpan
};
//# sourceMappingURL=chunk-7GS5HKXM.js.map
