import {
  buildWithScope,
  context as context2,
  merge as merge3
} from "./chunk-3LT46UFL.js";
import {
  CurrentIterationMetadata,
  EffectTypeId,
  ScheduleDriverTypeId,
  ScheduleTypeId,
  _void,
  acquireRelease as acquireRelease2,
  acquireUseRelease,
  addDelay,
  addDelayEffect,
  all as all3,
  andThen,
  andThenEither,
  as as as2,
  as2 as as3,
  asSome,
  asVoid as asVoid2,
  asVoid2 as asVoid3,
  async,
  bothInOut,
  catchAll as catchAll2,
  catchAllCause,
  catchSomeCause,
  check,
  checkEffect,
  clock,
  collectAllInputs,
  collectAllOutputs,
  collectUntil,
  collectUntilEffect,
  collectWhile,
  collectWhileEffect,
  compose,
  context,
  contextWith,
  count2 as count,
  cron,
  dayOfMonth,
  dayOfWeek,
  delayed,
  delayedEffect,
  delayedSchedule,
  delays,
  die as die3,
  dieMessage,
  dieSync,
  driver,
  dropUntil,
  dropWhile as dropWhile2,
  duration,
  either,
  eitherWith,
  elapsed,
  ensuring as ensuring2,
  ensuring2 as ensuring3,
  exit,
  exponential,
  fail as fail4,
  failCause as failCause3,
  failCauseSync,
  failSync,
  fiberIdWith,
  fibonacci,
  filter as filter2,
  findFirst as findFirst2,
  fixed,
  flatMap as flatMap3,
  flatten as flatten2,
  forEach as forEach2,
  forever,
  forever2,
  forkDaemon,
  forkIn as forkIn2,
  forkScoped,
  fromDelay,
  fromDelays,
  fromFunction,
  gen,
  hourOfDay,
  identity as identity2,
  ignore,
  interrupt as interrupt5,
  interruptible as interruptible2,
  intersect,
  intersectWith,
  intoDeferred,
  isEffect,
  isSchedule,
  jittered,
  jitteredWith,
  linear,
  makeSemaphore,
  makeSpan,
  makeWithState,
  map as map6,
  map2 as map7,
  mapBoth as mapBoth2,
  mapBoth2 as mapBoth3,
  mapBothEffect,
  mapEffect,
  mapError,
  mapInput,
  mapInputContext,
  mapInputEffect,
  match2 as match4,
  matchCause,
  matchCauseEffect,
  matchEffect as matchEffect2,
  minuteOfHour,
  modifyDelay,
  modifyDelayEffect,
  never,
  onDecision,
  onError,
  once,
  orDie,
  orElse as orElse2,
  passthrough,
  promise,
  provide,
  provideContext,
  provideService as provideService2,
  provideServiceEffect,
  race,
  raceFirst,
  raceWith,
  recurUntil,
  recurUntilEffect,
  recurUntilOption,
  recurUpTo,
  recurWhile,
  recurWhileEffect,
  recurs,
  reduce as reduce2,
  reduce2 as reduce3,
  reduceEffect,
  repeat,
  repetitions,
  resetAfter,
  resetWhen,
  run,
  runFork,
  runPromise,
  runSync,
  runtime,
  scheduleDefectRefailCause,
  scheduleFromStep,
  scope,
  scopeWith,
  scoped,
  scopedWith,
  secondOfMinute,
  spaced,
  stop,
  succeed as succeed3,
  succeed2 as succeed5,
  succeedNone,
  suspend as suspend2,
  sync as sync2,
  sync2 as sync3,
  takeWhile as takeWhile2,
  tap as tap2,
  tapErrorCause,
  tapInput,
  tapOutput,
  timeoutFail,
  timeoutFailCause,
  tryPromise,
  unfold,
  uninterruptible as uninterruptible2,
  uninterruptibleMask as uninterruptibleMask2,
  union,
  unionWith,
  unsafeMakeLatch,
  untilInput,
  untilInputEffect,
  untilOutput,
  untilOutputEffect,
  upTo,
  when,
  whileInput,
  whileInputEffect,
  whileOutput,
  whileOutputEffect,
  windowed,
  withFiberRuntime as withFiberRuntime2,
  zip as zip2,
  zipLeft,
  zipLeft2,
  zipRight as zipRight3,
  zipRight2 as zipRight4,
  zipWith as zipWith2,
  zipWith2 as zipWith3
} from "./chunk-LXSJW2UJ.js";
import {
  Class,
  EmptyMutableQueue,
  FiberFailureCauseId,
  IllegalArgumentException,
  NoSuchElementException,
  RuntimeException,
  TypeId2 as TypeId,
  _await,
  _await2,
  acquireRelease,
  addFinalizer2 as addFinalizer,
  addFinalizerExit,
  addSpanStackTrace,
  all,
  all2,
  bind,
  bindTo as bindTo2,
  bounded,
  capacity,
  close,
  currentCache,
  currentCacheEnabled,
  currentLoggers,
  currentMinimumLogLevel,
  currentRuntimeFlags,
  currentScheduler,
  currentSupervisor,
  currentTimeMillis,
  defaultRuntime,
  defaultRuntimeFlags,
  deleteFiberRef,
  die,
  die2,
  disableRuntimeFlag,
  enableRuntimeFlag,
  endSpan,
  ensuring,
  extend,
  fail,
  fail3 as fail2,
  fail5 as fail3,
  failCause,
  failCause4 as failCause2,
  failureOrCause,
  fiberFailure,
  fiberRefMake,
  fiberRefMakeContext,
  fiberRefMakeRuntimeFlags,
  fiberRefMakeWith,
  fiberRefUnsafeMakeSupervisor,
  flipCauseOption,
  forEachConcurrentDiscard,
  forEachEffect,
  forEachParUnbounded,
  fork2 as fork,
  forkIn,
  get2 as get3,
  getAndSet2 as getAndSet,
  inheritAll,
  interrupt as interrupt2,
  interrupt2 as interrupt3,
  interrupt3 as interrupt4,
  interruptAs,
  interruptors,
  isAsyncFiberException,
  isDieType,
  isDone3 as isDone,
  isEmpty as isEmpty2,
  isExit,
  isFailure2 as isFailure,
  isFiberFailure,
  isInterrupted,
  isInterruptedOnly,
  isRuntimeException,
  isSuccess2 as isSuccess,
  join,
  length,
  make11 as make6,
  make12 as make7,
  make13 as make8,
  make4 as make5,
  map as map4,
  map4 as map5,
  mapBoth,
  match2 as match3,
  matchEffect,
  modify2 as modify,
  nextPow2,
  offer,
  offerAll,
  parallel,
  poll,
  poll2,
  poll3,
  pollUpTo,
  provideService2 as provideService,
  scopeMake,
  scopeTag,
  sequential,
  sequential2,
  set2 as set3,
  setFiberRef,
  sleep,
  sleep2,
  spanTag,
  squash,
  succeed as succeed2,
  succeed6 as succeed4,
  unbounded,
  unsafeDone,
  unsafeFork,
  unsafeMakeSemaphore,
  unsafeRunCallback,
  unsafeRunPromise,
  unsafeRunPromiseExit,
  unsafeRunSync,
  unsafeRunSyncExit,
  update2 as update,
  updateContext,
  updateFiberRefs2 as updateFiberRefs,
  updateRuntimeFlags,
  void_ as void_2,
  zip,
  zipRight as zipRight2
} from "./chunk-UYCMDTGT.js";
import {
  FiberRefTypeId,
  InvalidPubSubCapacityException,
  SingleShotGen,
  add as add2,
  as2 as as,
  asVoid,
  catchAll,
  combineAll,
  currentConcurrency,
  currentContext,
  currentInterruptedCause,
  currentLogAnnotations,
  currentLogLevel,
  currentLogSpan,
  currentMaxOpsBeforeYield,
  currentMetricLabels,
  currentRequestBatching,
  currentSchedulingPriority,
  currentTracerEnabled,
  currentTracerSpanAnnotations,
  currentTracerSpanLinks,
  currentTracerTimingEnabled,
  currentUnhandledErrorLogLevel,
  currentVersionMismatchErrorLogLevel,
  decode,
  deferredAwait,
  deferredInterruptWith,
  deferredMake,
  deferredSucceed,
  deferredUnsafeDone,
  deferredUnsafeMake,
  empty as empty3,
  empty2 as empty4,
  exitInterrupt,
  exitVoid,
  fiberId,
  fiberRefDelete,
  fiberRefGet,
  fiberRefGetAndSet,
  fiberRefGetAndUpdate,
  fiberRefGetAndUpdateSome,
  fiberRefGetWith,
  fiberRefLocally,
  fiberRefModify,
  fiberRefModifySome,
  fiberRefReset,
  fiberRefSet,
  fiberRefUnsafeMake,
  fiberRefUnsafeMakeContext,
  fiberRefUnsafeMakeHashSet,
  fiberRefUnsafeMakePatch,
  fiberRefUpdate,
  fiberRefUpdateAndGet,
  fiberRefUpdateSome,
  fiberRefUpdateSomeAndGet,
  flatMap2,
  forEach,
  get as get2,
  greaterThan,
  has,
  has2,
  interrupt2 as interrupt,
  interruptFiber,
  interruptible2 as interruptible,
  isFinite,
  make3 as make4,
  map2 as map3,
  millis,
  onInterrupt,
  partitionMap,
  remove,
  scopeClose,
  set,
  set2,
  size,
  succeed,
  suspend,
  sync,
  tap,
  toMillis,
  uninterruptible,
  uninterruptibleMask,
  unsafeGet as unsafeGet3,
  void_,
  whenEffect,
  withFiberRuntime,
  zero,
  zipRight
} from "./chunk-7GS5HKXM.js";
import {
  append2 as append,
  appendAll2 as appendAll,
  drop2 as drop,
  dropWhile,
  empty3 as empty,
  empty4 as empty2,
  every,
  filter3 as filter,
  filterMap2 as filterMap,
  filterMapWhile,
  findFirst2 as findFirst,
  flatMap2 as flatMap,
  flatten2 as flatten,
  fromIterable,
  fromIterable2,
  head2 as head,
  head3 as head2,
  isChunk,
  isEmpty,
  isNonEmpty,
  isNonEmptyArray2 as isNonEmptyArray,
  last2 as last,
  left,
  make,
  make3 as make2,
  map5 as map2,
  mapAccum,
  match,
  merge,
  of2 as of,
  prepend2 as prepend,
  range,
  reduce3 as reduce,
  reduceRight2 as reduceRight,
  right,
  span,
  splitAt,
  splitAt2,
  splitWhere,
  take,
  takeRight,
  takeWhile,
  toReadonlyArray,
  unsafeFromArray,
  unsafeGet2 as unsafeGet,
  unsafeHead2 as unsafeHead,
  unsafeLast,
  zipWith
} from "./chunk-JDLTRRA5.js";
import {
  add,
  get,
  isContext,
  make as make3,
  merge as merge2,
  unsafeGet as unsafeGet2
} from "./chunk-RG46YT3R.js";
import {
  bindTo,
  fromNullable,
  getOrElse,
  getOrThrow,
  isNone,
  isSome,
  let_,
  map,
  match as match2,
  none,
  orElse,
  some
} from "./chunk-5XJTUU64.js";
import {
  OP_COMMIT,
  StreamTypeId,
  YieldWrap,
  cached,
  combine,
  effectVariance,
  equals,
  hasProperty,
  hash,
  internalCall,
  isIterable,
  pipeArguments,
  random,
  symbol,
  symbol2,
  yieldWrapGet
} from "./chunk-SPGJQP32.js";
import {
  constFalse,
  constTrue,
  constUndefined,
  constVoid,
  dual,
  identity,
  pipe
} from "./chunk-LX4DLJUA.js";
import {
  __export
} from "./chunk-G3PMV62Z.js";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Stream.js
var Stream_exports = {};
__export(Stream_exports, {
  DefaultChunkSize: () => DefaultChunkSize2,
  Do: () => Do2,
  StreamTypeId: () => StreamTypeId3,
  accumulate: () => accumulate2,
  accumulateChunks: () => accumulateChunks2,
  acquireRelease: () => acquireRelease4,
  aggregate: () => aggregate2,
  aggregateWithin: () => aggregateWithin2,
  aggregateWithinEither: () => aggregateWithinEither2,
  as: () => as9,
  async: () => _async2,
  asyncEffect: () => asyncEffect2,
  asyncPush: () => asyncPush2,
  asyncScoped: () => asyncScoped2,
  bind: () => bind4,
  bindEffect: () => bindEffect2,
  bindTo: () => bindTo5,
  branchAfter: () => branchAfter2,
  broadcast: () => broadcast2,
  broadcastDynamic: () => broadcastDynamic2,
  broadcastedQueues: () => broadcastedQueues2,
  broadcastedQueuesDynamic: () => broadcastedQueuesDynamic2,
  buffer: () => buffer3,
  bufferChunks: () => bufferChunks2,
  catchAll: () => catchAll6,
  catchAllCause: () => catchAllCause4,
  catchSome: () => catchSome3,
  catchSomeCause: () => catchSomeCause3,
  catchTag: () => catchTag3,
  catchTags: () => catchTags3,
  changes: () => changes2,
  changesWith: () => changesWith2,
  changesWithEffect: () => changesWithEffect2,
  chunks: () => chunks2,
  chunksWith: () => chunksWith2,
  combine: () => combine3,
  combineChunks: () => combineChunks2,
  concat: () => concat2,
  concatAll: () => concatAll3,
  context: () => context7,
  contextWith: () => contextWith6,
  contextWithEffect: () => contextWithEffect4,
  contextWithStream: () => contextWithStream2,
  cross: () => cross2,
  crossLeft: () => crossLeft2,
  crossRight: () => crossRight2,
  crossWith: () => crossWith2,
  debounce: () => debounce2,
  decodeText: () => decodeText2,
  die: () => die9,
  dieMessage: () => dieMessage6,
  dieSync: () => dieSync5,
  distributedWith: () => distributedWith2,
  distributedWithDynamic: () => distributedWithDynamic2,
  drain: () => drain4,
  drainFork: () => drainFork2,
  drop: () => drop4,
  dropRight: () => dropRight2,
  dropUntil: () => dropUntil4,
  dropUntilEffect: () => dropUntilEffect3,
  dropWhile: () => dropWhile5,
  dropWhileEffect: () => dropWhileEffect3,
  either: () => either5,
  empty: () => empty6,
  encodeText: () => encodeText2,
  ensuring: () => ensuring9,
  ensuringWith: () => ensuringWith4,
  execute: () => execute2,
  fail: () => fail11,
  failCause: () => failCause9,
  failCauseSync: () => failCauseSync5,
  failSync: () => failSync6,
  filter: () => filter6,
  filterEffect: () => filterEffect2,
  filterMap: () => filterMap3,
  filterMapEffect: () => filterMapEffect2,
  filterMapWhile: () => filterMapWhile3,
  filterMapWhileEffect: () => filterMapWhileEffect2,
  finalizer: () => finalizer2,
  find: () => find2,
  findEffect: () => findEffect3,
  flatMap: () => flatMap8,
  flatten: () => flatten6,
  flattenChunks: () => flattenChunks2,
  flattenEffect: () => flattenEffect2,
  flattenExitOption: () => flattenExitOption2,
  flattenIterables: () => flattenIterables2,
  flattenTake: () => flattenTake2,
  forever: () => forever5,
  fromAsyncIterable: () => fromAsyncIterable2,
  fromChannel: () => fromChannel3,
  fromChunk: () => fromChunk2,
  fromChunkPubSub: () => fromChunkPubSub2,
  fromChunkQueue: () => fromChunkQueue2,
  fromChunks: () => fromChunks2,
  fromEffect: () => fromEffect6,
  fromEffectOption: () => fromEffectOption2,
  fromEventListener: () => fromEventListener2,
  fromIterable: () => fromIterable4,
  fromIterableEffect: () => fromIterableEffect2,
  fromIteratorSucceed: () => fromIteratorSucceed2,
  fromPubSub: () => fromPubSub4,
  fromPull: () => fromPull3,
  fromQueue: () => fromQueue4,
  fromReadableStream: () => fromReadableStream2,
  fromReadableStreamByob: () => fromReadableStreamByob2,
  fromSchedule: () => fromSchedule2,
  fromTPubSub: () => fromTPubSub2,
  fromTQueue: () => fromTQueue2,
  groupAdjacentBy: () => groupAdjacentBy2,
  groupBy: () => groupBy2,
  groupByKey: () => groupByKey2,
  grouped: () => grouped2,
  groupedWithin: () => groupedWithin2,
  haltAfter: () => haltAfter2,
  haltWhen: () => haltWhen2,
  haltWhenDeferred: () => haltWhenDeferred2,
  identity: () => identity4,
  interleave: () => interleave2,
  interleaveWith: () => interleaveWith2,
  interruptAfter: () => interruptAfter2,
  interruptWhen: () => interruptWhen3,
  interruptWhenDeferred: () => interruptWhenDeferred3,
  intersperse: () => intersperse2,
  intersperseAffixes: () => intersperseAffixes2,
  iterate: () => iterate3,
  let: () => let_4,
  make: () => make24,
  map: () => map14,
  mapAccum: () => mapAccum3,
  mapAccumEffect: () => mapAccumEffect2,
  mapBoth: () => mapBoth7,
  mapChunks: () => mapChunks2,
  mapChunksEffect: () => mapChunksEffect2,
  mapConcat: () => mapConcat2,
  mapConcatChunk: () => mapConcatChunk2,
  mapConcatChunkEffect: () => mapConcatChunkEffect2,
  mapConcatEffect: () => mapConcatEffect2,
  mapEffect: () => mapEffect5,
  mapError: () => mapError6,
  mapErrorCause: () => mapErrorCause3,
  mapInputContext: () => mapInputContext6,
  merge: () => merge6,
  mergeAll: () => mergeAll4,
  mergeEither: () => mergeEither2,
  mergeLeft: () => mergeLeft2,
  mergeRight: () => mergeRight2,
  mergeWith: () => mergeWith3,
  mergeWithTag: () => mergeWithTag2,
  mkString: () => mkString3,
  never: () => never5,
  onDone: () => onDone2,
  onEnd: () => onEnd2,
  onError: () => onError3,
  onStart: () => onStart2,
  orDie: () => orDie5,
  orDieWith: () => orDieWith4,
  orElse: () => orElse7,
  orElseEither: () => orElseEither3,
  orElseFail: () => orElseFail3,
  orElseIfEmpty: () => orElseIfEmpty2,
  orElseIfEmptyChunk: () => orElseIfEmptyChunk2,
  orElseIfEmptyStream: () => orElseIfEmptyStream2,
  orElseSucceed: () => orElseSucceed3,
  paginate: () => paginate2,
  paginateChunk: () => paginateChunk2,
  paginateChunkEffect: () => paginateChunkEffect2,
  paginateEffect: () => paginateEffect2,
  partition: () => partition3,
  partitionEither: () => partitionEither2,
  peel: () => peel2,
  pipeThrough: () => pipeThrough2,
  pipeThroughChannel: () => pipeThroughChannel2,
  pipeThroughChannelOrFail: () => pipeThroughChannelOrFail2,
  prepend: () => prepend3,
  provideContext: () => provideContext7,
  provideLayer: () => provideLayer3,
  provideService: () => provideService8,
  provideServiceEffect: () => provideServiceEffect3,
  provideServiceStream: () => provideServiceStream2,
  provideSomeContext: () => provideSomeContext3,
  provideSomeLayer: () => provideSomeLayer3,
  race: () => race4,
  raceAll: () => raceAll2,
  range: () => range3,
  rechunk: () => rechunk2,
  refineOrDie: () => refineOrDie4,
  refineOrDieWith: () => refineOrDieWith4,
  repeat: () => repeat3,
  repeatEffect: () => repeatEffect2,
  repeatEffectChunk: () => repeatEffectChunk2,
  repeatEffectChunkOption: () => repeatEffectChunkOption2,
  repeatEffectOption: () => repeatEffectOption2,
  repeatEffectWithSchedule: () => repeatEffectWithSchedule2,
  repeatEither: () => repeatEither2,
  repeatElements: () => repeatElements2,
  repeatElementsWith: () => repeatElementsWith2,
  repeatValue: () => repeatValue2,
  repeatWith: () => repeatWith2,
  retry: () => retry4,
  run: () => run5,
  runCollect: () => runCollect3,
  runCount: () => runCount2,
  runDrain: () => runDrain3,
  runFold: () => runFold2,
  runFoldEffect: () => runFoldEffect2,
  runFoldScoped: () => runFoldScoped2,
  runFoldScopedEffect: () => runFoldScopedEffect2,
  runFoldWhile: () => runFoldWhile2,
  runFoldWhileEffect: () => runFoldWhileEffect2,
  runFoldWhileScoped: () => runFoldWhileScoped2,
  runFoldWhileScopedEffect: () => runFoldWhileScopedEffect2,
  runForEach: () => runForEach2,
  runForEachChunk: () => runForEachChunk2,
  runForEachChunkScoped: () => runForEachChunkScoped2,
  runForEachScoped: () => runForEachScoped2,
  runForEachWhile: () => runForEachWhile2,
  runForEachWhileScoped: () => runForEachWhileScoped2,
  runHead: () => runHead2,
  runIntoPubSub: () => runIntoPubSub2,
  runIntoPubSubScoped: () => runIntoPubSubScoped2,
  runIntoQueue: () => runIntoQueue2,
  runIntoQueueElementsScoped: () => runIntoQueueElementsScoped2,
  runIntoQueueScoped: () => runIntoQueueScoped2,
  runLast: () => runLast2,
  runScoped: () => runScoped3,
  runSum: () => runSum2,
  scan: () => scan2,
  scanEffect: () => scanEffect2,
  scanReduce: () => scanReduce2,
  scanReduceEffect: () => scanReduceEffect2,
  schedule: () => schedule2,
  scheduleWith: () => scheduleWith2,
  scoped: () => scoped4,
  scopedWith: () => scopedWith4,
  share: () => share2,
  sliding: () => sliding10,
  slidingSize: () => slidingSize2,
  some: () => some5,
  someOrElse: () => someOrElse2,
  someOrFail: () => someOrFail2,
  split: () => split2,
  splitLines: () => splitLines3,
  splitOnChunk: () => splitOnChunk2,
  succeed: () => succeed12,
  suspend: () => suspend8,
  sync: () => sync9,
  take: () => take9,
  takeRight: () => takeRight3,
  takeUntil: () => takeUntil2,
  takeUntilEffect: () => takeUntilEffect2,
  takeWhile: () => takeWhile4,
  tap: () => tap6,
  tapBoth: () => tapBoth3,
  tapError: () => tapError3,
  tapErrorCause: () => tapErrorCause3,
  tapSink: () => tapSink2,
  throttle: () => throttle2,
  throttleEffect: () => throttleEffect2,
  tick: () => tick2,
  timeout: () => timeout2,
  timeoutFail: () => timeoutFail3,
  timeoutFailCause: () => timeoutFailCause3,
  timeoutTo: () => timeoutTo2,
  toAsyncIterable: () => toAsyncIterable2,
  toAsyncIterableEffect: () => toAsyncIterableEffect2,
  toAsyncIterableRuntime: () => toAsyncIterableRuntime2,
  toChannel: () => toChannel3,
  toPubSub: () => toPubSub3,
  toPull: () => toPull3,
  toQueue: () => toQueue3,
  toQueueOfElements: () => toQueueOfElements2,
  toReadableStream: () => toReadableStream2,
  toReadableStreamEffect: () => toReadableStreamEffect2,
  toReadableStreamRuntime: () => toReadableStreamRuntime2,
  transduce: () => transduce2,
  unfold: () => unfold4,
  unfoldChunk: () => unfoldChunk2,
  unfoldChunkEffect: () => unfoldChunkEffect2,
  unfoldEffect: () => unfoldEffect2,
  unwrap: () => unwrap4,
  unwrapScoped: () => unwrapScoped4,
  unwrapScopedWith: () => unwrapScopedWith4,
  updateService: () => updateService3,
  void: () => void_7,
  when: () => when4,
  whenCase: () => whenCase2,
  whenCaseEffect: () => whenCaseEffect2,
  whenEffect: () => whenEffect3,
  withExecutionPlan: () => withExecutionPlan2,
  withSpan: () => withSpan3,
  zip: () => zip7,
  zipAll: () => zipAll2,
  zipAllLeft: () => zipAllLeft2,
  zipAllRight: () => zipAllRight2,
  zipAllSortedByKey: () => zipAllSortedByKey2,
  zipAllSortedByKeyLeft: () => zipAllSortedByKeyLeft2,
  zipAllSortedByKeyRight: () => zipAllSortedByKeyRight2,
  zipAllSortedByKeyWith: () => zipAllSortedByKeyWith2,
  zipAllWith: () => zipAllWith2,
  zipFlatten: () => zipFlatten2,
  zipLatest: () => zipLatest2,
  zipLatestAll: () => zipLatestAll2,
  zipLatestWith: () => zipLatestWith2,
  zipLeft: () => zipLeft8,
  zipRight: () => zipRight10,
  zipWith: () => zipWith8,
  zipWithChunks: () => zipWithChunks2,
  zipWithIndex: () => zipWithIndex2,
  zipWithNext: () => zipWithNext2,
  zipWithPrevious: () => zipWithPrevious2,
  zipWithPreviousAndNext: () => zipWithPreviousAndNext2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Queue.js
var Queue_exports = {};
__export(Queue_exports, {
  BackingQueueTypeId: () => BackingQueueTypeId2,
  DequeueTypeId: () => DequeueTypeId2,
  EnqueueTypeId: () => EnqueueTypeId2,
  QueueStrategyTypeId: () => QueueStrategyTypeId2,
  awaitShutdown: () => awaitShutdown2,
  backPressureStrategy: () => backPressureStrategy2,
  bounded: () => bounded3,
  capacity: () => capacity3,
  dropping: () => dropping2,
  droppingStrategy: () => droppingStrategy2,
  isDequeue: () => isDequeue2,
  isEmpty: () => isEmpty4,
  isEnqueue: () => isEnqueue2,
  isFull: () => isFull2,
  isQueue: () => isQueue2,
  isShutdown: () => isShutdown2,
  make: () => make10,
  offer: () => offer3,
  offerAll: () => offerAll3,
  poll: () => poll5,
  shutdown: () => shutdown2,
  size: () => size3,
  sliding: () => sliding2,
  slidingStrategy: () => slidingStrategy2,
  take: () => take3,
  takeAll: () => takeAll2,
  takeBetween: () => takeBetween2,
  takeN: () => takeN2,
  takeUpTo: () => takeUpTo2,
  unbounded: () => unbounded3,
  unsafeOffer: () => unsafeOffer2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/queue.js
var EnqueueSymbolKey = "effect/QueueEnqueue";
var EnqueueTypeId = Symbol.for(EnqueueSymbolKey);
var DequeueSymbolKey = "effect/QueueDequeue";
var DequeueTypeId = Symbol.for(DequeueSymbolKey);
var QueueStrategySymbolKey = "effect/QueueStrategy";
var QueueStrategyTypeId = Symbol.for(QueueStrategySymbolKey);
var BackingQueueSymbolKey = "effect/BackingQueue";
var BackingQueueTypeId = Symbol.for(BackingQueueSymbolKey);
var queueStrategyVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var backingQueueVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var enqueueVariance = {
  /* c8 ignore next */
  _In: (_) => _
};
var dequeueVariance = {
  /* c8 ignore next */
  _Out: (_) => _
};
var QueueImpl = class extends Class {
  queue;
  takers;
  shutdownHook;
  shutdownFlag;
  strategy;
  [EnqueueTypeId] = enqueueVariance;
  [DequeueTypeId] = dequeueVariance;
  constructor(queue, takers, shutdownHook, shutdownFlag, strategy) {
    super();
    this.queue = queue;
    this.takers = takers;
    this.shutdownHook = shutdownHook;
    this.shutdownFlag = shutdownFlag;
    this.strategy = strategy;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  commit() {
    return this.take;
  }
  capacity() {
    return this.queue.capacity();
  }
  get size() {
    return suspend(() => catchAll(this.unsafeSize(), () => interrupt));
  }
  unsafeSize() {
    if (get2(this.shutdownFlag)) {
      return none();
    }
    return some(this.queue.length() - length(this.takers) + this.strategy.surplusSize());
  }
  get isEmpty() {
    return map3(this.size, (size10) => size10 <= 0);
  }
  get isFull() {
    return map3(this.size, (size10) => size10 >= this.capacity());
  }
  get shutdown() {
    return uninterruptible(withFiberRuntime((state) => {
      pipe(this.shutdownFlag, set(true));
      return pipe(forEachConcurrentDiscard(unsafePollAll(this.takers), (d) => deferredInterruptWith(d, state.id()), false, false), zipRight(this.strategy.shutdown), whenEffect(deferredSucceed(this.shutdownHook, void 0)), asVoid);
    }));
  }
  get isShutdown() {
    return sync(() => get2(this.shutdownFlag));
  }
  get awaitShutdown() {
    return deferredAwait(this.shutdownHook);
  }
  isActive() {
    return !get2(this.shutdownFlag);
  }
  unsafeOffer(value) {
    if (get2(this.shutdownFlag)) {
      return false;
    }
    let noRemaining;
    if (this.queue.length() === 0) {
      const taker = pipe(this.takers, poll3(EmptyMutableQueue));
      if (taker !== EmptyMutableQueue) {
        unsafeCompleteDeferred(taker, value);
        noRemaining = true;
      } else {
        noRemaining = false;
      }
    } else {
      noRemaining = false;
    }
    if (noRemaining) {
      return true;
    }
    const succeeded = this.queue.offer(value);
    unsafeCompleteTakers(this.strategy, this.queue, this.takers);
    return succeeded;
  }
  offer(value) {
    return suspend(() => {
      if (get2(this.shutdownFlag)) {
        return interrupt;
      }
      let noRemaining;
      if (this.queue.length() === 0) {
        const taker = pipe(this.takers, poll3(EmptyMutableQueue));
        if (taker !== EmptyMutableQueue) {
          unsafeCompleteDeferred(taker, value);
          noRemaining = true;
        } else {
          noRemaining = false;
        }
      } else {
        noRemaining = false;
      }
      if (noRemaining) {
        return succeed(true);
      }
      const succeeded = this.queue.offer(value);
      unsafeCompleteTakers(this.strategy, this.queue, this.takers);
      return succeeded ? succeed(true) : this.strategy.handleSurplus([value], this.queue, this.takers, this.shutdownFlag);
    });
  }
  offerAll(iterable) {
    return suspend(() => {
      if (get2(this.shutdownFlag)) {
        return interrupt;
      }
      const values = fromIterable(iterable);
      const pTakers = this.queue.length() === 0 ? fromIterable(unsafePollN(this.takers, values.length)) : empty;
      const [forTakers, remaining] = pipe(values, splitAt(pTakers.length));
      for (let i = 0; i < pTakers.length; i++) {
        const taker = pTakers[i];
        const item = forTakers[i];
        unsafeCompleteDeferred(taker, item);
      }
      if (remaining.length === 0) {
        return succeed(true);
      }
      const surplus = this.queue.offerAll(remaining);
      unsafeCompleteTakers(this.strategy, this.queue, this.takers);
      return isEmpty(surplus) ? succeed(true) : this.strategy.handleSurplus(surplus, this.queue, this.takers, this.shutdownFlag);
    });
  }
  get take() {
    return withFiberRuntime((state) => {
      if (get2(this.shutdownFlag)) {
        return interrupt;
      }
      const item = this.queue.poll(EmptyMutableQueue);
      if (item !== EmptyMutableQueue) {
        this.strategy.unsafeOnQueueEmptySpace(this.queue, this.takers);
        return succeed(item);
      } else {
        const deferred = deferredUnsafeMake(state.id());
        return pipe(suspend(() => {
          pipe(this.takers, offer(deferred));
          unsafeCompleteTakers(this.strategy, this.queue, this.takers);
          return get2(this.shutdownFlag) ? interrupt : deferredAwait(deferred);
        }), onInterrupt(() => {
          return sync(() => unsafeRemove(this.takers, deferred));
        }));
      }
    });
  }
  get takeAll() {
    return suspend(() => {
      return get2(this.shutdownFlag) ? interrupt : sync(() => {
        const values = this.queue.pollUpTo(Number.POSITIVE_INFINITY);
        this.strategy.unsafeOnQueueEmptySpace(this.queue, this.takers);
        return fromIterable2(values);
      });
    });
  }
  takeUpTo(max) {
    return suspend(() => get2(this.shutdownFlag) ? interrupt : sync(() => {
      const values = this.queue.pollUpTo(max);
      this.strategy.unsafeOnQueueEmptySpace(this.queue, this.takers);
      return fromIterable2(values);
    }));
  }
  takeBetween(min, max) {
    return suspend(() => takeRemainderLoop(this, min, max, empty2()));
  }
};
var takeRemainderLoop = (self, min, max, acc) => {
  if (max < min) {
    return succeed(acc);
  }
  return pipe(takeUpTo(self, max), flatMap2((bs) => {
    const remaining = min - bs.length;
    if (remaining === 1) {
      return pipe(take2(self), map3((b) => pipe(acc, appendAll(bs), append(b))));
    }
    if (remaining > 1) {
      return pipe(take2(self), flatMap2((b) => takeRemainderLoop(self, remaining - 1, max - bs.length - 1, pipe(acc, appendAll(bs), append(b)))));
    }
    return succeed(pipe(acc, appendAll(bs)));
  }));
};
var isQueue = (u) => isEnqueue(u) && isDequeue(u);
var isEnqueue = (u) => hasProperty(u, EnqueueTypeId);
var isDequeue = (u) => hasProperty(u, DequeueTypeId);
var bounded2 = (requestedCapacity) => pipe(sync(() => bounded(requestedCapacity)), flatMap2((queue) => make9(backingQueueFromMutableQueue(queue), backPressureStrategy())));
var dropping = (requestedCapacity) => pipe(sync(() => bounded(requestedCapacity)), flatMap2((queue) => make9(backingQueueFromMutableQueue(queue), droppingStrategy())));
var sliding = (requestedCapacity) => pipe(sync(() => bounded(requestedCapacity)), flatMap2((queue) => make9(backingQueueFromMutableQueue(queue), slidingStrategy())));
var unbounded2 = () => pipe(sync(() => unbounded()), flatMap2((queue) => make9(backingQueueFromMutableQueue(queue), droppingStrategy())));
var unsafeMake = (queue, takers, shutdownHook, shutdownFlag, strategy) => {
  return new QueueImpl(queue, takers, shutdownHook, shutdownFlag, strategy);
};
var make9 = (queue, strategy) => pipe(deferredMake(), map3((deferred) => unsafeMake(queue, unbounded(), deferred, make4(false), strategy)));
var BackingQueueFromMutableQueue = class {
  mutable;
  [BackingQueueTypeId] = backingQueueVariance;
  constructor(mutable) {
    this.mutable = mutable;
  }
  poll(def) {
    return poll3(this.mutable, def);
  }
  pollUpTo(limit) {
    return pollUpTo(this.mutable, limit);
  }
  offerAll(elements) {
    return offerAll(this.mutable, elements);
  }
  offer(element) {
    return offer(this.mutable, element);
  }
  capacity() {
    return capacity(this.mutable);
  }
  length() {
    return length(this.mutable);
  }
};
var backingQueueFromMutableQueue = (mutable) => new BackingQueueFromMutableQueue(mutable);
var capacity2 = (self) => self.capacity();
var size2 = (self) => self.size;
var isFull = (self) => self.isFull;
var isEmpty3 = (self) => self.isEmpty;
var isShutdown = (self) => self.isShutdown;
var awaitShutdown = (self) => self.awaitShutdown;
var shutdown = (self) => self.shutdown;
var offer2 = dual(2, (self, value) => self.offer(value));
var unsafeOffer = dual(2, (self, value) => self.unsafeOffer(value));
var offerAll2 = dual(2, (self, iterable) => self.offerAll(iterable));
var poll4 = (self) => map3(self.takeUpTo(1), head2);
var take2 = (self) => self.take;
var takeAll = (self) => self.takeAll;
var takeUpTo = dual(2, (self, max) => self.takeUpTo(max));
var takeBetween = dual(3, (self, min, max) => self.takeBetween(min, max));
var takeN = dual(2, (self, n) => self.takeBetween(n, n));
var backPressureStrategy = () => new BackPressureStrategy();
var droppingStrategy = () => new DroppingStrategy();
var slidingStrategy = () => new SlidingStrategy();
var BackPressureStrategy = class {
  [QueueStrategyTypeId] = queueStrategyVariance;
  putters = unbounded();
  surplusSize() {
    return length(this.putters);
  }
  onCompleteTakersWithEmptyQueue(takers) {
    while (!isEmpty2(this.putters) && !isEmpty2(takers)) {
      const taker = poll3(takers, void 0);
      const putter = poll3(this.putters, void 0);
      if (putter[2]) {
        unsafeCompleteDeferred(putter[1], true);
      }
      unsafeCompleteDeferred(taker, putter[0]);
    }
  }
  get shutdown() {
    return pipe(fiberId, flatMap2((fiberId3) => pipe(sync(() => unsafePollAll(this.putters)), flatMap2((putters) => forEachConcurrentDiscard(putters, ([_, deferred, isLastItem]) => isLastItem ? pipe(deferredInterruptWith(deferred, fiberId3), asVoid) : void_, false, false)))));
  }
  handleSurplus(iterable, queue, takers, isShutdown9) {
    return withFiberRuntime((state) => {
      const deferred = deferredUnsafeMake(state.id());
      return pipe(suspend(() => {
        this.unsafeOffer(iterable, deferred);
        this.unsafeOnQueueEmptySpace(queue, takers);
        unsafeCompleteTakers(this, queue, takers);
        return get2(isShutdown9) ? interrupt : deferredAwait(deferred);
      }), onInterrupt(() => sync(() => this.unsafeRemove(deferred))));
    });
  }
  unsafeOnQueueEmptySpace(queue, takers) {
    let keepPolling = true;
    while (keepPolling && (queue.capacity() === Number.POSITIVE_INFINITY || queue.length() < queue.capacity())) {
      const putter = pipe(this.putters, poll3(EmptyMutableQueue));
      if (putter === EmptyMutableQueue) {
        keepPolling = false;
      } else {
        const offered = queue.offer(putter[0]);
        if (offered && putter[2]) {
          unsafeCompleteDeferred(putter[1], true);
        } else if (!offered) {
          unsafeOfferAll(this.putters, pipe(unsafePollAll(this.putters), prepend(putter)));
        }
        unsafeCompleteTakers(this, queue, takers);
      }
    }
  }
  unsafeOffer(iterable, deferred) {
    const stuff = fromIterable(iterable);
    for (let i = 0; i < stuff.length; i++) {
      const value = stuff[i];
      if (i === stuff.length - 1) {
        pipe(this.putters, offer([value, deferred, true]));
      } else {
        pipe(this.putters, offer([value, deferred, false]));
      }
    }
  }
  unsafeRemove(deferred) {
    unsafeOfferAll(this.putters, pipe(unsafePollAll(this.putters), filter(([, _]) => _ !== deferred)));
  }
};
var DroppingStrategy = class {
  [QueueStrategyTypeId] = queueStrategyVariance;
  surplusSize() {
    return 0;
  }
  get shutdown() {
    return void_;
  }
  onCompleteTakersWithEmptyQueue() {
  }
  handleSurplus(_iterable, _queue, _takers, _isShutdown) {
    return succeed(false);
  }
  unsafeOnQueueEmptySpace(_queue, _takers) {
  }
};
var SlidingStrategy = class {
  [QueueStrategyTypeId] = queueStrategyVariance;
  surplusSize() {
    return 0;
  }
  get shutdown() {
    return void_;
  }
  onCompleteTakersWithEmptyQueue() {
  }
  handleSurplus(iterable, queue, takers, _isShutdown) {
    return sync(() => {
      this.unsafeOffer(queue, iterable);
      unsafeCompleteTakers(this, queue, takers);
      return true;
    });
  }
  unsafeOnQueueEmptySpace(_queue, _takers) {
  }
  unsafeOffer(queue, iterable) {
    const iterator = iterable[Symbol.iterator]();
    let next;
    let offering = true;
    while (!(next = iterator.next()).done && offering) {
      if (queue.capacity() === 0) {
        return;
      }
      queue.poll(EmptyMutableQueue);
      offering = queue.offer(next.value);
    }
  }
};
var unsafeCompleteDeferred = (deferred, a) => {
  return deferredUnsafeDone(deferred, succeed(a));
};
var unsafeOfferAll = (queue, as10) => {
  return pipe(queue, offerAll(as10));
};
var unsafePollAll = (queue) => {
  return pipe(queue, pollUpTo(Number.POSITIVE_INFINITY));
};
var unsafePollN = (queue, max) => {
  return pipe(queue, pollUpTo(max));
};
var unsafeRemove = (queue, a) => {
  unsafeOfferAll(queue, pipe(unsafePollAll(queue), filter((b) => a !== b)));
};
var unsafeCompleteTakers = (strategy, queue, takers) => {
  let keepPolling = true;
  while (keepPolling && queue.length() !== 0) {
    const taker = pipe(takers, poll3(EmptyMutableQueue));
    if (taker !== EmptyMutableQueue) {
      const element = queue.poll(EmptyMutableQueue);
      if (element !== EmptyMutableQueue) {
        unsafeCompleteDeferred(taker, element);
        strategy.unsafeOnQueueEmptySpace(queue, takers);
      } else {
        unsafeOfferAll(takers, pipe(unsafePollAll(takers), prepend(taker)));
      }
      keepPolling = true;
    } else {
      keepPolling = false;
    }
  }
  if (keepPolling && queue.length() === 0 && !isEmpty2(takers)) {
    strategy.onCompleteTakersWithEmptyQueue(takers);
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Queue.js
var EnqueueTypeId2 = EnqueueTypeId;
var DequeueTypeId2 = DequeueTypeId;
var QueueStrategyTypeId2 = QueueStrategyTypeId;
var BackingQueueTypeId2 = BackingQueueTypeId;
var isQueue2 = isQueue;
var isDequeue2 = isDequeue;
var isEnqueue2 = isEnqueue;
var backPressureStrategy2 = backPressureStrategy;
var droppingStrategy2 = droppingStrategy;
var slidingStrategy2 = slidingStrategy;
var make10 = make9;
var bounded3 = bounded2;
var dropping2 = dropping;
var sliding2 = sliding;
var unbounded3 = unbounded2;
var capacity3 = capacity2;
var size3 = size2;
var isEmpty4 = isEmpty3;
var isFull2 = isFull;
var isShutdown2 = isShutdown;
var awaitShutdown2 = awaitShutdown;
var shutdown2 = shutdown;
var offer3 = offer2;
var unsafeOffer2 = unsafeOffer;
var offerAll3 = offerAll2;
var poll5 = poll4;
var take3 = take2;
var takeAll2 = takeAll;
var takeUpTo2 = takeUpTo;
var takeBetween2 = takeBetween;
var takeN2 = takeN;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/FiberRef.js
var FiberRef_exports = {};
__export(FiberRef_exports, {
  FiberRefTypeId: () => FiberRefTypeId2,
  currentConcurrency: () => currentConcurrency2,
  currentContext: () => currentContext2,
  currentLogAnnotations: () => currentLogAnnotations2,
  currentLogLevel: () => currentLogLevel2,
  currentLogSpan: () => currentLogSpan2,
  currentLoggers: () => currentLoggers2,
  currentMaxOpsBeforeYield: () => currentMaxOpsBeforeYield2,
  currentMetricLabels: () => currentMetricLabels2,
  currentMinimumLogLevel: () => currentMinimumLogLevel2,
  currentRequestBatchingEnabled: () => currentRequestBatchingEnabled,
  currentRequestCache: () => currentRequestCache,
  currentRequestCacheEnabled: () => currentRequestCacheEnabled,
  currentRuntimeFlags: () => currentRuntimeFlags2,
  currentScheduler: () => currentScheduler2,
  currentSchedulingPriority: () => currentSchedulingPriority2,
  currentSupervisor: () => currentSupervisor2,
  currentTracerEnabled: () => currentTracerEnabled2,
  currentTracerSpanAnnotations: () => currentTracerSpanAnnotations2,
  currentTracerSpanLinks: () => currentTracerSpanLinks2,
  currentTracerTimingEnabled: () => currentTracerTimingEnabled2,
  delete: () => _delete,
  get: () => get4,
  getAndSet: () => getAndSet2,
  getAndUpdate: () => getAndUpdate,
  getAndUpdateSome: () => getAndUpdateSome,
  getWith: () => getWith,
  interruptedCause: () => interruptedCause,
  make: () => make11,
  makeContext: () => makeContext,
  makeRuntimeFlags: () => makeRuntimeFlags,
  makeWith: () => makeWith,
  modify: () => modify2,
  modifySome: () => modifySome,
  reset: () => reset,
  set: () => set4,
  unhandledErrorLogLevel: () => unhandledErrorLogLevel,
  unsafeMake: () => unsafeMake2,
  unsafeMakeContext: () => unsafeMakeContext,
  unsafeMakeHashSet: () => unsafeMakeHashSet,
  unsafeMakePatch: () => unsafeMakePatch,
  unsafeMakeSupervisor: () => unsafeMakeSupervisor,
  update: () => update2,
  updateAndGet: () => updateAndGet,
  updateSome: () => updateSome,
  updateSomeAndGet: () => updateSomeAndGet,
  versionMismatchErrorLogLevel: () => versionMismatchErrorLogLevel
});
var FiberRefTypeId2 = FiberRefTypeId;
var make11 = fiberRefMake;
var makeWith = fiberRefMakeWith;
var makeContext = fiberRefMakeContext;
var makeRuntimeFlags = fiberRefMakeRuntimeFlags;
var unsafeMake2 = fiberRefUnsafeMake;
var unsafeMakeHashSet = fiberRefUnsafeMakeHashSet;
var unsafeMakeContext = fiberRefUnsafeMakeContext;
var unsafeMakeSupervisor = fiberRefUnsafeMakeSupervisor;
var unsafeMakePatch = fiberRefUnsafeMakePatch;
var get4 = fiberRefGet;
var getAndSet2 = fiberRefGetAndSet;
var getAndUpdate = fiberRefGetAndUpdate;
var getAndUpdateSome = fiberRefGetAndUpdateSome;
var getWith = fiberRefGetWith;
var set4 = fiberRefSet;
var _delete = fiberRefDelete;
var reset = fiberRefReset;
var modify2 = fiberRefModify;
var modifySome = fiberRefModifySome;
var update2 = fiberRefUpdate;
var updateSome = fiberRefUpdateSome;
var updateAndGet = fiberRefUpdateAndGet;
var updateSomeAndGet = fiberRefUpdateSomeAndGet;
var currentConcurrency2 = currentConcurrency;
var currentRequestBatchingEnabled = currentRequestBatching;
var currentRequestCache = currentCache;
var currentRequestCacheEnabled = currentCacheEnabled;
var currentContext2 = currentContext;
var currentSchedulingPriority2 = currentSchedulingPriority;
var currentMaxOpsBeforeYield2 = currentMaxOpsBeforeYield;
var unhandledErrorLogLevel = currentUnhandledErrorLogLevel;
var versionMismatchErrorLogLevel = currentVersionMismatchErrorLogLevel;
var currentLogAnnotations2 = currentLogAnnotations;
var currentLoggers2 = currentLoggers;
var currentLogLevel2 = currentLogLevel;
var currentMinimumLogLevel2 = currentMinimumLogLevel;
var currentLogSpan2 = currentLogSpan;
var currentRuntimeFlags2 = currentRuntimeFlags;
var currentScheduler2 = currentScheduler;
var currentSupervisor2 = currentSupervisor;
var currentMetricLabels2 = currentMetricLabels;
var currentTracerEnabled2 = currentTracerEnabled;
var currentTracerTimingEnabled2 = currentTracerTimingEnabled;
var currentTracerSpanAnnotations2 = currentTracerSpanAnnotations;
var currentTracerSpanLinks2 = currentTracerSpanLinks;
var interruptedCause = currentInterruptedCause;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/PubSub.js
var PubSub_exports = {};
__export(PubSub_exports, {
  awaitShutdown: () => awaitShutdown4,
  bounded: () => bounded5,
  capacity: () => capacity5,
  dropping: () => dropping4,
  isEmpty: () => isEmpty6,
  isFull: () => isFull4,
  isShutdown: () => isShutdown4,
  publish: () => publish2,
  publishAll: () => publishAll2,
  shutdown: () => shutdown4,
  size: () => size5,
  sliding: () => sliding4,
  subscribe: () => subscribe2,
  unbounded: () => unbounded5
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/pubsub.js
var AbsentValue = Symbol.for("effect/PubSub/AbsentValue");
var addSubscribers = (subscription, pollers) => (subscribers) => {
  if (!subscribers.has(subscription)) {
    subscribers.set(subscription, /* @__PURE__ */ new Set());
  }
  const set6 = subscribers.get(subscription);
  set6.add(pollers);
};
var removeSubscribers = (subscription, pollers) => (subscribers) => {
  if (!subscribers.has(subscription)) {
    return;
  }
  const set6 = subscribers.get(subscription);
  set6.delete(pollers);
  if (set6.size === 0) {
    subscribers.delete(subscription);
  }
};
var bounded4 = (capacity10) => suspend(() => {
  const pubsub = makeBoundedPubSub(capacity10);
  return makePubSub(pubsub, new BackPressureStrategy2());
});
var dropping3 = (capacity10) => suspend(() => {
  const pubsub = makeBoundedPubSub(capacity10);
  return makePubSub(pubsub, new DroppingStrategy2());
});
var sliding3 = (capacity10) => suspend(() => {
  const pubsub = makeBoundedPubSub(capacity10);
  return makePubSub(pubsub, new SlidingStrategy2());
});
var unbounded4 = (options) => suspend(() => {
  const pubsub = makeUnboundedPubSub(options);
  return makePubSub(pubsub, new DroppingStrategy2());
});
var capacity4 = (self) => self.capacity();
var size4 = (self) => self.size;
var isFull3 = (self) => self.isFull;
var isEmpty5 = (self) => self.isEmpty;
var shutdown3 = (self) => self.shutdown;
var isShutdown3 = (self) => self.isShutdown;
var awaitShutdown3 = (self) => self.awaitShutdown;
var publish = dual(2, (self, value) => self.publish(value));
var publishAll = dual(2, (self, elements) => self.publishAll(elements));
var subscribe = (self) => self.subscribe;
var makeBoundedPubSub = (capacity10) => {
  const options = typeof capacity10 === "number" ? {
    capacity: capacity10
  } : capacity10;
  ensureCapacity(options.capacity);
  const replayBuffer = options.replay && options.replay > 0 ? new ReplayBuffer(Math.ceil(options.replay)) : void 0;
  if (options.capacity === 1) {
    return new BoundedPubSubSingle(replayBuffer);
  } else if (nextPow2(options.capacity) === options.capacity) {
    return new BoundedPubSubPow2(options.capacity, replayBuffer);
  } else {
    return new BoundedPubSubArb(options.capacity, replayBuffer);
  }
};
var makeUnboundedPubSub = (options) => new UnboundedPubSub(options?.replay ? new ReplayBuffer(options.replay) : void 0);
var makeSubscription = (pubsub, subscribers, strategy) => map3(deferredMake(), (deferred) => unsafeMakeSubscription(pubsub, subscribers, pubsub.subscribe(), unbounded(), deferred, make4(false), strategy));
var unsafeMakeSubscription = (pubsub, subscribers, subscription, pollers, shutdownHook, shutdownFlag, strategy) => new SubscriptionImpl(pubsub, subscribers, subscription, pollers, shutdownHook, shutdownFlag, strategy, pubsub.replayWindow());
var BoundedPubSubArb = class {
  capacity;
  replayBuffer;
  array;
  publisherIndex = 0;
  subscribers;
  subscriberCount = 0;
  subscribersIndex = 0;
  constructor(capacity10, replayBuffer) {
    this.capacity = capacity10;
    this.replayBuffer = replayBuffer;
    this.array = Array.from({
      length: capacity10
    });
    this.subscribers = Array.from({
      length: capacity10
    });
  }
  replayWindow() {
    return this.replayBuffer ? new ReplayWindowImpl(this.replayBuffer) : emptyReplayWindow;
  }
  isEmpty() {
    return this.publisherIndex === this.subscribersIndex;
  }
  isFull() {
    return this.publisherIndex === this.subscribersIndex + this.capacity;
  }
  size() {
    return this.publisherIndex - this.subscribersIndex;
  }
  publish(value) {
    if (this.isFull()) {
      return false;
    }
    if (this.subscriberCount !== 0) {
      const index = this.publisherIndex % this.capacity;
      this.array[index] = value;
      this.subscribers[index] = this.subscriberCount;
      this.publisherIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.offer(value);
    }
    return true;
  }
  publishAll(elements) {
    if (this.subscriberCount === 0) {
      if (this.replayBuffer) {
        this.replayBuffer.offerAll(elements);
      }
      return empty2();
    }
    const chunk2 = fromIterable2(elements);
    const n = chunk2.length;
    const size10 = this.publisherIndex - this.subscribersIndex;
    const available = this.capacity - size10;
    const forPubSub = Math.min(n, available);
    if (forPubSub === 0) {
      return chunk2;
    }
    let iteratorIndex = 0;
    const publishAllIndex = this.publisherIndex + forPubSub;
    while (this.publisherIndex !== publishAllIndex) {
      const a = unsafeGet(chunk2, iteratorIndex++);
      const index = this.publisherIndex % this.capacity;
      this.array[index] = a;
      this.subscribers[index] = this.subscriberCount;
      this.publisherIndex += 1;
      if (this.replayBuffer) {
        this.replayBuffer.offer(a);
      }
    }
    return drop(chunk2, iteratorIndex);
  }
  slide() {
    if (this.subscribersIndex !== this.publisherIndex) {
      const index = this.subscribersIndex % this.capacity;
      this.array[index] = AbsentValue;
      this.subscribers[index] = 0;
      this.subscribersIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.slide();
    }
  }
  subscribe() {
    this.subscriberCount += 1;
    return new BoundedPubSubArbSubscription(this, this.publisherIndex, false);
  }
};
var BoundedPubSubArbSubscription = class {
  self;
  subscriberIndex;
  unsubscribed;
  constructor(self, subscriberIndex, unsubscribed) {
    this.self = self;
    this.subscriberIndex = subscriberIndex;
    this.unsubscribed = unsubscribed;
  }
  isEmpty() {
    return this.unsubscribed || this.self.publisherIndex === this.subscriberIndex || this.self.publisherIndex === this.self.subscribersIndex;
  }
  size() {
    if (this.unsubscribed) {
      return 0;
    }
    return this.self.publisherIndex - Math.max(this.subscriberIndex, this.self.subscribersIndex);
  }
  poll(default_) {
    if (this.unsubscribed) {
      return default_;
    }
    this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
    if (this.subscriberIndex !== this.self.publisherIndex) {
      const index = this.subscriberIndex % this.self.capacity;
      const elem = this.self.array[index];
      this.self.subscribers[index] -= 1;
      if (this.self.subscribers[index] === 0) {
        this.self.array[index] = AbsentValue;
        this.self.subscribersIndex += 1;
      }
      this.subscriberIndex += 1;
      return elem;
    }
    return default_;
  }
  pollUpTo(n) {
    if (this.unsubscribed) {
      return empty2();
    }
    this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
    const size10 = this.self.publisherIndex - this.subscriberIndex;
    const toPoll = Math.min(n, size10);
    if (toPoll <= 0) {
      return empty2();
    }
    const builder = [];
    const pollUpToIndex = this.subscriberIndex + toPoll;
    while (this.subscriberIndex !== pollUpToIndex) {
      const index = this.subscriberIndex % this.self.capacity;
      const a = this.self.array[index];
      this.self.subscribers[index] -= 1;
      if (this.self.subscribers[index] === 0) {
        this.self.array[index] = AbsentValue;
        this.self.subscribersIndex += 1;
      }
      builder.push(a);
      this.subscriberIndex += 1;
    }
    return fromIterable2(builder);
  }
  unsubscribe() {
    if (!this.unsubscribed) {
      this.unsubscribed = true;
      this.self.subscriberCount -= 1;
      this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
      while (this.subscriberIndex !== this.self.publisherIndex) {
        const index = this.subscriberIndex % this.self.capacity;
        this.self.subscribers[index] -= 1;
        if (this.self.subscribers[index] === 0) {
          this.self.array[index] = AbsentValue;
          this.self.subscribersIndex += 1;
        }
        this.subscriberIndex += 1;
      }
    }
  }
};
var BoundedPubSubPow2 = class {
  capacity;
  replayBuffer;
  array;
  mask;
  publisherIndex = 0;
  subscribers;
  subscriberCount = 0;
  subscribersIndex = 0;
  constructor(capacity10, replayBuffer) {
    this.capacity = capacity10;
    this.replayBuffer = replayBuffer;
    this.array = Array.from({
      length: capacity10
    });
    this.mask = capacity10 - 1;
    this.subscribers = Array.from({
      length: capacity10
    });
  }
  replayWindow() {
    return this.replayBuffer ? new ReplayWindowImpl(this.replayBuffer) : emptyReplayWindow;
  }
  isEmpty() {
    return this.publisherIndex === this.subscribersIndex;
  }
  isFull() {
    return this.publisherIndex === this.subscribersIndex + this.capacity;
  }
  size() {
    return this.publisherIndex - this.subscribersIndex;
  }
  publish(value) {
    if (this.isFull()) {
      return false;
    }
    if (this.subscriberCount !== 0) {
      const index = this.publisherIndex & this.mask;
      this.array[index] = value;
      this.subscribers[index] = this.subscriberCount;
      this.publisherIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.offer(value);
    }
    return true;
  }
  publishAll(elements) {
    if (this.subscriberCount === 0) {
      if (this.replayBuffer) {
        this.replayBuffer.offerAll(elements);
      }
      return empty2();
    }
    const chunk2 = fromIterable2(elements);
    const n = chunk2.length;
    const size10 = this.publisherIndex - this.subscribersIndex;
    const available = this.capacity - size10;
    const forPubSub = Math.min(n, available);
    if (forPubSub === 0) {
      return chunk2;
    }
    let iteratorIndex = 0;
    const publishAllIndex = this.publisherIndex + forPubSub;
    while (this.publisherIndex !== publishAllIndex) {
      const elem = unsafeGet(chunk2, iteratorIndex++);
      const index = this.publisherIndex & this.mask;
      this.array[index] = elem;
      this.subscribers[index] = this.subscriberCount;
      this.publisherIndex += 1;
      if (this.replayBuffer) {
        this.replayBuffer.offer(elem);
      }
    }
    return drop(chunk2, iteratorIndex);
  }
  slide() {
    if (this.subscribersIndex !== this.publisherIndex) {
      const index = this.subscribersIndex & this.mask;
      this.array[index] = AbsentValue;
      this.subscribers[index] = 0;
      this.subscribersIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.slide();
    }
  }
  subscribe() {
    this.subscriberCount += 1;
    return new BoundedPubSubPow2Subscription(this, this.publisherIndex, false);
  }
};
var BoundedPubSubPow2Subscription = class {
  self;
  subscriberIndex;
  unsubscribed;
  constructor(self, subscriberIndex, unsubscribed) {
    this.self = self;
    this.subscriberIndex = subscriberIndex;
    this.unsubscribed = unsubscribed;
  }
  isEmpty() {
    return this.unsubscribed || this.self.publisherIndex === this.subscriberIndex || this.self.publisherIndex === this.self.subscribersIndex;
  }
  size() {
    if (this.unsubscribed) {
      return 0;
    }
    return this.self.publisherIndex - Math.max(this.subscriberIndex, this.self.subscribersIndex);
  }
  poll(default_) {
    if (this.unsubscribed) {
      return default_;
    }
    this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
    if (this.subscriberIndex !== this.self.publisherIndex) {
      const index = this.subscriberIndex & this.self.mask;
      const elem = this.self.array[index];
      this.self.subscribers[index] -= 1;
      if (this.self.subscribers[index] === 0) {
        this.self.array[index] = AbsentValue;
        this.self.subscribersIndex += 1;
      }
      this.subscriberIndex += 1;
      return elem;
    }
    return default_;
  }
  pollUpTo(n) {
    if (this.unsubscribed) {
      return empty2();
    }
    this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
    const size10 = this.self.publisherIndex - this.subscriberIndex;
    const toPoll = Math.min(n, size10);
    if (toPoll <= 0) {
      return empty2();
    }
    const builder = [];
    const pollUpToIndex = this.subscriberIndex + toPoll;
    while (this.subscriberIndex !== pollUpToIndex) {
      const index = this.subscriberIndex & this.self.mask;
      const elem = this.self.array[index];
      this.self.subscribers[index] -= 1;
      if (this.self.subscribers[index] === 0) {
        this.self.array[index] = AbsentValue;
        this.self.subscribersIndex += 1;
      }
      builder.push(elem);
      this.subscriberIndex += 1;
    }
    return fromIterable2(builder);
  }
  unsubscribe() {
    if (!this.unsubscribed) {
      this.unsubscribed = true;
      this.self.subscriberCount -= 1;
      this.subscriberIndex = Math.max(this.subscriberIndex, this.self.subscribersIndex);
      while (this.subscriberIndex !== this.self.publisherIndex) {
        const index = this.subscriberIndex & this.self.mask;
        this.self.subscribers[index] -= 1;
        if (this.self.subscribers[index] === 0) {
          this.self.array[index] = AbsentValue;
          this.self.subscribersIndex += 1;
        }
        this.subscriberIndex += 1;
      }
    }
  }
};
var BoundedPubSubSingle = class {
  replayBuffer;
  publisherIndex = 0;
  subscriberCount = 0;
  subscribers = 0;
  value = AbsentValue;
  capacity = 1;
  constructor(replayBuffer) {
    this.replayBuffer = replayBuffer;
  }
  replayWindow() {
    return this.replayBuffer ? new ReplayWindowImpl(this.replayBuffer) : emptyReplayWindow;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  isEmpty() {
    return this.subscribers === 0;
  }
  isFull() {
    return !this.isEmpty();
  }
  size() {
    return this.isEmpty() ? 0 : 1;
  }
  publish(value) {
    if (this.isFull()) {
      return false;
    }
    if (this.subscriberCount !== 0) {
      this.value = value;
      this.subscribers = this.subscriberCount;
      this.publisherIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.offer(value);
    }
    return true;
  }
  publishAll(elements) {
    if (this.subscriberCount === 0) {
      if (this.replayBuffer) {
        this.replayBuffer.offerAll(elements);
      }
      return empty2();
    }
    const chunk2 = fromIterable2(elements);
    if (isEmpty(chunk2)) {
      return chunk2;
    }
    if (this.publish(unsafeHead(chunk2))) {
      return drop(chunk2, 1);
    } else {
      return chunk2;
    }
  }
  slide() {
    if (this.isFull()) {
      this.subscribers = 0;
      this.value = AbsentValue;
    }
    if (this.replayBuffer) {
      this.replayBuffer.slide();
    }
  }
  subscribe() {
    this.subscriberCount += 1;
    return new BoundedPubSubSingleSubscription(this, this.publisherIndex, false);
  }
};
var BoundedPubSubSingleSubscription = class {
  self;
  subscriberIndex;
  unsubscribed;
  constructor(self, subscriberIndex, unsubscribed) {
    this.self = self;
    this.subscriberIndex = subscriberIndex;
    this.unsubscribed = unsubscribed;
  }
  isEmpty() {
    return this.unsubscribed || this.self.subscribers === 0 || this.subscriberIndex === this.self.publisherIndex;
  }
  size() {
    return this.isEmpty() ? 0 : 1;
  }
  poll(default_) {
    if (this.isEmpty()) {
      return default_;
    }
    const elem = this.self.value;
    this.self.subscribers -= 1;
    if (this.self.subscribers === 0) {
      this.self.value = AbsentValue;
    }
    this.subscriberIndex += 1;
    return elem;
  }
  pollUpTo(n) {
    if (this.isEmpty() || n < 1) {
      return empty2();
    }
    const a = this.self.value;
    this.self.subscribers -= 1;
    if (this.self.subscribers === 0) {
      this.self.value = AbsentValue;
    }
    this.subscriberIndex += 1;
    return of(a);
  }
  unsubscribe() {
    if (!this.unsubscribed) {
      this.unsubscribed = true;
      this.self.subscriberCount -= 1;
      if (this.subscriberIndex !== this.self.publisherIndex) {
        this.self.subscribers -= 1;
        if (this.self.subscribers === 0) {
          this.self.value = AbsentValue;
        }
      }
    }
  }
};
var UnboundedPubSub = class {
  replayBuffer;
  publisherHead = {
    value: AbsentValue,
    subscribers: 0,
    next: null
  };
  publisherTail = this.publisherHead;
  publisherIndex = 0;
  subscribersIndex = 0;
  capacity = Number.MAX_SAFE_INTEGER;
  constructor(replayBuffer) {
    this.replayBuffer = replayBuffer;
  }
  replayWindow() {
    return this.replayBuffer ? new ReplayWindowImpl(this.replayBuffer) : emptyReplayWindow;
  }
  isEmpty() {
    return this.publisherHead === this.publisherTail;
  }
  isFull() {
    return false;
  }
  size() {
    return this.publisherIndex - this.subscribersIndex;
  }
  publish(value) {
    const subscribers = this.publisherTail.subscribers;
    if (subscribers !== 0) {
      this.publisherTail.next = {
        value,
        subscribers,
        next: null
      };
      this.publisherTail = this.publisherTail.next;
      this.publisherIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.offer(value);
    }
    return true;
  }
  publishAll(elements) {
    if (this.publisherTail.subscribers !== 0) {
      for (const a of elements) {
        this.publish(a);
      }
    } else if (this.replayBuffer) {
      this.replayBuffer.offerAll(elements);
    }
    return empty2();
  }
  slide() {
    if (this.publisherHead !== this.publisherTail) {
      this.publisherHead = this.publisherHead.next;
      this.publisherHead.value = AbsentValue;
      this.subscribersIndex += 1;
    }
    if (this.replayBuffer) {
      this.replayBuffer.slide();
    }
  }
  subscribe() {
    this.publisherTail.subscribers += 1;
    return new UnboundedPubSubSubscription(this, this.publisherTail, this.publisherIndex, false);
  }
};
var UnboundedPubSubSubscription = class {
  self;
  subscriberHead;
  subscriberIndex;
  unsubscribed;
  constructor(self, subscriberHead, subscriberIndex, unsubscribed) {
    this.self = self;
    this.subscriberHead = subscriberHead;
    this.subscriberIndex = subscriberIndex;
    this.unsubscribed = unsubscribed;
  }
  isEmpty() {
    if (this.unsubscribed) {
      return true;
    }
    let empty7 = true;
    let loop2 = true;
    while (loop2) {
      if (this.subscriberHead === this.self.publisherTail) {
        loop2 = false;
      } else {
        if (this.subscriberHead.next.value !== AbsentValue) {
          empty7 = false;
          loop2 = false;
        } else {
          this.subscriberHead = this.subscriberHead.next;
          this.subscriberIndex += 1;
        }
      }
    }
    return empty7;
  }
  size() {
    if (this.unsubscribed) {
      return 0;
    }
    return this.self.publisherIndex - Math.max(this.subscriberIndex, this.self.subscribersIndex);
  }
  poll(default_) {
    if (this.unsubscribed) {
      return default_;
    }
    let loop2 = true;
    let polled = default_;
    while (loop2) {
      if (this.subscriberHead === this.self.publisherTail) {
        loop2 = false;
      } else {
        const elem = this.subscriberHead.next.value;
        if (elem !== AbsentValue) {
          polled = elem;
          this.subscriberHead.subscribers -= 1;
          if (this.subscriberHead.subscribers === 0) {
            this.self.publisherHead = this.self.publisherHead.next;
            this.self.publisherHead.value = AbsentValue;
            this.self.subscribersIndex += 1;
          }
          loop2 = false;
        }
        this.subscriberHead = this.subscriberHead.next;
        this.subscriberIndex += 1;
      }
    }
    return polled;
  }
  pollUpTo(n) {
    const builder = [];
    const default_ = AbsentValue;
    let i = 0;
    while (i !== n) {
      const a = this.poll(default_);
      if (a === default_) {
        i = n;
      } else {
        builder.push(a);
        i += 1;
      }
    }
    return fromIterable2(builder);
  }
  unsubscribe() {
    if (!this.unsubscribed) {
      this.unsubscribed = true;
      this.self.publisherTail.subscribers -= 1;
      while (this.subscriberHead !== this.self.publisherTail) {
        if (this.subscriberHead.next.value !== AbsentValue) {
          this.subscriberHead.subscribers -= 1;
          if (this.subscriberHead.subscribers === 0) {
            this.self.publisherHead = this.self.publisherHead.next;
            this.self.publisherHead.value = AbsentValue;
            this.self.subscribersIndex += 1;
          }
        }
        this.subscriberHead = this.subscriberHead.next;
      }
    }
  }
};
var SubscriptionImpl = class extends Class {
  pubsub;
  subscribers;
  subscription;
  pollers;
  shutdownHook;
  shutdownFlag;
  strategy;
  replayWindow;
  [DequeueTypeId] = dequeueVariance;
  constructor(pubsub, subscribers, subscription, pollers, shutdownHook, shutdownFlag, strategy, replayWindow) {
    super();
    this.pubsub = pubsub;
    this.subscribers = subscribers;
    this.subscription = subscription;
    this.pollers = pollers;
    this.shutdownHook = shutdownHook;
    this.shutdownFlag = shutdownFlag;
    this.strategy = strategy;
    this.replayWindow = replayWindow;
  }
  commit() {
    return this.take;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  capacity() {
    return this.pubsub.capacity;
  }
  isActive() {
    return !get2(this.shutdownFlag);
  }
  get size() {
    return suspend(() => get2(this.shutdownFlag) ? interrupt : succeed(this.subscription.size() + this.replayWindow.remaining));
  }
  unsafeSize() {
    if (get2(this.shutdownFlag)) {
      return none();
    }
    return some(this.subscription.size() + this.replayWindow.remaining);
  }
  get isFull() {
    return suspend(() => get2(this.shutdownFlag) ? interrupt : succeed(this.subscription.size() === this.capacity()));
  }
  get isEmpty() {
    return map3(this.size, (size10) => size10 === 0);
  }
  get shutdown() {
    return uninterruptible(withFiberRuntime((state) => {
      set(this.shutdownFlag, true);
      return pipe(forEachParUnbounded(unsafePollAllQueue(this.pollers), (d) => deferredInterruptWith(d, state.id()), false), zipRight(sync(() => {
        this.subscribers.delete(this.subscription);
        this.subscription.unsubscribe();
        this.strategy.unsafeOnPubSubEmptySpace(this.pubsub, this.subscribers);
      })), whenEffect(deferredSucceed(this.shutdownHook, void 0)), asVoid);
    }));
  }
  get isShutdown() {
    return sync(() => get2(this.shutdownFlag));
  }
  get awaitShutdown() {
    return deferredAwait(this.shutdownHook);
  }
  get take() {
    return withFiberRuntime((state) => {
      if (get2(this.shutdownFlag)) {
        return interrupt;
      }
      if (this.replayWindow.remaining > 0) {
        const message2 = this.replayWindow.take();
        return succeed(message2);
      }
      const message = isEmpty2(this.pollers) ? this.subscription.poll(EmptyMutableQueue) : EmptyMutableQueue;
      if (message === EmptyMutableQueue) {
        const deferred = deferredUnsafeMake(state.id());
        return pipe(suspend(() => {
          pipe(this.pollers, offer(deferred));
          pipe(this.subscribers, addSubscribers(this.subscription, this.pollers));
          this.strategy.unsafeCompletePollers(this.pubsub, this.subscribers, this.subscription, this.pollers);
          return get2(this.shutdownFlag) ? interrupt : deferredAwait(deferred);
        }), onInterrupt(() => sync(() => unsafeRemove2(this.pollers, deferred))));
      } else {
        this.strategy.unsafeOnPubSubEmptySpace(this.pubsub, this.subscribers);
        return succeed(message);
      }
    });
  }
  get takeAll() {
    return suspend(() => {
      if (get2(this.shutdownFlag)) {
        return interrupt;
      }
      const as10 = isEmpty2(this.pollers) ? unsafePollAllSubscription(this.subscription) : empty2();
      this.strategy.unsafeOnPubSubEmptySpace(this.pubsub, this.subscribers);
      if (this.replayWindow.remaining > 0) {
        return succeed(appendAll(this.replayWindow.takeAll(), as10));
      }
      return succeed(as10);
    });
  }
  takeUpTo(max) {
    return suspend(() => {
      if (get2(this.shutdownFlag)) {
        return interrupt;
      }
      let replay = void 0;
      if (this.replayWindow.remaining >= max) {
        const as11 = this.replayWindow.takeN(max);
        return succeed(as11);
      } else if (this.replayWindow.remaining > 0) {
        replay = this.replayWindow.takeAll();
        max = max - replay.length;
      }
      const as10 = isEmpty2(this.pollers) ? unsafePollN2(this.subscription, max) : empty2();
      this.strategy.unsafeOnPubSubEmptySpace(this.pubsub, this.subscribers);
      return replay ? succeed(appendAll(replay, as10)) : succeed(as10);
    });
  }
  takeBetween(min, max) {
    return suspend(() => takeRemainderLoop2(this, min, max, empty2()));
  }
};
var takeRemainderLoop2 = (self, min, max, acc) => {
  if (max < min) {
    return succeed(acc);
  }
  return pipe(self.takeUpTo(max), flatMap2((bs) => {
    const remaining = min - bs.length;
    if (remaining === 1) {
      return pipe(self.take, map3((b) => pipe(acc, appendAll(bs), append(b))));
    }
    if (remaining > 1) {
      return pipe(self.take, flatMap2((b) => takeRemainderLoop2(self, remaining - 1, max - bs.length - 1, pipe(acc, appendAll(bs), append(b)))));
    }
    return succeed(pipe(acc, appendAll(bs)));
  }));
};
var PubSubImpl = class {
  pubsub;
  subscribers;
  scope;
  shutdownHook;
  shutdownFlag;
  strategy;
  [EnqueueTypeId] = enqueueVariance;
  [DequeueTypeId] = dequeueVariance;
  constructor(pubsub, subscribers, scope2, shutdownHook, shutdownFlag, strategy) {
    this.pubsub = pubsub;
    this.subscribers = subscribers;
    this.scope = scope2;
    this.shutdownHook = shutdownHook;
    this.shutdownFlag = shutdownFlag;
    this.strategy = strategy;
  }
  capacity() {
    return this.pubsub.capacity;
  }
  get size() {
    return suspend(() => get2(this.shutdownFlag) ? interrupt : sync(() => this.pubsub.size()));
  }
  unsafeSize() {
    if (get2(this.shutdownFlag)) {
      return none();
    }
    return some(this.pubsub.size());
  }
  get isFull() {
    return map3(this.size, (size10) => size10 === this.capacity());
  }
  get isEmpty() {
    return map3(this.size, (size10) => size10 === 0);
  }
  get awaitShutdown() {
    return deferredAwait(this.shutdownHook);
  }
  get isShutdown() {
    return sync(() => get2(this.shutdownFlag));
  }
  get shutdown() {
    return uninterruptible(withFiberRuntime((state) => {
      pipe(this.shutdownFlag, set(true));
      return pipe(this.scope.close(exitInterrupt(state.id())), zipRight(this.strategy.shutdown), whenEffect(deferredSucceed(this.shutdownHook, void 0)), asVoid);
    }));
  }
  publish(value) {
    return suspend(() => {
      if (get2(this.shutdownFlag)) {
        return interrupt;
      }
      if (this.pubsub.publish(value)) {
        this.strategy.unsafeCompleteSubscribers(this.pubsub, this.subscribers);
        return succeed(true);
      }
      return this.strategy.handleSurplus(this.pubsub, this.subscribers, of(value), this.shutdownFlag);
    });
  }
  isActive() {
    return !get2(this.shutdownFlag);
  }
  unsafeOffer(value) {
    if (get2(this.shutdownFlag)) {
      return false;
    }
    if (this.pubsub.publish(value)) {
      this.strategy.unsafeCompleteSubscribers(this.pubsub, this.subscribers);
      return true;
    }
    return false;
  }
  publishAll(elements) {
    return suspend(() => {
      if (get2(this.shutdownFlag)) {
        return interrupt;
      }
      const surplus = unsafePublishAll(this.pubsub, elements);
      this.strategy.unsafeCompleteSubscribers(this.pubsub, this.subscribers);
      if (isEmpty(surplus)) {
        return succeed(true);
      }
      return this.strategy.handleSurplus(this.pubsub, this.subscribers, surplus, this.shutdownFlag);
    });
  }
  get subscribe() {
    const acquire = tap(all2([this.scope.fork(sequential), makeSubscription(this.pubsub, this.subscribers, this.strategy)]), (tuple) => tuple[0].addFinalizer(() => tuple[1].shutdown));
    return map3(acquireRelease(acquire, (tuple, exit2) => tuple[0].close(exit2)), (tuple) => tuple[1]);
  }
  offer(value) {
    return this.publish(value);
  }
  offerAll(elements) {
    return this.publishAll(elements);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makePubSub = (pubsub, strategy) => flatMap2(scopeMake(), (scope2) => map3(deferredMake(), (deferred) => unsafeMakePubSub(pubsub, /* @__PURE__ */ new Map(), scope2, deferred, make4(false), strategy)));
var unsafeMakePubSub = (pubsub, subscribers, scope2, shutdownHook, shutdownFlag, strategy) => new PubSubImpl(pubsub, subscribers, scope2, shutdownHook, shutdownFlag, strategy);
var ensureCapacity = (capacity10) => {
  if (capacity10 <= 0) {
    throw new InvalidPubSubCapacityException(`Cannot construct PubSub with capacity of ${capacity10}`);
  }
};
var unsafeCompleteDeferred2 = (deferred, a) => {
  deferredUnsafeDone(deferred, succeed(a));
};
var unsafeOfferAll2 = (queue, as10) => {
  return pipe(queue, offerAll(as10));
};
var unsafePollAllQueue = (queue) => {
  return pipe(queue, pollUpTo(Number.POSITIVE_INFINITY));
};
var unsafePollAllSubscription = (subscription) => {
  return subscription.pollUpTo(Number.POSITIVE_INFINITY);
};
var unsafePollN2 = (subscription, max) => {
  return subscription.pollUpTo(max);
};
var unsafePublishAll = (pubsub, as10) => {
  return pubsub.publishAll(as10);
};
var unsafeRemove2 = (queue, value) => {
  unsafeOfferAll2(queue, pipe(unsafePollAllQueue(queue), filter((elem) => elem !== value)));
};
var BackPressureStrategy2 = class {
  publishers = unbounded();
  get shutdown() {
    return flatMap2(fiberId, (fiberId3) => flatMap2(sync(() => unsafePollAllQueue(this.publishers)), (publishers) => forEachConcurrentDiscard(publishers, ([_, deferred, last3]) => last3 ? pipe(deferredInterruptWith(deferred, fiberId3), asVoid) : void_, false, false)));
  }
  handleSurplus(pubsub, subscribers, elements, isShutdown9) {
    return withFiberRuntime((state) => {
      const deferred = deferredUnsafeMake(state.id());
      return pipe(suspend(() => {
        this.unsafeOffer(elements, deferred);
        this.unsafeOnPubSubEmptySpace(pubsub, subscribers);
        this.unsafeCompleteSubscribers(pubsub, subscribers);
        return get2(isShutdown9) ? interrupt : deferredAwait(deferred);
      }), onInterrupt(() => sync(() => this.unsafeRemove(deferred))));
    });
  }
  unsafeOnPubSubEmptySpace(pubsub, subscribers) {
    let keepPolling = true;
    while (keepPolling && !pubsub.isFull()) {
      const publisher = pipe(this.publishers, poll3(EmptyMutableQueue));
      if (publisher === EmptyMutableQueue) {
        keepPolling = false;
      } else {
        const published = pubsub.publish(publisher[0]);
        if (published && publisher[2]) {
          unsafeCompleteDeferred2(publisher[1], true);
        } else if (!published) {
          unsafeOfferAll2(this.publishers, pipe(unsafePollAllQueue(this.publishers), prepend(publisher)));
        }
        this.unsafeCompleteSubscribers(pubsub, subscribers);
      }
    }
  }
  unsafeCompletePollers(pubsub, subscribers, subscription, pollers) {
    return unsafeStrategyCompletePollers(this, pubsub, subscribers, subscription, pollers);
  }
  unsafeCompleteSubscribers(pubsub, subscribers) {
    return unsafeStrategyCompleteSubscribers(this, pubsub, subscribers);
  }
  unsafeOffer(elements, deferred) {
    const iterator = elements[Symbol.iterator]();
    let next = iterator.next();
    if (!next.done) {
      while (1) {
        const value = next.value;
        next = iterator.next();
        if (next.done) {
          pipe(this.publishers, offer([value, deferred, true]));
          break;
        }
        pipe(this.publishers, offer([value, deferred, false]));
      }
    }
  }
  unsafeRemove(deferred) {
    unsafeOfferAll2(this.publishers, pipe(unsafePollAllQueue(this.publishers), filter(([_, a]) => a !== deferred)));
  }
};
var DroppingStrategy2 = class {
  get shutdown() {
    return void_;
  }
  handleSurplus(_pubsub, _subscribers, _elements, _isShutdown) {
    return succeed(false);
  }
  unsafeOnPubSubEmptySpace(_pubsub, _subscribers) {
  }
  unsafeCompletePollers(pubsub, subscribers, subscription, pollers) {
    return unsafeStrategyCompletePollers(this, pubsub, subscribers, subscription, pollers);
  }
  unsafeCompleteSubscribers(pubsub, subscribers) {
    return unsafeStrategyCompleteSubscribers(this, pubsub, subscribers);
  }
};
var SlidingStrategy2 = class {
  get shutdown() {
    return void_;
  }
  handleSurplus(pubsub, subscribers, elements, _isShutdown) {
    return sync(() => {
      this.unsafeSlidingPublish(pubsub, elements);
      this.unsafeCompleteSubscribers(pubsub, subscribers);
      return true;
    });
  }
  unsafeOnPubSubEmptySpace(_pubsub, _subscribers) {
  }
  unsafeCompletePollers(pubsub, subscribers, subscription, pollers) {
    return unsafeStrategyCompletePollers(this, pubsub, subscribers, subscription, pollers);
  }
  unsafeCompleteSubscribers(pubsub, subscribers) {
    return unsafeStrategyCompleteSubscribers(this, pubsub, subscribers);
  }
  unsafeSlidingPublish(pubsub, elements) {
    const it = elements[Symbol.iterator]();
    let next = it.next();
    if (!next.done && pubsub.capacity > 0) {
      let a = next.value;
      let loop2 = true;
      while (loop2) {
        pubsub.slide();
        const pub = pubsub.publish(a);
        if (pub && (next = it.next()) && !next.done) {
          a = next.value;
        } else if (pub) {
          loop2 = false;
        }
      }
    }
  }
};
var unsafeStrategyCompletePollers = (strategy, pubsub, subscribers, subscription, pollers) => {
  let keepPolling = true;
  while (keepPolling && !subscription.isEmpty()) {
    const poller = pipe(pollers, poll3(EmptyMutableQueue));
    if (poller === EmptyMutableQueue) {
      pipe(subscribers, removeSubscribers(subscription, pollers));
      if (isEmpty2(pollers)) {
        keepPolling = false;
      } else {
        pipe(subscribers, addSubscribers(subscription, pollers));
      }
    } else {
      const pollResult = subscription.poll(EmptyMutableQueue);
      if (pollResult === EmptyMutableQueue) {
        unsafeOfferAll2(pollers, pipe(unsafePollAllQueue(pollers), prepend(poller)));
      } else {
        unsafeCompleteDeferred2(poller, pollResult);
        strategy.unsafeOnPubSubEmptySpace(pubsub, subscribers);
      }
    }
  }
};
var unsafeStrategyCompleteSubscribers = (strategy, pubsub, subscribers) => {
  for (const [subscription, pollersSet] of subscribers) {
    for (const pollers of pollersSet) {
      strategy.unsafeCompletePollers(pubsub, subscribers, subscription, pollers);
    }
  }
};
var ReplayBuffer = class {
  capacity;
  constructor(capacity10) {
    this.capacity = capacity10;
  }
  head = {
    value: AbsentValue,
    next: null
  };
  tail = this.head;
  size = 0;
  index = 0;
  slide() {
    this.index++;
  }
  offer(a) {
    this.tail.value = a;
    this.tail.next = {
      value: AbsentValue,
      next: null
    };
    this.tail = this.tail.next;
    if (this.size === this.capacity) {
      this.head = this.head.next;
    } else {
      this.size += 1;
    }
  }
  offerAll(as10) {
    for (const a of as10) {
      this.offer(a);
    }
  }
};
var ReplayWindowImpl = class {
  buffer;
  head;
  index;
  remaining;
  constructor(buffer4) {
    this.buffer = buffer4;
    this.index = buffer4.index;
    this.remaining = buffer4.size;
    this.head = buffer4.head;
  }
  fastForward() {
    while (this.index < this.buffer.index) {
      this.head = this.head.next;
      this.index++;
    }
  }
  take() {
    if (this.remaining === 0) {
      return void 0;
    } else if (this.index < this.buffer.index) {
      this.fastForward();
    }
    this.remaining--;
    const value = this.head.value;
    this.head = this.head.next;
    return value;
  }
  takeN(n) {
    if (this.remaining === 0) {
      return empty2();
    } else if (this.index < this.buffer.index) {
      this.fastForward();
    }
    const len = Math.min(n, this.remaining);
    const items = new Array(len);
    for (let i = 0; i < len; i++) {
      const value = this.head.value;
      this.head = this.head.next;
      items[i] = value;
    }
    this.remaining -= len;
    return unsafeFromArray(items);
  }
  takeAll() {
    return this.takeN(this.remaining);
  }
};
var emptyReplayWindow = {
  remaining: 0,
  take: () => void 0,
  takeN: () => empty2(),
  takeAll: () => empty2()
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/PubSub.js
var bounded5 = bounded4;
var dropping4 = dropping3;
var sliding4 = sliding3;
var unbounded5 = unbounded4;
var capacity5 = capacity4;
var size5 = size4;
var isFull4 = isFull3;
var isEmpty6 = isEmpty5;
var shutdown4 = shutdown3;
var isShutdown4 = isShutdown3;
var awaitShutdown4 = awaitShutdown3;
var publish2 = publish;
var publishAll2 = publishAll;
var subscribe2 = subscribe;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channelChildExecutorDecision.js
var OP_CONTINUE = "Continue";
var OP_CLOSE = "Close";
var OP_YIELD = "Yield";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/childExecutorDecision.js
var ChildExecutorDecisionSymbolKey = "effect/ChannelChildExecutorDecision";
var ChildExecutorDecisionTypeId = Symbol.for(ChildExecutorDecisionSymbolKey);
var proto = {
  [ChildExecutorDecisionTypeId]: ChildExecutorDecisionTypeId
};
var Continue = (_) => {
  const op = Object.create(proto);
  op._tag = OP_CONTINUE;
  return op;
};
var Close = (value) => {
  const op = Object.create(proto);
  op._tag = OP_CLOSE;
  op.value = value;
  return op;
};
var Yield = (_) => {
  const op = Object.create(proto);
  op._tag = OP_YIELD;
  return op;
};
var isChildExecutorDecision = (u) => hasProperty(u, ChildExecutorDecisionTypeId);
var isContinue = (self) => self._tag === OP_CONTINUE;
var isClose = (self) => self._tag === OP_CLOSE;
var isYield = (self) => self._tag === OP_YIELD;
var match5 = dual(2, (self, {
  onClose,
  onContinue,
  onYield
}) => {
  switch (self._tag) {
    case OP_CONTINUE: {
      return onContinue();
    }
    case OP_CLOSE: {
      return onClose(self.value);
    }
    case OP_YIELD: {
      return onYield();
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/continuation.js
var OP_CONTINUATION_K = "ContinuationK";
var OP_CONTINUATION_FINALIZER = "ContinuationFinalizer";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/continuation.js
var ContinuationTypeId = Symbol.for("effect/ChannelContinuation");
var continuationVariance = {
  /* c8 ignore next */
  _Env: (_) => _,
  /* c8 ignore next */
  _InErr: (_) => _,
  /* c8 ignore next */
  _InElem: (_) => _,
  /* c8 ignore next */
  _InDone: (_) => _,
  /* c8 ignore next */
  _OutErr: (_) => _,
  /* c8 ignore next */
  _OutDone: (_) => _,
  /* c8 ignore next */
  _OutErr2: (_) => _,
  /* c8 ignore next */
  _OutElem: (_) => _,
  /* c8 ignore next */
  _OutDone2: (_) => _
};
var ContinuationKImpl = class {
  onSuccess;
  onHalt;
  _tag = OP_CONTINUATION_K;
  [ContinuationTypeId] = continuationVariance;
  constructor(onSuccess, onHalt) {
    this.onSuccess = onSuccess;
    this.onHalt = onHalt;
  }
  onExit(exit2) {
    return isFailure(exit2) ? this.onHalt(exit2.cause) : this.onSuccess(exit2.value);
  }
};
var ContinuationFinalizerImpl = class {
  finalizer;
  _tag = OP_CONTINUATION_FINALIZER;
  [ContinuationTypeId] = continuationVariance;
  constructor(finalizer3) {
    this.finalizer = finalizer3;
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channelUpstreamPullStrategy.js
var OP_PULL_AFTER_NEXT = "PullAfterNext";
var OP_PULL_AFTER_ALL_ENQUEUED = "PullAfterAllEnqueued";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/upstreamPullStrategy.js
var UpstreamPullStrategySymbolKey = "effect/ChannelUpstreamPullStrategy";
var UpstreamPullStrategyTypeId = Symbol.for(UpstreamPullStrategySymbolKey);
var upstreamPullStrategyVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var proto2 = {
  [UpstreamPullStrategyTypeId]: upstreamPullStrategyVariance
};
var PullAfterNext = (emitSeparator) => {
  const op = Object.create(proto2);
  op._tag = OP_PULL_AFTER_NEXT;
  op.emitSeparator = emitSeparator;
  return op;
};
var PullAfterAllEnqueued = (emitSeparator) => {
  const op = Object.create(proto2);
  op._tag = OP_PULL_AFTER_ALL_ENQUEUED;
  op.emitSeparator = emitSeparator;
  return op;
};
var isUpstreamPullStrategy = (u) => hasProperty(u, UpstreamPullStrategyTypeId);
var isPullAfterNext = (self) => self._tag === OP_PULL_AFTER_NEXT;
var isPullAfterAllEnqueued = (self) => self._tag === OP_PULL_AFTER_ALL_ENQUEUED;
var match6 = dual(2, (self, {
  onAllEnqueued,
  onNext
}) => {
  switch (self._tag) {
    case OP_PULL_AFTER_NEXT: {
      return onNext(self.emitSeparator);
    }
    case OP_PULL_AFTER_ALL_ENQUEUED: {
      return onAllEnqueued(self.emitSeparator);
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channel.js
var OP_BRACKET_OUT = "BracketOut";
var OP_BRIDGE = "Bridge";
var OP_CONCAT_ALL = "ConcatAll";
var OP_EMIT = "Emit";
var OP_ENSURING = "Ensuring";
var OP_FAIL = "Fail";
var OP_FOLD = "Fold";
var OP_FROM_EFFECT = "FromEffect";
var OP_PIPE_TO = "PipeTo";
var OP_PROVIDE = "Provide";
var OP_READ = "Read";
var OP_SUCCEED = "Succeed";
var OP_SUCCEED_NOW = "SucceedNow";
var OP_SUSPEND = "Suspend";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/core-stream.js
var ChannelSymbolKey = "effect/Channel";
var ChannelTypeId = Symbol.for(ChannelSymbolKey);
var channelVariance = {
  /* c8 ignore next */
  _Env: (_) => _,
  /* c8 ignore next */
  _InErr: (_) => _,
  /* c8 ignore next */
  _InElem: (_) => _,
  /* c8 ignore next */
  _InDone: (_) => _,
  /* c8 ignore next */
  _OutErr: (_) => _,
  /* c8 ignore next */
  _OutElem: (_) => _,
  /* c8 ignore next */
  _OutDone: (_) => _
};
var proto3 = {
  [ChannelTypeId]: channelVariance,
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isChannel = (u) => hasProperty(u, ChannelTypeId) || isEffect(u);
var acquireReleaseOut = dual(2, (self, release) => {
  const op = Object.create(proto3);
  op._tag = OP_BRACKET_OUT;
  op.acquire = () => self;
  op.finalizer = release;
  return op;
});
var catchAllCause2 = dual(2, (self, f) => {
  const op = Object.create(proto3);
  op._tag = OP_FOLD;
  op.channel = self;
  op.k = new ContinuationKImpl(succeed6, f);
  return op;
});
var collectElements = (self) => {
  return suspend3(() => {
    const builder = [];
    return flatMap4(pipeTo(self, collectElementsReader(builder)), (value) => sync4(() => [fromIterable2(builder), value]));
  });
};
var collectElementsReader = (builder) => readWith({
  onInput: (outElem) => flatMap4(sync4(() => {
    builder.push(outElem);
  }), () => collectElementsReader(builder)),
  onFailure: fail5,
  onDone: succeedNow
});
var concatAll = (channels) => concatAllWith(channels, constVoid, constVoid);
var concatAllWith = (channels, f, g) => {
  const op = Object.create(proto3);
  op._tag = OP_CONCAT_ALL;
  op.combineInners = f;
  op.combineAll = g;
  op.onPull = () => PullAfterNext(none());
  op.onEmit = () => Continue;
  op.value = () => channels;
  op.k = identity;
  return op;
};
var concatMapWith = dual(4, (self, f, g, h) => {
  const op = Object.create(proto3);
  op._tag = OP_CONCAT_ALL;
  op.combineInners = g;
  op.combineAll = h;
  op.onPull = () => PullAfterNext(none());
  op.onEmit = () => Continue;
  op.value = () => self;
  op.k = f;
  return op;
});
var concatMapWithCustom = dual(6, (self, f, g, h, onPull, onEmit) => {
  const op = Object.create(proto3);
  op._tag = OP_CONCAT_ALL;
  op.combineInners = g;
  op.combineAll = h;
  op.onPull = onPull;
  op.onEmit = onEmit;
  op.value = () => self;
  op.k = f;
  return op;
});
var embedInput = dual(2, (self, input) => {
  const op = Object.create(proto3);
  op._tag = OP_BRIDGE;
  op.input = input;
  op.channel = self;
  return op;
});
var ensuringWith = dual(2, (self, finalizer3) => {
  const op = Object.create(proto3);
  op._tag = OP_ENSURING;
  op.channel = self;
  op.finalizer = finalizer3;
  return op;
});
var fail5 = (error) => failCause4(fail2(error));
var failSync2 = (evaluate2) => failCauseSync2(() => fail2(evaluate2()));
var failCause4 = (cause) => failCauseSync2(() => cause);
var failCauseSync2 = (evaluate2) => {
  const op = Object.create(proto3);
  op._tag = OP_FAIL;
  op.error = evaluate2;
  return op;
};
var flatMap4 = dual(2, (self, f) => {
  const op = Object.create(proto3);
  op._tag = OP_FOLD;
  op.channel = self;
  op.k = new ContinuationKImpl(f, failCause4);
  return op;
});
var foldCauseChannel = dual(2, (self, options) => {
  const op = Object.create(proto3);
  op._tag = OP_FOLD;
  op.channel = self;
  op.k = new ContinuationKImpl(options.onSuccess, options.onFailure);
  return op;
});
var fromEffect = (effect3) => {
  const op = Object.create(proto3);
  op._tag = OP_FROM_EFFECT;
  op.effect = () => effect3;
  return op;
};
var pipeTo = dual(2, (self, that) => {
  const op = Object.create(proto3);
  op._tag = OP_PIPE_TO;
  op.left = () => self;
  op.right = () => that;
  return op;
});
var provideContext2 = dual(2, (self, env) => {
  const op = Object.create(proto3);
  op._tag = OP_PROVIDE;
  op.context = () => env;
  op.inner = self;
  return op;
});
var readOrFail = (error) => {
  const op = Object.create(proto3);
  op._tag = OP_READ;
  op.more = succeed6;
  op.done = new ContinuationKImpl(() => fail5(error), () => fail5(error));
  return op;
};
var readWith = (options) => readWithCause({
  onInput: options.onInput,
  onFailure: (cause) => match(failureOrCause(cause), {
    onLeft: options.onFailure,
    onRight: failCause4
  }),
  onDone: options.onDone
});
var readWithCause = (options) => {
  const op = Object.create(proto3);
  op._tag = OP_READ;
  op.more = options.onInput;
  op.done = new ContinuationKImpl(options.onDone, options.onFailure);
  return op;
};
var succeed6 = (value) => sync4(() => value);
var succeedNow = (result) => {
  const op = Object.create(proto3);
  op._tag = OP_SUCCEED_NOW;
  op.terminal = result;
  return op;
};
var suspend3 = (evaluate2) => {
  const op = Object.create(proto3);
  op._tag = OP_SUSPEND;
  op.channel = evaluate2;
  return op;
};
var sync4 = (evaluate2) => {
  const op = Object.create(proto3);
  op._tag = OP_SUCCEED;
  op.evaluate = evaluate2;
  return op;
};
var void_3 = succeedNow(void 0);
var write = (out) => {
  const op = Object.create(proto3);
  op._tag = OP_EMIT;
  op.out = out;
  return op;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channelState.js
var OP_DONE = "Done";
var OP_EMIT2 = "Emit";
var OP_FROM_EFFECT2 = "FromEffect";
var OP_READ2 = "Read";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/channelState.js
var ChannelStateTypeId = Symbol.for("effect/ChannelState");
var channelStateVariance = {
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _R: (_) => _
};
var proto4 = {
  [ChannelStateTypeId]: channelStateVariance
};
var Done = () => {
  const op = Object.create(proto4);
  op._tag = OP_DONE;
  return op;
};
var Emit = () => {
  const op = Object.create(proto4);
  op._tag = OP_EMIT2;
  return op;
};
var fromEffect2 = (effect3) => {
  const op = Object.create(proto4);
  op._tag = OP_FROM_EFFECT2;
  op.effect = effect3;
  return op;
};
var Read = (upstream, onEffect, onEmit, onDone3) => {
  const op = Object.create(proto4);
  op._tag = OP_READ2;
  op.upstream = upstream;
  op.onEffect = onEffect;
  op.onEmit = onEmit;
  op.onDone = onDone3;
  return op;
};
var isFromEffect = (self) => self._tag === OP_FROM_EFFECT2;
var effect = (self) => isFromEffect(self) ? self.effect : _void;
var effectOrUndefinedIgnored = (self) => isFromEffect(self) ? ignore(self.effect) : void 0;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/subexecutor.js
var OP_PULL_FROM_CHILD = "PullFromChild";
var OP_PULL_FROM_UPSTREAM = "PullFromUpstream";
var OP_DRAIN_CHILD_EXECUTORS = "DrainChildExecutors";
var OP_EMIT3 = "Emit";
var PullFromChild = class {
  childExecutor;
  parentSubexecutor;
  onEmit;
  _tag = OP_PULL_FROM_CHILD;
  constructor(childExecutor, parentSubexecutor, onEmit) {
    this.childExecutor = childExecutor;
    this.parentSubexecutor = parentSubexecutor;
    this.onEmit = onEmit;
  }
  close(exit2) {
    const fin1 = this.childExecutor.close(exit2);
    const fin2 = this.parentSubexecutor.close(exit2);
    if (fin1 !== void 0 && fin2 !== void 0) {
      return zipWith3(exit(fin1), exit(fin2), (exit1, exit22) => pipe(exit1, zipRight2(exit22)));
    } else if (fin1 !== void 0) {
      return fin1;
    } else if (fin2 !== void 0) {
      return fin2;
    } else {
      return void 0;
    }
  }
  enqueuePullFromChild(_child) {
    return this;
  }
};
var PullFromUpstream = class _PullFromUpstream {
  upstreamExecutor;
  createChild;
  lastDone;
  activeChildExecutors;
  combineChildResults;
  combineWithChildResult;
  onPull;
  onEmit;
  _tag = OP_PULL_FROM_UPSTREAM;
  constructor(upstreamExecutor, createChild, lastDone, activeChildExecutors, combineChildResults, combineWithChildResult, onPull, onEmit) {
    this.upstreamExecutor = upstreamExecutor;
    this.createChild = createChild;
    this.lastDone = lastDone;
    this.activeChildExecutors = activeChildExecutors;
    this.combineChildResults = combineChildResults;
    this.combineWithChildResult = combineWithChildResult;
    this.onPull = onPull;
    this.onEmit = onEmit;
  }
  close(exit2) {
    const fin1 = this.upstreamExecutor.close(exit2);
    const fins = [...this.activeChildExecutors.map((child) => child !== void 0 ? child.childExecutor.close(exit2) : void 0), fin1];
    const result = fins.reduce((acc, next) => {
      if (acc !== void 0 && next !== void 0) {
        return zipWith3(acc, exit(next), (exit1, exit22) => zipRight2(exit1, exit22));
      } else if (acc !== void 0) {
        return acc;
      } else if (next !== void 0) {
        return exit(next);
      } else {
        return void 0;
      }
    }, void 0);
    return result === void 0 ? result : result;
  }
  enqueuePullFromChild(child) {
    return new _PullFromUpstream(this.upstreamExecutor, this.createChild, this.lastDone, [...this.activeChildExecutors, child], this.combineChildResults, this.combineWithChildResult, this.onPull, this.onEmit);
  }
};
var DrainChildExecutors = class _DrainChildExecutors {
  upstreamExecutor;
  lastDone;
  activeChildExecutors;
  upstreamDone;
  combineChildResults;
  combineWithChildResult;
  onPull;
  _tag = OP_DRAIN_CHILD_EXECUTORS;
  constructor(upstreamExecutor, lastDone, activeChildExecutors, upstreamDone, combineChildResults, combineWithChildResult, onPull) {
    this.upstreamExecutor = upstreamExecutor;
    this.lastDone = lastDone;
    this.activeChildExecutors = activeChildExecutors;
    this.upstreamDone = upstreamDone;
    this.combineChildResults = combineChildResults;
    this.combineWithChildResult = combineWithChildResult;
    this.onPull = onPull;
  }
  close(exit2) {
    const fin1 = this.upstreamExecutor.close(exit2);
    const fins = [...this.activeChildExecutors.map((child) => child !== void 0 ? child.childExecutor.close(exit2) : void 0), fin1];
    const result = fins.reduce((acc, next) => {
      if (acc !== void 0 && next !== void 0) {
        return zipWith3(acc, exit(next), (exit1, exit22) => zipRight2(exit1, exit22));
      } else if (acc !== void 0) {
        return acc;
      } else if (next !== void 0) {
        return exit(next);
      } else {
        return void 0;
      }
    }, void 0);
    return result === void 0 ? result : result;
  }
  enqueuePullFromChild(child) {
    return new _DrainChildExecutors(this.upstreamExecutor, this.lastDone, [...this.activeChildExecutors, child], this.upstreamDone, this.combineChildResults, this.combineWithChildResult, this.onPull);
  }
};
var Emit2 = class {
  value;
  next;
  _tag = OP_EMIT3;
  constructor(value, next) {
    this.value = value;
    this.next = next;
  }
  close(exit2) {
    const result = this.next.close(exit2);
    return result === void 0 ? result : result;
  }
  enqueuePullFromChild(_child) {
    return this;
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channelUpstreamPullRequest.js
var OP_PULLED = "Pulled";
var OP_NO_UPSTREAM = "NoUpstream";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/upstreamPullRequest.js
var UpstreamPullRequestSymbolKey = "effect/ChannelUpstreamPullRequest";
var UpstreamPullRequestTypeId = Symbol.for(UpstreamPullRequestSymbolKey);
var upstreamPullRequestVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var proto5 = {
  [UpstreamPullRequestTypeId]: upstreamPullRequestVariance
};
var Pulled = (value) => {
  const op = Object.create(proto5);
  op._tag = OP_PULLED;
  op.value = value;
  return op;
};
var NoUpstream = (activeDownstreamCount) => {
  const op = Object.create(proto5);
  op._tag = OP_NO_UPSTREAM;
  op.activeDownstreamCount = activeDownstreamCount;
  return op;
};
var isUpstreamPullRequest = (u) => hasProperty(u, UpstreamPullRequestTypeId);
var isPulled = (self) => self._tag === OP_PULLED;
var isNoUpstream = (self) => self._tag === OP_NO_UPSTREAM;
var match7 = dual(2, (self, {
  onNoUpstream,
  onPulled
}) => {
  switch (self._tag) {
    case OP_PULLED: {
      return onPulled(self.value);
    }
    case OP_NO_UPSTREAM: {
      return onNoUpstream(self.activeDownstreamCount);
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/channelExecutor.js
var ChannelExecutor = class _ChannelExecutor {
  _activeSubexecutor = void 0;
  _cancelled = void 0;
  _closeLastSubstream = void 0;
  _currentChannel;
  _done = void 0;
  _doneStack = [];
  _emitted = void 0;
  _executeCloseLastSubstream;
  _input = void 0;
  _inProgressFinalizer = void 0;
  _providedEnv;
  constructor(initialChannel, providedEnv, executeCloseLastSubstream) {
    this._currentChannel = initialChannel;
    this._executeCloseLastSubstream = executeCloseLastSubstream;
    this._providedEnv = providedEnv;
  }
  run() {
    let result = void 0;
    while (result === void 0) {
      if (this._cancelled !== void 0) {
        result = this.processCancellation();
      } else if (this._activeSubexecutor !== void 0) {
        result = this.runSubexecutor();
      } else {
        try {
          if (this._currentChannel === void 0) {
            result = Done();
          } else {
            if (isEffect(this._currentChannel)) {
              this._currentChannel = fromEffect(this._currentChannel);
            }
            switch (this._currentChannel._tag) {
              case OP_BRACKET_OUT: {
                result = this.runBracketOut(this._currentChannel);
                break;
              }
              case OP_BRIDGE: {
                const bridgeInput = this._currentChannel.input;
                this._currentChannel = this._currentChannel.channel;
                if (this._input !== void 0) {
                  const inputExecutor = this._input;
                  this._input = void 0;
                  const drainer = () => flatMap3(bridgeInput.awaitRead(), () => suspend2(() => {
                    const state = inputExecutor.run();
                    switch (state._tag) {
                      case OP_DONE: {
                        return match3(inputExecutor.getDone(), {
                          onFailure: (cause) => bridgeInput.error(cause),
                          onSuccess: (value) => bridgeInput.done(value)
                        });
                      }
                      case OP_EMIT2: {
                        return flatMap3(bridgeInput.emit(inputExecutor.getEmit()), () => drainer());
                      }
                      case OP_FROM_EFFECT2: {
                        return matchCauseEffect(state.effect, {
                          onFailure: (cause) => bridgeInput.error(cause),
                          onSuccess: () => drainer()
                        });
                      }
                      case OP_READ2: {
                        return readUpstream(state, () => drainer(), (cause) => bridgeInput.error(cause));
                      }
                    }
                  }));
                  result = fromEffect2(flatMap3(forkDaemon(interruptible2(drainer())), (fiber) => sync3(() => this.addFinalizer((exit2) => flatMap3(interrupt4(fiber), () => suspend2(() => {
                    const effect3 = this.restorePipe(exit2, inputExecutor);
                    return effect3 !== void 0 ? effect3 : _void;
                  }))))));
                }
                break;
              }
              case OP_CONCAT_ALL: {
                const executor = new _ChannelExecutor(this._currentChannel.value(), this._providedEnv, (effect3) => sync3(() => {
                  const prevLastClose = this._closeLastSubstream === void 0 ? _void : this._closeLastSubstream;
                  this._closeLastSubstream = pipe(prevLastClose, zipRight4(effect3));
                }));
                executor._input = this._input;
                const channel = this._currentChannel;
                this._activeSubexecutor = new PullFromUpstream(executor, (value) => channel.k(value), void 0, [], (x, y) => channel.combineInners(x, y), (x, y) => channel.combineAll(x, y), (request) => channel.onPull(request), (value) => channel.onEmit(value));
                this._closeLastSubstream = void 0;
                this._currentChannel = void 0;
                break;
              }
              case OP_EMIT: {
                this._emitted = this._currentChannel.out;
                this._currentChannel = this._activeSubexecutor !== void 0 ? void 0 : void_3;
                result = Emit();
                break;
              }
              case OP_ENSURING: {
                this.runEnsuring(this._currentChannel);
                break;
              }
              case OP_FAIL: {
                result = this.doneHalt(this._currentChannel.error());
                break;
              }
              case OP_FOLD: {
                this._doneStack.push(this._currentChannel.k);
                this._currentChannel = this._currentChannel.channel;
                break;
              }
              case OP_FROM_EFFECT: {
                const effect3 = this._providedEnv === void 0 ? this._currentChannel.effect() : pipe(this._currentChannel.effect(), provide(this._providedEnv));
                result = fromEffect2(matchCauseEffect(effect3, {
                  onFailure: (cause) => {
                    const state = this.doneHalt(cause);
                    return state !== void 0 && isFromEffect(state) ? state.effect : _void;
                  },
                  onSuccess: (value) => {
                    const state = this.doneSucceed(value);
                    return state !== void 0 && isFromEffect(state) ? state.effect : _void;
                  }
                }));
                break;
              }
              case OP_PIPE_TO: {
                const previousInput = this._input;
                const leftExec = new _ChannelExecutor(this._currentChannel.left(), this._providedEnv, (effect3) => this._executeCloseLastSubstream(effect3));
                leftExec._input = previousInput;
                this._input = leftExec;
                this.addFinalizer((exit2) => {
                  const effect3 = this.restorePipe(exit2, previousInput);
                  return effect3 !== void 0 ? effect3 : _void;
                });
                this._currentChannel = this._currentChannel.right();
                break;
              }
              case OP_PROVIDE: {
                const previousEnv = this._providedEnv;
                this._providedEnv = this._currentChannel.context();
                this._currentChannel = this._currentChannel.inner;
                this.addFinalizer(() => sync3(() => {
                  this._providedEnv = previousEnv;
                }));
                break;
              }
              case OP_READ: {
                const read2 = this._currentChannel;
                result = Read(this._input, identity, (emitted) => {
                  try {
                    this._currentChannel = read2.more(emitted);
                  } catch (error) {
                    this._currentChannel = read2.done.onExit(die(error));
                  }
                  return void 0;
                }, (exit2) => {
                  const onExit = (exit3) => {
                    return read2.done.onExit(exit3);
                  };
                  this._currentChannel = onExit(exit2);
                  return void 0;
                });
                break;
              }
              case OP_SUCCEED: {
                result = this.doneSucceed(this._currentChannel.evaluate());
                break;
              }
              case OP_SUCCEED_NOW: {
                result = this.doneSucceed(this._currentChannel.terminal);
                break;
              }
              case OP_SUSPEND: {
                this._currentChannel = this._currentChannel.channel();
                break;
              }
            }
          }
        } catch (error) {
          this._currentChannel = failCause4(die2(error));
        }
      }
    }
    return result;
  }
  getDone() {
    return this._done;
  }
  getEmit() {
    return this._emitted;
  }
  cancelWith(exit2) {
    this._cancelled = exit2;
  }
  clearInProgressFinalizer() {
    this._inProgressFinalizer = void 0;
  }
  storeInProgressFinalizer(finalizer3) {
    this._inProgressFinalizer = finalizer3;
  }
  popAllFinalizers(exit2) {
    const finalizers = [];
    let next = this._doneStack.pop();
    while (next) {
      if (next._tag === "ContinuationFinalizer") {
        finalizers.push(next.finalizer);
      }
      next = this._doneStack.pop();
    }
    const effect3 = finalizers.length === 0 ? _void : runFinalizers(finalizers, exit2);
    this.storeInProgressFinalizer(effect3);
    return effect3;
  }
  popNextFinalizers() {
    const builder = [];
    while (this._doneStack.length !== 0) {
      const cont = this._doneStack[this._doneStack.length - 1];
      if (cont._tag === OP_CONTINUATION_K) {
        return builder;
      }
      builder.push(cont);
      this._doneStack.pop();
    }
    return builder;
  }
  restorePipe(exit2, prev) {
    const currInput = this._input;
    this._input = prev;
    if (currInput !== void 0) {
      const effect3 = currInput.close(exit2);
      return effect3;
    }
    return _void;
  }
  close(exit2) {
    let runInProgressFinalizers = void 0;
    const finalizer3 = this._inProgressFinalizer;
    if (finalizer3 !== void 0) {
      runInProgressFinalizers = pipe(finalizer3, ensuring3(sync3(() => this.clearInProgressFinalizer())));
    }
    let closeSelf = void 0;
    const selfFinalizers = this.popAllFinalizers(exit2);
    if (selfFinalizers !== void 0) {
      closeSelf = pipe(selfFinalizers, ensuring3(sync3(() => this.clearInProgressFinalizer())));
    }
    const closeSubexecutors = this._activeSubexecutor === void 0 ? void 0 : this._activeSubexecutor.close(exit2);
    if (closeSubexecutors === void 0 && runInProgressFinalizers === void 0 && closeSelf === void 0) {
      return void 0;
    }
    return pipe(
      exit(ifNotNull(closeSubexecutors)),
      zip2(exit(ifNotNull(runInProgressFinalizers))),
      zip2(exit(ifNotNull(closeSelf))),
      map7(([[exit1, exit22], exit3]) => pipe(exit1, zipRight2(exit22), zipRight2(exit3))),
      uninterruptible2,
      // TODO: remove
      flatMap3((exit3) => suspend2(() => exit3))
    );
  }
  doneSucceed(value) {
    if (this._doneStack.length === 0) {
      this._done = succeed2(value);
      this._currentChannel = void 0;
      return Done();
    }
    const head5 = this._doneStack[this._doneStack.length - 1];
    if (head5._tag === OP_CONTINUATION_K) {
      this._doneStack.pop();
      this._currentChannel = head5.onSuccess(value);
      return void 0;
    }
    const finalizers = this.popNextFinalizers();
    if (this._doneStack.length === 0) {
      this._doneStack = finalizers.reverse();
      this._done = succeed2(value);
      this._currentChannel = void 0;
      return Done();
    }
    const finalizerEffect = runFinalizers(finalizers.map((f) => f.finalizer), succeed2(value));
    this.storeInProgressFinalizer(finalizerEffect);
    const effect3 = pipe(finalizerEffect, ensuring3(sync3(() => this.clearInProgressFinalizer())), uninterruptible2, flatMap3(() => sync3(() => this.doneSucceed(value))));
    return fromEffect2(effect3);
  }
  doneHalt(cause) {
    if (this._doneStack.length === 0) {
      this._done = failCause(cause);
      this._currentChannel = void 0;
      return Done();
    }
    const head5 = this._doneStack[this._doneStack.length - 1];
    if (head5._tag === OP_CONTINUATION_K) {
      this._doneStack.pop();
      try {
        this._currentChannel = head5.onHalt(cause);
      } catch (error) {
        this._currentChannel = failCause4(die2(error));
      }
      return void 0;
    }
    const finalizers = this.popNextFinalizers();
    if (this._doneStack.length === 0) {
      this._doneStack = finalizers.reverse();
      this._done = failCause(cause);
      this._currentChannel = void 0;
      return Done();
    }
    const finalizerEffect = runFinalizers(finalizers.map((f) => f.finalizer), failCause(cause));
    this.storeInProgressFinalizer(finalizerEffect);
    const effect3 = pipe(finalizerEffect, ensuring3(sync3(() => this.clearInProgressFinalizer())), uninterruptible2, flatMap3(() => sync3(() => this.doneHalt(cause))));
    return fromEffect2(effect3);
  }
  processCancellation() {
    this._currentChannel = void 0;
    this._done = this._cancelled;
    this._cancelled = void 0;
    return Done();
  }
  runBracketOut(bracketOut) {
    const effect3 = uninterruptible2(matchCauseEffect(this.provide(bracketOut.acquire()), {
      onFailure: (cause) => sync3(() => {
        this._currentChannel = failCause4(cause);
      }),
      onSuccess: (out) => sync3(() => {
        this.addFinalizer((exit2) => this.provide(bracketOut.finalizer(out, exit2)));
        this._currentChannel = write(out);
      })
    }));
    return fromEffect2(effect3);
  }
  provide(effect3) {
    if (this._providedEnv === void 0) {
      return effect3;
    }
    return pipe(effect3, provide(this._providedEnv));
  }
  runEnsuring(ensuring10) {
    this.addFinalizer(ensuring10.finalizer);
    this._currentChannel = ensuring10.channel;
  }
  addFinalizer(f) {
    this._doneStack.push(new ContinuationFinalizerImpl(f));
  }
  runSubexecutor() {
    const subexecutor = this._activeSubexecutor;
    switch (subexecutor._tag) {
      case OP_PULL_FROM_CHILD: {
        return this.pullFromChild(subexecutor.childExecutor, subexecutor.parentSubexecutor, subexecutor.onEmit, subexecutor);
      }
      case OP_PULL_FROM_UPSTREAM: {
        return this.pullFromUpstream(subexecutor);
      }
      case OP_DRAIN_CHILD_EXECUTORS: {
        return this.drainChildExecutors(subexecutor);
      }
      case OP_EMIT3: {
        this._emitted = subexecutor.value;
        this._activeSubexecutor = subexecutor.next;
        return Emit();
      }
    }
  }
  replaceSubexecutor(nextSubExec) {
    this._currentChannel = void 0;
    this._activeSubexecutor = nextSubExec;
  }
  finishWithExit(exit2) {
    const state = match3(exit2, {
      onFailure: (cause) => this.doneHalt(cause),
      onSuccess: (value) => this.doneSucceed(value)
    });
    this._activeSubexecutor = void 0;
    return state === void 0 ? _void : effect(state);
  }
  finishSubexecutorWithCloseEffect(subexecutorDone, ...closeFuncs) {
    this.addFinalizer(() => pipe(closeFuncs, forEach2((closeFunc) => pipe(sync3(() => closeFunc(subexecutorDone)), flatMap3((closeEffect) => closeEffect !== void 0 ? closeEffect : _void)), {
      discard: true
    })));
    const state = pipe(subexecutorDone, match3({
      onFailure: (cause) => this.doneHalt(cause),
      onSuccess: (value) => this.doneSucceed(value)
    }));
    this._activeSubexecutor = void 0;
    return state;
  }
  applyUpstreamPullStrategy(upstreamFinished, queue, strategy) {
    switch (strategy._tag) {
      case OP_PULL_AFTER_NEXT: {
        const shouldPrepend = !upstreamFinished || queue.some((subexecutor) => subexecutor !== void 0);
        return [strategy.emitSeparator, shouldPrepend ? [void 0, ...queue] : queue];
      }
      case OP_PULL_AFTER_ALL_ENQUEUED: {
        const shouldEnqueue = !upstreamFinished || queue.some((subexecutor) => subexecutor !== void 0);
        return [strategy.emitSeparator, shouldEnqueue ? [...queue, void 0] : queue];
      }
    }
  }
  pullFromChild(childExecutor, parentSubexecutor, onEmitted, subexecutor) {
    return Read(childExecutor, identity, (emitted) => {
      const childExecutorDecision = onEmitted(emitted);
      switch (childExecutorDecision._tag) {
        case OP_CONTINUE: {
          break;
        }
        case OP_CLOSE: {
          this.finishWithDoneValue(childExecutor, parentSubexecutor, childExecutorDecision.value);
          break;
        }
        case OP_YIELD: {
          const modifiedParent = parentSubexecutor.enqueuePullFromChild(subexecutor);
          this.replaceSubexecutor(modifiedParent);
          break;
        }
      }
      this._activeSubexecutor = new Emit2(emitted, this._activeSubexecutor);
      return void 0;
    }, match3({
      onFailure: (cause) => {
        const state = this.handleSubexecutorFailure(childExecutor, parentSubexecutor, cause);
        return state === void 0 ? void 0 : effectOrUndefinedIgnored(state);
      },
      onSuccess: (doneValue) => {
        this.finishWithDoneValue(childExecutor, parentSubexecutor, doneValue);
        return void 0;
      }
    }));
  }
  finishWithDoneValue(childExecutor, parentSubexecutor, doneValue) {
    const subexecutor = parentSubexecutor;
    switch (subexecutor._tag) {
      case OP_PULL_FROM_UPSTREAM: {
        const modifiedParent = new PullFromUpstream(subexecutor.upstreamExecutor, subexecutor.createChild, subexecutor.lastDone !== void 0 ? subexecutor.combineChildResults(subexecutor.lastDone, doneValue) : doneValue, subexecutor.activeChildExecutors, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull, subexecutor.onEmit);
        this._closeLastSubstream = childExecutor.close(succeed2(doneValue));
        this.replaceSubexecutor(modifiedParent);
        break;
      }
      case OP_DRAIN_CHILD_EXECUTORS: {
        const modifiedParent = new DrainChildExecutors(subexecutor.upstreamExecutor, subexecutor.lastDone !== void 0 ? subexecutor.combineChildResults(subexecutor.lastDone, doneValue) : doneValue, subexecutor.activeChildExecutors, subexecutor.upstreamDone, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull);
        this._closeLastSubstream = childExecutor.close(succeed2(doneValue));
        this.replaceSubexecutor(modifiedParent);
        break;
      }
      default: {
        break;
      }
    }
  }
  handleSubexecutorFailure(childExecutor, parentSubexecutor, cause) {
    return this.finishSubexecutorWithCloseEffect(failCause(cause), (exit2) => parentSubexecutor.close(exit2), (exit2) => childExecutor.close(exit2));
  }
  pullFromUpstream(subexecutor) {
    if (subexecutor.activeChildExecutors.length === 0) {
      return this.performPullFromUpstream(subexecutor);
    }
    const activeChild = subexecutor.activeChildExecutors[0];
    const parentSubexecutor = new PullFromUpstream(subexecutor.upstreamExecutor, subexecutor.createChild, subexecutor.lastDone, subexecutor.activeChildExecutors.slice(1), subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull, subexecutor.onEmit);
    if (activeChild === void 0) {
      return this.performPullFromUpstream(parentSubexecutor);
    }
    this.replaceSubexecutor(new PullFromChild(activeChild.childExecutor, parentSubexecutor, activeChild.onEmit));
    return void 0;
  }
  performPullFromUpstream(subexecutor) {
    return Read(subexecutor.upstreamExecutor, (effect3) => {
      const closeLastSubstream = this._closeLastSubstream === void 0 ? _void : this._closeLastSubstream;
      this._closeLastSubstream = void 0;
      return pipe(this._executeCloseLastSubstream(closeLastSubstream), zipRight4(effect3));
    }, (emitted) => {
      if (this._closeLastSubstream !== void 0) {
        const closeLastSubstream = this._closeLastSubstream;
        this._closeLastSubstream = void 0;
        return pipe(this._executeCloseLastSubstream(closeLastSubstream), map7(() => {
          const childExecutor2 = new _ChannelExecutor(subexecutor.createChild(emitted), this._providedEnv, this._executeCloseLastSubstream);
          childExecutor2._input = this._input;
          const [emitSeparator2, updatedChildExecutors2] = this.applyUpstreamPullStrategy(false, subexecutor.activeChildExecutors, subexecutor.onPull(Pulled(emitted)));
          this._activeSubexecutor = new PullFromChild(childExecutor2, new PullFromUpstream(subexecutor.upstreamExecutor, subexecutor.createChild, subexecutor.lastDone, updatedChildExecutors2, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull, subexecutor.onEmit), subexecutor.onEmit);
          if (isSome(emitSeparator2)) {
            this._activeSubexecutor = new Emit2(emitSeparator2.value, this._activeSubexecutor);
          }
          return void 0;
        }));
      }
      const childExecutor = new _ChannelExecutor(subexecutor.createChild(emitted), this._providedEnv, this._executeCloseLastSubstream);
      childExecutor._input = this._input;
      const [emitSeparator, updatedChildExecutors] = this.applyUpstreamPullStrategy(false, subexecutor.activeChildExecutors, subexecutor.onPull(Pulled(emitted)));
      this._activeSubexecutor = new PullFromChild(childExecutor, new PullFromUpstream(subexecutor.upstreamExecutor, subexecutor.createChild, subexecutor.lastDone, updatedChildExecutors, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull, subexecutor.onEmit), subexecutor.onEmit);
      if (isSome(emitSeparator)) {
        this._activeSubexecutor = new Emit2(emitSeparator.value, this._activeSubexecutor);
      }
      return void 0;
    }, (exit2) => {
      if (subexecutor.activeChildExecutors.some((subexecutor2) => subexecutor2 !== void 0)) {
        const drain5 = new DrainChildExecutors(subexecutor.upstreamExecutor, subexecutor.lastDone, [void 0, ...subexecutor.activeChildExecutors], subexecutor.upstreamExecutor.getDone(), subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull);
        if (this._closeLastSubstream !== void 0) {
          const closeLastSubstream2 = this._closeLastSubstream;
          this._closeLastSubstream = void 0;
          return pipe(this._executeCloseLastSubstream(closeLastSubstream2), map7(() => this.replaceSubexecutor(drain5)));
        }
        this.replaceSubexecutor(drain5);
        return void 0;
      }
      const closeLastSubstream = this._closeLastSubstream;
      const state = this.finishSubexecutorWithCloseEffect(pipe(exit2, map4((a) => subexecutor.combineWithChildResult(subexecutor.lastDone, a))), () => closeLastSubstream, (exit3) => subexecutor.upstreamExecutor.close(exit3));
      return state === void 0 ? void 0 : (
        // NOTE: assuming finalizers cannot fail
        effectOrUndefinedIgnored(state)
      );
    });
  }
  drainChildExecutors(subexecutor) {
    if (subexecutor.activeChildExecutors.length === 0) {
      const lastClose = this._closeLastSubstream;
      if (lastClose !== void 0) {
        this.addFinalizer(() => succeed5(lastClose));
      }
      return this.finishSubexecutorWithCloseEffect(subexecutor.upstreamDone, () => lastClose, (exit2) => subexecutor.upstreamExecutor.close(exit2));
    }
    const activeChild = subexecutor.activeChildExecutors[0];
    const rest = subexecutor.activeChildExecutors.slice(1);
    if (activeChild === void 0) {
      const [emitSeparator, remainingExecutors] = this.applyUpstreamPullStrategy(true, rest, subexecutor.onPull(NoUpstream(rest.reduce((n, curr) => curr !== void 0 ? n + 1 : n, 0))));
      this.replaceSubexecutor(new DrainChildExecutors(subexecutor.upstreamExecutor, subexecutor.lastDone, remainingExecutors, subexecutor.upstreamDone, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull));
      if (isSome(emitSeparator)) {
        this._emitted = emitSeparator.value;
        return Emit();
      }
      return void 0;
    }
    const parentSubexecutor = new DrainChildExecutors(subexecutor.upstreamExecutor, subexecutor.lastDone, rest, subexecutor.upstreamDone, subexecutor.combineChildResults, subexecutor.combineWithChildResult, subexecutor.onPull);
    this.replaceSubexecutor(new PullFromChild(activeChild.childExecutor, parentSubexecutor, activeChild.onEmit));
    return void 0;
  }
};
var ifNotNull = (effect3) => effect3 !== void 0 ? effect3 : _void;
var runFinalizers = (finalizers, exit2) => {
  return pipe(forEach2(finalizers, (fin) => exit(fin(exit2))), map7((exits) => pipe(all(exits), getOrElse(() => void_2))), flatMap3((exit3) => suspend2(() => exit3)));
};
var readUpstream = (r, onSuccess, onFailure) => {
  const readStack = [r];
  const read2 = () => {
    const current2 = readStack.pop();
    if (current2 === void 0 || current2.upstream === void 0) {
      return dieMessage("Unexpected end of input for channel execution");
    }
    const state = current2.upstream.run();
    switch (state._tag) {
      case OP_EMIT2: {
        const emitEffect = current2.onEmit(current2.upstream.getEmit());
        if (readStack.length === 0) {
          if (emitEffect === void 0) {
            return suspend2(onSuccess);
          }
          return pipe(emitEffect, matchCauseEffect({
            onFailure,
            onSuccess
          }));
        }
        if (emitEffect === void 0) {
          return suspend2(() => read2());
        }
        return pipe(emitEffect, matchCauseEffect({
          onFailure,
          onSuccess: () => read2()
        }));
      }
      case OP_DONE: {
        const doneEffect = current2.onDone(current2.upstream.getDone());
        if (readStack.length === 0) {
          if (doneEffect === void 0) {
            return suspend2(onSuccess);
          }
          return pipe(doneEffect, matchCauseEffect({
            onFailure,
            onSuccess
          }));
        }
        if (doneEffect === void 0) {
          return suspend2(() => read2());
        }
        return pipe(doneEffect, matchCauseEffect({
          onFailure,
          onSuccess: () => read2()
        }));
      }
      case OP_FROM_EFFECT2: {
        readStack.push(current2);
        return pipe(current2.onEffect(state.effect), catchAllCause((cause) => suspend2(() => {
          const doneEffect = current2.onDone(failCause(cause));
          return doneEffect === void 0 ? _void : doneEffect;
        })), matchCauseEffect({
          onFailure,
          onSuccess: () => read2()
        }));
      }
      case OP_READ2: {
        readStack.push(current2);
        readStack.push(state);
        return suspend2(() => read2());
      }
    }
  };
  return read2();
};
var runIn = dual(2, (self, scope2) => {
  const run6 = (channelDeferred, scopeDeferred, scope3) => acquireUseRelease(sync3(() => new ChannelExecutor(self, void 0, identity)), (exec) => suspend2(() => runScopedInterpret(exec.run(), exec).pipe(intoDeferred(channelDeferred), zipRight4(_await2(channelDeferred)), zipLeft2(_await2(scopeDeferred)))), (exec, exit2) => {
    const finalize = exec.close(exit2);
    if (finalize === void 0) {
      return _void;
    }
    return tapErrorCause(finalize, (cause) => addFinalizer(scope3, failCause3(cause)));
  });
  return uninterruptibleMask2((restore) => all3([fork(scope2, sequential2), make8(), make8()]).pipe(flatMap3(([child, channelDeferred, scopeDeferred]) => restore(run6(channelDeferred, scopeDeferred, child)).pipe(forkIn2(scope2), flatMap3((fiber) => scope2.addFinalizer((exit2) => {
    const interruptors2 = isFailure(exit2) ? interruptors(exit2.cause) : void 0;
    return isDone(channelDeferred).pipe(flatMap3((isDone4) => isDone4 ? succeed4(scopeDeferred, void 0).pipe(zipRight4(_await(fiber)), zipRight4(inheritAll(fiber))) : succeed4(scopeDeferred, void 0).pipe(zipRight4(interruptors2 && size(interruptors2) > 0 ? interruptAs(fiber, combineAll(interruptors2)) : interrupt4(fiber)), zipRight4(inheritAll(fiber)))));
  }).pipe(zipRight4(restore(_await2(channelDeferred)))))))));
});
var runScopedInterpret = (channelState, exec) => {
  const op = channelState;
  switch (op._tag) {
    case OP_FROM_EFFECT2: {
      return pipe(op.effect, flatMap3(() => runScopedInterpret(exec.run(), exec)));
    }
    case OP_EMIT2: {
      return runScopedInterpret(exec.run(), exec);
    }
    case OP_DONE: {
      return suspend2(() => exec.getDone());
    }
    case OP_READ2: {
      return readUpstream(op, () => runScopedInterpret(exec.run(), exec), failCause3);
    }
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channelMergeDecision.js
var OP_DONE2 = "Done";
var OP_AWAIT = "Await";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/mergeDecision.js
var MergeDecisionSymbolKey = "effect/ChannelMergeDecision";
var MergeDecisionTypeId = Symbol.for(MergeDecisionSymbolKey);
var proto6 = {
  [MergeDecisionTypeId]: {
    _R: (_) => _,
    _E0: (_) => _,
    _Z0: (_) => _,
    _E: (_) => _,
    _Z: (_) => _
  }
};
var Done2 = (effect3) => {
  const op = Object.create(proto6);
  op._tag = OP_DONE2;
  op.effect = effect3;
  return op;
};
var Await = (f) => {
  const op = Object.create(proto6);
  op._tag = OP_AWAIT;
  op.f = f;
  return op;
};
var AwaitConst = (effect3) => Await(() => effect3);
var isMergeDecision = (u) => hasProperty(u, MergeDecisionTypeId);
var match8 = dual(2, (self, {
  onAwait,
  onDone: onDone3
}) => {
  const op = self;
  switch (op._tag) {
    case OP_DONE2:
      return onDone3(op.effect);
    case OP_AWAIT:
      return onAwait(op.f);
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channelMergeState.js
var OP_BOTH_RUNNING = "BothRunning";
var OP_LEFT_DONE = "LeftDone";
var OP_RIGHT_DONE = "RightDone";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/mergeState.js
var MergeStateSymbolKey = "effect/ChannelMergeState";
var MergeStateTypeId = Symbol.for(MergeStateSymbolKey);
var proto7 = {
  [MergeStateTypeId]: MergeStateTypeId
};
var BothRunning = (left2, right2) => {
  const op = Object.create(proto7);
  op._tag = OP_BOTH_RUNNING;
  op.left = left2;
  op.right = right2;
  return op;
};
var LeftDone = (f) => {
  const op = Object.create(proto7);
  op._tag = OP_LEFT_DONE;
  op.f = f;
  return op;
};
var RightDone = (f) => {
  const op = Object.create(proto7);
  op._tag = OP_RIGHT_DONE;
  op.f = f;
  return op;
};
var isMergeState = (u) => hasProperty(u, MergeStateTypeId);
var isBothRunning = (self) => {
  return self._tag === OP_BOTH_RUNNING;
};
var isLeftDone = (self) => {
  return self._tag === OP_LEFT_DONE;
};
var isRightDone = (self) => {
  return self._tag === OP_RIGHT_DONE;
};
var match9 = dual(2, (self, {
  onBothRunning,
  onLeftDone,
  onRightDone
}) => {
  switch (self._tag) {
    case OP_BOTH_RUNNING: {
      return onBothRunning(self.left, self.right);
    }
    case OP_LEFT_DONE: {
      return onLeftDone(self.f);
    }
    case OP_RIGHT_DONE: {
      return onRightDone(self.f);
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/channelMergeStrategy.js
var OP_BACK_PRESSURE = "BackPressure";
var OP_BUFFER_SLIDING = "BufferSliding";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/mergeStrategy.js
var MergeStrategySymbolKey = "effect/ChannelMergeStrategy";
var MergeStrategyTypeId = Symbol.for(MergeStrategySymbolKey);
var proto8 = {
  [MergeStrategyTypeId]: MergeStrategyTypeId
};
var BackPressure = (_) => {
  const op = Object.create(proto8);
  op._tag = OP_BACK_PRESSURE;
  return op;
};
var BufferSliding = (_) => {
  const op = Object.create(proto8);
  op._tag = OP_BUFFER_SLIDING;
  return op;
};
var isMergeStrategy = (u) => hasProperty(u, MergeStrategyTypeId);
var isBackPressure = (self) => self._tag === OP_BACK_PRESSURE;
var isBufferSliding = (self) => self._tag === OP_BUFFER_SLIDING;
var match10 = dual(2, (self, {
  onBackPressure,
  onBufferSliding
}) => {
  switch (self._tag) {
    case OP_BACK_PRESSURE: {
      return onBackPressure();
    }
    case OP_BUFFER_SLIDING: {
      return onBufferSliding();
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel/singleProducerAsyncInput.js
var OP_STATE_EMPTY = "Empty";
var OP_STATE_EMIT = "Emit";
var OP_STATE_ERROR = "Error";
var OP_STATE_DONE = "Done";
var stateEmpty = (notifyProducer) => ({
  _tag: OP_STATE_EMPTY,
  notifyProducer
});
var stateEmit = (notifyConsumers) => ({
  _tag: OP_STATE_EMIT,
  notifyConsumers
});
var stateError = (cause) => ({
  _tag: OP_STATE_ERROR,
  cause
});
var stateDone = (done4) => ({
  _tag: OP_STATE_DONE,
  done: done4
});
var SingleProducerAsyncInputImpl = class {
  ref;
  constructor(ref) {
    this.ref = ref;
  }
  awaitRead() {
    return flatten2(modify(this.ref, (state) => state._tag === OP_STATE_EMPTY ? [_await2(state.notifyProducer), state] : [_void, state]));
  }
  get close() {
    return fiberIdWith((fiberId3) => this.error(interrupt3(fiberId3)));
  }
  done(value) {
    return flatten2(modify(this.ref, (state) => {
      switch (state._tag) {
        case OP_STATE_EMPTY: {
          return [_await2(state.notifyProducer), state];
        }
        case OP_STATE_EMIT: {
          return [forEach2(state.notifyConsumers, (deferred) => succeed4(deferred, left(value)), {
            discard: true
          }), stateDone(value)];
        }
        case OP_STATE_ERROR: {
          return [interrupt5, state];
        }
        case OP_STATE_DONE: {
          return [interrupt5, state];
        }
      }
    }));
  }
  emit(element) {
    return flatMap3(make8(), (deferred) => flatten2(modify(this.ref, (state) => {
      switch (state._tag) {
        case OP_STATE_EMPTY: {
          return [_await2(state.notifyProducer), state];
        }
        case OP_STATE_EMIT: {
          const notifyConsumer = state.notifyConsumers[0];
          const notifyConsumers = state.notifyConsumers.slice(1);
          if (notifyConsumer !== void 0) {
            return [succeed4(notifyConsumer, right(element)), notifyConsumers.length === 0 ? stateEmpty(deferred) : stateEmit(notifyConsumers)];
          }
          throw new Error("Bug: Channel.SingleProducerAsyncInput.emit - Queue was empty! please report an issue at https://github.com/Effect-TS/effect/issues");
        }
        case OP_STATE_ERROR: {
          return [interrupt5, state];
        }
        case OP_STATE_DONE: {
          return [interrupt5, state];
        }
      }
    })));
  }
  error(cause) {
    return flatten2(modify(this.ref, (state) => {
      switch (state._tag) {
        case OP_STATE_EMPTY: {
          return [_await2(state.notifyProducer), state];
        }
        case OP_STATE_EMIT: {
          return [forEach2(state.notifyConsumers, (deferred) => failCause2(deferred, cause), {
            discard: true
          }), stateError(cause)];
        }
        case OP_STATE_ERROR: {
          return [interrupt5, state];
        }
        case OP_STATE_DONE: {
          return [interrupt5, state];
        }
      }
    }));
  }
  get take() {
    return this.takeWith((cause) => failCause(map5(cause, left)), (elem) => succeed2(elem), (done4) => fail(right(done4)));
  }
  takeWith(onError4, onElement, onDone3) {
    return flatMap3(make8(), (deferred) => flatten2(modify(this.ref, (state) => {
      switch (state._tag) {
        case OP_STATE_EMPTY: {
          return [zipRight4(succeed4(state.notifyProducer, void 0), matchCause(_await2(deferred), {
            onFailure: onError4,
            onSuccess: match({
              onLeft: onDone3,
              onRight: onElement
            })
          })), stateEmit([deferred])];
        }
        case OP_STATE_EMIT: {
          return [matchCause(_await2(deferred), {
            onFailure: onError4,
            onSuccess: match({
              onLeft: onDone3,
              onRight: onElement
            })
          }), stateEmit([...state.notifyConsumers, deferred])];
        }
        case OP_STATE_ERROR: {
          return [succeed5(onError4(state.cause)), state];
        }
        case OP_STATE_DONE: {
          return [succeed5(onDone3(state.done)), state];
        }
      }
    })));
  }
};
var make12 = () => pipe(make8(), flatMap3((deferred) => make5(stateEmpty(deferred))), map7((ref) => new SingleProducerAsyncInputImpl(ref)));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/channel.js
var acquireUseRelease2 = (acquire, use, release) => flatMap4(fromEffect(make5(() => _void)), (ref) => pipe(fromEffect(uninterruptible2(tap2(acquire, (a) => set3(ref, (exit2) => release(a, exit2))))), flatMap4(use), ensuringWith((exit2) => flatMap3(get3(ref), (f) => f(exit2)))));
var as4 = dual(2, (self, value) => map8(self, () => value));
var asVoid4 = (self) => map8(self, constVoid);
var buffer = (options) => suspend3(() => {
  const doBuffer = (empty7, isEmpty11, ref) => unwrap(modify(ref, (inElem) => isEmpty11(inElem) ? [readWith({
    onInput: (input) => flatMap4(write(input), () => doBuffer(empty7, isEmpty11, ref)),
    onFailure: (error) => fail5(error),
    onDone: (done4) => succeedNow(done4)
  }), inElem] : [flatMap4(write(inElem), () => doBuffer(empty7, isEmpty11, ref)), empty7]));
  return doBuffer(options.empty, options.isEmpty, options.ref);
});
var bufferChunk = (ref) => buffer({
  empty: empty2(),
  isEmpty,
  ref
});
var catchAll3 = dual(2, (self, f) => catchAllCause2(self, (cause) => match(failureOrCause(cause), {
  onLeft: f,
  onRight: failCause4
})));
var concatMap = dual(2, (self, f) => concatMapWith(self, f, () => void 0, () => void 0));
var collect = dual(2, (self, pf) => {
  const collector = readWith({
    onInput: (out) => match2(pf(out), {
      onNone: () => collector,
      onSome: (out2) => flatMap4(write(out2), () => collector)
    }),
    onFailure: fail5,
    onDone: succeedNow
  });
  return pipeTo(self, collector);
});
var concatOut = (self) => concatAll(self);
var mapInput2 = dual(2, (self, f) => {
  const reader = readWith({
    onInput: (inElem) => flatMap4(write(inElem), () => reader),
    onFailure: fail5,
    onDone: (done4) => succeedNow(f(done4))
  });
  return pipeTo(reader, self);
});
var mapInputEffect2 = dual(2, (self, f) => {
  const reader = readWith({
    onInput: (inElem) => flatMap4(write(inElem), () => reader),
    onFailure: fail5,
    onDone: (done4) => fromEffect(f(done4))
  });
  return pipeTo(reader, self);
});
var mapInputError = dual(2, (self, f) => {
  const reader = readWith({
    onInput: (inElem) => flatMap4(write(inElem), () => reader),
    onFailure: (error) => fail5(f(error)),
    onDone: succeedNow
  });
  return pipeTo(reader, self);
});
var mapInputErrorEffect = dual(2, (self, f) => {
  const reader = readWith({
    onInput: (inElem) => flatMap4(write(inElem), () => reader),
    onFailure: (error) => fromEffect(f(error)),
    onDone: succeedNow
  });
  return pipeTo(reader, self);
});
var mapInputIn = dual(2, (self, f) => {
  const reader = readWith({
    onInput: (inElem) => flatMap4(write(f(inElem)), () => reader),
    onFailure: fail5,
    onDone: succeedNow
  });
  return pipeTo(reader, self);
});
var mapInputInEffect = dual(2, (self, f) => {
  const reader = readWith({
    onInput: (inElem) => flatMap4(flatMap4(fromEffect(f(inElem)), write), () => reader),
    onFailure: fail5,
    onDone: succeedNow
  });
  return pipeTo(reader, self);
});
var doneCollect = (self) => suspend3(() => {
  const builder = [];
  return pipe(pipeTo(self, doneCollectReader(builder)), flatMap4((outDone) => succeed6([unsafeFromArray(builder), outDone])));
});
var doneCollectReader = (builder) => {
  return readWith({
    onInput: (outElem) => flatMap4(sync4(() => {
      builder.push(outElem);
    }), () => doneCollectReader(builder)),
    onFailure: fail5,
    onDone: succeed6
  });
};
var drain = (self) => {
  const drainer = readWithCause({
    onInput: () => drainer,
    onFailure: failCause4,
    onDone: succeed6
  });
  return pipeTo(self, drainer);
};
var emitCollect = (self) => flatMap4(doneCollect(self), write);
var ensuring4 = dual(2, (self, finalizer3) => ensuringWith(self, () => finalizer3));
var context3 = () => fromEffect(context());
var contextWith2 = (f) => map8(context3(), f);
var contextWithChannel = (f) => flatMap4(context3(), f);
var contextWithEffect = (f) => mapEffect2(context3(), f);
var flatten3 = (self) => flatMap4(self, identity);
var foldChannel = dual(2, (self, options) => foldCauseChannel(self, {
  onFailure: (cause) => {
    const either6 = failureOrCause(cause);
    switch (either6._tag) {
      case "Left": {
        return options.onFailure(either6.left);
      }
      case "Right": {
        return failCause4(either6.right);
      }
    }
  },
  onSuccess: options.onSuccess
}));
var fromEither = (either6) => suspend3(() => match(either6, {
  onLeft: fail5,
  onRight: succeed6
}));
var fromInput = (input) => unwrap(input.takeWith(failCause4, (elem) => flatMap4(write(elem), () => fromInput(input)), succeed6));
var fromPubSub = (pubsub) => unwrapScoped(map7(subscribe2(pubsub), fromQueue));
var fromPubSubScoped = (pubsub) => map7(subscribe2(pubsub), fromQueue);
var fromOption = (option2) => suspend3(() => match2(option2, {
  onNone: () => fail5(none()),
  onSome: succeed6
}));
var fromQueue = (queue) => suspend3(() => fromQueueInternal(queue));
var fromQueueInternal = (queue) => pipe(fromEffect(take3(queue)), flatMap4(match({
  onLeft: match3({
    onFailure: failCause4,
    onSuccess: succeedNow
  }),
  onRight: (elem) => flatMap4(write(elem), () => fromQueueInternal(queue))
})));
var identityChannel = () => readWith({
  onInput: (input) => flatMap4(write(input), () => identityChannel()),
  onFailure: fail5,
  onDone: succeedNow
});
var interruptWhen = dual(2, (self, effect3) => mergeWith(self, {
  other: fromEffect(effect3),
  onSelfDone: (selfDone) => Done2(suspend2(() => selfDone)),
  onOtherDone: (effectDone) => Done2(suspend2(() => effectDone))
}));
var interruptWhenDeferred = dual(2, (self, deferred) => interruptWhen(self, _await2(deferred)));
var map8 = dual(2, (self, f) => flatMap4(self, (a) => sync4(() => f(a))));
var mapEffect2 = dual(2, (self, f) => flatMap4(self, (z) => fromEffect(f(z))));
var mapError2 = dual(2, (self, f) => mapErrorCause(self, map5(f)));
var mapErrorCause = dual(2, (self, f) => catchAllCause2(self, (cause) => failCause4(f(cause))));
var mapOut = dual(2, (self, f) => {
  const reader = readWith({
    onInput: (outElem) => flatMap4(write(f(outElem)), () => reader),
    onFailure: fail5,
    onDone: succeedNow
  });
  return pipeTo(self, reader);
});
var mapOutEffect = dual(2, (self, f) => {
  const reader = readWithCause({
    onInput: (outElem) => pipe(fromEffect(f(outElem)), flatMap4(write), flatMap4(() => reader)),
    onFailure: failCause4,
    onDone: succeedNow
  });
  return pipeTo(self, reader);
});
var mapOutEffectPar = dual(3, (self, f, n) => unwrapScopedWith((scope2) => gen(function* () {
  const input = yield* make12();
  const queueReader = fromInput(input);
  const queue = yield* bounded3(n);
  yield* addFinalizer(scope2, shutdown2(queue));
  const errorSignal = yield* make8();
  const withPermits = n === Number.POSITIVE_INFINITY ? (_) => identity : (yield* makeSemaphore(n)).withPermits;
  const pull = yield* queueReader.pipe(pipeTo(self), toPullIn(scope2));
  yield* pull.pipe(matchCauseEffect({
    onFailure: (cause) => offer3(queue, failCause3(cause)),
    onSuccess: match({
      onLeft: (outDone) => zipRight4(interruptible2(withPermits(n)(_void)), asVoid3(offer3(queue, succeed5(left(outDone))))),
      onRight: (outElem) => gen(function* () {
        const deferred = yield* make8();
        const latch = yield* make8();
        yield* offer3(queue, map7(_await2(deferred), right));
        yield* succeed4(latch, void 0).pipe(zipRight4(uninterruptibleMask2((restore) => exit(restore(_await2(errorSignal))).pipe(raceFirst(exit(restore(f(outElem)))), flatMap3(identity))).pipe(tapErrorCause((cause) => failCause2(errorSignal, cause)), intoDeferred(deferred))), withPermits(1), forkIn2(scope2));
        yield* _await2(latch);
      })
    })
  }), forever2, interruptible2, forkIn2(scope2));
  const consumer = unwrap(matchCause(flatten2(take3(queue)), {
    onFailure: failCause4,
    onSuccess: match({
      onLeft: succeedNow,
      onRight: (outElem) => flatMap4(write(outElem), () => consumer)
    })
  }));
  return embedInput(consumer, input);
})));
var mergeAll = (options) => {
  return (channels) => mergeAllWith(options)(channels, constVoid);
};
var mergeAllUnbounded = (channels) => mergeAllWith({
  concurrency: "unbounded"
})(channels, constVoid);
var mergeAllUnboundedWith = (channels, f) => mergeAllWith({
  concurrency: "unbounded"
})(channels, f);
var mergeAllWith = ({
  bufferSize = 16,
  concurrency,
  mergeStrategy = BackPressure()
}) => (channels, f) => unwrapScopedWith((scope2) => gen(function* () {
  const concurrencyN = concurrency === "unbounded" ? Number.MAX_SAFE_INTEGER : concurrency;
  const input = yield* make12();
  const queueReader = fromInput(input);
  const queue = yield* bounded3(bufferSize);
  yield* addFinalizer(scope2, shutdown2(queue));
  const cancelers = yield* unbounded3();
  yield* addFinalizer(scope2, shutdown2(cancelers));
  const lastDone = yield* make5(none());
  const errorSignal = yield* make8();
  const withPermits = (yield* makeSemaphore(concurrencyN)).withPermits;
  const pull = yield* toPullIn(pipeTo(queueReader, channels), scope2);
  function evaluatePull(pull2) {
    return pull2.pipe(flatMap3(match({
      onLeft: (done4) => succeed5(some(done4)),
      onRight: (outElem) => as3(offer3(queue, succeed5(right(outElem))), none())
    })), repeat({
      until: (_) => isSome(_)
    }), flatMap3((outDone) => update(lastDone, match2({
      onNone: () => some(outDone.value),
      onSome: (lastDone2) => some(f(lastDone2, outDone.value))
    }))), catchAllCause((cause) => isInterrupted(cause) ? failCause3(cause) : offer3(queue, failCause3(cause)).pipe(zipRight4(succeed4(errorSignal, void 0)), asVoid3)));
  }
  yield* pull.pipe(matchCauseEffect({
    onFailure: (cause) => offer3(queue, failCause3(cause)).pipe(zipRight4(succeed5(false))),
    onSuccess: match({
      onLeft: (outDone) => raceWith(interruptible2(_await2(errorSignal)), interruptible2(withPermits(concurrencyN)(_void)), {
        onSelfDone: (_, permitAcquisition) => as3(interrupt4(permitAcquisition), false),
        onOtherDone: (_, failureAwait) => zipRight4(interrupt4(failureAwait), get3(lastDone).pipe(flatMap3(match2({
          onNone: () => offer3(queue, succeed5(left(outDone))),
          onSome: (lastDone2) => offer3(queue, succeed5(left(f(lastDone2, outDone))))
        })), as3(false)))
      }),
      onRight: (channel) => match10(mergeStrategy, {
        onBackPressure: () => gen(function* () {
          const latch = yield* make8();
          const raceEffects = scopedWith((scope3) => toPullIn(pipeTo(queueReader, channel), scope3).pipe(flatMap3((pull2) => race(exit(evaluatePull(pull2)), exit(interruptible2(_await2(errorSignal))))), flatMap3(identity)));
          yield* succeed4(latch, void 0).pipe(zipRight4(raceEffects), withPermits(1), forkIn2(scope2));
          yield* _await2(latch);
          const errored = yield* isDone(errorSignal);
          return !errored;
        }),
        onBufferSliding: () => gen(function* () {
          const canceler = yield* make8();
          const latch = yield* make8();
          const size10 = yield* size3(cancelers);
          yield* take3(cancelers).pipe(flatMap3((canceler2) => succeed4(canceler2, void 0)), when(() => size10 >= concurrencyN));
          yield* offer3(cancelers, canceler);
          const raceEffects = scopedWith((scope3) => toPullIn(pipeTo(queueReader, channel), scope3).pipe(flatMap3((pull2) => exit(evaluatePull(pull2)).pipe(race(exit(interruptible2(_await2(errorSignal)))), race(exit(interruptible2(_await2(canceler)))))), flatMap3(identity)));
          yield* succeed4(latch, void 0).pipe(zipRight4(raceEffects), withPermits(1), forkIn2(scope2));
          yield* _await2(latch);
          const errored = yield* isDone(errorSignal);
          return !errored;
        })
      })
    })
  }), repeat({
    while: (_) => _
  }), forkIn2(scope2));
  const consumer = pipe(take3(queue), flatten2, matchCause({
    onFailure: failCause4,
    onSuccess: match({
      onLeft: succeedNow,
      onRight: (outElem) => flatMap4(write(outElem), () => consumer)
    })
  }), unwrap);
  return embedInput(consumer, input);
}));
var mergeMap = dual(3, (self, f, options) => mergeAll(options)(mapOut(self, f)));
var mergeOut = dual(2, (self, n) => mergeAll({
  concurrency: n
})(mapOut(self, identity)));
var mergeOutWith = dual(3, (self, n, f) => mergeAllWith({
  concurrency: n
})(mapOut(self, identity), f));
var mergeWith = dual(2, (self, options) => {
  function merge7(scope2) {
    return gen(function* () {
      const input = yield* make12();
      const queueReader = fromInput(input);
      const pullL = yield* toPullIn(pipeTo(queueReader, self), scope2);
      const pullR = yield* toPullIn(pipeTo(queueReader, options.other), scope2);
      function handleSide(exit2, fiber, pull) {
        return (done4, both, single) => {
          function onDecision3(decision) {
            const op = decision;
            if (op._tag === OP_DONE2) {
              return succeed5(fromEffect(zipRight4(interrupt4(fiber), op.effect)));
            }
            return map7(_await(fiber), match3({
              onFailure: (cause) => fromEffect(op.f(failCause(cause))),
              onSuccess: match({
                onLeft: (done5) => fromEffect(op.f(succeed2(done5))),
                onRight: (elem) => zipRight5(write(elem), go(single(op.f)))
              })
            }));
          }
          return match3(exit2, {
            onFailure: (cause) => onDecision3(done4(failCause(cause))),
            onSuccess: match({
              onLeft: (z) => onDecision3(done4(succeed2(z))),
              onRight: (elem) => succeed5(flatMap4(write(elem), () => flatMap4(fromEffect(forkIn2(interruptible2(pull), scope2)), (leftFiber) => go(both(leftFiber, fiber)))))
            })
          });
        };
      }
      function go(state) {
        switch (state._tag) {
          case OP_BOTH_RUNNING: {
            const leftJoin = interruptible2(join(state.left));
            const rightJoin = interruptible2(join(state.right));
            return unwrap(raceWith(leftJoin, rightJoin, {
              onSelfDone: (leftExit, rf) => zipRight4(interrupt4(rf), handleSide(leftExit, state.right, pullL)(options.onSelfDone, BothRunning, (f) => LeftDone(f))),
              onOtherDone: (rightExit, lf) => zipRight4(interrupt4(lf), handleSide(rightExit, state.left, pullR)(options.onOtherDone, (left2, right2) => BothRunning(right2, left2), (f) => RightDone(f)))
            }));
          }
          case OP_LEFT_DONE: {
            return unwrap(map7(exit(pullR), match3({
              onFailure: (cause) => fromEffect(state.f(failCause(cause))),
              onSuccess: match({
                onLeft: (done4) => fromEffect(state.f(succeed2(done4))),
                onRight: (elem) => flatMap4(write(elem), () => go(LeftDone(state.f)))
              })
            })));
          }
          case OP_RIGHT_DONE: {
            return unwrap(map7(exit(pullL), match3({
              onFailure: (cause) => fromEffect(state.f(failCause(cause))),
              onSuccess: match({
                onLeft: (done4) => fromEffect(state.f(succeed2(done4))),
                onRight: (elem) => flatMap4(write(elem), () => go(RightDone(state.f)))
              })
            })));
          }
        }
      }
      return fromEffect(withFiberRuntime2((parent) => {
        const inherit = withFiberRuntime2((state) => {
          ;
          state.transferChildren(parent.scope());
          return _void;
        });
        const leftFiber = interruptible2(pullL).pipe(ensuring3(inherit), forkIn2(scope2));
        const rightFiber = interruptible2(pullR).pipe(ensuring3(inherit), forkIn2(scope2));
        return zipWith3(leftFiber, rightFiber, (left2, right2) => BothRunning(left2, right2));
      })).pipe(flatMap4(go), embedInput(input));
    });
  }
  return unwrapScopedWith(merge7);
});
var never2 = fromEffect(never);
var orDie2 = dual(2, (self, error) => orDieWith(self, error));
var orDieWith = dual(2, (self, f) => catchAll3(self, (e) => failCauseSync2(() => die2(f(e)))));
var orElse3 = dual(2, (self, that) => catchAll3(self, that));
var pipeToOrFail = dual(2, (self, that) => suspend3(() => {
  let channelException = void 0;
  const reader = readWith({
    onInput: (outElem) => flatMap4(write(outElem), () => reader),
    onFailure: (outErr) => {
      channelException = ChannelException(outErr);
      return failCause4(die2(channelException));
    },
    onDone: succeedNow
  });
  const writer = readWithCause({
    onInput: (outElem) => pipe(write(outElem), flatMap4(() => writer)),
    onFailure: (cause) => isDieType(cause) && isChannelException(cause.defect) && equals(cause.defect, channelException) ? fail5(cause.defect.error) : failCause4(cause),
    onDone: succeedNow
  });
  return pipeTo(pipeTo(pipeTo(self, reader), that), writer);
}));
var provideService3 = dual(3, (self, tag, service) => {
  return flatMap4(context3(), (context8) => provideContext2(self, add(context8, tag, service)));
});
var provideLayer = dual(2, (self, layer) => unwrapScopedWith((scope2) => map7(buildWithScope(layer, scope2), (context8) => provideContext2(self, context8))));
var mapInputContext2 = dual(2, (self, f) => contextWithChannel((context8) => provideContext2(self, f(context8))));
var provideSomeLayer = dual(2, (self, layer) => (
  // @ts-expect-error
  provideLayer(self, merge3(context2(), layer))
));
var read = () => readOrFail(none());
var repeated = (self) => flatMap4(self, () => repeated(self));
var run2 = (self) => scopedWith((scope2) => runIn(self, scope2));
var runCollect = (self) => run2(collectElements(self));
var runDrain = (self) => run2(drain(self));
var runScoped = (self) => scopeWith((scope2) => runIn(self, scope2));
var scoped2 = (effect3) => unwrap(uninterruptibleMask2((restore) => map7(make6(), (scope2) => acquireReleaseOut(tapErrorCause(restore(extend(effect3, scope2)), (cause) => close(scope2, failCause(cause))), (_, exit2) => close(scope2, exit2)))));
var scopedWith2 = (f) => unwrapScoped(map7(scope, (scope2) => flatMap4(fromEffect(f(scope2)), write)));
var splitLines = () => suspend3(() => {
  let stringBuilder = "";
  let midCRLF = false;
  const splitLinesChunk = (chunk2) => {
    const chunkBuilder = [];
    map2(chunk2, (str) => {
      if (str.length !== 0) {
        let from = 0;
        let indexOfCR = str.indexOf("\r");
        let indexOfLF = str.indexOf("\n");
        if (midCRLF) {
          if (indexOfLF === 0) {
            chunkBuilder.push(stringBuilder);
            stringBuilder = "";
            from = 1;
            indexOfLF = str.indexOf("\n", from);
          } else {
            stringBuilder = stringBuilder + "\r";
          }
          midCRLF = false;
        }
        while (indexOfCR !== -1 || indexOfLF !== -1) {
          if (indexOfCR === -1 || indexOfLF !== -1 && indexOfLF < indexOfCR) {
            if (stringBuilder.length === 0) {
              chunkBuilder.push(str.substring(from, indexOfLF));
            } else {
              chunkBuilder.push(stringBuilder + str.substring(from, indexOfLF));
              stringBuilder = "";
            }
            from = indexOfLF + 1;
            indexOfLF = str.indexOf("\n", from);
          } else {
            if (str.length === indexOfCR + 1) {
              midCRLF = true;
              indexOfCR = -1;
            } else {
              if (indexOfLF === indexOfCR + 1) {
                if (stringBuilder.length === 0) {
                  chunkBuilder.push(str.substring(from, indexOfCR));
                } else {
                  stringBuilder = stringBuilder + str.substring(from, indexOfCR);
                  chunkBuilder.push(stringBuilder);
                  stringBuilder = "";
                }
                from = indexOfCR + 2;
                indexOfCR = str.indexOf("\r", from);
                indexOfLF = str.indexOf("\n", from);
              } else {
                indexOfCR = str.indexOf("\r", indexOfCR + 1);
              }
            }
          }
        }
        if (midCRLF) {
          stringBuilder = stringBuilder + str.substring(from, str.length - 1);
        } else {
          stringBuilder = stringBuilder + str.substring(from, str.length);
        }
      }
    });
    return unsafeFromArray(chunkBuilder);
  };
  const loop2 = readWithCause({
    onInput: (input) => {
      const out = splitLinesChunk(input);
      return isEmpty(out) ? loop2 : flatMap4(write(out), () => loop2);
    },
    onFailure: (cause) => stringBuilder.length === 0 ? failCause4(cause) : flatMap4(write(of(stringBuilder)), () => failCause4(cause)),
    onDone: (done4) => stringBuilder.length === 0 ? succeed6(done4) : flatMap4(write(of(stringBuilder)), () => succeed6(done4))
  });
  return loop2;
});
var toPubSub = (pubsub) => toQueue(pubsub);
var toPull = (self) => flatMap3(scope, (scope2) => toPullIn(self, scope2));
var toPullIn = dual(2, (self, scope2) => zip2(sync3(() => new ChannelExecutor(self, void 0, identity)), runtime()).pipe(tap2(([executor, runtime2]) => addFinalizerExit(scope2, (exit2) => {
  const finalizer3 = executor.close(exit2);
  return finalizer3 !== void 0 ? provide(finalizer3, runtime2) : _void;
})), uninterruptible2, map7(([executor]) => suspend2(() => interpretToPull(executor.run(), executor)))));
var interpretToPull = (channelState, exec) => {
  const state = channelState;
  switch (state._tag) {
    case OP_DONE: {
      return match3(exec.getDone(), {
        onFailure: failCause3,
        onSuccess: (done4) => succeed5(left(done4))
      });
    }
    case OP_EMIT2: {
      return succeed5(right(exec.getEmit()));
    }
    case OP_FROM_EFFECT2: {
      return pipe(state.effect, flatMap3(() => interpretToPull(exec.run(), exec)));
    }
    case OP_READ2: {
      return readUpstream(state, () => interpretToPull(exec.run(), exec), (cause) => failCause3(cause));
    }
  }
};
var toQueue = (queue) => suspend3(() => toQueueInternal(queue));
var toQueueInternal = (queue) => {
  return readWithCause({
    onInput: (elem) => flatMap4(fromEffect(offer3(queue, right(elem))), () => toQueueInternal(queue)),
    onFailure: (cause) => fromEffect(offer3(queue, left(failCause(cause)))),
    onDone: (done4) => fromEffect(offer3(queue, left(succeed2(done4))))
  });
};
var unwrap = (channel) => flatten3(fromEffect(channel));
var unwrapScoped = (self) => concatAllWith(scoped2(self), (d, _) => d, (d, _) => d);
var unwrapScopedWith = (f) => concatAllWith(scopedWith2(f), (d, _) => d, (d, _) => d);
var updateService = dual(3, (self, tag, f) => mapInputContext2(self, (context8) => merge2(context8, make3(tag, f(unsafeGet2(context8, tag))))));
var withSpan = function() {
  const dataFirst = typeof arguments[0] !== "string";
  const name = dataFirst ? arguments[1] : arguments[0];
  const options = addSpanStackTrace(dataFirst ? arguments[2] : arguments[1]);
  const acquire = all3([makeSpan(name, options), context(), clock, get4(currentTracerTimingEnabled2)]);
  if (dataFirst) {
    const self = arguments[0];
    return acquireUseRelease2(acquire, ([span2, context8]) => provideContext2(self, add(context8, spanTag, span2)), ([span2, , clock2, timingEnabled], exit2) => endSpan(span2, exit2, clock2, timingEnabled));
  }
  return (self) => acquireUseRelease2(acquire, ([span2, context8]) => provideContext2(self, add(context8, spanTag, span2)), ([span2, , clock2, timingEnabled], exit2) => endSpan(span2, exit2, clock2, timingEnabled));
};
var writeAll = (...outs) => writeChunk(fromIterable2(outs));
var writeChunk = (outs) => writeChunkWriter(0, outs.length, outs);
var writeChunkWriter = (idx, len, chunk2) => {
  return idx === len ? void_3 : pipe(write(pipe(chunk2, unsafeGet(idx))), flatMap4(() => writeChunkWriter(idx + 1, len, chunk2)));
};
var zip3 = dual((args) => isChannel(args[1]), (self, that, options) => options?.concurrent ? mergeWith(self, {
  other: that,
  onSelfDone: (exit1) => Await((exit2) => suspend2(() => zip(exit1, exit2))),
  onOtherDone: (exit2) => Await((exit1) => suspend2(() => zip(exit1, exit2)))
}) : flatMap4(self, (a) => map8(that, (b) => [a, b])));
var zipLeft3 = dual((args) => isChannel(args[1]), (self, that, options) => options?.concurrent ? map8(zip3(self, that, {
  concurrent: true
}), (tuple) => tuple[0]) : flatMap4(self, (z) => as4(that, z)));
var zipRight5 = dual((args) => isChannel(args[1]), (self, that, options) => options?.concurrent ? map8(zip3(self, that, {
  concurrent: true
}), (tuple) => tuple[1]) : flatMap4(self, () => that));
var ChannelExceptionTypeId = Symbol.for("effect/Channel/ChannelException");
var ChannelException = (error) => ({
  _tag: "ChannelException",
  [ChannelExceptionTypeId]: ChannelExceptionTypeId,
  error
});
var isChannelException = (u) => hasProperty(u, ChannelExceptionTypeId);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/MergeDecision.js
var MergeDecision_exports = {};
__export(MergeDecision_exports, {
  Await: () => Await2,
  AwaitConst: () => AwaitConst2,
  Done: () => Done3,
  MergeDecisionTypeId: () => MergeDecisionTypeId2,
  isMergeDecision: () => isMergeDecision2,
  match: () => match11
});
var MergeDecisionTypeId2 = MergeDecisionTypeId;
var Done3 = Done2;
var Await2 = Await;
var AwaitConst2 = AwaitConst;
var isMergeDecision2 = isMergeDecision;
var match11 = match8;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RcRef.js
var RcRef_exports = {};
__export(RcRef_exports, {
  TypeId: () => TypeId3,
  get: () => get6,
  invalidate: () => invalidate2,
  make: () => make14
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/rcRef.js
var TypeId2 = Symbol.for("effect/RcRef");
var stateEmpty2 = {
  _tag: "Empty"
};
var stateClosed = {
  _tag: "Closed"
};
var variance = {
  _A: identity,
  _E: identity
};
var RcRefImpl = class extends Class {
  acquire;
  context;
  scope;
  idleTimeToLive;
  [TypeId2] = variance;
  [TypeId] = TypeId;
  state = stateEmpty2;
  semaphore = unsafeMakeSemaphore(1);
  constructor(acquire, context8, scope2, idleTimeToLive) {
    super();
    this.acquire = acquire;
    this.context = context8;
    this.scope = scope2;
    this.idleTimeToLive = idleTimeToLive;
    this.get = get5(this);
  }
  get;
  commit() {
    return this.get;
  }
};
var make13 = (options) => withFiberRuntime((fiber) => {
  const context8 = fiber.getFiberRef(currentContext);
  const scope2 = get(context8, scopeTag);
  const ref = new RcRefImpl(options.acquire, context8, scope2, options.idleTimeToLive ? decode(options.idleTimeToLive) : void 0);
  return as(scope2.addFinalizer(() => ref.semaphore.withPermits(1)(suspend(() => {
    const close2 = ref.state._tag === "Acquired" ? scopeClose(ref.state.scope, exitVoid) : void_;
    ref.state = stateClosed;
    return close2;
  }))), ref);
});
var get5 = (self_) => {
  const self = self_;
  const isInfinite = self.idleTimeToLive && !isFinite(self.idleTimeToLive);
  return uninterruptibleMask((restore) => suspend(() => {
    switch (self.state._tag) {
      case "Closed": {
        return interrupt;
      }
      case "Acquired": {
        self.state.refCount++;
        return self.state.fiber ? as(interruptFiber(self.state.fiber), self.state) : succeed(self.state);
      }
      case "Empty": {
        return scopeMake().pipe(bindTo2("scope"), bind("value", ({
          scope: scope2
        }) => restore(fiberRefLocally(self.acquire, currentContext, add(self.context, scopeTag, scope2)))), map3(({
          scope: scope2,
          value
        }) => {
          const state = {
            _tag: "Acquired",
            value,
            scope: scope2,
            fiber: void 0,
            refCount: 1
          };
          self.state = state;
          return state;
        }));
      }
    }
  })).pipe(self.semaphore.withPermits(1), bindTo2("state"), bind("scope", () => scopeTag), tap(({
    scope: scope2,
    state
  }) => scope2.addFinalizer(() => suspend(() => {
    state.refCount--;
    if (state.refCount > 0 || isInfinite) {
      return void_;
    }
    if (self.idleTimeToLive === void 0) {
      self.state = stateEmpty2;
      return scopeClose(state.scope, exitVoid);
    }
    return sleep2(self.idleTimeToLive).pipe(interruptible, zipRight(suspend(() => {
      if (self.state._tag === "Acquired" && self.state.refCount === 0) {
        self.state = stateEmpty2;
        return scopeClose(state.scope, exitVoid);
      }
      return void_;
    })), ensuring(sync(() => {
      state.fiber = void 0;
    })), forkIn(self.scope), tap((fiber) => {
      state.fiber = fiber;
    }), self.semaphore.withPermits(1));
  }))), map3(({
    state
  }) => state.value));
};
var invalidate = (self_) => {
  const self = self_;
  return uninterruptible(suspend(() => {
    if (self.state._tag !== "Acquired") {
      return void_;
    }
    const state = self.state;
    self.state = stateEmpty2;
    return state.scope.close(exitVoid);
  }));
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/RcRef.js
var TypeId3 = TypeId2;
var make14 = make13;
var get6 = get5;
var invalidate2 = invalidate;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Runtime.js
var Runtime_exports = {};
__export(Runtime_exports, {
  FiberFailureCauseId: () => FiberFailureCauseId2,
  FiberFailureId: () => FiberFailureId,
  defaultRuntime: () => defaultRuntime2,
  defaultRuntimeFlags: () => defaultRuntimeFlags2,
  deleteFiberRef: () => deleteFiberRef2,
  disableRuntimeFlag: () => disableRuntimeFlag2,
  enableRuntimeFlag: () => enableRuntimeFlag2,
  isAsyncFiberException: () => isAsyncFiberException2,
  isFiberFailure: () => isFiberFailure2,
  make: () => make15,
  makeFiberFailure: () => makeFiberFailure,
  provideService: () => provideService4,
  runCallback: () => runCallback,
  runFork: () => runFork2,
  runPromise: () => runPromise2,
  runPromiseExit: () => runPromiseExit,
  runSync: () => runSync2,
  runSyncExit: () => runSyncExit,
  setFiberRef: () => setFiberRef2,
  updateContext: () => updateContext2,
  updateFiberRefs: () => updateFiberRefs2,
  updateRuntimeFlags: () => updateRuntimeFlags2
});
var runFork2 = unsafeFork;
var runSyncExit = unsafeRunSyncExit;
var runSync2 = unsafeRunSync;
var runCallback = unsafeRunCallback;
var runPromise2 = unsafeRunPromise;
var runPromiseExit = unsafeRunPromiseExit;
var defaultRuntime2 = defaultRuntime;
var defaultRuntimeFlags2 = defaultRuntimeFlags;
var make15 = make7;
var FiberFailureId = Symbol.for("effect/Runtime/FiberFailure");
var FiberFailureCauseId2 = FiberFailureCauseId;
var isAsyncFiberException2 = isAsyncFiberException;
var isFiberFailure2 = isFiberFailure;
var makeFiberFailure = fiberFailure;
var updateRuntimeFlags2 = updateRuntimeFlags;
var enableRuntimeFlag2 = enableRuntimeFlag;
var disableRuntimeFlag2 = disableRuntimeFlag;
var updateContext2 = updateContext;
var provideService4 = provideService;
var updateFiberRefs2 = updateFiberRefs;
var setFiberRef2 = setFiberRef;
var deleteFiberRef2 = deleteFiberRef;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Schedule.js
var Schedule_exports = {};
__export(Schedule_exports, {
  CurrentIterationMetadata: () => CurrentIterationMetadata2,
  ScheduleDriverTypeId: () => ScheduleDriverTypeId2,
  ScheduleTypeId: () => ScheduleTypeId2,
  addDelay: () => addDelay2,
  addDelayEffect: () => addDelayEffect2,
  andThen: () => andThen2,
  andThenEither: () => andThenEither2,
  as: () => as5,
  asVoid: () => asVoid5,
  bothInOut: () => bothInOut2,
  check: () => check2,
  checkEffect: () => checkEffect2,
  collectAllInputs: () => collectAllInputs2,
  collectAllOutputs: () => collectAllOutputs2,
  collectUntil: () => collectUntil2,
  collectUntilEffect: () => collectUntilEffect2,
  collectWhile: () => collectWhile2,
  collectWhileEffect: () => collectWhileEffect2,
  compose: () => compose2,
  count: () => count2,
  cron: () => cron2,
  dayOfMonth: () => dayOfMonth2,
  dayOfWeek: () => dayOfWeek2,
  delayed: () => delayed2,
  delayedEffect: () => delayedEffect2,
  delayedSchedule: () => delayedSchedule2,
  delays: () => delays2,
  driver: () => driver2,
  duration: () => duration2,
  either: () => either2,
  eitherWith: () => eitherWith2,
  elapsed: () => elapsed2,
  ensuring: () => ensuring5,
  exponential: () => exponential2,
  fibonacci: () => fibonacci2,
  fixed: () => fixed2,
  forever: () => forever3,
  fromDelay: () => fromDelay2,
  fromDelays: () => fromDelays2,
  fromFunction: () => fromFunction2,
  hourOfDay: () => hourOfDay2,
  identity: () => identity3,
  intersect: () => intersect2,
  intersectWith: () => intersectWith2,
  isSchedule: () => isSchedule2,
  jittered: () => jittered2,
  jitteredWith: () => jitteredWith2,
  linear: () => linear2,
  makeWithState: () => makeWithState2,
  map: () => map9,
  mapBoth: () => mapBoth4,
  mapBothEffect: () => mapBothEffect2,
  mapEffect: () => mapEffect3,
  mapInput: () => mapInput3,
  mapInputContext: () => mapInputContext3,
  mapInputEffect: () => mapInputEffect3,
  minuteOfHour: () => minuteOfHour2,
  modifyDelay: () => modifyDelay2,
  modifyDelayEffect: () => modifyDelayEffect2,
  onDecision: () => onDecision2,
  once: () => once2,
  passthrough: () => passthrough2,
  provideContext: () => provideContext3,
  provideService: () => provideService5,
  recurUntil: () => recurUntil2,
  recurUntilEffect: () => recurUntilEffect2,
  recurUntilOption: () => recurUntilOption2,
  recurUpTo: () => recurUpTo2,
  recurWhile: () => recurWhile2,
  recurWhileEffect: () => recurWhileEffect2,
  recurs: () => recurs2,
  reduce: () => reduce4,
  reduceEffect: () => reduceEffect2,
  repeatForever: () => repeatForever,
  repetitions: () => repetitions2,
  resetAfter: () => resetAfter2,
  resetWhen: () => resetWhen2,
  run: () => run3,
  secondOfMinute: () => secondOfMinute2,
  spaced: () => spaced2,
  stop: () => stop2,
  succeed: () => succeed7,
  sync: () => sync5,
  tapInput: () => tapInput2,
  tapOutput: () => tapOutput2,
  unfold: () => unfold2,
  union: () => union2,
  unionWith: () => unionWith2,
  untilInput: () => untilInput2,
  untilInputEffect: () => untilInputEffect2,
  untilOutput: () => untilOutput2,
  untilOutputEffect: () => untilOutputEffect2,
  upTo: () => upTo2,
  whileInput: () => whileInput2,
  whileInputEffect: () => whileInputEffect2,
  whileOutput: () => whileOutput2,
  whileOutputEffect: () => whileOutputEffect2,
  windowed: () => windowed2,
  zipLeft: () => zipLeft4,
  zipRight: () => zipRight6,
  zipWith: () => zipWith4
});
var ScheduleTypeId2 = ScheduleTypeId;
var ScheduleDriverTypeId2 = ScheduleDriverTypeId;
var makeWithState2 = makeWithState;
var isSchedule2 = isSchedule;
var addDelay2 = addDelay;
var addDelayEffect2 = addDelayEffect;
var andThen2 = andThen;
var andThenEither2 = andThenEither;
var as5 = as2;
var asVoid5 = asVoid2;
var bothInOut2 = bothInOut;
var check2 = check;
var checkEffect2 = checkEffect;
var collectAllInputs2 = collectAllInputs;
var collectAllOutputs2 = collectAllOutputs;
var collectUntil2 = collectUntil;
var collectUntilEffect2 = collectUntilEffect;
var collectWhile2 = collectWhile;
var collectWhileEffect2 = collectWhileEffect;
var compose2 = compose;
var mapInput3 = mapInput;
var mapInputEffect3 = mapInputEffect;
var mapInputContext3 = mapInputContext;
var count2 = count;
var cron2 = cron;
var secondOfMinute2 = secondOfMinute;
var minuteOfHour2 = minuteOfHour;
var hourOfDay2 = hourOfDay;
var dayOfMonth2 = dayOfMonth;
var dayOfWeek2 = dayOfWeek;
var delayed2 = delayed;
var delayedEffect2 = delayedEffect;
var delayedSchedule2 = delayedSchedule;
var delays2 = delays;
var mapBoth4 = mapBoth2;
var mapBothEffect2 = mapBothEffect;
var driver2 = driver;
var duration2 = duration;
var either2 = either;
var eitherWith2 = eitherWith;
var elapsed2 = elapsed;
var ensuring5 = ensuring2;
var exponential2 = exponential;
var fibonacci2 = fibonacci;
var fixed2 = fixed;
var forever3 = forever;
var fromDelay2 = fromDelay;
var fromDelays2 = fromDelays;
var fromFunction2 = fromFunction;
var identity3 = identity2;
var passthrough2 = passthrough;
var intersect2 = intersect;
var intersectWith2 = intersectWith;
var jittered2 = jittered;
var jitteredWith2 = jitteredWith;
var linear2 = linear;
var map9 = map6;
var mapEffect3 = mapEffect;
var modifyDelay2 = modifyDelay;
var modifyDelayEffect2 = modifyDelayEffect;
var onDecision2 = onDecision;
var once2 = once;
var provideContext3 = provideContext;
var provideService5 = provideService2;
var recurUntil2 = recurUntil;
var recurUntilEffect2 = recurUntilEffect;
var recurUntilOption2 = recurUntilOption;
var recurUpTo2 = recurUpTo;
var recurWhile2 = recurWhile;
var recurWhileEffect2 = recurWhileEffect;
var recurs2 = recurs;
var reduce4 = reduce2;
var reduceEffect2 = reduceEffect;
var repeatForever = forever;
var repetitions2 = repetitions;
var resetAfter2 = resetAfter;
var resetWhen2 = resetWhen;
var run3 = run;
var spaced2 = spaced;
var stop2 = stop;
var succeed7 = succeed3;
var sync5 = sync2;
var tapInput2 = tapInput;
var tapOutput2 = tapOutput;
var unfold2 = unfold;
var union2 = union;
var unionWith2 = unionWith;
var untilInput2 = untilInput;
var untilInputEffect2 = untilInputEffect;
var untilOutput2 = untilOutput;
var untilOutputEffect2 = untilOutputEffect;
var upTo2 = upTo;
var whileInput2 = whileInput;
var whileInputEffect2 = whileInputEffect;
var whileOutput2 = whileOutput;
var whileOutputEffect2 = whileOutputEffect;
var windowed2 = windowed;
var zipLeft4 = zipLeft;
var zipRight6 = zipRight3;
var zipWith4 = zipWith2;
var CurrentIterationMetadata2 = CurrentIterationMetadata;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/StreamHaltStrategy.js
var StreamHaltStrategy_exports = {};
__export(StreamHaltStrategy_exports, {
  Both: () => Both2,
  Either: () => Either2,
  Left: () => Left2,
  Right: () => Right2,
  fromInput: () => fromInput3,
  isBoth: () => isBoth2,
  isEither: () => isEither2,
  isLeft: () => isLeft2,
  isRight: () => isRight2,
  match: () => match13
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/opCodes/streamHaltStrategy.js
var OP_LEFT = "Left";
var OP_RIGHT = "Right";
var OP_BOTH = "Both";
var OP_EITHER = "Either";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/haltStrategy.js
var Left = {
  _tag: OP_LEFT
};
var Right = {
  _tag: OP_RIGHT
};
var Both = {
  _tag: OP_BOTH
};
var Either = {
  _tag: OP_EITHER
};
var fromInput2 = (input) => {
  switch (input) {
    case "left":
      return Left;
    case "right":
      return Right;
    case "both":
      return Both;
    case "either":
      return Either;
    default:
      return input;
  }
};
var isLeft = (self) => self._tag === OP_LEFT;
var isRight = (self) => self._tag === OP_RIGHT;
var isBoth = (self) => self._tag === OP_BOTH;
var isEither = (self) => self._tag === OP_EITHER;
var match12 = dual(2, (self, options) => {
  switch (self._tag) {
    case OP_LEFT: {
      return options.onLeft();
    }
    case OP_RIGHT: {
      return options.onRight();
    }
    case OP_BOTH: {
      return options.onBoth();
    }
    case OP_EITHER: {
      return options.onEither();
    }
  }
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/StreamHaltStrategy.js
var Left2 = Left;
var Right2 = Right;
var Both2 = Both;
var Either2 = Either;
var fromInput3 = fromInput2;
var isLeft2 = isLeft;
var isRight2 = isRight;
var isBoth2 = isBoth;
var isEither2 = isEither;
var match13 = match12;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TPubSub.js
var TPubSub_exports = {};
__export(TPubSub_exports, {
  TPubSubTypeId: () => TPubSubTypeId2,
  awaitShutdown: () => awaitShutdown7,
  bounded: () => bounded8,
  capacity: () => capacity8,
  dropping: () => dropping7,
  isEmpty: () => isEmpty9,
  isFull: () => isFull7,
  isShutdown: () => isShutdown7,
  publish: () => publish4,
  publishAll: () => publishAll4,
  shutdown: () => shutdown7,
  size: () => size8,
  sliding: () => sliding7,
  subscribe: () => subscribe4,
  subscribeScoped: () => subscribeScoped2,
  unbounded: () => unbounded8
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/sink.js
var SinkTypeId = Symbol.for("effect/Sink");
var sinkVariance = {
  /* c8 ignore next */
  _A: (_) => _,
  /* c8 ignore next */
  _In: (_) => _,
  /* c8 ignore next */
  _L: (_) => _,
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _R: (_) => _
};
var SinkImpl = class {
  channel;
  [SinkTypeId] = sinkVariance;
  constructor(channel) {
    this.channel = channel;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isSink = (u) => hasProperty(u, SinkTypeId);
var suspend4 = (evaluate2) => new SinkImpl(suspend3(() => toChannel(evaluate2())));
var as6 = dual(2, (self, a) => pipe(self, map10(() => a)));
var collectAll = () => new SinkImpl(collectAllLoop(empty2()));
var collectAllLoop = (acc) => readWithCause({
  onInput: (chunk2) => collectAllLoop(pipe(acc, appendAll(chunk2))),
  onFailure: failCause4,
  onDone: () => succeed6(acc)
});
var collectAllN = (n) => suspend4(() => fromChannel(collectAllNLoop(n, empty2())));
var collectAllNLoop = (n, acc) => readWithCause({
  onInput: (chunk2) => {
    const [collected, leftovers] = splitAt2(chunk2, n);
    if (collected.length < n) {
      return collectAllNLoop(n - collected.length, appendAll(acc, collected));
    }
    if (isEmpty(leftovers)) {
      return succeed6(appendAll(acc, collected));
    }
    return flatMap4(write(leftovers), () => succeed6(appendAll(acc, collected)));
  },
  onFailure: failCause4,
  onDone: () => succeed6(acc)
});
var collectAllFrom = (self) => collectAllWhileWith(self, {
  initial: empty2(),
  while: constTrue,
  body: (chunk2, a) => pipe(chunk2, append(a))
});
var collectAllToMap = (key, merge7) => {
  return foldLeftChunks(empty4(), (map15, chunk2) => pipe(chunk2, reduce(map15, (map16, input) => {
    const k = key(input);
    const v = pipe(map16, has2(k)) ? merge7(pipe(map16, unsafeGet3(k)), input) : input;
    return pipe(map16, set2(k, v));
  })));
};
var collectAllToMapN = (n, key, merge7) => {
  return foldWeighted({
    initial: empty4(),
    maxCost: n,
    cost: (acc, input) => pipe(acc, has2(key(input))) ? 0 : 1,
    body: (acc, input) => {
      const k = key(input);
      const v = pipe(acc, has2(k)) ? merge7(pipe(acc, unsafeGet3(k)), input) : input;
      return pipe(acc, set2(k, v));
    }
  });
};
var collectAllToSet = () => foldLeftChunks(empty3(), (acc, chunk2) => pipe(chunk2, reduce(acc, (acc2, input) => pipe(acc2, add2(input)))));
var collectAllToSetN = (n) => foldWeighted({
  initial: empty3(),
  maxCost: n,
  cost: (acc, input) => has(acc, input) ? 0 : 1,
  body: (acc, input) => add2(acc, input)
});
var collectAllUntil = (p) => {
  return pipe(fold([empty2(), true], (tuple) => tuple[1], ([chunk2, _], input) => [pipe(chunk2, append(input)), !p(input)]), map10((tuple) => tuple[0]));
};
var collectAllUntilEffect = (p) => {
  return pipe(foldEffect([empty2(), true], (tuple) => tuple[1], ([chunk2, _], input) => pipe(p(input), map7((bool) => [pipe(chunk2, append(input)), !bool]))), map10((tuple) => tuple[0]));
};
var collectAllWhile = (predicate) => fromChannel(collectAllWhileReader(predicate, empty2()));
var collectAllWhileReader = (predicate, done4) => readWith({
  onInput: (input) => {
    const [collected, leftovers] = pipe(toReadonlyArray(input), span(predicate));
    if (leftovers.length === 0) {
      return collectAllWhileReader(predicate, pipe(done4, appendAll(unsafeFromArray(collected))));
    }
    return pipe(write(unsafeFromArray(leftovers)), zipRight5(succeed6(pipe(done4, appendAll(unsafeFromArray(collected))))));
  },
  onFailure: fail5,
  onDone: () => succeed6(done4)
});
var collectAllWhileEffect = (predicate) => fromChannel(collectAllWhileEffectReader(predicate, empty2()));
var collectAllWhileEffectReader = (predicate, done4) => readWith({
  onInput: (input) => pipe(fromEffect(pipe(input, takeWhile2(predicate), map7(unsafeFromArray))), flatMap4((collected) => {
    const leftovers = pipe(input, drop(collected.length));
    if (isEmpty(leftovers)) {
      return collectAllWhileEffectReader(predicate, pipe(done4, appendAll(collected)));
    }
    return pipe(write(leftovers), zipRight5(succeed6(pipe(done4, appendAll(collected)))));
  })),
  onFailure: fail5,
  onDone: () => succeed6(done4)
});
var collectAllWhileWith = dual(2, (self, options) => {
  const refs = pipe(make5(empty2()), zip2(make5(false)));
  const newChannel = pipe(fromEffect(refs), flatMap4(([leftoversRef, upstreamDoneRef]) => {
    const upstreamMarker = readWith({
      onInput: (input) => pipe(write(input), flatMap4(() => upstreamMarker)),
      onFailure: fail5,
      onDone: (done4) => pipe(fromEffect(set3(upstreamDoneRef, true)), as4(done4))
    });
    return pipe(upstreamMarker, pipeTo(bufferChunk(leftoversRef)), pipeTo(collectAllWhileWithLoop(self, leftoversRef, upstreamDoneRef, options.initial, options.while, options.body)));
  }));
  return new SinkImpl(newChannel);
});
var collectAllWhileWithLoop = (self, leftoversRef, upstreamDoneRef, currentResult, p, f) => {
  return pipe(toChannel(self), doneCollect, foldChannel({
    onFailure: fail5,
    onSuccess: ([leftovers, doneValue]) => p(doneValue) ? pipe(fromEffect(set3(leftoversRef, flatten(leftovers))), flatMap4(() => pipe(fromEffect(get3(upstreamDoneRef)), flatMap4((upstreamDone) => {
      const accumulatedResult = f(currentResult, doneValue);
      return upstreamDone ? pipe(write(flatten(leftovers)), as4(accumulatedResult)) : collectAllWhileWithLoop(self, leftoversRef, upstreamDoneRef, accumulatedResult, p, f);
    })))) : pipe(write(flatten(leftovers)), as4(currentResult))
  }));
};
var collectLeftover = (self) => new SinkImpl(pipe(collectElements(toChannel(self)), map8(([chunks3, z]) => [z, flatten(chunks3)])));
var mapInput4 = dual(2, (self, f) => pipe(self, mapInputChunks(map2(f))));
var mapInputEffect4 = dual(2, (self, f) => mapInputChunksEffect(self, (chunk2) => map7(forEach2(chunk2, (v) => f(v)), unsafeFromArray)));
var mapInputChunks = dual(2, (self, f) => {
  const loop2 = readWith({
    onInput: (chunk2) => pipe(write(f(chunk2)), flatMap4(() => loop2)),
    onFailure: fail5,
    onDone: succeed6
  });
  return new SinkImpl(pipe(loop2, pipeTo(toChannel(self))));
});
var mapInputChunksEffect = dual(2, (self, f) => {
  const loop2 = readWith({
    onInput: (chunk2) => pipe(fromEffect(f(chunk2)), flatMap4(write), flatMap4(() => loop2)),
    onFailure: fail5,
    onDone: succeed6
  });
  return new SinkImpl(pipe(loop2, pipeToOrFail(toChannel(self))));
});
var die4 = (defect) => failCause5(die2(defect));
var dieMessage2 = (message) => failCause5(die2(new RuntimeException(message)));
var dieSync2 = (evaluate2) => failCauseSync3(() => die2(evaluate2()));
var dimap = dual(2, (self, options) => map10(mapInput4(self, options.onInput), options.onDone));
var dimapEffect = dual(2, (self, options) => mapEffect4(mapInputEffect4(self, options.onInput), options.onDone));
var dimapChunks = dual(2, (self, options) => map10(mapInputChunks(self, options.onInput), options.onDone));
var dimapChunksEffect = dual(2, (self, options) => mapEffect4(mapInputChunksEffect(self, options.onInput), options.onDone));
var drain2 = new SinkImpl(drain(identityChannel()));
var drop2 = (n) => suspend4(() => new SinkImpl(dropLoop(n)));
var dropLoop = (n) => readWith({
  onInput: (input) => {
    const dropped = pipe(input, drop(n));
    const leftover2 = Math.max(n - input.length, 0);
    const more = isEmpty(input) || leftover2 > 0;
    if (more) {
      return dropLoop(leftover2);
    }
    return pipe(write(dropped), zipRight5(identityChannel()));
  },
  onFailure: fail5,
  onDone: () => void_3
});
var dropUntil2 = (predicate) => new SinkImpl(pipe(toChannel(dropWhile3((input) => !predicate(input))), pipeToOrFail(toChannel(drop2(1)))));
var dropUntilEffect = (predicate) => suspend4(() => new SinkImpl(dropUntilEffectReader(predicate)));
var dropUntilEffectReader = (predicate) => readWith({
  onInput: (input) => pipe(input, dropUntil(predicate), map7((leftover2) => {
    const more = leftover2.length === 0;
    return more ? dropUntilEffectReader(predicate) : pipe(write(unsafeFromArray(leftover2)), zipRight5(identityChannel()));
  }), unwrap),
  onFailure: fail5,
  onDone: () => void_3
});
var dropWhile3 = (predicate) => new SinkImpl(dropWhileReader(predicate));
var dropWhileReader = (predicate) => readWith({
  onInput: (input) => {
    const out = pipe(input, dropWhile(predicate));
    if (isEmpty(out)) {
      return dropWhileReader(predicate);
    }
    return pipe(write(out), zipRight5(identityChannel()));
  },
  onFailure: fail5,
  onDone: succeedNow
});
var dropWhileEffect = (predicate) => suspend4(() => new SinkImpl(dropWhileEffectReader(predicate)));
var dropWhileEffectReader = (predicate) => readWith({
  onInput: (input) => pipe(input, dropWhile2(predicate), map7((leftover2) => {
    const more = leftover2.length === 0;
    return more ? dropWhileEffectReader(predicate) : pipe(write(unsafeFromArray(leftover2)), zipRight5(identityChannel()));
  }), unwrap),
  onFailure: fail5,
  onDone: () => void_3
});
var ensuring6 = dual(2, (self, finalizer3) => new SinkImpl(pipe(self, toChannel, ensuring4(finalizer3))));
var ensuringWith2 = dual(2, (self, finalizer3) => new SinkImpl(pipe(self, toChannel, ensuringWith(finalizer3))));
var context4 = () => fromEffect3(context());
var contextWith3 = (f) => pipe(context4(), map10(f));
var contextWithEffect2 = (f) => pipe(context4(), mapEffect4(f));
var contextWithSink = (f) => new SinkImpl(unwrap(contextWith((context8) => toChannel(f(context8)))));
var every2 = (predicate) => fold(true, identity, (acc, input) => acc && predicate(input));
var fail6 = (e) => new SinkImpl(fail5(e));
var failSync3 = (evaluate2) => new SinkImpl(failSync2(evaluate2));
var failCause5 = (cause) => new SinkImpl(failCause4(cause));
var failCauseSync3 = (evaluate2) => new SinkImpl(failCauseSync2(evaluate2));
var filterInput = (f) => {
  return (self) => pipe(self, mapInputChunks(filter(f)));
};
var filterInputEffect = dual(2, (self, f) => mapInputChunksEffect(self, (chunk2) => map7(filter2(chunk2, f), unsafeFromArray)));
var findEffect = dual(2, (self, f) => {
  const newChannel = pipe(fromEffect(pipe(make5(empty2()), zip2(make5(false)))), flatMap4(([leftoversRef, upstreamDoneRef]) => {
    const upstreamMarker = readWith({
      onInput: (input) => pipe(write(input), flatMap4(() => upstreamMarker)),
      onFailure: fail5,
      onDone: (done4) => pipe(fromEffect(set3(upstreamDoneRef, true)), as4(done4))
    });
    const loop2 = foldChannel(collectElements(toChannel(self)), {
      onFailure: fail5,
      onSuccess: ([leftovers, doneValue]) => pipe(fromEffect(f(doneValue)), flatMap4((satisfied) => pipe(fromEffect(set3(leftoversRef, flatten(leftovers))), zipRight5(pipe(fromEffect(get3(upstreamDoneRef)), flatMap4((upstreamDone) => {
        if (satisfied) {
          return pipe(write(flatten(leftovers)), as4(some(doneValue)));
        }
        if (upstreamDone) {
          return pipe(write(flatten(leftovers)), as4(none()));
        }
        return loop2;
      }))))))
    });
    return pipe(upstreamMarker, pipeTo(bufferChunk(leftoversRef)), pipeTo(loop2));
  }));
  return new SinkImpl(newChannel);
});
var fold = (s, contFn, f) => suspend4(() => new SinkImpl(foldReader(s, contFn, f)));
var foldReader = (s, contFn, f) => {
  if (!contFn(s)) {
    return succeedNow(s);
  }
  return readWith({
    onInput: (input) => {
      const [nextS, leftovers] = foldChunkSplit(s, input, contFn, f, 0, input.length);
      if (isNonEmpty(leftovers)) {
        return pipe(write(leftovers), as4(nextS));
      }
      return foldReader(nextS, contFn, f);
    },
    onFailure: fail5,
    onDone: () => succeedNow(s)
  });
};
var foldChunkSplit = (s, chunk2, contFn, f, index, length2) => {
  if (index === length2) {
    return [s, empty2()];
  }
  const s1 = f(s, pipe(chunk2, unsafeGet(index)));
  if (contFn(s1)) {
    return foldChunkSplit(s1, chunk2, contFn, f, index + 1, length2);
  }
  return [s1, pipe(chunk2, drop(index + 1))];
};
var foldSink = dual(2, (self, options) => {
  const newChannel = pipe(toChannel(self), collectElements, foldChannel({
    onFailure: (error) => toChannel(options.onFailure(error)),
    onSuccess: ([leftovers, z]) => suspend3(() => {
      const leftoversRef = {
        ref: pipe(leftovers, filter(isNonEmpty))
      };
      const refReader = pipe(
        sync4(() => {
          const ref = leftoversRef.ref;
          leftoversRef.ref = empty2();
          return ref;
        }),
        // This cast is safe because of the L1 >: L <: In1 bound. It follows that
        // L <: In1 and therefore Chunk[L] can be safely cast to Chunk[In1].
        flatMap4((chunk2) => writeChunk(chunk2))
      );
      const passthrough3 = identityChannel();
      const continuationSink = pipe(refReader, zipRight5(passthrough3), pipeTo(toChannel(options.onSuccess(z))));
      return flatMap4(collectElements(continuationSink), ([newLeftovers, z1]) => pipe(succeed6(leftoversRef.ref), flatMap4(writeChunk), zipRight5(writeChunk(newLeftovers)), as4(z1)));
    })
  }));
  return new SinkImpl(newChannel);
});
var foldChunks = (s, contFn, f) => suspend4(() => new SinkImpl(foldChunksReader(s, contFn, f)));
var foldChunksReader = (s, contFn, f) => {
  if (!contFn(s)) {
    return succeedNow(s);
  }
  return readWith({
    onInput: (input) => foldChunksReader(f(s, input), contFn, f),
    onFailure: fail5,
    onDone: () => succeedNow(s)
  });
};
var foldChunksEffect = (s, contFn, f) => suspend4(() => new SinkImpl(foldChunksEffectReader(s, contFn, f)));
var foldChunksEffectReader = (s, contFn, f) => {
  if (!contFn(s)) {
    return succeedNow(s);
  }
  return readWith({
    onInput: (input) => pipe(fromEffect(f(s, input)), flatMap4((s2) => foldChunksEffectReader(s2, contFn, f))),
    onFailure: fail5,
    onDone: () => succeedNow(s)
  });
};
var foldEffect = (s, contFn, f) => suspend4(() => new SinkImpl(foldEffectReader(s, contFn, f)));
var foldEffectReader = (s, contFn, f) => {
  if (!contFn(s)) {
    return succeedNow(s);
  }
  return readWith({
    onInput: (input) => pipe(fromEffect(foldChunkSplitEffect(s, input, contFn, f)), flatMap4(([nextS, leftovers]) => pipe(leftovers, match2({
      onNone: () => foldEffectReader(nextS, contFn, f),
      onSome: (leftover2) => pipe(write(leftover2), as4(nextS))
    })))),
    onFailure: fail5,
    onDone: () => succeedNow(s)
  });
};
var foldChunkSplitEffect = (s, chunk2, contFn, f) => foldChunkSplitEffectInternal(s, chunk2, 0, chunk2.length, contFn, f);
var foldChunkSplitEffectInternal = (s, chunk2, index, length2, contFn, f) => {
  if (index === length2) {
    return succeed5([s, none()]);
  }
  return pipe(f(s, pipe(chunk2, unsafeGet(index))), flatMap3((s1) => contFn(s1) ? foldChunkSplitEffectInternal(s1, chunk2, index + 1, length2, contFn, f) : succeed5([s1, some(pipe(chunk2, drop(index + 1)))])));
};
var foldLeft = (s, f) => ignoreLeftover(fold(s, constTrue, f));
var foldLeftChunks = (s, f) => foldChunks(s, constTrue, f);
var foldLeftChunksEffect = (s, f) => ignoreLeftover(foldChunksEffect(s, constTrue, f));
var foldLeftEffect = (s, f) => foldEffect(s, constTrue, f);
var foldUntil = (s, max, f) => pipe(fold([s, 0], (tuple) => tuple[1] < max, ([output, count4], input) => [f(output, input), count4 + 1]), map10((tuple) => tuple[0]));
var foldUntilEffect = (s, max, f) => pipe(foldEffect([s, 0], (tuple) => tuple[1] < max, ([output, count4], input) => pipe(f(output, input), map7((s2) => [s2, count4 + 1]))), map10((tuple) => tuple[0]));
var foldWeighted = (options) => foldWeightedDecompose({
  ...options,
  decompose: of
});
var foldWeightedDecompose = (options) => suspend4(() => new SinkImpl(foldWeightedDecomposeLoop(options.initial, 0, false, options.maxCost, options.cost, options.decompose, options.body)));
var foldWeightedDecomposeLoop = (s, cost, dirty, max, costFn, decompose, f) => readWith({
  onInput: (input) => {
    const [nextS, nextCost, nextDirty, leftovers] = foldWeightedDecomposeFold(input, s, cost, dirty, max, costFn, decompose, f);
    if (isNonEmpty(leftovers)) {
      return pipe(write(leftovers), zipRight5(succeedNow(nextS)));
    }
    if (cost > max) {
      return succeedNow(nextS);
    }
    return foldWeightedDecomposeLoop(nextS, nextCost, nextDirty, max, costFn, decompose, f);
  },
  onFailure: fail5,
  onDone: () => succeedNow(s)
});
var foldWeightedDecomposeFold = (input, s, cost, dirty, max, costFn, decompose, f) => {
  for (let index = 0; index < input.length; index++) {
    const elem = unsafeGet(input, index);
    const prevCost = cost;
    cost = cost + costFn(s, elem);
    if (cost <= max) {
      s = f(s, elem);
      dirty = true;
      continue;
    }
    const decomposed = decompose(elem);
    if (decomposed.length <= 1 && !dirty) {
      return [f(s, elem), cost, true, drop(input, index + 1)];
    }
    if (decomposed.length <= 1 && dirty) {
      return [s, prevCost, dirty, drop(input, index)];
    }
    input = appendAll(decomposed, drop(input, index + 1));
    cost = prevCost;
    index = -1;
  }
  return [s, cost, dirty, empty2()];
};
var foldWeightedDecomposeEffect = (options) => suspend4(() => new SinkImpl(foldWeightedDecomposeEffectLoop(options.initial, options.maxCost, options.cost, options.decompose, options.body, 0, false)));
var foldWeightedEffect = (options) => foldWeightedDecomposeEffect({
  ...options,
  decompose: (input) => succeed5(of(input))
});
var foldWeightedDecomposeEffectLoop = (s, max, costFn, decompose, f, cost, dirty) => readWith({
  onInput: (input) => pipe(fromEffect(foldWeightedDecomposeEffectFold(s, max, costFn, decompose, f, input, dirty, cost, 0)), flatMap4(([nextS, nextCost, nextDirty, leftovers]) => {
    if (isNonEmpty(leftovers)) {
      return pipe(write(leftovers), zipRight5(succeedNow(nextS)));
    }
    if (cost > max) {
      return succeedNow(nextS);
    }
    return foldWeightedDecomposeEffectLoop(nextS, max, costFn, decompose, f, nextCost, nextDirty);
  })),
  onFailure: fail5,
  onDone: () => succeedNow(s)
});
var foldWeightedDecomposeEffectFold = (s, max, costFn, decompose, f, input, dirty, cost, index) => {
  if (index === input.length) {
    return succeed5([s, cost, dirty, empty2()]);
  }
  const elem = pipe(input, unsafeGet(index));
  return pipe(costFn(s, elem), map7((newCost) => cost + newCost), flatMap3((total) => {
    if (total <= max) {
      return pipe(f(s, elem), flatMap3((s2) => foldWeightedDecomposeEffectFold(s2, max, costFn, decompose, f, input, true, total, index + 1)));
    }
    return pipe(decompose(elem), flatMap3((decomposed) => {
      if (decomposed.length <= 1 && !dirty) {
        return pipe(f(s, elem), map7((s2) => [s2, total, true, pipe(input, drop(index + 1))]));
      }
      if (decomposed.length <= 1 && dirty) {
        return succeed5([s, cost, dirty, pipe(input, drop(index))]);
      }
      const next = pipe(decomposed, appendAll(pipe(input, drop(index + 1))));
      return foldWeightedDecomposeEffectFold(s, max, costFn, decompose, f, next, dirty, cost, 0);
    }));
  }));
};
var flatMap5 = dual(2, (self, f) => foldSink(self, {
  onFailure: fail6,
  onSuccess: f
}));
var forEach3 = (f) => {
  const process = readWithCause({
    onInput: (input) => pipe(fromEffect(forEach2(input, (v) => f(v), {
      discard: true
    })), flatMap4(() => process)),
    onFailure: failCause4,
    onDone: () => void_3
  });
  return new SinkImpl(process);
};
var forEachChunk = (f) => {
  const process = readWithCause({
    onInput: (input) => pipe(fromEffect(f(input)), flatMap4(() => process)),
    onFailure: failCause4,
    onDone: () => void_3
  });
  return new SinkImpl(process);
};
var forEachWhile = (f) => {
  const process = readWithCause({
    onInput: (input) => forEachWhileReader(f, input, 0, input.length, process),
    onFailure: failCause4,
    onDone: () => void_3
  });
  return new SinkImpl(process);
};
var forEachWhileReader = (f, input, index, length2, cont) => {
  if (index === length2) {
    return cont;
  }
  return pipe(fromEffect(f(pipe(input, unsafeGet(index)))), flatMap4((bool) => bool ? forEachWhileReader(f, input, index + 1, length2, cont) : write(pipe(input, drop(index)))), catchAll3((error) => pipe(write(pipe(input, drop(index))), zipRight5(fail5(error)))));
};
var forEachChunkWhile = (f) => {
  const reader = readWith({
    onInput: (input) => pipe(fromEffect(f(input)), flatMap4((cont) => cont ? reader : void_3)),
    onFailure: fail5,
    onDone: () => void_3
  });
  return new SinkImpl(reader);
};
var fromChannel = (channel) => new SinkImpl(channel);
var fromEffect3 = (effect3) => new SinkImpl(fromEffect(effect3));
var fromPubSub2 = (pubsub, options) => fromQueue2(pubsub, options);
var fromPush = (push) => new SinkImpl(unwrapScoped(pipe(push, map7(fromPushPull))));
var fromPushPull = (push) => readWith({
  onInput: (input) => foldChannel(fromEffect(push(some(input))), {
    onFailure: ([either6, leftovers]) => match(either6, {
      onLeft: (error) => pipe(write(leftovers), zipRight5(fail5(error))),
      onRight: (z) => pipe(write(leftovers), zipRight5(succeedNow(z)))
    }),
    onSuccess: () => fromPushPull(push)
  }),
  onFailure: fail5,
  onDone: () => foldChannel(fromEffect(push(none())), {
    onFailure: ([either6, leftovers]) => match(either6, {
      onLeft: (error) => pipe(write(leftovers), zipRight5(fail5(error))),
      onRight: (z) => pipe(write(leftovers), zipRight5(succeedNow(z)))
    }),
    onSuccess: () => fromEffect(dieMessage("BUG: Sink.fromPush - please report an issue at https://github.com/Effect-TS/effect/issues"))
  })
});
var fromQueue2 = (queue, options) => options?.shutdown ? unwrapScoped2(map7(acquireRelease2(succeed5(queue), shutdown2), fromQueue2)) : forEachChunk((input) => offerAll3(queue, input));
var head3 = () => fold(none(), isNone, (option2, input) => match2(option2, {
  onNone: () => some(input),
  onSome: () => option2
}));
var ignoreLeftover = (self) => new SinkImpl(drain(toChannel(self)));
var last2 = () => foldLeftChunks(none(), (s, input) => orElse(last(input), () => s));
var leftover = (chunk2) => new SinkImpl(suspend3(() => write(chunk2)));
var map10 = dual(2, (self, f) => {
  return new SinkImpl(pipe(toChannel(self), map8(f)));
});
var mapEffect4 = dual(2, (self, f) => new SinkImpl(pipe(toChannel(self), mapEffect2(f))));
var mapError3 = dual(2, (self, f) => new SinkImpl(pipe(toChannel(self), mapError2(f))));
var mapLeftover = dual(2, (self, f) => new SinkImpl(pipe(toChannel(self), mapOut(map2(f)))));
var never3 = fromEffect3(never);
var orElse4 = dual(2, (self, that) => new SinkImpl(pipe(toChannel(self), orElse3(() => toChannel(that())))));
var provideContext4 = dual(2, (self, context8) => new SinkImpl(pipe(toChannel(self), provideContext2(context8))));
var race2 = dual(2, (self, that) => pipe(self, raceBoth(that), map10(merge)));
var raceBoth = dual((args) => isSink(args[1]), (self, that, options) => raceWith2(self, {
  other: that,
  onSelfDone: (selfDone) => Done2(map7(selfDone, left)),
  onOtherDone: (thatDone) => Done2(map7(thatDone, right)),
  capacity: options?.capacity ?? 16
}));
var raceWith2 = dual(2, (self, options) => {
  function race5(scope2) {
    return gen(function* () {
      const pubsub = yield* bounded5(options?.capacity ?? 16);
      const subscription1 = yield* extend(subscribe2(pubsub), scope2);
      const subscription2 = yield* extend(subscribe2(pubsub), scope2);
      const reader = toPubSub(pubsub);
      const writer = fromQueue(subscription1).pipe(pipeTo(toChannel(self)), zipLeft3(fromEffect(shutdown2(subscription1))), mergeWith({
        other: fromQueue(subscription2).pipe(pipeTo(toChannel(options.other)), zipLeft3(fromEffect(shutdown2(subscription2)))),
        onSelfDone: options.onSelfDone,
        onOtherDone: options.onOtherDone
      }));
      const racedChannel = mergeWith(reader, {
        other: writer,
        onSelfDone: () => Await(identity),
        onOtherDone: (exit2) => Done2(exit2)
      });
      return new SinkImpl(racedChannel);
    });
  }
  return unwrapScopedWith2(race5);
});
var refineOrDie = dual(2, (self, pf) => pipe(self, refineOrDieWith(pf, identity)));
var refineOrDieWith = dual(3, (self, pf, f) => {
  const newChannel = pipe(self, toChannel, catchAll3((error) => match2(pf(error), {
    onNone: () => failCauseSync2(() => die2(f(error))),
    onSome: fail5
  })));
  return new SinkImpl(newChannel);
});
var some2 = (predicate) => fold(false, (bool) => !bool, (acc, input) => acc || predicate(input));
var splitWhere2 = dual(2, (self, f) => {
  const newChannel = pipe(fromEffect(make5(empty2())), flatMap4((ref) => pipe(splitWhereSplitter(false, ref, f), pipeToOrFail(toChannel(self)), collectElements, flatMap4(([leftovers, z]) => pipe(fromEffect(get3(ref)), flatMap4((leftover2) => pipe(write(pipe(leftover2, appendAll(flatten(leftovers)))), zipRight5(succeed6(z)))))))));
  return new SinkImpl(newChannel);
});
var splitWhereSplitter = (written, leftovers, f) => readWithCause({
  onInput: (input) => {
    if (isEmpty(input)) {
      return splitWhereSplitter(written, leftovers, f);
    }
    if (written) {
      const index2 = indexWhere(input, f);
      if (index2 === -1) {
        return zipRight5(write(input), splitWhereSplitter(true, leftovers, f));
      }
      const [left3, right3] = splitAt2(input, index2);
      return zipRight5(write(left3), fromEffect(set3(leftovers, right3)));
    }
    const index = indexWhere(input, f, 1);
    if (index === -1) {
      return zipRight5(write(input), splitWhereSplitter(true, leftovers, f));
    }
    const [left2, right2] = pipe(input, splitAt2(Math.max(index, 1)));
    return zipRight5(write(left2), fromEffect(set3(leftovers, right2)));
  },
  onFailure: failCause4,
  onDone: succeed6
});
var indexWhere = (self, predicate, from = 0) => {
  const iterator = self[Symbol.iterator]();
  let index = 0;
  let result = -1;
  let next;
  while (result < 0 && (next = iterator.next()) && !next.done) {
    const a = next.value;
    if (index >= from && predicate(a)) {
      result = index;
    }
    index = index + 1;
  }
  return result;
};
var succeed8 = (a) => new SinkImpl(succeed6(a));
var sum = foldLeftChunks(0, (acc, chunk2) => acc + reduce(chunk2, 0, (s, a) => s + a));
var summarized = dual(3, (self, summary, f) => {
  const newChannel = pipe(fromEffect(summary), flatMap4((start) => pipe(self, toChannel, flatMap4((done4) => pipe(fromEffect(summary), map8((end4) => [done4, f(start, end4)]))))));
  return new SinkImpl(newChannel);
});
var sync6 = (evaluate2) => new SinkImpl(sync4(evaluate2));
var take4 = (n) => pipe(foldChunks(empty2(), (chunk2) => chunk2.length < n, (acc, chunk2) => pipe(acc, appendAll(chunk2))), flatMap5((acc) => {
  const [taken, leftover2] = pipe(acc, splitAt2(n));
  return new SinkImpl(pipe(write(leftover2), zipRight5(succeedNow(taken))));
}));
var toChannel = (self) => isEffect(self) ? toChannel(fromEffect3(self)) : self.channel;
var unwrap2 = (effect3) => new SinkImpl(unwrap(pipe(effect3, map7((sink) => toChannel(sink)))));
var unwrapScoped2 = (effect3) => new SinkImpl(unwrapScoped(effect3.pipe(map7((sink) => toChannel(sink)))));
var unwrapScopedWith2 = (f) => new SinkImpl(unwrapScopedWith((scope2) => f(scope2).pipe(map7((sink) => toChannel(sink)))));
var withDuration = (self) => pipe(self, summarized(currentTimeMillis, (start, end4) => millis(end4 - start)));
var zip4 = dual((args) => isSink(args[1]), (self, that, options) => zipWith5(self, that, (z, z2) => [z, z2], options));
var zipLeft5 = dual((args) => isSink(args[1]), (self, that, options) => zipWith5(self, that, (z, _) => z, options));
var zipRight7 = dual((args) => isSink(args[1]), (self, that, options) => zipWith5(self, that, (_, z2) => z2, options));
var zipWith5 = dual((args) => isSink(args[1]), (self, that, f, options) => options?.concurrent ? raceWith2(self, {
  other: that,
  onSelfDone: match3({
    onFailure: (cause) => Done2(failCause3(cause)),
    onSuccess: (leftZ) => Await(match3({
      onFailure: failCause3,
      onSuccess: (rightZ) => succeed5(f(leftZ, rightZ))
    }))
  }),
  onOtherDone: match3({
    onFailure: (cause) => Done2(failCause3(cause)),
    onSuccess: (rightZ) => Await(match3({
      onFailure: failCause3,
      onSuccess: (leftZ) => succeed5(f(leftZ, rightZ))
    }))
  })
}) : flatMap5(self, (z) => map10(that, (z2) => f(z, z2))));
var channelToSink = (self) => new SinkImpl(self);
var count3 = foldLeftChunks(0, (acc, chunk2) => acc + chunk2.length);
var mkString = suspend4(() => {
  const strings = [];
  return pipe(foldLeftChunks(void 0, (_, elems) => map2(elems, (elem) => {
    strings.push(String(elem));
  })), map10(() => strings.join("")));
});
var timed = pipe(withDuration(drain2), map10((tuple) => tuple[1]));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/versioned.js
var Versioned = class {
  value;
  constructor(value) {
    this.value = value;
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/entry.js
var make16 = (ref, isNew) => ({
  ref,
  isNew,
  isChanged: false,
  expected: ref.versioned,
  newValue: ref.versioned.value
});
var unsafeGet4 = (self) => {
  return self.newValue;
};
var unsafeSet = (self, value) => {
  self.isChanged = true;
  self.newValue = value;
};
var commit = (self) => {
  self.ref.versioned = new Versioned(self.newValue);
};
var copy = (self) => ({
  ref: self.ref,
  isNew: self.isNew,
  isChanged: self.isChanged,
  expected: self.expected,
  newValue: self.newValue
});
var isInvalid = (self) => {
  return self.ref.versioned !== self.expected;
};
var isChanged = (self) => {
  return self.isChanged;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/journal.js
var JournalAnalysisInvalid = "Invalid";
var JournalAnalysisReadWrite = "ReadWrite";
var JournalAnalysisReadOnly = "ReadOnly";
var commitJournal = (journal) => {
  for (const entry of journal) {
    commit(entry[1]);
  }
};
var analyzeJournal = (journal) => {
  let val = JournalAnalysisReadOnly;
  for (const [, entry] of journal) {
    val = isInvalid(entry) ? JournalAnalysisInvalid : isChanged(entry) ? JournalAnalysisReadWrite : val;
    if (val === JournalAnalysisInvalid) {
      return val;
    }
  }
  return val;
};
var prepareResetJournal = (journal) => {
  const saved = /* @__PURE__ */ new Map();
  for (const entry of journal) {
    saved.set(entry[0], copy(entry[1]));
  }
  return () => {
    journal.clear();
    for (const entry of saved) {
      journal.set(entry[0], entry[1]);
    }
  };
};
var collectTodos = (journal) => {
  const allTodos = /* @__PURE__ */ new Map();
  for (const [, entry] of journal) {
    for (const todo of entry.ref.todos) {
      allTodos.set(todo[0], todo[1]);
    }
    entry.ref.todos = /* @__PURE__ */ new Map();
  }
  return allTodos;
};
var execTodos = (todos) => {
  const todosSorted = Array.from(todos.entries()).sort((x, y) => x[0] - y[0]);
  for (const [_, todo] of todosSorted) {
    todo();
  }
};
var addTodo = (txnId, journal, todoEffect) => {
  let added = false;
  for (const [, entry] of journal) {
    if (!entry.ref.todos.has(txnId)) {
      entry.ref.todos.set(txnId, todoEffect);
      added = true;
    }
  }
  return added;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/opCodes/stm.js
var OP_WITH_STM_RUNTIME = "WithSTMRuntime";
var OP_ON_FAILURE = "OnFailure";
var OP_ON_RETRY = "OnRetry";
var OP_ON_SUCCESS = "OnSuccess";
var OP_PROVIDE2 = "Provide";
var OP_SYNC = "Sync";
var OP_SUCCEED2 = "Succeed";
var OP_RETRY = "Retry";
var OP_FAIL2 = "Fail";
var OP_DIE = "Die";
var OP_INTERRUPT = "Interrupt";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/opCodes/tExit.js
var OP_FAIL3 = "Fail";
var OP_DIE2 = "Die";
var OP_INTERRUPT2 = "Interrupt";
var OP_SUCCEED3 = "Succeed";
var OP_RETRY2 = "Retry";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/opCodes/tryCommit.js
var OP_DONE3 = "Done";
var OP_SUSPEND2 = "Suspend";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/opCodes/stmState.js
var OP_DONE4 = "Done";
var OP_INTERRUPTED = "Interrupted";
var OP_RUNNING = "Running";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/stmState.js
var STMStateSymbolKey = "effect/STM/State";
var STMStateTypeId = Symbol.for(STMStateSymbolKey);
var isSTMState = (u) => hasProperty(u, STMStateTypeId);
var isRunning = (self) => {
  return self._tag === OP_RUNNING;
};
var isDone2 = (self) => {
  return self._tag === OP_DONE4;
};
var done = (exit2) => {
  return {
    [STMStateTypeId]: STMStateTypeId,
    _tag: OP_DONE4,
    exit: exit2,
    [symbol]() {
      return pipe(hash(STMStateSymbolKey), combine(hash(OP_DONE4)), combine(hash(exit2)), cached(this));
    },
    [symbol2](that) {
      return isSTMState(that) && that._tag === OP_DONE4 && equals(exit2, that.exit);
    }
  };
};
var interruptedHash = pipe(hash(STMStateSymbolKey), combine(hash(OP_INTERRUPTED)), combine(hash("interrupted")));
var interrupted = {
  [STMStateTypeId]: STMStateTypeId,
  _tag: OP_INTERRUPTED,
  [symbol]() {
    return interruptedHash;
  },
  [symbol2](that) {
    return isSTMState(that) && that._tag === OP_INTERRUPTED;
  }
};
var runningHash = pipe(hash(STMStateSymbolKey), combine(hash(OP_RUNNING)), combine(hash("running")));
var running = {
  [STMStateTypeId]: STMStateTypeId,
  _tag: OP_RUNNING,
  [symbol]() {
    return runningHash;
  },
  [symbol2](that) {
    return isSTMState(that) && that._tag === OP_RUNNING;
  }
};
var fromTExit = (tExit) => {
  switch (tExit._tag) {
    case OP_FAIL3: {
      return done(fail(tExit.error));
    }
    case OP_DIE2: {
      return done(die(tExit.defect));
    }
    case OP_INTERRUPT2: {
      return done(interrupt2(tExit.fiberId));
    }
    case OP_SUCCEED3: {
      return done(succeed2(tExit.value));
    }
    case OP_RETRY2: {
      throw new Error("BUG: STM.STMState.fromTExit - please report an issue at https://github.com/Effect-TS/effect/issues");
    }
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tExit.js
var TExitSymbolKey = "effect/TExit";
var TExitTypeId = Symbol.for(TExitSymbolKey);
var variance2 = {
  /* c8 ignore next */
  _A: (_) => _,
  /* c8 ignore next */
  _E: (_) => _
};
var isExit2 = (u) => hasProperty(u, TExitTypeId);
var isSuccess2 = (self) => {
  return self._tag === OP_SUCCEED3;
};
var isRetry = (self) => {
  return self._tag === OP_RETRY2;
};
var fail7 = (error) => ({
  [TExitTypeId]: variance2,
  _tag: OP_FAIL3,
  error,
  [symbol]() {
    return pipe(hash(TExitSymbolKey), combine(hash(OP_FAIL3)), combine(hash(error)), cached(this));
  },
  [symbol2](that) {
    return isExit2(that) && that._tag === OP_FAIL3 && equals(error, that.error);
  }
});
var die5 = (defect) => ({
  [TExitTypeId]: variance2,
  _tag: OP_DIE2,
  defect,
  [symbol]() {
    return pipe(hash(TExitSymbolKey), combine(hash(OP_DIE2)), combine(hash(defect)), cached(this));
  },
  [symbol2](that) {
    return isExit2(that) && that._tag === OP_DIE2 && equals(defect, that.defect);
  }
});
var interrupt6 = (fiberId3) => ({
  [TExitTypeId]: variance2,
  _tag: OP_INTERRUPT2,
  fiberId: fiberId3,
  [symbol]() {
    return pipe(hash(TExitSymbolKey), combine(hash(OP_INTERRUPT2)), combine(hash(fiberId3)), cached(this));
  },
  [symbol2](that) {
    return isExit2(that) && that._tag === OP_INTERRUPT2 && equals(fiberId3, that.fiberId);
  }
});
var succeed9 = (value) => ({
  [TExitTypeId]: variance2,
  _tag: OP_SUCCEED3,
  value,
  [symbol]() {
    return pipe(hash(TExitSymbolKey), combine(hash(OP_SUCCEED3)), combine(hash(value)), cached(this));
  },
  [symbol2](that) {
    return isExit2(that) && that._tag === OP_SUCCEED3 && equals(value, that.value);
  }
});
var retryHash = pipe(hash(TExitSymbolKey), combine(hash(OP_RETRY2)), combine(hash("retry")));
var retry = {
  [TExitTypeId]: variance2,
  _tag: OP_RETRY2,
  [symbol]() {
    return retryHash;
  },
  [symbol2](that) {
    return isExit2(that) && isRetry(that);
  }
};
var void_4 = succeed9(void 0);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tryCommit.js
var done2 = (exit2) => {
  return {
    _tag: OP_DONE3,
    exit: exit2
  };
};
var suspend5 = (journal) => {
  return {
    _tag: OP_SUSPEND2,
    journal
  };
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/txnId.js
var txnCounter = {
  ref: 0
};
var make17 = () => {
  const newId = txnCounter.ref + 1;
  txnCounter.ref = newId;
  return newId;
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/core.js
var STMSymbolKey = "effect/STM";
var STMTypeId = Symbol.for(STMSymbolKey);
var stmVariance = {
  /* c8 ignore next */
  _R: (_) => _,
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _A: (_) => _
};
var STMPrimitive = class {
  effect_instruction_i0;
  _op = OP_COMMIT;
  effect_instruction_i1 = void 0;
  effect_instruction_i2 = void 0;
  [EffectTypeId];
  [StreamTypeId];
  [SinkTypeId];
  [ChannelTypeId];
  get [STMTypeId]() {
    return stmVariance;
  }
  constructor(effect_instruction_i0) {
    this.effect_instruction_i0 = effect_instruction_i0;
    this[EffectTypeId] = effectVariance;
    this[StreamTypeId] = stmVariance;
    this[SinkTypeId] = stmVariance;
    this[ChannelTypeId] = stmVariance;
  }
  [symbol2](that) {
    return this === that;
  }
  [symbol]() {
    return cached(this, random(this));
  }
  [Symbol.iterator]() {
    return new SingleShotGen(new YieldWrap(this));
  }
  commit() {
    return unsafeAtomically(this, constVoid, constVoid);
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isSTM = (u) => hasProperty(u, STMTypeId);
var commit2 = (self) => unsafeAtomically(self, constVoid, constVoid);
var unsafeAtomically = (self, onDone3, onInterrupt2) => withFiberRuntime((state) => {
  const fiberId3 = state.id();
  const env = state.getFiberRef(currentContext2);
  const scheduler = state.getFiberRef(currentScheduler2);
  const priority = state.getFiberRef(currentSchedulingPriority2);
  const commitResult = tryCommitSync(fiberId3, self, env, scheduler, priority);
  switch (commitResult._tag) {
    case OP_DONE3: {
      onDone3(commitResult.exit);
      return commitResult.exit;
    }
    case OP_SUSPEND2: {
      const txnId = make17();
      const state2 = {
        value: running
      };
      const effect3 = async((k) => tryCommitAsync(fiberId3, self, txnId, state2, env, scheduler, priority, k));
      return uninterruptibleMask2((restore) => pipe(restore(effect3), catchAllCause((cause) => {
        let currentState = state2.value;
        if (isRunning(currentState)) {
          state2.value = interrupted;
        }
        currentState = state2.value;
        if (isDone2(currentState)) {
          onDone3(currentState.exit);
          return currentState.exit;
        }
        onInterrupt2();
        return failCause3(cause);
      })));
    }
  }
});
var tryCommit = (fiberId3, stm, state, env, scheduler, priority) => {
  const journal = /* @__PURE__ */ new Map();
  const tExit = new STMDriver(stm, journal, fiberId3, env).run();
  const analysis = analyzeJournal(journal);
  if (analysis === JournalAnalysisReadWrite) {
    commitJournal(journal);
  } else if (analysis === JournalAnalysisInvalid) {
    throw new Error("BUG: STM.TryCommit.tryCommit - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  switch (tExit._tag) {
    case OP_SUCCEED3: {
      state.value = fromTExit(tExit);
      return completeTodos(succeed2(tExit.value), journal, scheduler, priority);
    }
    case OP_FAIL3: {
      state.value = fromTExit(tExit);
      const cause = fail2(tExit.error);
      return completeTodos(failCause(cause), journal, scheduler, priority);
    }
    case OP_DIE2: {
      state.value = fromTExit(tExit);
      const cause = die2(tExit.defect);
      return completeTodos(failCause(cause), journal, scheduler, priority);
    }
    case OP_INTERRUPT2: {
      state.value = fromTExit(tExit);
      const cause = interrupt3(fiberId3);
      return completeTodos(failCause(cause), journal, scheduler, priority);
    }
    case OP_RETRY2: {
      return suspend5(journal);
    }
  }
};
var tryCommitSync = (fiberId3, stm, env, scheduler, priority) => {
  const journal = /* @__PURE__ */ new Map();
  const tExit = new STMDriver(stm, journal, fiberId3, env).run();
  const analysis = analyzeJournal(journal);
  if (analysis === JournalAnalysisReadWrite && isSuccess2(tExit)) {
    commitJournal(journal);
  } else if (analysis === JournalAnalysisInvalid) {
    throw new Error("BUG: STM.TryCommit.tryCommitSync - please report an issue at https://github.com/Effect-TS/effect/issues");
  }
  switch (tExit._tag) {
    case OP_SUCCEED3: {
      return completeTodos(succeed2(tExit.value), journal, scheduler, priority);
    }
    case OP_FAIL3: {
      const cause = fail2(tExit.error);
      return completeTodos(failCause(cause), journal, scheduler, priority);
    }
    case OP_DIE2: {
      const cause = die2(tExit.defect);
      return completeTodos(failCause(cause), journal, scheduler, priority);
    }
    case OP_INTERRUPT2: {
      const cause = interrupt3(fiberId3);
      return completeTodos(failCause(cause), journal, scheduler, priority);
    }
    case OP_RETRY2: {
      return suspend5(journal);
    }
  }
};
var tryCommitAsync = (fiberId3, self, txnId, state, context8, scheduler, priority, k) => {
  if (isRunning(state.value)) {
    const result = tryCommit(fiberId3, self, state, context8, scheduler, priority);
    switch (result._tag) {
      case OP_DONE3: {
        completeTryCommit(result.exit, k);
        break;
      }
      case OP_SUSPEND2: {
        addTodo(txnId, result.journal, () => tryCommitAsync(fiberId3, self, txnId, state, context8, scheduler, priority, k));
        break;
      }
    }
  }
};
var completeTodos = (exit2, journal, scheduler, priority) => {
  const todos = collectTodos(journal);
  if (todos.size > 0) {
    scheduler.scheduleTask(() => execTodos(todos), priority);
  }
  return done2(exit2);
};
var completeTryCommit = (exit2, k) => {
  k(exit2);
};
var context5 = () => effect2((_, __, env) => env);
var contextWith4 = (f) => map11(context5(), f);
var contextWithSTM = (f) => flatMap6(context5(), f);
var STMDriver = class {
  self;
  journal;
  fiberId;
  contStack = [];
  env;
  constructor(self, journal, fiberId3, r0) {
    this.self = self;
    this.journal = journal;
    this.fiberId = fiberId3;
    this.env = r0;
  }
  getEnv() {
    return this.env;
  }
  pushStack(cont) {
    this.contStack.push(cont);
  }
  popStack() {
    return this.contStack.pop();
  }
  nextSuccess() {
    let current2 = this.popStack();
    while (current2 !== void 0 && current2.effect_instruction_i0 !== OP_ON_SUCCESS) {
      current2 = this.popStack();
    }
    return current2;
  }
  nextFailure() {
    let current2 = this.popStack();
    while (current2 !== void 0 && current2.effect_instruction_i0 !== OP_ON_FAILURE) {
      current2 = this.popStack();
    }
    return current2;
  }
  nextRetry() {
    let current2 = this.popStack();
    while (current2 !== void 0 && current2.effect_instruction_i0 !== OP_ON_RETRY) {
      current2 = this.popStack();
    }
    return current2;
  }
  run() {
    let curr = this.self;
    let exit2 = void 0;
    while (exit2 === void 0 && curr !== void 0) {
      try {
        const current2 = curr;
        if (current2) {
          switch (current2._op) {
            case "Tag": {
              curr = effect2((_, __, env) => unsafeGet2(env, current2));
              break;
            }
            case "Left": {
              curr = fail8(current2.left);
              break;
            }
            case "None": {
              curr = fail8(new NoSuchElementException());
              break;
            }
            case "Right": {
              curr = succeed10(current2.right);
              break;
            }
            case "Some": {
              curr = succeed10(current2.value);
              break;
            }
            case "Commit": {
              switch (current2.effect_instruction_i0) {
                case OP_DIE: {
                  exit2 = die5(internalCall(() => current2.effect_instruction_i1()));
                  break;
                }
                case OP_FAIL2: {
                  const cont = this.nextFailure();
                  if (cont === void 0) {
                    exit2 = fail7(internalCall(() => current2.effect_instruction_i1()));
                  } else {
                    curr = internalCall(() => cont.effect_instruction_i2(internalCall(() => current2.effect_instruction_i1())));
                  }
                  break;
                }
                case OP_RETRY: {
                  const cont = this.nextRetry();
                  if (cont === void 0) {
                    exit2 = retry;
                  } else {
                    curr = internalCall(() => cont.effect_instruction_i2());
                  }
                  break;
                }
                case OP_INTERRUPT: {
                  exit2 = interrupt6(this.fiberId);
                  break;
                }
                case OP_WITH_STM_RUNTIME: {
                  curr = internalCall(() => current2.effect_instruction_i1(this));
                  break;
                }
                case OP_ON_SUCCESS:
                case OP_ON_FAILURE:
                case OP_ON_RETRY: {
                  this.pushStack(current2);
                  curr = current2.effect_instruction_i1;
                  break;
                }
                case OP_PROVIDE2: {
                  const env = this.env;
                  this.env = internalCall(() => current2.effect_instruction_i2(env));
                  curr = pipe(current2.effect_instruction_i1, ensuring7(sync7(() => this.env = env)));
                  break;
                }
                case OP_SUCCEED2: {
                  const value = current2.effect_instruction_i1;
                  const cont = this.nextSuccess();
                  if (cont === void 0) {
                    exit2 = succeed9(value);
                  } else {
                    curr = internalCall(() => cont.effect_instruction_i2(value));
                  }
                  break;
                }
                case OP_SYNC: {
                  const value = internalCall(() => current2.effect_instruction_i1());
                  const cont = this.nextSuccess();
                  if (cont === void 0) {
                    exit2 = succeed9(value);
                  } else {
                    curr = internalCall(() => cont.effect_instruction_i2(value));
                  }
                  break;
                }
              }
              break;
            }
          }
        }
      } catch (e) {
        curr = die6(e);
      }
    }
    return exit2;
  }
};
var catchAll4 = dual(2, (self, f) => {
  const stm = new STMPrimitive(OP_ON_FAILURE);
  stm.effect_instruction_i1 = self;
  stm.effect_instruction_i2 = f;
  return stm;
});
var mapInputContext4 = dual(2, (self, f) => {
  const stm = new STMPrimitive(OP_PROVIDE2);
  stm.effect_instruction_i1 = self;
  stm.effect_instruction_i2 = f;
  return stm;
});
var die6 = (defect) => dieSync3(() => defect);
var dieMessage3 = (message) => dieSync3(() => new RuntimeException(message));
var dieSync3 = (evaluate2) => {
  const stm = new STMPrimitive(OP_DIE);
  stm.effect_instruction_i1 = evaluate2;
  return stm;
};
var effect2 = (f) => withSTMRuntime((_) => succeed10(f(_.journal, _.fiberId, _.getEnv())));
var ensuring7 = dual(2, (self, finalizer3) => matchSTM(self, {
  onFailure: (e) => zipRight8(finalizer3, fail8(e)),
  onSuccess: (a) => zipRight8(finalizer3, succeed10(a))
}));
var fail8 = (error) => failSync4(() => error);
var failSync4 = (evaluate2) => {
  const stm = new STMPrimitive(OP_FAIL2);
  stm.effect_instruction_i1 = evaluate2;
  return stm;
};
var flatMap6 = dual(2, (self, f) => {
  const stm = new STMPrimitive(OP_ON_SUCCESS);
  stm.effect_instruction_i1 = self;
  stm.effect_instruction_i2 = f;
  return stm;
});
var matchSTM = dual(2, (self, {
  onFailure,
  onSuccess
}) => pipe(self, map11(right), catchAll4((e) => pipe(onFailure(e), map11(left))), flatMap6((either6) => {
  switch (either6._tag) {
    case "Left": {
      return succeed10(either6.left);
    }
    case "Right": {
      return onSuccess(either6.right);
    }
  }
})));
var withSTMRuntime = (f) => {
  const stm = new STMPrimitive(OP_WITH_STM_RUNTIME);
  stm.effect_instruction_i1 = f;
  return stm;
};
var interrupt7 = withSTMRuntime((_) => {
  const stm = new STMPrimitive(OP_INTERRUPT);
  stm.effect_instruction_i1 = _.fiberId;
  return stm;
});
var interruptAs2 = (fiberId3) => {
  const stm = new STMPrimitive(OP_INTERRUPT);
  stm.effect_instruction_i1 = fiberId3;
  return stm;
};
var map11 = dual(2, (self, f) => pipe(self, flatMap6((a) => sync7(() => f(a)))));
var orTry = dual(2, (self, that) => {
  const stm = new STMPrimitive(OP_ON_RETRY);
  stm.effect_instruction_i1 = self;
  stm.effect_instruction_i2 = that;
  return stm;
});
var retry2 = new STMPrimitive(OP_RETRY);
var succeed10 = (value) => {
  const stm = new STMPrimitive(OP_SUCCEED2);
  stm.effect_instruction_i1 = value;
  return stm;
};
var sync7 = (evaluate2) => {
  const stm = new STMPrimitive(OP_SYNC);
  stm.effect_instruction_i1 = evaluate2;
  return stm;
};
var zip5 = dual(2, (self, that) => pipe(self, zipWith6(that, (a, a1) => [a, a1])));
var zipLeft6 = dual(2, (self, that) => pipe(self, flatMap6((a) => pipe(that, map11(() => a)))));
var zipRight8 = dual(2, (self, that) => pipe(self, flatMap6(() => that)));
var zipWith6 = dual(3, (self, that, f) => pipe(self, flatMap6((a) => pipe(that, map11((b) => f(a, b))))));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/opCodes/strategy.js
var OP_BACKPRESSURE_STRATEGY = "BackPressure";
var OP_DROPPING_STRATEGY = "Dropping";
var OP_SLIDING_STRATEGY = "Sliding";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/stm.js
var acquireUseRelease3 = dual(3, (acquire, use, release) => uninterruptibleMask2((restore) => {
  let state = running;
  return pipe(restore(unsafeAtomically(acquire, (exit2) => {
    state = done(exit2);
  }, () => {
    state = interrupted;
  })), matchCauseEffect({
    onFailure: (cause) => {
      if (isDone2(state) && isSuccess(state.exit)) {
        return pipe(release(state.exit.value), matchCauseEffect({
          onFailure: (cause2) => failCause3(parallel(cause, cause2)),
          onSuccess: () => failCause3(cause)
        }));
      }
      return failCause3(cause);
    },
    onSuccess: (a) => pipe(restore(use(a)), matchCauseEffect({
      onFailure: (cause) => pipe(release(a), matchCauseEffect({
        onFailure: (cause2) => failCause3(parallel(cause, cause2)),
        onSuccess: () => failCause3(cause)
      })),
      onSuccess: (a2) => pipe(release(a), as3(a2))
    }))
  }));
}));
var as7 = dual(2, (self, value) => pipe(self, map11(() => value)));
var asSome2 = (self) => pipe(self, map11(some));
var asSomeError = (self) => pipe(self, mapError4(some));
var asVoid6 = (self) => pipe(self, map11(constVoid));
var attempt = (evaluate2) => suspend6(() => {
  try {
    return succeed10(evaluate2());
  } catch (defect) {
    return fail8(defect);
  }
});
var bind2 = dual(3, (self, tag, f) => flatMap6(self, (k) => map11(f(k), (a) => ({
  ...k,
  [tag]: a
}))));
var bindTo3 = dual(2, (self, tag) => map11(self, (a) => ({
  [tag]: a
})));
var let_2 = dual(3, (self, tag, f) => map11(self, (k) => ({
  ...k,
  [tag]: f(k)
})));
var catchSome = dual(2, (self, pf) => catchAll4(self, (e) => getOrElse(pf(e), () => fail8(e))));
var catchTag = dual(3, (self, k, f) => catchAll4(self, (e) => {
  if ("_tag" in e && e["_tag"] === k) {
    return f(e);
  }
  return fail8(e);
}));
var catchTags = dual(2, (self, cases) => catchAll4(self, (e) => {
  const keys = Object.keys(cases);
  if ("_tag" in e && keys.includes(e["_tag"])) {
    return cases[e["_tag"]](e);
  }
  return fail8(e);
}));
var check3 = (predicate) => suspend6(() => predicate() ? void_5 : retry2);
var collect2 = dual(2, (self, pf) => collectSTM(self, (a) => map(pf(a), succeed10)));
var collectSTM = dual(2, (self, pf) => matchSTM(self, {
  onFailure: fail8,
  onSuccess: (a) => {
    const option2 = pf(a);
    return isSome(option2) ? option2.value : retry2;
  }
}));
var commitEither = (self) => flatten2(commit2(either3(self)));
var cond = (predicate, error, result) => {
  return suspend6(() => predicate() ? sync7(result) : failSync4(error));
};
var either3 = (self) => match14(self, {
  onFailure: left,
  onSuccess: right
});
var eventually = (self) => matchSTM(self, {
  onFailure: () => eventually(self),
  onSuccess: succeed10
});
var every3 = dual(2, (iterable, predicate) => flatMap6(sync7(() => iterable[Symbol.iterator]()), (iterator) => {
  const loop2 = suspend6(() => {
    const next = iterator.next();
    if (next.done) {
      return succeed10(true);
    }
    return pipe(predicate(next.value), flatMap6((bool) => bool ? loop2 : succeed10(bool)));
  });
  return loop2;
}));
var exists = dual(2, (iterable, predicate) => flatMap6(sync7(() => iterable[Symbol.iterator]()), (iterator) => {
  const loop2 = suspend6(() => {
    const next = iterator.next();
    if (next.done) {
      return succeed10(false);
    }
    return flatMap6(predicate(next.value), (bool) => bool ? succeed10(bool) : loop2);
  });
  return loop2;
}));
var fiberId2 = effect2((_, fiberId3) => fiberId3);
var filter3 = dual(2, (iterable, predicate) => Array.from(iterable).reduce((acc, curr) => pipe(acc, zipWith6(predicate(curr), (as10, p) => {
  if (p) {
    as10.push(curr);
    return as10;
  }
  return as10;
})), succeed10([])));
var filterNot = dual(2, (iterable, predicate) => filter3(iterable, (a) => negate(predicate(a))));
var filterOrDie = dual(3, (self, predicate, defect) => filterOrElse(self, predicate, () => dieSync3(defect)));
var filterOrDieMessage = dual(3, (self, predicate, message) => filterOrElse(self, predicate, () => dieMessage3(message)));
var filterOrElse = dual(3, (self, predicate, orElse8) => flatMap6(self, (a) => predicate(a) ? succeed10(a) : orElse8(a)));
var filterOrFail = dual(3, (self, predicate, orFailWith) => filterOrElse(self, predicate, (a) => failSync4(() => orFailWith(a))));
var flatten4 = (self) => flatMap6(self, identity);
var flip = (self) => matchSTM(self, {
  onFailure: succeed10,
  onSuccess: fail8
});
var flipWith = dual(2, (self, f) => flip(f(flip(self))));
var match14 = dual(2, (self, {
  onFailure,
  onSuccess
}) => matchSTM(self, {
  onFailure: (e) => succeed10(onFailure(e)),
  onSuccess: (a) => succeed10(onSuccess(a))
}));
var forEach4 = dual((args) => isIterable(args[0]), (iterable, f, options) => {
  if (options?.discard) {
    return pipe(sync7(() => iterable[Symbol.iterator]()), flatMap6((iterator) => {
      const loop2 = suspend6(() => {
        const next = iterator.next();
        if (next.done) {
          return void_5;
        }
        return pipe(f(next.value), flatMap6(() => loop2));
      });
      return loop2;
    }));
  }
  return suspend6(() => fromIterable(iterable).reduce((acc, curr) => zipWith6(acc, f(curr), (array, elem) => {
    array.push(elem);
    return array;
  }), succeed10([])));
});
var fromEither2 = (either6) => {
  switch (either6._tag) {
    case "Left": {
      return fail8(either6.left);
    }
    case "Right": {
      return succeed10(either6.right);
    }
  }
};
var fromOption2 = (option2) => match2(option2, {
  onNone: () => fail8(none()),
  onSome: succeed10
});
var gen2 = (...args) => suspend6(() => {
  const f = args.length === 1 ? args[0] : args[1].bind(args[0]);
  const iterator = f(pipe);
  const state = iterator.next();
  const run6 = (state2) => state2.done ? succeed10(state2.value) : flatMap6(yieldWrapGet(state2.value), (val) => run6(iterator.next(val)));
  return run6(state);
});
var head4 = (self) => pipe(self, matchSTM({
  onFailure: (e) => fail8(some(e)),
  onSuccess: (a) => {
    const i = a[Symbol.iterator]();
    const res = i.next();
    if (res.done) {
      return fail8(none());
    } else {
      return succeed10(res.value);
    }
  }
}));
var if_ = dual((args) => typeof args[0] === "boolean" || isSTM(args[0]), (self, {
  onFalse,
  onTrue
}) => {
  if (typeof self === "boolean") {
    return self ? onTrue : onFalse;
  }
  return flatMap6(self, (bool) => bool ? onTrue : onFalse);
});
var ignore2 = (self) => match14(self, {
  onFailure: () => void_5,
  onSuccess: () => void_5
});
var isFailure2 = (self) => match14(self, {
  onFailure: constTrue,
  onSuccess: constFalse
});
var isSuccess3 = (self) => match14(self, {
  onFailure: constFalse,
  onSuccess: constTrue
});
var iterate = (initial, options) => iterateLoop(initial, options.while, options.body);
var iterateLoop = (initial, cont, body) => {
  if (cont(initial)) {
    return pipe(body(initial), flatMap6((z) => iterateLoop(z, cont, body)));
  }
  return succeed10(initial);
};
var loop = (initial, options) => options.discard ? loopDiscardLoop(initial, options.while, options.step, options.body) : map11(loopLoop(initial, options.while, options.step, options.body), (a) => Array.from(a));
var loopLoop = (initial, cont, inc, body) => {
  if (cont(initial)) {
    return pipe(body(initial), flatMap6((a) => pipe(loopLoop(inc(initial), cont, inc, body), map11(append(a)))));
  }
  return succeed10(empty2());
};
var loopDiscardLoop = (initial, cont, inc, body) => {
  if (cont(initial)) {
    return pipe(body(initial), flatMap6(() => loopDiscardLoop(inc(initial), cont, inc, body)));
  }
  return void_5;
};
var mapAttempt = dual(2, (self, f) => matchSTM(self, {
  onFailure: (e) => fail8(e),
  onSuccess: (a) => attempt(() => f(a))
}));
var mapBoth5 = dual(2, (self, {
  onFailure,
  onSuccess
}) => matchSTM(self, {
  onFailure: (e) => fail8(onFailure(e)),
  onSuccess: (a) => succeed10(onSuccess(a))
}));
var mapError4 = dual(2, (self, f) => matchSTM(self, {
  onFailure: (e) => fail8(f(e)),
  onSuccess: succeed10
}));
var merge4 = (self) => matchSTM(self, {
  onFailure: (e) => succeed10(e),
  onSuccess: succeed10
});
var mergeAll2 = dual(3, (iterable, zero2, f) => suspend6(() => Array.from(iterable).reduce((acc, curr) => pipe(acc, zipWith6(curr, f)), succeed10(zero2))));
var negate = (self) => pipe(self, map11((b) => !b));
var none2 = (self) => matchSTM(self, {
  onFailure: (e) => fail8(some(e)),
  onSuccess: match2({
    onNone: () => void_5,
    onSome: () => fail8(none())
  })
});
var option = (self) => match14(self, {
  onFailure: () => none(),
  onSuccess: some
});
var orDie3 = (self) => pipe(self, orDieWith2(identity));
var orDieWith2 = dual(2, (self, f) => pipe(self, mapError4(f), catchAll4(die6)));
var orElse5 = dual(2, (self, that) => flatMap6(effect2((journal) => prepareResetJournal(journal)), (reset2) => pipe(orTry(self, () => flatMap6(sync7(reset2), that)), catchAll4(() => flatMap6(sync7(reset2), that)))));
var orElseEither = dual(2, (self, that) => orElse5(map11(self, left), () => map11(that(), right)));
var orElseFail = dual(2, (self, error) => orElse5(self, () => failSync4(error)));
var orElseOptional = dual(2, (self, that) => catchAll4(self, match2({
  onNone: that,
  onSome: (e) => fail8(some(e))
})));
var orElseSucceed = dual(2, (self, value) => orElse5(self, () => sync7(value)));
var provideContext5 = dual(2, (self, env) => mapInputContext4(self, (_) => env));
var provideSomeContext = dual(2, (self, context8) => mapInputContext4(self, (parent) => merge2(parent, context8)));
var provideService6 = dual(3, (self, tag, resource) => provideServiceSTM(self, tag, succeed10(resource)));
var provideServiceSTM = dual(3, (self, tag, stm) => contextWithSTM((env) => flatMap6(stm, (service) => provideContext5(self, add(env, tag, service)))));
var reduce5 = dual(3, (iterable, zero2, f) => suspend6(() => Array.from(iterable).reduce((acc, curr) => pipe(acc, flatMap6((s) => f(s, curr))), succeed10(zero2))));
var reduceAll = dual(3, (iterable, initial, f) => suspend6(() => Array.from(iterable).reduce((acc, curr) => pipe(acc, zipWith6(curr, f)), initial)));
var reduceRight2 = dual(3, (iterable, zero2, f) => suspend6(() => Array.from(iterable).reduceRight((acc, curr) => pipe(acc, flatMap6((s) => f(s, curr))), succeed10(zero2))));
var refineOrDie2 = dual(2, (self, pf) => refineOrDieWith2(self, pf, identity));
var refineOrDieWith2 = dual(3, (self, pf, f) => catchAll4(self, (e) => match2(pf(e), {
  onNone: () => die6(f(e)),
  onSome: fail8
})));
var reject = dual(2, (self, pf) => rejectSTM(self, (a) => map(pf(a), fail8)));
var rejectSTM = dual(2, (self, pf) => flatMap6(self, (a) => match2(pf(a), {
  onNone: () => succeed10(a),
  onSome: flatMap6(fail8)
})));
var repeatUntil = dual(2, (self, predicate) => repeatUntilLoop(self, predicate));
var repeatUntilLoop = (self, predicate) => flatMap6(self, (a) => predicate(a) ? succeed10(a) : repeatUntilLoop(self, predicate));
var repeatWhile = dual(2, (self, predicate) => repeatWhileLoop(self, predicate));
var repeatWhileLoop = (self, predicate) => flatMap6(self, (a) => predicate(a) ? repeatWhileLoop(self, predicate) : succeed10(a));
var replicate = dual(2, (self, n) => Array.from({
  length: n
}, () => self));
var replicateSTM = dual(2, (self, n) => all4(replicate(self, n)));
var replicateSTMDiscard = dual(2, (self, n) => all4(replicate(self, n), {
  discard: true
}));
var retryUntil = dual(2, (self, predicate) => matchSTM(self, {
  onFailure: fail8,
  onSuccess: (a) => predicate(a) ? succeed10(a) : retry2
}));
var retryWhile = dual(2, (self, predicate) => matchSTM(self, {
  onFailure: fail8,
  onSuccess: (a) => !predicate(a) ? succeed10(a) : retry2
}));
var partition = dual(2, (elements, f) => pipe(forEach4(elements, (a) => either3(f(a))), map11((as10) => partitionMap(as10, identity))));
var some3 = (self) => matchSTM(self, {
  onFailure: (e) => fail8(some(e)),
  onSuccess: match2({
    onNone: () => fail8(none()),
    onSome: succeed10
  })
});
var all4 = (input, options) => {
  if (Symbol.iterator in input) {
    return forEach4(input, identity, options);
  } else if (options?.discard) {
    return forEach4(Object.values(input), identity, options);
  }
  return map11(forEach4(Object.entries(input), ([_, e]) => map11(e, (a) => [_, a])), (values) => {
    const res = {};
    for (const [k, v] of values) {
      ;
      res[k] = v;
    }
    return res;
  });
};
var succeedNone2 = succeed10(none());
var succeedSome = (value) => succeed10(some(value));
var summarized2 = dual(3, (self, summary, f) => flatMap6(summary, (start) => flatMap6(self, (value) => map11(summary, (end4) => [f(start, end4), value]))));
var suspend6 = (evaluate2) => flatten4(sync7(evaluate2));
var tap3 = dual(2, (self, f) => flatMap6(self, (a) => as7(f(a), a)));
var tapBoth = dual(2, (self, {
  onFailure,
  onSuccess
}) => matchSTM(self, {
  onFailure: (e) => pipe(onFailure(e), zipRight8(fail8(e))),
  onSuccess: (a) => pipe(onSuccess(a), as7(a))
}));
var tapError = dual(2, (self, f) => matchSTM(self, {
  onFailure: (e) => zipRight8(f(e), fail8(e)),
  onSuccess: succeed10
}));
var try_ = (arg) => {
  const evaluate2 = typeof arg === "function" ? arg : arg.try;
  return suspend6(() => {
    try {
      return succeed10(evaluate2());
    } catch (error) {
      return fail8("catch" in arg ? arg.catch(error) : error);
    }
  });
};
var void_5 = succeed10(void 0);
var unless = dual(2, (self, predicate) => suspend6(() => predicate() ? succeedNone2 : asSome2(self)));
var unlessSTM = dual(2, (self, predicate) => flatMap6(predicate, (bool) => bool ? succeedNone2 : asSome2(self)));
var unsome = (self) => matchSTM(self, {
  onFailure: match2({
    onNone: () => succeed10(none()),
    onSome: fail8
  }),
  onSuccess: (a) => succeed10(some(a))
});
var validateAll = dual(2, (elements, f) => flatMap6(partition(elements, f), ([errors, values]) => isNonEmptyArray(errors) ? fail8(errors) : succeed10(values)));
var validateFirst = dual(2, (elements, f) => flip(forEach4(elements, (a) => flip(f(a)))));
var when2 = dual(2, (self, predicate) => suspend6(() => predicate() ? asSome2(self) : succeedNone2));
var whenSTM = dual(2, (self, predicate) => flatMap6(predicate, (bool) => bool ? asSome2(self) : succeedNone2));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tRef.js
var TRefSymbolKey = "effect/TRef";
var TRefTypeId = Symbol.for(TRefSymbolKey);
var tRefVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var TRefImpl = class {
  [TRefTypeId] = tRefVariance;
  /** @internal */
  todos;
  /** @internal */
  versioned;
  constructor(value) {
    this.versioned = new Versioned(value);
    this.todos = /* @__PURE__ */ new Map();
  }
  modify(f) {
    return effect2((journal) => {
      const entry = getOrMakeEntry(this, journal);
      const [retValue, newValue] = f(unsafeGet4(entry));
      unsafeSet(entry, newValue);
      return retValue;
    });
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var make18 = (value) => effect2((journal) => {
  const ref = new TRefImpl(value);
  journal.set(ref, make16(ref, true));
  return ref;
});
var get7 = (self) => self.modify((a) => [a, a]);
var set5 = dual(2, (self, value) => self.modify(() => [void 0, value]));
var getAndSet3 = dual(2, (self, value) => self.modify((a) => [a, value]));
var getAndUpdate2 = dual(2, (self, f) => self.modify((a) => [a, f(a)]));
var getAndUpdateSome2 = dual(2, (self, f) => self.modify((a) => match2(f(a), {
  onNone: () => [a, a],
  onSome: (b) => [a, b]
})));
var setAndGet = dual(2, (self, value) => self.modify(() => [value, value]));
var modify3 = dual(2, (self, f) => self.modify(f));
var modifySome2 = dual(3, (self, fallback, f) => self.modify((a) => match2(f(a), {
  onNone: () => [fallback, a],
  onSome: (b) => b
})));
var update3 = dual(2, (self, f) => self.modify((a) => [void 0, f(a)]));
var updateAndGet2 = dual(2, (self, f) => self.modify((a) => {
  const b = f(a);
  return [b, b];
}));
var updateSome2 = dual(2, (self, f) => self.modify((a) => [void 0, match2(f(a), {
  onNone: () => a,
  onSome: (b) => b
})]));
var updateSomeAndGet2 = dual(2, (self, f) => self.modify((a) => match2(f(a), {
  onNone: () => [a, a],
  onSome: (b) => [b, b]
})));
var getOrMakeEntry = (self, journal) => {
  if (journal.has(self)) {
    return journal.get(self);
  }
  const entry = make16(self, false);
  journal.set(self, entry);
  return entry;
};
var unsafeGet5 = dual(2, (self, journal) => unsafeGet4(getOrMakeEntry(self, journal)));
var unsafeSet2 = dual(3, (self, value, journal) => {
  const entry = getOrMakeEntry(self, journal);
  unsafeSet(entry, value);
  return void 0;
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tQueue.js
var TEnqueueSymbolKey = "effect/TQueue/TEnqueue";
var TEnqueueTypeId = Symbol.for(TEnqueueSymbolKey);
var TDequeueSymbolKey = "effect/TQueue/TDequeue";
var TDequeueTypeId = Symbol.for(TDequeueSymbolKey);
var BackPressure2 = {
  _tag: OP_BACKPRESSURE_STRATEGY
};
var Dropping = {
  _tag: OP_DROPPING_STRATEGY
};
var Sliding = {
  _tag: OP_SLIDING_STRATEGY
};
var tDequeueVariance = {
  /* c8 ignore next */
  _Out: (_) => _
};
var tEnqueueVariance = {
  /* c8 ignore next */
  _In: (_) => _
};
var TQueueImpl = class {
  ref;
  requestedCapacity;
  strategy;
  [TDequeueTypeId] = tDequeueVariance;
  [TEnqueueTypeId] = tEnqueueVariance;
  constructor(ref, requestedCapacity, strategy) {
    this.ref = ref;
    this.requestedCapacity = requestedCapacity;
    this.strategy = strategy;
  }
  capacity() {
    return this.requestedCapacity;
  }
  size = withSTMRuntime((runtime2) => {
    const queue = unsafeGet5(this.ref, runtime2.journal);
    if (queue === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    return succeed10(queue.length);
  });
  isFull = map11(this.size, (size10) => size10 === this.requestedCapacity);
  isEmpty = map11(this.size, (size10) => size10 === 0);
  shutdown = withSTMRuntime((runtime2) => {
    unsafeSet2(this.ref, void 0, runtime2.journal);
    return void_5;
  });
  isShutdown = effect2((journal) => {
    const queue = unsafeGet5(this.ref, journal);
    return queue === void 0;
  });
  awaitShutdown = flatMap6(this.isShutdown, (isShutdown9) => isShutdown9 ? void_5 : retry2);
  offer(value) {
    return withSTMRuntime((runtime2) => {
      const queue = pipe(this.ref, unsafeGet5(runtime2.journal));
      if (queue === void 0) {
        return interruptAs2(runtime2.fiberId);
      }
      if (queue.length < this.requestedCapacity) {
        queue.push(value);
        unsafeSet2(this.ref, queue, runtime2.journal);
        return succeed10(true);
      }
      switch (this.strategy._tag) {
        case OP_BACKPRESSURE_STRATEGY: {
          return retry2;
        }
        case OP_DROPPING_STRATEGY: {
          return succeed10(false);
        }
        case OP_SLIDING_STRATEGY: {
          if (queue.length === 0) {
            return succeed10(true);
          }
          queue.shift();
          queue.push(value);
          unsafeSet2(this.ref, queue, runtime2.journal);
          return succeed10(true);
        }
      }
    });
  }
  offerAll(iterable) {
    return withSTMRuntime((runtime2) => {
      const as10 = Array.from(iterable);
      const queue = unsafeGet5(this.ref, runtime2.journal);
      if (queue === void 0) {
        return interruptAs2(runtime2.fiberId);
      }
      if (queue.length + as10.length <= this.requestedCapacity) {
        unsafeSet2(this.ref, [...queue, ...as10], runtime2.journal);
        return succeed10(true);
      }
      switch (this.strategy._tag) {
        case OP_BACKPRESSURE_STRATEGY: {
          return retry2;
        }
        case OP_DROPPING_STRATEGY: {
          const forQueue = as10.slice(0, this.requestedCapacity - queue.length);
          unsafeSet2(this.ref, [...queue, ...forQueue], runtime2.journal);
          return succeed10(false);
        }
        case OP_SLIDING_STRATEGY: {
          const forQueue = as10.slice(0, this.requestedCapacity - queue.length);
          const toDrop = queue.length + forQueue.length - this.requestedCapacity;
          const newQueue = queue.slice(toDrop);
          unsafeSet2(this.ref, [...newQueue, ...forQueue], runtime2.journal);
          return succeed10(true);
        }
      }
    });
  }
  peek = withSTMRuntime((runtime2) => {
    const queue = unsafeGet5(this.ref, runtime2.journal);
    if (queue === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    if (queue.length === 0) {
      return retry2;
    }
    return succeed10(queue[0]);
  });
  peekOption = withSTMRuntime((runtime2) => {
    const queue = unsafeGet5(this.ref, runtime2.journal);
    if (queue === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    return succeed10(fromNullable(queue[0]));
  });
  take = withSTMRuntime((runtime2) => {
    const queue = unsafeGet5(this.ref, runtime2.journal);
    if (queue === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    if (queue.length === 0) {
      return retry2;
    }
    const dequeued = queue.shift();
    unsafeSet2(this.ref, queue, runtime2.journal);
    return succeed10(dequeued);
  });
  takeAll = withSTMRuntime((runtime2) => {
    const queue = unsafeGet5(this.ref, runtime2.journal);
    if (queue === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    unsafeSet2(this.ref, [], runtime2.journal);
    return succeed10(queue);
  });
  takeUpTo(max) {
    return withSTMRuntime((runtime2) => {
      const queue = unsafeGet5(this.ref, runtime2.journal);
      if (queue === void 0) {
        return interruptAs2(runtime2.fiberId);
      }
      const [toTake, remaining] = splitAt2(unsafeFromArray(queue), max);
      unsafeSet2(this.ref, Array.from(remaining), runtime2.journal);
      return succeed10(Array.from(toTake));
    });
  }
};
var isTQueue = (u) => {
  return isTEnqueue(u) && isTDequeue(u);
};
var isTEnqueue = (u) => hasProperty(u, TEnqueueTypeId);
var isTDequeue = (u) => hasProperty(u, TDequeueTypeId);
var awaitShutdown5 = (self) => self.awaitShutdown;
var bounded6 = (requestedCapacity) => makeQueue(requestedCapacity, BackPressure2);
var capacity6 = (self) => {
  return self.capacity();
};
var dropping5 = (requestedCapacity) => makeQueue(requestedCapacity, Dropping);
var isEmpty7 = (self) => self.isEmpty;
var isFull5 = (self) => self.isFull;
var isShutdown5 = (self) => self.isShutdown;
var offer4 = dual(2, (self, value) => self.offer(value));
var offerAll4 = dual(2, (self, iterable) => self.offerAll(iterable));
var peek = (self) => self.peek;
var peekOption = (self) => self.peekOption;
var poll6 = (self) => pipe(self.takeUpTo(1), map11(head));
var seek = dual(2, (self, predicate) => seekLoop(self, predicate));
var seekLoop = (self, predicate) => flatMap6(self.take, (a) => predicate(a) ? succeed10(a) : seekLoop(self, predicate));
var shutdown5 = (self) => self.shutdown;
var size6 = (self) => self.size;
var sliding5 = (requestedCapacity) => makeQueue(requestedCapacity, Sliding);
var take5 = (self) => self.take;
var takeAll3 = (self) => self.takeAll;
var takeBetween3 = dual(3, (self, min, max) => suspend6(() => {
  const takeRemainder = (min2, max2, acc) => {
    if (max2 < min2) {
      return succeed10(acc);
    }
    return pipe(self.takeUpTo(max2), flatMap6((taken) => {
      const remaining = min2 - taken.length;
      if (remaining === 1) {
        return pipe(self.take, map11((a) => pipe(acc, appendAll(unsafeFromArray(taken)), append(a))));
      }
      if (remaining > 1) {
        return pipe(self.take, flatMap6((a) => takeRemainder(remaining - 1, max2 - taken.length - 1, pipe(acc, appendAll(unsafeFromArray(taken)), append(a)))));
      }
      return succeed10(pipe(acc, appendAll(unsafeFromArray(taken))));
    }));
  };
  return map11(takeRemainder(min, max, empty2()), (c) => Array.from(c));
}));
var takeN3 = dual(2, (self, n) => pipe(self, takeBetween3(n, n)));
var takeUpTo3 = dual(2, (self, max) => self.takeUpTo(max));
var unbounded6 = () => makeQueue(Number.MAX_SAFE_INTEGER, Dropping);
var makeQueue = (requestedCapacity, strategy) => map11(make18([]), (ref) => new TQueueImpl(ref, requestedCapacity, strategy));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stm/tPubSub.js
var TPubSubSymbolKey = "effect/TPubSub";
var TPubSubTypeId = Symbol.for(TPubSubSymbolKey);
var AbsentValue2 = Symbol.for("effect/TPubSub/AbsentValue");
var makeNode = (head5, subscribers, tail) => ({
  head: head5,
  subscribers,
  tail
});
var TPubSubImpl = class {
  pubsubSize;
  publisherHead;
  publisherTail;
  requestedCapacity;
  strategy;
  subscriberCount;
  subscribers;
  [TPubSubTypeId] = {
    _A: (_) => _
  };
  [TEnqueueTypeId] = tEnqueueVariance;
  constructor(pubsubSize, publisherHead, publisherTail, requestedCapacity, strategy, subscriberCount, subscribers) {
    this.pubsubSize = pubsubSize;
    this.publisherHead = publisherHead;
    this.publisherTail = publisherTail;
    this.requestedCapacity = requestedCapacity;
    this.strategy = strategy;
    this.subscriberCount = subscriberCount;
    this.subscribers = subscribers;
  }
  isShutdown = effect2((journal) => {
    const currentPublisherTail = unsafeGet5(this.publisherTail, journal);
    return currentPublisherTail === void 0;
  });
  awaitShutdown = flatMap6(this.isShutdown, (isShutdown9) => isShutdown9 ? void_5 : retry2);
  capacity() {
    return this.requestedCapacity;
  }
  size = withSTMRuntime((runtime2) => {
    const currentPublisherTail = unsafeGet5(this.publisherTail, runtime2.journal);
    if (currentPublisherTail === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    return succeed10(unsafeGet5(this.pubsubSize, runtime2.journal));
  });
  isEmpty = map11(this.size, (size10) => size10 === 0);
  isFull = map11(this.size, (size10) => size10 === this.capacity());
  offer(value) {
    return withSTMRuntime((runtime2) => {
      const currentPublisherTail = unsafeGet5(this.publisherTail, runtime2.journal);
      if (currentPublisherTail === void 0) {
        return interruptAs2(runtime2.fiberId);
      }
      const currentSubscriberCount = unsafeGet5(this.subscriberCount, runtime2.journal);
      if (currentSubscriberCount === 0) {
        return succeed10(true);
      }
      const currentPubSubSize = unsafeGet5(this.pubsubSize, runtime2.journal);
      if (currentPubSubSize < this.requestedCapacity) {
        const updatedPublisherTail = new TRefImpl(void 0);
        const updatedNode = makeNode(value, currentSubscriberCount, updatedPublisherTail);
        unsafeSet2(currentPublisherTail, updatedNode, runtime2.journal);
        unsafeSet2(this.publisherTail, updatedPublisherTail, runtime2.journal);
        unsafeSet2(this.pubsubSize, currentPubSubSize + 1, runtime2.journal);
        return succeed10(true);
      }
      switch (this.strategy._tag) {
        case OP_BACKPRESSURE_STRATEGY: {
          return retry2;
        }
        case OP_DROPPING_STRATEGY: {
          return succeed10(false);
        }
        case OP_SLIDING_STRATEGY: {
          if (this.requestedCapacity > 0) {
            let currentPublisherHead = unsafeGet5(this.publisherHead, runtime2.journal);
            let loop2 = true;
            while (loop2) {
              const node = unsafeGet5(currentPublisherHead, runtime2.journal);
              if (node === void 0) {
                return retry2;
              }
              const head5 = node.head;
              const tail = node.tail;
              if (head5 !== AbsentValue2) {
                const updatedNode2 = makeNode(AbsentValue2, node.subscribers, node.tail);
                unsafeSet2(currentPublisherHead, updatedNode2, runtime2.journal);
                unsafeSet2(this.publisherHead, tail, runtime2.journal);
                loop2 = false;
              } else {
                currentPublisherHead = tail;
              }
            }
          }
          const updatedPublisherTail = new TRefImpl(void 0);
          const updatedNode = makeNode(value, currentSubscriberCount, updatedPublisherTail);
          unsafeSet2(currentPublisherTail, updatedNode, runtime2.journal);
          unsafeSet2(this.publisherTail, updatedPublisherTail, runtime2.journal);
          return succeed10(true);
        }
      }
    });
  }
  offerAll(iterable) {
    return map11(forEach4(iterable, (a) => this.offer(a)), every(identity));
  }
  shutdown = effect2((journal) => {
    const currentPublisherTail = unsafeGet5(this.publisherTail, journal);
    if (currentPublisherTail !== void 0) {
      unsafeSet2(this.publisherTail, void 0, journal);
      const currentSubscribers = unsafeGet5(this.subscribers, journal);
      forEach(currentSubscribers, (subscriber) => {
        unsafeSet2(subscriber, void 0, journal);
      });
      unsafeSet2(this.subscribers, empty3(), journal);
    }
  });
};
var TPubSubSubscriptionImpl = class {
  pubsubSize;
  publisherHead;
  requestedCapacity;
  subscriberHead;
  subscriberCount;
  subscribers;
  [TPubSubTypeId] = TPubSubTypeId;
  [TDequeueTypeId] = tDequeueVariance;
  constructor(pubsubSize, publisherHead, requestedCapacity, subscriberHead, subscriberCount, subscribers) {
    this.pubsubSize = pubsubSize;
    this.publisherHead = publisherHead;
    this.requestedCapacity = requestedCapacity;
    this.subscriberHead = subscriberHead;
    this.subscriberCount = subscriberCount;
    this.subscribers = subscribers;
  }
  isShutdown = effect2((journal) => {
    const currentSubscriberHead = unsafeGet5(this.subscriberHead, journal);
    return currentSubscriberHead === void 0;
  });
  awaitShutdown = flatMap6(this.isShutdown, (isShutdown9) => isShutdown9 ? void_5 : retry2);
  capacity() {
    return this.requestedCapacity;
  }
  size = withSTMRuntime((runtime2) => {
    let currentSubscriberHead = unsafeGet5(this.subscriberHead, runtime2.journal);
    if (currentSubscriberHead === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    let loop2 = true;
    let size10 = 0;
    while (loop2) {
      const node = unsafeGet5(currentSubscriberHead, runtime2.journal);
      if (node === void 0) {
        loop2 = false;
      } else {
        const head5 = node.head;
        const tail = node.tail;
        if (head5 !== AbsentValue2) {
          size10 = size10 + 1;
          if (size10 >= Number.MAX_SAFE_INTEGER) {
            loop2 = false;
          }
        }
        currentSubscriberHead = tail;
      }
    }
    return succeed10(size10);
  });
  isEmpty = map11(this.size, (size10) => size10 === 0);
  isFull = map11(this.size, (size10) => size10 === this.capacity());
  peek = withSTMRuntime((runtime2) => {
    let currentSubscriberHead = unsafeGet5(this.subscriberHead, runtime2.journal);
    if (currentSubscriberHead === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    let value = AbsentValue2;
    let loop2 = true;
    while (loop2) {
      const node = unsafeGet5(currentSubscriberHead, runtime2.journal);
      if (node === void 0) {
        return retry2;
      }
      const head5 = node.head;
      const tail = node.tail;
      if (head5 !== AbsentValue2) {
        value = head5;
        loop2 = false;
      } else {
        currentSubscriberHead = tail;
      }
    }
    return succeed10(value);
  });
  peekOption = withSTMRuntime((runtime2) => {
    let currentSubscriberHead = unsafeGet5(this.subscriberHead, runtime2.journal);
    if (currentSubscriberHead === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    let value = none();
    let loop2 = true;
    while (loop2) {
      const node = unsafeGet5(currentSubscriberHead, runtime2.journal);
      if (node === void 0) {
        value = none();
        loop2 = false;
      } else {
        const head5 = node.head;
        const tail = node.tail;
        if (head5 !== AbsentValue2) {
          value = some(head5);
          loop2 = false;
        } else {
          currentSubscriberHead = tail;
        }
      }
    }
    return succeed10(value);
  });
  shutdown = effect2((journal) => {
    let currentSubscriberHead = unsafeGet5(this.subscriberHead, journal);
    if (currentSubscriberHead !== void 0) {
      unsafeSet2(this.subscriberHead, void 0, journal);
      let loop2 = true;
      while (loop2) {
        const node = unsafeGet5(currentSubscriberHead, journal);
        if (node === void 0) {
          loop2 = false;
        } else {
          const head5 = node.head;
          const tail = node.tail;
          if (head5 !== AbsentValue2) {
            const subscribers = node.subscribers;
            if (subscribers === 1) {
              const size10 = unsafeGet5(this.pubsubSize, journal);
              const updatedNode = makeNode(AbsentValue2, 0, tail);
              unsafeSet2(currentSubscriberHead, updatedNode, journal);
              unsafeSet2(this.publisherHead, tail, journal);
              unsafeSet2(this.pubsubSize, size10 - 1, journal);
            } else {
              const updatedNode = makeNode(head5, subscribers - 1, tail);
              unsafeSet2(currentSubscriberHead, updatedNode, journal);
            }
          }
          currentSubscriberHead = tail;
        }
      }
      const currentSubscriberCount = unsafeGet5(this.subscriberCount, journal);
      unsafeSet2(this.subscriberCount, currentSubscriberCount - 1, journal);
      unsafeSet2(this.subscribers, remove(unsafeGet5(this.subscribers, journal), this.subscriberHead), journal);
    }
  });
  take = withSTMRuntime((runtime2) => {
    let currentSubscriberHead = unsafeGet5(this.subscriberHead, runtime2.journal);
    if (currentSubscriberHead === void 0) {
      return interruptAs2(runtime2.fiberId);
    }
    let value = AbsentValue2;
    let loop2 = true;
    while (loop2) {
      const node = unsafeGet5(currentSubscriberHead, runtime2.journal);
      if (node === void 0) {
        return retry2;
      }
      const head5 = node.head;
      const tail = node.tail;
      if (head5 !== AbsentValue2) {
        const subscribers = node.subscribers;
        if (subscribers === 1) {
          const size10 = unsafeGet5(this.pubsubSize, runtime2.journal);
          const updatedNode = makeNode(AbsentValue2, 0, tail);
          unsafeSet2(currentSubscriberHead, updatedNode, runtime2.journal);
          unsafeSet2(this.publisherHead, tail, runtime2.journal);
          unsafeSet2(this.pubsubSize, size10 - 1, runtime2.journal);
        } else {
          const updatedNode = makeNode(head5, subscribers - 1, tail);
          unsafeSet2(currentSubscriberHead, updatedNode, runtime2.journal);
        }
        unsafeSet2(this.subscriberHead, tail, runtime2.journal);
        value = head5;
        loop2 = false;
      } else {
        currentSubscriberHead = tail;
      }
    }
    return succeed10(value);
  });
  takeAll = this.takeUpTo(Number.POSITIVE_INFINITY);
  takeUpTo(max) {
    return withSTMRuntime((runtime2) => {
      let currentSubscriberHead = unsafeGet5(this.subscriberHead, runtime2.journal);
      if (currentSubscriberHead === void 0) {
        return interruptAs2(runtime2.fiberId);
      }
      const builder = [];
      let n = 0;
      while (n !== max) {
        const node = unsafeGet5(currentSubscriberHead, runtime2.journal);
        if (node === void 0) {
          n = max;
        } else {
          const head5 = node.head;
          const tail = node.tail;
          if (head5 !== AbsentValue2) {
            const subscribers = node.subscribers;
            if (subscribers === 1) {
              const size10 = unsafeGet5(this.pubsubSize, runtime2.journal);
              const updatedNode = makeNode(AbsentValue2, 0, tail);
              unsafeSet2(currentSubscriberHead, updatedNode, runtime2.journal);
              unsafeSet2(this.publisherHead, tail, runtime2.journal);
              unsafeSet2(this.pubsubSize, size10 - 1, runtime2.journal);
            } else {
              const updatedNode = makeNode(head5, subscribers - 1, tail);
              unsafeSet2(currentSubscriberHead, updatedNode, runtime2.journal);
            }
            builder.push(head5);
            n = n + 1;
          }
          currentSubscriberHead = tail;
        }
      }
      unsafeSet2(this.subscriberHead, currentSubscriberHead, runtime2.journal);
      return succeed10(builder);
    });
  }
};
var makeTPubSub = (requestedCapacity, strategy) => pipe(all4([make18(void 0), make18(0)]), flatMap6(([empty7, pubsubSize]) => pipe(all4([make18(empty7), make18(empty7), make18(0), make18(empty3())]), map11(([publisherHead, publisherTail, subscriberCount, subscribers]) => new TPubSubImpl(pubsubSize, publisherHead, publisherTail, requestedCapacity, strategy, subscriberCount, subscribers)))));
var makeSubscription2 = (pubsubSize, publisherHead, publisherTail, requestedCapacity, subscriberCount, subscribers) => pipe(get7(publisherTail), flatMap6((currentPublisherTail) => pipe(all4([make18(currentPublisherTail), get7(subscriberCount), get7(subscribers)]), tap3(([_, currentSubscriberCount]) => pipe(subscriberCount, set5(currentSubscriberCount + 1))), tap3(([subscriberHead, _, currentSubscribers]) => pipe(subscribers, set5(pipe(currentSubscribers, add2(subscriberHead))))), map11(([subscriberHead]) => new TPubSubSubscriptionImpl(pubsubSize, publisherHead, requestedCapacity, subscriberHead, subscriberCount, subscribers)))));
var awaitShutdown6 = (self) => self.awaitShutdown;
var bounded7 = (requestedCapacity) => makeTPubSub(requestedCapacity, BackPressure2);
var capacity7 = (self) => self.capacity();
var dropping6 = (requestedCapacity) => makeTPubSub(requestedCapacity, Dropping);
var isEmpty8 = (self) => self.isEmpty;
var isFull6 = (self) => self.isFull;
var isShutdown6 = (self) => self.isShutdown;
var publish3 = dual(2, (self, value) => self.offer(value));
var publishAll3 = dual(2, (self, iterable) => self.offerAll(iterable));
var size7 = (self) => self.size;
var shutdown6 = (self) => self.shutdown;
var sliding6 = (requestedCapacity) => makeTPubSub(requestedCapacity, Sliding);
var subscribe3 = (self) => makeSubscription2(self.pubsubSize, self.publisherHead, self.publisherTail, self.requestedCapacity, self.subscriberCount, self.subscribers);
var subscribeScoped = (self) => acquireRelease2(subscribe3(self), (dequeue) => shutdown5(dequeue));
var unbounded7 = () => makeTPubSub(Number.MAX_SAFE_INTEGER, Dropping);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TPubSub.js
var TPubSubTypeId2 = TPubSubTypeId;
var awaitShutdown7 = awaitShutdown6;
var bounded8 = bounded7;
var capacity8 = capacity7;
var dropping7 = dropping6;
var isEmpty9 = isEmpty8;
var isFull7 = isFull6;
var shutdown7 = shutdown6;
var isShutdown7 = isShutdown6;
var publish4 = publish3;
var publishAll4 = publishAll3;
var size8 = size7;
var sliding7 = sliding6;
var subscribe4 = subscribe3;
var subscribeScoped2 = subscribeScoped;
var unbounded8 = unbounded7;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/TQueue.js
var TQueue_exports = {};
__export(TQueue_exports, {
  TDequeueTypeId: () => TDequeueTypeId2,
  TEnqueueTypeId: () => TEnqueueTypeId2,
  awaitShutdown: () => awaitShutdown8,
  bounded: () => bounded9,
  capacity: () => capacity9,
  dropping: () => dropping8,
  isEmpty: () => isEmpty10,
  isFull: () => isFull8,
  isShutdown: () => isShutdown8,
  isTDequeue: () => isTDequeue2,
  isTEnqueue: () => isTEnqueue2,
  isTQueue: () => isTQueue2,
  offer: () => offer5,
  offerAll: () => offerAll5,
  peek: () => peek2,
  peekOption: () => peekOption2,
  poll: () => poll7,
  seek: () => seek2,
  shutdown: () => shutdown8,
  size: () => size9,
  sliding: () => sliding8,
  take: () => take6,
  takeAll: () => takeAll4,
  takeBetween: () => takeBetween4,
  takeN: () => takeN4,
  takeUpTo: () => takeUpTo4,
  unbounded: () => unbounded9
});
var TDequeueTypeId2 = TDequeueTypeId;
var TEnqueueTypeId2 = TEnqueueTypeId;
var isTQueue2 = isTQueue;
var isTDequeue2 = isTDequeue;
var isTEnqueue2 = isTEnqueue;
var awaitShutdown8 = awaitShutdown5;
var bounded9 = bounded6;
var capacity9 = capacity6;
var dropping8 = dropping5;
var isEmpty10 = isEmpty7;
var isFull8 = isFull5;
var isShutdown8 = isShutdown5;
var offer5 = offer4;
var offerAll5 = offerAll4;
var peek2 = peek;
var peekOption2 = peekOption;
var poll7 = poll6;
var seek2 = seek;
var shutdown8 = shutdown5;
var size9 = size6;
var sliding8 = sliding5;
var take6 = take5;
var takeAll4 = takeAll3;
var takeBetween4 = takeBetween3;
var takeN4 = takeN3;
var takeUpTo4 = takeUpTo3;
var unbounded9 = unbounded6;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/ringBuffer.js
var RingBuffer = class {
  capacity;
  array;
  size = 0;
  current = 0;
  constructor(capacity10) {
    this.capacity = capacity10;
    this.array = Array.from({
      length: capacity10
    }, constUndefined);
  }
  head() {
    return fromNullable(this.array[this.current]);
  }
  lastOrNull() {
    if (this.size === 0) {
      return void 0;
    }
    const index = this.current === 0 ? this.array.length - 1 : this.current - 1;
    return this.array[index] ?? void 0;
  }
  put(value) {
    this.array[this.current] = value;
    this.increment();
  }
  dropLast() {
    if (this.size > 0) {
      this.decrement();
      this.array[this.current] = void 0;
    }
  }
  toChunk() {
    const begin = this.current - this.size;
    const newArray = begin < 0 ? [...this.array.slice(this.capacity + begin, this.capacity), ...this.array.slice(0, this.current)] : this.array.slice(begin, this.current);
    return fromIterable2(newArray);
  }
  increment() {
    if (this.size < this.capacity) {
      this.size += 1;
    }
    this.current = (this.current + 1) % this.capacity;
  }
  decrement() {
    this.size -= 1;
    if (this.current > 0) {
      this.current -= 1;
    } else {
      this.current = this.capacity - 1;
    }
  }
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/debounceState.js
var OP_NOT_STARTED = "NotStarted";
var OP_PREVIOUS = "Previous";
var OP_CURRENT = "Current";
var notStarted = {
  _tag: OP_NOT_STARTED
};
var previous = (fiber) => ({
  _tag: OP_PREVIOUS,
  fiber
});
var current = (fiber) => ({
  _tag: OP_CURRENT,
  fiber
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/emit.js
var make19 = (emit2) => {
  const ops = {
    chunk(as10) {
      return this(succeed5(as10));
    },
    die(defect) {
      return this(die3(defect));
    },
    dieMessage(message) {
      return this(dieMessage(message));
    },
    done(exit2) {
      return this(suspend2(() => mapBoth(exit2, {
        onFailure: some,
        onSuccess: of
      })));
    },
    end() {
      return this(fail4(none()));
    },
    fail(e) {
      return this(fail4(some(e)));
    },
    fromEffect(effect3) {
      return this(mapBoth3(effect3, {
        onFailure: some,
        onSuccess: of
      }));
    },
    fromEffectChunk(effect3) {
      return this(pipe(effect3, mapError(some)));
    },
    halt(cause) {
      return this(failCause3(pipe(cause, map5(some))));
    },
    single(value) {
      return this(succeed5(of(value)));
    }
  };
  return Object.assign(emit2, ops);
};
var makePush = (queue, scheduler) => {
  let finished = false;
  let buffer4 = [];
  let running2 = false;
  function array(items) {
    if (finished) return false;
    if (items.length <= 5e4) {
      buffer4.push.apply(buffer4, items);
    } else {
      for (let i = 0; i < items.length; i++) {
        buffer4.push(items[0]);
      }
    }
    if (!running2) {
      running2 = true;
      scheduler.scheduleTask(flush, 0);
    }
    return true;
  }
  function flush() {
    running2 = false;
    if (buffer4.length > 0) {
      queue.unsafeOffer(buffer4);
      buffer4 = [];
    }
  }
  function done4(exit2) {
    if (finished) return;
    finished = true;
    if (exit2._tag === "Success") {
      buffer4.push(exit2.value);
    }
    flush();
    queue.unsafeOffer(exit2._tag === "Success" ? void_2 : exit2);
  }
  return {
    single(value) {
      if (finished) return false;
      buffer4.push(value);
      if (!running2) {
        running2 = true;
        scheduler.scheduleTask(flush, 0);
      }
      return true;
    },
    array,
    chunk(chunk2) {
      return array(toReadonlyArray(chunk2));
    },
    done: done4,
    end() {
      if (finished) return;
      finished = true;
      flush();
      queue.unsafeOffer(void_2);
    },
    halt(cause) {
      return done4(failCause(cause));
    },
    fail(error) {
      return done4(fail(error));
    },
    die(defect) {
      return done4(die(defect));
    },
    dieMessage(message) {
      return done4(die(new Error(message)));
    }
  };
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/handoff.js
var HandoffTypeId = Symbol.for("effect/Stream/Handoff");
var OP_HANDOFF_STATE_EMPTY = "Empty";
var OP_HANDOFF_STATE_FULL = "Full";
var handoffStateEmpty = (notifyConsumer) => ({
  _tag: OP_HANDOFF_STATE_EMPTY,
  notifyConsumer
});
var handoffStateFull = (value, notifyProducer) => ({
  _tag: OP_HANDOFF_STATE_FULL,
  value,
  notifyProducer
});
var handoffStateMatch = (onEmpty, onFull) => {
  return (self) => {
    switch (self._tag) {
      case OP_HANDOFF_STATE_EMPTY: {
        return onEmpty(self.notifyConsumer);
      }
      case OP_HANDOFF_STATE_FULL: {
        return onFull(self.value, self.notifyProducer);
      }
    }
  };
};
var handoffVariance = {
  /* c8 ignore next */
  _A: (_) => _
};
var make20 = () => pipe(make8(), flatMap3((deferred) => make5(handoffStateEmpty(deferred))), map7((ref) => ({
  [HandoffTypeId]: handoffVariance,
  ref
})));
var offer6 = dual(2, (self, value) => {
  return flatMap3(make8(), (deferred) => flatten2(modify(self.ref, (state) => pipe(state, handoffStateMatch((notifyConsumer) => [zipRight4(succeed4(notifyConsumer, void 0), _await2(deferred)), handoffStateFull(value, deferred)], (_, notifyProducer) => [flatMap3(_await2(notifyProducer), () => pipe(self, offer6(value))), state])))));
});
var take7 = (self) => flatMap3(make8(), (deferred) => flatten2(modify(self.ref, (state) => pipe(state, handoffStateMatch((notifyConsumer) => [flatMap3(_await2(notifyConsumer), () => take7(self)), state], (value, notifyProducer) => [as3(succeed4(notifyProducer, void 0), value), handoffStateEmpty(deferred)])))));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/handoffSignal.js
var OP_EMIT4 = "Emit";
var OP_HALT = "Halt";
var OP_END = "End";
var emit = (elements) => ({
  _tag: OP_EMIT4,
  elements
});
var halt = (cause) => ({
  _tag: OP_HALT,
  cause
});
var end = (reason) => ({
  _tag: OP_END,
  reason
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/take.js
var TakeSymbolKey = "effect/Take";
var TakeTypeId = Symbol.for(TakeSymbolKey);
var takeVariance = {
  /* c8 ignore next */
  _A: (_) => _,
  /* c8 ignore next */
  _E: (_) => _
};
var TakeImpl = class {
  exit;
  [TakeTypeId] = takeVariance;
  constructor(exit2) {
    this.exit = exit2;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var chunk = (chunk2) => new TakeImpl(succeed2(chunk2));
var die7 = (defect) => new TakeImpl(die(defect));
var dieMessage4 = (message) => new TakeImpl(die(new RuntimeException(message)));
var done3 = (self) => suspend2(() => self.exit);
var end2 = new TakeImpl(fail(none()));
var fail9 = (error) => new TakeImpl(fail(some(error)));
var failCause6 = (cause) => new TakeImpl(failCause(pipe(cause, map5(some))));
var fromEffect4 = (effect3) => matchCause(effect3, {
  onFailure: failCause6,
  onSuccess: of2
});
var fromExit = (exit2) => new TakeImpl(pipe(exit2, mapBoth({
  onFailure: some,
  onSuccess: of
})));
var fromPull = (pull) => matchCause(pull, {
  onFailure: (cause) => match2(flipCauseOption(cause), {
    onNone: () => end2,
    onSome: failCause6
  }),
  onSuccess: chunk
});
var isDone3 = (self) => match3(self.exit, {
  onFailure: (cause) => isNone(flipCauseOption(cause)),
  onSuccess: constFalse
});
var isFailure3 = (self) => match3(self.exit, {
  onFailure: (cause) => isSome(flipCauseOption(cause)),
  onSuccess: constFalse
});
var isSuccess4 = (self) => match3(self.exit, {
  onFailure: constFalse,
  onSuccess: constTrue
});
var make21 = (exit2) => new TakeImpl(exit2);
var match15 = dual(2, (self, {
  onEnd: onEnd3,
  onFailure,
  onSuccess
}) => match3(self.exit, {
  onFailure: (cause) => match2(flipCauseOption(cause), {
    onNone: onEnd3,
    onSome: onFailure
  }),
  onSuccess
}));
var matchEffect3 = dual(2, (self, {
  onEnd: onEnd3,
  onFailure,
  onSuccess
}) => matchEffect(self.exit, {
  onFailure: (cause) => match2(flipCauseOption(cause), {
    onNone: () => onEnd3,
    onSome: onFailure
  }),
  onSuccess
}));
var map12 = dual(2, (self, f) => new TakeImpl(pipe(self.exit, map4(map2(f)))));
var of2 = (value) => new TakeImpl(succeed2(of(value)));
var tap4 = dual(2, (self, f) => pipe(self.exit, forEachEffect(f), asVoid3));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/pull.js
var end3 = () => fail4(none());
var failCause7 = (cause) => mapError(failCause3(cause), some);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/sinkEndReason.js
var OP_SCHEDULE_END = "ScheduleEnd";
var OP_UPSTREAM_END = "UpstreamEnd";
var ScheduleEnd = {
  _tag: OP_SCHEDULE_END
};
var UpstreamEnd = {
  _tag: OP_UPSTREAM_END
};

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/zipAllState.js
var OP_DRAIN_LEFT = "DrainLeft";
var OP_DRAIN_RIGHT = "DrainRight";
var OP_PULL_BOTH = "PullBoth";
var OP_PULL_LEFT = "PullLeft";
var OP_PULL_RIGHT = "PullRight";
var DrainLeft = {
  _tag: OP_DRAIN_LEFT
};
var DrainRight = {
  _tag: OP_DRAIN_RIGHT
};
var PullBoth = {
  _tag: OP_PULL_BOTH
};
var PullLeft = (rightChunk) => ({
  _tag: OP_PULL_LEFT,
  rightChunk
});
var PullRight = (leftChunk) => ({
  _tag: OP_PULL_RIGHT,
  leftChunk
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream/zipChunksState.js
var OP_PULL_BOTH2 = "PullBoth";
var OP_PULL_LEFT2 = "PullLet";
var OP_PULL_RIGHT2 = "PullRight";
var PullBoth2 = {
  _tag: OP_PULL_BOTH2
};
var PullLeft2 = (rightChunk) => ({
  _tag: OP_PULL_LEFT2,
  rightChunk
});
var PullRight2 = (leftChunk) => ({
  _tag: OP_PULL_RIGHT2,
  leftChunk
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/stream.js
var StreamSymbolKey = "effect/Stream";
var StreamTypeId2 = Symbol.for(StreamSymbolKey);
var streamVariance = {
  _R: (_) => _,
  _E: (_) => _,
  _A: (_) => _
};
var StreamImpl = class {
  channel;
  [StreamTypeId2] = streamVariance;
  constructor(channel) {
    this.channel = channel;
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var isStream = (u) => hasProperty(u, StreamTypeId2) || isEffect(u);
var DefaultChunkSize = 4096;
var accumulate = (self) => chunks(accumulateChunks(self));
var accumulateChunks = (self) => {
  const accumulator = (s) => readWith({
    onInput: (input) => {
      const next = appendAll(s, input);
      return flatMap4(write(next), () => accumulator(next));
    },
    onFailure: fail5,
    onDone: () => void_3
  });
  return new StreamImpl(pipeTo(toChannel2(self), accumulator(empty2())));
};
var acquireRelease3 = (acquire, release) => scoped3(acquireRelease2(acquire, release));
var aggregate = dual(2, (self, sink) => aggregateWithin(self, sink, forever3));
var aggregateWithin = dual(3, (self, sink, schedule3) => filterMap2(aggregateWithinEither(self, sink, schedule3), (_) => match(_, {
  onLeft: none,
  onRight: some
})));
var aggregateWithinEither = dual(3, (self, sink, schedule3) => {
  const layer = all3([make20(), make5(ScheduleEnd), make5(empty2()), driver2(schedule3), make5(false), make5(false)]);
  return fromEffect5(layer).pipe(flatMap7(([handoff, sinkEndReason, sinkLeftovers, scheduleDriver, consumed, endAfterEmit]) => {
    const handoffProducer = readWithCause({
      onInput: (input) => flatMap4(fromEffect(pipe(handoff, offer6(emit(input)), when(() => isNonEmpty(input)))), () => handoffProducer),
      onFailure: (cause) => fromEffect(offer6(handoff, halt(cause))),
      onDone: () => fromEffect(offer6(handoff, end(UpstreamEnd)))
    });
    const handoffConsumer = pipe(getAndSet(sinkLeftovers, empty2()), flatMap3((leftovers) => {
      if (isNonEmpty(leftovers)) {
        return pipe(set3(consumed, true), zipRight4(succeed5(pipe(write(leftovers), flatMap4(() => handoffConsumer)))));
      }
      return pipe(take7(handoff), map7((signal) => {
        switch (signal._tag) {
          case OP_EMIT4: {
            return pipe(fromEffect(set3(consumed, true)), zipRight5(write(signal.elements)), zipRight5(fromEffect(get3(endAfterEmit))), flatMap4((bool) => bool ? void_3 : handoffConsumer));
          }
          case OP_HALT: {
            return failCause4(signal.cause);
          }
          case OP_END: {
            if (signal.reason._tag === OP_SCHEDULE_END) {
              return pipe(get3(consumed), map7((bool) => bool ? fromEffect(pipe(set3(sinkEndReason, ScheduleEnd), zipRight4(set3(endAfterEmit, true)))) : pipe(fromEffect(pipe(set3(sinkEndReason, ScheduleEnd), zipRight4(set3(endAfterEmit, true)))), flatMap4(() => handoffConsumer))), unwrap);
            }
            return pipe(set3(sinkEndReason, signal.reason), zipRight4(set3(endAfterEmit, true)), fromEffect);
          }
        }
      }));
    }), unwrap);
    const timeout3 = (lastB) => scheduleDriver.next(lastB);
    const scheduledAggregator = (sinkFiber, scheduleFiber, scope2) => {
      const forkSink = pipe(set3(consumed, false), zipRight4(set3(endAfterEmit, false)), zipRight4(pipe(handoffConsumer, pipeToOrFail(toChannel(sink)), collectElements, run2, forkIn2(scope2))));
      const handleSide = (leftovers, b, c) => pipe(set3(sinkLeftovers, flatten(leftovers)), zipRight4(map7(get3(sinkEndReason), (reason) => {
        switch (reason._tag) {
          case OP_SCHEDULE_END: {
            return pipe(all3([get3(consumed), forkSink, pipe(timeout3(some(b)), forkIn2(scope2))]), map7(([wasConsumed, sinkFiber2, scheduleFiber2]) => {
              const toWrite = pipe(c, match2({
                onNone: () => of(right(b)),
                onSome: (c2) => make2(right(b), left(c2))
              }));
              if (wasConsumed) {
                return pipe(write(toWrite), flatMap4(() => scheduledAggregator(sinkFiber2, scheduleFiber2, scope2)));
              }
              return scheduledAggregator(sinkFiber2, scheduleFiber2, scope2);
            }), unwrap);
          }
          case OP_UPSTREAM_END: {
            return pipe(get3(consumed), map7((wasConsumed) => wasConsumed ? write(of(right(b))) : void_3), unwrap);
          }
        }
      })), unwrap);
      return unwrap(raceWith(join(sinkFiber), join(scheduleFiber), {
        onSelfDone: (sinkExit, _) => pipe(interrupt4(scheduleFiber), zipRight4(pipe(suspend2(() => sinkExit), map7(([leftovers, b]) => handleSide(leftovers, b, none()))))),
        onOtherDone: (scheduleExit, _) => matchCauseEffect(suspend2(() => scheduleExit), {
          onFailure: (cause) => match(failureOrCause(cause), {
            onLeft: () => pipe(handoff, offer6(end(ScheduleEnd)), forkDaemon, zipRight4(pipe(join(sinkFiber), map7(([leftovers, b]) => handleSide(leftovers, b, none()))))),
            onRight: (cause2) => pipe(handoff, offer6(halt(cause2)), forkDaemon, zipRight4(pipe(join(sinkFiber), map7(([leftovers, b]) => handleSide(leftovers, b, none())))))
          }),
          onSuccess: (c) => pipe(handoff, offer6(end(ScheduleEnd)), forkDaemon, zipRight4(pipe(join(sinkFiber), map7(([leftovers, b]) => handleSide(leftovers, b, some(c))))))
        })
      }));
    };
    return unwrapScopedWith3((scope2) => pipeTo(toChannel2(self), handoffProducer).pipe(run2, forkIn2(scope2), zipRight4(pipeToOrFail(handoffConsumer, toChannel(sink)).pipe(collectElements, run2, forkIn2(scope2), flatMap3((sinkFiber) => timeout3(none()).pipe(forkIn2(scope2), map7((scheduleFiber) => new StreamImpl(scheduledAggregator(sinkFiber, scheduleFiber, scope2)))))))));
  }));
});
var as8 = dual(2, (self, value) => map13(self, () => value));
var queueFromBufferOptions = (bufferSize) => {
  if (bufferSize === "unbounded") {
    return unbounded3();
  } else if (typeof bufferSize === "number" || bufferSize === void 0) {
    return bounded3(bufferSize ?? 16);
  }
  switch (bufferSize.strategy) {
    case "dropping":
      return dropping2(bufferSize.bufferSize ?? 16);
    case "sliding":
      return sliding2(bufferSize.bufferSize ?? 16);
    default:
      return bounded3(bufferSize.bufferSize ?? 16);
  }
};
var _async = (register, bufferSize) => acquireRelease2(queueFromBufferOptions(bufferSize), (queue) => shutdown2(queue)).pipe(flatMap3((output) => runtime().pipe(flatMap3((runtime2) => sync3(() => {
  const runPromiseExit2 = runPromiseExit(runtime2);
  const canceler = register(make19((resume) => fromPull(resume).pipe(flatMap3((take10) => offer3(output, take10)), asVoid3, runPromiseExit2).then((exit2) => {
    if (isFailure(exit2)) {
      if (!isInterrupted(exit2.cause)) {
        throw squash(exit2.cause);
      }
    }
  })));
  return canceler;
})), map7((value) => {
  const loop2 = take3(output).pipe(flatMap3((take10) => done3(take10)), match4({
    onFailure: (maybeError) => fromEffect(shutdown2(output)).pipe(zipRight5(match2(maybeError, {
      onNone: () => void_3,
      onSome: (error) => fail5(error)
    }))),
    onSuccess: (chunk2) => write(chunk2).pipe(flatMap4(() => loop2))
  }), unwrap);
  return fromChannel2(loop2).pipe(ensuring8(value ?? _void));
}))), unwrapScoped3);
var asyncEffect = (register, bufferSize) => pipe(acquireRelease2(queueFromBufferOptions(bufferSize), (queue) => shutdown2(queue)), flatMap3((output) => pipe(runtime(), flatMap3((runtime2) => pipe(register(make19((k) => pipe(fromPull(k), flatMap3((take10) => offer3(output, take10)), asVoid3, runPromiseExit(runtime2)).then((exit2) => {
  if (isFailure(exit2)) {
    if (!isInterrupted(exit2.cause)) {
      throw squash(exit2.cause);
    }
  }
}))), map7(() => {
  const loop2 = pipe(take3(output), flatMap3(done3), match4({
    onFailure: (maybeError) => pipe(fromEffect(shutdown2(output)), zipRight5(match2(maybeError, {
      onNone: () => void_3,
      onSome: fail5
    }))),
    onSuccess: (chunk2) => pipe(write(chunk2), flatMap4(() => loop2))
  }), unwrap);
  return loop2;
}))))), unwrapScoped, fromChannel2);
var queueFromBufferOptionsPush = (options) => {
  if (options?.bufferSize === "unbounded" || options?.bufferSize === void 0 && options?.strategy === void 0) {
    return unbounded3();
  }
  switch (options?.strategy) {
    case "sliding":
      return sliding2(options.bufferSize ?? 16);
    default:
      return dropping2(options?.bufferSize ?? 16);
  }
};
var asyncPush = (register, options) => acquireRelease2(queueFromBufferOptionsPush(options), shutdown2).pipe(tap2((queue) => getWith(currentScheduler2, (scheduler) => register(makePush(queue, scheduler)))), map7((queue) => {
  const loop2 = flatMap4(take3(queue), (item) => isExit(item) ? isSuccess(item) ? void_3 : failCause4(item.cause) : zipRight5(write(unsafeFromArray(item)), loop2));
  return loop2;
}), unwrapScoped, fromChannel2);
var asyncScoped = (register, bufferSize) => pipe(acquireRelease2(queueFromBufferOptions(bufferSize), (queue) => shutdown2(queue)), flatMap3((output) => pipe(runtime(), flatMap3((runtime2) => pipe(register(make19((k) => pipe(fromPull(k), flatMap3((take10) => offer3(output, take10)), asVoid3, runPromiseExit(runtime2)).then((exit2) => {
  if (isFailure(exit2)) {
    if (!isInterrupted(exit2.cause)) {
      throw squash(exit2.cause);
    }
  }
}))), zipRight4(make5(false)), flatMap3((ref) => pipe(get3(ref), map7((isDone4) => isDone4 ? end3() : pipe(take3(output), flatMap3(done3), onError(() => pipe(set3(ref, true), zipRight4(shutdown2(output)))))))))))), scoped3, flatMap7(repeatEffectChunkOption));
var branchAfter = dual(3, (self, n, f) => suspend7(() => {
  const buffering = (acc) => readWith({
    onInput: (input) => {
      const nextSize = acc.length + input.length;
      if (nextSize >= n) {
        const [b1, b2] = pipe(input, splitAt2(n - acc.length));
        return running2(pipe(acc, appendAll(b1)), b2);
      }
      return buffering(pipe(acc, appendAll(input)));
    },
    onFailure: fail5,
    onDone: () => running2(acc, empty2())
  });
  const running2 = (prefix, leftover2) => pipeTo(zipRight5(write(leftover2), identityChannel()), toChannel2(f(prefix)));
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(buffering(empty2()))));
}));
var broadcast = dual(3, (self, n, maximumLag) => pipe(self, broadcastedQueues(n, maximumLag), map7((tuple) => tuple.map((queue) => flattenTake(fromQueue3(queue, {
  shutdown: true
}))))));
var broadcastDynamic = dual(2, (self, maximumLag) => map7(toPubSub2(self, maximumLag), (pubsub) => flattenTake(fromPubSub3(pubsub))));
var share = dual(2, (self, options) => map7(make14({
  acquire: broadcastDynamic(self, options),
  idleTimeToLive: options.idleTimeToLive
}), (rcRef) => unwrapScoped3(get6(rcRef))));
var broadcastedQueues = dual(3, (self, n, maximumLag) => flatMap3(pubsubFromOptions(maximumLag), (pubsub) => pipe(all3(Array.from({
  length: n
}, () => subscribe2(pubsub))), tap2(() => forkScoped(runIntoPubSubScoped(self, pubsub))))));
var broadcastedQueuesDynamic = dual(2, (self, maximumLag) => map7(toPubSub2(self, maximumLag), subscribe2));
var buffer2 = dual(2, (self, options) => {
  if (options.capacity === "unbounded") {
    return bufferUnbounded(self);
  } else if (options.strategy === "dropping") {
    return bufferDropping(self, options.capacity);
  } else if (options.strategy === "sliding") {
    return bufferSliding(self, options.capacity);
  }
  const queue = toQueueOfElements(self, options);
  return new StreamImpl(unwrapScoped(map7(queue, (queue2) => {
    const process = pipe(fromEffect(take3(queue2)), flatMap4(match3({
      onFailure: (cause) => pipe(flipCauseOption(cause), match2({
        onNone: () => void_3,
        onSome: failCause4
      })),
      onSuccess: (value) => flatMap4(write(of(value)), () => process)
    })));
    return process;
  })));
});
var bufferChunks = dual(2, (self, options) => {
  if (options.strategy === "dropping") {
    return bufferChunksDropping(self, options.capacity);
  } else if (options.strategy === "sliding") {
    return bufferChunksSliding(self, options.capacity);
  }
  const queue = toQueue2(self, options);
  return new StreamImpl(unwrapScoped(map7(queue, (queue2) => {
    const process = pipe(fromEffect(take3(queue2)), flatMap4(match15({
      onEnd: () => void_3,
      onFailure: failCause4,
      onSuccess: (value) => pipe(write(value), flatMap4(() => process))
    })));
    return process;
  })));
});
var bufferChunksDropping = dual(2, (self, capacity10) => {
  const queue = acquireRelease2(dropping2(capacity10), (queue2) => shutdown2(queue2));
  return new StreamImpl(bufferSignal(queue, toChannel2(self)));
});
var bufferChunksSliding = dual(2, (self, capacity10) => {
  const queue = acquireRelease2(sliding2(capacity10), (queue2) => shutdown2(queue2));
  return new StreamImpl(bufferSignal(queue, toChannel2(self)));
});
var bufferDropping = dual(2, (self, capacity10) => {
  const queue = acquireRelease2(dropping2(capacity10), (queue2) => shutdown2(queue2));
  return new StreamImpl(bufferSignal(queue, toChannel2(rechunk(1)(self))));
});
var bufferSliding = dual(2, (self, capacity10) => {
  const queue = acquireRelease2(sliding2(capacity10), (queue2) => shutdown2(queue2));
  return new StreamImpl(bufferSignal(queue, toChannel2(pipe(self, rechunk(1)))));
});
var bufferUnbounded = (self) => {
  const queue = toQueue2(self, {
    strategy: "unbounded"
  });
  return new StreamImpl(unwrapScoped(map7(queue, (queue2) => {
    const process = pipe(fromEffect(take3(queue2)), flatMap4(match15({
      onEnd: () => void_3,
      onFailure: failCause4,
      onSuccess: (value) => flatMap4(write(value), () => process)
    })));
    return process;
  })));
};
var bufferSignal = (scoped5, bufferChannel) => {
  const producer = (queue, ref) => {
    const terminate = (take10) => pipe(get3(ref), tap2(_await2), zipRight4(make8()), flatMap3((deferred) => pipe(offer3(queue, [take10, deferred]), zipRight4(set3(ref, deferred)), zipRight4(_await2(deferred)))), asVoid3, fromEffect);
    return readWithCause({
      onInput: (input) => pipe(make8(), flatMap3((deferred) => pipe(offer3(queue, [chunk(input), deferred]), flatMap3((added) => pipe(set3(ref, deferred), when(() => added))))), asVoid3, fromEffect, flatMap4(() => producer(queue, ref))),
      onFailure: (error) => terminate(failCause6(error)),
      onDone: () => terminate(end2)
    });
  };
  const consumer = (queue) => {
    const process = pipe(fromEffect(take3(queue)), flatMap4(([take10, deferred]) => zipRight5(fromEffect(succeed4(deferred, void 0)), match15(take10, {
      onEnd: () => void_3,
      onFailure: failCause4,
      onSuccess: (value) => pipe(write(value), flatMap4(() => process))
    }))));
    return process;
  };
  return unwrapScoped(pipe(scoped5, flatMap3((queue) => pipe(make8(), tap2((start) => succeed4(start, void 0)), flatMap3((start) => pipe(make5(start), flatMap3((ref) => pipe(bufferChannel, pipeTo(producer(queue, ref)), runScoped, forkScoped)), as3(consumer(queue))))))));
};
var catchAll5 = dual(2, (self, f) => catchAllCause3(self, (cause) => match(failureOrCause(cause), {
  onLeft: f,
  onRight: failCause8
})));
var catchAllCause3 = dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), catchAllCause2((cause) => toChannel2(f(cause))))));
var catchSome2 = dual(2, (self, pf) => pipe(self, catchAll5((error) => pipe(pf(error), getOrElse(() => fail10(error))))));
var catchSomeCause2 = dual(2, (self, pf) => pipe(self, catchAllCause3((cause) => pipe(pf(cause), getOrElse(() => failCause8(cause))))));
var catchTag2 = dual(3, (self, k, f) => catchAll5(self, (e) => {
  if ("_tag" in e && e["_tag"] === k) {
    return f(e);
  }
  return fail10(e);
}));
var catchTags2 = dual(2, (self, cases) => catchAll5(self, (e) => {
  const keys = Object.keys(cases);
  if ("_tag" in e && keys.includes(e["_tag"])) {
    return cases[e["_tag"]](e);
  }
  return fail10(e);
}));
var changes = (self) => pipe(self, changesWith((x, y) => equals(y)(x)));
var changesWith = dual(2, (self, f) => {
  const writer = (last3) => readWithCause({
    onInput: (input) => {
      const [newLast, newChunk] = reduce(input, [last3, empty2()], ([option2, outputs], output) => {
        if (isSome(option2) && f(option2.value, output)) {
          return [some(output), outputs];
        }
        return [some(output), pipe(outputs, append(output))];
      });
      if (isEmpty(newChunk)) return writer(newLast);
      return flatMap4(write(newChunk), () => writer(newLast));
    },
    onFailure: failCause4,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(writer(none()))));
});
var changesWithEffect = dual(2, (self, f) => {
  const writer = (last3) => readWithCause({
    onInput: (input) => pipe(input, reduce3([last3, empty2()], ([option2, outputs], output) => {
      if (isSome(option2)) {
        return pipe(f(option2.value, output), map7((bool) => bool ? [some(output), outputs] : [some(output), pipe(outputs, append(output))]));
      }
      return succeed5([some(output), pipe(outputs, append(output))]);
    }), fromEffect, flatMap4(([newLast, newChunk]) => pipe(write(newChunk), flatMap4(() => writer(newLast))))),
    onFailure: failCause4,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(writer(none()))));
});
var chunks = (self) => pipe(self, mapChunks(of));
var chunksWith = dual(2, (self, f) => flattenChunks(f(chunks(self))));
var unsome2 = (effect3) => catchAll2(asSome(effect3), (o) => o._tag === "None" ? succeedNone : fail4(o.value));
var combine2 = dual(4, (self, that, s, f) => {
  function producer(handoff, latch) {
    return fromEffect(take7(latch)).pipe(zipRight5(readWithCause({
      onInput: (input) => flatMap4(fromEffect(offer6(handoff, succeed2(input))), () => producer(handoff, latch)),
      onFailure: (cause) => fromEffect(offer6(handoff, failCause(pipe(cause, map5(some))))),
      onDone: () => flatMap4(fromEffect(offer6(handoff, fail(none()))), () => producer(handoff, latch))
    })));
  }
  return new StreamImpl(unwrapScopedWith((scope2) => all3([make20(), make20(), make20(), make20()]).pipe(tap2(([left2, _, latchL]) => toChannel2(self).pipe(concatMap(writeChunk), pipeTo(producer(left2, latchL)), runIn(scope2), forkIn2(scope2))), tap2(([, right2, _, rightL]) => toChannel2(that).pipe(concatMap(writeChunk), pipeTo(producer(right2, rightL)), runIn(scope2), forkIn2(scope2))), map7(([left2, right2, latchL, latchR]) => {
    const pullLeft = offer6(latchL, void 0).pipe(zipRight4(take7(left2).pipe(flatMap3(identity))));
    const pullRight = offer6(latchR, void 0).pipe(zipRight4(take7(right2).pipe(flatMap3(identity))));
    return toChannel2(unfoldEffect(s, (s2) => flatMap3(f(s2, pullLeft, pullRight), unsome2)));
  }))));
});
var combineChunks = dual(4, (self, that, s, f) => {
  const producer = (handoff, latch) => zipRight5(fromEffect(take7(latch)), readWithCause({
    onInput: (input) => flatMap4(fromEffect(pipe(handoff, offer6(chunk(input)))), () => producer(handoff, latch)),
    onFailure: (cause) => fromEffect(offer6(handoff, failCause6(cause))),
    onDone: () => fromEffect(offer6(handoff, end2))
  }));
  return new StreamImpl(unwrapScopedWith((scope2) => all3([make20(), make20(), make20(), make20()]).pipe(tap2(([left2, _, latchL]) => pipeTo(toChannel2(self), producer(left2, latchL)).pipe(runIn(scope2), forkIn2(scope2))), tap2(([_, right2, __, latchR]) => pipeTo(toChannel2(that), producer(right2, latchR)).pipe(runIn(scope2), forkIn2(scope2))), map7(([left2, right2, latchL, latchR]) => {
    const pullLeft = offer6(latchL, void 0).pipe(zipRight4(take7(left2).pipe(flatMap3(done3))));
    const pullRight = offer6(latchR, void 0).pipe(zipRight4(take7(right2).pipe(flatMap3(done3))));
    return toChannel2(unfoldChunkEffect(s, (s2) => flatMap3(f(s2, pullLeft, pullRight), unsome2)));
  }))));
});
var concat = dual(2, (self, that) => new StreamImpl(pipe(toChannel2(self), zipRight5(toChannel2(that)))));
var concatAll2 = (streams) => suspend7(() => pipe(streams, reduce(empty5, (x, y) => concat(y)(x))));
var cross = dual(2, (left2, right2) => pipe(left2, crossWith(right2, (a, a2) => [a, a2])));
var crossLeft = dual(2, (left2, right2) => pipe(left2, crossWith(right2, (a, _) => a)));
var crossRight = dual(2, (left2, right2) => flatMap7(left2, () => right2));
var crossWith = dual(3, (left2, right2, f) => pipe(left2, flatMap7((a) => pipe(right2, map13((b) => f(a, b))))));
var debounce = dual(2, (self, duration3) => unwrapScopedWith3((scope2) => gen(function* () {
  const handoff = yield* make20();
  function enqueue(last3) {
    return sleep(duration3).pipe(as3(last3), forkIn2(scope2), map7((fiber) => consumer(previous(fiber))));
  }
  const producer = readWithCause({
    onInput: (input) => match2(last(input), {
      onNone: () => producer,
      onSome: (elem) => fromEffect(offer6(handoff, emit(of(elem)))).pipe(flatMap4(() => producer))
    }),
    onFailure: (cause) => fromEffect(offer6(handoff, halt(cause))),
    onDone: () => fromEffect(offer6(handoff, end(UpstreamEnd)))
  });
  function consumer(state) {
    switch (state._tag) {
      case OP_NOT_STARTED: {
        return unwrap(take7(handoff).pipe(map7((signal) => {
          switch (signal._tag) {
            case OP_EMIT4: {
              return unwrap(enqueue(signal.elements));
            }
            case OP_HALT: {
              return failCause4(signal.cause);
            }
            case OP_END: {
              return void_3;
            }
          }
        })));
      }
      case OP_PREVIOUS: {
        return unwrap(take7(handoff).pipe(forkIn2(scope2), flatMap3((handoffFiber) => raceWith(join(state.fiber), join(handoffFiber), {
          onSelfDone: (leftExit, current2) => match3(leftExit, {
            onFailure: (cause) => interrupt4(current2).pipe(as3(failCause4(cause))),
            onSuccess: (chunk2) => interrupt4(current2).pipe(zipRight4(succeed5(write(chunk2).pipe(flatMap4(() => consumer(current(handoffFiber)))))))
          }),
          onOtherDone: (rightExit, previous2) => match3(rightExit, {
            onFailure: (cause) => interrupt4(previous2).pipe(as3(failCause4(cause))),
            onSuccess: (signal) => {
              switch (signal._tag) {
                case OP_EMIT4: {
                  return interrupt4(previous2).pipe(zipRight4(enqueue(signal.elements)));
                }
                case OP_HALT: {
                  return interrupt4(previous2).pipe(as3(failCause4(signal.cause)));
                }
                case OP_END: {
                  return join(previous2).pipe(map7((chunk2) => write(chunk2).pipe(zipRight5(void_3))));
                }
              }
            }
          })
        }))));
      }
      case OP_CURRENT: {
        return unwrap(join(state.fiber).pipe(map7((signal) => {
          switch (signal._tag) {
            case OP_EMIT4: {
              return unwrap(enqueue(signal.elements));
            }
            case OP_HALT: {
              return failCause4(signal.cause);
            }
            case OP_END: {
              return void_3;
            }
          }
        })));
      }
    }
  }
  return scopedWith3((scope3) => pipeTo(toChannel2(self), producer).pipe(runIn(scope3), forkIn2(scope3))).pipe(crossRight(new StreamImpl(consumer(notStarted))));
})));
var die8 = (defect) => fromEffect5(die3(defect));
var dieSync4 = (evaluate2) => fromEffect5(dieSync(evaluate2));
var dieMessage5 = (message) => fromEffect5(dieMessage(message));
var distributedWith = dual(2, (self, options) => pipe(make8(), flatMap3((deferred) => pipe(self, distributedWithDynamic({
  maximumLag: options.maximumLag,
  decide: (a) => flatMap3(_await2(deferred), (f) => f(a))
}), flatMap3((next) => pipe(all3(map2(range(0, options.size - 1), (id) => map7(next, ([key, queue]) => [[key, id], queue]))), map7(unsafeFromArray), flatMap3((entries) => {
  const [mappings, queues] = reduceRight(entries, [/* @__PURE__ */ new Map(), empty2()], ([mappings2, queues2], [mapping, queue]) => [mappings2.set(mapping[0], mapping[1]), pipe(queues2, prepend(queue))]);
  return pipe(succeed4(deferred, (a) => map7(options.decide(a), (f) => (key) => f(mappings.get(key)))), as3(Array.from(queues)));
})))))));
var distributedWithDynamicId = {
  ref: 0
};
var newDistributedWithDynamicId = () => {
  const current2 = distributedWithDynamicId.ref;
  distributedWithDynamicId.ref = current2 + 1;
  return current2;
};
var distributedWithDynamic = dual(2, (self, options) => distributedWithDynamicCallback(self, options.maximumLag, options.decide, () => _void));
var distributedWithDynamicCallback = dual(4, (self, maximumLag, decide, done4) => pipe(acquireRelease2(make5(/* @__PURE__ */ new Map()), (ref, _) => pipe(get3(ref), flatMap3((queues) => pipe(queues.values(), forEach2(shutdown2))))), flatMap3((queuesRef) => gen(function* () {
  const offer7 = (a) => pipe(decide(a), flatMap3((shouldProcess) => pipe(get3(queuesRef), flatMap3((queues) => pipe(queues.entries(), reduce3(empty2(), (acc, [id, queue]) => {
    if (shouldProcess(id)) {
      return pipe(offer3(queue, succeed2(a)), matchCauseEffect({
        onFailure: (cause) => (
          // Ignore all downstream queues that were shut
          // down and remove them later
          isInterrupted(cause) ? succeed5(pipe(acc, prepend(id))) : failCause3(cause)
        ),
        onSuccess: () => succeed5(acc)
      }));
    }
    return succeed5(acc);
  }), flatMap3((ids) => {
    if (isNonEmpty(ids)) {
      return update(queuesRef, (map15) => {
        for (const id of ids) {
          map15.delete(id);
        }
        return map15;
      });
    }
    return _void;
  }))))), asVoid3);
  const queuesLock = yield* makeSemaphore(1);
  const newQueue = yield* make5(pipe(bounded3(maximumLag), flatMap3((queue) => {
    const id = newDistributedWithDynamicId();
    return pipe(update(queuesRef, (map15) => map15.set(id, queue)), as3([id, queue]));
  })));
  const finalize = (endTake) => (
    // Make sure that no queues are currently being added
    queuesLock.withPermits(1)(pipe(set3(newQueue, pipe(
      // All newly created queues should end immediately
      bounded3(1),
      tap2((queue) => offer3(queue, endTake)),
      flatMap3((queue) => {
        const id = newDistributedWithDynamicId();
        return pipe(update(queuesRef, (map15) => map15.set(id, queue)), as3(make(id, queue)));
      })
    )), zipRight4(pipe(get3(queuesRef), flatMap3((map15) => pipe(fromIterable2(map15.values()), forEach2((queue) => pipe(offer3(queue, endTake), catchSomeCause((cause) => isInterrupted(cause) ? some(_void) : none()))))))), zipRight4(done4(endTake)), asVoid3))
  );
  yield* pipe(self, runForEachScoped(offer7), matchCauseEffect({
    onFailure: (cause) => finalize(failCause(pipe(cause, map5(some)))),
    onSuccess: () => finalize(fail(none()))
  }), forkScoped);
  return queuesLock.withPermits(1)(flatten2(get3(newQueue)));
}))));
var drain3 = (self) => new StreamImpl(drain(toChannel2(self)));
var drainFork = dual(2, (self, that) => fromEffect5(make8()).pipe(flatMap7((backgroundDied) => scopedWith3((scope2) => toChannel2(that).pipe(drain, runIn(scope2), catchAllCause((cause) => failCause2(backgroundDied, cause)), forkIn2(scope2))).pipe(crossRight(interruptWhenDeferred2(self, backgroundDied))))));
var drop3 = dual(2, (self, n) => {
  const loop2 = (r) => readWith({
    onInput: (input) => {
      const dropped = pipe(input, drop(r));
      const leftover2 = Math.max(0, r - input.length);
      const more = isEmpty(input) || leftover2 > 0;
      if (more) {
        return loop2(leftover2);
      }
      return pipe(write(dropped), zipRight5(identityChannel()));
    },
    onFailure: fail5,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop2(n))));
});
var dropRight = dual(2, (self, n) => {
  if (n <= 0) {
    return identityStream();
  }
  return suspend7(() => {
    const queue = new RingBuffer(n);
    const reader = readWith({
      onInput: (input) => {
        const outputs = pipe(input, filterMap((elem) => {
          const head5 = queue.head();
          queue.put(elem);
          return head5;
        }));
        return pipe(write(outputs), flatMap4(() => reader));
      },
      onFailure: fail5,
      onDone: () => void_3
    });
    return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(reader)));
  });
});
var dropUntil3 = dual(2, (self, predicate) => drop3(dropWhile4(self, (a) => !predicate(a)), 1));
var dropUntilEffect2 = dual(2, (self, predicate) => {
  const loop2 = readWith({
    onInput: (input) => pipe(dropUntil(input, predicate), map7(unsafeFromArray), map7((leftover2) => {
      const more = isEmpty(leftover2);
      if (more) {
        return suspend3(() => loop2);
      }
      return pipe(write(leftover2), zipRight5(identityChannel()));
    }), unwrap),
    onFailure: fail5,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop2)));
});
var dropWhile4 = dual(2, (self, predicate) => {
  const loop2 = readWith({
    onInput: (input) => {
      const output = dropWhile(input, predicate);
      if (isEmpty(output)) {
        return suspend3(() => loop2);
      }
      return zipRight5(write(output), identityChannel());
    },
    onFailure: fail5,
    onDone: succeedNow
  });
  return new StreamImpl(pipeToOrFail(toChannel2(self), loop2));
});
var dropWhileEffect2 = dual(2, (self, predicate) => {
  const loop2 = readWith({
    onInput: (input) => pipe(dropWhile2(input, predicate), map7(unsafeFromArray), map7((leftover2) => {
      const more = isEmpty(leftover2);
      if (more) {
        return suspend3(() => loop2);
      }
      return zipRight5(write(leftover2), identityChannel());
    }), unwrap),
    onFailure: fail5,
    onDone: () => void_3
  });
  return new StreamImpl(pipeToOrFail(toChannel2(self), loop2));
});
var either4 = (self) => pipe(self, map13(right), catchAll5((error) => make22(left(error))));
var empty5 = new StreamImpl(void_3);
var ensuring8 = dual(2, (self, finalizer3) => new StreamImpl(pipe(toChannel2(self), ensuring4(finalizer3))));
var ensuringWith3 = dual(2, (self, finalizer3) => new StreamImpl(ensuringWith(toChannel2(self), finalizer3)));
var context6 = () => fromEffect5(context());
var contextWith5 = (f) => pipe(context6(), map13(f));
var contextWithEffect3 = (f) => pipe(context6(), mapEffectSequential(f));
var contextWithStream = (f) => pipe(context6(), flatMap7(f));
var execute = (effect3) => drain3(fromEffect5(effect3));
var fail10 = (error) => fromEffectOption(fail4(some(error)));
var failSync5 = (evaluate2) => fromEffectOption(failSync(() => some(evaluate2())));
var failCause8 = (cause) => fromEffect5(failCause3(cause));
var failCauseSync4 = (evaluate2) => fromEffect5(failCauseSync(evaluate2));
var filter4 = dual(2, (self, predicate) => mapChunks(self, filter(predicate)));
var filterEffect = dual(2, (self, f) => {
  const loop2 = (iterator) => {
    const next = iterator.next();
    if (next.done) {
      return readWithCause({
        onInput: (input) => loop2(input[Symbol.iterator]()),
        onFailure: failCause4,
        onDone: succeed6
      });
    } else {
      return pipe(f(next.value), map7((bool) => bool ? pipe(write(of(next.value)), flatMap4(() => loop2(iterator))) : loop2(iterator)), unwrap);
    }
  };
  return new StreamImpl(suspend3(() => pipe(toChannel2(self), pipeTo(loop2(empty2()[Symbol.iterator]())))));
});
var filterMap2 = dual(2, (self, pf) => mapChunks(self, filterMap(pf)));
var filterMapEffect = dual(2, (self, pf) => suspend7(() => {
  const loop2 = (iterator) => {
    const next = iterator.next();
    if (next.done) {
      return readWithCause({
        onInput: (input) => loop2(input[Symbol.iterator]()),
        onFailure: failCause4,
        onDone: succeed6
      });
    } else {
      return pipe(pf(next.value), match2({
        onNone: () => sync3(() => loop2(iterator)),
        onSome: map7((a2) => flatMap4(write(of(a2)), () => loop2(iterator)))
      }), unwrap);
    }
  };
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop2(empty2()[Symbol.iterator]()))));
}));
var filterMapWhile2 = dual(2, (self, pf) => {
  const loop2 = readWith({
    onInput: (input) => {
      const mapped = filterMapWhile(input, pf);
      if (mapped.length === input.length) {
        return pipe(write(mapped), flatMap4(() => loop2));
      }
      return write(mapped);
    },
    onFailure: fail5,
    onDone: succeed6
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop2)));
});
var filterMapWhileEffect = dual(2, (self, pf) => suspend7(() => {
  const loop2 = (iterator) => {
    const next = iterator.next();
    if (next.done) {
      return readWithCause({
        onInput: (input) => loop2(input[Symbol.iterator]()),
        onFailure: failCause4,
        onDone: succeed6
      });
    } else {
      return unwrap(match2(pf(next.value), {
        onNone: () => succeed5(void_3),
        onSome: map7((a2) => flatMap4(write(of(a2)), () => loop2(iterator)))
      }));
    }
  };
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop2(empty2()[Symbol.iterator]()))));
}));
var finalizer = (finalizer3) => acquireRelease3(_void, () => finalizer3);
var find = dual(2, (self, predicate) => {
  const loop2 = readWith({
    onInput: (input) => match2(findFirst(input, predicate), {
      onNone: () => loop2,
      onSome: (n) => write(of(n))
    }),
    onFailure: fail5,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop2)));
});
var findEffect2 = dual(2, (self, predicate) => {
  const loop2 = readWith({
    onInput: (input) => pipe(findFirst2(input, predicate), map7(match2({
      onNone: () => loop2,
      onSome: (n) => write(of(n))
    })), unwrap),
    onFailure: fail5,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop2)));
});
var flatMap7 = dual((args) => isStream(args[0]), (self, f, options) => {
  const bufferSize = options?.bufferSize ?? 16;
  if (options?.switch) {
    return matchConcurrency(options?.concurrency, () => flatMapParSwitchBuffer(self, 1, bufferSize, f), (n) => flatMapParSwitchBuffer(self, n, bufferSize, f));
  }
  return matchConcurrency(options?.concurrency, () => new StreamImpl(concatMap(toChannel2(self), (as10) => pipe(as10, map2((a) => toChannel2(f(a))), reduce(void_3, (left2, right2) => pipe(left2, zipRight5(right2)))))), (_) => new StreamImpl(pipe(toChannel2(self), concatMap(writeChunk), mergeMap((out) => toChannel2(f(out)), options))));
});
var matchConcurrency = (concurrency, sequential3, bounded10) => {
  switch (concurrency) {
    case void 0:
      return sequential3();
    case "unbounded":
      return bounded10(Number.MAX_SAFE_INTEGER);
    default:
      return concurrency > 1 ? bounded10(concurrency) : sequential3();
  }
};
var flatMapParSwitchBuffer = dual(4, (self, n, bufferSize, f) => new StreamImpl(pipe(toChannel2(self), concatMap(writeChunk), mergeMap((out) => toChannel2(f(out)), {
  concurrency: n,
  mergeStrategy: BufferSliding(),
  bufferSize
}))));
var flatten5 = dual((args) => isStream(args[0]), (self, options) => flatMap7(self, identity, options));
var flattenChunks = (self) => {
  const flatten7 = readWithCause({
    onInput: (chunks3) => flatMap4(writeChunk(chunks3), () => flatten7),
    onFailure: failCause4,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(flatten7)));
};
var flattenEffect = dual((args) => isStream(args[0]), (self, options) => options?.unordered ? flatMap7(self, (a) => fromEffect5(a), {
  concurrency: options.concurrency
}) : matchConcurrency(options?.concurrency, () => mapEffectSequential(self, identity), (n) => new StreamImpl(pipe(toChannel2(self), concatMap(writeChunk), mapOutEffectPar(identity, n), mapOut(of)))));
var flattenExitOption = (self) => {
  const processChunk = (chunk2, cont) => {
    const [toEmit, rest] = pipe(chunk2, splitWhere((exit2) => !isSuccess(exit2)));
    const next = pipe(head2(rest), match2({
      onNone: () => cont,
      onSome: match3({
        onFailure: (cause) => match2(flipCauseOption(cause), {
          onNone: () => void_3,
          onSome: failCause4
        }),
        onSuccess: () => void_3
      })
    }));
    return pipe(write(pipe(toEmit, filterMap((exit2) => isSuccess(exit2) ? some(exit2.value) : none()))), flatMap4(() => next));
  };
  const process = readWithCause({
    onInput: (chunk2) => processChunk(chunk2, process),
    onFailure: (cause) => failCause4(cause),
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(process)));
};
var flattenIterables = (self) => pipe(self, map13(fromIterable2), flattenChunks);
var flattenTake = (self) => flattenChunks(flattenExitOption(pipe(self, map13((take10) => take10.exit))));
var forever4 = (self) => new StreamImpl(repeated(toChannel2(self)));
var fromAsyncIterable = (iterable, onError4) => pipe(acquireRelease2(sync3(() => iterable[Symbol.asyncIterator]()), (iterator) => iterator.return ? promise(async () => iterator.return()) : _void), map7((iterator) => repeatEffectOption(pipe(tryPromise({
  try: async () => iterator.next(),
  catch: (reason) => some(onError4(reason))
}), flatMap3((result) => result.done ? fail4(none()) : succeed5(result.value))))), unwrapScoped3);
var fromChannel2 = (channel) => new StreamImpl(channel);
var toChannel2 = (stream) => {
  if ("channel" in stream) {
    return stream.channel;
  } else if (isEffect(stream)) {
    return toChannel2(fromEffect5(stream));
  } else {
    throw new TypeError(`Expected a Stream.`);
  }
};
var fromChunk = (chunk2) => new StreamImpl(isEmpty(chunk2) ? void_3 : write(chunk2));
var fromChunkPubSub = (pubsub, options) => {
  if (options?.scoped) {
    const effect3 = map7(subscribe2(pubsub), fromChunkQueue);
    return options.shutdown ? map7(effect3, ensuring8(shutdown4(pubsub))) : effect3;
  }
  const stream = flatMap7(scoped3(subscribe2(pubsub)), fromChunkQueue);
  return options?.shutdown ? ensuring8(stream, shutdown4(pubsub)) : stream;
};
var fromChunkQueue = (queue, options) => pipe(take3(queue), catchAllCause((cause) => pipe(isShutdown2(queue), flatMap3((isShutdown9) => isShutdown9 && isInterrupted(cause) ? end3() : failCause7(cause)))), repeatEffectChunkOption, options?.shutdown ? ensuring8(shutdown2(queue)) : identity);
var fromChunks = (...chunks3) => pipe(fromIterable3(chunks3), flatMap7(fromChunk));
var fromEffect5 = (effect3) => pipe(effect3, mapError(some), fromEffectOption);
var fromEffectOption = (effect3) => new StreamImpl(unwrap(match4(effect3, {
  onFailure: match2({
    onNone: () => void_3,
    onSome: fail5
  }),
  onSuccess: (a) => write(of(a))
})));
var fromPubSub3 = (pubsub, options) => {
  const maxChunkSize = options?.maxChunkSize ?? DefaultChunkSize;
  if (options?.scoped) {
    const effect3 = map7(subscribe2(pubsub), (queue) => fromQueue3(queue, {
      maxChunkSize,
      shutdown: true
    }));
    return options.shutdown ? map7(effect3, ensuring8(shutdown4(pubsub))) : effect3;
  }
  const stream = flatMap7(scoped3(subscribe2(pubsub)), (queue) => fromQueue3(queue, {
    maxChunkSize
  }));
  return options?.shutdown ? ensuring8(stream, shutdown4(pubsub)) : stream;
};
var fromTPubSub = (pubsub) => {
  return unwrapScoped3(map7(subscribeScoped2(pubsub), (queue) => fromTQueue(queue)));
};
var fromIterable3 = (iterable) => suspend7(() => isChunk(iterable) ? fromChunk(iterable) : fromIteratorSucceed(iterable[Symbol.iterator]()));
var fromIterableEffect = (effect3) => pipe(effect3, map7(fromIterable3), unwrap3);
var fromIteratorSucceed = (iterator, maxChunkSize = DefaultChunkSize) => {
  return pipe(sync3(() => {
    let builder = [];
    const loop2 = (iterator2) => pipe(sync3(() => {
      let next = iterator2.next();
      if (maxChunkSize === 1) {
        if (next.done) {
          return void_3;
        }
        return pipe(write(of(next.value)), flatMap4(() => loop2(iterator2)));
      }
      builder = [];
      let count4 = 0;
      while (next.done === false) {
        builder.push(next.value);
        count4 = count4 + 1;
        if (count4 >= maxChunkSize) {
          break;
        }
        next = iterator2.next();
      }
      if (count4 > 0) {
        return pipe(write(unsafeFromArray(builder)), flatMap4(() => loop2(iterator2)));
      }
      return void_3;
    }), unwrap);
    return new StreamImpl(loop2(iterator));
  }), unwrap3);
};
var fromPull2 = (effect3) => pipe(effect3, map7(repeatEffectChunkOption), unwrapScoped3);
var fromQueue3 = (queue, options) => pipe(takeBetween2(queue, 1, options?.maxChunkSize ?? DefaultChunkSize), catchAllCause((cause) => pipe(isShutdown2(queue), flatMap3((isShutdown9) => isShutdown9 && isInterrupted(cause) ? end3() : failCause7(cause)))), repeatEffectChunkOption, options?.shutdown ? ensuring8(shutdown2(queue)) : identity);
var fromTQueue = (queue) => pipe(take6(queue), map7(of), catchAllCause((cause) => pipe(isShutdown8(queue), flatMap3((isShutdown9) => isShutdown9 && isInterrupted(cause) ? end3() : failCause7(cause)))), repeatEffectChunkOption);
var fromSchedule = (schedule3) => pipe(driver2(schedule3), map7((driver3) => repeatEffectOption(driver3.next(void 0))), unwrap3);
var fromReadableStream = (...args) => {
  const evaluate2 = args.length === 1 ? args[0].evaluate : args[0];
  const onError4 = args.length === 1 ? args[0].onError : args[1];
  const releaseLockOnEnd = args.length === 1 ? args[0].releaseLockOnEnd === true : false;
  return unwrapScoped3(map7(acquireRelease2(sync3(() => evaluate2().getReader()), (reader) => releaseLockOnEnd ? sync3(() => reader.releaseLock()) : promise(() => reader.cancel())), (reader) => repeatEffectOption(flatMap3(tryPromise({
    try: () => reader.read(),
    catch: (reason) => some(onError4(reason))
  }), ({
    done: done4,
    value
  }) => done4 ? fail4(none()) : succeed5(value)))));
};
var fromReadableStreamByob = (...args) => {
  const evaluate2 = args.length === 1 ? args[0].evaluate : args[0];
  const onError4 = args.length === 1 ? args[0].onError : args[1];
  const allocSize = (args.length === 1 ? args[0].bufferSize : args[2]) ?? 4096;
  const releaseLockOnEnd = args.length === 1 ? args[0].releaseLockOnEnd === true : false;
  return unwrapScoped3(map7(acquireRelease2(sync3(() => evaluate2().getReader({
    mode: "byob"
  })), (reader) => releaseLockOnEnd ? sync3(() => reader.releaseLock()) : promise(() => reader.cancel())), (reader) => catchAll5(forever4(readChunkStreamByobReader(reader, onError4, allocSize)), (error) => error === EOF ? empty5 : fail10(error))));
};
var EOF = Symbol.for("effect/Stream/EOF");
var readChunkStreamByobReader = (reader, onError4, size10) => {
  const buffer4 = new ArrayBuffer(size10);
  return paginateEffect(0, (offset) => flatMap3(tryPromise({
    try: () => reader.read(new Uint8Array(buffer4, offset, buffer4.byteLength - offset)),
    catch: (reason) => onError4(reason)
  }), ({
    done: done4,
    value
  }) => {
    if (done4) {
      return fail4(EOF);
    }
    const newOffset = offset + value.byteLength;
    return succeed5([value, newOffset >= buffer4.byteLength ? none() : some(newOffset)]);
  }));
};
var groupAdjacentBy = dual(2, (self, f) => {
  const groupAdjacentByChunk = (state, chunk2) => {
    if (isEmpty(chunk2)) {
      return [state, empty2()];
    }
    const builder = [];
    let from = 0;
    let until = 0;
    let key = void 0;
    let previousChunk = empty2();
    switch (state._tag) {
      case "Some": {
        const tuple = state.value;
        key = tuple[0];
        let loop2 = true;
        while (loop2 && until < chunk2.length) {
          const input = unsafeGet(chunk2, until);
          const updatedKey = f(input);
          if (!equals(key, updatedKey)) {
            const previousChunk2 = tuple[1];
            const additionalChunk = unsafeFromArray(Array.from(chunk2).slice(from, until));
            const group = appendAll(previousChunk2, additionalChunk);
            builder.push([key, group]);
            key = updatedKey;
            from = until;
            loop2 = false;
          }
          until = until + 1;
        }
        if (loop2) {
          previousChunk = tuple[1];
        }
        break;
      }
      case "None": {
        key = f(unsafeGet(chunk2, until));
        until = until + 1;
        break;
      }
    }
    while (until < chunk2.length) {
      const input = unsafeGet(chunk2, until);
      const updatedKey = f(input);
      if (!equals(key, updatedKey)) {
        builder.push([key, unsafeFromArray(Array.from(chunk2).slice(from, until))]);
        key = updatedKey;
        from = until;
      }
      until = until + 1;
    }
    const nonEmptyChunk = appendAll(previousChunk, unsafeFromArray(Array.from(chunk2).slice(from, until)));
    const output = unsafeFromArray(builder);
    return [some([key, nonEmptyChunk]), output];
  };
  const groupAdjacent = (state) => readWithCause({
    onInput: (input) => {
      const [updatedState, output] = groupAdjacentByChunk(state, input);
      return isEmpty(output) ? groupAdjacent(updatedState) : flatMap4(write(output), () => groupAdjacent(updatedState));
    },
    onFailure: (cause) => match2(state, {
      onNone: () => failCause4(cause),
      onSome: (output) => flatMap4(write(of(output)), () => failCause4(cause))
    }),
    onDone: (done4) => match2(state, {
      onNone: () => succeedNow(done4),
      onSome: (output) => flatMap4(write(of(output)), () => succeedNow(done4))
    })
  });
  return new StreamImpl(pipeToOrFail(toChannel2(self), groupAdjacent(none())));
});
var grouped = dual(2, (self, chunkSize) => pipe(self, rechunk(chunkSize), chunks));
var groupedWithin = dual(3, (self, chunkSize, duration3) => aggregateWithin(self, collectAllN(chunkSize), spaced2(duration3)));
var haltWhen = dual(2, (self, effect3) => {
  const writer = (fiber) => pipe(poll(fiber), map7(match2({
    onNone: () => readWith({
      onInput: (input) => flatMap4(write(input), () => writer(fiber)),
      onFailure: fail5,
      onDone: () => void_3
    }),
    onSome: match3({
      onFailure: failCause4,
      onSuccess: () => void_3
    })
  })), unwrap);
  return new StreamImpl(unwrapScopedWith((scope2) => effect3.pipe(forkIn2(scope2), map7((fiber) => toChannel2(self).pipe(pipeTo(writer(fiber)))))));
});
var haltAfter = dual(2, (self, duration3) => pipe(self, haltWhen(sleep(duration3))));
var haltWhenDeferred = dual(2, (self, deferred) => {
  const writer = pipe(poll2(deferred), map7(match2({
    onNone: () => readWith({
      onInput: (input) => pipe(write(input), flatMap4(() => writer)),
      onFailure: fail5,
      onDone: () => void_3
    }),
    onSome: (effect3) => unwrap(match4(effect3, {
      onFailure: fail5,
      onSuccess: () => void_3
    }))
  })), unwrap);
  return new StreamImpl(pipe(toChannel2(self), pipeTo(writer)));
});
var identityStream = () => new StreamImpl(identityChannel());
var interleave = dual(2, (self, that) => pipe(self, interleaveWith(that, forever4(make22(true, false)))));
var interleaveWith = dual(3, (self, that, decider) => {
  const producer = (handoff) => readWithCause({
    onInput: (value) => flatMap4(fromEffect(offer6(handoff, of2(value))), () => producer(handoff)),
    onFailure: (cause) => fromEffect(offer6(handoff, failCause6(cause))),
    onDone: () => fromEffect(offer6(handoff, end2))
  });
  return new StreamImpl(unwrapScopedWith((scope2) => pipe(make20(), zip2(make20()), tap2(([left2]) => toChannel2(self).pipe(concatMap(writeChunk), pipeTo(producer(left2)), runIn(scope2), forkIn2(scope2))), tap2(([_, right2]) => toChannel2(that).pipe(concatMap(writeChunk), pipeTo(producer(right2)), runIn(scope2), forkIn2(scope2))), map7(([left2, right2]) => {
    const process = (leftDone, rightDone) => readWithCause({
      onInput: (bool) => {
        if (bool && !leftDone) {
          return pipe(fromEffect(take7(left2)), flatMap4(match15({
            onEnd: () => rightDone ? void_3 : process(true, rightDone),
            onFailure: failCause4,
            onSuccess: (chunk2) => pipe(write(chunk2), flatMap4(() => process(leftDone, rightDone)))
          })));
        }
        if (!bool && !rightDone) {
          return pipe(fromEffect(take7(right2)), flatMap4(match15({
            onEnd: () => leftDone ? void_3 : process(leftDone, true),
            onFailure: failCause4,
            onSuccess: (chunk2) => pipe(write(chunk2), flatMap4(() => process(leftDone, rightDone)))
          })));
        }
        return process(leftDone, rightDone);
      },
      onFailure: failCause4,
      onDone: () => void_3
    });
    return pipe(toChannel2(decider), concatMap(writeChunk), pipeTo(process(false, false)));
  }))));
});
var intersperse = dual(2, (self, element) => new StreamImpl(pipe(toChannel2(self), pipeToOrFail(suspend3(() => {
  const writer = (isFirst) => readWithCause({
    onInput: (chunk2) => {
      const builder = [];
      let flagResult = isFirst;
      for (const output of chunk2) {
        if (flagResult) {
          flagResult = false;
          builder.push(output);
        } else {
          builder.push(element);
          builder.push(output);
        }
      }
      return pipe(write(unsafeFromArray(builder)), flatMap4(() => writer(flagResult)));
    },
    onFailure: failCause4,
    onDone: () => void_3
  });
  return writer(true);
})))));
var intersperseAffixes = dual(2, (self, {
  end: end4,
  middle,
  start
}) => pipe(make22(start), concat(pipe(self, intersperse(middle))), concat(make22(end4))));
var interruptAfter = dual(2, (self, duration3) => pipe(self, interruptWhen2(sleep(duration3))));
var interruptWhen2 = dual(2, (self, effect3) => new StreamImpl(pipe(toChannel2(self), interruptWhen(effect3))));
var interruptWhenDeferred2 = dual(2, (self, deferred) => new StreamImpl(pipe(toChannel2(self), interruptWhenDeferred(deferred))));
var iterate2 = (value, next) => unfold3(value, (a) => some([a, next(a)]));
var make22 = (...as10) => fromIterable3(as10);
var map13 = dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapOut(map2(f)))));
var mapAccum2 = dual(3, (self, s, f) => {
  const accumulator = (s2) => readWith({
    onInput: (input) => {
      const [nextS, chunk2] = mapAccum(input, s2, f);
      return flatMap4(write(chunk2), () => accumulator(nextS));
    },
    onFailure: fail5,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(accumulator(s))));
});
var mapAccumEffect = dual(3, (self, s, f) => suspend7(() => {
  const accumulator = (s2) => readWith({
    onInput: (input) => pipe(suspend2(() => {
      const outputs = [];
      const emit2 = (output) => sync3(() => {
        outputs.push(output);
      });
      return pipe(input, reduce3(s2, (s3, a) => pipe(f(s3, a), flatMap3(([s4, a2]) => pipe(emit2(a2), as3(s4))))), match4({
        onFailure: (error) => {
          if (outputs.length !== 0) {
            return zipRight5(write(unsafeFromArray(outputs)), fail5(error));
          }
          return fail5(error);
        },
        onSuccess: (s3) => flatMap4(write(unsafeFromArray(outputs)), () => accumulator(s3))
      }));
    }), unwrap),
    onFailure: fail5,
    onDone: () => void_3
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(accumulator(s))));
}));
var mapBoth6 = dual(2, (self, options) => pipe(self, mapError5(options.onFailure), map13(options.onSuccess)));
var mapChunks = dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapOut(f))));
var mapChunksEffect = dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapOutEffect(f))));
var mapConcat = dual(2, (self, f) => pipe(self, mapConcatChunk((a) => fromIterable2(f(a)))));
var mapConcatChunk = dual(2, (self, f) => pipe(self, mapChunks(flatMap(f))));
var mapConcatChunkEffect = dual(2, (self, f) => pipe(self, mapEffectSequential(f), mapConcatChunk(identity)));
var mapConcatEffect = dual(2, (self, f) => pipe(self, mapEffectSequential((a) => pipe(f(a), map7(fromIterable2))), mapConcatChunk(identity)));
var mapEffectSequential = dual(2, (self, f) => {
  const loop2 = (iterator) => {
    const next = iterator.next();
    if (next.done) {
      return readWithCause({
        onInput: (elem) => loop2(elem[Symbol.iterator]()),
        onFailure: failCause4,
        onDone: succeed6
      });
    } else {
      const value = next.value;
      return unwrap(map7(f(value), (a2) => flatMap4(write(of(a2)), () => loop2(iterator))));
    }
  };
  return new StreamImpl(pipe(toChannel2(self), pipeTo(suspend3(() => loop2(empty2()[Symbol.iterator]())))));
});
var mapEffectPar = dual(3, (self, n, f) => new StreamImpl(pipe(toChannel2(self), concatMap(writeChunk), mapOutEffectPar(f, n), mapOut(of))));
var mapError5 = dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapError2(f))));
var mapErrorCause2 = dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), mapErrorCause(f))));
var merge5 = dual((args) => isStream(args[1]), (self, that, options) => mergeWith2(self, that, {
  onSelf: identity,
  onOther: identity,
  haltStrategy: options?.haltStrategy
}));
var mergeAll3 = dual((args) => Symbol.iterator in args[0], (streams, options) => flatten5(fromIterable3(streams), options));
var mergeWithTag = dual(2, (streams, options) => {
  const keys = Object.keys(streams);
  const values = keys.map((key) => streams[key].pipe(map13((value) => ({
    _tag: key,
    value
  }))));
  return mergeAll3(values, options);
});
var mergeEither = dual(2, (self, that) => mergeWith2(self, that, {
  onSelf: left,
  onOther: right
}));
var mergeLeft = dual(2, (left2, right2) => pipe(left2, merge5(drain3(right2))));
var mergeRight = dual(2, (left2, right2) => pipe(drain3(left2), merge5(right2)));
var mergeWith2 = dual(3, (self, other, options) => {
  const strategy = options.haltStrategy ? fromInput2(options.haltStrategy) : Both2;
  const handler = (terminate) => (exit2) => terminate || !isSuccess(exit2) ? (
    // TODO: remove
    Done3(suspend2(() => exit2))
  ) : Await2((exit3) => suspend2(() => exit3));
  return new StreamImpl(mergeWith(toChannel2(map13(self, options.onSelf)), {
    other: toChannel2(map13(other, options.onOther)),
    onSelfDone: handler(strategy._tag === "Either" || strategy._tag === "Left"),
    onOtherDone: handler(strategy._tag === "Either" || strategy._tag === "Right")
  }));
});
var mkString2 = (self) => run4(self, mkString);
var never4 = fromEffect5(never);
var onEnd = dual(2, (self, effect3) => concat(self, drain3(fromEffect5(effect3))));
var onError2 = dual(2, (self, cleanup) => pipe(self, catchAllCause3((cause) => fromEffect5(pipe(cleanup(cause), zipRight4(failCause3(cause)))))));
var onDone = dual(2, (self, cleanup) => new StreamImpl(pipe(toChannel2(self), ensuringWith((exit2) => isSuccess(exit2) ? cleanup() : _void))));
var onStart = dual(2, (self, effect3) => unwrap3(as3(effect3, self)));
var orDie4 = (self) => pipe(self, orDieWith3(identity));
var orDieWith3 = dual(2, (self, f) => new StreamImpl(pipe(toChannel2(self), orDieWith(f))));
var orElse6 = dual(2, (self, that) => new StreamImpl(pipe(toChannel2(self), orElse3(() => toChannel2(that())))));
var orElseEither2 = dual(2, (self, that) => pipe(self, map13(left), orElse6(() => pipe(that(), map13(right)))));
var orElseFail2 = dual(2, (self, error) => pipe(self, orElse6(() => failSync5(error))));
var orElseIfEmpty = dual(2, (self, element) => pipe(self, orElseIfEmptyChunk(() => of(element()))));
var orElseIfEmptyChunk = dual(2, (self, chunk2) => pipe(self, orElseIfEmptyStream(() => new StreamImpl(write(chunk2())))));
var orElseIfEmptyStream = dual(2, (self, stream) => {
  const writer = readWith({
    onInput: (input) => {
      if (isEmpty(input)) {
        return suspend3(() => writer);
      }
      return pipe(write(input), zipRight5(identityChannel()));
    },
    onFailure: fail5,
    onDone: () => suspend3(() => toChannel2(stream()))
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(writer)));
});
var orElseSucceed2 = dual(2, (self, value) => pipe(self, orElse6(() => sync8(value))));
var paginate = (s, f) => paginateChunk(s, (s2) => {
  const page = f(s2);
  return [of(page[0]), page[1]];
});
var paginateChunk = (s, f) => {
  const loop2 = (s2) => {
    const page = f(s2);
    return match2(page[1], {
      onNone: () => zipRight5(write(page[0]), void_3),
      onSome: (s3) => flatMap4(write(page[0]), () => loop2(s3))
    });
  };
  return new StreamImpl(suspend3(() => loop2(s)));
};
var paginateChunkEffect = (s, f) => {
  const loop2 = (s2) => unwrap(map7(f(s2), ([chunk2, option2]) => match2(option2, {
    onNone: () => zipRight5(write(chunk2), void_3),
    onSome: (s3) => flatMap4(write(chunk2), () => loop2(s3))
  })));
  return new StreamImpl(suspend3(() => loop2(s)));
};
var paginateEffect = (s, f) => paginateChunkEffect(s, (s2) => pipe(f(s2), map7(([a, s3]) => [of(a), s3])));
var peel = dual(2, (self, sink) => {
  const OP_EMIT5 = "Emit";
  const OP_HALT2 = "Halt";
  const OP_END2 = "End";
  return pipe(make8(), flatMap3((deferred) => pipe(make20(), map7((handoff) => {
    const consumer = foldSink(collectLeftover(sink), {
      onFailure: (error) => zipRight7(fromEffect3(fail3(deferred, error)), fail6(error)),
      onSuccess: ([z, leftovers]) => {
        const loop2 = readWithCause({
          onInput: (elements) => flatMap4(fromEffect(offer6(handoff, {
            _tag: OP_EMIT5,
            elements
          })), () => loop2),
          onFailure: (cause) => zipRight5(fromEffect(offer6(handoff, {
            _tag: OP_HALT2,
            cause
          })), failCause4(cause)),
          onDone: (_) => zipRight5(fromEffect(offer6(handoff, {
            _tag: OP_END2
          })), void_3)
        });
        return fromChannel(pipe(fromEffect(succeed4(deferred, z)), zipRight5(fromEffect(pipe(handoff, offer6({
          _tag: OP_EMIT5,
          elements: leftovers
        })))), zipRight5(loop2)));
      }
    });
    const producer = pipe(take7(handoff), map7((signal) => {
      switch (signal._tag) {
        case OP_EMIT5: {
          return pipe(write(signal.elements), flatMap4(() => producer));
        }
        case OP_HALT2: {
          return failCause4(signal.cause);
        }
        case OP_END2: {
          return void_3;
        }
      }
    }), unwrap);
    return pipe(self, tapErrorCause2((cause) => failCause2(deferred, cause)), run4(consumer), forkScoped, zipRight4(_await2(deferred)), map7((z) => [z, new StreamImpl(producer)]));
  }))), flatten2);
});
var partition2 = dual((args) => typeof args[1] === "function", (self, predicate, options) => partitionEither(self, (a) => succeed5(predicate(a) ? right(a) : left(a)), options));
var partitionEither = dual((args) => typeof args[1] === "function", (self, predicate, options) => pipe(mapEffectSequential(self, predicate), distributedWith({
  size: 2,
  maximumLag: options?.bufferSize ?? 16,
  decide: match({
    onLeft: () => succeed5((n) => n === 0),
    onRight: () => succeed5((n) => n === 1)
  })
}), flatMap3(([queue1, queue2]) => succeed5([filterMap2(flattenExitOption(fromQueue3(queue1, {
  shutdown: true
})), (_) => match(_, {
  onLeft: some,
  onRight: none
})), filterMap2(flattenExitOption(fromQueue3(queue2, {
  shutdown: true
})), (_) => match(_, {
  onLeft: none,
  onRight: some
}))]))));
var pipeThrough = dual(2, (self, sink) => new StreamImpl(pipe(toChannel2(self), pipeToOrFail(toChannel(sink)))));
var pipeThroughChannel = dual(2, (self, channel) => new StreamImpl(pipeTo(toChannel2(self), channel)));
var pipeThroughChannelOrFail = dual(2, (self, chan) => new StreamImpl(pipe(toChannel2(self), pipeToOrFail(chan))));
var prepend2 = dual(2, (self, values) => new StreamImpl(zipRight5(write(values), toChannel2(self))));
var provideContext6 = dual(2, (self, context8) => new StreamImpl(pipe(toChannel2(self), provideContext2(context8))));
var provideSomeContext2 = dual(2, (self, context8) => mapInputContext5(self, merge2(context8)));
var provideLayer2 = dual(2, (self, layer) => new StreamImpl(unwrapScopedWith((scope2) => buildWithScope(layer, scope2).pipe(map7((env) => pipe(toChannel2(self), provideContext2(env)))))));
var provideService7 = dual(3, (self, tag, resource) => provideServiceEffect2(self, tag, succeed5(resource)));
var provideServiceEffect2 = dual(3, (self, tag, effect3) => provideServiceStream(self, tag, fromEffect5(effect3)));
var provideServiceStream = dual(3, (self, tag, stream) => contextWithStream((env) => flatMap7(stream, (service) => pipe(self, provideContext6(add(env, tag, service))))));
var mapInputContext5 = dual(2, (self, f) => contextWithStream((env) => pipe(self, provideContext6(f(env)))));
var provideSomeLayer2 = dual(2, (self, layer) => (
  // @ts-expect-error
  // @effect-diagnostics-next-line missingEffectContext:off
  pipe(self, provideLayer2(pipe(context2(), merge3(layer))))
));
var range2 = (min, max, chunkSize = DefaultChunkSize) => suspend7(() => {
  if (min > max) {
    return empty5;
  }
  const go = (min2, max2, chunkSize2) => {
    const remaining = max2 - min2 + 1;
    if (remaining > chunkSize2) {
      return pipe(write(range(min2, min2 + chunkSize2 - 1)), flatMap4(() => go(min2 + chunkSize2, max2, chunkSize2)));
    }
    return write(range(min2, min2 + remaining - 1));
  };
  return new StreamImpl(go(min, max, chunkSize));
});
var race3 = dual(2, (left2, right2) => raceAll(left2, right2));
var raceAll = (...streams) => make8().pipe(map7((halt2) => {
  let winner = null;
  return mergeAll3(streams.map((stream, index) => stream.pipe(takeWhile3(() => {
    if (winner === null) {
      winner = index;
      unsafeDone(halt2, void_2);
      return true;
    }
    return winner === index;
  }), interruptWhen2(_await2(halt2).pipe(flatMap3(() => winner === index ? never : _void))))), {
    concurrency: streams.length
  });
}), unwrap3);
var rechunk = dual(2, (self, n) => suspend7(() => {
  const target = Math.max(n, 1);
  const process = rechunkProcess(new StreamRechunker(target), target);
  return new StreamImpl(pipe(toChannel2(self), pipeTo(process)));
}));
var rechunkProcess = (rechunker, target) => readWithCause({
  onInput: (chunk2) => {
    if (chunk2.length === target && rechunker.isEmpty()) {
      return flatMap4(write(chunk2), () => rechunkProcess(rechunker, target));
    }
    if (chunk2.length > 0) {
      const chunks3 = [];
      let result = void 0;
      let index = 0;
      while (index < chunk2.length) {
        while (index < chunk2.length && result === void 0) {
          result = rechunker.write(pipe(chunk2, unsafeGet(index)));
          index = index + 1;
        }
        if (result !== void 0) {
          chunks3.push(result);
          result = void 0;
        }
      }
      return flatMap4(writeAll(...chunks3), () => rechunkProcess(rechunker, target));
    }
    return suspend3(() => rechunkProcess(rechunker, target));
  },
  onFailure: (cause) => zipRight5(rechunker.emitIfNotEmpty(), failCause4(cause)),
  onDone: () => rechunker.emitIfNotEmpty()
});
var StreamRechunker = class {
  n;
  builder = [];
  pos = 0;
  constructor(n) {
    this.n = n;
  }
  isEmpty() {
    return this.pos === 0;
  }
  write(elem) {
    this.builder.push(elem);
    this.pos += 1;
    if (this.pos === this.n) {
      const result = unsafeFromArray(this.builder);
      this.builder = [];
      this.pos = 0;
      return result;
    }
    return void 0;
  }
  emitIfNotEmpty() {
    if (this.pos !== 0) {
      return write(unsafeFromArray(this.builder));
    }
    return void_3;
  }
};
var refineOrDie3 = dual(2, (self, pf) => pipe(self, refineOrDieWith3(pf, identity)));
var refineOrDieWith3 = dual(3, (self, pf, f) => new StreamImpl(catchAll3(toChannel2(self), (error) => match2(pf(error), {
  onNone: () => failCause4(die2(f(error))),
  onSome: fail5
}))));
var repeat2 = dual(2, (self, schedule3) => filterMap2(repeatEither(self, schedule3), (_) => match(_, {
  onLeft: none,
  onRight: some
})));
var repeatEffect = (effect3) => repeatEffectOption(pipe(effect3, mapError(some)));
var repeatEffectChunk = (effect3) => repeatEffectChunkOption(pipe(effect3, mapError(some)));
var repeatEffectChunkOption = (effect3) => unfoldChunkEffect(effect3, (effect4) => pipe(map7(effect4, (chunk2) => some([chunk2, effect4])), catchAll2(match2({
  onNone: () => succeed5(none()),
  onSome: fail4
}))));
var repeatEffectOption = (effect3) => repeatEffectChunkOption(pipe(effect3, map7(of)));
var repeatEither = dual(2, (self, schedule3) => repeatWith(self, schedule3, {
  onElement: (a) => right(a),
  onSchedule: left
}));
var repeatElements = dual(2, (self, schedule3) => filterMap2(repeatElementsWith(self, schedule3, {
  onElement: (a) => some(a),
  onSchedule: none
}), identity));
var repeatElementsWith = dual(3, (self, schedule3, options) => {
  const driver3 = pipe(driver2(schedule3), map7((driver4) => {
    const feed = (input) => match2(head2(input), {
      onNone: () => loop2,
      onSome: (a) => zipRight5(write(of(options.onElement(a))), step(pipe(input, drop(1)), a))
    });
    const step = (input, a) => {
      const advance = pipe(driver4.next(a), as3(pipe(write(of(options.onElement(a))), flatMap4(() => step(input, a)))));
      const reset2 = pipe(driver4.last, orDie, flatMap3((b) => pipe(driver4.reset, map7(() => pipe(write(of(options.onSchedule(b))), zipRight5(feed(input)))))));
      return pipe(advance, orElse2(() => reset2), unwrap);
    };
    const loop2 = readWith({
      onInput: feed,
      onFailure: fail5,
      onDone: () => void_3
    });
    return loop2;
  }), unwrap);
  return new StreamImpl(pipe(toChannel2(self), pipeTo(driver3)));
});
var repeatValue = (value) => new StreamImpl(repeated(write(of(value))));
var repeatWith = dual(3, (self, schedule3, options) => {
  return pipe(driver2(schedule3), map7((driver3) => {
    const provideLastIterationInfo = provideServiceEffect2(CurrentIterationMetadata2, get3(driver3.iterationMeta));
    const process = pipe(self, provideLastIterationInfo, map13(options.onElement), toChannel2);
    const loop2 = unwrap(match4(driver3.next(void 0), {
      onFailure: () => void_3,
      onSuccess: (output) => flatMap4(process, () => zipRight5(write(of(options.onSchedule(output))), loop2))
    }));
    return new StreamImpl(zipRight5(process, loop2));
  }), unwrap3);
});
var repeatWithSchedule = (value, schedule3) => repeatEffectWithSchedule(succeed5(value), schedule3);
var repeatEffectWithSchedule = (effect3, schedule3) => flatMap7(fromEffect5(zip2(effect3, driver2(schedule3))), ([a, driver3]) => {
  const provideLastIterationInfo = provideServiceEffect(CurrentIterationMetadata2, get3(driver3.iterationMeta));
  return concat(succeed11(a), unfoldEffect(a, (s) => matchEffect2(driver3.next(s), {
    onFailure: succeed5,
    onSuccess: () => map7(provideLastIterationInfo(effect3), (nextA) => some([nextA, nextA]))
  })));
});
var retry3 = dual(2, (self, policy) => driver2(policy).pipe(map7((driver3) => {
  const provideLastIterationInfo = provideServiceEffect2(CurrentIterationMetadata2, get3(driver3.iterationMeta));
  const loop2 = toChannel2(provideLastIterationInfo(self)).pipe(mapOutEffect((out) => as3(driver3.reset, out)), catchAll3((error) => driver3.next(error).pipe(match4({
    onFailure: () => fail5(error),
    onSuccess: () => loop2
  }), unwrap)));
  return loop2;
}), unwrap, fromChannel2));
var withExecutionPlan = dual((args) => isStream(args[0]), (self, policy, options) => suspend7(() => {
  const preventFallbackOnPartialStream = options?.preventFallbackOnPartialStream ?? false;
  let i = 0;
  let lastError = none();
  const loop2 = suspend7(() => {
    const step = policy.steps[i++];
    if (!step) {
      return fail10(getOrThrow(lastError));
    }
    let nextStream = isContext(step.provide) ? provideSomeContext2(self, step.provide) : provideSomeLayer2(self, step.provide);
    let receivedElements = false;
    if (isSome(lastError)) {
      const error = lastError.value;
      let attempted = false;
      const wrapped = nextStream;
      nextStream = suspend7(() => {
        if (attempted) return wrapped;
        attempted = true;
        return fail10(error);
      });
      nextStream = scheduleDefectRefail(retry3(nextStream, scheduleFromStep(step, false)));
    } else {
      const schedule3 = scheduleFromStep(step, true);
      nextStream = schedule3 ? scheduleDefectRefail(retry3(nextStream, schedule3)) : nextStream;
    }
    return catchAll5(preventFallbackOnPartialStream ? mapChunks(nextStream, (chunk2) => {
      receivedElements = true;
      return chunk2;
    }) : nextStream, (error) => {
      if (preventFallbackOnPartialStream && receivedElements) {
        return fail10(error);
      }
      lastError = some(error);
      return loop2;
    });
  });
  return loop2;
}));
var scheduleDefectRefail = (self) => catchAllCause3(self, (cause) => failCause8(scheduleDefectRefailCause(cause)));
var run4 = dual(2, (self, sink) => toChannel2(self).pipe(pipeToOrFail(toChannel(sink)), runDrain));
var runCollect2 = (self) => run4(self, collectAll());
var runCount = (self) => run4(self, count3);
var runDrain2 = (self) => run4(self, drain2);
var runFold = dual(3, (self, s, f) => runFoldWhile(self, s, constTrue, f));
var runFoldEffect = dual(3, (self, s, f) => runFoldWhileEffect(self, s, constTrue, f));
var runFoldScoped = dual(3, (self, s, f) => pipe(self, runFoldWhileScoped(s, constTrue, f)));
var runFoldScopedEffect = dual(3, (self, s, f) => pipe(self, runFoldWhileScopedEffect(s, constTrue, f)));
var runFoldWhile = dual(4, (self, s, cont, f) => run4(self, fold(s, cont, f)));
var runFoldWhileEffect = dual(4, (self, s, cont, f) => run4(self, foldEffect(s, cont, f)));
var runFoldWhileScoped = dual(4, (self, s, cont, f) => pipe(self, runScoped2(fold(s, cont, f))));
var runFoldWhileScopedEffect = dual(4, (self, s, cont, f) => pipe(self, runScoped2(foldEffect(s, cont, f))));
var runForEach = dual(2, (self, f) => run4(self, forEach3(f)));
var runForEachChunk = dual(2, (self, f) => run4(self, forEachChunk(f)));
var runForEachChunkScoped = dual(2, (self, f) => pipe(self, runScoped2(forEachChunk(f))));
var runForEachScoped = dual(2, (self, f) => pipe(self, runScoped2(forEach3(f))));
var runForEachWhile = dual(2, (self, f) => run4(self, forEachWhile(f)));
var runForEachWhileScoped = dual(2, (self, f) => pipe(self, runScoped2(forEachWhile(f))));
var runHead = (self) => run4(self, head3());
var runIntoPubSub = dual(2, (self, pubsub) => pipe(self, runIntoQueue(pubsub)));
var runIntoPubSubScoped = dual(2, (self, pubsub) => pipe(self, runIntoQueueScoped(pubsub)));
var runIntoQueue = dual(2, (self, queue) => pipe(self, runIntoQueueScoped(queue), scoped));
var runIntoQueueElementsScoped = dual(2, (self, queue) => {
  const writer = readWithCause({
    onInput: (input) => flatMap4(fromEffect(offerAll3(queue, map2(input, succeed2))), () => writer),
    onFailure: (cause) => fromEffect(offer3(queue, failCause(map5(cause, some)))),
    onDone: () => fromEffect(offer3(queue, fail(none())))
  });
  return pipe(pipeTo(toChannel2(self), writer), drain, runScoped, asVoid3);
});
var runIntoQueueScoped = dual(2, (self, queue) => {
  const writer = readWithCause({
    onInput: (input) => flatMap4(write(chunk(input)), () => writer),
    onFailure: (cause) => write(failCause6(cause)),
    onDone: () => write(end2)
  });
  return pipe(pipeTo(toChannel2(self), writer), mapOutEffect((take10) => offer3(queue, take10)), drain, runScoped, asVoid3);
});
var runLast = (self) => run4(self, last2());
var runScoped2 = dual(2, (self, sink) => pipe(toChannel2(self), pipeToOrFail(toChannel(sink)), drain, runScoped));
var runSum = (self) => run4(self, sum);
var scan = dual(3, (self, s, f) => pipe(self, scanEffect(s, (s2, a) => succeed5(f(s2, a)))));
var scanReduce = dual(2, (self, f) => pipe(self, scanReduceEffect((a2, a) => succeed5(f(a2, a)))));
var scanReduceEffect = dual(2, (self, f) => pipe(self, mapAccumEffect(none(), (option2, a) => {
  switch (option2._tag) {
    case "None": {
      return succeed5([some(a), a]);
    }
    case "Some": {
      return pipe(f(option2.value, a), map7((b) => [some(b), b]));
    }
  }
})));
var schedule = dual(2, (self, schedule3) => filterMap2(scheduleWith(self, schedule3, {
  onElement: some,
  onSchedule: none
}), identity));
var scheduleWith = dual(3, (self, schedule3, options) => {
  const loop2 = (driver3, iterator) => {
    const next = iterator.next();
    if (next.done) {
      return readWithCause({
        onInput: (chunk2) => loop2(driver3, chunk2[Symbol.iterator]()),
        onFailure: failCause4,
        onDone: succeedNow
      });
    }
    return unwrap(matchEffect2(driver3.next(next.value), {
      onFailure: () => pipe(driver3.last, orDie, map7((b) => pipe(write(make2(options.onElement(next.value), options.onSchedule(b))), flatMap4(() => loop2(driver3, iterator)))), zipLeft2(driver3.reset)),
      onSuccess: () => succeed5(pipe(write(of(options.onElement(next.value))), flatMap4(() => loop2(driver3, iterator))))
    }));
  };
  return new StreamImpl(pipe(fromEffect(driver2(schedule3)), flatMap4((driver3) => pipe(toChannel2(self), pipeTo(loop2(driver3, empty2()[Symbol.iterator]()))))));
});
var scanEffect = dual(3, (self, s, f) => new StreamImpl(pipe(write(of(s)), flatMap4(() => toChannel2(pipe(self, mapAccumEffect(s, (s2, a) => pipe(f(s2, a), map7((s3) => [s3, s3])))))))));
var scoped3 = (effect3) => new StreamImpl(ensuring4(scoped2(pipe(effect3, map7(of))), _void));
var scopedWith3 = (f) => new StreamImpl(scopedWith2((scope2) => f(scope2).pipe(map7(of))));
var some4 = (self) => pipe(self, mapError5(some), someOrFail(() => none()));
var someOrElse = dual(2, (self, fallback) => pipe(self, map13(getOrElse(fallback))));
var someOrFail = dual(2, (self, error) => mapEffectSequential(self, match2({
  onNone: () => failSync(error),
  onSome: succeed5
})));
var sliding9 = dual(2, (self, chunkSize) => slidingSize(self, chunkSize, 1));
var slidingSize = dual(3, (self, chunkSize, stepSize) => {
  if (chunkSize <= 0 || stepSize <= 0) {
    return die8(new IllegalArgumentException("Invalid bounds - `chunkSize` and `stepSize` must be greater than zero"));
  }
  return new StreamImpl(suspend3(() => {
    const queue = new RingBuffer(chunkSize);
    const emitOnStreamEnd = (queueSize, channelEnd) => {
      if (queueSize < chunkSize) {
        const items = queue.toChunk();
        const result2 = isEmpty(items) ? empty2() : of(items);
        return pipe(write(result2), flatMap4(() => channelEnd));
      }
      const lastEmitIndex = queueSize - (queueSize - chunkSize) % stepSize;
      if (lastEmitIndex === queueSize) {
        return channelEnd;
      }
      const leftovers = queueSize - (lastEmitIndex - chunkSize + stepSize);
      const lastItems = pipe(queue.toChunk(), takeRight(leftovers));
      const result = isEmpty(lastItems) ? empty2() : of(lastItems);
      return pipe(write(result), flatMap4(() => channelEnd));
    };
    const reader = (queueSize) => readWithCause({
      onInput: (input) => flatMap4(write(filterMap(input, (element, index) => {
        queue.put(element);
        const currentIndex = queueSize + index + 1;
        if (currentIndex < chunkSize || (currentIndex - chunkSize) % stepSize > 0) {
          return none();
        }
        return some(queue.toChunk());
      })), () => reader(queueSize + input.length)),
      onFailure: (cause) => emitOnStreamEnd(queueSize, failCause4(cause)),
      onDone: () => emitOnStreamEnd(queueSize, void_3)
    });
    return pipe(toChannel2(self), pipeTo(reader(0)));
  }));
});
var split = dual(2, (self, predicate) => {
  const split3 = (leftovers, input) => {
    const [chunk2, remaining] = pipe(leftovers, appendAll(input), splitWhere(predicate));
    if (isEmpty(chunk2) || isEmpty(remaining)) {
      return loop2(pipe(chunk2, appendAll(pipe(remaining, drop(1)))));
    }
    return pipe(write(of(chunk2)), flatMap4(() => split3(empty2(), pipe(remaining, drop(1)))));
  };
  const loop2 = (leftovers) => readWith({
    onInput: (input) => split3(leftovers, input),
    onFailure: fail5,
    onDone: () => {
      if (isEmpty(leftovers)) {
        return void_3;
      }
      if (isNone(pipe(leftovers, findFirst(predicate)))) {
        return zipRight5(write(of(leftovers)), void_3);
      }
      return zipRight5(split3(empty2(), leftovers), void_3);
    }
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop2(empty2()))));
});
var splitOnChunk = dual(2, (self, delimiter) => {
  const next = (leftover2, delimiterIndex) => readWithCause({
    onInput: (inputChunk) => {
      let buffer4;
      const [carry, delimiterCursor] = pipe(inputChunk, reduce([pipe(leftover2, getOrElse(() => empty2())), delimiterIndex], ([carry2, delimiterCursor2], a) => {
        const concatenated = pipe(carry2, append(a));
        if (delimiterCursor2 < delimiter.length && equals(a, pipe(delimiter, unsafeGet(delimiterCursor2)))) {
          if (delimiterCursor2 + 1 === delimiter.length) {
            if (buffer4 === void 0) {
              buffer4 = [];
            }
            buffer4.push(pipe(concatenated, take(concatenated.length - delimiter.length)));
            return [empty2(), 0];
          }
          return [concatenated, delimiterCursor2 + 1];
        }
        return [concatenated, equals(a, pipe(delimiter, unsafeGet(0))) ? 1 : 0];
      }));
      const output = buffer4 === void 0 ? empty2() : unsafeFromArray(buffer4);
      return flatMap4(write(output), () => next(isNonEmpty(carry) ? some(carry) : none(), delimiterCursor));
    },
    onFailure: (cause) => match2(leftover2, {
      onNone: () => failCause4(cause),
      onSome: (chunk2) => zipRight5(write(of(chunk2)), failCause4(cause))
    }),
    onDone: (done4) => match2(leftover2, {
      onNone: () => succeed6(done4),
      onSome: (chunk2) => zipRight5(write(of(chunk2)), succeed6(done4))
    })
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(next(none(), 0))));
});
var splitLines2 = (self) => pipeThroughChannel(self, splitLines());
var succeed11 = (value) => fromChunk(of(value));
var sync8 = (evaluate2) => suspend7(() => fromChunk(of(evaluate2())));
var suspend7 = (stream) => new StreamImpl(suspend3(() => toChannel2(stream())));
var take8 = dual(2, (self, n) => {
  if (!Number.isInteger(n)) {
    return die8(new IllegalArgumentException(`${n} must be an integer`));
  }
  const loop2 = (n2) => readWith({
    onInput: (input) => {
      const taken = pipe(input, take(Math.min(n2, Number.POSITIVE_INFINITY)));
      const leftover2 = Math.max(0, n2 - taken.length);
      const more = leftover2 > 0;
      if (more) {
        return pipe(write(taken), flatMap4(() => loop2(leftover2)));
      }
      return write(taken);
    },
    onFailure: fail5,
    onDone: succeed6
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(0 < n ? loop2(n) : void_3)));
});
var takeRight2 = dual(2, (self, n) => {
  if (n <= 0) {
    return empty5;
  }
  return new StreamImpl(pipe(succeed5(new RingBuffer(n)), map7((queue) => {
    const reader = readWith({
      onInput: (input) => {
        for (const element of input) {
          queue.put(element);
        }
        return reader;
      },
      onFailure: fail5,
      onDone: () => pipe(write(queue.toChunk()), zipRight5(void_3))
    });
    return pipe(toChannel2(self), pipeTo(reader));
  }), unwrap));
});
var takeUntil = dual(2, (self, predicate) => {
  const loop2 = readWith({
    onInput: (input) => {
      const taken = pipe(input, takeWhile((a) => !predicate(a)));
      const last3 = pipe(input, drop(taken.length), take(1));
      if (isEmpty(last3)) {
        return pipe(write(taken), flatMap4(() => loop2));
      }
      return write(pipe(taken, appendAll(last3)));
    },
    onFailure: fail5,
    onDone: succeed6
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop2)));
});
var takeUntilEffect = dual(2, (self, predicate) => {
  const loop2 = (iterator) => {
    const next = iterator.next();
    if (next.done) {
      return readWithCause({
        onInput: (elem) => loop2(elem[Symbol.iterator]()),
        onFailure: failCause4,
        onDone: succeed6
      });
    }
    return pipe(predicate(next.value), map7((bool) => bool ? write(of(next.value)) : pipe(write(of(next.value)), flatMap4(() => loop2(iterator)))), unwrap);
  };
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop2(empty2()[Symbol.iterator]()))));
});
var takeWhile3 = dual(2, (self, predicate) => {
  const loop2 = readWith({
    onInput: (input) => {
      const taken = pipe(input, takeWhile(predicate));
      const more = taken.length === input.length;
      if (more) {
        return pipe(write(taken), flatMap4(() => loop2));
      }
      return write(taken);
    },
    onFailure: fail5,
    onDone: succeed6
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(loop2)));
});
var tap5 = dual(2, (self, f) => mapEffectSequential(self, (a) => as3(f(a), a)));
var tapBoth2 = dual(2, (self, options) => pipe(self, tapError2(options.onFailure), tap5(options.onSuccess)));
var tapError2 = dual(2, (self, f) => catchAll5(self, (error) => fromEffect5(zipRight4(f(error), fail4(error)))));
var tapErrorCause2 = dual(2, (self, f) => {
  const loop2 = readWithCause({
    onInput: (chunk2) => flatMap4(write(chunk2), () => loop2),
    onFailure: (cause) => fromEffect(zipRight4(f(cause), failCause3(cause))),
    onDone: succeedNow
  });
  return new StreamImpl(pipe(toChannel2(self), pipeTo(loop2)));
});
var tapSink = dual(2, (self, sink) => pipe(fromEffect5(all3([bounded3(1), make8()])), flatMap7(([queue, deferred]) => {
  const right2 = flattenTake(fromQueue3(queue, {
    maxChunkSize: 1
  }));
  const loop2 = readWithCause({
    onInput: (chunk2) => pipe(fromEffect(offer3(queue, chunk(chunk2))), foldCauseChannel({
      onFailure: () => flatMap4(write(chunk2), () => identityChannel()),
      onSuccess: () => flatMap4(write(chunk2), () => loop2)
    })),
    onFailure: (cause) => pipe(fromEffect(offer3(queue, failCause6(cause))), foldCauseChannel({
      onFailure: () => failCause4(cause),
      onSuccess: () => failCause4(cause)
    })),
    onDone: () => pipe(fromEffect(offer3(queue, end2)), foldCauseChannel({
      onFailure: () => void_3,
      onSuccess: () => void_3
    }))
  });
  return pipe(new StreamImpl(pipe(pipeTo(toChannel2(self), loop2), ensuring4(zipRight4(forkDaemon(offer3(queue, end2)), _await2(deferred))))), merge5(execute(pipe(run4(right2, sink), ensuring3(zipRight4(shutdown2(queue), succeed4(deferred, void 0)))))));
})));
var throttle = dual(2, (self, options) => throttleEffect(self, {
  ...options,
  cost: (chunk2) => succeed5(options.cost(chunk2))
}));
var throttleEffect = dual(2, (self, options) => {
  if (options.strategy === "enforce") {
    return throttleEnforceEffect(self, options.cost, options.units, options.duration, options.burst ?? 0);
  }
  return throttleShapeEffect(self, options.cost, options.units, options.duration, options.burst ?? 0);
});
var throttleEnforceEffect = (self, cost, units, duration3, burst) => {
  const loop2 = (tokens, timestampMillis) => readWithCause({
    onInput: (input) => pipe(cost(input), zip2(currentTimeMillis), map7(([weight, currentTimeMillis2]) => {
      const elapsed3 = currentTimeMillis2 - timestampMillis;
      const cycles = elapsed3 / toMillis(duration3);
      const sum2 = tokens + cycles * units;
      const max = units + burst < 0 ? Number.POSITIVE_INFINITY : units + burst;
      const available = sum2 < 0 ? max : Math.min(sum2, max);
      if (weight <= available) {
        return pipe(write(input), flatMap4(() => loop2(available - weight, currentTimeMillis2)));
      }
      return loop2(tokens, timestampMillis);
    }), unwrap),
    onFailure: failCause4,
    onDone: () => void_3
  });
  const throttled = pipe(currentTimeMillis, map7((currentTimeMillis2) => loop2(units, currentTimeMillis2)), unwrap);
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(throttled)));
};
var throttleShapeEffect = (self, costFn, units, duration3, burst) => {
  const loop2 = (tokens, timestampMillis) => readWithCause({
    onInput: (input) => pipe(costFn(input), zip2(currentTimeMillis), map7(([weight, currentTimeMillis2]) => {
      const elapsed3 = currentTimeMillis2 - timestampMillis;
      const cycles = elapsed3 / toMillis(duration3);
      const sum2 = tokens + cycles * units;
      const max = units + burst < 0 ? Number.POSITIVE_INFINITY : units + burst;
      const available = sum2 < 0 ? max : Math.min(sum2, max);
      const remaining = available - weight;
      const waitCycles = remaining >= 0 ? 0 : -remaining / units;
      const delay = millis(Math.max(0, waitCycles * toMillis(duration3)));
      if (greaterThan(delay, zero)) {
        return pipe(fromEffect(sleep(delay)), zipRight5(write(input)), flatMap4(() => loop2(remaining, currentTimeMillis2)));
      }
      return flatMap4(write(input), () => loop2(remaining, currentTimeMillis2));
    }), unwrap),
    onFailure: failCause4,
    onDone: () => void_3
  });
  const throttled = pipe(currentTimeMillis, map7((currentTimeMillis2) => loop2(units, currentTimeMillis2)), unwrap);
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(throttled)));
};
var tick = (interval) => repeatWithSchedule(void 0, spaced2(interval));
var timeout = dual(2, (self, duration3) => pipe(toPull2(self), map7(timeoutFail({
  onTimeout: () => none(),
  duration: duration3
})), fromPull2));
var timeoutFail2 = dual(3, (self, error, duration3) => pipe(self, timeoutTo(duration3, failSync5(error))));
var timeoutFailCause2 = dual(3, (self, cause, duration3) => pipe(toPull2(self), map7(timeoutFailCause({
  onTimeout: () => map5(cause(), some),
  duration: duration3
})), fromPull2));
var timeoutTo = dual(3, (self, duration3, that) => {
  const StreamTimeout = new RuntimeException("Stream Timeout");
  return pipe(self, timeoutFailCause2(() => die2(StreamTimeout), duration3), catchSomeCause2((cause) => isDieType(cause) && isRuntimeException(cause.defect) && cause.defect.message !== void 0 && cause.defect.message === "Stream Timeout" ? some(that) : none()));
});
var pubsubFromOptions = (options) => {
  if (typeof options === "number") {
    return bounded5(options);
  } else if (options.capacity === "unbounded") {
    return unbounded5({
      replay: options.replay
    });
  }
  switch (options.strategy) {
    case "dropping":
      return dropping4(options);
    case "sliding":
      return sliding4(options);
    default:
      return bounded5(options);
  }
};
var toPubSub2 = dual(2, (self, capacity10) => pipe(acquireRelease2(pubsubFromOptions(capacity10), (pubsub) => shutdown4(pubsub)), tap2((pubsub) => pipe(self, runIntoPubSubScoped(pubsub), forkScoped))));
var toPull2 = (self) => map7(toPull(toChannel2(self)), (pull) => pipe(pull, mapError(some), flatMap3(match({
  onLeft: () => fail4(none()),
  onRight: succeed5
}))));
var toQueue2 = dual((args) => isStream(args[0]), (self, options) => tap2(acquireRelease2(options?.strategy === "unbounded" ? unbounded3() : options?.strategy === "dropping" ? dropping2(options.capacity ?? 2) : options?.strategy === "sliding" ? sliding2(options.capacity ?? 2) : bounded3(options?.capacity ?? 2), (queue) => shutdown2(queue)), (queue) => forkScoped(runIntoQueueScoped(self, queue))));
var toQueueOfElements = dual((args) => isStream(args[0]), (self, options) => tap2(acquireRelease2(bounded3(options?.capacity ?? 2), (queue) => shutdown2(queue)), (queue) => forkScoped(runIntoQueueElementsScoped(self, queue))));
var toReadableStream = dual((args) => isStream(args[0]), (self, options) => toReadableStreamRuntime(self, defaultRuntime2, options));
var toReadableStreamEffect = dual((args) => isStream(args[0]), (self, options) => map7(runtime(), (runtime2) => toReadableStreamRuntime(self, runtime2, options)));
var toReadableStreamRuntime = dual((args) => isStream(args[0]), (self, runtime2, options) => {
  const runFork3 = runFork2(runtime2);
  let currentResolve = void 0;
  let fiber = void 0;
  const latch = unsafeMakeLatch(false);
  return new ReadableStream({
    start(controller) {
      fiber = runFork3(runForEachChunk(self, (chunk2) => {
        if (chunk2.length === 0) return _void;
        return latch.whenOpen(sync3(() => {
          latch.unsafeClose();
          for (const item of chunk2) {
            controller.enqueue(item);
          }
          currentResolve();
          currentResolve = void 0;
        }));
      }));
      fiber.addObserver((exit2) => {
        try {
          if (exit2._tag === "Failure") {
            controller.error(squash(exit2.cause));
          } else {
            controller.close();
          }
        } catch {
        }
      });
    },
    pull() {
      return new Promise((resolve) => {
        currentResolve = resolve;
        runSync(latch.open);
      });
    },
    cancel() {
      if (!fiber) return;
      return runPromise(asVoid3(interrupt4(fiber)));
    }
  }, options?.strategy);
});
var transduce = dual(2, (self, sink) => {
  const newChannel = suspend3(() => {
    const leftovers = {
      ref: empty2()
    };
    const upstreamDone = {
      ref: false
    };
    const buffer4 = suspend3(() => {
      const leftover2 = leftovers.ref;
      if (isEmpty(leftover2)) {
        return readWith({
          onInput: (input) => pipe(write(input), flatMap4(() => buffer4)),
          onFailure: fail5,
          onDone: succeedNow
        });
      }
      leftovers.ref = empty2();
      return pipe(writeChunk(leftover2), flatMap4(() => buffer4));
    });
    const concatAndGet = (chunk2) => {
      const leftover2 = leftovers.ref;
      const concatenated = appendAll(leftover2, filter(chunk2, (chunk3) => chunk3.length !== 0));
      leftovers.ref = concatenated;
      return concatenated;
    };
    const upstreamMarker = readWith({
      onInput: (input) => flatMap4(write(input), () => upstreamMarker),
      onFailure: fail5,
      onDone: (done4) => zipRight5(sync4(() => {
        upstreamDone.ref = true;
      }), succeedNow(done4))
    });
    const transducer = pipe(sink, toChannel, collectElements, flatMap4(([leftover2, z]) => pipe(succeed6([upstreamDone.ref, concatAndGet(leftover2)]), flatMap4(([done4, newLeftovers]) => {
      const nextChannel = done4 && isEmpty(newLeftovers) ? void_3 : transducer;
      return pipe(write(of(z)), flatMap4(() => nextChannel));
    }))));
    return pipe(toChannel2(self), pipeTo(upstreamMarker), pipeTo(buffer4), pipeToOrFail(transducer));
  });
  return new StreamImpl(newChannel);
});
var toAsyncIterableRuntime = dual((args) => isStream(args[0]), (self, runtime2) => {
  const runFork3 = runFork2(runtime2);
  return {
    [Symbol.asyncIterator]() {
      let currentResolve = void 0;
      let currentReject = void 0;
      let fiber = void 0;
      const latch = unsafeMakeLatch(false);
      let returned = false;
      return {
        next() {
          if (!fiber) {
            fiber = runFork3(runForEach(self, (value) => latch.whenOpen(sync3(() => {
              latch.unsafeClose();
              currentResolve({
                done: false,
                value
              });
              currentResolve = currentReject = void 0;
            }))));
            fiber.addObserver((exit2) => {
              if (returned) return;
              fiber = runFork(latch.whenOpen(sync3(() => {
                if (exit2._tag === "Failure") {
                  currentReject(squash(exit2.cause));
                } else {
                  currentResolve({
                    done: true,
                    value: void 0
                  });
                }
                currentResolve = currentReject = void 0;
              })));
            });
          }
          return new Promise((resolve, reject2) => {
            currentResolve = resolve;
            currentReject = reject2;
            latch.unsafeOpen();
          });
        },
        return() {
          returned = true;
          if (!fiber) return Promise.resolve({
            done: true,
            value: void 0
          });
          return runPromise(as3(interrupt4(fiber), {
            done: true,
            value: void 0
          }));
        }
      };
    }
  };
});
var toAsyncIterable = (self) => toAsyncIterableRuntime(self, defaultRuntime2);
var toAsyncIterableEffect = (self) => map7(runtime(), (runtime2) => toAsyncIterableRuntime(self, runtime2));
var unfold3 = (s, f) => unfoldChunk(s, (s2) => pipe(f(s2), map(([a, s3]) => [of(a), s3])));
var unfoldChunk = (s, f) => {
  const loop2 = (s2) => match2(f(s2), {
    onNone: () => void_3,
    onSome: ([chunk2, s3]) => flatMap4(write(chunk2), () => loop2(s3))
  });
  return new StreamImpl(suspend3(() => loop2(s)));
};
var unfoldChunkEffect = (s, f) => suspend7(() => {
  const loop2 = (s2) => unwrap(map7(f(s2), match2({
    onNone: () => void_3,
    onSome: ([chunk2, s3]) => flatMap4(write(chunk2), () => loop2(s3))
  })));
  return new StreamImpl(loop2(s));
});
var unfoldEffect = (s, f) => unfoldChunkEffect(s, (s2) => pipe(f(s2), map7(map(([a, s3]) => [of(a), s3]))));
var void_6 = succeed11(void 0);
var unwrap3 = (effect3) => flatten5(fromEffect5(effect3));
var unwrapScoped3 = (effect3) => flatten5(scoped3(effect3));
var unwrapScopedWith3 = (f) => flatten5(scopedWith3((scope2) => f(scope2)));
var updateService2 = dual(3, (self, tag, f) => pipe(self, mapInputContext5((context8) => pipe(context8, add(tag, f(pipe(context8, unsafeGet2(tag))))))));
var when3 = dual(2, (self, test) => pipe(self, whenEffect2(sync3(test))));
var whenCase = (evaluate2, pf) => whenCaseEffect(pf)(sync3(evaluate2));
var whenCaseEffect = dual(2, (self, pf) => pipe(fromEffect5(self), flatMap7((a) => pipe(pf(a), getOrElse(() => empty5)))));
var whenEffect2 = dual(2, (self, effect3) => pipe(fromEffect5(effect3), flatMap7((bool) => bool ? self : empty5)));
var withSpan2 = function() {
  const dataFirst = typeof arguments[0] !== "string";
  const name = dataFirst ? arguments[1] : arguments[0];
  const options = addSpanStackTrace(dataFirst ? arguments[2] : arguments[1]);
  if (dataFirst) {
    const self = arguments[0];
    return new StreamImpl(withSpan(toChannel2(self), name, options));
  }
  return (self) => new StreamImpl(withSpan(toChannel2(self), name, options));
};
var zip6 = dual(2, (self, that) => pipe(self, zipWith7(that, (a, a2) => [a, a2])));
var zipFlatten = dual(2, (self, that) => pipe(self, zipWith7(that, (a, a2) => [...a, a2])));
var zipAll = dual(2, (self, options) => zipAllWith(self, {
  other: options.other,
  onSelf: (a) => [a, options.defaultOther],
  onOther: (a2) => [options.defaultSelf, a2],
  onBoth: (a, a2) => [a, a2]
}));
var zipAllLeft = dual(3, (self, other, defaultSelf) => zipAllWith(self, {
  other,
  onSelf: identity,
  onOther: () => defaultSelf,
  onBoth: (a) => a
}));
var zipAllRight = dual(3, (self, other, defaultRight) => zipAllWith(self, {
  other,
  onSelf: () => defaultRight,
  onOther: identity,
  onBoth: (_, a2) => a2
}));
var zipAllSortedByKey = dual(2, (self, options) => zipAllSortedByKeyWith(self, {
  other: options.other,
  onSelf: (a) => [a, options.defaultOther],
  onOther: (a2) => [options.defaultSelf, a2],
  onBoth: (a, a2) => [a, a2],
  order: options.order
}));
var zipAllSortedByKeyLeft = dual(2, (self, options) => zipAllSortedByKeyWith(self, {
  other: options.other,
  onSelf: identity,
  onOther: () => options.defaultSelf,
  onBoth: (a) => a,
  order: options.order
}));
var zipAllSortedByKeyRight = dual(2, (self, options) => zipAllSortedByKeyWith(self, {
  other: options.other,
  onSelf: () => options.defaultOther,
  onOther: identity,
  onBoth: (_, a2) => a2,
  order: options.order
}));
var zipAllSortedByKeyWith = dual(2, (self, options) => {
  const pull = (state, pullLeft, pullRight) => {
    switch (state._tag) {
      case OP_DRAIN_LEFT: {
        return pipe(pullLeft, match4({
          onFailure: fail,
          onSuccess: (leftChunk) => succeed2([map2(leftChunk, ([k, a]) => [k, options.onSelf(a)]), DrainLeft])
        }));
      }
      case OP_DRAIN_RIGHT: {
        return pipe(pullRight, match4({
          onFailure: fail,
          onSuccess: (rightChunk) => succeed2([map2(rightChunk, ([k, a2]) => [k, options.onOther(a2)]), DrainRight])
        }));
      }
      case OP_PULL_BOTH: {
        return pipe(unsome2(pullLeft), zip2(unsome2(pullRight), {
          concurrent: true
        }), matchEffect2({
          onFailure: (error) => succeed5(fail(some(error))),
          onSuccess: ([leftOption, rightOption]) => {
            if (isSome(leftOption) && isSome(rightOption)) {
              if (isEmpty(leftOption.value) && isEmpty(rightOption.value)) {
                return pull(PullBoth, pullLeft, pullRight);
              }
              if (isEmpty(leftOption.value)) {
                return pull(PullLeft(rightOption.value), pullLeft, pullRight);
              }
              if (isEmpty(rightOption.value)) {
                return pull(PullRight(leftOption.value), pullLeft, pullRight);
              }
              return succeed5(succeed2(merge7(leftOption.value, rightOption.value)));
            }
            if (isSome(leftOption) && isNone(rightOption)) {
              if (isEmpty(leftOption.value)) {
                return pull(DrainLeft, pullLeft, pullRight);
              }
              return succeed5(succeed2([pipe(leftOption.value, map2(([k, a]) => [k, options.onSelf(a)])), DrainLeft]));
            }
            if (isNone(leftOption) && isSome(rightOption)) {
              if (isEmpty(rightOption.value)) {
                return pull(DrainRight, pullLeft, pullRight);
              }
              return succeed5(succeed2([pipe(rightOption.value, map2(([k, a2]) => [k, options.onOther(a2)])), DrainRight]));
            }
            return succeed5(fail(none()));
          }
        }));
      }
      case OP_PULL_LEFT: {
        return matchEffect2(pullLeft, {
          onFailure: match2({
            onNone: () => succeed5(succeed2([pipe(state.rightChunk, map2(([k, a2]) => [k, options.onOther(a2)])), DrainRight])),
            onSome: (error) => succeed5(fail(some(error)))
          }),
          onSuccess: (leftChunk) => isEmpty(leftChunk) ? pull(PullLeft(state.rightChunk), pullLeft, pullRight) : succeed5(succeed2(merge7(leftChunk, state.rightChunk)))
        });
      }
      case OP_PULL_RIGHT: {
        return matchEffect2(pullRight, {
          onFailure: match2({
            onNone: () => succeed5(succeed2([map2(state.leftChunk, ([k, a]) => [k, options.onSelf(a)]), DrainLeft])),
            onSome: (error) => succeed5(fail(some(error)))
          }),
          onSuccess: (rightChunk) => isEmpty(rightChunk) ? pull(PullRight(state.leftChunk), pullLeft, pullRight) : succeed5(succeed2(merge7(state.leftChunk, rightChunk)))
        });
      }
    }
  };
  const merge7 = (leftChunk, rightChunk) => {
    const hasNext = (chunk2, index) => index < chunk2.length - 1;
    const builder = [];
    let state = void 0;
    let leftIndex = 0;
    let rightIndex = 0;
    let leftTuple = pipe(leftChunk, unsafeGet(leftIndex));
    let rightTuple = pipe(rightChunk, unsafeGet(rightIndex));
    let k1 = leftTuple[0];
    let a = leftTuple[1];
    let k2 = rightTuple[0];
    let a2 = rightTuple[1];
    let loop2 = true;
    while (loop2) {
      const compare = options.order(k1, k2);
      if (compare === 0) {
        builder.push([k1, options.onBoth(a, a2)]);
        if (hasNext(leftChunk, leftIndex) && hasNext(rightChunk, rightIndex)) {
          leftIndex = leftIndex + 1;
          rightIndex = rightIndex + 1;
          leftTuple = pipe(leftChunk, unsafeGet(leftIndex));
          rightTuple = pipe(rightChunk, unsafeGet(rightIndex));
          k1 = leftTuple[0];
          a = leftTuple[1];
          k2 = rightTuple[0];
          a2 = rightTuple[1];
        } else if (hasNext(leftChunk, leftIndex)) {
          state = PullRight(pipe(leftChunk, drop(leftIndex + 1)));
          loop2 = false;
        } else if (hasNext(rightChunk, rightIndex)) {
          state = PullLeft(pipe(rightChunk, drop(rightIndex + 1)));
          loop2 = false;
        } else {
          state = PullBoth;
          loop2 = false;
        }
      } else if (compare < 0) {
        builder.push([k1, options.onSelf(a)]);
        if (hasNext(leftChunk, leftIndex)) {
          leftIndex = leftIndex + 1;
          leftTuple = pipe(leftChunk, unsafeGet(leftIndex));
          k1 = leftTuple[0];
          a = leftTuple[1];
        } else {
          const rightBuilder = [];
          rightBuilder.push(rightTuple);
          while (hasNext(rightChunk, rightIndex)) {
            rightIndex = rightIndex + 1;
            rightTuple = pipe(rightChunk, unsafeGet(rightIndex));
            rightBuilder.push(rightTuple);
          }
          state = PullLeft(unsafeFromArray(rightBuilder));
          loop2 = false;
        }
      } else {
        builder.push([k2, options.onOther(a2)]);
        if (hasNext(rightChunk, rightIndex)) {
          rightIndex = rightIndex + 1;
          rightTuple = pipe(rightChunk, unsafeGet(rightIndex));
          k2 = rightTuple[0];
          a2 = rightTuple[1];
        } else {
          const leftBuilder = [];
          leftBuilder.push(leftTuple);
          while (hasNext(leftChunk, leftIndex)) {
            leftIndex = leftIndex + 1;
            leftTuple = pipe(leftChunk, unsafeGet(leftIndex));
            leftBuilder.push(leftTuple);
          }
          state = PullRight(unsafeFromArray(leftBuilder));
          loop2 = false;
        }
      }
    }
    return [unsafeFromArray(builder), state];
  };
  return combineChunks(self, options.other, PullBoth, pull);
});
var zipAllWith = dual(2, (self, options) => {
  const pull = (state, pullLeft, pullRight) => {
    switch (state._tag) {
      case OP_DRAIN_LEFT: {
        return matchEffect2(pullLeft, {
          onFailure: (error) => succeed5(fail(error)),
          onSuccess: (leftChunk) => succeed5(succeed2([map2(leftChunk, options.onSelf), DrainLeft]))
        });
      }
      case OP_DRAIN_RIGHT: {
        return matchEffect2(pullRight, {
          onFailure: (error) => succeed5(fail(error)),
          onSuccess: (rightChunk) => succeed5(succeed2([map2(rightChunk, options.onOther), DrainRight]))
        });
      }
      case OP_PULL_BOTH: {
        return pipe(unsome2(pullLeft), zip2(unsome2(pullRight), {
          concurrent: true
        }), matchEffect2({
          onFailure: (error) => succeed5(fail(some(error))),
          onSuccess: ([leftOption, rightOption]) => {
            if (isSome(leftOption) && isSome(rightOption)) {
              if (isEmpty(leftOption.value) && isEmpty(rightOption.value)) {
                return pull(PullBoth, pullLeft, pullRight);
              }
              if (isEmpty(leftOption.value)) {
                return pull(PullLeft(rightOption.value), pullLeft, pullRight);
              }
              if (isEmpty(rightOption.value)) {
                return pull(PullRight(leftOption.value), pullLeft, pullRight);
              }
              return succeed5(succeed2(zip8(leftOption.value, rightOption.value, options.onBoth)));
            }
            if (isSome(leftOption) && isNone(rightOption)) {
              return succeed5(succeed2([map2(leftOption.value, options.onSelf), DrainLeft]));
            }
            if (isNone(leftOption) && isSome(rightOption)) {
              return succeed5(succeed2([map2(rightOption.value, options.onOther), DrainRight]));
            }
            return succeed5(fail(none()));
          }
        }));
      }
      case OP_PULL_LEFT: {
        return matchEffect2(pullLeft, {
          onFailure: match2({
            onNone: () => succeed5(succeed2([map2(state.rightChunk, options.onOther), DrainRight])),
            onSome: (error) => succeed5(fail(some(error)))
          }),
          onSuccess: (leftChunk) => {
            if (isEmpty(leftChunk)) {
              return pull(PullLeft(state.rightChunk), pullLeft, pullRight);
            }
            if (isEmpty(state.rightChunk)) {
              return pull(PullRight(leftChunk), pullLeft, pullRight);
            }
            return succeed5(succeed2(zip8(leftChunk, state.rightChunk, options.onBoth)));
          }
        });
      }
      case OP_PULL_RIGHT: {
        return matchEffect2(pullRight, {
          onFailure: match2({
            onNone: () => succeed5(succeed2([map2(state.leftChunk, options.onSelf), DrainLeft])),
            onSome: (error) => succeed5(fail(some(error)))
          }),
          onSuccess: (rightChunk) => {
            if (isEmpty(rightChunk)) {
              return pull(PullRight(state.leftChunk), pullLeft, pullRight);
            }
            if (isEmpty(state.leftChunk)) {
              return pull(PullLeft(rightChunk), pullLeft, pullRight);
            }
            return succeed5(succeed2(zip8(state.leftChunk, rightChunk, options.onBoth)));
          }
        });
      }
    }
  };
  const zip8 = (leftChunk, rightChunk, f) => {
    const [output, either6] = zipChunks(leftChunk, rightChunk, f);
    switch (either6._tag) {
      case "Left": {
        if (isEmpty(either6.left)) {
          return [output, PullBoth];
        }
        return [output, PullRight(either6.left)];
      }
      case "Right": {
        if (isEmpty(either6.right)) {
          return [output, PullBoth];
        }
        return [output, PullLeft(either6.right)];
      }
    }
  };
  return combineChunks(self, options.other, PullBoth, pull);
});
var zipLatest = dual(2, (left2, right2) => pipe(left2, zipLatestWith(right2, (a, a2) => [a, a2])));
var zipLatestAll = (...streams) => {
  if (streams.length === 0) {
    return empty5;
  } else if (streams.length === 1) {
    return map13(streams[0], (x) => [x]);
  }
  const [head5, ...tail] = streams;
  return zipLatestWith(head5, zipLatestAll(...tail), (first2, second) => [first2, ...second]);
};
var zipLatestWith = dual(3, (left2, right2, f) => {
  const pullNonEmpty = (pull) => pipe(pull, flatMap3((chunk2) => isEmpty(chunk2) ? pullNonEmpty(pull) : succeed5(chunk2)));
  return pipe(toPull2(left2), map7(pullNonEmpty), zip2(pipe(toPull2(right2), map7(pullNonEmpty))), flatMap3(([left3, right3]) => pipe(fromEffectOption(raceWith(left3, right3, {
    onSelfDone: (leftDone, rightFiber) => pipe(suspend2(() => leftDone), zipWith3(join(rightFiber), (l, r) => [l, r, true])),
    onOtherDone: (rightDone, leftFiber) => pipe(suspend2(() => rightDone), zipWith3(join(leftFiber), (l, r) => [r, l, false]))
  })), flatMap7(([l, r, leftFirst]) => pipe(fromEffect5(make5([unsafeLast(l), unsafeLast(r)])), flatMap7((latest) => pipe(fromChunk(leftFirst ? pipe(r, map2((a2) => f(unsafeLast(l), a2))) : pipe(l, map2((a) => f(a, unsafeLast(r))))), concat(pipe(repeatEffectOption(left3), mergeEither(repeatEffectOption(right3)), mapEffectSequential(match({
    onLeft: (leftChunk) => modify(latest, ([_, rightLatest]) => [pipe(leftChunk, map2((a) => f(a, rightLatest))), [unsafeLast(leftChunk), rightLatest]]),
    onRight: (rightChunk) => modify(latest, ([leftLatest, _]) => [pipe(rightChunk, map2((a2) => f(leftLatest, a2))), [leftLatest, unsafeLast(rightChunk)]])
  })), flatMap7(fromChunk))))))), toPull2)), fromPull2);
});
var zipLeft7 = dual(2, (left2, right2) => pipe(left2, zipWithChunks(right2, (left3, right3) => {
  if (left3.length > right3.length) {
    return [pipe(left3, take(right3.length)), left(pipe(left3, take(right3.length)))];
  }
  return [left3, right(pipe(right3, drop(left3.length)))];
})));
var zipRight9 = dual(2, (left2, right2) => pipe(left2, zipWithChunks(right2, (left3, right3) => {
  if (left3.length > right3.length) {
    return [right3, left(pipe(left3, take(right3.length)))];
  }
  return [pipe(right3, take(left3.length)), right(pipe(right3, drop(left3.length)))];
})));
var zipWith7 = dual(3, (left2, right2, f) => pipe(left2, zipWithChunks(right2, (leftChunk, rightChunk) => zipChunks(leftChunk, rightChunk, f))));
var zipWithChunks = dual(3, (self, that, f) => {
  const pull = (state, pullLeft, pullRight) => {
    switch (state._tag) {
      case OP_PULL_BOTH2: {
        return pipe(unsome2(pullLeft), zip2(unsome2(pullRight), {
          concurrent: true
        }), matchEffect2({
          onFailure: (error) => succeed5(fail(some(error))),
          onSuccess: ([leftOption, rightOption]) => {
            if (isSome(leftOption) && isSome(rightOption)) {
              if (isEmpty(leftOption.value) && isEmpty(rightOption.value)) {
                return pull(PullBoth2, pullLeft, pullRight);
              }
              if (isEmpty(leftOption.value)) {
                return pull(PullLeft2(rightOption.value), pullLeft, pullRight);
              }
              if (isEmpty(rightOption.value)) {
                return pull(PullRight2(leftOption.value), pullLeft, pullRight);
              }
              return succeed5(succeed2(zip8(leftOption.value, rightOption.value)));
            }
            return succeed5(fail(none()));
          }
        }));
      }
      case OP_PULL_LEFT2: {
        return matchEffect2(pullLeft, {
          onFailure: (error) => succeed5(fail(error)),
          onSuccess: (leftChunk) => {
            if (isEmpty(leftChunk)) {
              return pull(PullLeft2(state.rightChunk), pullLeft, pullRight);
            }
            if (isEmpty(state.rightChunk)) {
              return pull(PullRight2(leftChunk), pullLeft, pullRight);
            }
            return succeed5(succeed2(zip8(leftChunk, state.rightChunk)));
          }
        });
      }
      case OP_PULL_RIGHT2: {
        return matchEffect2(pullRight, {
          onFailure: (error) => succeed5(fail(error)),
          onSuccess: (rightChunk) => {
            if (isEmpty(rightChunk)) {
              return pull(PullRight2(state.leftChunk), pullLeft, pullRight);
            }
            if (isEmpty(state.leftChunk)) {
              return pull(PullLeft2(rightChunk), pullLeft, pullRight);
            }
            return succeed5(succeed2(zip8(state.leftChunk, rightChunk)));
          }
        });
      }
    }
  };
  const zip8 = (leftChunk, rightChunk) => {
    const [output, either6] = f(leftChunk, rightChunk);
    switch (either6._tag) {
      case "Left": {
        if (isEmpty(either6.left)) {
          return [output, PullBoth2];
        }
        return [output, PullRight2(either6.left)];
      }
      case "Right": {
        if (isEmpty(either6.right)) {
          return [output, PullBoth2];
        }
        return [output, PullLeft2(either6.right)];
      }
    }
  };
  return pipe(self, combineChunks(that, PullBoth2, pull));
});
var zipWithIndex = (self) => pipe(self, mapAccum2(0, (index, a) => [index + 1, [a, index]]));
var zipWithNext = (self) => {
  const process = (last3) => readWithCause({
    onInput: (input) => {
      const [newLast, chunk2] = mapAccum(input, last3, (prev, curr) => [some(curr), pipe(prev, map((a) => [a, curr]))]);
      const output = filterMap(chunk2, (option2) => isSome(option2) ? some([option2.value[0], some(option2.value[1])]) : none());
      return flatMap4(write(output), () => process(newLast));
    },
    onFailure: failCause4,
    onDone: () => match2(last3, {
      onNone: () => void_3,
      onSome: (value) => zipRight5(write(of([value, none()])), void_3)
    })
  });
  return new StreamImpl(pipe(toChannel2(self), pipeToOrFail(process(none()))));
};
var zipWithPrevious = (self) => pipe(self, mapAccum2(none(), (prev, curr) => [some(curr), [prev, curr]]));
var zipWithPreviousAndNext = (self) => pipe(zipWithNext(zipWithPrevious(self)), map13(([[prev, curr], next]) => [prev, curr, pipe(next, map((tuple) => tuple[1]))]));
var zipChunks = (left2, right2, f) => {
  if (left2.length > right2.length) {
    return [pipe(left2, take(right2.length), zipWith(right2, f)), left(pipe(left2, drop(right2.length)))];
  }
  return [pipe(left2, zipWith(pipe(right2, take(left2.length)), f)), right(pipe(right2, drop(left2.length)))];
};
var Do = succeed11({});
var bind3 = dual((args) => typeof args[0] !== "string", (self, tag, f, options) => flatMap7(self, (k) => map13(f(k), (a) => ({
  ...k,
  [tag]: a
})), options));
var bindTo4 = bindTo(map13);
var let_3 = let_(map13);
var channelToStream = (self) => {
  return new StreamImpl(self);
};
var decodeText = dual((args) => isStream(args[0]), (self, encoding = "utf-8") => suspend7(() => {
  const decoder = new TextDecoder(encoding);
  return map13(self, (s) => decoder.decode(s, {
    stream: true
  }));
}));
var encodeText = (self) => suspend7(() => {
  const encoder = new TextEncoder();
  return map13(self, (s) => encoder.encode(s));
});
var fromEventListener = (target, type, options) => asyncPush((emit2) => acquireRelease2(sync3(() => target.addEventListener(type, emit2.single, options)), () => sync3(() => target.removeEventListener(type, emit2.single, options))), {
  bufferSize: typeof options === "object" ? options.bufferSize : void 0
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/groupBy.js
var GroupBySymbolKey = "effect/GroupBy";
var GroupByTypeId = Symbol.for(GroupBySymbolKey);
var groupByVariance = {
  /* c8 ignore next */
  _R: (_) => _,
  /* c8 ignore next */
  _E: (_) => _,
  /* c8 ignore next */
  _K: (_) => _,
  /* c8 ignore next */
  _V: (_) => _
};
var isGroupBy = (u) => hasProperty(u, GroupByTypeId);
var evaluate = dual((args) => isGroupBy(args[0]), (self, f, options) => flatMap7(self.grouped, ([key, queue]) => f(key, flattenTake(fromQueue3(queue, {
  shutdown: true
}))), {
  concurrency: "unbounded",
  bufferSize: options?.bufferSize ?? 16
}));
var filter5 = dual(2, (self, predicate) => make23(pipe(self.grouped, filterEffect((tuple) => {
  if (predicate(tuple[0])) {
    return pipe(succeed5(tuple), as3(true));
  }
  return pipe(shutdown2(tuple[1]), as3(false));
}))));
var first = dual(2, (self, n) => make23(pipe(zipWithIndex(self.grouped), filterEffect((tuple) => {
  const index = tuple[1];
  const queue = tuple[0][1];
  if (index < n) {
    return pipe(succeed5(tuple), as3(true));
  }
  return pipe(shutdown2(queue), as3(false));
}), map13((tuple) => tuple[0]))));
var make23 = (grouped3) => ({
  [GroupByTypeId]: groupByVariance,
  pipe() {
    return pipeArguments(this, arguments);
  },
  grouped: grouped3
});
var groupBy = dual((args) => isStream(args[0]), (self, f, options) => make23(unwrapScoped3(gen(function* () {
  const decider = yield* make8();
  const output = yield* acquireRelease2(bounded3(options?.bufferSize ?? 16), (queue) => shutdown2(queue));
  const ref = yield* make5(/* @__PURE__ */ new Map());
  const add3 = yield* pipe(mapEffectSequential(self, f), distributedWithDynamicCallback(options?.bufferSize ?? 16, ([key, value]) => flatMap3(_await2(decider), (f2) => f2(key, value)), (exit2) => offer3(output, exit2)));
  yield* succeed4(decider, (key, _) => pipe(get3(ref), map7((map15) => fromNullable(map15.get(key))), flatMap3(match2({
    onNone: () => flatMap3(add3, ([index, queue]) => zipRight4(update(ref, (map15) => map15.set(key, index)), pipe(offer3(output, succeed2([key, mapDequeue(queue, (exit2) => new TakeImpl(pipe(exit2, map4((tuple) => of(tuple[1])))))])), as3((n) => n === index)))),
    onSome: (index) => succeed5((n) => n === index)
  }))));
  return flattenExitOption(fromQueue3(output, {
    shutdown: true
  }));
}))));
var mapEffectOptions = dual((args) => typeof args[0] !== "function", (self, f, options) => {
  if (options?.key) {
    return evaluate(groupByKey(self, options.key, {
      bufferSize: options.bufferSize
    }), (_, s) => mapEffectSequential(s, f));
  }
  return matchConcurrency(options?.concurrency, () => mapEffectSequential(self, f), (n) => options?.unordered ? flatMap7(self, (a) => fromEffect5(f(a)), {
    concurrency: n
  }) : mapEffectPar(self, n, f));
});
var bindEffect = dual((args) => typeof args[0] !== "string", (self, tag, f, options) => mapEffectOptions(self, (k) => map7(f(k), (a) => ({
  ...k,
  [tag]: a
})), options));
var mapDequeue = (dequeue, f) => new MapDequeue(dequeue, f);
var MapDequeue = class extends Class {
  dequeue;
  f;
  [DequeueTypeId2] = {
    _Out: (_) => _
  };
  constructor(dequeue, f) {
    super();
    this.dequeue = dequeue;
    this.f = f;
  }
  capacity() {
    return capacity3(this.dequeue);
  }
  get size() {
    return size3(this.dequeue);
  }
  unsafeSize() {
    return this.dequeue.unsafeSize();
  }
  get awaitShutdown() {
    return awaitShutdown2(this.dequeue);
  }
  isActive() {
    return this.dequeue.isActive();
  }
  get isShutdown() {
    return isShutdown2(this.dequeue);
  }
  get shutdown() {
    return shutdown2(this.dequeue);
  }
  get isFull() {
    return isFull2(this.dequeue);
  }
  get isEmpty() {
    return isEmpty4(this.dequeue);
  }
  get take() {
    return pipe(take3(this.dequeue), map7((a) => this.f(a)));
  }
  get takeAll() {
    return pipe(takeAll2(this.dequeue), map7(map2((a) => this.f(a))));
  }
  takeUpTo(max) {
    return pipe(takeUpTo2(this.dequeue, max), map7(map2((a) => this.f(a))));
  }
  takeBetween(min, max) {
    return pipe(takeBetween2(this.dequeue, min, max), map7(map2((a) => this.f(a))));
  }
  takeN(n) {
    return pipe(takeN2(this.dequeue, n), map7(map2((a) => this.f(a))));
  }
  poll() {
    return pipe(poll5(this.dequeue), map7(map((a) => this.f(a))));
  }
  pipe() {
    return pipeArguments(this, arguments);
  }
  commit() {
    return this.take;
  }
};
var groupByKey = dual((args) => typeof args[0] !== "function", (self, f, options) => {
  const loop2 = (map15, outerQueue) => readWithCause({
    onInput: (input) => flatMap4(fromEffect(forEach2(groupByIterable(input, f), ([key, values]) => {
      const innerQueue = map15.get(key);
      if (innerQueue === void 0) {
        return pipe(bounded3(options?.bufferSize ?? 16), flatMap3((innerQueue2) => pipe(sync3(() => {
          map15.set(key, innerQueue2);
        }), zipRight4(offer3(outerQueue, of2([key, innerQueue2]))), zipRight4(pipe(offer3(innerQueue2, chunk(values)), catchSomeCause((cause) => isInterruptedOnly(cause) ? some(_void) : none()))))));
      }
      return catchSomeCause(offer3(innerQueue, chunk(values)), (cause) => isInterruptedOnly(cause) ? some(_void) : none());
    }, {
      discard: true
    })), () => loop2(map15, outerQueue)),
    onFailure: (cause) => fromEffect(offer3(outerQueue, failCause6(cause))),
    onDone: () => fromEffect(pipe(forEach2(map15.entries(), ([_, innerQueue]) => pipe(offer3(innerQueue, end2), catchSomeCause((cause) => isInterruptedOnly(cause) ? some(_void) : none())), {
      discard: true
    }), zipRight4(offer3(outerQueue, end2))))
  });
  return make23(unwrapScopedWith3((scope2) => gen(function* () {
    const map15 = /* @__PURE__ */ new Map();
    const queue = yield* unbounded3();
    yield* addFinalizer(scope2, shutdown2(queue));
    return yield* toChannel2(self).pipe(pipeTo(loop2(map15, queue)), drain, runIn(scope2), forkIn2(scope2), as3(flattenTake(fromQueue3(queue, {
      shutdown: true
    }))));
  })));
});
var groupByIterable = dual(2, (iterable, f) => {
  const builder = [];
  const iterator = iterable[Symbol.iterator]();
  const map15 = /* @__PURE__ */ new Map();
  let next;
  while ((next = iterator.next()) && !next.done) {
    const value = next.value;
    const key = f(value);
    if (map15.has(key)) {
      const innerBuilder = map15.get(key);
      innerBuilder.push(value);
    } else {
      const innerBuilder = [value];
      builder.push([key, innerBuilder]);
      map15.set(key, innerBuilder);
    }
  }
  return unsafeFromArray(builder.map((tuple) => [tuple[0], unsafeFromArray(tuple[1])]));
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Stream.js
var StreamTypeId3 = StreamTypeId2;
var DefaultChunkSize2 = DefaultChunkSize;
var accumulate2 = accumulate;
var accumulateChunks2 = accumulateChunks;
var acquireRelease4 = acquireRelease3;
var aggregate2 = aggregate;
var aggregateWithin2 = aggregateWithin;
var aggregateWithinEither2 = aggregateWithinEither;
var as9 = as8;
var _async2 = _async;
var asyncEffect2 = asyncEffect;
var asyncPush2 = asyncPush;
var asyncScoped2 = asyncScoped;
var branchAfter2 = branchAfter;
var broadcast2 = broadcast;
var share2 = share;
var broadcastDynamic2 = broadcastDynamic;
var broadcastedQueues2 = broadcastedQueues;
var broadcastedQueuesDynamic2 = broadcastedQueuesDynamic;
var buffer3 = buffer2;
var bufferChunks2 = bufferChunks;
var catchAll6 = catchAll5;
var catchAllCause4 = catchAllCause3;
var catchSome3 = catchSome2;
var catchTag3 = catchTag2;
var catchTags3 = catchTags2;
var catchSomeCause3 = catchSomeCause2;
var changes2 = changes;
var changesWith2 = changesWith;
var changesWithEffect2 = changesWithEffect;
var chunks2 = chunks;
var chunksWith2 = chunksWith;
var combine3 = combine2;
var combineChunks2 = combineChunks;
var concat2 = concat;
var concatAll3 = concatAll2;
var cross2 = cross;
var crossLeft2 = crossLeft;
var crossRight2 = crossRight;
var crossWith2 = crossWith;
var debounce2 = debounce;
var die9 = die8;
var dieSync5 = dieSync4;
var dieMessage6 = dieMessage5;
var distributedWith2 = distributedWith;
var distributedWithDynamic2 = distributedWithDynamic;
var drain4 = drain3;
var drainFork2 = drainFork;
var drop4 = drop3;
var dropRight2 = dropRight;
var dropUntil4 = dropUntil3;
var dropUntilEffect3 = dropUntilEffect2;
var dropWhile5 = dropWhile4;
var dropWhileEffect3 = dropWhileEffect2;
var either5 = either4;
var empty6 = empty5;
var ensuring9 = ensuring8;
var ensuringWith4 = ensuringWith3;
var context7 = context6;
var contextWith6 = contextWith5;
var contextWithEffect4 = contextWithEffect3;
var contextWithStream2 = contextWithStream;
var execute2 = execute;
var fail11 = fail10;
var failSync6 = failSync5;
var failCause9 = failCause8;
var failCauseSync5 = failCauseSync4;
var filter6 = filter4;
var filterEffect2 = filterEffect;
var filterMap3 = filterMap2;
var filterMapEffect2 = filterMapEffect;
var filterMapWhile3 = filterMapWhile2;
var filterMapWhileEffect2 = filterMapWhileEffect;
var finalizer2 = finalizer;
var find2 = find;
var findEffect3 = findEffect2;
var flatMap8 = flatMap7;
var flatten6 = flatten5;
var flattenChunks2 = flattenChunks;
var flattenEffect2 = flattenEffect;
var flattenExitOption2 = flattenExitOption;
var flattenIterables2 = flattenIterables;
var flattenTake2 = flattenTake;
var forever5 = forever4;
var fromAsyncIterable2 = fromAsyncIterable;
var fromChannel3 = fromChannel2;
var toChannel3 = toChannel2;
var fromChunk2 = fromChunk;
var fromChunkPubSub2 = fromChunkPubSub;
var fromChunkQueue2 = fromChunkQueue;
var fromChunks2 = fromChunks;
var fromEffect6 = fromEffect5;
var fromEffectOption2 = fromEffectOption;
var fromPubSub4 = fromPubSub3;
var fromTPubSub2 = fromTPubSub;
var fromIterable4 = fromIterable3;
var fromIterableEffect2 = fromIterableEffect;
var fromIteratorSucceed2 = fromIteratorSucceed;
var fromPull3 = fromPull2;
var fromQueue4 = fromQueue3;
var fromTQueue2 = fromTQueue;
var fromReadableStream2 = fromReadableStream;
var fromReadableStreamByob2 = fromReadableStreamByob;
var fromSchedule2 = fromSchedule;
var groupAdjacentBy2 = groupAdjacentBy;
var groupBy2 = groupBy;
var groupByKey2 = groupByKey;
var grouped2 = grouped;
var groupedWithin2 = groupedWithin;
var haltAfter2 = haltAfter;
var haltWhen2 = haltWhen;
var haltWhenDeferred2 = haltWhenDeferred;
var identity4 = identityStream;
var interleave2 = interleave;
var interleaveWith2 = interleaveWith;
var intersperse2 = intersperse;
var intersperseAffixes2 = intersperseAffixes;
var interruptAfter2 = interruptAfter;
var interruptWhen3 = interruptWhen2;
var interruptWhenDeferred3 = interruptWhenDeferred2;
var iterate3 = iterate2;
var make24 = make22;
var map14 = map13;
var mapAccum3 = mapAccum2;
var mapAccumEffect2 = mapAccumEffect;
var mapBoth7 = mapBoth6;
var mapChunks2 = mapChunks;
var mapChunksEffect2 = mapChunksEffect;
var mapConcat2 = mapConcat;
var mapConcatChunk2 = mapConcatChunk;
var mapConcatChunkEffect2 = mapConcatChunkEffect;
var mapConcatEffect2 = mapConcatEffect;
var mapEffect5 = mapEffectOptions;
var mapError6 = mapError5;
var mapErrorCause3 = mapErrorCause2;
var merge6 = merge5;
var mergeAll4 = mergeAll3;
var mergeWithTag2 = mergeWithTag;
var mergeWith3 = mergeWith2;
var mergeEither2 = mergeEither;
var mergeLeft2 = mergeLeft;
var mergeRight2 = mergeRight;
var mkString3 = mkString2;
var never5 = never4;
var onEnd2 = onEnd;
var onError3 = onError2;
var onDone2 = onDone;
var onStart2 = onStart;
var orDie5 = orDie4;
var orDieWith4 = orDieWith3;
var orElse7 = orElse6;
var orElseEither3 = orElseEither2;
var orElseFail3 = orElseFail2;
var orElseIfEmpty2 = orElseIfEmpty;
var orElseIfEmptyChunk2 = orElseIfEmptyChunk;
var orElseIfEmptyStream2 = orElseIfEmptyStream;
var orElseSucceed3 = orElseSucceed2;
var paginate2 = paginate;
var paginateChunk2 = paginateChunk;
var paginateChunkEffect2 = paginateChunkEffect;
var paginateEffect2 = paginateEffect;
var partition3 = partition2;
var partitionEither2 = partitionEither;
var peel2 = peel;
var pipeThrough2 = pipeThrough;
var pipeThroughChannel2 = pipeThroughChannel;
var pipeThroughChannelOrFail2 = pipeThroughChannelOrFail;
var prepend3 = prepend2;
var provideContext7 = provideContext6;
var provideSomeContext3 = provideSomeContext2;
var provideLayer3 = provideLayer2;
var provideService8 = provideService7;
var provideServiceEffect3 = provideServiceEffect2;
var provideServiceStream2 = provideServiceStream;
var mapInputContext6 = mapInputContext5;
var provideSomeLayer3 = provideSomeLayer2;
var race4 = race3;
var raceAll2 = raceAll;
var range3 = range2;
var rechunk2 = rechunk;
var refineOrDie4 = refineOrDie3;
var refineOrDieWith4 = refineOrDieWith3;
var repeat3 = repeat2;
var repeatEffect2 = repeatEffect;
var repeatEffectChunk2 = repeatEffectChunk;
var repeatEffectChunkOption2 = repeatEffectChunkOption;
var repeatEffectOption2 = repeatEffectOption;
var repeatEffectWithSchedule2 = repeatEffectWithSchedule;
var repeatEither2 = repeatEither;
var repeatElements2 = repeatElements;
var repeatElementsWith2 = repeatElementsWith;
var repeatValue2 = repeatValue;
var repeatWith2 = repeatWith;
var retry4 = retry3;
var withExecutionPlan2 = withExecutionPlan;
var run5 = run4;
var runCollect3 = runCollect2;
var runCount2 = runCount;
var runDrain3 = runDrain2;
var runFold2 = runFold;
var runFoldEffect2 = runFoldEffect;
var runFoldScoped2 = runFoldScoped;
var runFoldScopedEffect2 = runFoldScopedEffect;
var runFoldWhile2 = runFoldWhile;
var runFoldWhileEffect2 = runFoldWhileEffect;
var runFoldWhileScoped2 = runFoldWhileScoped;
var runFoldWhileScopedEffect2 = runFoldWhileScopedEffect;
var runForEach2 = runForEach;
var runForEachChunk2 = runForEachChunk;
var runForEachChunkScoped2 = runForEachChunkScoped;
var runForEachScoped2 = runForEachScoped;
var runForEachWhile2 = runForEachWhile;
var runForEachWhileScoped2 = runForEachWhileScoped;
var runHead2 = runHead;
var runIntoPubSub2 = runIntoPubSub;
var runIntoPubSubScoped2 = runIntoPubSubScoped;
var runIntoQueue2 = runIntoQueue;
var runIntoQueueElementsScoped2 = runIntoQueueElementsScoped;
var runIntoQueueScoped2 = runIntoQueueScoped;
var runLast2 = runLast;
var runScoped3 = runScoped2;
var runSum2 = runSum;
var scan2 = scan;
var scanEffect2 = scanEffect;
var scanReduce2 = scanReduce;
var scanReduceEffect2 = scanReduceEffect;
var schedule2 = schedule;
var scheduleWith2 = scheduleWith;
var scoped4 = scoped3;
var scopedWith4 = scopedWith3;
var sliding10 = sliding9;
var slidingSize2 = slidingSize;
var some5 = some4;
var someOrElse2 = someOrElse;
var someOrFail2 = someOrFail;
var split2 = split;
var splitOnChunk2 = splitOnChunk;
var splitLines3 = splitLines2;
var succeed12 = succeed11;
var sync9 = sync8;
var suspend8 = suspend7;
var take9 = take8;
var takeRight3 = takeRight2;
var takeUntil2 = takeUntil;
var takeUntilEffect2 = takeUntilEffect;
var takeWhile4 = takeWhile3;
var tap6 = tap5;
var tapBoth3 = tapBoth2;
var tapError3 = tapError2;
var tapErrorCause3 = tapErrorCause2;
var tapSink2 = tapSink;
var throttle2 = throttle;
var throttleEffect2 = throttleEffect;
var tick2 = tick;
var timeout2 = timeout;
var timeoutFail3 = timeoutFail2;
var timeoutFailCause3 = timeoutFailCause2;
var timeoutTo2 = timeoutTo;
var toPubSub3 = toPubSub2;
var toPull3 = toPull2;
var toQueue3 = toQueue2;
var toQueueOfElements2 = toQueueOfElements;
var toReadableStream2 = toReadableStream;
var toReadableStreamEffect2 = toReadableStreamEffect;
var toReadableStreamRuntime2 = toReadableStreamRuntime;
var toAsyncIterableRuntime2 = toAsyncIterableRuntime;
var toAsyncIterableEffect2 = toAsyncIterableEffect;
var toAsyncIterable2 = toAsyncIterable;
var transduce2 = transduce;
var unfold4 = unfold3;
var unfoldChunk2 = unfoldChunk;
var unfoldChunkEffect2 = unfoldChunkEffect;
var unfoldEffect2 = unfoldEffect;
var void_7 = void_6;
var unwrap4 = unwrap3;
var unwrapScoped4 = unwrapScoped3;
var unwrapScopedWith4 = unwrapScopedWith3;
var updateService3 = updateService2;
var when4 = when3;
var whenCase2 = whenCase;
var whenCaseEffect2 = whenCaseEffect;
var whenEffect3 = whenEffect2;
var withSpan3 = withSpan2;
var zip7 = zip6;
var zipFlatten2 = zipFlatten;
var zipAll2 = zipAll;
var zipAllLeft2 = zipAllLeft;
var zipAllRight2 = zipAllRight;
var zipAllSortedByKey2 = zipAllSortedByKey;
var zipAllSortedByKeyLeft2 = zipAllSortedByKeyLeft;
var zipAllSortedByKeyRight2 = zipAllSortedByKeyRight;
var zipAllSortedByKeyWith2 = zipAllSortedByKeyWith;
var zipAllWith2 = zipAllWith;
var zipLatest2 = zipLatest;
var zipLatestAll2 = zipLatestAll;
var zipLatestWith2 = zipLatestWith;
var zipLeft8 = zipLeft7;
var zipRight10 = zipRight9;
var zipWith8 = zipWith7;
var zipWithChunks2 = zipWithChunks;
var zipWithNext2 = zipWithNext;
var zipWithPrevious2 = zipWithPrevious;
var zipWithPreviousAndNext2 = zipWithPreviousAndNext;
var zipWithIndex2 = zipWithIndex;
var Do2 = Do;
var bind4 = bind3;
var bindEffect2 = bindEffect;
var bindTo5 = bindTo4;
var let_4 = let_3;
var decodeText2 = decodeText;
var encodeText2 = encodeText;
var fromEventListener2 = fromEventListener;

export {
  runFork2 as runFork,
  runSync2 as runSync,
  runCallback,
  defaultRuntime2 as defaultRuntime,
  defaultRuntimeFlags2 as defaultRuntimeFlags,
  make15 as make,
  updateContext2 as updateContext,
  Runtime_exports,
  unsafeMake2 as unsafeMake,
  get4 as get,
  set4 as set,
  update2 as update,
  currentContext2 as currentContext,
  currentLoggers2 as currentLoggers,
  currentScheduler2 as currentScheduler,
  currentTracerEnabled2 as currentTracerEnabled,
  FiberRef_exports,
  unbounded2 as unbounded,
  unbounded5 as unbounded2,
  publish2 as publish,
  PubSub_exports,
  bounded3 as bounded,
  shutdown2 as shutdown,
  unsafeOffer2 as unsafeOffer,
  Queue_exports,
  ChildExecutorDecisionTypeId,
  Continue,
  Close,
  Yield,
  isChildExecutorDecision,
  isContinue,
  isClose,
  isYield,
  match5 as match,
  UpstreamPullStrategyTypeId,
  PullAfterNext,
  PullAfterAllEnqueued,
  isUpstreamPullStrategy,
  isPullAfterNext,
  isPullAfterAllEnqueued,
  match6 as match2,
  ChannelTypeId,
  isChannel,
  acquireReleaseOut,
  catchAllCause2 as catchAllCause,
  concatAll,
  concatAllWith,
  concatMapWith,
  concatMapWithCustom,
  embedInput,
  ensuringWith,
  fail5 as fail,
  failSync2 as failSync,
  failCause4 as failCause,
  failCauseSync2 as failCauseSync,
  flatMap4 as flatMap,
  foldCauseChannel,
  fromEffect,
  pipeTo,
  provideContext2 as provideContext,
  readOrFail,
  readWith,
  readWithCause,
  succeed6 as succeed,
  suspend3 as suspend,
  sync4 as sync,
  void_3 as void_,
  write,
  UpstreamPullRequestTypeId,
  Pulled,
  NoUpstream,
  isUpstreamPullRequest,
  isPulled,
  isNoUpstream,
  match7 as match3,
  runIn,
  MergeStateTypeId,
  BothRunning,
  LeftDone,
  RightDone,
  isMergeState,
  isBothRunning,
  isLeftDone,
  isRightDone,
  match9 as match4,
  MergeStrategyTypeId,
  BackPressure,
  BufferSliding,
  isMergeStrategy,
  isBackPressure,
  isBufferSliding,
  match10 as match5,
  make12 as make2,
  acquireUseRelease2 as acquireUseRelease,
  as4 as as,
  asVoid4 as asVoid,
  buffer,
  bufferChunk,
  catchAll3 as catchAll,
  concatMap,
  collect,
  concatOut,
  mapInput2 as mapInput,
  mapInputEffect2 as mapInputEffect,
  mapInputError,
  mapInputErrorEffect,
  mapInputIn,
  mapInputInEffect,
  doneCollect,
  drain,
  emitCollect,
  ensuring4 as ensuring,
  context3 as context,
  contextWith2 as contextWith,
  contextWithChannel,
  contextWithEffect,
  flatten3 as flatten,
  foldChannel,
  fromEither,
  fromInput,
  fromPubSub,
  fromPubSubScoped,
  fromOption,
  fromQueue,
  identityChannel,
  interruptWhen,
  interruptWhenDeferred,
  map8 as map,
  mapEffect2 as mapEffect,
  mapError2 as mapError,
  mapErrorCause,
  mapOut,
  mapOutEffect,
  mapOutEffectPar,
  mergeAll,
  mergeAllUnbounded,
  mergeAllUnboundedWith,
  mergeAllWith,
  mergeMap,
  mergeOut,
  mergeOutWith,
  mergeWith,
  never2 as never,
  orDie2 as orDie,
  orDieWith,
  orElse3 as orElse,
  pipeToOrFail,
  provideService3 as provideService,
  provideLayer,
  mapInputContext2 as mapInputContext,
  provideSomeLayer,
  read,
  repeated,
  run2 as run,
  runCollect,
  runDrain,
  runScoped,
  scoped2 as scoped,
  scopedWith2 as scopedWith,
  splitLines,
  toPubSub,
  toPull,
  toPullIn,
  toQueue,
  unwrap,
  unwrapScoped,
  unwrapScopedWith,
  updateService,
  withSpan,
  writeAll,
  writeChunk,
  zip3 as zip,
  zipLeft3 as zipLeft,
  zipRight5 as zipRight,
  ChannelExceptionTypeId,
  ChannelException,
  isChannelException,
  MergeDecision_exports,
  RcRef_exports,
  ScheduleTypeId2 as ScheduleTypeId,
  exponential2 as exponential,
  spaced2 as spaced,
  union2 as union,
  Schedule_exports,
  StreamHaltStrategy_exports,
  SinkTypeId,
  suspend4 as suspend2,
  as6 as as2,
  collectAll,
  collectAllN,
  collectAllFrom,
  collectAllToMap,
  collectAllToMapN,
  collectAllToSet,
  collectAllToSetN,
  collectAllUntil,
  collectAllUntilEffect,
  collectAllWhile,
  collectAllWhileEffect,
  collectAllWhileWith,
  collectLeftover,
  mapInput4 as mapInput2,
  mapInputEffect4 as mapInputEffect2,
  mapInputChunks,
  mapInputChunksEffect,
  die4 as die,
  dieMessage2 as dieMessage,
  dieSync2 as dieSync,
  dimap,
  dimapEffect,
  dimapChunks,
  dimapChunksEffect,
  drain2,
  drop2 as drop,
  dropUntil2 as dropUntil,
  dropUntilEffect,
  dropWhile3 as dropWhile,
  dropWhileEffect,
  ensuring6 as ensuring2,
  ensuringWith2,
  context4 as context2,
  contextWith3 as contextWith2,
  contextWithEffect2,
  contextWithSink,
  every2 as every,
  fail6 as fail2,
  failSync3 as failSync2,
  failCause5 as failCause2,
  failCauseSync3 as failCauseSync2,
  filterInput,
  filterInputEffect,
  findEffect,
  fold,
  foldSink,
  foldChunks,
  foldChunksEffect,
  foldEffect,
  foldLeft,
  foldLeftChunks,
  foldLeftChunksEffect,
  foldLeftEffect,
  foldUntil,
  foldUntilEffect,
  foldWeighted,
  foldWeightedDecompose,
  foldWeightedDecomposeEffect,
  foldWeightedEffect,
  flatMap5 as flatMap2,
  forEach3 as forEach,
  forEachChunk,
  forEachWhile,
  forEachChunkWhile,
  fromChannel,
  fromEffect3 as fromEffect2,
  fromPubSub2,
  fromPush,
  fromQueue2,
  head3 as head,
  ignoreLeftover,
  last2 as last,
  leftover,
  map10 as map2,
  mapEffect4 as mapEffect2,
  mapError3 as mapError2,
  mapLeftover,
  never3 as never2,
  orElse4 as orElse2,
  provideContext4 as provideContext2,
  race2 as race,
  raceBoth,
  raceWith2 as raceWith,
  refineOrDie,
  refineOrDieWith,
  some2 as some,
  splitWhere2 as splitWhere,
  succeed8 as succeed2,
  sum,
  summarized,
  sync6 as sync2,
  take4 as take,
  toChannel,
  unwrap2,
  unwrapScoped2,
  unwrapScopedWith2,
  withDuration,
  zip4 as zip2,
  zipLeft5 as zipLeft2,
  zipRight7 as zipRight2,
  zipWith5 as zipWith,
  channelToSink,
  count3 as count,
  mkString,
  timed,
  STMTypeId,
  isSTM,
  commit2 as commit,
  context5 as context3,
  contextWith4 as contextWith3,
  contextWithSTM,
  catchAll4 as catchAll2,
  mapInputContext4 as mapInputContext2,
  die6 as die2,
  dieMessage3 as dieMessage2,
  dieSync3 as dieSync2,
  effect2 as effect,
  ensuring7 as ensuring3,
  fail8 as fail3,
  failSync4 as failSync3,
  flatMap6 as flatMap3,
  matchSTM,
  withSTMRuntime,
  interrupt7 as interrupt,
  interruptAs2 as interruptAs,
  map11 as map3,
  orTry,
  retry2 as retry,
  succeed10 as succeed3,
  sync7 as sync3,
  zip5 as zip3,
  zipLeft6 as zipLeft3,
  zipRight8 as zipRight3,
  zipWith6 as zipWith2,
  acquireUseRelease3 as acquireUseRelease2,
  as7 as as3,
  asSome2 as asSome,
  asSomeError,
  asVoid6 as asVoid2,
  attempt,
  bind2 as bind,
  bindTo3 as bindTo,
  let_2 as let_,
  catchSome,
  catchTag,
  catchTags,
  check3 as check,
  collect2,
  collectSTM,
  commitEither,
  cond,
  either3 as either,
  eventually,
  every3 as every2,
  exists,
  fiberId2 as fiberId,
  filter3 as filter,
  filterNot,
  filterOrDie,
  filterOrDieMessage,
  filterOrElse,
  filterOrFail,
  flatten4 as flatten2,
  flip,
  flipWith,
  match14 as match6,
  forEach4 as forEach2,
  fromEither2,
  fromOption2,
  gen2 as gen,
  head4 as head2,
  if_,
  ignore2 as ignore,
  isFailure2 as isFailure,
  isSuccess3 as isSuccess,
  iterate,
  loop,
  mapAttempt,
  mapBoth5 as mapBoth,
  mapError4 as mapError3,
  merge4 as merge,
  mergeAll2,
  negate,
  none2 as none,
  option,
  orDie3 as orDie2,
  orDieWith2,
  orElse5 as orElse3,
  orElseEither,
  orElseFail,
  orElseOptional,
  orElseSucceed,
  provideContext5 as provideContext3,
  provideSomeContext,
  provideService6 as provideService2,
  provideServiceSTM,
  reduce5 as reduce,
  reduceAll,
  reduceRight2 as reduceRight,
  refineOrDie2,
  refineOrDieWith2,
  reject,
  rejectSTM,
  repeatUntil,
  repeatWhile,
  replicate,
  replicateSTM,
  replicateSTMDiscard,
  retryUntil,
  retryWhile,
  partition,
  some3 as some2,
  all4 as all,
  succeedNone2 as succeedNone,
  succeedSome,
  summarized2,
  suspend6 as suspend3,
  tap3 as tap,
  tapBoth,
  tapError,
  try_,
  void_5 as void_2,
  unless,
  unlessSTM,
  unsome,
  validateAll,
  validateFirst,
  when2 as when,
  whenSTM,
  TRefTypeId,
  tRefVariance,
  TRefImpl,
  make18 as make3,
  get7 as get2,
  set5 as set2,
  getAndSet3 as getAndSet,
  getAndUpdate2 as getAndUpdate,
  getAndUpdateSome2 as getAndUpdateSome,
  setAndGet,
  modify3 as modify,
  modifySome2 as modifySome,
  update3 as update2,
  updateAndGet2 as updateAndGet,
  updateSome2 as updateSome,
  updateSomeAndGet2 as updateSomeAndGet,
  unsafeGet5 as unsafeGet,
  unsafeSet2 as unsafeSet,
  tDequeueVariance,
  publish4 as publish2,
  subscribe4 as subscribe,
  unbounded8 as unbounded3,
  TPubSub_exports,
  TDequeueTypeId2 as TDequeueTypeId,
  offer5 as offer,
  shutdown8 as shutdown2,
  unbounded9 as unbounded4,
  TQueue_exports,
  TakeTypeId,
  chunk,
  die7 as die3,
  dieMessage4 as dieMessage3,
  done3 as done,
  end2 as end,
  fail9 as fail4,
  failCause6 as failCause3,
  fromEffect4 as fromEffect3,
  fromExit,
  fromPull,
  isDone3 as isDone,
  isFailure3 as isFailure2,
  isSuccess4 as isSuccess2,
  make21 as make4,
  match15 as match7,
  matchEffect3 as matchEffect,
  map12 as map4,
  of2 as of,
  tap4 as tap2,
  isStream,
  concat,
  fromChannel2,
  toChannel2,
  fromPubSub3,
  fromTQueue,
  make22 as make5,
  unwrap3,
  unwrapScoped3,
  channelToStream,
  GroupByTypeId,
  evaluate,
  filter5 as filter2,
  first,
  make23 as make6,
  StreamTypeId3 as StreamTypeId,
  DefaultChunkSize2 as DefaultChunkSize,
  accumulate2 as accumulate,
  accumulateChunks2 as accumulateChunks,
  acquireRelease4 as acquireRelease,
  aggregate2 as aggregate,
  aggregateWithin2 as aggregateWithin,
  aggregateWithinEither2 as aggregateWithinEither,
  as9 as as4,
  _async2 as _async,
  asyncEffect2 as asyncEffect,
  asyncPush2 as asyncPush,
  asyncScoped2 as asyncScoped,
  branchAfter2 as branchAfter,
  broadcast2 as broadcast,
  share2 as share,
  broadcastDynamic2 as broadcastDynamic,
  broadcastedQueues2 as broadcastedQueues,
  broadcastedQueuesDynamic2 as broadcastedQueuesDynamic,
  buffer3 as buffer2,
  bufferChunks2 as bufferChunks,
  catchAll6 as catchAll3,
  catchAllCause4 as catchAllCause2,
  catchSome3 as catchSome2,
  catchTag3 as catchTag2,
  catchTags3 as catchTags2,
  catchSomeCause3 as catchSomeCause,
  changes2 as changes,
  changesWith2 as changesWith,
  changesWithEffect2 as changesWithEffect,
  chunks2 as chunks,
  chunksWith2 as chunksWith,
  combine3 as combine,
  combineChunks2 as combineChunks,
  concat2,
  concatAll3 as concatAll2,
  cross2 as cross,
  crossLeft2 as crossLeft,
  crossRight2 as crossRight,
  crossWith2 as crossWith,
  debounce2 as debounce,
  die9 as die4,
  dieSync5 as dieSync3,
  dieMessage6 as dieMessage4,
  distributedWith2 as distributedWith,
  distributedWithDynamic2 as distributedWithDynamic,
  drain4 as drain3,
  drainFork2 as drainFork,
  drop4 as drop2,
  dropRight2 as dropRight,
  dropUntil4 as dropUntil2,
  dropUntilEffect3 as dropUntilEffect2,
  dropWhile5 as dropWhile2,
  dropWhileEffect3 as dropWhileEffect2,
  either5 as either2,
  empty6 as empty,
  ensuring9 as ensuring4,
  ensuringWith4 as ensuringWith3,
  context7 as context4,
  contextWith6 as contextWith4,
  contextWithEffect4 as contextWithEffect3,
  contextWithStream2 as contextWithStream,
  execute2 as execute,
  fail11 as fail5,
  failSync6 as failSync4,
  failCause9 as failCause4,
  failCauseSync5 as failCauseSync3,
  filter6 as filter3,
  filterEffect2 as filterEffect,
  filterMap3 as filterMap,
  filterMapEffect2 as filterMapEffect,
  filterMapWhile3 as filterMapWhile,
  filterMapWhileEffect2 as filterMapWhileEffect,
  finalizer2 as finalizer,
  find2 as find,
  findEffect3 as findEffect2,
  flatMap8 as flatMap4,
  flatten6 as flatten3,
  flattenChunks2 as flattenChunks,
  flattenEffect2 as flattenEffect,
  flattenExitOption2 as flattenExitOption,
  flattenIterables2 as flattenIterables,
  flattenTake2 as flattenTake,
  forever5 as forever,
  fromAsyncIterable2 as fromAsyncIterable,
  fromChannel3,
  toChannel3,
  fromChunk2 as fromChunk,
  fromChunkPubSub2 as fromChunkPubSub,
  fromChunkQueue2 as fromChunkQueue,
  fromChunks2 as fromChunks,
  fromEffect6 as fromEffect4,
  fromEffectOption2 as fromEffectOption,
  fromPubSub4,
  fromTPubSub2 as fromTPubSub,
  fromIterable4 as fromIterable,
  fromIterableEffect2 as fromIterableEffect,
  fromIteratorSucceed2 as fromIteratorSucceed,
  fromPull3 as fromPull2,
  fromQueue4 as fromQueue3,
  fromTQueue2,
  fromReadableStream2 as fromReadableStream,
  fromReadableStreamByob2 as fromReadableStreamByob,
  fromSchedule2 as fromSchedule,
  groupAdjacentBy2 as groupAdjacentBy,
  groupBy2 as groupBy,
  groupByKey2 as groupByKey,
  grouped2 as grouped,
  groupedWithin2 as groupedWithin,
  haltAfter2 as haltAfter,
  haltWhen2 as haltWhen,
  haltWhenDeferred2 as haltWhenDeferred,
  identity4 as identity,
  interleave2 as interleave,
  interleaveWith2 as interleaveWith,
  intersperse2 as intersperse,
  intersperseAffixes2 as intersperseAffixes,
  interruptAfter2 as interruptAfter,
  interruptWhen3 as interruptWhen2,
  interruptWhenDeferred3 as interruptWhenDeferred2,
  iterate3 as iterate2,
  make24 as make7,
  map14 as map5,
  mapAccum3 as mapAccum,
  mapAccumEffect2 as mapAccumEffect,
  mapBoth7 as mapBoth2,
  mapChunks2 as mapChunks,
  mapChunksEffect2 as mapChunksEffect,
  mapConcat2 as mapConcat,
  mapConcatChunk2 as mapConcatChunk,
  mapConcatChunkEffect2 as mapConcatChunkEffect,
  mapConcatEffect2 as mapConcatEffect,
  mapEffect5 as mapEffect3,
  mapError6 as mapError4,
  mapErrorCause3 as mapErrorCause2,
  merge6 as merge2,
  mergeAll4 as mergeAll3,
  mergeWithTag2 as mergeWithTag,
  mergeWith3 as mergeWith2,
  mergeEither2 as mergeEither,
  mergeLeft2 as mergeLeft,
  mergeRight2 as mergeRight,
  mkString3 as mkString2,
  never5 as never3,
  onEnd2 as onEnd,
  onError3 as onError,
  onDone2 as onDone,
  onStart2 as onStart,
  orDie5 as orDie3,
  orDieWith4 as orDieWith3,
  orElse7 as orElse4,
  orElseEither3 as orElseEither2,
  orElseFail3 as orElseFail2,
  orElseIfEmpty2 as orElseIfEmpty,
  orElseIfEmptyChunk2 as orElseIfEmptyChunk,
  orElseIfEmptyStream2 as orElseIfEmptyStream,
  orElseSucceed3 as orElseSucceed2,
  paginate2 as paginate,
  paginateChunk2 as paginateChunk,
  paginateChunkEffect2 as paginateChunkEffect,
  paginateEffect2 as paginateEffect,
  partition3 as partition2,
  partitionEither2 as partitionEither,
  peel2 as peel,
  pipeThrough2 as pipeThrough,
  pipeThroughChannel2 as pipeThroughChannel,
  pipeThroughChannelOrFail2 as pipeThroughChannelOrFail,
  prepend3 as prepend,
  provideContext7 as provideContext4,
  provideSomeContext3 as provideSomeContext2,
  provideLayer3 as provideLayer2,
  provideService8 as provideService3,
  provideServiceEffect3 as provideServiceEffect,
  provideServiceStream2 as provideServiceStream,
  mapInputContext6 as mapInputContext3,
  provideSomeLayer3 as provideSomeLayer2,
  race4 as race2,
  raceAll2 as raceAll,
  range3 as range,
  rechunk2 as rechunk,
  refineOrDie4 as refineOrDie3,
  refineOrDieWith4 as refineOrDieWith3,
  repeat3 as repeat,
  repeatEffect2 as repeatEffect,
  repeatEffectChunk2 as repeatEffectChunk,
  repeatEffectChunkOption2 as repeatEffectChunkOption,
  repeatEffectOption2 as repeatEffectOption,
  repeatEffectWithSchedule2 as repeatEffectWithSchedule,
  repeatEither2 as repeatEither,
  repeatElements2 as repeatElements,
  repeatElementsWith2 as repeatElementsWith,
  repeatValue2 as repeatValue,
  repeatWith2 as repeatWith,
  retry4 as retry2,
  withExecutionPlan2 as withExecutionPlan,
  run5 as run2,
  runCollect3 as runCollect2,
  runCount2 as runCount,
  runDrain3 as runDrain2,
  runFold2 as runFold,
  runFoldEffect2 as runFoldEffect,
  runFoldScoped2 as runFoldScoped,
  runFoldScopedEffect2 as runFoldScopedEffect,
  runFoldWhile2 as runFoldWhile,
  runFoldWhileEffect2 as runFoldWhileEffect,
  runFoldWhileScoped2 as runFoldWhileScoped,
  runFoldWhileScopedEffect2 as runFoldWhileScopedEffect,
  runForEach2 as runForEach,
  runForEachChunk2 as runForEachChunk,
  runForEachChunkScoped2 as runForEachChunkScoped,
  runForEachScoped2 as runForEachScoped,
  runForEachWhile2 as runForEachWhile,
  runForEachWhileScoped2 as runForEachWhileScoped,
  runHead2 as runHead,
  runIntoPubSub2 as runIntoPubSub,
  runIntoPubSubScoped2 as runIntoPubSubScoped,
  runIntoQueue2 as runIntoQueue,
  runIntoQueueElementsScoped2 as runIntoQueueElementsScoped,
  runIntoQueueScoped2 as runIntoQueueScoped,
  runLast2 as runLast,
  runScoped3 as runScoped2,
  runSum2 as runSum,
  scan2 as scan,
  scanEffect2 as scanEffect,
  scanReduce2 as scanReduce,
  scanReduceEffect2 as scanReduceEffect,
  schedule2 as schedule,
  scheduleWith2 as scheduleWith,
  scoped4 as scoped2,
  scopedWith4 as scopedWith2,
  sliding10 as sliding,
  slidingSize2 as slidingSize,
  some5 as some3,
  someOrElse2 as someOrElse,
  someOrFail2 as someOrFail,
  split2 as split,
  splitOnChunk2 as splitOnChunk,
  splitLines3 as splitLines2,
  succeed12 as succeed4,
  sync9 as sync4,
  suspend8 as suspend4,
  take9 as take2,
  takeRight3 as takeRight,
  takeUntil2 as takeUntil,
  takeUntilEffect2 as takeUntilEffect,
  takeWhile4 as takeWhile,
  tap6 as tap3,
  tapBoth3 as tapBoth2,
  tapError3 as tapError2,
  tapErrorCause3 as tapErrorCause,
  tapSink2 as tapSink,
  throttle2 as throttle,
  throttleEffect2 as throttleEffect,
  tick2 as tick,
  timeout2 as timeout,
  timeoutFail3 as timeoutFail,
  timeoutFailCause3 as timeoutFailCause,
  timeoutTo2 as timeoutTo,
  toPubSub3 as toPubSub2,
  toPull3 as toPull2,
  toQueue3 as toQueue2,
  toQueueOfElements2 as toQueueOfElements,
  toReadableStream2 as toReadableStream,
  toReadableStreamEffect2 as toReadableStreamEffect,
  toReadableStreamRuntime2 as toReadableStreamRuntime,
  toAsyncIterableRuntime2 as toAsyncIterableRuntime,
  toAsyncIterableEffect2 as toAsyncIterableEffect,
  toAsyncIterable2 as toAsyncIterable,
  transduce2 as transduce,
  unfold4 as unfold,
  unfoldChunk2 as unfoldChunk,
  unfoldChunkEffect2 as unfoldChunkEffect,
  unfoldEffect2 as unfoldEffect,
  void_7 as void_3,
  unwrap4,
  unwrapScoped4,
  unwrapScopedWith4 as unwrapScopedWith3,
  updateService3 as updateService2,
  when4 as when2,
  whenCase2 as whenCase,
  whenCaseEffect2 as whenCaseEffect,
  whenEffect3 as whenEffect,
  withSpan3 as withSpan2,
  zip7 as zip4,
  zipFlatten2 as zipFlatten,
  zipAll2 as zipAll,
  zipAllLeft2 as zipAllLeft,
  zipAllRight2 as zipAllRight,
  zipAllSortedByKey2 as zipAllSortedByKey,
  zipAllSortedByKeyLeft2 as zipAllSortedByKeyLeft,
  zipAllSortedByKeyRight2 as zipAllSortedByKeyRight,
  zipAllSortedByKeyWith2 as zipAllSortedByKeyWith,
  zipAllWith2 as zipAllWith,
  zipLatest2 as zipLatest,
  zipLatestAll2 as zipLatestAll,
  zipLatestWith2 as zipLatestWith,
  zipLeft8 as zipLeft4,
  zipRight10 as zipRight4,
  zipWith8 as zipWith3,
  zipWithChunks2 as zipWithChunks,
  zipWithNext2 as zipWithNext,
  zipWithPrevious2 as zipWithPrevious,
  zipWithPreviousAndNext2 as zipWithPreviousAndNext,
  zipWithIndex2 as zipWithIndex,
  Do2 as Do,
  bind4 as bind2,
  bindEffect2 as bindEffect,
  bindTo5 as bindTo2,
  let_4 as let_2,
  decodeText2 as decodeText,
  encodeText2 as encodeText,
  fromEventListener2 as fromEventListener,
  Stream_exports
};
//# sourceMappingURL=chunk-HVF2XBK2.js.map
