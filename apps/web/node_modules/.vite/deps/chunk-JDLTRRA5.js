import {
  TypeId,
  array,
  array2 as array3,
  bind,
  bindTo,
  combineAll,
  fromOption,
  getLeft,
  getOrElse,
  getRight,
  isEither,
  isLeft,
  isNone as isNone2,
  isRight,
  isSome,
  left,
  let_,
  make,
  mapInput2 as mapInput,
  max,
  min,
  none as none2,
  right,
  some as some2,
  toArray,
  tuple,
  tuple2
} from "./chunk-5XJTUU64.js";
import {
  NodeInspectSymbol,
  adapter,
  array as array2,
  cached,
  equals,
  equivalence,
  format,
  hasProperty,
  isBoolean,
  isFunction,
  isGenKind,
  isNone,
  isTupleOf,
  isTupleOfAtLeast,
  none,
  pipeArguments,
  some,
  symbol,
  symbol2,
  toJSON,
  yieldWrapGet
} from "./chunk-SPGJQP32.js";
import {
  constNull,
  constUndefined,
  dual,
  identity,
  pipe
} from "./chunk-LX4DLJUA.js";
import {
  __export
} from "./chunk-G3PMV62Z.js";

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Chunk.js
var Chunk_exports = {};
__export(Chunk_exports, {
  append: () => append3,
  appendAll: () => appendAll3,
  chunksOf: () => chunksOf3,
  compact: () => compact,
  contains: () => contains3,
  containsWith: () => containsWith3,
  dedupe: () => dedupe2,
  dedupeAdjacent: () => dedupeAdjacent3,
  difference: () => difference3,
  differenceWith: () => differenceWith2,
  drop: () => drop3,
  dropRight: () => dropRight2,
  dropWhile: () => dropWhile2,
  empty: () => empty4,
  every: () => every3,
  filter: () => filter4,
  filterMap: () => filterMap4,
  filterMapWhile: () => filterMapWhile3,
  findFirst: () => findFirst4,
  findFirstIndex: () => findFirstIndex2,
  findLast: () => findLast3,
  findLastIndex: () => findLastIndex2,
  flatMap: () => flatMap4,
  flatten: () => flatten3,
  forEach: () => forEach3,
  fromIterable: () => fromIterable2,
  get: () => get3,
  getEquivalence: () => getEquivalence5,
  head: () => head3,
  headNonEmpty: () => headNonEmpty2,
  intersection: () => intersection3,
  isChunk: () => isChunk,
  isEmpty: () => isEmpty2,
  isNonEmpty: () => isNonEmpty,
  join: () => join2,
  last: () => last2,
  lastNonEmpty: () => lastNonEmpty2,
  make: () => make4,
  makeBy: () => makeBy3,
  map: () => map6,
  mapAccum: () => mapAccum2,
  modify: () => modify3,
  modifyOption: () => modifyOption3,
  of: () => of3,
  partition: () => partition3,
  partitionMap: () => partitionMap3,
  prepend: () => prepend3,
  prependAll: () => prependAll3,
  range: () => range3,
  reduce: () => reduce4,
  reduceRight: () => reduceRight2,
  remove: () => remove3,
  removeOption: () => removeOption2,
  replace: () => replace3,
  replaceOption: () => replaceOption3,
  reverse: () => reverse2,
  separate: () => separate3,
  size: () => size3,
  some: () => some6,
  sort: () => sort2,
  sortWith: () => sortWith2,
  split: () => split2,
  splitAt: () => splitAt2,
  splitNonEmptyAt: () => splitNonEmptyAt2,
  splitWhere: () => splitWhere2,
  tail: () => tail2,
  tailNonEmpty: () => tailNonEmpty2,
  take: () => take3,
  takeRight: () => takeRight2,
  takeWhile: () => takeWhile3,
  toArray: () => toArray2,
  toReadonlyArray: () => toReadonlyArray,
  union: () => union3,
  unsafeFromArray: () => unsafeFromArray,
  unsafeFromNonEmptyArray: () => unsafeFromNonEmptyArray,
  unsafeGet: () => unsafeGet2,
  unsafeHead: () => unsafeHead2,
  unsafeLast: () => unsafeLast,
  unzip: () => unzip2,
  zip: () => zip3,
  zipWith: () => zipWith4
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Array.js
var Array_exports = {};
__export(Array_exports, {
  Do: () => Do2,
  allocate: () => allocate,
  append: () => append2,
  appendAll: () => appendAll2,
  bind: () => bind3,
  bindTo: () => bindTo3,
  cartesian: () => cartesian2,
  cartesianWith: () => cartesianWith2,
  chop: () => chop,
  chunksOf: () => chunksOf2,
  contains: () => contains2,
  containsWith: () => containsWith2,
  copy: () => copy,
  countBy: () => countBy2,
  dedupe: () => dedupe,
  dedupeAdjacent: () => dedupeAdjacent2,
  dedupeAdjacentWith: () => dedupeAdjacentWith2,
  dedupeWith: () => dedupeWith,
  difference: () => difference2,
  differenceWith: () => differenceWith,
  drop: () => drop2,
  dropRight: () => dropRight,
  dropWhile: () => dropWhile,
  empty: () => empty3,
  ensure: () => ensure,
  every: () => every2,
  extend: () => extend,
  filter: () => filter3,
  filterMap: () => filterMap3,
  filterMapWhile: () => filterMapWhile2,
  findFirst: () => findFirst3,
  findFirstIndex: () => findFirstIndex,
  findFirstWithIndex: () => findFirstWithIndex,
  findLast: () => findLast2,
  findLastIndex: () => findLastIndex,
  flatMap: () => flatMap3,
  flatMapNullable: () => flatMapNullable2,
  flatten: () => flatten2,
  forEach: () => forEach2,
  fromIterable: () => fromIterable,
  fromNullable: () => fromNullable2,
  fromOption: () => fromOption3,
  fromRecord: () => fromRecord2,
  get: () => get2,
  getEquivalence: () => getEquivalence4,
  getLefts: () => getLefts3,
  getOrder: () => getOrder2,
  getRights: () => getRights3,
  getSomes: () => getSomes3,
  group: () => group2,
  groupBy: () => groupBy2,
  groupWith: () => groupWith2,
  head: () => head2,
  headNonEmpty: () => headNonEmpty,
  init: () => init,
  initNonEmpty: () => initNonEmpty,
  insertAt: () => insertAt,
  intersection: () => intersection2,
  intersectionWith: () => intersectionWith,
  intersperse: () => intersperse2,
  isArray: () => isArray,
  isEmptyArray: () => isEmptyArray,
  isEmptyReadonlyArray: () => isEmptyReadonlyArray,
  isNonEmptyArray: () => isNonEmptyArray2,
  isNonEmptyReadonlyArray: () => isNonEmptyReadonlyArray,
  join: () => join,
  last: () => last,
  lastNonEmpty: () => lastNonEmpty,
  length: () => length,
  let: () => let_3,
  liftEither: () => liftEither,
  liftNullable: () => liftNullable,
  liftOption: () => liftOption,
  liftPredicate: () => liftPredicate2,
  make: () => make3,
  makeBy: () => makeBy2,
  map: () => map5,
  mapAccum: () => mapAccum,
  match: () => match2,
  matchLeft: () => matchLeft,
  matchRight: () => matchRight,
  max: () => max2,
  min: () => min2,
  modify: () => modify2,
  modifyNonEmptyHead: () => modifyNonEmptyHead,
  modifyNonEmptyLast: () => modifyNonEmptyLast,
  modifyOption: () => modifyOption2,
  of: () => of2,
  pad: () => pad,
  partition: () => partition2,
  partitionMap: () => partitionMap2,
  prepend: () => prepend2,
  prependAll: () => prependAll2,
  range: () => range2,
  reduce: () => reduce3,
  reduceRight: () => reduceRight,
  remove: () => remove2,
  removeOption: () => removeOption,
  replace: () => replace2,
  replaceOption: () => replaceOption2,
  replicate: () => replicate2,
  reverse: () => reverse,
  rotate: () => rotate,
  scan: () => scan2,
  scanRight: () => scanRight,
  separate: () => separate2,
  setNonEmptyHead: () => setNonEmptyHead,
  setNonEmptyLast: () => setNonEmptyLast,
  some: () => some5,
  sort: () => sort,
  sortBy: () => sortBy,
  sortWith: () => sortWith,
  span: () => span,
  split: () => split,
  splitAt: () => splitAt,
  splitNonEmptyAt: () => splitNonEmptyAt,
  splitWhere: () => splitWhere,
  tail: () => tail,
  tailNonEmpty: () => tailNonEmpty,
  take: () => take2,
  takeRight: () => takeRight,
  takeWhile: () => takeWhile2,
  unappend: () => unappend,
  unfold: () => unfold2,
  union: () => union2,
  unionWith: () => unionWith,
  unprepend: () => unprepend,
  unsafeGet: () => unsafeGet,
  unzip: () => unzip,
  window: () => window,
  zip: () => zip2,
  zipWith: () => zipWith3
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Either.js
var Either_exports = {};
__export(Either_exports, {
  Do: () => Do,
  TypeId: () => TypeId2,
  all: () => all,
  andThen: () => andThen,
  ap: () => ap,
  bind: () => bind2,
  bindTo: () => bindTo2,
  filterOrLeft: () => filterOrLeft,
  flatMap: () => flatMap,
  flip: () => flip,
  fromNullable: () => fromNullable,
  fromOption: () => fromOption2,
  gen: () => gen,
  getEquivalence: () => getEquivalence,
  getLeft: () => getLeft2,
  getOrElse: () => getOrElse2,
  getOrNull: () => getOrNull,
  getOrThrow: () => getOrThrow,
  getOrThrowWith: () => getOrThrowWith,
  getOrUndefined: () => getOrUndefined,
  getRight: () => getRight2,
  isEither: () => isEither2,
  isLeft: () => isLeft2,
  isRight: () => isRight2,
  left: () => left2,
  let: () => let_2,
  liftPredicate: () => liftPredicate,
  map: () => map,
  mapBoth: () => mapBoth,
  mapLeft: () => mapLeft,
  match: () => match,
  merge: () => merge,
  orElse: () => orElse,
  right: () => right2,
  transposeMapOption: () => transposeMapOption,
  transposeOption: () => transposeOption,
  try: () => try_,
  void: () => void_,
  zipWith: () => zipWith
});
var TypeId2 = TypeId;
var right2 = right;
var void_ = right2(void 0);
var left2 = left;
var fromNullable = dual(2, (self, onNullable) => self == null ? left2(onNullable(self)) : right2(self));
var fromOption2 = fromOption;
var try_ = (evaluate) => {
  if (isFunction(evaluate)) {
    try {
      return right2(evaluate());
    } catch (e) {
      return left2(e);
    }
  } else {
    try {
      return right2(evaluate.try());
    } catch (e) {
      return left2(evaluate.catch(e));
    }
  }
};
var isEither2 = isEither;
var isLeft2 = isLeft;
var isRight2 = isRight;
var getRight2 = getRight;
var getLeft2 = getLeft;
var getEquivalence = ({
  left: left3,
  right: right3
}) => make((x, y) => isLeft2(x) ? isLeft2(y) && left3(x.left, y.left) : isRight2(y) && right3(x.right, y.right));
var mapBoth = dual(2, (self, {
  onLeft,
  onRight
}) => isLeft2(self) ? left2(onLeft(self.left)) : right2(onRight(self.right)));
var mapLeft = dual(2, (self, f) => isLeft2(self) ? left2(f(self.left)) : right2(self.right));
var map = dual(2, (self, f) => isRight2(self) ? right2(f(self.right)) : left2(self.left));
var match = dual(2, (self, {
  onLeft,
  onRight
}) => isLeft2(self) ? onLeft(self.left) : onRight(self.right));
var liftPredicate = dual(3, (a, predicate, orLeftWith) => predicate(a) ? right2(a) : left2(orLeftWith(a)));
var filterOrLeft = dual(3, (self, predicate, orLeftWith) => flatMap(self, (r) => predicate(r) ? right2(r) : left2(orLeftWith(r))));
var merge = match({
  onLeft: identity,
  onRight: identity
});
var getOrElse2 = dual(2, (self, onLeft) => isLeft2(self) ? onLeft(self.left) : self.right);
var getOrNull = getOrElse2(constNull);
var getOrUndefined = getOrElse2(constUndefined);
var getOrThrowWith = dual(2, (self, onLeft) => {
  if (isRight2(self)) {
    return self.right;
  }
  throw onLeft(self.left);
});
var getOrThrow = getOrThrowWith(() => new Error("getOrThrow called on a Left"));
var orElse = dual(2, (self, that) => isLeft2(self) ? that(self.left) : right2(self.right));
var flatMap = dual(2, (self, f) => isLeft2(self) ? left2(self.left) : f(self.right));
var andThen = dual(2, (self, f) => flatMap(self, (a) => {
  const b = isFunction(f) ? f(a) : f;
  return isEither2(b) ? b : right2(b);
}));
var zipWith = dual(3, (self, that, f) => flatMap(self, (r) => map(that, (r2) => f(r, r2))));
var ap = dual(2, (self, that) => zipWith(self, that, (f, a) => f(a)));
var all = (input) => {
  if (Symbol.iterator in input) {
    const out2 = [];
    for (const e of input) {
      if (isLeft2(e)) {
        return e;
      }
      out2.push(e.right);
    }
    return right2(out2);
  }
  const out = {};
  for (const key of Object.keys(input)) {
    const e = input[key];
    if (isLeft2(e)) {
      return e;
    }
    out[key] = e.right;
  }
  return right2(out);
};
var flip = (self) => isLeft2(self) ? right2(self.left) : left2(self.right);
var adapter2 = adapter();
var gen = (...args) => {
  const f = args.length === 1 ? args[0] : args[1].bind(args[0]);
  const iterator = f(adapter2);
  let state = iterator.next();
  while (!state.done) {
    const current = isGenKind(state.value) ? state.value.value : yieldWrapGet(state.value);
    if (isLeft2(current)) {
      return current;
    }
    state = iterator.next(current.right);
  }
  return right2(state.value);
};
var Do = right2({});
var bind2 = bind(map, flatMap);
var bindTo2 = bindTo(map);
var let_2 = let_(map);
var transposeOption = (self) => {
  return isNone(self) ? right2(none) : map(self.value, some);
};
var transposeMapOption = dual(2, (self, f) => isNone(self) ? right2(none) : map(f(self.value), some));

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/internal/array.js
var isNonEmptyArray = (self) => self.length > 0;

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Iterable.js
var Iterable_exports = {};
__export(Iterable_exports, {
  append: () => append,
  appendAll: () => appendAll,
  cartesian: () => cartesian,
  cartesianWith: () => cartesianWith,
  chunksOf: () => chunksOf,
  contains: () => contains,
  containsWith: () => containsWith,
  countBy: () => countBy,
  dedupeAdjacent: () => dedupeAdjacent,
  dedupeAdjacentWith: () => dedupeAdjacentWith,
  drop: () => drop,
  empty: () => empty,
  filter: () => filter,
  filterMap: () => filterMap,
  filterMapWhile: () => filterMapWhile,
  findFirst: () => findFirst,
  findLast: () => findLast,
  flatMap: () => flatMap2,
  flatMapNullable: () => flatMapNullable,
  flatten: () => flatten,
  forEach: () => forEach,
  fromRecord: () => fromRecord,
  getLefts: () => getLefts,
  getRights: () => getRights,
  getSomes: () => getSomes,
  group: () => group,
  groupBy: () => groupBy,
  groupWith: () => groupWith,
  head: () => head,
  intersperse: () => intersperse,
  isEmpty: () => isEmpty,
  makeBy: () => makeBy,
  map: () => map3,
  of: () => of,
  prepend: () => prepend,
  prependAll: () => prependAll,
  range: () => range,
  reduce: () => reduce,
  replicate: () => replicate,
  scan: () => scan,
  size: () => size,
  some: () => some3,
  take: () => take,
  takeWhile: () => takeWhile,
  unfold: () => unfold,
  unsafeHead: () => unsafeHead,
  zip: () => zip,
  zipWith: () => zipWith2
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Tuple.js
var Tuple_exports = {};
__export(Tuple_exports, {
  appendElement: () => appendElement,
  at: () => at,
  getEquivalence: () => getEquivalence2,
  getFirst: () => getFirst,
  getOrder: () => getOrder,
  getSecond: () => getSecond,
  isTupleOf: () => isTupleOf,
  isTupleOfAtLeast: () => isTupleOfAtLeast,
  make: () => make2,
  map: () => map2,
  mapBoth: () => mapBoth2,
  mapFirst: () => mapFirst,
  mapSecond: () => mapSecond,
  swap: () => swap
});
var make2 = (...elements) => elements;
var getFirst = (self) => self[0];
var getSecond = (self) => self[1];
var map2 = dual(2, (self, fn) => self.map((element) => fn(element)));
var mapBoth2 = dual(2, (self, {
  onFirst,
  onSecond
}) => [onFirst(self[0]), onSecond(self[1])]);
var mapFirst = dual(2, (self, f) => [f(self[0]), self[1]]);
var mapSecond = dual(2, (self, f) => [self[0], f(self[1])]);
var swap = (self) => [self[1], self[0]];
var getEquivalence2 = tuple;
var getOrder = tuple2;
var appendElement = dual(2, (self, that) => [...self, that]);
var at = dual(2, (self, index) => self[index]);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Iterable.js
var makeBy = (f, options) => {
  const max3 = options?.length !== void 0 ? Math.max(1, Math.floor(options.length)) : Infinity;
  return {
    [Symbol.iterator]() {
      let i = 0;
      return {
        next() {
          if (i < max3) {
            return {
              value: f(i++),
              done: false
            };
          }
          return {
            done: true,
            value: void 0
          };
        }
      };
    }
  };
};
var range = (start, end) => {
  if (end === void 0) {
    return makeBy((i) => start + i);
  }
  return makeBy((i) => start + i, {
    length: start <= end ? end - start + 1 : 1
  });
};
var replicate = dual(2, (a, n) => makeBy(() => a, {
  length: n
}));
var fromRecord = (self) => ({
  *[Symbol.iterator]() {
    for (const key in self) {
      if (Object.prototype.hasOwnProperty.call(self, key)) {
        yield [key, self[key]];
      }
    }
  }
});
var prepend = dual(2, (self, head4) => prependAll(self, [head4]));
var prependAll = dual(2, (self, that) => appendAll(that, self));
var append = dual(2, (self, last3) => appendAll(self, [last3]));
var appendAll = dual(2, (self, that) => ({
  [Symbol.iterator]() {
    const iterA = self[Symbol.iterator]();
    let doneA = false;
    let iterB;
    return {
      next() {
        if (!doneA) {
          const r = iterA.next();
          if (r.done) {
            doneA = true;
            iterB = that[Symbol.iterator]();
            return iterB.next();
          }
          return r;
        }
        return iterB.next();
      }
    };
  }
}));
var scan = dual(3, (self, b, f) => ({
  [Symbol.iterator]() {
    let acc = b;
    let iterator;
    function next() {
      if (iterator === void 0) {
        iterator = self[Symbol.iterator]();
        return {
          done: false,
          value: acc
        };
      }
      const result = iterator.next();
      if (result.done) {
        return result;
      }
      acc = f(acc, result.value);
      return {
        done: false,
        value: acc
      };
    }
    return {
      next
    };
  }
}));
var isEmpty = (self) => {
  const iterator = self[Symbol.iterator]();
  return iterator.next().done === true;
};
var size = (self) => {
  const iterator = self[Symbol.iterator]();
  let count = 0;
  while (!iterator.next().done) {
    count++;
  }
  return count;
};
var head = (self) => {
  const iterator = self[Symbol.iterator]();
  const result = iterator.next();
  return result.done ? none2() : some2(result.value);
};
var unsafeHead = (self) => {
  const iterator = self[Symbol.iterator]();
  const result = iterator.next();
  if (result.done) throw new Error("unsafeHead: empty iterable");
  return result.value;
};
var take = dual(2, (self, n) => ({
  [Symbol.iterator]() {
    let i = 0;
    const iterator = self[Symbol.iterator]();
    return {
      next() {
        if (i < n) {
          i++;
          return iterator.next();
        }
        return {
          done: true,
          value: void 0
        };
      }
    };
  }
}));
var takeWhile = dual(2, (self, predicate) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let i = 0;
    return {
      next() {
        const result = iterator.next();
        if (result.done || !predicate(result.value, i++)) {
          return {
            done: true,
            value: void 0
          };
        }
        return result;
      }
    };
  }
}));
var drop = dual(2, (self, n) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let i = 0;
    return {
      next() {
        while (i < n) {
          const result = iterator.next();
          if (result.done) {
            return {
              done: true,
              value: void 0
            };
          }
          i++;
        }
        return iterator.next();
      }
    };
  }
}));
var findFirst = dual(2, (self, f) => {
  let i = 0;
  for (const a of self) {
    const o = f(a, i);
    if (isBoolean(o)) {
      if (o) {
        return some2(a);
      }
    } else {
      if (isSome(o)) {
        return o;
      }
    }
    i++;
  }
  return none2();
});
var findLast = dual(2, (self, f) => {
  let i = 0;
  let last3 = none2();
  for (const a of self) {
    const o = f(a, i);
    if (isBoolean(o)) {
      if (o) {
        last3 = some2(a);
      }
    } else {
      if (isSome(o)) {
        last3 = o;
      }
    }
    i++;
  }
  return last3;
});
var zip = dual(2, (self, that) => zipWith2(self, that, make2));
var zipWith2 = dual(3, (self, that, f) => ({
  [Symbol.iterator]() {
    const selfIterator = self[Symbol.iterator]();
    const thatIterator = that[Symbol.iterator]();
    return {
      next() {
        const selfResult = selfIterator.next();
        const thatResult = thatIterator.next();
        if (selfResult.done || thatResult.done) {
          return {
            done: true,
            value: void 0
          };
        }
        return {
          done: false,
          value: f(selfResult.value, thatResult.value)
        };
      }
    };
  }
}));
var intersperse = dual(2, (self, middle) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let next = iterator.next();
    let emitted = false;
    return {
      next() {
        if (next.done) {
          return next;
        } else if (emitted) {
          emitted = false;
          return {
            done: false,
            value: middle
          };
        }
        emitted = true;
        const result = next;
        next = iterator.next();
        return result;
      }
    };
  }
}));
var containsWith = (isEquivalent) => dual(2, (self, a) => {
  for (const i of self) {
    if (isEquivalent(a, i)) {
      return true;
    }
  }
  return false;
});
var _equivalence = equivalence();
var contains = containsWith(_equivalence);
var chunksOf = dual(2, (self, n) => {
  const safeN = Math.max(1, Math.floor(n));
  return {
    [Symbol.iterator]() {
      let iterator = self[Symbol.iterator]();
      return {
        next() {
          if (iterator === void 0) {
            return {
              done: true,
              value: void 0
            };
          }
          const chunk = [];
          for (let i = 0; i < safeN; i++) {
            const result = iterator.next();
            if (result.done) {
              iterator = void 0;
              return chunk.length === 0 ? {
                done: true,
                value: void 0
              } : {
                done: false,
                value: chunk
              };
            }
            chunk.push(result.value);
          }
          return {
            done: false,
            value: chunk
          };
        }
      };
    }
  };
});
var groupWith = dual(2, (self, isEquivalent) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let nextResult;
    return {
      next() {
        let result;
        if (nextResult !== void 0) {
          if (nextResult.done) {
            return {
              done: true,
              value: void 0
            };
          }
          result = nextResult;
          nextResult = void 0;
        } else {
          result = iterator.next();
          if (result.done) {
            return {
              done: true,
              value: void 0
            };
          }
        }
        const chunk = [result.value];
        while (true) {
          const next = iterator.next();
          if (next.done || !isEquivalent(result.value, next.value)) {
            nextResult = next;
            return {
              done: false,
              value: chunk
            };
          }
          chunk.push(next.value);
        }
      }
    };
  }
}));
var group = groupWith(equivalence());
var groupBy = dual(2, (self, f) => {
  const out = {};
  for (const a of self) {
    const k = f(a);
    if (Object.prototype.hasOwnProperty.call(out, k)) {
      out[k].push(a);
    } else {
      out[k] = [a];
    }
  }
  return out;
});
var constEmpty = {
  [Symbol.iterator]() {
    return constEmptyIterator;
  }
};
var constEmptyIterator = {
  next() {
    return {
      done: true,
      value: void 0
    };
  }
};
var empty = () => constEmpty;
var of = (a) => [a];
var map3 = dual(2, (self, f) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let i = 0;
    return {
      next() {
        const result = iterator.next();
        if (result.done) {
          return {
            done: true,
            value: void 0
          };
        }
        return {
          done: false,
          value: f(result.value, i++)
        };
      }
    };
  }
}));
var flatMap2 = dual(2, (self, f) => flatten(map3(self, f)));
var flatten = (self) => ({
  [Symbol.iterator]() {
    const outerIterator = self[Symbol.iterator]();
    let innerIterator;
    function next() {
      if (innerIterator === void 0) {
        const next2 = outerIterator.next();
        if (next2.done) {
          return next2;
        }
        innerIterator = next2.value[Symbol.iterator]();
      }
      const result = innerIterator.next();
      if (result.done) {
        innerIterator = void 0;
        return next();
      }
      return result;
    }
    return {
      next
    };
  }
});
var filterMap = dual(2, (self, f) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let i = 0;
    return {
      next() {
        let result = iterator.next();
        while (!result.done) {
          const b = f(result.value, i++);
          if (isSome(b)) {
            return {
              done: false,
              value: b.value
            };
          }
          result = iterator.next();
        }
        return {
          done: true,
          value: void 0
        };
      }
    };
  }
}));
var filterMapWhile = dual(2, (self, f) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let i = 0;
    return {
      next() {
        const result = iterator.next();
        if (result.done) {
          return {
            done: true,
            value: void 0
          };
        }
        const b = f(result.value, i++);
        if (isSome(b)) {
          return {
            done: false,
            value: b.value
          };
        }
        return {
          done: true,
          value: void 0
        };
      }
    };
  }
}));
var getSomes = filterMap(identity);
var getLefts = (self) => filterMap(self, getLeft2);
var getRights = (self) => filterMap(self, getRight2);
var filter = dual(2, (self, predicate) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let i = 0;
    return {
      next() {
        let result = iterator.next();
        while (!result.done) {
          if (predicate(result.value, i++)) {
            return {
              done: false,
              value: result.value
            };
          }
          result = iterator.next();
        }
        return {
          done: true,
          value: void 0
        };
      }
    };
  }
}));
var flatMapNullable = dual(2, (self, f) => filterMap(self, (a) => {
  const b = f(a);
  return b == null ? none2() : some2(b);
}));
var some3 = dual(2, (self, predicate) => {
  let i = 0;
  for (const a of self) {
    if (predicate(a, i++)) {
      return true;
    }
  }
  return false;
});
var unfold = (b, f) => ({
  [Symbol.iterator]() {
    let next = b;
    return {
      next() {
        const o = f(next);
        if (isNone2(o)) {
          return {
            done: true,
            value: void 0
          };
        }
        const [a, b2] = o.value;
        next = b2;
        return {
          done: false,
          value: a
        };
      }
    };
  }
});
var forEach = dual(2, (self, f) => {
  let i = 0;
  for (const a of self) {
    f(a, i++);
  }
});
var reduce = dual(3, (self, b, f) => {
  if (Array.isArray(self)) {
    return self.reduce(f, b);
  }
  let i = 0;
  let result = b;
  for (const n of self) {
    result = f(result, n, i++);
  }
  return result;
});
var dedupeAdjacentWith = dual(2, (self, isEquivalent) => ({
  [Symbol.iterator]() {
    const iterator = self[Symbol.iterator]();
    let first = true;
    let last3;
    function next() {
      const result = iterator.next();
      if (result.done) {
        return {
          done: true,
          value: void 0
        };
      }
      if (first) {
        first = false;
        last3 = result.value;
        return result;
      }
      const current = result.value;
      if (isEquivalent(last3, current)) {
        return next();
      }
      last3 = current;
      return result;
    }
    return {
      next
    };
  }
}));
var dedupeAdjacent = dedupeAdjacentWith(equivalence());
var cartesianWith = dual(3, (self, that, f) => flatMap2(self, (a) => map3(that, (b) => f(a, b))));
var cartesian = dual(2, (self, that) => cartesianWith(self, that, (a, b) => [a, b]));
var countBy = dual(2, (self, f) => {
  let count = 0;
  let i = 0;
  for (const a of self) {
    if (f(a, i)) {
      count++;
    }
    i++;
  }
  return count;
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Record.js
var Record_exports = {};
__export(Record_exports, {
  collect: () => collect,
  difference: () => difference,
  empty: () => empty2,
  every: () => every,
  filter: () => filter2,
  filterMap: () => filterMap2,
  findFirst: () => findFirst2,
  fromEntries: () => fromEntries,
  fromIterableBy: () => fromIterableBy,
  fromIterableWith: () => fromIterableWith,
  get: () => get,
  getEquivalence: () => getEquivalence3,
  getLefts: () => getLefts2,
  getRights: () => getRights2,
  getSomes: () => getSomes2,
  has: () => has,
  intersection: () => intersection,
  isEmptyReadonlyRecord: () => isEmptyReadonlyRecord,
  isEmptyRecord: () => isEmptyRecord,
  isSubrecord: () => isSubrecord,
  isSubrecordBy: () => isSubrecordBy,
  keys: () => keys,
  map: () => map4,
  mapEntries: () => mapEntries,
  mapKeys: () => mapKeys,
  modify: () => modify,
  modifyOption: () => modifyOption,
  partition: () => partition,
  partitionMap: () => partitionMap,
  pop: () => pop,
  reduce: () => reduce2,
  remove: () => remove,
  replace: () => replace,
  replaceOption: () => replaceOption,
  separate: () => separate,
  set: () => set,
  singleton: () => singleton,
  size: () => size2,
  some: () => some4,
  toEntries: () => toEntries,
  union: () => union,
  values: () => values
});
var empty2 = () => ({});
var isEmptyRecord = (self) => keys(self).length === 0;
var isEmptyReadonlyRecord = isEmptyRecord;
var fromIterableWith = dual(2, (self, f) => {
  const out = empty2();
  for (const a of self) {
    const [k, b] = f(a);
    out[k] = b;
  }
  return out;
});
var fromIterableBy = (items, f) => fromIterableWith(items, (a) => [f(a), a]);
var fromEntries = Object.fromEntries;
var collect = dual(2, (self, f) => {
  const out = [];
  for (const key of keys(self)) {
    out.push(f(key, self[key]));
  }
  return out;
});
var toEntries = collect((key, value) => [key, value]);
var size2 = (self) => keys(self).length;
var has = dual(2, (self, key) => Object.prototype.hasOwnProperty.call(self, key));
var get = dual(2, (self, key) => has(self, key) ? some2(self[key]) : none2());
var modify = dual(3, (self, key, f) => {
  if (!has(self, key)) {
    return {
      ...self
    };
  }
  return {
    ...self,
    [key]: f(self[key])
  };
});
var modifyOption = dual(3, (self, key, f) => {
  if (!has(self, key)) {
    return none2();
  }
  return some2({
    ...self,
    [key]: f(self[key])
  });
});
var replaceOption = dual(3, (self, key, b) => modifyOption(self, key, () => b));
var remove = dual(2, (self, key) => {
  if (!has(self, key)) {
    return {
      ...self
    };
  }
  const out = {
    ...self
  };
  delete out[key];
  return out;
});
var pop = dual(2, (self, key) => has(self, key) ? some2([self[key], remove(self, key)]) : none2());
var map4 = dual(2, (self, f) => {
  const out = {
    ...self
  };
  for (const key of keys(self)) {
    out[key] = f(self[key], key);
  }
  return out;
});
var mapKeys = dual(2, (self, f) => {
  const out = {};
  for (const key of keys(self)) {
    const a = self[key];
    out[f(key, a)] = a;
  }
  return out;
});
var mapEntries = dual(2, (self, f) => {
  const out = {};
  for (const key of keys(self)) {
    const [k, b] = f(self[key], key);
    out[k] = b;
  }
  return out;
});
var filterMap2 = dual(2, (self, f) => {
  const out = empty2();
  for (const key of keys(self)) {
    const o = f(self[key], key);
    if (isSome(o)) {
      out[key] = o.value;
    }
  }
  return out;
});
var filter2 = dual(2, (self, predicate) => {
  const out = empty2();
  for (const key of keys(self)) {
    if (predicate(self[key], key)) {
      out[key] = self[key];
    }
  }
  return out;
});
var getSomes2 = filterMap2(identity);
var getLefts2 = (self) => {
  const out = empty2();
  for (const key of keys(self)) {
    const value = self[key];
    if (isLeft2(value)) {
      out[key] = value.left;
    }
  }
  return out;
};
var getRights2 = (self) => {
  const out = empty2();
  for (const key of keys(self)) {
    const value = self[key];
    if (isRight2(value)) {
      out[key] = value.right;
    }
  }
  return out;
};
var partitionMap = dual(2, (self, f) => {
  const left3 = empty2();
  const right3 = empty2();
  for (const key of keys(self)) {
    const e = f(self[key], key);
    if (isLeft2(e)) {
      left3[key] = e.left;
    } else {
      right3[key] = e.right;
    }
  }
  return [left3, right3];
});
var separate = partitionMap(identity);
var partition = dual(2, (self, predicate) => {
  const left3 = empty2();
  const right3 = empty2();
  for (const key of keys(self)) {
    if (predicate(self[key], key)) {
      right3[key] = self[key];
    } else {
      left3[key] = self[key];
    }
  }
  return [left3, right3];
});
var keys = (self) => Object.keys(self);
var values = (self) => collect(self, (_, a) => a);
var set = dual(3, (self, key, value) => {
  return {
    ...self,
    [key]: value
  };
});
var replace = dual(3, (self, key, value) => {
  if (has(self, key)) {
    return {
      ...self,
      [key]: value
    };
  }
  return {
    ...self
  };
});
var isSubrecordBy = (equivalence2) => dual(2, (self, that) => {
  for (const key of keys(self)) {
    if (!has(that, key) || !equivalence2(self[key], that[key])) {
      return false;
    }
  }
  return true;
});
var isSubrecord = isSubrecordBy(equivalence());
var reduce2 = dual(3, (self, zero, f) => {
  let out = zero;
  for (const key of keys(self)) {
    out = f(out, self[key], key);
  }
  return out;
});
var every = dual(2, (self, refinement) => {
  for (const key of keys(self)) {
    if (!refinement(self[key], key)) {
      return false;
    }
  }
  return true;
});
var some4 = dual(2, (self, predicate) => {
  for (const key of keys(self)) {
    if (predicate(self[key], key)) {
      return true;
    }
  }
  return false;
});
var union = dual(3, (self, that, combine) => {
  if (isEmptyRecord(self)) {
    return {
      ...that
    };
  }
  if (isEmptyRecord(that)) {
    return {
      ...self
    };
  }
  const out = empty2();
  for (const key of keys(self)) {
    if (has(that, key)) {
      out[key] = combine(self[key], that[key]);
    } else {
      out[key] = self[key];
    }
  }
  for (const key of keys(that)) {
    if (!has(out, key)) {
      out[key] = that[key];
    }
  }
  return out;
});
var intersection = dual(3, (self, that, combine) => {
  const out = empty2();
  if (isEmptyRecord(self) || isEmptyRecord(that)) {
    return out;
  }
  for (const key of keys(self)) {
    if (has(that, key)) {
      out[key] = combine(self[key], that[key]);
    }
  }
  return out;
});
var difference = dual(2, (self, that) => {
  if (isEmptyRecord(self)) {
    return {
      ...that
    };
  }
  if (isEmptyRecord(that)) {
    return {
      ...self
    };
  }
  const out = {};
  for (const key of keys(self)) {
    if (!has(that, key)) {
      out[key] = self[key];
    }
  }
  for (const key of keys(that)) {
    if (!has(self, key)) {
      out[key] = that[key];
    }
  }
  return out;
});
var getEquivalence3 = (equivalence2) => {
  const is = isSubrecordBy(equivalence2);
  return (self, that) => is(self, that) && is(that, self);
};
var singleton = (key, value) => ({
  [key]: value
});
var findFirst2 = dual(2, (self, f) => {
  const k = keys(self);
  for (let i = 0; i < k.length; i++) {
    const key = k[i];
    if (f(self[key], key)) {
      return some2([key, self[key]]);
    }
  }
  return none2();
});

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Array.js
var make3 = (...elements) => elements;
var allocate = (n) => new Array(n);
var makeBy2 = dual(2, (n, f) => {
  const max3 = Math.max(1, Math.floor(n));
  const out = new Array(max3);
  for (let i = 0; i < max3; i++) {
    out[i] = f(i);
  }
  return out;
});
var range2 = (start, end) => start <= end ? makeBy2(end - start + 1, (i) => start + i) : [start];
var replicate2 = dual(2, (a, n) => makeBy2(n, () => a));
var fromIterable = (collection) => Array.isArray(collection) ? collection : Array.from(collection);
var ensure = (self) => Array.isArray(self) ? self : [self];
var fromRecord2 = toEntries;
var fromOption3 = toArray;
var match2 = dual(2, (self, {
  onEmpty,
  onNonEmpty
}) => isNonEmptyReadonlyArray(self) ? onNonEmpty(self) : onEmpty());
var matchLeft = dual(2, (self, {
  onEmpty,
  onNonEmpty
}) => isNonEmptyReadonlyArray(self) ? onNonEmpty(headNonEmpty(self), tailNonEmpty(self)) : onEmpty());
var matchRight = dual(2, (self, {
  onEmpty,
  onNonEmpty
}) => isNonEmptyReadonlyArray(self) ? onNonEmpty(initNonEmpty(self), lastNonEmpty(self)) : onEmpty());
var prepend2 = dual(2, (self, head4) => [head4, ...self]);
var prependAll2 = dual(2, (self, that) => fromIterable(that).concat(fromIterable(self)));
var append2 = dual(2, (self, last3) => [...self, last3]);
var appendAll2 = dual(2, (self, that) => fromIterable(self).concat(fromIterable(that)));
var scan2 = dual(3, (self, b, f) => {
  const out = [b];
  let i = 0;
  for (const a of self) {
    out[i + 1] = f(out[i], a);
    i++;
  }
  return out;
});
var scanRight = dual(3, (self, b, f) => {
  const input = fromIterable(self);
  const out = new Array(input.length + 1);
  out[input.length] = b;
  for (let i = input.length - 1; i >= 0; i--) {
    out[i] = f(out[i + 1], input[i]);
  }
  return out;
});
var isArray = Array.isArray;
var isEmptyArray = (self) => self.length === 0;
var isEmptyReadonlyArray = isEmptyArray;
var isNonEmptyArray2 = isNonEmptyArray;
var isNonEmptyReadonlyArray = isNonEmptyArray;
var length = (self) => self.length;
var isOutOfBounds = (i, as) => i < 0 || i >= as.length;
var clamp = (i, as) => Math.floor(Math.min(Math.max(0, i), as.length));
var get2 = dual(2, (self, index) => {
  const i = Math.floor(index);
  return isOutOfBounds(i, self) ? none2() : some2(self[i]);
});
var unsafeGet = dual(2, (self, index) => {
  const i = Math.floor(index);
  if (isOutOfBounds(i, self)) {
    throw new Error(`Index ${i} out of bounds`);
  }
  return self[i];
});
var unprepend = (self) => [headNonEmpty(self), tailNonEmpty(self)];
var unappend = (self) => [initNonEmpty(self), lastNonEmpty(self)];
var head2 = get2(0);
var headNonEmpty = unsafeGet(0);
var last = (self) => isNonEmptyReadonlyArray(self) ? some2(lastNonEmpty(self)) : none2();
var lastNonEmpty = (self) => self[self.length - 1];
var tail = (self) => {
  const input = fromIterable(self);
  return isNonEmptyReadonlyArray(input) ? some2(tailNonEmpty(input)) : none2();
};
var tailNonEmpty = (self) => self.slice(1);
var init = (self) => {
  const input = fromIterable(self);
  return isNonEmptyReadonlyArray(input) ? some2(initNonEmpty(input)) : none2();
};
var initNonEmpty = (self) => self.slice(0, -1);
var take2 = dual(2, (self, n) => {
  const input = fromIterable(self);
  return input.slice(0, clamp(n, input));
});
var takeRight = dual(2, (self, n) => {
  const input = fromIterable(self);
  const i = clamp(n, input);
  return i === 0 ? [] : input.slice(-i);
});
var takeWhile2 = dual(2, (self, predicate) => {
  let i = 0;
  const out = [];
  for (const a of self) {
    if (!predicate(a, i)) {
      break;
    }
    out.push(a);
    i++;
  }
  return out;
});
var spanIndex = (self, predicate) => {
  let i = 0;
  for (const a of self) {
    if (!predicate(a, i)) {
      break;
    }
    i++;
  }
  return i;
};
var span = dual(2, (self, predicate) => splitAt(self, spanIndex(self, predicate)));
var drop2 = dual(2, (self, n) => {
  const input = fromIterable(self);
  return input.slice(clamp(n, input), input.length);
});
var dropRight = dual(2, (self, n) => {
  const input = fromIterable(self);
  return input.slice(0, input.length - clamp(n, input));
});
var dropWhile = dual(2, (self, predicate) => fromIterable(self).slice(spanIndex(self, predicate)));
var findFirstIndex = dual(2, (self, predicate) => {
  let i = 0;
  for (const a of self) {
    if (predicate(a, i)) {
      return some2(i);
    }
    i++;
  }
  return none2();
});
var findLastIndex = dual(2, (self, predicate) => {
  const input = fromIterable(self);
  for (let i = input.length - 1; i >= 0; i--) {
    if (predicate(input[i], i)) {
      return some2(i);
    }
  }
  return none2();
});
var findFirst3 = findFirst;
var findLast2 = dual(2, (self, f) => {
  const input = fromIterable(self);
  for (let i = input.length - 1; i >= 0; i--) {
    const a = input[i];
    const o = f(a, i);
    if (isBoolean(o)) {
      if (o) {
        return some2(a);
      }
    } else {
      if (isSome(o)) {
        return o;
      }
    }
  }
  return none2();
});
var findFirstWithIndex = dual(2, (self, f) => {
  let i = 0;
  for (const a of self) {
    const o = f(a, i);
    if (isBoolean(o)) {
      if (o) {
        return some2([a, i]);
      }
    } else {
      if (isSome(o)) {
        return some2([o.value, i]);
      }
    }
    i++;
  }
  return none2();
});
var countBy2 = dual(2, (self, f) => {
  let count = 0;
  const as = fromIterable(self);
  for (let i = 0; i < as.length; i++) {
    const a = as[i];
    if (f(a, i)) {
      count++;
    }
  }
  return count;
});
var insertAt = dual(3, (self, i, b) => {
  const out = Array.from(self);
  if (i < 0 || i > out.length) {
    return none2();
  }
  out.splice(i, 0, b);
  return some2(out);
});
var replace2 = dual(3, (self, i, b) => modify2(self, i, () => b));
var replaceOption2 = dual(3, (self, i, b) => modifyOption2(self, i, () => b));
var modify2 = dual(3, (self, i, f) => {
  const out = Array.from(self);
  if (isOutOfBounds(i, out)) {
    return out;
  }
  const b = f(out[i]);
  out[i] = b;
  return out;
});
var modifyOption2 = dual(3, (self, i, f) => {
  const arr = fromIterable(self);
  if (isOutOfBounds(i, arr)) {
    return none2();
  }
  const out = Array.isArray(self) ? self.slice() : arr;
  const b = f(arr[i]);
  out[i] = b;
  return some2(out);
});
var remove2 = dual(2, (self, i) => {
  const out = Array.from(self);
  if (isOutOfBounds(i, out)) {
    return out;
  }
  out.splice(i, 1);
  return out;
});
var removeOption = dual(2, (self, i) => {
  const arr = fromIterable(self);
  if (isOutOfBounds(i, arr)) {
    return none2();
  }
  const out = Array.isArray(self) ? self.slice() : arr;
  out.splice(i, 1);
  return some2(out);
});
var reverse = (self) => Array.from(self).reverse();
var sort = dual(2, (self, O) => {
  const out = Array.from(self);
  out.sort(O);
  return out;
});
var sortWith = dual(3, (self, f, order) => Array.from(self).map((a) => [a, f(a)]).sort(([, a], [, b]) => order(a, b)).map(([_]) => _));
var sortBy = (...orders) => {
  const sortByAll = sort(combineAll(orders));
  return (self) => {
    const input = fromIterable(self);
    if (isNonEmptyReadonlyArray(input)) {
      return sortByAll(input);
    }
    return [];
  };
};
var zip2 = dual(2, (self, that) => zipWith3(self, that, make2));
var zipWith3 = dual(3, (self, that, f) => {
  const as = fromIterable(self);
  const bs = fromIterable(that);
  if (isNonEmptyReadonlyArray(as) && isNonEmptyReadonlyArray(bs)) {
    const out = [f(headNonEmpty(as), headNonEmpty(bs))];
    const len = Math.min(as.length, bs.length);
    for (let i = 1; i < len; i++) {
      out[i] = f(as[i], bs[i]);
    }
    return out;
  }
  return [];
});
var unzip = (self) => {
  const input = fromIterable(self);
  if (isNonEmptyReadonlyArray(input)) {
    const fa = [input[0][0]];
    const fb = [input[0][1]];
    for (let i = 1; i < input.length; i++) {
      fa[i] = input[i][0];
      fb[i] = input[i][1];
    }
    return [fa, fb];
  }
  return [[], []];
};
var intersperse2 = dual(2, (self, middle) => {
  const input = fromIterable(self);
  if (isNonEmptyReadonlyArray(input)) {
    const out = [headNonEmpty(input)];
    const tail3 = tailNonEmpty(input);
    for (let i = 0; i < tail3.length; i++) {
      if (i < tail3.length) {
        out.push(middle);
      }
      out.push(tail3[i]);
    }
    return out;
  }
  return [];
});
var modifyNonEmptyHead = dual(2, (self, f) => [f(headNonEmpty(self)), ...tailNonEmpty(self)]);
var setNonEmptyHead = dual(2, (self, b) => modifyNonEmptyHead(self, () => b));
var modifyNonEmptyLast = dual(2, (self, f) => append2(initNonEmpty(self), f(lastNonEmpty(self))));
var setNonEmptyLast = dual(2, (self, b) => modifyNonEmptyLast(self, () => b));
var rotate = dual(2, (self, n) => {
  const input = fromIterable(self);
  if (isNonEmptyReadonlyArray(input)) {
    const len = input.length;
    const m = Math.round(n) % len;
    if (isOutOfBounds(Math.abs(m), input) || m === 0) {
      return copy(input);
    }
    if (m < 0) {
      const [f, s] = splitNonEmptyAt(input, -m);
      return appendAll2(s, f);
    } else {
      return rotate(self, m - len);
    }
  }
  return [];
});
var containsWith2 = (isEquivalent) => dual(2, (self, a) => {
  for (const i of self) {
    if (isEquivalent(a, i)) {
      return true;
    }
  }
  return false;
});
var _equivalence2 = equivalence();
var contains2 = containsWith2(_equivalence2);
var chop = dual(2, (self, f) => {
  const input = fromIterable(self);
  if (isNonEmptyReadonlyArray(input)) {
    const [b, rest] = f(input);
    const out = [b];
    let next = rest;
    while (isNonEmptyArray(next)) {
      const [b2, rest2] = f(next);
      out.push(b2);
      next = rest2;
    }
    return out;
  }
  return [];
});
var splitAt = dual(2, (self, n) => {
  const input = Array.from(self);
  const _n = Math.floor(n);
  if (isNonEmptyReadonlyArray(input)) {
    if (_n >= 1) {
      return splitNonEmptyAt(input, _n);
    }
    return [[], input];
  }
  return [input, []];
});
var splitNonEmptyAt = dual(2, (self, n) => {
  const _n = Math.max(1, Math.floor(n));
  return _n >= self.length ? [copy(self), []] : [prepend2(self.slice(1, _n), headNonEmpty(self)), self.slice(_n)];
});
var split = dual(2, (self, n) => {
  const input = fromIterable(self);
  return chunksOf2(input, Math.ceil(input.length / Math.floor(n)));
});
var splitWhere = dual(2, (self, predicate) => span(self, (a, i) => !predicate(a, i)));
var copy = (self) => self.slice();
var pad = dual(3, (self, n, fill) => {
  if (self.length >= n) {
    return take2(self, n);
  }
  return appendAll2(self, makeBy2(n - self.length, () => fill));
});
var chunksOf2 = dual(2, (self, n) => {
  const input = fromIterable(self);
  if (isNonEmptyReadonlyArray(input)) {
    return chop(input, splitNonEmptyAt(n));
  }
  return [];
});
var window = dual(2, (self, n) => {
  const input = fromIterable(self);
  if (n > 0 && isNonEmptyReadonlyArray(input)) {
    return Array.from({
      length: input.length - (n - 1)
    }, (_, index) => input.slice(index, index + n));
  }
  return [];
});
var groupWith2 = dual(2, (self, isEquivalent) => chop(self, (as) => {
  const h = headNonEmpty(as);
  const out = [h];
  let i = 1;
  for (; i < as.length; i++) {
    const a = as[i];
    if (isEquivalent(a, h)) {
      out.push(a);
    } else {
      break;
    }
  }
  return [out, as.slice(i)];
}));
var group2 = groupWith2(equivalence());
var groupBy2 = dual(2, (self, f) => {
  const out = {};
  for (const a of self) {
    const k = f(a);
    if (Object.prototype.hasOwnProperty.call(out, k)) {
      out[k].push(a);
    } else {
      out[k] = [a];
    }
  }
  return out;
});
var unionWith = dual(3, (self, that, isEquivalent) => {
  const a = fromIterable(self);
  const b = fromIterable(that);
  if (isNonEmptyReadonlyArray(a)) {
    if (isNonEmptyReadonlyArray(b)) {
      const dedupe3 = dedupeWith(isEquivalent);
      return dedupe3(appendAll2(a, b));
    }
    return a;
  }
  return b;
});
var union2 = dual(2, (self, that) => unionWith(self, that, _equivalence2));
var intersectionWith = (isEquivalent) => {
  const has2 = containsWith2(isEquivalent);
  return dual(2, (self, that) => {
    const bs = fromIterable(that);
    return fromIterable(self).filter((a) => has2(bs, a));
  });
};
var intersection2 = intersectionWith(_equivalence2);
var differenceWith = (isEquivalent) => {
  const has2 = containsWith2(isEquivalent);
  return dual(2, (self, that) => {
    const bs = fromIterable(that);
    return fromIterable(self).filter((a) => !has2(bs, a));
  });
};
var difference2 = differenceWith(_equivalence2);
var empty3 = () => [];
var of2 = (a) => [a];
var map5 = dual(2, (self, f) => self.map(f));
var flatMap3 = dual(2, (self, f) => {
  if (isEmptyReadonlyArray(self)) {
    return [];
  }
  const out = [];
  for (let i = 0; i < self.length; i++) {
    const inner = f(self[i], i);
    for (let j = 0; j < inner.length; j++) {
      out.push(inner[j]);
    }
  }
  return out;
});
var flatten2 = flatMap3(identity);
var filterMap3 = dual(2, (self, f) => {
  const as = fromIterable(self);
  const out = [];
  for (let i = 0; i < as.length; i++) {
    const o = f(as[i], i);
    if (isSome(o)) {
      out.push(o.value);
    }
  }
  return out;
});
var filterMapWhile2 = dual(2, (self, f) => {
  let i = 0;
  const out = [];
  for (const a of self) {
    const b = f(a, i);
    if (isSome(b)) {
      out.push(b.value);
    } else {
      break;
    }
    i++;
  }
  return out;
});
var partitionMap2 = dual(2, (self, f) => {
  const left3 = [];
  const right3 = [];
  const as = fromIterable(self);
  for (let i = 0; i < as.length; i++) {
    const e = f(as[i], i);
    if (isLeft2(e)) {
      left3.push(e.left);
    } else {
      right3.push(e.right);
    }
  }
  return [left3, right3];
});
var getSomes3 = filterMap3(identity);
var getLefts3 = (self) => {
  const out = [];
  for (const a of self) {
    if (isLeft2(a)) {
      out.push(a.left);
    }
  }
  return out;
};
var getRights3 = (self) => {
  const out = [];
  for (const a of self) {
    if (isRight2(a)) {
      out.push(a.right);
    }
  }
  return out;
};
var filter3 = dual(2, (self, predicate) => {
  const as = fromIterable(self);
  const out = [];
  for (let i = 0; i < as.length; i++) {
    if (predicate(as[i], i)) {
      out.push(as[i]);
    }
  }
  return out;
});
var partition2 = dual(2, (self, predicate) => {
  const left3 = [];
  const right3 = [];
  const as = fromIterable(self);
  for (let i = 0; i < as.length; i++) {
    if (predicate(as[i], i)) {
      right3.push(as[i]);
    } else {
      left3.push(as[i]);
    }
  }
  return [left3, right3];
});
var separate2 = partitionMap2(identity);
var reduce3 = dual(3, (self, b, f) => fromIterable(self).reduce((b2, a, i) => f(b2, a, i), b));
var reduceRight = dual(3, (self, b, f) => fromIterable(self).reduceRight((b2, a, i) => f(b2, a, i), b));
var liftPredicate2 = (predicate) => (b) => predicate(b) ? [b] : [];
var liftOption = (f) => (...a) => fromOption3(f(...a));
var fromNullable2 = (a) => a == null ? empty3() : [a];
var liftNullable = (f) => (...a) => fromNullable2(f(...a));
var flatMapNullable2 = dual(2, (self, f) => flatMap3(self, (a) => fromNullable2(f(a))));
var liftEither = (f) => (...a) => {
  const e = f(...a);
  return isLeft2(e) ? [] : [e.right];
};
var every2 = dual(2, (self, refinement) => self.every(refinement));
var some5 = dual(2, (self, predicate) => self.some(predicate));
var extend = dual(2, (self, f) => self.map((_, i, as) => f(as.slice(i))));
var min2 = dual(2, (self, O) => self.reduce(min(O)));
var max2 = dual(2, (self, O) => self.reduce(max(O)));
var unfold2 = (b, f) => {
  const out = [];
  let next = b;
  let o;
  while (isSome(o = f(next))) {
    const [a, b2] = o.value;
    out.push(a);
    next = b2;
  }
  return out;
};
var getOrder2 = array3;
var getEquivalence4 = array;
var forEach2 = dual(2, (self, f) => fromIterable(self).forEach((a, i) => f(a, i)));
var dedupeWith = dual(2, (self, isEquivalent) => {
  const input = fromIterable(self);
  if (isNonEmptyReadonlyArray(input)) {
    const out = [headNonEmpty(input)];
    const rest = tailNonEmpty(input);
    for (const r of rest) {
      if (out.every((a) => !isEquivalent(r, a))) {
        out.push(r);
      }
    }
    return out;
  }
  return [];
});
var dedupe = (self) => dedupeWith(self, equivalence());
var dedupeAdjacentWith2 = dual(2, (self, isEquivalent) => {
  const out = [];
  let lastA = none2();
  for (const a of self) {
    if (isNone2(lastA) || !isEquivalent(a, lastA.value)) {
      out.push(a);
      lastA = some2(a);
    }
  }
  return out;
});
var dedupeAdjacent2 = dedupeAdjacentWith2(equivalence());
var join = dual(2, (self, sep) => fromIterable(self).join(sep));
var mapAccum = dual(3, (self, s, f) => {
  let i = 0;
  let s1 = s;
  const out = [];
  for (const a of self) {
    const r = f(s1, a, i);
    s1 = r[0];
    out.push(r[1]);
    i++;
  }
  return [s1, out];
});
var cartesianWith2 = dual(3, (self, that, f) => flatMap3(self, (a) => map5(that, (b) => f(a, b))));
var cartesian2 = dual(2, (self, that) => cartesianWith2(self, that, (a, b) => [a, b]));
var Do2 = of2({});
var bind3 = bind(map5, flatMap3);
var bindTo3 = bindTo(map5);
var let_3 = let_(map5);

// ../../node_modules/.bun/effect@3.19.19/node_modules/effect/dist/esm/Chunk.js
var TypeId3 = Symbol.for("effect/Chunk");
function copy2(src, srcPos, dest, destPos, len) {
  for (let i = srcPos; i < Math.min(src.length, srcPos + len); i++) {
    dest[destPos + i - srcPos] = src[i];
  }
  return dest;
}
var emptyArray = [];
var getEquivalence5 = (isEquivalent) => make((self, that) => self.length === that.length && toReadonlyArray(self).every((value, i) => isEquivalent(value, unsafeGet2(that, i))));
var _equivalence3 = getEquivalence5(equals);
var ChunkProto = {
  [TypeId3]: {
    _A: (_) => _
  },
  toString() {
    return format(this.toJSON());
  },
  toJSON() {
    return {
      _id: "Chunk",
      values: toReadonlyArray(this).map(toJSON)
    };
  },
  [NodeInspectSymbol]() {
    return this.toJSON();
  },
  [symbol2](that) {
    return isChunk(that) && _equivalence3(this, that);
  },
  [symbol]() {
    return cached(this, array2(toReadonlyArray(this)));
  },
  [Symbol.iterator]() {
    switch (this.backing._tag) {
      case "IArray": {
        return this.backing.array[Symbol.iterator]();
      }
      case "IEmpty": {
        return emptyArray[Symbol.iterator]();
      }
      default: {
        return toReadonlyArray(this)[Symbol.iterator]();
      }
    }
  },
  pipe() {
    return pipeArguments(this, arguments);
  }
};
var makeChunk = (backing) => {
  const chunk = Object.create(ChunkProto);
  chunk.backing = backing;
  switch (backing._tag) {
    case "IEmpty": {
      chunk.length = 0;
      chunk.depth = 0;
      chunk.left = chunk;
      chunk.right = chunk;
      break;
    }
    case "IConcat": {
      chunk.length = backing.left.length + backing.right.length;
      chunk.depth = 1 + Math.max(backing.left.depth, backing.right.depth);
      chunk.left = backing.left;
      chunk.right = backing.right;
      break;
    }
    case "IArray": {
      chunk.length = backing.array.length;
      chunk.depth = 0;
      chunk.left = _empty;
      chunk.right = _empty;
      break;
    }
    case "ISingleton": {
      chunk.length = 1;
      chunk.depth = 0;
      chunk.left = _empty;
      chunk.right = _empty;
      break;
    }
    case "ISlice": {
      chunk.length = backing.length;
      chunk.depth = backing.chunk.depth + 1;
      chunk.left = _empty;
      chunk.right = _empty;
      break;
    }
  }
  return chunk;
};
var isChunk = (u) => hasProperty(u, TypeId3);
var _empty = makeChunk({
  _tag: "IEmpty"
});
var empty4 = () => _empty;
var make4 = (...as) => unsafeFromNonEmptyArray(as);
var of3 = (a) => makeChunk({
  _tag: "ISingleton",
  a
});
var fromIterable2 = (self) => isChunk(self) ? self : unsafeFromArray(fromIterable(self));
var copyToArray = (self, array4, initial) => {
  switch (self.backing._tag) {
    case "IArray": {
      copy2(self.backing.array, 0, array4, initial, self.length);
      break;
    }
    case "IConcat": {
      copyToArray(self.left, array4, initial);
      copyToArray(self.right, array4, initial + self.left.length);
      break;
    }
    case "ISingleton": {
      array4[initial] = self.backing.a;
      break;
    }
    case "ISlice": {
      let i = 0;
      let j = initial;
      while (i < self.length) {
        array4[j] = unsafeGet2(self, i);
        i += 1;
        j += 1;
      }
      break;
    }
  }
};
var toArray_ = (self) => toReadonlyArray(self).slice();
var toArray2 = toArray_;
var toReadonlyArray_ = (self) => {
  switch (self.backing._tag) {
    case "IEmpty": {
      return emptyArray;
    }
    case "IArray": {
      return self.backing.array;
    }
    default: {
      const arr = new Array(self.length);
      copyToArray(self, arr, 0);
      self.backing = {
        _tag: "IArray",
        array: arr
      };
      self.left = _empty;
      self.right = _empty;
      self.depth = 0;
      return arr;
    }
  }
};
var toReadonlyArray = toReadonlyArray_;
var reverseChunk = (self) => {
  switch (self.backing._tag) {
    case "IEmpty":
    case "ISingleton":
      return self;
    case "IArray": {
      return makeChunk({
        _tag: "IArray",
        array: reverse(self.backing.array)
      });
    }
    case "IConcat": {
      return makeChunk({
        _tag: "IConcat",
        left: reverse2(self.backing.right),
        right: reverse2(self.backing.left)
      });
    }
    case "ISlice":
      return unsafeFromArray(reverse(toReadonlyArray(self)));
  }
};
var reverse2 = reverseChunk;
var get3 = dual(2, (self, index) => index < 0 || index >= self.length ? none2() : some2(unsafeGet2(self, index)));
var unsafeFromArray = (self) => self.length === 0 ? empty4() : self.length === 1 ? of3(self[0]) : makeChunk({
  _tag: "IArray",
  array: self
});
var unsafeFromNonEmptyArray = (self) => unsafeFromArray(self);
var unsafeGet2 = dual(2, (self, index) => {
  switch (self.backing._tag) {
    case "IEmpty": {
      throw new Error(`Index out of bounds`);
    }
    case "ISingleton": {
      if (index !== 0) {
        throw new Error(`Index out of bounds`);
      }
      return self.backing.a;
    }
    case "IArray": {
      if (index >= self.length || index < 0) {
        throw new Error(`Index out of bounds`);
      }
      return self.backing.array[index];
    }
    case "IConcat": {
      return index < self.left.length ? unsafeGet2(self.left, index) : unsafeGet2(self.right, index - self.left.length);
    }
    case "ISlice": {
      return unsafeGet2(self.backing.chunk, index + self.backing.offset);
    }
  }
});
var append3 = dual(2, (self, a) => appendAll3(self, of3(a)));
var prepend3 = dual(2, (self, elem) => appendAll3(of3(elem), self));
var take3 = dual(2, (self, n) => {
  if (n <= 0) {
    return _empty;
  } else if (n >= self.length) {
    return self;
  } else {
    switch (self.backing._tag) {
      case "ISlice": {
        return makeChunk({
          _tag: "ISlice",
          chunk: self.backing.chunk,
          length: n,
          offset: self.backing.offset
        });
      }
      case "IConcat": {
        if (n > self.left.length) {
          return makeChunk({
            _tag: "IConcat",
            left: self.left,
            right: take3(self.right, n - self.left.length)
          });
        }
        return take3(self.left, n);
      }
      default: {
        return makeChunk({
          _tag: "ISlice",
          chunk: self,
          offset: 0,
          length: n
        });
      }
    }
  }
});
var drop3 = dual(2, (self, n) => {
  if (n <= 0) {
    return self;
  } else if (n >= self.length) {
    return _empty;
  } else {
    switch (self.backing._tag) {
      case "ISlice": {
        return makeChunk({
          _tag: "ISlice",
          chunk: self.backing.chunk,
          offset: self.backing.offset + n,
          length: self.backing.length - n
        });
      }
      case "IConcat": {
        if (n > self.left.length) {
          return drop3(self.right, n - self.left.length);
        }
        return makeChunk({
          _tag: "IConcat",
          left: drop3(self.left, n),
          right: self.right
        });
      }
      default: {
        return makeChunk({
          _tag: "ISlice",
          chunk: self,
          offset: n,
          length: self.length - n
        });
      }
    }
  }
});
var dropRight2 = dual(2, (self, n) => take3(self, Math.max(0, self.length - n)));
var dropWhile2 = dual(2, (self, predicate) => {
  const arr = toReadonlyArray(self);
  const len = arr.length;
  let i = 0;
  while (i < len && predicate(arr[i])) {
    i++;
  }
  return drop3(self, i);
});
var prependAll3 = dual(2, (self, that) => appendAll3(that, self));
var appendAll3 = dual(2, (self, that) => {
  if (self.backing._tag === "IEmpty") {
    return that;
  }
  if (that.backing._tag === "IEmpty") {
    return self;
  }
  const diff = that.depth - self.depth;
  if (Math.abs(diff) <= 1) {
    return makeChunk({
      _tag: "IConcat",
      left: self,
      right: that
    });
  } else if (diff < -1) {
    if (self.left.depth >= self.right.depth) {
      const nr = appendAll3(self.right, that);
      return makeChunk({
        _tag: "IConcat",
        left: self.left,
        right: nr
      });
    } else {
      const nrr = appendAll3(self.right.right, that);
      if (nrr.depth === self.depth - 3) {
        const nr = makeChunk({
          _tag: "IConcat",
          left: self.right.left,
          right: nrr
        });
        return makeChunk({
          _tag: "IConcat",
          left: self.left,
          right: nr
        });
      } else {
        const nl = makeChunk({
          _tag: "IConcat",
          left: self.left,
          right: self.right.left
        });
        return makeChunk({
          _tag: "IConcat",
          left: nl,
          right: nrr
        });
      }
    }
  } else {
    if (that.right.depth >= that.left.depth) {
      const nl = appendAll3(self, that.left);
      return makeChunk({
        _tag: "IConcat",
        left: nl,
        right: that.right
      });
    } else {
      const nll = appendAll3(self, that.left.left);
      if (nll.depth === that.depth - 3) {
        const nl = makeChunk({
          _tag: "IConcat",
          left: nll,
          right: that.left.right
        });
        return makeChunk({
          _tag: "IConcat",
          left: nl,
          right: that.right
        });
      } else {
        const nr = makeChunk({
          _tag: "IConcat",
          left: that.left.right,
          right: that.right
        });
        return makeChunk({
          _tag: "IConcat",
          left: nll,
          right: nr
        });
      }
    }
  }
});
var filterMap4 = dual(2, (self, f) => unsafeFromArray(filterMap3(self, f)));
var filter4 = dual(2, (self, predicate) => unsafeFromArray(filter3(self, predicate)));
var filterMapWhile3 = dual(2, (self, f) => unsafeFromArray(filterMapWhile2(self, f)));
var compact = (self) => filterMap4(self, identity);
var flatMap4 = dual(2, (self, f) => {
  if (self.backing._tag === "ISingleton") {
    return f(self.backing.a, 0);
  }
  let out = _empty;
  let i = 0;
  for (const k of self) {
    out = appendAll3(out, f(k, i++));
  }
  return out;
});
var forEach3 = dual(2, (self, f) => toReadonlyArray(self).forEach(f));
var flatten3 = flatMap4(identity);
var chunksOf3 = dual(2, (self, n) => {
  const gr = [];
  let current = [];
  toReadonlyArray(self).forEach((a) => {
    current.push(a);
    if (current.length >= n) {
      gr.push(unsafeFromArray(current));
      current = [];
    }
  });
  if (current.length > 0) {
    gr.push(unsafeFromArray(current));
  }
  return unsafeFromArray(gr);
});
var intersection3 = dual(2, (self, that) => unsafeFromArray(intersection2(toReadonlyArray(self), toReadonlyArray(that))));
var isEmpty2 = (self) => self.length === 0;
var isNonEmpty = (self) => self.length > 0;
var head3 = get3(0);
var unsafeHead2 = (self) => unsafeGet2(self, 0);
var headNonEmpty2 = unsafeHead2;
var last2 = (self) => get3(self, self.length - 1);
var unsafeLast = (self) => unsafeGet2(self, self.length - 1);
var lastNonEmpty2 = unsafeLast;
var map6 = dual(2, (self, f) => self.backing._tag === "ISingleton" ? of3(f(self.backing.a, 0)) : unsafeFromArray(pipe(toReadonlyArray(self), map5((a, i) => f(a, i)))));
var mapAccum2 = dual(3, (self, s, f) => {
  const [s1, as] = mapAccum(self, s, f);
  return [s1, unsafeFromArray(as)];
});
var partition3 = dual(2, (self, predicate) => pipe(partition2(toReadonlyArray(self), predicate), ([l, r]) => [unsafeFromArray(l), unsafeFromArray(r)]));
var partitionMap3 = dual(2, (self, f) => pipe(partitionMap2(toReadonlyArray(self), f), ([l, r]) => [unsafeFromArray(l), unsafeFromArray(r)]));
var separate3 = (self) => pipe(separate2(toReadonlyArray(self)), ([l, r]) => [unsafeFromArray(l), unsafeFromArray(r)]);
var size3 = (self) => self.length;
var sort2 = dual(2, (self, O) => unsafeFromArray(sort(toReadonlyArray(self), O)));
var sortWith2 = dual(3, (self, f, order) => sort2(self, mapInput(order, f)));
var splitAt2 = dual(2, (self, n) => [take3(self, n), drop3(self, n)]);
var splitNonEmptyAt2 = dual(2, (self, n) => {
  const _n = Math.max(1, Math.floor(n));
  return _n >= self.length ? [self, empty4()] : [take3(self, _n), drop3(self, _n)];
});
var split2 = dual(2, (self, n) => chunksOf3(self, Math.ceil(self.length / Math.floor(n))));
var splitWhere2 = dual(2, (self, predicate) => {
  let i = 0;
  for (const a of toReadonlyArray(self)) {
    if (predicate(a)) {
      break;
    } else {
      i++;
    }
  }
  return splitAt2(self, i);
});
var tail2 = (self) => self.length > 0 ? some2(drop3(self, 1)) : none2();
var tailNonEmpty2 = (self) => drop3(self, 1);
var takeRight2 = dual(2, (self, n) => drop3(self, self.length - n));
var takeWhile3 = dual(2, (self, predicate) => {
  const out = [];
  for (const a of toReadonlyArray(self)) {
    if (predicate(a)) {
      out.push(a);
    } else {
      break;
    }
  }
  return unsafeFromArray(out);
});
var union3 = dual(2, (self, that) => unsafeFromArray(union2(toReadonlyArray(self), toReadonlyArray(that))));
var dedupe2 = (self) => unsafeFromArray(dedupe(toReadonlyArray(self)));
var dedupeAdjacent3 = (self) => unsafeFromArray(dedupeAdjacent2(self));
var unzip2 = (self) => {
  const [left3, right3] = unzip(self);
  return [unsafeFromArray(left3), unsafeFromArray(right3)];
};
var zipWith4 = dual(3, (self, that, f) => unsafeFromArray(zipWith3(self, that, f)));
var zip3 = dual(2, (self, that) => zipWith4(self, that, (a, b) => [a, b]));
var remove3 = dual(2, (self, i) => {
  if (i < 0 || i >= self.length) return self;
  return unsafeFromArray(remove2(toReadonlyArray(self), i));
});
var removeOption2 = dual(2, (self, i) => {
  if (i < 0 || i >= self.length) return none2();
  return some2(unsafeFromArray(remove2(toReadonlyArray(self), i)));
});
var modifyOption3 = dual(3, (self, i, f) => {
  if (i < 0 || i >= self.length) return none2();
  return some2(unsafeFromArray(modify2(toReadonlyArray(self), i, f)));
});
var modify3 = dual(3, (self, i, f) => getOrElse(modifyOption3(self, i, f), () => self));
var replace3 = dual(3, (self, i, b) => modify3(self, i, () => b));
var replaceOption3 = dual(3, (self, i, b) => modifyOption3(self, i, () => b));
var makeBy3 = dual(2, (n, f) => fromIterable2(makeBy2(n, f)));
var range3 = (start, end) => start <= end ? makeBy3(end - start + 1, (i) => start + i) : of3(start);
var contains3 = contains2;
var containsWith3 = containsWith2;
var findFirst4 = findFirst3;
var findFirstIndex2 = findFirstIndex;
var findLast3 = findLast2;
var findLastIndex2 = findLastIndex;
var every3 = dual(2, (self, refinement) => fromIterable(self).every(refinement));
var some6 = dual(2, (self, predicate) => fromIterable(self).some(predicate));
var join2 = join;
var reduce4 = reduce3;
var reduceRight2 = reduceRight;
var differenceWith2 = (isEquivalent) => {
  return dual(2, (self, that) => unsafeFromArray(differenceWith(isEquivalent)(that, self)));
};
var difference3 = dual(2, (self, that) => unsafeFromArray(difference2(that, self)));

export {
  TypeId2 as TypeId,
  right2 as right,
  left2 as left,
  fromOption2 as fromOption,
  try_,
  isEither2 as isEither,
  isLeft2 as isLeft,
  isRight2 as isRight,
  getRight2 as getRight,
  getEquivalence,
  mapBoth,
  mapLeft,
  map,
  match,
  liftPredicate,
  merge,
  getOrThrowWith,
  getOrThrow,
  all,
  Either_exports,
  isNonEmptyArray,
  make2 as make,
  Tuple_exports,
  head,
  unsafeHead,
  empty,
  map3 as map2,
  filter,
  reduce,
  Iterable_exports,
  empty2,
  isEmptyRecord,
  has,
  get,
  remove,
  map4 as map3,
  set,
  getEquivalence3 as getEquivalence2,
  Record_exports,
  make3 as make2,
  allocate,
  makeBy2 as makeBy,
  fromIterable,
  ensure,
  match2,
  matchLeft,
  prepend2 as prepend,
  append2 as append,
  appendAll2 as appendAll,
  isArray,
  isEmptyReadonlyArray,
  isNonEmptyArray2,
  isNonEmptyReadonlyArray,
  unsafeGet,
  head2,
  headNonEmpty,
  last,
  lastNonEmpty,
  tailNonEmpty,
  span,
  drop2 as drop,
  findFirst3 as findFirst,
  findLast2 as findLast,
  reverse,
  sort,
  zip2 as zip,
  splitAt,
  copy,
  union2 as union,
  intersectionWith,
  intersection2 as intersection,
  empty3,
  of2 as of,
  map5 as map4,
  flatMap3 as flatMap,
  flatten2 as flatten,
  filterMap3 as filterMap,
  partitionMap2 as partitionMap,
  getSomes3 as getSomes,
  filter3 as filter2,
  reduce3 as reduce2,
  reduceRight,
  every2 as every,
  unfold2 as unfold,
  getEquivalence4 as getEquivalence3,
  dedupe,
  join,
  Array_exports,
  getEquivalence5 as getEquivalence4,
  isChunk,
  empty4,
  make4 as make3,
  of3 as of2,
  fromIterable2,
  toArray2 as toArray,
  toReadonlyArray,
  reverse2,
  get3 as get2,
  unsafeFromArray,
  unsafeFromNonEmptyArray,
  unsafeGet2,
  append3 as append2,
  prepend3 as prepend2,
  take3 as take,
  drop3 as drop2,
  dropRight2 as dropRight,
  dropWhile2 as dropWhile,
  prependAll3 as prependAll,
  appendAll3 as appendAll2,
  filterMap4 as filterMap2,
  filter4 as filter3,
  filterMapWhile3 as filterMapWhile,
  compact,
  flatMap4 as flatMap2,
  forEach3 as forEach,
  flatten3 as flatten2,
  chunksOf3 as chunksOf,
  intersection3 as intersection2,
  isEmpty2 as isEmpty,
  isNonEmpty,
  head3,
  unsafeHead2,
  headNonEmpty2,
  last2,
  unsafeLast,
  lastNonEmpty2,
  map6 as map5,
  mapAccum2 as mapAccum,
  partition3 as partition,
  partitionMap3 as partitionMap2,
  separate3 as separate,
  size3 as size,
  sort2,
  sortWith2 as sortWith,
  splitAt2,
  splitNonEmptyAt2 as splitNonEmptyAt,
  split2 as split,
  splitWhere2 as splitWhere,
  tail2 as tail,
  tailNonEmpty2,
  takeRight2 as takeRight,
  takeWhile3 as takeWhile,
  union3 as union2,
  dedupe2,
  dedupeAdjacent3 as dedupeAdjacent,
  unzip2 as unzip,
  zipWith4 as zipWith,
  zip3 as zip2,
  remove3 as remove2,
  removeOption2 as removeOption,
  modifyOption3 as modifyOption,
  modify3 as modify,
  replace3 as replace,
  replaceOption3 as replaceOption,
  makeBy3 as makeBy2,
  range3 as range,
  contains3 as contains,
  containsWith3 as containsWith,
  findFirst4 as findFirst2,
  findFirstIndex2 as findFirstIndex,
  findLast3 as findLast2,
  findLastIndex2 as findLastIndex,
  every3 as every2,
  some6 as some,
  join2,
  reduce4 as reduce3,
  reduceRight2,
  differenceWith2 as differenceWith,
  difference3 as difference,
  Chunk_exports
};
//# sourceMappingURL=chunk-JDLTRRA5.js.map
