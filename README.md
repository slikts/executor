# Executor v2 Scaffold

This directory is a fresh monorepo scaffold for the Executor v2 architecture.

## Included

- Workspace root `package.json` and `tsconfig.json`
- App and package folders with `src/`, `package.json`, `tsconfig.json`, and `README.md`
- No implementation files yet
